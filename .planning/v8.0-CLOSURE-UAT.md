---
title: v8.0 Closure — Final UAT Checklist
status: in-progress
created: 2026-05-20
owner: operator (Nick)
target_milestone_audit: /gsd-audit-milestone v8.0
---

# v8.0 — Final UAT to close the milestone

All shipped phases have green code-side verification. What remains is **observation of the running system** — visual checks in the dashboard and one live-traffic smoke. No code changes expected.

This is the ordered checklist. Each item is independent — you can do them in any order, but the order below minimises context-switching (one Zap session, one browser session, one inbox session).

---

## Block A — Zapier session (Phase 82.9 Plan 01 Task 3)

### A-1. Extend the `nxt.candidate_details` Zap

- [ ] Open Zap on Catch Hook `https://hooks.zapier.com/hooks/catch/15380147/uv5ju6h/`
- [ ] Extend the SQL step to JOIN contact-persons + invoices and project:
  - `contact_person` (string \| null — primary contact name)
  - `recent_invoices` (string[], `ORDER BY invoice_date DESC LIMIT 5`)
- [ ] Replay the Zap with a recent acceptance NXT `customer_id` (pick one from any recent Stage 2 `sender_match` row)
- [ ] Confirm response includes both new fields and `recent_invoices.length <= 5`
- [ ] (Optional A1 fallback) If `contact_person` isn't reachable from the SQL connection, send `contact_person: null` — the schema accepts it

**Prod Supabase migration:** ✅ already applied 2026-05-20 04:46 UTC. Registry row `nxt.candidate_details` declares both fields. No further DB action.

---

## Block B — Browser session (Phases 82.9 + 999.8 visual UATs)

Boot the app (acceptance env) and walk through these screens.

### B-1. Stage 2 audit panel renders new evidence (Phase 82.9 Plan 04 Task 3)

Open Bulk Review → click into a recent email that went through Stage 2 → open the Stage 2 evidence panel.

Verify for each resolver path you can find a sample of:

- [ ] **`regex_subject_match`** — INPUTS shows the matched subject snippet + invoice pattern; CANDIDATES has the resolved customer
- [ ] **`sender_match`** — INPUTS shows `sender_email`; CANDIDATES shows single hit with `id + name + contact_person + recent_invoices` (post-Zap-edit) or "No contact recorded" / empty invoices (pre-Zap-edit)
- [ ] **`identifier_match`** — INPUTS shows `matched_identifiers: [...]`; CANDIDATES shows matched customer(s)
- [ ] **`llm_tiebreaker`** — INPUTS shows candidate IDs considered; **REASONING** section visible with the LLM's `reason` string; CANDIDATES shows the full shortlist
- [ ] **`unresolved`** — INPUTS shows empty / "no candidates"; CANDIDATES empty
- [ ] **Legacy rows** (pre-82.9 emails, `payload.inputs == null`) — render chip-only with the muted line **"Legacy run — limited evidence captured"**
- [ ] SCREENSHOTS section + raw JSON toggle still work (unchanged)

If any of the above misrenders → screenshot + comment in the audit panel; we'll patch.

### B-2. Stage 1 predictor/confidence chip strip (Phase 999.8 follow-up smoke)

Navigate to `/automations/debtor-email/stage-1`.

- [ ] **Predictor chip row** (All | regex | LLM) renders below the noise-category strip
- [ ] **Confidence chip row** (All | high | medium | low) renders below the predictor row
- [ ] Clicking a chip updates the URL: `?predictor=llm_2nd_pass`, `?confidence=medium`, etc., and round-trips on reload
- [ ] `?predictor=garbage&confidence=garbage` silently drops to "All" (server-side allowlist)
- [ ] Per-row chip on each predicted row reads either `LLM · medium` / `LLM · high` / `LLM · low` or `regex`, or **nothing** for pre-cutover rows (no layout drift)
- [ ] Visit `/automations/debtor-email/stage-1?sub=pending` → **the predictor/confidence strip does NOT render** (candidate-rule list unaffected)

---

## Block C — Inbox / live traffic (Phase 999.8 end-to-end smoke)

### C-1. Therese-Hendriks-shaped email regression smoke

Send a fresh test email into the debtor inbox that mimics the original regression: an invoice-correction request with a `FW: Invoice ####` subject. Then verify in the dashboard + DB:

- [ ] **No Outlook side-effect** — the message is NOT auto-categorised/archived
- [ ] In `automation_runs`, the row has `status = 'predicted'` (not `auto_actioned`)
- [ ] In `pipeline_events.decision_details` for the Stage 1 row: `predictor = 'llm_2nd_pass'` AND `llm_confidence ∈ {'medium','low'}` (or category `unknown/medium`)
- [ ] In `/stage-1`, the row appears with chip `LLM · medium` (or whatever confidence the agent assigned)
- [ ] In Bulk Review, the operator can approve / reject / override-category as usual

If any of those four bullets fails → it's a real bug — capture row + decision_details JSON in a fresh debug session.

---

## Block D — Phase 83 carry-over (informational, not gating)

Phase 83 closed with V1/V2 PASS within known Graph-404 noise floor and V3/V4 PARTIAL pending live traffic. These were explicitly **not** marked as gating for v8.0 close per the punch list — they accumulate evidence as live traffic flows. If you want to glance at them while you're already in the dashboard:

- [ ] (optional) Spot-check a recent forwarded debtor email in Bulk Review → confirm full quoted body is visible (Phase 83 body ingestion)

Do not block v8.0 closure on this.

---

## When all of A + B + C are checked

Run:

```
/gsd-audit-milestone v8.0
```

Then archive v8.0 and roll into v8.1 (Phases 83 → 87 — body ingestion follow-ons, Stage 1 AP-automation noise rules, Stage 3 intent definitions v3, open-set intent discovery, retro-classification baseline).

---

## Status mirror

When each block completes, mark it in `.planning/STATE.md` under the v8.0 punch list. Final state after C-1 passes:

```
- [x] Phase 82.9: Stage 2 audit-panel evidence expansion
- [x] Phase 999.8: Stage 1 LLM confidence gate + predictor attribution
```

That's the gate.
