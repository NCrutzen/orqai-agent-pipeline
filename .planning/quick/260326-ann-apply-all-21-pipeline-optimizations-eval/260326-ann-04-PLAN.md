---
phase: quick-260326-ann
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/agents/deployer.md
  - orq-agent/agents/hardener.md
autonomous: true
requirements: [OPT-06, OPT-07, OPT-08, OPT-09, OPT-10, OPT-11, OPT-20, OPT-21]

must_haves:
  truths:
    - "deployer.md has Phase 1.6 Memory Store provisioning"
    - "deployer.md has team_of_agents using {key, role} objects (not try-strings-first)"
    - "deployer.md includes response_format in create/update payload and comparison allowlist"
    - "deployer.md includes settings.evaluators and settings.guardrails in create payload"
    - "deployer.md has streaming note for user-facing agents"
    - "deployer.md has webhook post-deploy recommendation"
    - "deployer.md removes bogus @orq-ai/node ^3.14.45 anti-pattern"
    - "hardener.md uses settings.guardrails API for guardrail attachment (not just spec file update)"
    - "hardener.md has Annotations/Feedback API note"
    - "hardener.md has webhook post-harden recommendation"
  artifacts:
    - path: "orq-agent/agents/deployer.md"
      provides: "Memory store provisioning, corrected team_of_agents, evaluator/guardrail attachment, streaming, webhooks"
    - path: "orq-agent/agents/hardener.md"
      provides: "Settings.guardrails API, annotations/feedback note, webhook recommendation"
  key_links:
    - from: "orq-agent/agents/deployer.md"
      to: "orq-agent/references/orqai-api-endpoints.md"
      via: "deployer loads API endpoints reference"
      pattern: "orqai-api-endpoints.md"
    - from: "orq-agent/agents/hardener.md"
      to: "orq-agent/references/orqai-evaluator-types.md"
      via: "hardener loads evaluator types for guardrail suggestion"
      pattern: "orqai-evaluator-types.md"
---

<objective>
Update deployer.md and hardener.md with memory store provisioning, evaluator/guardrail deploy-time attachment, corrected team_of_agents format, streaming/webhook notes, feedback API, and SDK corrections.

Purpose: These agents handle deployment and hardening of agents on Orq.ai. They need the latest API capabilities for evaluator/guardrail attachment, memory store provisioning, and corrected SDK patterns.

Output: 2 updated deploy/harden agent specs.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260326-ann-apply-all-21-pipeline-optimizations-eval/260326-ann-RESEARCH.md
@orq-agent/agents/deployer.md
@orq-agent/agents/hardener.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update deployer.md with memory stores, evaluators/guardrails, and corrections</name>
  <files>orq-agent/agents/deployer.md</files>
  <action>
**1. Add Phase 1.6: Memory Store Provisioning (Item 8):**

After Phase 1.5 (Provision Knowledge Bases) and before Phase 2 (Deploy Sub-Agents), insert a new phase:

```markdown
## Phase 1.6: Provision Memory Stores

For each memory store referenced in agent specs (parsed from `memory_stores` arrays during Step 0.3), provision them before agent deployment.

### Step 1.6.0: REST-Only Pattern

Memory store operations are REST-only. No MCP tools exist for memory store CRUD. All calls use `Authorization: Bearer $ORQ_API_KEY` against REST endpoints directly. Same pattern as KB operations (Phase 1.5).

### Step 1.6.1: Lookup Existing Memory Stores

`GET /v2/memory-stores?limit=200` with Bearer auth. Search response for matching `key` field.

**Cache the memory store list** after the first call. Same caching pattern as tools (Step 1.1) and KBs (Step 1.5.1).

### Step 1.6.2: Per-Memory-Store Provisioning

For each memory store referenced by agents in the swarm:

1. Check cached list for existing memory store with matching `key`
2. If not found: Create via REST:
   ```bash
   POST /v2/memory-stores
   Authorization: Bearer $ORQ_API_KEY
   Content-Type: application/json

   {
     "key": "memory-store-key",
     "embedding_config": { "model": "cohere/embed-english-v3.0" },
     "description": "Memory store purpose from agent spec",
     "path": "Default"
   }
   ```
3. If found: Check if `description` matches. Update if different. Skip if identical.
4. Record `memory_store_id` from response

**Build `memory_store_id_map`:** After all stores are processed, dictionary maps `store_key -> memory_store_id`.

### Step 1.6.3: Error Handling

- **Memory store creation failure is a WARNING** (not a blocker). Agents can still deploy without memory stores -- memory functionality will be unavailable but the agent will operate. Log the warning and continue.
- Unlike KBs (which block because agents wire `knowledge_id`), memory stores are referenced by key at runtime and do not need IDs in the agent payload.

### Step 1.6.4: Report Progress

Display: `Provisioning memory stores... (N/M)` where N is current store and M is total.

After all stores: `Provisioning memory stores... (M/M) done`
```

**2. Fix team_of_agents format (Item 6):**

In Phase 3 (Deploy Orchestrator), Step 3.2 (Build Orchestrator Payload), replace the current "try strings first, fallback to objects" logic:

Find:
```markdown
- **`team_of_agents`**: Array referencing sub-agent keys. Try array of strings first:
  ```json
  { "team_of_agents": ["sub-agent-key-1", "sub-agent-key-2"] }
  ```
  If the API returns a 422 validation error, switch to array of objects:
  ```json
  { "team_of_agents": [{"key": "sub-agent-key-1"}, {"key": "sub-agent-key-2"}] }
  ```
  Document which format the API accepted for future reference.
```

Replace with:
```markdown
- **`team_of_agents`**: Array of objects with `key` and `role` fields:
  ```json
  {
    "team_of_agents": [
      { "key": "sub-agent-key-1", "role": "Handles data extraction and parsing" },
      { "key": "sub-agent-key-2", "role": "Validates and enriches extracted data" }
    ]
  }
  ```
  Each entry identifies a sub-agent by its `key` (matching the sub-agent's deployed key) and describes its `role` (from the ORCHESTRATION.md agent descriptions). The `role` field helps the orchestrator understand what each sub-agent does.
```

**3. Add evaluator/guardrail deploy-time attachment (Item 7):**

In Phase 2 (Deploy Sub-Agents), Step 2.2 (Create or Update), update the agent create payload example to include the new fields. After the existing `"variables": {...}` line in the payload, add:

```json
  "settings": {
    "max_iterations": 10,
    "max_execution_time": 300,
    "tools": [...],
    "evaluators": [],
    "guardrails": [],
    "max_cost": null,
    "tool_approval_required": "respect_tool"
  }
```

Also add a section note:

```markdown
**Deploy-time evaluator/guardrail attachment:** When the agent spec includes evaluators or guardrails (added by the hardener in Phase 9), include them in the `settings.evaluators` and `settings.guardrails` arrays:

```json
{
  "settings": {
    "evaluators": [
      { "id": "evaluator-platform-id", "execute_on": "output", "sample_rate": 100 }
    ],
    "guardrails": [
      { "id": "guardrail-platform-id", "execute_on": "output", "sample_rate": 100 }
    ]
  }
}
```

Evaluator/guardrail IDs must be resolved platform IDs (from `GET /v2/evaluators`), not display names. This is the Control Tower integration -- evaluators attached at deploy time automatically monitor agent outputs.

**On first deploy (no hardening yet):** `evaluators` and `guardrails` arrays will be empty. After the hardener runs and attaches guardrails, subsequent re-deploys include them.
```

**4. Add response_format to comparison allowlist (Item 9):**

In Phase 2, Step 2.2, in the "Diff local spec against Orq.ai state" comparison fields list, add `response_format` to the list:
```
- `response_format`
```

Also add `response_format` to the Phase 4.2 comparison allowlist ("Agent fields to compare"):
```
- `response_format`
```

Also ensure the create payload mentions: when the agent spec has a `response_format` field, include it in the API payload.

**5. Fix SDK anti-pattern (Item 10):**

In the Anti-Patterns section, find:
```
- **Installing @orq-ai/node@latest** -- Must be `^3.14.45`. Version 4 dropped the MCP server binary. Never use `latest`.
```
Replace with:
```
- **SDK version pinning** -- Do NOT pin `@orq-ai/node` to `^3.14.45` (does not exist on npm). The deployer uses MCP tools and REST API directly (no SDK needed for deployment). For other pipeline stages that use the SDK, install the latest compatible version. See `orqai-api-endpoints.md` SDK and Integration Patterns for guidance.
```

**6. Add streaming and webhook notes (Items 20, 21):**

After Phase 5 (Annotate Local Spec Files), add a new section:

```markdown
## Post-Deploy Recommendations

After successful deployment, these additional configurations can be set up in Orq.ai Studio:

### Streaming for User-Facing Agents

For agents that will serve user-facing applications, recommend using the streaming endpoint (`POST /v2/agents/{agent_id}/stream`) instead of the synchronous `/execute` endpoint. Streaming provides:
- Progressive response rendering (better UX for long responses)
- Server-Sent Events (SSE) with OpenAI-compatible chunk format
- Same authentication and message format as `/execute`

The deployer does NOT configure streaming (it is an endpoint choice at invocation time, not a deployment setting). Document in the deploy log: "For user-facing agents, use `/stream` endpoint in production."

### Webhook Monitoring

Webhooks can be configured in Orq.ai Studio (Organization > Webhooks) after deployment. Key events:
- `agent.created` / `agent.updated` -- track deployment changes
- `deployment.invoked` -- monitor execution (includes response, token usage, latency, evaluation results)

Webhooks use HMAC-SHA256 signing (`X-Orq-Signature` header). Recommend setting up after initial deployment is stable.

This cannot be automated by the deployer (Studio-only configuration). Include a note in the deploy log when suggesting webhook setup.
```

**7. Update deploy manifest ordering (Step 0.4):**

In Step 0.4 (Build Deploy Manifest), update the ordered list to include memory stores:

```markdown
1. **Tools** (from TOOLS.md) -- deployed first because agents reference them
2. **Knowledge Bases** (from ORCHESTRATION.md) -- deployed second because agents reference knowledge_ids
3. **Memory Stores** (from agent specs) -- deployed third, referenced by key at runtime
4. **Sub-agents** (non-orchestrator agents) -- deployed fourth
5. **Orchestrator** (agent with `team_of_agents`) -- deployed last
```
  </action>
  <verify>
    <automated>grep -c "Phase 1.6\|memory-store\|team_of_agents.*key.*role\|settings.evaluators\|settings.guardrails\|response_format\|Streaming for User\|Webhook Monitoring\|SDK version pinning" orq-agent/agents/deployer.md | grep -v ':0$'</automated>
  </verify>
  <done>
  - deployer.md has Phase 1.6 Memory Store Provisioning with REST-only pattern
  - deployer.md uses {key, role} objects for team_of_agents (no try-strings-first)
  - deployer.md includes settings.evaluators and settings.guardrails in agent payload
  - deployer.md includes response_format in comparison allowlist
  - deployer.md removes bogus ^3.14.45 version pin
  - deployer.md has Post-Deploy Recommendations section with streaming and webhook guidance
  - deployer.md deploy manifest includes memory stores in correct ordering
  </done>
</task>

<task type="auto">
  <name>Task 2: Update hardener.md with settings.guardrails API and feedback/webhook notes</name>
  <files>orq-agent/agents/hardener.md</files>
  <action>
**1. Update guardrail attachment to use settings.guardrails API (Item 7):**

Read the full hardener.md file. Find the phase where guardrails are attached to deployed agents (should be a phase after HITL approval where the hardener updates agent config on Orq.ai). Update the attachment mechanism to use `settings.guardrails`:

In the phase that attaches guardrails to agents (likely Phase 4 or similar), update the API call documentation:

```markdown
### Guardrail Attachment via settings.guardrails

After user approval, attach guardrails to the deployed agent using the `settings.guardrails` array on the agent update payload:

```bash
# Resolve guardrail evaluator IDs first
EVALUATORS=$(curl -s "https://api.orq.ai/v2/evaluators?limit=200" \
  -H "Authorization: Bearer $ORQ_API_KEY")

# For each approved guardrail, extract platform ID:
GUARDRAIL_ID=$(echo "$EVALUATORS" | jq -r '.data[] | select(.name == "toxicity") | .id')

# Update agent with guardrails
curl -s -X PATCH "https://api.orq.ai/v2/agents/{agent_key}" \
  -H "Authorization: Bearer $ORQ_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "settings": {
      "guardrails": [
        { "id": "'$GUARDRAIL_ID'", "execute_on": "output", "sample_rate": 100 }
      ]
    }
  }'
```

**Guardrail format:** Each guardrail in `settings.guardrails` is an object:
```json
{
  "id": "evaluator-platform-id",
  "execute_on": "output",
  "sample_rate": 100
}
```

- `id`: Platform evaluator ID (resolved via `GET /v2/evaluators`)
- `execute_on`: When to run the guardrail. Use `"output"` for post-generation checks.
- `sample_rate`: Percentage of invocations to evaluate (1-100). Safety guardrails: 100. Quality guardrails: 80.

**Evaluator attachment (monitoring):** For evaluators that should monitor but not block, use `settings.evaluators` with the same format:
```json
{
  "settings": {
    "evaluators": [
      { "id": "eval-platform-id", "execute_on": "output", "sample_rate": 80 }
    ],
    "guardrails": [
      { "id": "guard-platform-id", "execute_on": "output", "sample_rate": 100 }
    ]
  }
}
```

**Distinction:** Guardrails can block/flag responses. Evaluators only monitor and score. Both use the same attachment format but serve different purposes. Safety items -> guardrails. Quality items -> evaluators.

This is the Control Tower integration -- attached evaluators/guardrails automatically appear in the Control Tower for monitoring.
```

Also update the agent spec file writing step to include the guardrail configuration in a format that the deployer can read on re-deploy. When writing guardrails to the agent spec file, include both the guardrail names AND platform IDs so the deployer can use `settings.guardrails` on the next deploy without re-resolving.

**2. Add Annotations/Feedback API note (Item 11):**

After the guardrail attachment phase, add a new section:

```markdown
## Annotations and Feedback

After hardening, the pipeline can submit programmatic feedback on low-scoring traces using the Orq.ai Feedback API:

```javascript
// Via @orq-ai/node SDK
orq.feedback.create({
  field: "rating",        // "rating" or "defects"
  value: ["poor"],        // rating: ["good"|"poor"], defects: ["hallucination", "off_topic", etc.]
  trace_id: trace_id      // from agent execution response
});
```

**Available defect types:** grammatical, spelling, hallucination, repetition, inappropriate, off_topic, incompleteness, ambiguity

**When to annotate:**
- After experiments, for any example where evaluator scores fell below thresholds
- The `trace_id` comes from the experiment execution results (each agent invocation produces a trace)
- Annotations feed into Orq.ai's Annotation Queues (configured via Trace Automations in Studio)

**Limitation:** Only SDK method is documented for feedback. REST endpoint is not publicly documented. If SDK is unavailable, skip annotations and log: "Feedback annotations skipped -- SDK not available."

This is an advisory feature, not a blocking step. The hardener should offer to annotate low-scoring traces after guardrail attachment is complete.
```

**3. Add webhook post-harden recommendation (Item 21):**

At the end of the hardener spec, add to the quality report output section or as a final note:

```markdown
### Post-Harden Webhook Recommendation

After hardening is complete, include in the quality report:

```markdown
## Recommended Next Steps

### Webhook Monitoring (Optional)
Configure webhooks in Orq.ai Studio (Organization > Webhooks) for production monitoring:
- `deployment.invoked` -- includes evaluation results from attached guardrails
- Enable for agents with guardrails to get real-time quality alerts
- Payloads signed with HMAC-SHA256 (`X-Orq-Signature` header)
```

This is a documentation-only addition to the quality report. Webhooks cannot be configured via API.
```
  </action>
  <verify>
    <automated>grep -c "settings.guardrails\|execute_on.*output\|sample_rate\|feedback.create\|Annotations and Feedback\|Webhook.*Recommendation\|Control Tower" orq-agent/agents/hardener.md | grep -v ':0$'</automated>
  </verify>
  <done>
  - hardener.md uses settings.guardrails array format for guardrail attachment
  - hardener.md uses settings.evaluators for monitoring-only evaluators
  - hardener.md distinguishes guardrails (can block) vs evaluators (monitor only)
  - hardener.md has Annotations and Feedback section with SDK method and defect types
  - hardener.md has Post-Harden Webhook Recommendation
  - hardener.md mentions Control Tower integration
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep "Phase 1.6" orq-agent/agents/deployer.md` shows memory store provisioning
2. `grep "team_of_agents" orq-agent/agents/deployer.md` shows {key, role} format, no "try strings first"
3. `grep "settings.guardrails" orq-agent/agents/deployer.md` shows deploy-time attachment
4. `grep "settings.guardrails" orq-agent/agents/hardener.md` shows attachment format
5. `grep "3.14.45" orq-agent/agents/deployer.md` returns NO matches
6. `grep "feedback.create" orq-agent/agents/hardener.md` shows annotations API
7. `grep "Webhook" orq-agent/agents/deployer.md orq-agent/agents/hardener.md` shows both files have webhook notes
</verification>

<success_criteria>
Both deploy/harden agents updated: deployer has memory store provisioning, corrected team_of_agents format, evaluator/guardrail deploy-time attachment, response_format in comparison, streaming/webhook recommendations, fixed SDK reference. Hardener uses settings.guardrails API, has feedback/annotations note, and webhook recommendation.
</success_criteria>

<output>
After completion, create `.planning/quick/260326-ann-apply-all-21-pipeline-optimizations-eval/260326-ann-04-SUMMARY.md`
</output>
