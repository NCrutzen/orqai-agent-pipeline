---
phase: quick-260326-ann
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/references/orqai-agent-fields.md
  - orq-agent/references/orqai-evaluator-types.md
  - orq-agent/references/orqai-api-endpoints.md
  - orq-agent/references/orqai-model-catalog.md
autonomous: true
requirements: [OPT-01, OPT-05, OPT-08, OPT-09, OPT-10, OPT-13, OPT-17, OPT-18, OPT-19, OPT-20, OPT-21]

must_haves:
  truths:
    - "orqai-agent-fields.md documents model as object with parameters (thinking, reasoning_effort, cache, load_balancer, retry)"
    - "orqai-agent-fields.md documents team_of_agents as {key, role} objects"
    - "orqai-agent-fields.md documents multimodal message parts (file kind with bytes/uri)"
    - "orqai-agent-fields.md documents response_format inside model.parameters"
    - "orqai-agent-fields.md documents settings.evaluators, settings.guardrails, settings.max_cost, settings.tool_approval_required"
    - "orqai-evaluator-types.md includes all missing function evaluators (contains_all, contains_any, contains_none, contains_valid_link, length_between, length_greater_than, length_less_than, bert_score)"
    - "orqai-evaluator-types.md includes all missing LLM evaluators (age_appropriate, bot_detection, fact_checking_knowledge_base, grammar, localization, pii, sentiment_classification, tone_of_voice, translation)"
    - "orqai-evaluator-types.md documents custom evaluator creation via API"
    - "orqai-api-endpoints.md includes memory store provisioning payload and SDK/REST/MCP usage clarification"
    - "orqai-api-endpoints.md removes bogus @orq-ai/node ^3.14.45 pin and documents actual usage patterns"
    - "orqai-model-catalog.md notes models-list returns ALL models and user must verify enabled status"
  artifacts:
    - path: "orq-agent/references/orqai-agent-fields.md"
      provides: "Complete agent field reference with model object, thinking, multimodal, deploy-time evaluators/guardrails"
    - path: "orq-agent/references/orqai-evaluator-types.md"
      provides: "Complete evaluator catalog with all function, LLM, RAGAS evaluators plus custom evaluator API creation"
    - path: "orq-agent/references/orqai-api-endpoints.md"
      provides: "API endpoints with memory store payload, streaming note, webhook note, SDK usage clarification"
    - path: "orq-agent/references/orqai-model-catalog.md"
      provides: "Model catalog with auto-router note and models-list returns ALL models warning"
  key_links:
    - from: "orq-agent/references/orqai-agent-fields.md"
      to: "orq-agent/agents/spec-generator.md"
      via: "spec-generator loads agent-fields as reference"
      pattern: "orqai-agent-fields.md"
    - from: "orq-agent/references/orqai-evaluator-types.md"
      to: "orq-agent/agents/experiment-runner.md"
      via: "experiment-runner loads evaluator-types for selection"
      pattern: "orqai-evaluator-types.md"
---

<objective>
Update all 4 reference files with the complete Orq.ai API capabilities discovered via MCP schema and research.

Purpose: Reference files are the single source of truth that all pipeline agents load. Updating them first ensures downstream agent updates in Plans 02-04 can reference accurate field names, evaluator types, and API patterns.

Output: 4 updated reference files covering agent fields, evaluator types, API endpoints, and model catalog.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260326-ann-apply-all-21-pipeline-optimizations-eval/260326-ann-RESEARCH.md
@orq-agent/references/orqai-agent-fields.md
@orq-agent/references/orqai-evaluator-types.md
@orq-agent/references/orqai-api-endpoints.md
@orq-agent/references/orqai-model-catalog.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update orqai-agent-fields.md and orqai-evaluator-types.md</name>
  <files>orq-agent/references/orqai-agent-fields.md, orq-agent/references/orqai-evaluator-types.md</files>
  <action>
**orqai-agent-fields.md -- make these specific changes:**

1. **Update `model` field** (row in Core Fields table): Change description to document model-as-object form:
   ```
   Model ID as string (`provider/model-name`) or object: `{ "id": "provider/model-name", "parameters": { "cache": {...}, "guardrails": [...], "load_balancer": {...}, "response_format": {...}, "reasoning_effort": "low"|"medium"|"high", "thinking": {...}, "timeout": 300 }, "retry": { "count": 3, "on_codes": [429, 500] } }`. See Model Parameters and orqai-model-catalog.md.
   ```

2. **Update `team_of_agents` field** (row in Core Fields table): Change type from `array of strings` to `array of objects`. Change description to:
   ```
   Sub-agent assignments for hierarchical workflows. Each entry: `{ "key": "sub-agent-key", "role": "description of what this sub-agent does" }`. The parent agent must also have `retrieve_agents` and `call_sub_agent` tools configured.
   ```

3. **Update `response_format` field** (row in Core Fields table): Add note that when using model-as-object form, `response_format` can also go inside `model.parameters.response_format` (the top-level field is equivalent and still works).

4. **Add new fields to Core Fields table:**
   - `settings.evaluators` | array | No | Deploy-time evaluator attachment. Each entry: `{ "id": "evaluator-platform-id", "execute_on": "output", "sample_rate": 100 }`. Resolve evaluator IDs via `GET /v2/evaluators`. Used for Control Tower integration.
   - `settings.guardrails` | array | No | Deploy-time guardrail attachment. Same structure as `settings.evaluators`: `{ "id": "guardrail-platform-id", "execute_on": "output", "sample_rate": 100 }`. Applied at runtime to every agent invocation.
   - `settings.max_cost` | number | No | Maximum cost limit in USD per invocation. Agent execution stops if cost exceeds this threshold.
   - `settings.tool_approval_required` | string | No | Tool approval mode: `"all"` (approve every tool call), `"respect_tool"` (use per-tool `requires_approval`), or `"none"` (no approval needed). Default: `"respect_tool"`.

5. **Add new "Model Parameters" section** after the Model Format section at the end. Content:

   ```markdown
   ## Model Parameters

   When using the object form of `model`, these parameters are available inside `model.parameters`:

   | Parameter | Type | Description |
   |-----------|------|-------------|
   | `thinking` | object | Thinking/reasoning config. Options: `{ "type": "disabled" }`, `{ "type": "enabled", "budget_tokens": 4096, "thinking_level": "low"\|"medium"\|"high" }`, or `{ "type": "adaptive" }`. Use "enabled" for complex reasoning tasks. |
   | `reasoning_effort` | string | Shorthand reasoning control: `"low"`, `"medium"`, or `"high"`. Alternative to the full `thinking` object for models that support it. |
   | `response_format` | object | Same as the top-level `response_format` field. Can be placed here when using model-as-object form. |
   | `cache` | object | Response caching config: `{ "type": "exact_match", "ttl": 1800 }`. TTL range: 1-259200 seconds (max 3 days). **NOTE:** Caching is an AI Gateway feature. It works when the agent's underlying model calls go through the Gateway (`/v2/router/chat/completions`). For standard agent execution via `/v2/agents/{id}/execute`, caching behavior depends on the platform's internal routing. |
   | `load_balancer` | object | Weighted multi-model routing configuration. Distributes requests across multiple models based on weights. |
   | `guardrails` | array | Model-level guardrail configuration (separate from `settings.guardrails` which applies at the agent level). |
   | `timeout` | integer | Model-level timeout in seconds. |

   ### Model Retry Configuration

   The `model.retry` object controls automatic retry behavior:

   ```json
   {
     "model": {
       "id": "anthropic/claude-sonnet-4-5",
       "parameters": { "thinking": { "type": "adaptive" } },
       "retry": { "count": 3, "on_codes": [429, 500, 502, 503] }
     }
   }
   ```

   ### Thinking Configuration Examples

   ```json
   // Disabled (default for most agents)
   { "thinking": { "type": "disabled" } }

   // Enabled with budget
   { "thinking": { "type": "enabled", "budget_tokens": 4096, "thinking_level": "medium" } }

   // Adaptive (model decides)
   { "thinking": { "type": "adaptive" } }
   ```
   ```

6. **Add new "Multimodal Message Format" section** after Model Parameters:

   ```markdown
   ## Multimodal Message Format

   Agents support file attachments via the A2A message parts format. Use this when agents process images or PDFs.

   ```json
   {
     "message": {
       "role": "user",
       "parts": [
         { "kind": "text", "text": "Analyze this invoice" },
         {
           "kind": "file",
           "file": {
             "bytes": "base64-encoded-content",
             "mimeType": "application/pdf",
             "name": "invoice.pdf"
           }
         }
       ]
     }
   }
   ```

   **File type rules:**
   - **Images:** Support both URL (`uri` field) and base64 (`bytes` field). Supported types: `image/jpeg`, `image/png`, `image/gif`, `image/webp`.
   - **PDFs:** Base64 (`bytes`) ONLY -- `uri` is NOT supported for PDFs. Type: `application/pdf`.
   - **Mandatory fields:** `mimeType` must always be specified. `name` is optional but recommended.
   - **Model requirement:** The agent's model must be vision-capable (e.g., `openai/gpt-4o`, `google-ai/gemini-2.5-flash`, `anthropic/claude-sonnet-4-5`). Non-vision models will ignore file parts.
   ```

7. **Fix deployer.md anti-pattern reference** (in the file, locate the Anti-Patterns reference if any exists about `@orq-ai/node` version): This is handled in orqai-api-endpoints.md, not here.

**orqai-evaluator-types.md -- make these specific changes:**

1. **Update Built-in Function Evaluators section** -- change count from (19) to (27) and add these 8 new evaluators to the table:
   - `contains_all` | All expected substrings present in output | Binary
   - `contains_any` | At least one expected substring present in output | Binary
   - `contains_none` | No expected substrings present in output | Binary
   - `contains_valid_link` | Output contains a valid, reachable URL | Binary
   - `length_between` | Output length within specified min/max range | Binary
   - `length_greater_than` | Output length exceeds specified minimum | Binary
   - `length_less_than` | Output length below specified maximum | Binary
   - `bert_score` | BERT-based semantic similarity score | Continuous (0-1)

2. **Update LLM Evaluators section** -- change count from (10) to (19) and add these 9 new evaluators to the table:
   - `age_appropriate` | Content suitable for the target age group | Continuous (1-5)
   - `bot_detection` | Output appears human-written vs AI-generated | Continuous (1-5)
   - `fact_checking_knowledge_base` | Claims verifiable against provided knowledge base | Continuous (1-5)
   - `grammar` | Grammatical correctness and proper language use | Continuous (1-5)
   - `localization` | Content appropriately localized for target locale | Continuous (1-5)
   - `pii` | Presence of personally identifiable information | Binary
   - `sentiment_classification` | Sentiment matches expected tone | Continuous (1-5)
   - `tone_of_voice` | Output matches desired tone and style | Continuous (1-5)
   - `translation` | Translation quality and accuracy | Continuous (1-5)

3. **Update Custom Evaluator Types section** -- Add a new subsection "Creating Custom Evaluators via API" after the existing table:

   ```markdown
   ### Creating Custom Evaluators via API

   Custom evaluators can be created programmatically using the REST API. This enables automated evaluator provisioning as part of the pipeline.

   ```bash
   POST /v2/evaluators
   Authorization: Bearer $ORQ_API_KEY
   Content-Type: application/json

   {
     "name": "custom-domain-eval",
     "description": "Domain-specific evaluation logic",
     "type": "llm",  // or "python", "http", "json"
     "config": {
       // Type-specific configuration
       // LLM: { "prompt": "...", "model": "provider/model-name" }
       // Python: { "code": "def evaluate(input, output): ..." }
       // HTTP: { "url": "https://...", "method": "POST" }
       // JSON: { "schema": {...}, "scoring_rules": {...} }
     }
   }
   ```

   The created evaluator's `id` can then be used in experiment creation and deploy-time guardrail/evaluator attachment. See `orqai-api-endpoints.md` for the full CRUD endpoints.
   ```

4. **Update Selection Guidance table** -- Add these new rows:
   - **Grammar and language quality** | LLM evaluators: `grammar`, `tone_of_voice`, `localization`
   - **Content moderation** | LLM evaluators: `age_appropriate`, `pii`, `bot_detection`
   - **Knowledge-grounded tasks** | LLM evaluators: `fact_checking_knowledge_base`
   - **Translation and localization** | LLM evaluators: `translation`, `localization`
   - **Substring/pattern matching** | Function: `contains_all`, `contains_any`, `contains_none`, `contains_valid_link`
   - **Output length constraints** -- update existing row to also mention: `length_between`, `length_greater_than`, `length_less_than`
  </action>
  <verify>
    <automated>grep -c "contains_all\|contains_any\|bert_score\|age_appropriate\|bot_detection\|thinking\|Multimodal\|settings.evaluators\|team_of_agents.*object" orq-agent/references/orqai-agent-fields.md orq-agent/references/orqai-evaluator-types.md | grep -v ':0$' | wc -l</automated>
  </verify>
  <done>
  - orqai-agent-fields.md has model-as-object documentation with parameters (thinking, cache, load_balancer, retry)
  - orqai-agent-fields.md has team_of_agents as {key, role} objects
  - orqai-agent-fields.md has settings.evaluators, settings.guardrails, settings.max_cost, settings.tool_approval_required
  - orqai-agent-fields.md has Multimodal Message Format section with file parts
  - orqai-evaluator-types.md has all 27 function evaluators and 19 LLM evaluators
  - orqai-evaluator-types.md has custom evaluator API creation section
  </done>
</task>

<task type="auto">
  <name>Task 2: Update orqai-api-endpoints.md and orqai-model-catalog.md</name>
  <files>orq-agent/references/orqai-api-endpoints.md, orq-agent/references/orqai-model-catalog.md</files>
  <action>
**orqai-api-endpoints.md -- make these specific changes:**

1. **Add Memory Store create payload** -- After the existing Memory Stores table (which only has method/path/description), add a "Create Payloads" subsection under Memory Stores (matching the pattern used by Knowledge Bases):

   ```markdown
   ### Create Payloads

   **Memory Store:**
   ```json
   {
     "key": "store-key",
     "embedding_config": { "model": "provider/model-name" },
     "description": "Store purpose",
     "path": "project/folder"
   }
   ```

   Optional: `ttl` (time-to-live in seconds for memory documents).

   **Notes:**
   - `key` pattern: `^[A-Za-z][A-Za-z0-9]*([._-][A-Za-z0-9]+)*$`
   - Memory stores use REST-only (no MCP tools exist for memory store CRUD, same as KBs)
   ```

2. **Add Deployments section** after the Experiments section -- Add the invoke endpoint used for A/B testing:

   ```markdown
   ## Deployments

   | Method | Path | Description |
   |--------|------|-------------|
   | POST | `/v2/deployments/invoke` | Invoke a deployment with optional `modelId` override (used for A/B testing) |
   ```

3. **Add Streaming and Webhooks notes** -- After the existing Usage Notes section, add:

   ```markdown
   ## Streaming

   Agent streaming is available via `POST /v2/agents/{agent_id}/stream`. Returns Server-Sent Events (SSE):

   - Each chunk: `{ id, object: "chat.completion.chunk", delta: { content: "..." }, finish_reason: null|"stop"|"length"|"tool_calls" }`
   - The `/stream` endpoint is already listed in the Agents table above
   - Use `/execute` (synchronous) for testing pipelines; recommend `/stream` for user-facing agents in production

   Gateway streaming is also available on `/v2/router/chat/completions` with `"stream": true`.

   ## Webhooks

   Webhooks are configured in Orq.ai Studio (Organization > Webhooks). Cannot be automated via API.

   **Available events:**

   | Category | Events |
   |----------|--------|
   | Agents | `agent.created`, `agent.updated`, `agent.deleted` |
   | Deployments | `deployment.created`, `deployment.updated`, `deployment.deleted`, `deployment.invoked` |
   | Prompts | `prompt.created`, `prompt.updated`, `prompt.deleted` |

   - Payloads signed with HMAC-SHA256 (`X-Orq-Signature` header)
   - `deployment.invoked` includes execution data (response, token usage, latency, evaluation results)
   - Recommended for production monitoring setups -- configure after initial deployment is stable
   ```

4. **Fix SDK guidance** -- Replace the entire "SDK Method Mapping" section with:

   ```markdown
   ## SDK and Integration Patterns

   The pipeline uses three integration methods with the Orq.ai platform. Choose based on the operation:

   ### 1. MCP Tools (Primary for Agent/Tool CRUD)

   Used by deployer, tester, hardener for agent and tool operations. See per-agent MCP Tool Names sections.

   ### 2. REST API (Primary for Experiments, KBs, Memory Stores, Datasets)

   Direct `curl`/`fetch` calls to `https://api.orq.ai/v2/`. Used when MCP tools don't exist for the operation (KBs, memory stores) or when REST is more reliable (experiments).

   ### 3. @orq-ai/node SDK (Specific Use Cases)

   The `@orq-ai/node` SDK IS actively used in the pipeline for specific patterns:

   - **`deployments.invoke()`** with `modelId` override -- used for A/B testing different models against the same deployment
   - **Direct `fetch()` to `/v2/router/chat/completions`** -- used for LLM-as-judge evaluation calls

   **Environment variable mapping:** The SDK expects `ORQ_API_KEY` but the project `.env` uses `ORQ_KEY`. When initializing the SDK, map accordingly:
   ```javascript
   const orq = new Orq({ apiKey: process.env.ORQ_KEY || process.env.ORQ_API_KEY });
   ```

   **Version:** Do NOT pin to a specific version like `^3.14.45` (does not exist on npm). Install the latest compatible version: `npm install @orq-ai/node`. If v4 causes MCP binary issues, install `@orq-ai/node@3` explicitly.

   ### REST Equivalents

   Key SDK methods and their REST equivalents for debugging or when the SDK is unavailable:

   | SDK Method | REST Equivalent | Used By |
   |------------|----------------|---------|
   | `client.agents.responses.create({ agent_id, messages })` | `POST /v2/agents/{agent_id}/execute` | Agent invocation |
   | `client.datasets.create({ name, ... })` | `POST /v2/datasets` | Dataset creation |
   | `client.datasets.addRows({ dataset_id, rows })` | `POST /v2/datasets/{dataset_id}/rows` | Row upload |
   | `client.deployments.invoke({ key, ... })` | `POST /v2/deployments/invoke` | A/B testing |
   | `orq.feedback.create({ field, value, trace_id })` | *REST endpoint undocumented* | Feedback/annotations |
   ```

5. **Update the Models section description** -- Change the description of `GET /v2/models` from just "List available models (used for API key validation)" to:
   ```
   List all models (used for API key validation and model discovery). **NOTE:** Returns ALL models across all providers, not just models enabled in your workspace. Verify model is enabled in Orq.ai Studio before using.
   ```

**orqai-model-catalog.md -- make these specific changes:**

1. **Add a note about models-list behavior** right after the opening blockquote/note, before the Provider Format section:

   ```markdown
   > **WARNING: `models-list` returns ALL models.** The MCP `models-list` tool and `GET /v2/models` endpoint return every model across all providers (300+), NOT just models enabled in your workspace. There is no API to check enable/disable status. After selecting a model, **verify it is enabled in Orq.ai Studio** (Models page) before deploying. An agent configured with a disabled model will fail at execution time with no clear error.
   ```

2. **Add Auto-Router mention** at the end of the "How to Choose" section:

   ```markdown
   6. **Consider the AI Router.** Orq.ai's AI Router (`/v2/router/`) provides a unified gateway to all 300+ models with automatic retries and fallbacks. The `fallback_models` field on agents provides basic failover. For more sophisticated routing (weighted load balancing across models), use the `model.parameters.load_balancer` configuration in agent specs. See `orqai-agent-fields.md` Model Parameters section.
   ```
  </action>
  <verify>
    <automated>grep -c "memory-store\|embedding_config\|Streaming\|Webhooks\|models-list returns ALL\|ORQ_KEY\|deployments.invoke\|Auto-Router\|load_balancer" orq-agent/references/orqai-api-endpoints.md orq-agent/references/orqai-model-catalog.md | grep -v ':0$' | wc -l</automated>
  </verify>
  <done>
  - orqai-api-endpoints.md has memory store create payload with required fields
  - orqai-api-endpoints.md has Streaming section with SSE format
  - orqai-api-endpoints.md has Webhooks section with event types
  - orqai-api-endpoints.md has fixed SDK section: @orq-ai/node kept, bogus version pin removed, env var mapping documented, SDK vs REST vs MCP usage clarified
  - orqai-api-endpoints.md has Deployments section with invoke endpoint
  - orqai-model-catalog.md has WARNING about models-list returning ALL models
  - orqai-model-catalog.md has auto-router/load_balancer mention in How to Choose
  </done>
</task>

</tasks>

<verification>
After both tasks, verify the reference files are internally consistent:
1. `grep "team_of_agents" orq-agent/references/orqai-agent-fields.md` shows {key, role} objects, not strings
2. `grep "settings.evaluators" orq-agent/references/orqai-agent-fields.md` exists
3. `grep "contains_all" orq-agent/references/orqai-evaluator-types.md` exists
4. `grep "age_appropriate" orq-agent/references/orqai-evaluator-types.md` exists
5. `grep "@orq-ai/node" orq-agent/references/orqai-api-endpoints.md` no longer mentions `^3.14.45`
6. `grep "Multimodal" orq-agent/references/orqai-agent-fields.md` exists
7. `grep "thinking" orq-agent/references/orqai-agent-fields.md` shows thinking configuration
</verification>

<success_criteria>
All 4 reference files updated with the complete Orq.ai API surface area. No stale information (bogus SDK version pin, team_of_agents as strings). All new capabilities documented (thinking, multimodal, evaluator/guardrail attachment, custom evaluators, memory store provisioning, streaming, webhooks, SDK usage patterns).
</success_criteria>

<output>
After completion, create `.planning/quick/260326-ann-apply-all-21-pipeline-optimizations-eval/260326-ann-01-SUMMARY.md`
</output>
