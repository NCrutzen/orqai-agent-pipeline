---
phase: quick-260323-ep2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260323-bzl-beoordeel-pipeline-agents-met-browserles/BROWSER-AUTOMATION-ASSESSMENT.md
  - .planning/quick/260323-bzl-beoordeel-pipeline-agents-met-browserles/ACTION-PLAN.md
autonomous: true
requirements: [EP2-01, EP2-02]

must_haves:
  truths:
    - "workflow-discovery agent is described as a NEW HIGH-priority agent that sits after discussion and before architect"
    - "workflow-builder replaces sop-analyzer and works from multiple input sources (conversation, screenshots, optional SOP) -- NOT dependent on formal SOP documents existing"
    - "architect priority is MEDIUM (not HIGH) because it receives workflow-discovery output instead of detecting no-API systems itself"
    - "All 17 existing agents are re-assessed with the new model (workflow-discovery feeds architect, workflow-builder replaces sop-analyzer)"
    - "Pipeline flow shows: discussion -> workflow-discovery -> user confirms -> architect -> ... -> workflow-builder -> script-generator -> deployer"
    - "The term 'sop-analyzer' does NOT appear as a proposed agent anywhere in the rewritten documents"
    - "ACTION-PLAN.md implementation phases reflect the new agent structure and revised priorities"
  artifacts:
    - path: ".planning/quick/260323-bzl-beoordeel-pipeline-agents-met-browserles/BROWSER-AUTOMATION-ASSESSMENT.md"
      provides: "Complete rewritten assessment of all 17 pipeline agents with new workflow-discovery + workflow-builder model"
      contains: "workflow-discovery"
    - path: ".planning/quick/260323-bzl-beoordeel-pipeline-agents-met-browserles/ACTION-PLAN.md"
      provides: "Complete rewritten action plan with new pipeline flow, implementation phases, and agent specifications"
      contains: "workflow-builder"
  key_links:
    - from: "BROWSER-AUTOMATION-ASSESSMENT.md"
      to: "ACTION-PLAN.md"
      via: "Assessment conclusions drive action plan structure"
      pattern: "workflow-discovery.*workflow-builder.*script-generator"
    - from: "workflow-discovery agent description"
      to: "architect priority re-assessment"
      via: "Discovery agent absorbs detection responsibility, architect becomes consumer"
      pattern: "architect.*MEDIUM"
---

<objective>
Rewrite BROWSER-AUTOMATION-ASSESSMENT.md and ACTION-PLAN.md in .planning/quick/260323-bzl-beoordeel-pipeline-agents-met-browserles/ with a fundamentally revised model that introduces a workflow-discovery agent (early pipeline), replaces sop-analyzer with workflow-builder (multiple input sources), revises architect from HIGH to MEDIUM priority, and updates the complete pipeline flow.

Purpose: The previous assessment (260323-bzl) made two flawed assumptions: (1) that SOPs always exist (they don't -- most processes live in people's heads), and (2) that no-API detection belongs in the architect (it should happen earlier, conversationally, via a dedicated workflow-discovery agent). This rewrite corrects both.

Output: Two rewritten planning documents that accurately reflect the new agent model and pipeline flow.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Reference material (read but do NOT copy structure -- rewrite from scratch with new model):
@.planning/quick/260323-bzl-beoordeel-pipeline-agents-met-browserles/BROWSER-AUTOMATION-ASSESSMENT.md
@.planning/quick/260323-bzl-beoordeel-pipeline-agents-met-browserles/ACTION-PLAN.md

Browserless.io capabilities context:
@/Users/nickcrutzen/Developer/agent-workforce/.planning/research/BROWSERLESS-CAPABILITIES.md

The 17 existing pipeline agents are in: orq-agent/agents/
(architect, researcher, spec-generator, orchestration-generator, dataset-generator, tool-resolver, deployer, tester, dataset-preparer, experiment-runner, results-analyzer, failure-diagnoser, prompt-editor, iterator, hardener, kb-generator, readme-generator)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite BROWSER-AUTOMATION-ASSESSMENT.md with revised agent model</name>
  <files>.planning/quick/260323-bzl-beoordeel-pipeline-agents-met-browserles/BROWSER-AUTOMATION-ASSESSMENT.md</files>
  <action>
Overwrite BROWSER-AUTOMATION-ASSESSMENT.md completely. The document structure should follow the same general pattern as the original (executive summary, priority table, detailed per-agent analysis, new agent proposals, pipeline integration, summary) but with FUNDAMENTALLY revised content reflecting the new model.

**Key changes that MUST be reflected throughout:**

1. **NEW agent: workflow-discovery (HIGH priority)**
   - Pipeline position: after discussion, BEFORE architect
   - Purpose: Conversationally identifies which systems and workflows a use case requires. Reasons about integration methods (API, browser automation, manual). Presents findings to user for confirmation/correction.
   - Example interaction: User says "Klanten Service agent" -> workflow-discovery reasons: "To answer customer questions, you'll need to look up customer details in NXT (no API -> browser automation needed), check order status in iController (no API -> browser automation), access FAQ knowledge base (standard tool). Does this match your expectations?"
   - This agent feeds structured workflow+system discovery output into the architect
   - Input: use case description from discussion output, optional systems.json lookup
   - Output: structured discovery result per system: { system_name, integration_method, workflows_identified, confidence, user_confirmed }

2. **REPLACED agent: workflow-builder (replaces sop-analyzer)**
   - Pipeline position: after spec-generation, before script-generator (same position as old sop-analyzer)
   - Critical difference from sop-analyzer: works from MULTIPLE input sources, NOT just SOPs
   - Input sources (in order of likelihood):
     a. Conversational input (MOST COMMON -- user describes the workflow step by step)
     b. Screenshots of the target system (user uploads)
     c. SOP documents (when available -- NOT assumed to exist)
     d. Any combination of the above
   - Purpose: Builds structured workflow definitions regardless of input source. Uses AI vision for screenshots, NLP for conversation parsing, document parsing for SOPs.
   - Output: Same structured workflow definition as before (steps, UI elements, screenshot refs, annotations) but derived from whatever input is available

3. **architect priority: HIGH -> MEDIUM**
   - Architect NO LONGER detects no-API systems itself
   - Architect RECEIVES workflow-discovery output and uses it to design the swarm with full knowledge of which systems need browser automation
   - Still needs awareness of browser automation (to design swarm topology correctly) but detection logic is removed
   - The `<browser_automation_detection>` section from the old assessment is NO LONGER in architect -- it moved to workflow-discovery

4. **Re-assess ALL 17 agents with the new model.** Many priorities shift because:
   - Agents that previously needed to "detect" browser automation now receive it as input
   - The workflow-discovery agent handles the conversational detection
   - The workflow-builder replaces sop-analyzer's role with broader input handling

5. **Updated pipeline flow:**
   ```
   User Input
     -> discussion (existing, unchanged)
       -> workflow-discovery (NEW -- identifies systems + workflows conversationally)
         -> user confirms/corrects
           -> architect (designs swarm WITH workflow knowledge from discovery)
             -> researcher, spec-generator, tool-resolver, etc.
               -> [V1.0 output complete]
                 -> workflow-builder (NEW -- builds workflow defs from conversation/screenshots/SOP)
                   -> script-generator (generates + tests Playwright scripts)
                     -> deployer (deploys agents + browser automation MCP tools)
                       -> test/iterate/harden pipeline
   ```

**Document format:** Write in the same bilingual style as the original (section headers in Dutch where the original used Dutch, detailed English content). Keep the same professional depth. Maintain the executive summary, priority table (with revised priorities), detailed per-agent analysis sections, new agent proposals, pipeline integration analysis, and summary sections.

**What to preserve from the original:**
- Browserless.io context (V4.0 decisions)
- MCP tool as integration surface concept
- Fixed scripts over dynamic browser-use decision
- Session Replay for verification
- The general assessment methodology (just applied with the new model)

**What MUST NOT appear:**
- "sop-analyzer" as a proposed agent name (use "workflow-builder" instead)
- "orq-browser-automation-detector" as a proposed agent (replaced by workflow-discovery)
- Architect as HIGH priority for browser automation
- Any assumption that SOPs always exist
  </action>
  <verify>
    <automated>grep -c "sop-analyzer" .planning/quick/260323-bzl-beoordeel-pipeline-agents-met-browserles/BROWSER-AUTOMATION-ASSESSMENT.md | grep "^0$" && grep -c "workflow-discovery" .planning/quick/260323-bzl-beoordeel-pipeline-agents-met-browserles/BROWSER-AUTOMATION-ASSESSMENT.md && grep -c "workflow-builder" .planning/quick/260323-bzl-beoordeel-pipeline-agents-met-browserles/BROWSER-AUTOMATION-ASSESSMENT.md && grep "architect.*MEDIUM" .planning/quick/260323-bzl-beoordeel-pipeline-agents-met-browserles/BROWSER-AUTOMATION-ASSESSMENT.md</automated>
  </verify>
  <done>
- BROWSER-AUTOMATION-ASSESSMENT.md is fully rewritten with the new model
- workflow-discovery agent is described as HIGH priority, positioned after discussion and before architect
- workflow-builder replaces sop-analyzer with multiple input source support (conversation, screenshots, optional SOP)
- architect is re-assessed as MEDIUM priority (receives discovery output, no longer detects)
- All 17 existing agents are re-assessed with revised priorities reflecting the new pipeline flow
- The term "sop-analyzer" does not appear as a proposed agent
- Pipeline flow diagram shows the correct new ordering
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ACTION-PLAN.md with revised implementation phases and pipeline flow</name>
  <files>.planning/quick/260323-bzl-beoordeel-pipeline-agents-met-browserles/ACTION-PLAN.md</files>
  <action>
Overwrite ACTION-PLAN.md completely. This is the implementation roadmap that follows from the rewritten assessment. It must reflect the new agent model throughout.

**Key changes that MUST be reflected:**

1. **New Pipeline Subagents section** must describe:
   - `orq-workflow-discovery` (NEW -- not in old plan at all)
     - Purpose: Conversational system and workflow identification
     - Pipeline position: After discussion, before architect
     - Input: Use case description, optional systems.json
     - Output: Structured discovery result with system names, integration methods, identified workflows, user confirmation status
     - Tools: Read (for systems.json), conversation context
     - Estimated size: Medium (~400 lines) -- conversational reasoning, system detection heuristics, user confirmation flow
   - `orq-workflow-builder` (replaces orq-sop-analyzer)
     - Purpose: Builds structured workflow definitions from multiple input sources
     - Pipeline position: After spec-generation, before script-generator
     - Input: User conversation about workflow, uploaded screenshots, optional SOP document, workflow-discovery output
     - Output: workflow-definition.json (same output format as old sop-analyzer)
     - Key difference: Does NOT assume SOP exists. Most common path is conversational input + screenshots.
     - Tools: Read, Write, Vision API, conversation context
     - Estimated size: Large (~600+ lines)
   - `orq-script-generator` (same as before, consumes workflow-builder output)

2. **Existing Agent Modifications section** must reflect revised priorities:
   - architect.md: MEDIUM priority (not HIGH). Changes are simpler: receive and use workflow-discovery output in swarm design, add `browser_automation` field to output format. NO `<browser_automation_detection>` section needed (that logic is in workflow-discovery).
   - researcher.md: Reassess -- does it remain HIGH? Probably still HIGH because it needs to research target system UI patterns for the workflow-builder.
   - spec-generator.md: Reassess with new model
   - tool-resolver.md: Reassess with new model
   - deployer.md: Reassess with new model
   - All MEDIUM and LOW agents: Reassess with new model
   - All NONE agents: Confirm they remain NONE

3. **Implementation Order** must be restructured:
   - Phase A (Foundation): workflow-discovery agent (this is now the foundation, not architect detection)
   - Phase B (Core Agents): workflow-builder + script-generator
   - Phase C (Integration): Modified existing agents that consume discovery output
   - Phase D (Testing): Downstream agents
   - The order changes because workflow-discovery is now the trigger, not architect detection

4. **Pipeline Flow Diagram** must show the new flow:
   ```
   User Input -> discussion -> workflow-discovery (NEW) -> user confirms
     -> architect (uses discovery) -> researcher -> tool-resolver
     -> spec-generator -> orchestration-generator -> dataset-generator
     -> readme-generator -> [V1.0 Output Complete]
     -> workflow-builder (NEW) -> script-generator -> deployer
     -> test/iterate/harden
   ```

5. **Risk Assessment** must be updated:
   - Remove risks specific to SOP availability (workflow-builder handles no-SOP cases)
   - Add risks for conversational workflow identification (workflow-discovery may miss workflows)
   - Update assumption risks (no longer assumes SOP exists)

6. **Pre-Implementation Validation Checklist** must be updated:
   - Remove "SOP document available" as a hard requirement
   - Add validation of conversational workflow identification approach

**What MUST NOT appear:**
- "sop-analyzer" as an agent name
- "orq-browser-automation-detector" as an agent
- SOP document as a hard requirement for pipeline execution
- Architect as the detection mechanism for no-API systems

**What to preserve:**
- General structure (subagents, modifications, implementation order, pipeline flow, risks, checklist)
- Browserless.io technical details (pricing, capabilities)
- MCP tool deployment pattern
- Contract tables between agents (updated for new agents)
  </action>
  <verify>
    <automated>grep -c "sop-analyzer" .planning/quick/260323-bzl-beoordeel-pipeline-agents-met-browserles/ACTION-PLAN.md | grep "^0$" && grep -c "workflow-discovery" .planning/quick/260323-bzl-beoordeel-pipeline-agents-met-browserles/ACTION-PLAN.md && grep -c "workflow-builder" .planning/quick/260323-bzl-beoordeel-pipeline-agents-met-browserles/ACTION-PLAN.md && grep "architect.*MEDIUM\|MEDIUM.*architect" .planning/quick/260323-bzl-beoordeel-pipeline-agents-met-browserles/ACTION-PLAN.md</automated>
  </verify>
  <done>
- ACTION-PLAN.md is fully rewritten with the new implementation model
- workflow-discovery agent is specified with full input/output contracts
- workflow-builder replaces sop-analyzer with multiple input source support
- Implementation phases are restructured with workflow-discovery as foundation
- Pipeline flow diagram shows the correct new ordering
- Risk assessment no longer assumes SOP availability
- The term "sop-analyzer" does not appear
- architect is listed as MEDIUM priority
- Contract table between agents is updated for new agent names and data flows
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full rewrite:

1. Neither document contains "sop-analyzer" as a proposed agent name
2. Both documents reference "workflow-discovery" as a NEW HIGH-priority agent
3. Both documents reference "workflow-builder" as the replacement for sop-analyzer
4. architect is MEDIUM priority in both documents
5. Pipeline flow in both documents shows: discussion -> workflow-discovery -> architect -> ... -> workflow-builder -> script-generator -> deployer
6. All 17 existing agents are covered in the assessment
7. ACTION-PLAN.md implementation phases reflect the new model
8. No assumption that SOPs must exist -- workflow-builder handles conversational + screenshot + optional SOP input
</verification>

<success_criteria>
- BROWSER-AUTOMATION-ASSESSMENT.md and ACTION-PLAN.md are completely rewritten (not patched) in .planning/quick/260323-bzl-beoordeel-pipeline-agents-met-browserles/
- The new model (workflow-discovery + workflow-builder + revised architect priority) is consistently reflected throughout both documents
- Both documents are self-contained and can be read independently of the old versions
- The pipeline flow is correct and consistent between both documents
</success_criteria>

<output>
After completion, create `.planning/quick/260323-ep2-herschrijf-browser-automation-assessment/260323-ep2-SUMMARY.md`
</output>
