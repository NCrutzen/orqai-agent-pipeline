---
phase: quick
plan: 260323-c2b
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/34-foundation-auth/ (DELETE directory)
  - .planning/phases/35-pipeline-engine/ (DELETE directory)
  - .planning/research/ (DELETE directory)
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements: [STRIP-02]

must_haves:
  truths:
    - "No V3.0 web pipeline phase directories remain in .planning/phases/"
    - "No V3.0 web UI research files remain in .planning/research/"
    - "Planning docs contain zero V3.0 archived/dropped/strikethrough content"
    - "All references to V4.0 and V5.0 future milestones are preserved"
    - "All shipped milestone content (v0.3, V2.0, V2.1) is preserved unchanged"
  artifacts:
    - path: ".planning/phases/"
      provides: "Only V2.1 phase directories (26-33), no 34-* or 35-*"
    - path: ".planning/ROADMAP.md"
      provides: "Clean roadmap with only shipped milestones and V4.0/V5.0 future phases"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Only V4.0/V5.0 future requirements, no archived V3.0 content"
    - path: ".planning/PROJECT.md"
      provides: "CLI-only project identity, no dropped decision rows or V3.0 archived sections"
  key_links:
    - from: ".planning/STATE.md"
      to: ".planning/PROJECT.md"
      via: "Project reference pointer"
      pattern: "See: .planning/PROJECT.md"
    - from: ".planning/ROADMAP.md"
      to: ".planning/MILESTONES.md"
      via: "Milestone archive references"
      pattern: "milestones/"
---

<objective>
Remove all V3.0 web pipeline artifacts from .planning/ -- phase directories, research files, and archived/dropped/strikethrough content in planning docs. This fork focuses exclusively on Claude Code Skills; V3.0 web pipeline remnants are dead weight that confuses context.

Purpose: Clean fork identity. When Claude reads planning context, it should see only shipped CLI skill milestones and future V4.0/V5.0 plans -- zero V3.0 web UI noise.
Output: Lean planning directory with no trace of dropped web pipeline.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/quick/260319-cbi-strip-web-interface-supabase-and-vercel-/260319-cbi-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete V3.0 phase directories and research files</name>
  <files>
    .planning/phases/34-foundation-auth/ (DELETE)
    .planning/phases/35-pipeline-engine/ (DELETE)
    .planning/research/ (DELETE)
  </files>
  <action>
Delete these three directories entirely:

1. `rm -rf .planning/phases/34-foundation-auth/` -- 10 files: PLANs (34-01, 34-02, 34-03), SUMMARYs (34-01, 34-02), CONTEXT, RESEARCH, SETUP-GUIDE
2. `rm -rf .planning/phases/35-pipeline-engine/` -- 11 files: PLANs (35-01, 35-02, 35-03, 35-04), SUMMARYs (35-01, 35-02, 35-03), CONTEXT, RESEARCH
3. `rm -rf .planning/research/` -- 5 files: ARCHITECTURE.md, FEATURES.md, PITFALLS.md, STACK.md, SUMMARY.md (all V3.0 web UI research)

These are all V3.0 web pipeline artifacts. The previous quick task (260319-cbi) deleted the actual web/ and supabase/ code directories but left these planning artifacts. The research/ directory is entirely V3.0 web UI research (confirmed by reading SUMMARY.md header: "Project: Orq Agent Designer V3.0 -- Web UI & Dashboard").
  </action>
  <verify>
    <automated>test ! -d .planning/phases/34-foundation-auth && test ! -d .planning/phases/35-pipeline-engine && test ! -d .planning/research && echo "PASS: All V3.0 directories deleted" || echo "FAIL: V3.0 directories still exist"</automated>
  </verify>
  <done>No V3.0 phase directories (34-*, 35-*) exist in .planning/phases/. No .planning/research/ directory exists. Only V2.1 phase directories (26-33) remain.</done>
</task>

<task type="auto">
  <name>Task 2: Purge V3.0 content from planning docs</name>
  <files>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/PROJECT.md
    .planning/STATE.md
  </files>
  <action>
Read each file, remove all V3.0 archived/dropped content, and write back a clean version. Preserve all shipped milestone content and V4.0/V5.0 future content unchanged.

**ROADMAP.md:**
- Remove the entire `V3.0 Web UI & Dashboard (Phases 34-38) -- DROPPED` details/summary block (lines 77-88)
- Remove the V3.0 milestone row from the Milestones table: `| **V3.0** | Web UI & Dashboard... | **Dropped** |`
- Remove "V3.0 phases dropped." sentence from Execution Order
- Remove all V3.0 rows from the first Progress table (phases 34-38, all Dropped)
- Remove the V3.0 row from the Progress Summary table: `| V3.0 | 34-38 (5 phases) | - | **Dropped** | - |`
- Keep the overview paragraph but remove "V3.0" from it if mentioned (it is not currently)
- Keep all shipped milestone details blocks (v0.3, V2.0, V2.1) exactly as-is
- Keep V4.0 and V5.0 sections exactly as-is

**REQUIREMENTS.md:**
- Remove the entire `## Archived (V3.0 -- Dropped)` section (lines 6-55): the intro paragraph and all 6 subsections (Foundation & Auth, Pipeline Execution, Real-time Dashboard, Node Graph, HITL Approval, Projects & Access, Swarm Activation)
- Remove the entire `## Traceability` section (lines 93-128) since it only contains Dropped V3.0 entries
- Remove the V3.0 coverage line from the Coverage section at bottom
- Remove the "Out of Scope" entries that reference web-only concepts: "Drag-and-drop pipeline builder", "Chat-based interface", "Role-based access control (RBAC)", "Multi-tenant workspace separation", "Custom evaluator configuration UI", "Mobile-optimized interface", "Generative UI". Keep "Real-time agent monitoring" (Orq.ai native) and "Zapier integration" (general scope)
- Keep `## Future Requirements` (V4.0, V5.0) exactly as-is
- Update the header/dates to reflect this cleanup

**PROJECT.md:**
- In `## Key Decisions` table: Remove all rows with "Dropped" outcome (Next.js + Supabase + Vercel, Node graph for swarm visualization, GitHub repo as single source of truth). These are the 3 rows at the bottom of the decided section.
- In `## Context` section: Remove "V3.0 context" bullet if present -- but keep V4.0 and V5.0 context bullets
- In `## Requirements > ### Archived (V3.0 -- Removed)` section: Remove entirely
- In `## Current Milestone` section: Remove mention of "V3.0 Web UI removed from scope" -- just state that V4.0 is the next milestone
- Update last-updated date to 2026-03-23

**STATE.md:**
- In `## Accumulated Context > ### Decisions`: Remove "V3.0 Web UI dropped from scope" decision line (it is the event record, now irrelevant to this fork). Remove the entire "Archived (V3.0 web infrastructure -- no longer relevant):" block with its 6 strikethrough items.
- Update `last_updated` to 2026-03-23
- Update `Current focus` to remove "V3.0 dropped" mention -- just say "Between milestones -- V4.0 not yet started"
- Update `Last activity` to reflect this cleanup task
- Update `Stopped at` and `Resume with` in Session Continuity
  </action>
  <verify>
    <automated>! grep -r "V3\.0" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md && ! grep -i "web.*UI.*dashboard\|web interface\|Inngest\|Next\.js.*Supabase\|Azure AD\|M365 SSO\|foundation.*auth\|pipeline.*engine" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md && echo "PASS: No V3.0 references in planning docs" || echo "FAIL: V3.0 references still present"</automated>
  </verify>
  <done>
- ROADMAP.md: No V3.0 milestone, no Dropped phases, no V3.0 progress rows. Shipped milestones and V4.0/V5.0 preserved.
- REQUIREMENTS.md: No archived V3.0 requirements, no Dropped traceability rows. V4.0/V5.0 future requirements preserved.
- PROJECT.md: No Dropped decision rows, no V3.0 archived sections. Key decisions table only has active/pending decisions.
- STATE.md: No V3.0 archived decisions. Current position reflects clean CLI-only identity.
- grep for "V3.0" across all four files returns zero matches.
  </done>
</task>

</tasks>

<verification>
1. `ls .planning/phases/` shows only directories 26-33 (V2.1 phases)
2. `ls .planning/` shows no `research/` directory
3. `grep -r "V3.0" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/STATE.md` returns zero matches
4. `grep -ri "web.*UI\|web interface\|Inngest\|foundation.*auth\|pipeline.*engine\|Phase 34\|Phase 35\|Phase 36\|Phase 37\|Phase 38" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/STATE.md` returns zero matches
5. V4.0 and V5.0 content preserved: `grep "V4.0\|V5.0" .planning/ROADMAP.md` returns expected lines
6. Shipped milestones preserved: `grep "Shipped" .planning/ROADMAP.md` returns v0.3, V2.0, V2.1 entries
</verification>

<success_criteria>
- Zero V3.0 web pipeline artifacts anywhere in .planning/
- Zero V3.0 references in ROADMAP.md, REQUIREMENTS.md, PROJECT.md, STATE.md
- All shipped milestone content (v0.3, V2.0, V2.1) preserved unchanged
- V4.0 and V5.0 future milestone content preserved unchanged
- .planning/phases/ contains only directories 26-33
- .planning/research/ does not exist
</success_criteria>

<output>
After completion, create `.planning/quick/260323-c2b-remove-web-pipeline-keep-only-claude-cod/260323-c2b-SUMMARY.md`
</output>
