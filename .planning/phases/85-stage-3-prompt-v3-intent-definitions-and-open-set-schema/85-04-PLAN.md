---
phase: 85
plan: 04
type: execute
wave: 3
depends_on: ["85-03"]
files_modified:
  - .planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-OPERATOR-SIGNOFF.md
  - .planning/todos/pending/2026-05-XX-phase85-v2-retirement.md
autonomous: false
requirements:
  - P85-R6
must_haves:
  truths:
    - "Operator has independently re-run the 12-email payment_dispute regression smoke against the live V3 agent and recorded the verdict."
    - "Operator has observed agent_runs.token_usage for ≥3 production days post-deploy and confirmed R-03 cost delta sits within the projected bucket from 85-CORPUS.md."
    - "A V2-retirement follow-up TODO exists with a fire-date 14 calendar days after deploy (RESEARCH Open Q #1 default)."
  artifacts:
    - path: ".planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-OPERATOR-SIGNOFF.md"
      provides: "Operator's independent regression run results + token-usage observation + V2-retirement scheduling decision."
    - path: ".planning/todos/pending/2026-05-XX-phase85-v2-retirement.md"
      provides: "Scheduled follow-up to delete V2 schema + tests once the 14-day window closes."
  key_links:
    - from: "Plan 03 sign-off (85-AGENT-RITUAL-LOG.md)"
      to: "Operator verification (this plan)"
      via: "Plan 03 ALL 3 SMOKES GREEN line is the precondition; this plan re-runs from a clean operator session to confirm reproducibility."
---

<objective>
Operator verification step: independent regression run + multi-day token-usage observation + scheduling of V2 retirement. This is the human-in-the-loop gate that closes Phase 85.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-CONTEXT.md
@.planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-RESEARCH.md
@.planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-REGRESSION-BASELINE.md
@.planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-AGENT-RITUAL-LOG.md
@web/scripts/phase85-smoke-v3.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator re-runs 12-email regression smoke independently</name>
  <files>.planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-OPERATOR-SIGNOFF.md</files>
  <action>Operator runs `web/scripts/phase85-smoke-v3.ts --regression` independently of the Plan 03 executor; verdict recorded in 85-OPERATOR-SIGNOFF.md. See <how-to-verify> for procedure.</action>
  <verify>85-OPERATOR-SIGNOFF.md contains Task 1 verdict line with summary.changed value and GO/NO-GO.</verify>
  <done>Independent regression smoke results recorded; per-changed-row inspection documents boundary-correction vs regression for each diff.</done>
  <what-built>
V3 prompt + schema deployed to live `debtor-intent-agent` (Plan 03 sign-off in 85-AGENT-RITUAL-LOG.md). `phase85-smoke-v3.ts` available for replay.
  </what-built>
  <how-to-verify>
Operator runs (from a clean shell, after Plan 03's executor session has ended — independence requirement):

```
cd web && EMAIL_IDS=$(grep -A1 'email_id csv' ../.planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-REGRESSION-BASELINE.md | tail -1) \
  npx tsx scripts/phase85-smoke-v3.ts --regression
```

(Or simpler: copy-paste the 12 email_ids from the bottom of `85-REGRESSION-BASELINE.md` into the `EMAIL_IDS=` env var.)

Confirm:
1. Script exits 0 (`summary.changed ≤ 1`).
2. The per-row breakdown matches Plan 03's Smoke #3 result (same email_ids, same V3 top-1 verdicts). If the cache from Plan 03 still serves, that's fine — operator can force-uncached by clearing `agent_runs.tool_outputs.intent_first_pass` for those rows OR by trusting that the same EMAIL_IDS deterministically hit the same V3 cache (the assembled-input is deterministic per email_id).
3. For any row that changed top-1, operator inspects the email body and confirms the new top-1 is *better* (D-02 boundary corrected) and not a regression. If a row is a regression → STOP, open a follow-up plan to revisit prompt v3.

Operator records in `85-OPERATOR-SIGNOFF.md`:
- Date + time of run.
- summary.changed value.
- Per-changed-row: email_id, V2 top-1, V3 top-1, "boundary correction OK" / "regression — needs work".
- Verdict: GO / NO-GO for Phase 85 close.
  </how-to-verify>
  <resume-signal>Type "regression GO" if smoke is clean, or describe the regression rows that need re-prompting.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator observes agent_runs.token_usage for ≥3 production days post-deploy</name>
  <files>.planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-OPERATOR-SIGNOFF.md</files>
  <action>Operator waits ≥3 production days then runs the agent_runs.token_usage SQL via mcp__supabase__execute_sql; records per-day delta + EUR cost projection in 85-OPERATOR-SIGNOFF.md. See <how-to-verify> for SQL + decision criteria.</action>
  <verify>85-OPERATOR-SIGNOFF.md Task 2 block shows ≥3 days of data + EUR delta sitting within the projected bucket.</verify>
  <done>Token-usage observation recorded; deviation flagged if actual EUR delta is >2× upper-bound estimate.</done>
  <what-built>
V3 prompt with +2-3k tokens per call (per RESEARCH §4) is live. Monthly volume bucket recorded in `85-CORPUS.md` Task 1 of Plan 01.
  </what-built>
  <how-to-verify>
Operator waits ≥3 production days (so post-deploy traffic accumulates), then runs:

```sql
SELECT
  date_trunc('day', created_at) AS day,
  count(*) AS calls,
  avg((token_usage->>'input_tokens')::int) AS avg_input_tokens,
  avg((token_usage->>'output_tokens')::int) AS avg_output_tokens,
  sum((token_usage->>'input_tokens')::int) AS total_input_tokens
FROM public.agent_runs
WHERE swarm_type = 'debtor-email'
  AND intent_version = '2026-05-19.v3'
  AND created_at > now() - interval '5 days'
GROUP BY 1
ORDER BY 1 DESC;
```

(Run via `mcp__supabase__execute_sql`; project `mvqjhlxfvtqqubqgdvhz`.)

Operator records in `85-OPERATOR-SIGNOFF.md`:
- Per-day call count + avg input tokens.
- Compare avg_input_tokens against the V2 baseline (operator pulls one V2-era day with the same SQL but `intent_version = '2026-05-01.v2'` from before deploy). The delta should be **+2-3k tokens** — confirming R-03's estimate.
- Compute monthly cost delta in EUR using Sonnet 4.5 input rate $3/1M and the bucket from `85-CORPUS.md` Token Budget Check.
- If actual delta deviates significantly from estimate (>2× the upper bound): flag for follow-up — possible the prompt is larger than expected or call volume has shifted.
- If monthly delta crosses €100/mo threshold: bump the prompt-cache TODO (RESEARCH §4) to urgent.
  </how-to-verify>
  <resume-signal>Type "tokens OK" if cost sits within bucket, or describe the deviation.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Schedule V2 retirement follow-up</name>
  <files>.planning/todos/pending/2026-05-XX-phase85-v2-retirement.md</files>
  <action>Operator picks one of options a/b/c (14/7/30 days) — see <decision>+<options> below. Choice recorded in 85-OPERATOR-SIGNOFF.md and used by Task 4 to compute the fire-date.</action>
  <verify>85-OPERATOR-SIGNOFF.md Task 3 block shows selected option + computed fire-date.</verify>
  <done>Operator-chosen retention window recorded; Task 4 ready to write the scheduled TODO with that fire-date.</done>
  <decision>
When to delete the V2 Zod schema, V2 type, INTENT_VERSION_V2 constant, and types-v2.test.ts? RESEARCH Open Q #1 default is 14 calendar days post-deploy (Phase 65→66 precedent).
  </decision>
  <context>
D-07 says "one release." In this repo a "release" is not formally tagged. The 14-day window is conservative — it allows two full operator workweeks of V3 in production. If anything regresses, V2 is still a one-token rollback (re-flip cache-key in `debtor-email-coordinator.ts` and re-PATCH the Orq agent to V2 prompt — both PATCHes are reversible).

The V2 retirement is a separate plan, not a Phase 85 task — it's deferred work, not in-phase delivery.
  </context>
  <options>
    <option id="option-a">
      <name>14-day default (RESEARCH Open Q #1 recommended)</name>
      <pros>Matches Phase 65→66 precedent. Two full operator workweeks. Conservative.</pros>
      <cons>Slightly slower V2-code-debt removal.</cons>
    </option>
    <option id="option-b">
      <name>7-day aggressive</name>
      <pros>Faster cleanup. V2 code path is dead weight in the discriminated union.</pros>
      <cons>One workweek may not surface edge-case regressions.</cons>
    </option>
    <option id="option-c">
      <name>30-day patient</name>
      <pros>Full month of production traffic before V2 deletion.</pros>
      <cons>V2 code debt lingers; risk of "we forgot to delete it" if not scheduled.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a (14 days), option-b (7 days), or option-c (30 days).</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Write the V2-retirement follow-up TODO + operator sign-off</name>
  <files>.planning/todos/pending/2026-05-XX-phase85-v2-retirement.md, .planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-OPERATOR-SIGNOFF.md</files>
  <action>
Per Task 3's decision, schedule the V2 retirement.

Compute the fire-date: deploy date (from `85-AGENT-RITUAL-LOG.md` sign-off line) + N days (14 / 7 / 30 per Task 3 decision).

Create `.planning/todos/pending/<fire-date>-phase85-v2-retirement.md`:

```markdown
# Phase 85 V2 Retirement (deferred from Phase 85 Plan 04 Task 3)

**Fire date:** <YYYY-MM-DD>
**Decision basis:** Phase 85 Plan 04 Task 3 (option-X chosen by operator).
**Rollback safety:** V2 is a one-token revert (cache-key flip in debtor-email-coordinator.ts + Orq agent PATCH back to V2 prompt). Until this TODO fires, retain that escape hatch.

## Scope
- Delete `INTENT_VERSION_V2`, `intentAgentOutputSchemaV2`, `IntentAgentOutputV2` from `web/lib/automations/debtor-email/coordinator/types.ts`.
- Replace `intentAgentOutputSchemaAny` (discriminated union) with a direct alias to `intentAgentOutputSchemaV3`.
- Simplify `invoke-intent.ts` discriminator back to a single `safeParse(intentAgentOutputSchemaV3)`.
- Delete `web/lib/automations/debtor-email/coordinator/__tests__/types-v2.test.ts` and `__tests__/invoke-intent-v2.test.ts`.
- `grep` for any lingering `INTENT_VERSION_V2` or `IntentAgentOutputV2` references and clean up.

## Pre-fire checks
- agent_runs counts of intent_version='2026-05-01.v2' are zero for the past 14 days (or window per decision).
- No open production-incident referencing intent_version mismatch.

## Out of scope for this TODO
- Phase 86 proposal-capture surface (separate phase).
- V9.0 Learning Inbox promotion (separate phase).
```

Finalise `85-OPERATOR-SIGNOFF.md` with:
- Task 1 verdict (regression GO/NO-GO).
- Task 2 verdict (tokens OK + actual EUR delta).
- Task 3 decision (option chosen + fire-date computed).
- Phase 85 CLOSE verdict + timestamp.

If any Task 1/2 was NO-GO, do not write the close verdict — instead document the open work and which plan should pick it up next.
  </action>
  <verify>
    <automated>test -f .planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-OPERATOR-SIGNOFF.md && ls .planning/todos/pending/ | grep -q "phase85-v2-retirement" && grep -q "PHASE 85 CLOSE" .planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-OPERATOR-SIGNOFF.md</automated>
  </verify>
  <done>85-OPERATOR-SIGNOFF.md exists with all three verdicts + PHASE 85 CLOSE line. V2-retirement TODO scheduled with explicit fire-date.</done>
</task>

</tasks>

<verification>
- 85-OPERATOR-SIGNOFF.md committed with regression verdict, token-usage observation, V2-retirement decision, close verdict.
- V2-retirement TODO present under `.planning/todos/pending/` with explicit fire-date.
- All three Plan 03 smokes were already green (precondition); Plan 04 re-verified independently.
</verification>

<deviation_rules>
- DO NOT delete V2 in this plan — only schedule it. V2 deletion is a separately-tracked future plan.
- DO NOT proceed past Task 1 if the regression run shows `changed > 1` — open a prompt-tuning follow-up.
- DO NOT proceed past Task 2 if token usage is 2× the upper-bound estimate — investigate before closing.
- DO NOT skip the 3-day production observation window — single-day data is too noisy.
- DO NOT touch production code or Orq agent in this plan — Plan 03 owns deploy; Plan 04 is verification only.
- DO NOT start Phase 86 work here — proposal-capture surface is a separate phase.
- DO NOT promote any captured proposals from `agent_runs.tool_outputs.intent_first_pass` to `swarm_intents` — that is V9.0 Learning Inbox scope.
</deviation_rules>

<out_of_scope>
- V2 schema deletion (scheduled TODO, executes outside Phase 85).
- Phase 86 proposal-capture surface.
- Promotion to swarm_intents (V9.0).
- Anthropic prompt-cache enablement (RESEARCH §4 follow-up, fires only if volume > 10k/mo or actual EUR delta crosses €100/mo).
- Adding Zod prefix-regex mirror on proposal_reason (Plan 03 deferred; depends on Smoke #4 pattern observation if run).
- Sales-email agent (V10.0).
</out_of_scope>

<success_criteria>
- Operator independent regression: `summary.changed ≤ 1` confirmed.
- Token-usage delta sits within RESEARCH §4 projected bucket (±2× upper bound acceptable; >2× requires follow-up).
- V2 retirement TODO exists with explicit fire-date.
- 85-OPERATOR-SIGNOFF.md closes Phase 85 with a clean verdict line.
</success_criteria>

<output>
After completion, create `.planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-04-SUMMARY.md` summarising: operator regression rerun verdict, observed token delta vs projected, V2 retirement schedule, any follow-up TODOs created.
</output>
