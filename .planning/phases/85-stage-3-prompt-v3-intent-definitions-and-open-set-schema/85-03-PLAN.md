---
phase: 85
plan: 03
type: execute
wave: 2
depends_on: ["85-01", "85-02"]
files_modified:
  - web/scripts/phase85-smoke-v3.ts
  - .planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-AGENT-RITUAL-LOG.md
autonomous: false
requirements:
  - P85-R1
  - P85-R5
user_setup:
  - service: orq-ai
    why: "Deploy V3 prompt + json_schema to live debtor-intent-agent via Studio + MCP."
    env_vars:
      - name: ORQ_API_KEY
        source: "web/.env.local (already present); needed by phase85-smoke-v3.ts"
    dashboard_config:
      - task: "Studio → Tools → Add JSON Schema resource (or update existing) named `debtor-intent-agent-output-v3` with anyOf-nullable shape per CLAUDE.md learning 3970bad9."
        location: "https://my.orq.ai/cura/agents/01KQECK191GE21CH8D8KEMTM9J → Tools tab"
      - task: "Studio → Model parameters → Response Format dropdown set to 'JSON Schema' and select the new V3 resource by name."
        location: "Same agent page → Model parameters"
must_haves:
  truths:
    - "get_agent shows V3 instructions (with <intent_definitions> block + 11+ few-shots) AND V3 json_schema reference."
    - "Smoke #1 (WKA-Breman novel email) returns intent_proposal non-null with snake_case label."
    - "Smoke #2 (clean copy_document_request email) returns intent_proposal null."
    - "Smoke #3 (12-email payment_dispute regression set) shows ≤1/12 changing top-1 closed-list intent."
  artifacts:
    - path: "web/scripts/phase85-smoke-v3.ts"
      provides: "Replayable agent invocation smoke harness, modelled on phase69-dispatch-smoke.ts."
      exports: ["default runner"]
    - path: ".planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-AGENT-RITUAL-LOG.md"
      provides: "Operator log: list_models excerpt, get_agent excerpt, 3 smoke responses verbatim."
  key_links:
    - from: "Live Orq agent debtor-intent-agent"
      to: "intentAgentOutputSchemaV3 in coordinator/types.ts"
      via: "intent_version literal '2026-05-19.v3' byte-equal on both sides (PATTERNS §types.ts literal-equality rule)"
      pattern: "2026-05-19.v3"
    - from: "phase85-smoke-v3.ts"
      to: "85-REGRESSION-BASELINE.md email_ids csv"
      via: "EMAIL_IDS env var read by script in --regression mode"
---

<objective>
Deploy prompt v3 + V3 json_schema to the live `debtor-intent-agent` (PATCH-only ritual, agent already exists at `01KQECK191GE21CH8D8KEMTM9J`), then prove the deploy via three smoke runs (novel-intent positive, no-false-novelty negative, 12-email disambiguation regression).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-CONTEXT.md
@.planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-RESEARCH.md
@.planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-PATTERNS.md
@.planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-CORPUS.md
@.planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-REGRESSION-BASELINE.md
@web/scripts/phase69-dispatch-smoke.ts
@web/scripts/smoke-orq-deployments.ts
@web/lib/automations/debtor-email/coordinator/invoke-intent.ts
@web/lib/automations/debtor-email/coordinator/assemble-input.ts

<interfaces>
**Agent identifiers:**
- Key (use this for `update_agent`): `debtor-intent-agent` (MEMORY: feedback_orq_update_agent_key_vs_id — generic "Failed to update agent" if slug-id is used).
- Slug ID (informational, do NOT pass to update_agent): `01KQECK191GE21CH8D8KEMTM9J`.

**Live prompt structure (D-01):** insert new `<intent_definitions>` block between existing `<intent_vocabulary>` and `<confidence_rubric>`. One `<intent name="...">` block per closed-list intent (payment_dispute, credit_request, copy_document_request, address_change, contract_inquiry, peppol_request, general_inquiry, other). Each ends with a one-line boundary sentence per D-02 disambiguation table.

**Few-shot examples (D-03):** ≥11 total (3 existing + ≥8 new); language mix 6 NL / 3 EN / 1 cross-language quoted prior (RESEARCH Open Q #4). Sourced from `85-CORPUS.md` slots A-I (with B = dual `payment_dispute` example).

**V3 json_schema (D-04 + PATTERNS §D anyOf-nullable):**
```json
{
  "ranked": { /* unchanged from V2 */ },
  "language": { "type": "string", "enum": ["nl","en","de","fr"] },
  "urgency": { "type": "string", "enum": [...] },
  "intent_version": { "type": "string", "const": "2026-05-19.v3" },
  "intent_proposal": {
    "anyOf": [
      { "type": "string", "maxLength": 64, "pattern": "^[a-z][a-z0-9_]*$" },
      { "type": "null" }
    ]
  },
  "proposal_reason": {
    "anyOf": [
      { "type": "string", "maxLength": 280, "pattern": "^No closed-list intent fits because" },
      { "type": "null" }
    ]
  }
}
```
NEVER use `type:["string","null"]` shorthand (CLAUDE.md learning 3970bad9).

**Smoke harness shape:** mirror `web/scripts/phase69-dispatch-smoke.ts` for env-loading + JSON-dump + exit codes. Two modes: single-email (`EMAIL_ID=<uuid>`) and regression (`EMAIL_IDS=<csv> --regression`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author prompt v3 string + V3 json_schema JSON in 85-AGENT-RITUAL-LOG.md</name>
  <files>.planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-AGENT-RITUAL-LOG.md</files>
  <action>
Create `85-AGENT-RITUAL-LOG.md`. Section 1: paste the full prompt v3 string ready to copy into Studio's `instructions` field. Compose it from:

1. The current live prompt (fetch via `mcp__orqai-mcp__get_agent debtor-intent-agent` and paste its `instructions` field as the base).
2. Insert `<intent_definitions>` block between `<intent_vocabulary>` and `<confidence_rubric>` per D-01 shape. One block per intent — content per D-02 boundary rules (the rule sentences are the load-bearing closing line of each block).
3. Replace/extend the existing `<examples>` block with the 11 redacted few-shots from `85-CORPUS.md` slots A-I (B=2 examples). Order: keep the 3 existing examples (copy_doc, address_change, general_inquiry) first, append the 8+ new ones grouped by intent. Each `<example>` includes: `<input>` (assembled-input shape mirroring assemble-input.ts output), `<output>` (a JSON literal matching the V3 schema — for closed-list-fitting examples `intent_proposal: null`, `proposal_reason: null`; for slot I `intent_proposal: "wka_data_request"` + reason starting `"No closed-list intent fits because ..."`).
4. Add an `<output_format>` rule (RESEARCH §5 step 2, R-01 mitigation): *"When `intent_proposal` is non-null, `proposal_reason` MUST begin verbatim with: `No closed-list intent fits because`. If you cannot complete that sentence honestly, set `intent_proposal` to null and `proposal_reason` to null."*
5. Bump every existing `intent_version` literal in the prompt (e.g. the few-shot `<output>` blocks) from `2026-05-01.v2` to `2026-05-19.v3`. **Byte-identical to `INTENT_VERSION_V3` in types.ts.**

Section 2: paste the V3 `json_schema.schema` JSON (anyOf-nullable per CLAUDE.md learning 3970bad9; see `<interfaces>` block above for exact shape). Use `proposal_reason.pattern` as belt-and-braces per RESEARCH §5 step 1 — the prompt rule is the load-bearing enforcement, the schema pattern is best-effort, and the Zod schema in types.ts is intentionally OFF the prefix regex until smoke confirms behaviour (per Plan 02 Task 1 decision).

Section 3: leave empty `## list_models output (Task 2)`, `## get_agent verification (Task 3 post-PATCH)`, `## Smoke #1`, `## Smoke #2`, `## Smoke #3` headers — populated by Task 2 + Task 4.

Per PATTERNS §Security: every few-shot body in the prompt must be PII-redacted AND injection-payload-scrubbed (debtor instruction-strings that could re-target the LLM). The `85-CORPUS.md` from Plan 01 already enforces this; verify nothing slipped through during copy-paste.
  </action>
  <verify>
    <automated>test -s .planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-AGENT-RITUAL-LOG.md && grep -c "<example>" .planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-AGENT-RITUAL-LOG.md | awk '$1>=11{exit 0}{exit 1}' && grep -q "2026-05-19.v3" .planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-AGENT-RITUAL-LOG.md && grep -q "anyOf" .planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-AGENT-RITUAL-LOG.md</automated>
  </verify>
  <done>85-AGENT-RITUAL-LOG.md exists with prompt v3 string (≥11 examples, intent_version=2026-05-19.v3) + V3 json_schema (anyOf-nullable) + empty headers for Task 2/4 to fill.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs list_models pre-flight + Studio JSON Schema bump + update_agent PATCH + get_agent verify</name>
  <files>.planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-AGENT-RITUAL-LOG.md</files>
  <action>Operator-driven deploy ritual; see <how-to-verify> for the 4-step procedure (list_models → Studio JSON Schema bump → update_agent PATCH → get_agent verify). All steps and outputs recorded verbatim in 85-AGENT-RITUAL-LOG.md.</action>
  <verify>get_agent excerpt pasted under `## get_agent verification` showing V3 instructions + V3 response_format reference.</verify>
  <done>list_models excerpt + get_agent excerpt recorded in 85-AGENT-RITUAL-LOG.md; operator confirms V3 instructions + V3 json_schema are persisted on the live agent.</done>
  <what-built>
Prompt v3 string + V3 json_schema JSON authored in 85-AGENT-RITUAL-LOG.md, ready to deploy.
  </what-built>
  <how-to-verify>
**Per CLAUDE.md learnings (f980a2a1, 3970bad9, cba7352b, feedback_orq_update_agent_key_vs_id) — strict ritual:**

1. **list_models pre-flight** — operator runs `mcp__orqai-mcp__list_models` and confirms the agent's primary model (currently `anthropic/claude-sonnet-4-5-20250929` per the live agent state — operator confirms) + all fallback IDs appear in the catalog. Paste the relevant excerpt under `## list_models output (Task 2)` in 85-AGENT-RITUAL-LOG.md. **If primary model is no longer in catalog → STOP — pick a still-valid Sonnet 4.5 ID per CLAUDE.md Anthropic routing table.**

2. **Studio JSON Schema resource bump** — operator opens https://my.orq.ai/cura/agents/01KQECK191GE21CH8D8KEMTM9J → Tools tab → JSON Schema. Either:
   - Update the existing V2 schema resource in place (preferred — Studio keeps the agent's response_format reference intact), OR
   - Add a new resource named `debtor-intent-agent-output-v3` and switch the agent's Response Format dropdown to it.
   Paste the exact JSON from 85-AGENT-RITUAL-LOG.md Section 2. **Studio rejects `type:["string","null"]` — use anyOf only.**

3. **update_agent PATCH** — operator runs:
   ```
   mcp__orqai-mcp__update_agent({
     key: "debtor-intent-agent",   // HUMAN-READABLE KEY, NOT slug ID
     instructions: <prompt v3 from Section 1>,
     model: {
       parameters: {
         // full block — temperature, max_tokens, response_format, etc.
         // PATCH overwrites what is passed; missing keys may null-out per cba7352b.
         response_format: { type: "json_schema", json_schema: { name: "debtor-intent-agent-output-v3" } },
         // include all existing parameters from the current get_agent verbatim
       },
     },
   })
   ```
   Use the agent KEY, not the slug — generic "Failed to update agent" symptom otherwise.

4. **get_agent verify** — operator runs `mcp__orqai-mcp__get_agent({ key: "debtor-intent-agent" })`. Confirm:
   - `instructions` contains `<intent_definitions>` block.
   - `instructions` contains ≥11 `<example>` blocks.
   - `instructions` contains the string `2026-05-19.v3` in every few-shot output literal.
   - `model.parameters.response_format` is populated (NOT null — cba7352b would null this out on create_agent; update_agent persists what was passed).
   - `model.parameters.response_format.json_schema.name` references the V3 schema resource.
   Paste the relevant excerpt under `## get_agent verification` in 85-AGENT-RITUAL-LOG.md.

**If get_agent shows null response_format or stale V2 schema name:** stop, do NOT proceed to smokes. Re-PATCH with the full model.parameters block (cba7352b root cause).
  </how-to-verify>
  <resume-signal>Type "approved" after pasting list_models + get_agent excerpts into 85-AGENT-RITUAL-LOG.md, or describe the issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Author phase85-smoke-v3.ts replayable smoke harness</name>
  <files>web/scripts/phase85-smoke-v3.ts</files>
  <action>
Mirror `web/scripts/phase69-dispatch-smoke.ts` for env loading + JSON dump + exit codes. The script:

1. Reads `ORQ_API_KEY` from `web/.env.local`.
2. Reads `EMAIL_ID` (single mode) or `EMAIL_IDS` csv (regression mode, triggered by `--regression` arg).
3. For each email_id: query Supabase via service-role (env: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`) for the email row, then call `assembleInput` (from `web/lib/automations/debtor-email/coordinator/assemble-input.ts`) to build the prompt input the same way the production coordinator does.
4. Invoke `invokeIntentAgent` (from `web/lib/automations/debtor-email/coordinator/invoke-intent.ts`) — this routes through the same transport as production and gives the V3-aware tolerant parser a workout.
5. Pretty-print `{ email_id, ranked: output.ranked, intent_version: output.intent_version, intent_proposal: output.intent_proposal ?? null, proposal_reason: output.proposal_reason ?? null }`.
6. In `--regression` mode: alongside each row, look up the email's previous V2 top-1 from `coordinator_runs` (via Supabase) and print `{ email_id, v2_top1, v3_top1, changed: <bool>, v3_intent_proposal }`. At the end, print `summary: { total: N, changed: M, threshold: 1 }` and exit non-zero if `M > 1`.

Per RESEARCH §3 — this is a one-off tsx script, NOT an npm test. It runs against the LIVE deployed agent. Imports the production transport so the V3 Zod gate is exercised end-to-end.

Per PATTERNS §Security V4: do NOT log `ORQ_API_KEY` or `SUPABASE_SERVICE_ROLE_KEY` to stdout. Only the response body + the email_id summary.

Run command (document in script header):
```
cd web && ORQ_API_KEY=$(grep '^ORQ_API_KEY=' .env.local | cut -d= -f2- | tr -d '"') \
  EMAIL_ID=<wka-uuid-from-corpus-slot-I> npx tsx scripts/phase85-smoke-v3.ts

cd web && EMAIL_IDS=<csv-from-85-REGRESSION-BASELINE.md> \
  npx tsx scripts/phase85-smoke-v3.ts --regression
```
  </action>
  <verify>
    <automated>test -f web/scripts/phase85-smoke-v3.ts && cd web && npx tsc --noEmit scripts/phase85-smoke-v3.ts 2>&1 | grep -v "node_modules" | head -20 && echo TYPECHECK-OK</automated>
  </verify>
  <done>Script type-checks clean; supports single-email + --regression modes; never logs secrets.</done>
</task>

<task type="auto">
  <name>Task 4: Run 3 smokes — record verbatim in 85-AGENT-RITUAL-LOG.md</name>
  <files>.planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-AGENT-RITUAL-LOG.md</files>
  <action>
Run three smokes using the harness from Task 3. Paste each response verbatim under its section in 85-AGENT-RITUAL-LOG.md.

**Smoke #1 — CONTEXT verification #2 (R-02 mitigation — novel-intent positive):**
- `EMAIL_ID=<slot I WKA-Breman email_id from 85-CORPUS.md>`
- Expectation: `intent_proposal` is non-null with a semantically reasonable snake_case label (e.g. `wka_data_request`, `chain_liability_data_request`, or similar). `proposal_reason` starts with `"No closed-list intent fits because"` (or is null IFF `intent_proposal` is null, per Zod refine in types.ts).
- **PASS if:** intent_proposal matches `^[a-z][a-z0-9_]*$`, intent_proposal label is recognisable as the WKA/chain-liability domain (operator judgment).
- **FAIL action:** if intent_proposal is null on this novel email → over-restrictive prompt → revisit `<output_format>` rule wording in Task 1 and re-PATCH.

**Smoke #2 — CONTEXT verification #3 (R-01 mitigation — no false novelty):**
- `EMAIL_ID=<a clean copy_document_request email_id; pick from 85-CORPUS.md slot A's neighbours that is firmly copy_doc, OR use the original existing few-shot's matching production email>`
- Expectation: `intent_proposal` is null. `proposal_reason` is null.
- **PASS if:** both null AND ranked top-1 is `copy_document_request`.
- **FAIL action:** if intent_proposal is non-null on a routine copy-doc → over-eager prompt → strengthen `<output_format>` rule and re-PATCH.

**Smoke #3 — CONTEXT verification #4 (disambiguation regression):**
- `EMAIL_IDS=<csv from 85-REGRESSION-BASELINE.md>` with `--regression` flag.
- Expectation: ≤1 of 12 (or fallback set) changes top-1 closed-list intent. The 12-email set was payment_dispute under V2; if a few re-rank to `credit_request` due to D-02 sharpening, that is the *intended* boundary correction, but the threshold for "we did not break what works" is ≤1.
- **PASS if:** `summary.changed ≤ 1`.
- **FAIL action:** if 2+ changes → D-02 boundary rules in prompt are pulling too hard → re-check the boundary sentences in `<intent_definitions>`, particularly `payment_dispute` vs `credit_request`. Re-PATCH and re-smoke.

**Per RESEARCH §5 step 4 — pattern enforcement smoke (optional belt-and-braces):**
After the 3 mandatory smokes, optionally invoke one more time with an email designed to trigger non-null proposal but instruct (via the prompt's regression-test variable if any) to omit the prefix — see whether the Orq validator rejects or the model self-corrects. Record the outcome in a final `## Pattern enforcement observation` block. This is non-blocking for Plan 03 sign-off but informs whether to add the Zod regex mirror in a Plan 04 follow-up.

After all smokes pass, append a `## Sign-off` block: timestamp, operator name, "ALL 3 SMOKES GREEN — V3 LIVE." This is the gate Plan 04 reads.
  </action>
  <verify>
    <automated>grep -q "ALL 3 SMOKES GREEN" .planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-AGENT-RITUAL-LOG.md</automated>
  </verify>
  <done>All 3 smokes recorded verbatim in 85-AGENT-RITUAL-LOG.md with PASS verdict; sign-off line present.</done>
</task>

</tasks>

<verification>
- 85-AGENT-RITUAL-LOG.md contains: list_models excerpt, get_agent excerpt showing V3 instructions + V3 response_format, 3 smoke responses, sign-off block.
- phase85-smoke-v3.ts committed; tsc clean.
- Live agent `debtor-intent-agent` returns V3 outputs (intent_version literal in response body = `2026-05-19.v3`).
- Smoke #3 regression `changed ≤ 1` — recorded in log.
</verification>

<deviation_rules>
- DO NOT use `create_agent` — the agent already exists; `create_agent` would (a) duplicate and (b) silently drop response_format per cba7352b.
- DO NOT pass the slug ID `01KQECK191GE21CH8D8KEMTM9J` to update_agent — use the key `debtor-intent-agent` (feedback_orq_update_agent_key_vs_id).
- DO NOT use `type:["string","null"]` anywhere in the json_schema — anyOf only (3970bad9).
- DO NOT PATCH without first running list_models — unknown model IDs are silently accepted and break Studio (f980a2a1).
- DO NOT PATCH model.parameters with a partial payload — pass the full block including response_format (cba7352b).
- DO NOT add the `proposal_reason` prefix regex to Zod in types.ts in this plan — that decision is deferred to a Plan 04 follow-up pending smoke evidence.
- DO NOT log ORQ_API_KEY or SUPABASE_SERVICE_ROLE_KEY in the smoke script's stdout.
- DO NOT skip Smoke #2 (no-false-novelty) — it is R-01's only regression gate.
- DO NOT touch stage-3-dispatcher.ts (D-05).
</deviation_rules>

<out_of_scope>
- Operator volume monitoring / token-cost dashboards (Plan 04 owns the post-deploy watch).
- V2 retirement (Phase 86+ follow-up; 14-day window per RESEARCH Open Q #1).
- Phase 86 proposal-capture surface.
- Promotion of proposals to swarm_intents (V9.0).
- Sales-email agent (V10.0).
- Anthropic prompt-cache enablement (RESEARCH §4 follow-up TODO if volume > 10k/mo).
</out_of_scope>

<success_criteria>
- get_agent shows V3 instructions + V3 json_schema literal.
- Smoke #1: intent_proposal non-null + valid snake_case + WKA-domain label.
- Smoke #2: intent_proposal null on clean copy_doc.
- Smoke #3: ≤1/12 top-1 changes vs V2 baseline.
- Sign-off recorded in 85-AGENT-RITUAL-LOG.md.
</success_criteria>

<output>
After completion, create `.planning/phases/85-stage-3-prompt-v3-intent-definitions-and-open-set-schema/85-03-SUMMARY.md` summarising: agent state pre/post PATCH, smoke verdicts, any post-PATCH adjustments needed, and the open question of whether to add Zod prefix-regex mirror in a follow-up (based on Smoke #4 pattern observation if run).
</output>
