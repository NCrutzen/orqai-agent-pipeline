---
phase: 05.2-fix-tool-catalog-and-pipeline-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/references/tool-catalog.md
  - orq-agent/agents/tool-resolver.md
  - orq-agent/commands/orq-agent.md
  - orq-agent/agents/orchestration-generator.md
  - orq-agent/agents/researcher.md
autonomous: true
requirements:
  - TOOL-02
  - ORCH-01
  - ARCH-04

must_haves:
  truths:
    - "tool-catalog.md uses correct memory tool identifiers matching orqai-agent-fields.md source of truth"
    - "tool-resolver.md valid types list uses correct memory tool identifiers and includes all memory-related tools"
    - "Orchestrator Wave 3 passes research brief path to both orchestration generator and README generator"
    - "Orchestration generator validates that research brief contains KB Design data before generating KB Design sections"
    - "Researcher agent has access to tool-catalog.md for grounding KB tool recommendations"
  artifacts:
    - path: "orq-agent/references/tool-catalog.md"
      provides: "Corrected memory tool identifiers with source-of-truth note"
      contains: "write_memory_store"
    - path: "orq-agent/agents/tool-resolver.md"
      provides: "Corrected valid types list with all memory tools"
      contains: "write_memory_store"
    - path: "orq-agent/commands/orq-agent.md"
      provides: "Research brief wiring to orchestration generator and README generator"
      contains: "research-brief.md"
    - path: "orq-agent/agents/orchestration-generator.md"
      provides: "KB Design input validation gate"
      contains: "KB Design Input Validation"
    - path: "orq-agent/agents/researcher.md"
      provides: "tool-catalog.md in files_to_read"
      contains: "tool-catalog.md"
  key_links:
    - from: "orq-agent/references/tool-catalog.md"
      to: "orq-agent/references/orqai-agent-fields.md"
      via: "identifier alignment"
      pattern: "write_memory_store|delete_memory_document"
    - from: "orq-agent/commands/orq-agent.md"
      to: "orq-agent/agents/orchestration-generator.md"
      via: "research brief path in spawn instructions"
      pattern: "research-brief\\.md"
---

<objective>
Fix wrong memory tool identifiers in tool-catalog.md and tool-resolver.md, wire research brief path to Wave 3 orchestration generator and README generator spawn instructions, add KB Design input validation gate in orchestration-generator.md, and add tool-catalog.md to researcher.md files_to_read.

Purpose: Close gaps from v0.3 audit — ensure built-in tool recommendations use correct Orq.ai identifiers and KB use cases produce complete orchestration output with validated research data.
Output: Five corrected markdown prompt files with accurate tool identifiers, complete pipeline wiring, and a KB Design validation gate.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-fix-tool-catalog-and-pipeline-wiring/05.2-CONTEXT.md
@.planning/phases/05.2-fix-tool-catalog-and-pipeline-wiring/05.2-RESEARCH.md

@orq-agent/references/orqai-agent-fields.md (source of truth for tool identifiers)
@orq-agent/references/tool-catalog.md
@orq-agent/agents/tool-resolver.md
@orq-agent/commands/orq-agent.md
@orq-agent/agents/orchestration-generator.md
@orq-agent/agents/researcher.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix memory tool identifiers in tool-catalog.md and tool-resolver.md</name>
  <files>orq-agent/references/tool-catalog.md, orq-agent/agents/tool-resolver.md</files>
  <action>
**tool-catalog.md changes:**

1. Add a source-of-truth note at the top of the Built-in Tools section (before the table). Wording along the lines of:
   `> **Source of truth:** All built-in tool identifiers below must match \`orqai-agent-fields.md\`. When in doubt, defer to that reference.`

2. Fix the two wrong memory tool identifiers in the Built-in Tools table:
   - `write_to_memory_store` → `write_memory_store`
   - `delete_from_memory_store` → `delete_memory_document`

3. Add the two missing memory tools to the Built-in Tools table (they exist in orqai-agent-fields.md but are absent from tool-catalog.md):
   - `retrieve_memory_stores` — Discover available memory stores — use when: "what memory stores exist", "list stores", memory store discovery
   - `query_memory_store` — Search stored information — use when: "recall", "what did we discuss", memory retrieval
   Place these near the other memory tools for logical grouping.

**tool-resolver.md changes:**

4. Fix the valid types list (line 29) to use correct identifiers:
   - `write_to_memory_store` → `write_memory_store`
   - `delete_from_memory_store` → `delete_memory_document`

5. Add the two missing memory tools to the valid types list:
   - `retrieve_memory_stores`
   - `query_memory_store`
   Group all memory tools together in the list for readability, matching the grouping in researcher.md line 210.

**Verification search:** After all edits, search the entire `orq-agent/` directory for the OLD identifiers `write_to_memory_store` and `delete_from_memory_store`. Zero hits expected — if any remain, fix them.
  </action>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/claude-code-prompt-agent && grep -rn "write_to_memory_store\|delete_from_memory_store" orq-agent/ | wc -l | xargs test 0 -eq && echo "PASS: No old identifiers found" || echo "FAIL: Old identifiers still present"</automated>
  </verify>
  <done>
- tool-catalog.md Built-in Tools table uses `write_memory_store` and `delete_memory_document` (matching orqai-agent-fields.md)
- tool-catalog.md Built-in Tools table includes `retrieve_memory_stores` and `query_memory_store`
- tool-catalog.md has source-of-truth note pointing to orqai-agent-fields.md
- tool-resolver.md valid types list uses `write_memory_store` and `delete_memory_document`
- tool-resolver.md valid types list includes `retrieve_memory_stores` and `query_memory_store`
- Zero grep hits for old identifiers across entire orq-agent/ directory
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire research brief to Wave 3 subagents and add KB Design validation gate</name>
  <files>orq-agent/commands/orq-agent.md, orq-agent/agents/orchestration-generator.md, orq-agent/agents/researcher.md</files>
  <action>
**orq-agent.md changes (Wave 3 spawn instructions):**

1. Update the Orchestration Generator spawn section (around lines 541-544) to include research brief path. Change from:
   ```
   - **Input:** Pass the blueprint path and all successfully generated agent spec file paths
   ```
   To a numbered list mirroring the dataset generator pattern:
   ```
   - **Input:** Pass the following file paths:
     1. Blueprint: `{OUTPUT_DIR}/[swarm-name]/blueprint.md`
     2. Research brief: `{OUTPUT_DIR}/[swarm-name]/research-brief.md` (or note if skipped/failed)
     3. All successfully generated agent spec file paths
   ```

2. Update the README Generator spawn section (around lines 556-562) to include research brief path. Add it as item 2 in the existing numbered list, shifting existing items 2-4 to 3-5:
   ```
   - **Input:** Pass all generated file paths:
     1. Blueprint: `{OUTPUT_DIR}/[swarm-name]/blueprint.md`
     2. Research brief: `{OUTPUT_DIR}/[swarm-name]/research-brief.md` (or note if skipped/failed)
     3. All agent spec paths: `{OUTPUT_DIR}/[swarm-name]/agents/*.md`
     4. Orchestration doc path: `{OUTPUT_DIR}/[swarm-name]/ORCHESTRATION.md` (if multi-agent)
     5. Dataset file list: all files in `{OUTPUT_DIR}/[swarm-name]/datasets/`
   ```

**orchestration-generator.md changes:**

3. Add a "KB Design Input Validation" section as a NEW dedicated section BEFORE the existing KB Design generation section (which starts around line 256). Do NOT add this to the existing Pre-Output Validation checklist (line 564+). Content:

   ```markdown
   ## KB Design Input Validation

   Before generating the Knowledge Base Design section, validate that the research brief contains KB Design data for every agent that needs a knowledge base.

   **Validation process:**
   1. Identify all agents in the blueprint that have `Knowledge base` set to something other than `none`
   2. For each KB-needing agent, check the research brief for a "Knowledge Base Design" section covering that agent's KB
   3. If ANY KB-needing agent lacks KB Design data in the research brief: STOP and report an error

   **Error behavior:** If validation fails, write the following to the ORCHESTRATION.md output instead of generating KB Design:

   ## Knowledge Base Design

   **ERROR: Research brief missing KB Design data.**

   The following agents require knowledge bases but no KB Design data was found in the research brief:
   - [list agent keys missing KB Design data]

   Re-run the pipeline with research enabled to generate KB Design recommendations before producing orchestration output.

   Do NOT generate KB Design sections with heuristic defaults when research data is missing. The researcher is the authoritative source for KB Design recommendations.
   ```

4. Add a note in the orchestration-generator.md input contract section (the "You receive" preamble around line 30-33) clarifying that the research brief is a REQUIRED runtime input (it is already listed there at line 33 but reinforce that it must actually be passed). Add a parenthetical like "(required — passed by orchestrator at spawn time)" after the research brief bullet.

**researcher.md changes:**

5. Add `orq-agent/references/tool-catalog.md` to the researcher.md `<files_to_read>` block (around lines 8-12), after the existing entries. This gives the researcher access to the full tool catalog for grounding KB tool recommendations.
  </action>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/claude-code-prompt-agent && grep -c "research-brief.md" orq-agent/commands/orq-agent.md | xargs test 2 -le && grep -c "KB Design Input Validation" orq-agent/agents/orchestration-generator.md | xargs test 1 -eq && grep -c "tool-catalog.md" orq-agent/agents/researcher.md | xargs test 1 -le && echo "PASS: All wiring and validation in place" || echo "FAIL: Missing wiring or validation"</automated>
  </verify>
  <done>
- orq-agent.md orchestration generator spawn includes research brief path (or skip note) in input list
- orq-agent.md README generator spawn includes research brief path (or skip note) in input list
- orchestration-generator.md has a dedicated "KB Design Input Validation" section BEFORE the KB Design generation section (NOT in the Pre-Output Validation checklist)
- orchestration-generator.md input contract notes research brief as required runtime input
- researcher.md files_to_read includes tool-catalog.md
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "write_to_memory_store\|delete_from_memory_store" orq-agent/` returns zero hits
2. `grep -n "write_memory_store" orq-agent/references/tool-catalog.md` returns at least one hit
3. `grep -n "delete_memory_document" orq-agent/references/tool-catalog.md` returns at least one hit
4. `grep -n "retrieve_memory_stores" orq-agent/references/tool-catalog.md` returns at least one hit
5. `grep -n "query_memory_store" orq-agent/references/tool-catalog.md` returns at least one hit
6. `grep -c "research-brief.md" orq-agent/commands/orq-agent.md` returns 2 or more (orchestration gen + README gen)
7. `grep -c "KB Design Input Validation" orq-agent/agents/orchestration-generator.md` returns 1
8. `grep -c "tool-catalog.md" orq-agent/agents/researcher.md` returns 1 or more
</verification>

<success_criteria>
- All built-in tool identifiers in tool-catalog.md and tool-resolver.md match orqai-agent-fields.md exactly
- All memory-related tools (retrieve_memory_stores, query_memory_store, write_memory_store, delete_memory_document) are listed in both tool-catalog.md and tool-resolver.md
- Wave 3 orchestration generator and README generator both receive research brief path from orchestrator
- Orchestration generator validates research brief KB Design data presence before generating KB Design sections
- Researcher agent can access tool-catalog.md for grounding recommendations
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-fix-tool-catalog-and-pipeline-wiring/05.2-01-SUMMARY.md`
</output>
