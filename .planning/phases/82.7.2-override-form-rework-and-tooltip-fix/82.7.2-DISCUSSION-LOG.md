---
phase: 82.7.2-override-form-rework-and-tooltip-fix
log_type: discussion
created: 2026-05-18
purpose: Audit trail of /gsd-discuss-phase Q&A. NOT consumed by downstream agents — see CONTEXT.md for locked decisions.
---

# Phase 82.7.2: Discussion Log

CONTEXT.md was drafted on 2026-05-18 directly from operator UAT findings
(no Q&A — Claude captured F-01..F-04 with embedded resolution paths). The
`/gsd-discuss-phase 82.7.2` invocation locked the remaining gray areas (F-02
strategy + F-04 disposition). F-01 (investigate-then-fix only path) and F-03
(operator's 5-item list locked verbatim) needed no Q&A.

## Q1 — F-02 strategy (only 3 of ~25 rows show colored dot)

**Options presented:**
1. **Investigate first, then decide** — query database for entity_brand
   distribution before any UI change. Result drives sub-plan.
2. Neutral muted dot fallback (no investigation) — ship a fallback dot, skip
   root cause.
3. Expand 5-brand registry — add more brands per operator input.
4. Drop the brand-color dot entirely.

**Operator selected:** Option 1 (investigate first).

**Why this matters downstream:** Plan 02 is a read-only Supabase query
(no code change). Plan output is a findings doc that drives a follow-on
plan (registry expand, neutral fallback, or stage-1 enrichment fix). G-04
explicitly forbids pre-emptive fallback shipping.

## Q2 — F-04 (em-dash safety guard from 82.7.1-05)

**First attempt:** Question was too abstract — operator response was
"I'm not quite sure what we are doing here." Claude reframed.

**Reframed question:** Plain language explanation that the em-dash is a
defensive guard for null chip labels, never visibly fired in current UAT.

**Options presented (after reframe):**
1. **Leave it alone (Recommended)** — keep the guard, no action.
2. Delete it — cleanup, less code.
3. Keep + log when it fires — see if it ever triggers in production.

**Operator selected:** Option 1 (leave it alone).

**Why this matters downstream:** F-04 closes with no plan, no code change.
G-10 documents the disposition. No log instrumentation added.

## Pre-locked (no Q&A needed)

### F-01 — Brand-color tooltip not firing
Only one path: investigate the Radix Tooltip render chain and fix whatever
is broken (Provider missing, hit target wrong, portal mount issue). No user
decision because there's nothing to choose. Locked as G-01.

### F-03 — Override form structural rework
Operator provided a complete 5-item bulleted list in the UAT message
(consolidate buttons, remove redundant label, fix color semantics, remove
"Override in progress", refine helper text). Each item is concrete and
non-ambiguous. Locked verbatim as G-05..G-09. Wireframe target added to
CONTEXT for visual clarity.

## Scope creep handled

None. Operator stayed within the 4 captured defects.

## Deferred (per CONTEXT.md)

- E-01 / E-02 — no pushback, assumed working.
- 999.8 item 7 — separate operator session.
- 5-brand registry expansion — gated on F-02 query result.
- Stage 1 entity_brand enrichment gap — separate phase if F-02 reveals it.
