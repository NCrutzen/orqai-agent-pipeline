---
phase: 82.7.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx
autonomous: true
requirements: [F-03]
tags: [ui, override-form, stage-1, polish]

must_haves:
  truths:
    - "The override form action row shows Cancel (ghost) and Submit (lime) on a single horizontal row."
    - "The orange/amber duplicate label 'Submit Stage 1 override' is gone."
    - "The 'Override in progress' status line is gone."
    - "The '⤓ Override + note save together' line is replaced by a muted-gray 'Note and override are saved together' hint."
    - "No var(--v7-amber) remains on non-warning text inside the dirty-branch override surface in stage-step.tsx."
    - "The lime Submit fill (background: var(--v7-lime)) from 82.7.1-04 is preserved on the Submit override button."
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx"
      provides: "Reworked dirty-branch override surface per G-05..G-09"
      contains: "Note and override are saved together"
  key_links:
    - from: "stage-step.tsx dirty branch action row"
      to: "stage-step.tsx onSubmitDirty / onCancelDirty handlers"
      via: "Single grouped action row (Cancel left, Submit right)"
      pattern: "onSubmitDirty\\?\\(\\)|onCancelDirty\\?\\(\\)"
---

<objective>
Apply the operator-locked F-03 override form rework (G-05..G-09) to the
dirty-branch render in `stage-step.tsx`. All five F-03 strings live in
this file (PATTERNS.md confirmed via grep); the `_shell` widget was a
red herring in the CONTEXT brief.

Purpose: Operator UAT 2026-05-18 surfaced that the override form is
"clunky with a lot of text and colors". The button styling fix from
82.7.1-04 was correct but too narrow — the surrounding amber text +
duplicate labels + arrow glyph were left untouched. Strip them out.

Output: Single-file edit to `stage-step.tsx` that (a) consolidates
Cancel+Submit on one row, (b) deletes the orange "Submit Stage N
override" duplicate label, (c) deletes the "Override in progress" line,
(d) rewrites the helper text with no arrow glyph and muted color, (e)
removes amber from all non-warning text on this surface.

**Regression guard:** the lime Submit fill landed by 82.7.1-04 at
`stage-step.tsx:268` (`background: "var(--v7-lime)"`) MUST survive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/82.7.2-override-form-rework-and-tooltip-fix/82.7.2-CONTEXT.md
@.planning/phases/82.7.2-override-form-rework-and-tooltip-fix/82.7.2-PATTERNS.md
@.planning/phases/82.7.1-detail-pane-uat-followups/82.7.1-04-SUMMARY.md
@web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx
</context>

<interfaces>
<!-- Current dirty-branch render in stage-step.tsx (L227-319) shape: -->

`stage.state === "dirty"` branch currently renders, in order:
1. `{stage.widget}` (the Select dropdown — DO NOT TOUCH)
2. A muted-amber helper div with `⤓ Override + note save together` (L230-243)
3. A flex row with amber label "Submit Stage N override" + lime Submit button (L251-290)
4. A flex row with amber label "Override in progress" + ghost "cancel override" link (L297-318)

Target shape after this plan:
1. `{stage.widget}` (UNCHANGED)
2. A muted-gray helper line `Note and override are saved together` (replaces #2 above; same `data-testid="override-coupling-helper-picker"` preserved)
3. A SINGLE flex row with Cancel (ghost outline, left) + Submit (lime, right) — collapses old #3 and #4 into one row. Keep both data-testids (`stage-{n}-submit-override`, `stage-{n}-cancel-override`). Drop both amber `<span>` labels.

Preserved tokens / styles on Submit button (DO NOT CHANGE):
- `background: "var(--v7-lime)"` (regression guard)
- `border: "1px solid var(--v7-lime)"`
- `borderRadius: "var(--v7-radius-pill)"`
- `color: "var(--v7-bg)"`
- `<kbd>⌘⏎</kbd>` keychip

Cancel button new style (mirror StageFeedbackPanel ghost pattern + the existing brand-secondary underline color the current `cancel override` link uses):
- `background: "transparent"`
- `border: "1px solid var(--v7-brand-secondary)"`
- `borderRadius: "var(--v7-radius-pill)"`
- `color: "var(--v7-brand-secondary)"`
- `padding: "6px 14px"`, `fontFamily: "var(--font-mono)"`, `fontSize: 12`
- Label text: `cancel override` (preserve casing; no change to verb)
- `aria-label`: keep `Cancel override on Stage ${stage.n}`
- `data-testid`: keep `stage-${stage.n}-cancel-override`
- `onClick={() => onCancelDirty?.()}` preserved
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rework stage-step dirty-branch — strip amber labels, consolidate action row, rewrite helper text</name>
  <files>web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx (full file; the dirty branch lives at ~L227-319)
    - .planning/phases/82.7.2-override-form-rework-and-tooltip-fix/82.7.2-PATTERNS.md (F-03 section)
    - .planning/phases/82.7.1-detail-pane-uat-followups/82.7.1-04-SUMMARY.md (regression guard — lime Submit fill MUST survive)
  </read_first>
  <behavior>
    - Dirty-branch render keeps `{stage.widget}` and `onMarkDirty`/`onSubmitDirty`/`onCancelDirty` semantics intact (no handler signature change).
    - The orange "Submit Stage N override" label text is removed (G-06).
    - The orange "Override in progress" label text is removed (G-08).
    - The "⤓ Override + note save together" line is replaced by a muted-gray "Note and override are saved together" line, no leading glyph (G-09). Color: `var(--v7-muted)`.
    - Cancel and Submit buttons sit on a single horizontal flex row, Cancel on the left, Submit on the right, justified-end (G-05).
    - Both buttons keep their existing data-testids and aria-labels.
    - No `var(--v7-amber)` color appears anywhere in the dirty-branch render after edit (G-07). The amber on `dotColor()` at L67 stays — that is the per-stage status dot for the dirty state, which IS warning semantics.
  </behavior>
  <action>
    Apply per D-G-05..G-09 to `web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx` dirty branch:

    1. **G-09 helper text rewrite** — Replace L230-243 helper div content:
       - Change text from `⤓ Override + note save together` to `Note and override are saved together`.
       - Change `color: "var(--v7-amber)"` to `color: "var(--v7-muted)"`.
       - Keep `data-testid="override-coupling-helper-picker"`, `marginTop: 4`, `fontFamily: "var(--font-mono)"`, `fontSize: 11`.
       - Drop the leading `⤓` glyph entirely.

    2. **G-05 + G-06 + G-08 collapse two rows into one** — Replace the two existing `<div className="flex items-center justify-between gap-3 mt-1">` blocks (L251-290 Submit row, L297-318 cancel-override row) with a SINGLE action row:
       ```
       <div className="flex items-center justify-end gap-2 mt-1">
         <button
           type="button"
           onClick={() => onCancelDirty?.()}
           aria-label={`Cancel override on Stage ${stage.n}`}
           data-testid={`stage-${stage.n}-cancel-override`}
           style={{
             display: "inline-flex",
             alignItems: "center",
             gap: 6,
             padding: "6px 14px",
             background: "transparent",
             border: "1px solid var(--v7-brand-secondary)",
             borderRadius: "var(--v7-radius-pill)",
             color: "var(--v7-brand-secondary)",
             cursor: "pointer",
             fontFamily: "var(--font-mono)",
             fontSize: 12,
           }}
         >
           cancel override
         </button>
         <button
           type="button"
           onClick={() => onSubmitDirty?.()}
           aria-label={`Submit override on Stage ${stage.n}`}
           data-testid={`stage-${stage.n}-submit-override`}
           style={{
             display: "inline-flex",
             alignItems: "center",
             gap: 6,
             padding: "6px 14px",
             background: "var(--v7-lime)",
             border: "1px solid var(--v7-lime)",
             borderRadius: "var(--v7-radius-pill)",
             color: "var(--v7-bg)",
             cursor: "pointer",
             fontFamily: "var(--font-mono)",
             fontWeight: 600,
             fontSize: 12,
           }}
         >
           <span data-row="primary">Submit override</span>
           <kbd
             style={{
               fontFamily: "var(--font-mono)",
               fontSize: 10,
               opacity: 0.7,
               marginLeft: 4,
             }}
           >
             ⌘⏎
           </kbd>
         </button>
       </div>
       ```
       This:
       - Deletes the orange `<span>Submit Stage {stage.n} override</span>` label (G-06).
       - Deletes the orange `<span>Override in progress</span>` label (G-08).
       - Puts Cancel and Submit on the same row (G-05).
       - Preserves the lime Submit fill (regression guard from 82.7.1-04).
       - Preserves both data-testids (existing tests rely on these — do not rename).
       - Drops `var(--v7-amber)` from the action row (G-07).

    3. **DO NOT TOUCH**:
       - Line 67 `if (state === "dirty") return "var(--v7-amber)";` — this is the per-stage dot color (warning semantics, legitimate amber use).
       - The `state === "ok"` branch (L194-225 area with "override stage" button).
       - The `state === "skipped"` branch (L321+).
       - `eval-type-radio.tsx` or any other file.
       - The `<kbd>⌘⏎</kbd>` keychip styling.
       - Per D-09 from G-09 footnote: muted-gray helper text placement = directly under the dropdown (existing position, just rewritten copy), not above the action row. This matches the locked wireframe in CONTEXT.md.

    4. After edits, run the verification greps in `<verify>` below.
  </action>
  <verify>
    <automated>
    cd /Users/nickcrutzen/Developer/agent-workforce &amp;&amp; \
    test "$(grep -c 'Submit Stage 1 override' 'web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx')" = "0" &amp;&amp; \
    test "$(grep -c 'Override in progress' 'web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx')" = "0" &amp;&amp; \
    test "$(grep -c '⤓ Override + note save together' 'web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx')" = "0" &amp;&amp; \
    test "$(grep -c 'Note and override are saved together' 'web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx')" = "1" &amp;&amp; \
    test "$(grep -c 'background: \"var(--v7-lime)\"' 'web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx')" = "1" &amp;&amp; \
    test "$(grep -c 'data-testid={`stage-${stage.n}-submit-override`}' 'web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx')" = "1" &amp;&amp; \
    test "$(grep -c 'data-testid={`stage-${stage.n}-cancel-override`}' 'web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx')" = "1" &amp;&amp; \
    echo "F-03 grep gates green"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Submit Stage 1 override" stage-step.tsx` returns 0 (G-06 satisfied).
    - `grep -c "Override in progress" stage-step.tsx` returns 0 (G-08 satisfied).
    - `grep -c "⤓ Override + note save together" stage-step.tsx` returns 0 (G-09 leading glyph absent).
    - `grep -c "Note and override are saved together" stage-step.tsx` returns 1 (G-09 replacement copy present).
    - `grep -c 'background: "var(--v7-lime)"' stage-step.tsx` returns 1 (regression guard: 82.7.1-04 lime Submit fill preserved).
    - Both data-testids (`stage-${stage.n}-submit-override`, `stage-${stage.n}-cancel-override`) still present (test contracts preserved).
    - `var(--v7-amber)` count on text properties inside the dirty branch (L227-319 range) is 0 — line 67 dotColor amber stays (warning dot semantics; D-G-07 explicitly allows warning use).
    - No edit to `eval-type-radio.tsx`, `_shell/components/stage-1-widget.tsx`, or any file outside the listed `files_modified`.
  </acceptance_criteria>
  <done>
    `stage-step.tsx` dirty-branch render matches the locked CONTEXT wireframe:
    dropdown → muted "Note and override are saved together" hint → single action row [Cancel (ghost)] [Submit override ⌘⏎ (lime)]. All grep gates green. Regression guard intact.
  </done>
</task>

</tasks>

<verification>
- All grep checks in Task 1 `<automated>` pass.
- `git diff --stat` shows exactly one file changed: `stage-step.tsx`.
- `npx tsc --noEmit` clean for this file (best effort; CI is canonical).
</verification>

<success_criteria>
- F-03 (G-05..G-09) all implemented in a single file edit.
- 82.7.1-04 regression guard intact (lime Submit fill preserved).
- Zero amber-on-text remaining in the dirty-branch action surface.
- No collateral edits.
</success_criteria>

<output>
After completion, create `.planning/phases/82.7.2-override-form-rework-and-tooltip-fix/82.7.2-01-SUMMARY.md` documenting:
- Five G-IDs satisfied (G-05..G-09) with the grep evidence.
- Confirmation that 82.7.1-04 lime Submit fill survived.
- Note that the F-03 strings were in `stage-step.tsx`, not `_shell/components/stage-1-widget.tsx` (CONTEXT brief candidate list was misread; PATTERNS.md grep confirmed).
</output>
