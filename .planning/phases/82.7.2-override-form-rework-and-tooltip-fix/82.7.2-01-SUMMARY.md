---
phase: 82.7.2
plan: 01
subsystem: ui/stage-1
tags: [ui, override-form, stage-1, polish, f-03]
requires:
  - 82.7.1-04 (lime Submit fill — regression guard)
provides:
  - F-03 (G-05..G-09) operator-locked override-form rework
affects:
  - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx
tech-stack:
  added: []
  patterns:
    - "Single-row Cancel(ghost)+Submit(lime) action layout (mirrors StageFeedbackPanel)"
key-files:
  modified:
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx
  created: []
decisions:
  - "F-03 strings live in stage-step.tsx (NOT _shell/components/stage-1-widget.tsx as the CONTEXT candidate list speculated). PATTERNS.md grep already noted the operator-quoted strings were absent from the _shell widget; grep in this plan confirmed all three (`Submit Stage 1 override`, `Override in progress`, `⤓ Override + note save together`) lived in stage-step.tsx dirty branch L230-318."
  - "Per-stage dot color amber at line 67 is a legitimate warning-semantic use (dirty state dot) and was preserved per CONTEXT G-07 footnote."
metrics:
  duration: 4m
  completed: 2026-05-18
---

# Phase 82.7.2 Plan 01: Override form rework (F-03) Summary

One-liner: Stripped duplicate amber labels, consolidated Cancel+Submit into a single ghost+lime action row, and rewrote the helper text to a muted single-line hint inside the Stage-N dirty-branch render of `stage-step.tsx`.

## What shipped

A single-file edit to `web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx` implementing all five F-03 sub-decisions (G-05..G-09).

### Before (L227-319, original)

```
{stage.widget}
├─ amber helper:  "⤓ Override + note save together"
├─ row 1:  [amber span "Submit Stage N override"] [lime Submit ⌘⏎]
└─ row 2:  [amber span "Override in progress"]    [ghost cancel-override link]
```

### After

```
{stage.widget}
├─ muted helper:  "Note and override are saved together"
└─ single row (justify-end):  [ghost-outline "cancel override"]  [lime "Submit override ⌘⏎"]
```

## G-IDs satisfied (with grep evidence)

| G-ID | Decision | Grep gate | Result |
|------|----------|-----------|--------|
| G-05 | Cancel + Submit on a single horizontal row | both buttons under one `<div className="flex items-center justify-end gap-2 mt-1">` | PASS (manual file inspection) |
| G-06 | Remove duplicate "Submit Stage 1 override" label | `grep -c "Submit Stage 1 override" stage-step.tsx` == 0 | PASS |
| G-07 | Amber → muted on non-warning text in dirty branch | only `var(--v7-amber)` reference left in file is L67 `dotColor()` (warning-dot semantics, explicitly allowed) | PASS |
| G-08 | Remove "Override in progress" status line | `grep -c "Override in progress" stage-step.tsx` == 0 | PASS |
| G-09 | Helper rewritten as muted single-line hint, no arrow glyph | `grep -c "⤓ Override + note save together"` == 0 AND `grep -c "Note and override are saved together"` == 1 | PASS |

## Regression guard (82.7.1-04 lime Submit fill)

Confirmed preserved:

- `grep -c 'background: "var(--v7-lime)"' stage-step.tsx` == 1
- Submit-override button retains `border: "1px solid var(--v7-lime)"`, `borderRadius: "var(--v7-radius-pill)"`, `color: "var(--v7-bg)"`, and the `<kbd>⌘⏎</kbd>` keychip.

## Test-contract preservation

Both data-testids retained for any existing RTL / Playwright tests:

- `stage-${stage.n}-submit-override` — present (1 match)
- `stage-${stage.n}-cancel-override` — present (1 match)

Plus the existing `data-testid="override-coupling-helper-picker"` on the muted-helper div (Plan 05 placement contract) was preserved.

## Note on CONTEXT misread (resolved)

CONTEXT.md and PATTERNS.md speculated that the three operator-quoted strings might live in either `_shell/components/stage-1-widget.tsx` or `_shell/detail-pane.tsx` and instructed the planner to grep the parent surface. The 01-PLAN narrowed it correctly to `stage-step.tsx`. This plan confirmed the narrow target via in-file grep before editing — all three F-03 strings were present in the dirty-branch render block at L230-318 of `stage-step.tsx`, none in the `_shell` widget. Plan 01 was the right unit of edit.

## Out-of-scope (untouched)

- Line 67 `nodeBorderColor()` amber for `state === "dirty"` — warning semantics, kept per G-07 footnote.
- `state === "ok"` branch (L183-226) — "override stage" link untouched.
- `state === "skipped"` branch (L321+) — untouched.
- `_shell/components/stage-1-widget.tsx` — no edit (confirmed unaffected).
- `eval-type-radio.tsx`, `predicted-row.tsx`, any tests, any RFC docs.

## Deviations from Plan

None. Plan executed exactly as written (G-05..G-09 applied verbatim; regression guard preserved; no scope creep).

One harmless mid-execution adjustment: the first attempt at the new explanatory comment block in the edit accidentally quoted the strings `"⤓ Override + note save together"` and `"Override in progress"` inside the new JSX comments, which would have left the strict grep gates at counts of 1. Caught immediately by the verify greps; rewrote the comment text to describe the prior copy without quoting it. Final grep gates all passed.

## Commits

- `6324961` — feat(82.7.2-01): rework Stage-N override dirty-branch (G-05..G-09)

## Self-Check

- File modified exists: `web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx` — FOUND
- Commit `6324961` — FOUND in `git log --oneline`
- All 5 grep gates from `<verify><automated>` block — PASS
- Regression grep (lime Submit fill count == 1) — PASS
- `git diff --stat` — exactly 1 file changed (35 insertions, 46 deletions)

## Self-Check: PASSED
