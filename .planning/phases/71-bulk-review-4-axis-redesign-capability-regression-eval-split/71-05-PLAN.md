---
phase: 71-bulk-review-4-axis-redesign-capability-regression-eval-split
plan: 05
type: execute
wave: 3
depends_on: [71-02, 71-03, 71-04]
files_modified:
  - web/app/(dashboard)/automations/[swarm]/review/page.tsx
  - web/app/(dashboard)/automations/[swarm]/review/row-list.tsx
  - web/app/(dashboard)/automations/[swarm]/review/detail-pane.tsx
  - web/app/(dashboard)/automations/[swarm]/review/keyboard-shortcuts.tsx
autonomous: false
requirements: [REVW-01, REVW-02, REVW-03, REVW-04, REVW-05, REVW-06]
tags: [ui-integration, keyboard, smoke, end-to-end]

must_haves:
  truths:
    - "Bulk Review page renders RecipientChipStrip + PredictedRow list driven by view"
    - "Detail pane renders PipelineFlow with 4 StageSteps; each stage's widget mounts when dirty"
    - "EvalTypeRadio + Discard/Submit bar render at bottom of detail pane"
    - "Submit POSTs to /api/automations/debtor-email/override with the correct payload shape (axis-specific)"
    - "Confirm dialog fires for the 3 trigger conditions (Stage 2 re-run, Stage 3, ≥2 dirty axes)"
    - "iController info banner renders post-submit when Stage 3/4 override fired AND draft exists"
    - "Keyboard shortcuts 1/2/3/4/c/g/⌘⏎/Esc work without breaking existing ↑/↓/⏎/Space/n/e/r//? bindings"
    - "Submit-bar disables during in-flight POST (DoS pitfall)"
    - "Manual smoke: 1 capability + 1 regression override per axis on acceptance verifies the full chain"
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/review/page.tsx"
      provides: "loadPageData passes recipient chips, swarm_categories, swarm_intents to client"
    - path: "web/app/(dashboard)/automations/[swarm]/review/row-list.tsx"
      provides: "Renders PredictedRow per row with 5-column grid"
    - path: "web/app/(dashboard)/automations/[swarm]/review/detail-pane.tsx"
      provides: "Mounts PipelineFlow + EvalTypeRadio + submit bar; submitOverride() handler"
    - path: "web/app/(dashboard)/automations/[swarm]/review/keyboard-shortcuts.tsx"
      provides: "Extended shortcuts: 1/2/3/4 (focus stage widget), c (capability), g (regression), ⌘⏎ (submit), Esc (discard)"
  key_links:
    - from: "detail-pane.tsx"
      to: "/api/automations/debtor-email/override"
      via: "fetch POST with JSON body"
      pattern: "fetch\\(['\"]/api/automations/debtor-email/override['\"]"
    - from: "page.tsx"
      to: "loadSwarmCategories + loadSwarmIntents"
      via: "RSC server-side load passed as props"
      pattern: "loadSwarmCategories|loadSwarmIntents"
    - from: "row-list.tsx"
      to: "components/predicted-row.tsx"
      via: "import"
      pattern: "from\\s+['\"]./components/predicted-row['\"]"
---

<objective>
Wire up Plan 04's components into the live Bulk Review surfaces (page.tsx, row-list.tsx, detail-pane.tsx, keyboard-shortcuts.tsx) and run end-to-end smoke verification on acceptance per VALIDATION §Manual-Only Verifications.

Purpose: closes REVW-01..06 with a working operator surface.
Output: 4 file edits + manual smoke checkpoint with screenshots and Supabase MCP query results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-bulk-review-4-axis-redesign-capability-regression-eval-split/71-CONTEXT.md
@.planning/phases/71-bulk-review-4-axis-redesign-capability-regression-eval-split/71-RESEARCH.md
@.planning/phases/71-bulk-review-4-axis-redesign-capability-regression-eval-split/71-PATTERNS.md
@.planning/phases/71-bulk-review-4-axis-redesign-capability-regression-eval-split/71-UI-SPEC.md
@.planning/phases/71-bulk-review-4-axis-redesign-capability-regression-eval-split/71-VALIDATION.md
@.planning/phases/71-bulk-review-4-axis-redesign-capability-regression-eval-split/71-02-SUMMARY.md
@.planning/phases/71-bulk-review-4-axis-redesign-capability-regression-eval-split/71-03-SUMMARY.md
@.planning/phases/71-bulk-review-4-axis-redesign-capability-regression-eval-split/71-04-SUMMARY.md
@web/app/(dashboard)/automations/[swarm]/review/page.tsx
@web/app/(dashboard)/automations/[swarm]/review/row-list.tsx
@web/app/(dashboard)/automations/[swarm]/review/detail-pane.tsx
@web/app/(dashboard)/automations/[swarm]/review/keyboard-shortcuts.tsx
@web/app/(dashboard)/automations/[swarm]/review/recipient-chip-strip.tsx
@web/app/(dashboard)/automations/[swarm]/review/components/predicted-row.tsx
@web/app/(dashboard)/automations/[swarm]/review/components/pipeline-flow.tsx
@web/app/(dashboard)/automations/[swarm]/review/components/stage-step.tsx
@web/app/(dashboard)/automations/[swarm]/review/components/stage-1-widget.tsx
@web/app/(dashboard)/automations/[swarm]/review/components/stage-2-widget.tsx
@web/app/(dashboard)/automations/[swarm]/review/components/stage-3-widget.tsx
@web/app/(dashboard)/automations/[swarm]/review/components/stage-4-widget.tsx
@web/app/(dashboard)/automations/[swarm]/review/components/eval-type-radio.tsx
@web/app/(dashboard)/automations/[swarm]/review/components/override-confirm-dialog.tsx
@web/app/(dashboard)/automations/[swarm]/review/components/icontroller-info-banner.tsx
@web/lib/swarms/registry.ts
@web/lib/pipeline-events/types.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire components into page.tsx + row-list.tsx + detail-pane.tsx + keyboard-shortcuts.tsx</name>
  <files>
    web/app/(dashboard)/automations/[swarm]/review/page.tsx,
    web/app/(dashboard)/automations/[swarm]/review/row-list.tsx,
    web/app/(dashboard)/automations/[swarm]/review/detail-pane.tsx,
    web/app/(dashboard)/automations/[swarm]/review/keyboard-shortcuts.tsx
  </files>
  <read_first>
    - All 4 files being modified (read each in full to understand current structure)
    - web/app/(dashboard)/automations/[swarm]/review/components/* (Plan 04 outputs)
    - web/lib/swarms/registry.ts (loadSwarmCategories + loadSwarmIntents)
    - .planning/phases/71-bulk-review-4-axis-redesign-capability-regression-eval-split/71-UI-SPEC.md §Layout & Interaction Contracts (entire section)
    - .planning/phases/71-bulk-review-4-axis-redesign-capability-regression-eval-split/71-CONTEXT.md D-15, D-16
    - 71-04-SUMMARY.md (component prop shapes finalized)
  </read_first>
  <behavior>
    - **page.tsx (RSC):** loadPageData (already swapped to view in Plan 03) additionally fetches `loadSwarmCategories(admin, 'debtor-email')` + `loadSwarmIntents(admin, 'debtor-email')` server-side. Computes `recipientChips` array — group view rows by recipient inbox + count. Pass these as props to client components. Add the `?inbox=` filter into the view query (or post-filter as Plan 03 documented).
    - **row-list.tsx:** Replace existing `RowStrip` rendering with `<PredictedRow row={...} selected={...} onClick={...} />` per row. Mount `<RecipientChipStrip chips={...} activeInbox={inbox} />` ABOVE the list. Use the existing `useSelection()` hook for selection state. Preserve any existing wrapper / grouping markup unless it directly conflicts with the new grid.
    - **detail-pane.tsx:**
      - Replace the existing single-stage Select drill-in with `<PipelineFlow stages={stagesData} />` where `stagesData` is computed from the selected email's raw pipeline_events timeline (sub-query 6 from page.tsx).
      - Each StageStep's `widget` prop receives the appropriate Stage1Widget / Stage2Widget / Stage3Widget / Stage4Widget, hydrated with registry data + current dirty state. Track dirty state via a single `useState<{ stage_1?, stage_2?, stage_3?, stage_4? }>` shape.
      - Render `<EvalTypeRadio value={evalType} onChange={setEvalType} />` below the pipeline flow (default `'regression'` per D-08).
      - Submit bar at the bottom: `<Button variant="ghost">Discard changes</Button>` (clears all dirty + closes confirm) + `<Button variant="default">Submit override (N stages dirty)</Button>` (disabled when 0 dirty OR in-flight). Brand-primary button is the focal point per UI-SPEC.
      - Render `<OverrideConfirmDialog>` and `<IControllerInfoBanner>` conditionally per UI-SPEC trigger conditions.
      - `submitOverride()` async handler: for each dirty stage, call `fetch('/api/automations/debtor-email/override', { method: 'POST', body: JSON.stringify(payload) })`. Payload shape per axis (matches Plan 02 zod):
        - axis_1: `{ axis: 'stage_1_category', email_id, original_event_id, original_decision, decision: <category_key>, decision_details: { category_key }, eval_type, reason? }`
        - axis_2: `{ axis: 'stage_2_customer', ..., decision: <customer_account_id>, decision_details: { customer_account_id, customer_name }, re_run_downstream }`
        - axis_3: `{ axis: 'stage_3_intent', ..., decision: <intent_key>, decision_details: { intent_key } }`
        - axis_4: `{ axis: 'stage_4_handler_output', ..., decision: 'draft_quality_rated', decision_details: { draft_quality, reason } }`
      - During in-flight, set submit-bar disabled = true (DoS mitigation T-71-02-04).
      - On success: show toast (sonner — already imported), optimistically mark row as overridden, clear dirty state, optionally show iController banner if Stage 3/4 with draft.
      - On error: destructive toast, keep form editable.
    - **keyboard-shortcuts.tsx:** Add to the existing `ACTION_EVENTS` const + window keydown handler:
      - `1` / `2` / `3` / `4` → emit `stage-N-focus` CustomEvent.
      - `c` → emit `eval-type-capability`.
      - `g` → emit `eval-type-regression`.
      - `⌘⏎` / `Ctrl+⏎` → emit `override-submit`.
      - `Esc` → emit `override-discard` (matches Discard changes button).
      - Update Cheatsheet UI with the new bindings + label `Esc` row as `Discard changes`.
      - Existing input-focus guard `isTypingTarget` MUST still suppress all numeric/letter keys.
      - detail-pane.tsx subscribes to these CustomEvents via `useEffect` listeners.
  </behavior>
  <action>
For each of the 4 files, perform a surgical edit that preserves all existing behaviour and adds the new wiring. Do NOT rewrite the files end-to-end — read each file's current state and make minimal precise additions.

For page.tsx:
- Add the two registry loads alongside existing loadPageData logic.
- Compute recipient chip data from view rows (group by recipient inbox; the view doesn't currently expose recipient — JOIN back to email_pipeline.emails OR pull from existing side-loaders the page already uses for the list. Read page.tsx carefully to find the existing recipient source).
- Pass `categories`, `intents`, `recipientChips` as props to the client component tree.

For row-list.tsx:
- Replace the row rendering (find current `<RowStrip>` usage) with `<PredictedRow row={r} selected={selectedId===r.email_id} onClick={() => select(r.email_id)} />`.
- Add `<RecipientChipStrip chips={chips} activeInbox={inbox ?? 'all'} />` above the list.

For detail-pane.tsx (the largest edit):
- Read existing imports + state setup.
- Add new state: `const [dirty, setDirty] = useState<DirtyShape>({})`, `const [evalType, setEvalType] = useState<'capability'|'regression'>('regression')`, `const [submitting, setSubmitting] = useState(false)`, `const [confirmOpen, setConfirmOpen] = useState(false)`, `const [showICBanner, setShowICBanner] = useState(false)`.
- Compute `stagesData` from the selected email's pipeline_events timeline (the existing detail-pane already loads this).
- Replace existing single-stage Select with `<PipelineFlow stages={stagesData} />`.
- Render eval radio + submit bar.
- Implement `submitOverride()` per the payload table above.
- Listen for keyboard CustomEvents from keyboard-shortcuts.tsx.
- Wire `OverrideConfirmDialog` + `IControllerInfoBanner` conditionally.

For keyboard-shortcuts.tsx:
- Read the existing `ACTION_EVENTS` shape and append the new entries following the existing pattern.
- Update `Cheatsheet` JSX to list the new bindings.

After edits, run `cd web && npx vitest run "app/(dashboard)/automations/[swarm]/review"` — all existing tests must stay green. Add no new tests in this task — UI integration is verified by Task 2 manual smoke.

Run `cd web && npx tsc --noEmit` after every file edit. Run `cd web && npm run lint` if a lint script exists.

Run the dev server briefly (`cd web && npm run dev` in background) and visit `http://localhost:3000/automations/debtor-email/review` to sanity-check the page renders without runtime errors. Stop the dev server.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | grep -E "(dashboard)/automations/\[swarm\]/review/(page|row-list|detail-pane|keyboard-shortcuts)" | wc -l | grep -qE "^\s*0\s*$" && cd web && npx vitest run "app/(dashboard)/automations/[swarm]/review" 2>&1 | tail -10 | grep -qE "passed" && grep -E "fetch\(['\"]/api/automations/debtor-email/override['\"]" "web/app/(dashboard)/automations/[swarm]/review/detail-pane.tsx" | wc -l | grep -qE "^\s*[1-9]" && grep -E "PipelineFlow|EvalTypeRadio|RecipientChipStrip" "web/app/(dashboard)/automations/[swarm]/review/detail-pane.tsx" "web/app/(dashboard)/automations/[swarm]/review/row-list.tsx" | wc -l | grep -qE "^\s*[3-9]" && grep -E "stage-1-focus|stage-2-focus|stage-3-focus|stage-4-focus|eval-type-capability|eval-type-regression|override-submit|override-discard" "web/app/(dashboard)/automations/[swarm]/review/keyboard-shortcuts.tsx" | wc -l | grep -qE "^\s*[6-9]"</automated>
  </verify>
  <acceptance_criteria>
    - tsc clean for all 4 modified files.
    - All existing review tests still green.
    - detail-pane.tsx contains literal `fetch('/api/automations/debtor-email/override'...)` POST call.
    - PipelineFlow + EvalTypeRadio + RecipientChipStrip imported into the integration files.
    - keyboard-shortcuts.tsx contains the 8 new CustomEvent emissions.
    - No new raw hex in any modified file.
    - Dev server starts and the review page renders without runtime error (manual sanity check during the task — note in SUMMARY).
  </acceptance_criteria>
  <done>UI fully wired; existing tests green; dev-server sanity check passed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual end-to-end smoke on acceptance — 8 overrides (axis × eval_type)</name>
  <what-built>
    Full Phase 71 surface ready for human verification on acceptance.

    The executor has:
    1. Confirmed `cd web && npm test` is fully green (all Phase 71 + regression tests).
    2. Deployed to acceptance (or run dev server pointed at acceptance Supabase).
    3. Identified 8 candidate emails in `pipeline_events` on acceptance with at least one row at each stage that the operator can override (use Supabase MCP to surface candidates).
  </what-built>
  <files>(none — manual smoke; results captured in 71-05-SUMMARY.md)</files>
  <action>
Execute the 8-override matrix on acceptance:
1. Confirm `cd web && npm test` is fully green; deploy to acceptance (or run dev server pointed at acceptance Supabase).
2. Use Supabase MCP to surface 8 candidate emails (one per axis × {capability, regression}) currently sitting in `pipeline_events`.
3. File 8 overrides via the live Bulk Review UI per the matrix in <how-to-verify>.
4. After each submit, query Supabase MCP to verify the override row landed with correct shape (override jsonb populated, eval_type set, triggered_by='operator-override', server-stamped operator_id).
5. Capture screenshots before/after each submit (or 2-3 representative pairs).
6. Run the realtime two-tab smoke + submit-bar disable smoke per VALIDATION.md.
7. Record all 8 override `pipeline_events.id` UUIDs and Inngest dispatch confirmations in 71-05-SUMMARY.md.
8. If any axis fails, course-correct (fix bug, re-run quick test suite, retry the affected axis) before signaling success.
  </action>
  <verify>All 8 overrides present in `pipeline_events` with expected shape; Inngest dashboard shows the corresponding dispatched events; realtime + submit-bar smokes pass.</verify>
  <done>Smoke matrix passes; SUMMARY captures the 8 IDs + screenshots + Inngest evidence.</done>
  <how-to-verify>
    Per VALIDATION.md §Manual-Only Verifications, file 8 overrides total via the live UI on acceptance:

    1. **Stage 1 capability** — pick an email currently classified as `unknown`, override to `payment_admittance`, eval_type `capability`, reason "new pattern test". Submit. Verify toast success, row disappears, then run via Supabase MCP:
       ```sql
       SELECT id, stage, decision, override, eval_type, triggered_by FROM pipeline_events WHERE email_id = '<email>' ORDER BY created_at DESC LIMIT 5;
       ```
       Expect a fresh row at stage=1 with `override.axis='stage_1_category'`, `eval_type='capability'`, `triggered_by='operator-override'`, server-stamped `override.operator_id` matching your auth.uid().

    2. **Stage 1 regression** — override another email to `noise`, eval_type `regression`. Confirm.

    3. **Stage 2 no-rerun** — override a Stage 2 mismatched-customer email, toggle re-run OFF, eval_type `regression`. Confirm Stage 3+4 do NOT re-fire (no new stage-3/4 rows).

    4. **Stage 2 with rerun** — override another, toggle re-run ON, eval_type `capability`. Confirm modal triggers ("Re-running downstream stages will spend additional LLM tokens..."). Confirm new stage-3/4 rows arrive.

    5. **Stage 3** — override an intent, e.g. invoice_copy → payment_dispute. Confirm modal triggers. eval_type `regression`. Verify new `<handler>.requested` event fired in Inngest dashboard.

    6. **Stage 3 capability** — second axis-3 override on a different email. eval_type `capability`. Verify dispatch.

    7. **Stage 4 regression** — pick an email with a draft created. Rate quality 2, reason "wrong invoice attached". eval_type `regression`. Verify NO handler re-run, NO iController dispatch. Verify iController info banner appears post-submit.

    8. **Stage 4 capability** — rate quality 5 on another. eval_type `capability`.

    Capture in the SUMMARY:
    - Screenshots of the UI before/after each submit (8 pairs OK; sample 2-3 if visually identical).
    - Supabase MCP query results showing each `(axis, eval_type)` pair present.
    - Inngest dashboard screenshots showing the 4 dispatched events (stages 1, 2-rerun, 3, 3-capability).
    - Realtime smoke: open two tabs, override in tab A, confirm row updates in tab B within 2s.
    - Submit-bar disable smoke: open devtools throttling, click submit, confirm bar is disabled until POST resolves.

    If anything fails, course-correct in this same task and re-test before proceeding.
  </how-to-verify>
  <resume-signal>Type "smoke-passed" with the 8 override IDs OR list specific failures with reproduction steps</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → /api/automations/debtor-email/override | Reuses Plan 02's auth-gated, zod-validated route. UI is defense-in-depth (Plan 02 is the trust boundary). |
| Operator browser → Supabase Realtime channel on pipeline_events | Existing Phase 70 publication; authenticated SELECT only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-05-01 | DoS | Operator clicks Submit repeatedly during in-flight POST | mitigate | submit-bar disabled while submitting=true; manual smoke confirms behaviour. |
| T-71-05-02 | Tampering | Operator races UI optimistic update vs Inngest replay | accept | Override row is append-only (D-02); UI re-fetches from view post-success; Realtime ensures fresh read. Manual smoke verifies two-tab consistency. |
| T-71-05-03 | Information Disclosure | Operator email rendered in tooltip leaks PII to other operators | accept | Phase 48 project_members gating ensures only authorised operators see the dashboard. Operator emails are intra-team. |
| T-71-05-04 | Tampering | Confirm modal can be bypassed via DOM tampering | accept | Defense-in-depth: Plan 02 zod re-validates on the route. Modal is UX, not a security boundary. |
</threat_model>

<verification>
- All 4 files compile clean.
- `cd web && npm test` fully green.
- 8-override manual smoke completed successfully on acceptance per Task 2 checkpoint.
- Realtime two-tab smoke passes.
- Submit-bar disable smoke passes.
</verification>

<success_criteria>
- Bulk Review surface live on acceptance with 4-axis override flow.
- 8 overrides recorded across the matrix; all rows server-stamped, eval_type set, override jsonb populated.
- iController banner fires correctly for stage 3/4 + draft cases.
- Keyboard shortcuts work without breaking existing bindings.
- All Phase 71 REVW-01..06 requirements demonstrably met.
</success_criteria>

<output>
After completion, create `.planning/phases/71-bulk-review-4-axis-redesign-capability-regression-eval-split/71-05-SUMMARY.md` containing:
- 8 override `pipeline_events.id` UUIDs from acceptance.
- Screenshots before/after submit (or paths to them).
- Inngest dispatch confirmation.
- Realtime + submit-bar smoke results.
- Any UX deviations from UI-SPEC noted at smoke time.
</output>
