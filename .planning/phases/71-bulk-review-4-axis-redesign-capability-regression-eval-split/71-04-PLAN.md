---
phase: 71-bulk-review-4-axis-redesign-capability-regression-eval-split
plan: 04
type: execute
wave: 2
depends_on: [71-01]
files_modified:
  - web/app/(dashboard)/automations/[swarm]/review/recipient-chip-strip.tsx
  - web/app/(dashboard)/automations/[swarm]/review/components/predicted-row.tsx
  - web/app/(dashboard)/automations/[swarm]/review/components/pipeline-flow.tsx
  - web/app/(dashboard)/automations/[swarm]/review/components/stage-step.tsx
  - web/app/(dashboard)/automations/[swarm]/review/components/stage-1-widget.tsx
  - web/app/(dashboard)/automations/[swarm]/review/components/stage-2-widget.tsx
  - web/app/(dashboard)/automations/[swarm]/review/components/stage-3-widget.tsx
  - web/app/(dashboard)/automations/[swarm]/review/components/stage-4-widget.tsx
  - web/app/(dashboard)/automations/[swarm]/review/components/eval-type-radio.tsx
  - web/app/(dashboard)/automations/[swarm]/review/components/override-confirm-dialog.tsx
  - web/app/(dashboard)/automations/[swarm]/review/components/icontroller-info-banner.tsx
autonomous: true
requirements: [REVW-01, REVW-02, REVW-03, REVW-04, REVW-05, REVW-06]
tags: [ui, components, shadcn, v7-tokens, accessibility]

must_haves:
  truths:
    - "All 4 per-stage override widgets render and emit the right callback payload shape (per UI-SPEC)"
    - "EvalTypeRadio defaults to 'regression' (D-08 safety bias)"
    - "Stage-2 customer combobox sources customers from the source locked in Plan 01 Task 6 checkpoint"
    - "Stage-1 widget sources categories from loadSwarmCategories('debtor-email') + synthetic noise/archive at top of list"
    - "Stage-3 widget sources handlers from loadSwarmIntents('debtor-email')"
    - "Override confirm dialog fires for the 3 trigger conditions per UI-SPEC § Confirmation modal"
    - "iController info banner only renders when stage 3 or 4 override was just submitted AND a draft exists"
    - "All NEW components use --v7-* tokens — zero raw hex"
    - "All interactive elements have visible focus rings (outline 2px var(--v7-brand-secondary), offset 2px)"
    - "RecipientChipStrip mutates ?inbox=… URL param (single-select; All chip default)"
    - "PredictedRow renders the 5-column grid (recipient | from/subj | 4 stage cells | cost | cap/reg pill) per UI-SPEC"
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/review/recipient-chip-strip.tsx"
      provides: "Recipient brand-dot chip strip; URL-driven filter via ?inbox=…"
    - path: "web/app/(dashboard)/automations/[swarm]/review/components/predicted-row.tsx"
      provides: "Single row: recipient col + from/subj + 4 stage cells + cost + cap/reg pill"
    - path: "web/app/(dashboard)/automations/[swarm]/review/components/pipeline-flow.tsx"
      provides: "Vertical N-stage data-driven timeline; renders StageStep[]"
    - path: "web/app/(dashboard)/automations/[swarm]/review/components/stage-step.tsx"
      provides: "Single stage node + control area (ok / dirty)"
    - path: "web/app/(dashboard)/automations/[swarm]/review/components/stage-1-widget.tsx"
      provides: "Stage 1 category Select widget"
    - path: "web/app/(dashboard)/automations/[swarm]/review/components/stage-2-widget.tsx"
      provides: "Stage 2 customer combobox + re-run Switch"
    - path: "web/app/(dashboard)/automations/[swarm]/review/components/stage-3-widget.tsx"
      provides: "Stage 3 handler Select"
    - path: "web/app/(dashboard)/automations/[swarm]/review/components/stage-4-widget.tsx"
      provides: "Stage 4 1-5 quality buttons + reason Textarea (max 1000 + char count >800)"
    - path: "web/app/(dashboard)/automations/[swarm]/review/components/eval-type-radio.tsx"
      provides: "capability/regression radio cards; default regression"
    - path: "web/app/(dashboard)/automations/[swarm]/review/components/override-confirm-dialog.tsx"
      provides: "Confirmation Dialog wrapper with UI-SPEC copy"
    - path: "web/app/(dashboard)/automations/[swarm]/review/components/icontroller-info-banner.tsx"
      provides: "Post-submit banner for Stage 3/4 with iController draft"
  key_links:
    - from: "stage-2-widget.tsx"
      to: "web/components/ui/switch.tsx"
      via: "Switch primitive import"
      pattern: "from\\s+['\"]@/components/ui/switch['\"]"
    - from: "eval-type-radio.tsx"
      to: "web/components/ui/radio-group.tsx"
      via: "RadioGroup primitive import"
      pattern: "from\\s+['\"]@/components/ui/radio-group['\"]"
    - from: "predicted-row.tsx"
      to: "web/lib/swarms/brand-color.ts"
      via: "brandColorToken import"
      pattern: "brandColorToken"
    - from: "override-confirm-dialog.tsx"
      to: "web/components/ui/dialog.tsx"
      via: "Dialog primitive"
      pattern: "from\\s+['\"]@/components/ui/dialog['\"]"
---

<objective>
Build the 11 NEW UI components defined in UI-SPEC. Each component is a pure, presentational unit consumed by the detail-pane and row-list (Plan 05 wires them in). Token-only chrome. Accessibility per UI-SPEC §Focus order.

Purpose: REVW-01..06 frontend surfaces. Plan 05 mounts them into detail-pane.tsx + row-list.tsx + page.tsx.
Output: 11 new component files. No tests in this plan — components are presentational; integration tested via the existing review test harness in Plan 05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-bulk-review-4-axis-redesign-capability-regression-eval-split/71-CONTEXT.md
@.planning/phases/71-bulk-review-4-axis-redesign-capability-regression-eval-split/71-RESEARCH.md
@.planning/phases/71-bulk-review-4-axis-redesign-capability-regression-eval-split/71-PATTERNS.md
@.planning/phases/71-bulk-review-4-axis-redesign-capability-regression-eval-split/71-UI-SPEC.md
@.planning/phases/71-bulk-review-4-axis-redesign-capability-regression-eval-split/71-01-SUMMARY.md
@web/app/(dashboard)/automations/[swarm]/review/page.tsx
@web/app/(dashboard)/automations/[swarm]/review/detail-pane.tsx
@web/app/(dashboard)/automations/[swarm]/review/row-list.tsx
@web/app/(dashboard)/automations/[swarm]/review/row-strip.tsx
@web/app/(dashboard)/automations/[swarm]/review/queue-tree.tsx
@web/app/(dashboard)/automations/[swarm]/review/race-cohort-banner.tsx
@web/app/(dashboard)/automations/[swarm]/review/selection-context.tsx
@web/components/ui/select.tsx
@web/components/ui/dialog.tsx
@web/components/ui/switch.tsx
@web/components/ui/radio-group.tsx
@web/components/ui/textarea.tsx
@web/components/ui/button.tsx
@web/components/ui/command.tsx
@web/components/ui/tooltip.tsx
@web/lib/swarms/brand-color.ts
@web/lib/swarms/registry.ts
@web/lib/swarms/types.ts
@web/lib/pipeline-events/types.ts
@web/app/globals.css
@.claude/skills/sketch-findings-agent-workforce/SKILL.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build foundational UI components (RecipientChipStrip, PredictedRow, PipelineFlow, StageStep, EvalTypeRadio, OverrideConfirmDialog, IControllerInfoBanner)</name>
  <files>
    web/app/(dashboard)/automations/[swarm]/review/recipient-chip-strip.tsx,
    web/app/(dashboard)/automations/[swarm]/review/components/predicted-row.tsx,
    web/app/(dashboard)/automations/[swarm]/review/components/pipeline-flow.tsx,
    web/app/(dashboard)/automations/[swarm]/review/components/stage-step.tsx,
    web/app/(dashboard)/automations/[swarm]/review/components/eval-type-radio.tsx,
    web/app/(dashboard)/automations/[swarm]/review/components/override-confirm-dialog.tsx,
    web/app/(dashboard)/automations/[swarm]/review/components/icontroller-info-banner.tsx
  </files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/review/queue-tree.tsx (analog: URL-param filter pattern for RecipientChipStrip)
    - web/app/(dashboard)/automations/[swarm]/review/row-strip.tsx (analog: single-row presentational for PredictedRow)
    - web/app/(dashboard)/automations/[swarm]/review/race-cohort-banner.tsx (analog: in-surface banner for IControllerInfoBanner)
    - web/app/(dashboard)/automations/[swarm]/review/selection-context.tsx (URL-state plumbing pattern)
    - web/components/ui/dialog.tsx (Dialog primitive for OverrideConfirmDialog)
    - web/components/ui/radio-group.tsx (Plan 01 vendored — for EvalTypeRadio)
    - web/lib/swarms/brand-color.ts (Plan 01 — for PredictedRow recipient dot)
    - .planning/phases/71-bulk-review-4-axis-redesign-capability-regression-eval-split/71-UI-SPEC.md §Layout (page structure, recipient chip strip behaviour, detail-pane vertical pipeline, row interactions, focus order, ARIA, copywriting confirmation modal copy + iController banner copy)
    - .planning/phases/71-bulk-review-4-axis-redesign-capability-regression-eval-split/71-PATTERNS.md (component-by-component analog table)
    - .claude/skills/sketch-findings-agent-workforce/SKILL.md (V7 token + sketch design decisions)
  </read_first>
  <behavior>
    - **RecipientChipStrip:** Client component. Props: `chips: { inbox: string; brand: Entity; rowCount: number }[]`, `activeInbox: string | "all"`. Reads/writes `?inbox=` via `useSearchParams` + `useRouter` from next/navigation. ARIA `role="tablist"`, each chip `role="tab"` + `aria-selected`. "All" chip default. Active chip uses `--v7-brand-secondary-soft` bg / `--v7-brand-secondary` border / text. Brand-dot 8px sized. Row-count badge mono 11px.
    - **PredictedRow:** Pure presentational. Props: `row: SummaryRow` (per Plan 03 shape). Grid `200px 280px 1fr 84px 70px`. Recipient col: 6px brand dot + email mono. From/subj col: stacked. 4 stage cells: render decision text in mono `--v7-fs-sm` OR `↻` glyph in `--override` amber when `stage_N_overridden=true` OR `—` em-dash when stage didn't fire. Cost col: `€{(total_cost_cents/100).toFixed(3)}`. Tag col: `cap` (lime pill) or `reg` (red pill) only when at least one stage was overridden. Tooltip on `↻` glyph: "Stage {N} overridden by {operator email} on {date}".
    - **PipelineFlow:** Props: `stages: StageData[]` where `StageData = { n: number; title: string; axis: OverrideAxis | null; state: 'ok'|'dirty'|'skipped'; currentValue?: string; widget?: ReactNode }`. Renders `<ol>` of `<StageStep>`. CSS pseudo-element `::before` for the connecting line (2px wide, `--v7-line`, 15px from left). Empty list → null.
    - **StageStep:** Props: `stage: StageData`, `onMarkDirty: () => void`. Layout: 30×30 node circle on left (border colour by state), title row + axis tag, current value row, control area. Control area when `state==='ok'`: "✓ Looks correct" + inline link "override stage" (right-aligned, secondary→primary on hover). When `state==='dirty'`: render `stage.widget`. Visually-hidden span announces state for screen readers (`aria-hidden` on circle).
    - **EvalTypeRadio:** Props: `value: 'capability' | 'regression'`, `onChange: (v) => void`. Radix RadioGroup wrapped from web/components/ui/radio-group.tsx. Two cards: regression (left, default selected, red `reg` pill + "Used to work" sub) + capability (right, lime `cap` pill + "New pattern" sub). Default value = "regression". Tooltip on section heading per UI-SPEC. ARIA `role="radiogroup"`, each `role="radio"` + `aria-checked`.
    - **OverrideConfirmDialog:** Props: `open`, `onOpenChange`, `trigger: 'stage_2_rerun' | 'stage_3' | 'multi_axis'`, `extra: { handler_key?: string; axis_count?: number; axis_list?: string[] }`, `onConfirm`, `onDismiss`. Renders shadcn Dialog with title "Confirm override", body text from UI-SPEC § Confirmation modal copy table (3 trigger-specific bodies), confirm button "Yes, submit override" (primary brand-primary), dismiss button "Keep editing" (ghost).
    - **IControllerInfoBanner:** Props: `iControllerDraftId: string`, `onDismiss: () => void`. Dismissible banner. Bg `--v7-blue-soft`, border `--v7-blue`, ⓘ glyph. Copy verbatim from UI-SPEC § Stage-4 info banner. Render only when parent decides (stage 3 or 4 override AND draft exists).
  </behavior>
  <action>
For each of the 7 files, write a complete implementation following the specs above. All components use:
- `"use client";` directive at the top.
- React + lucide-react for icons (✓, ↻, ⓘ, etc.).
- Tailwind utility classes that resolve to V7 tokens via existing globals.css mappings.
- Inline `style={{ outline: ... }}` ONLY when V7 tokens require literal CSS-var references (e.g. `style={{ borderColor: 'var(--v7-line)' }}`).
- ZERO raw hex literals. Use `var(--v7-*)` references or Tailwind classes that resolve to V7.

Specific snippets to copy verbatim from UI-SPEC:
- OverrideConfirmDialog body copy (UI-SPEC §Confirmation modal — Stage 2 re-run, Stage 3, multi-axis variants).
- IControllerInfoBanner copy (UI-SPEC §Stage-4 info banner — `Override recorded — please update the draft in iController separately. We didn't auto-modify draft #{iC_id} because operator workflows vary.`).
- EvalTypeRadio card headings + subheads + tooltip copy.
- PredictedRow tooltip copy: `Stage {N} overridden by {operator email} on {date}`.
- RecipientChipStrip "All" default + URL pattern `?inbox=debiteuren@smeba.nl` (or `?inbox=all`).

For RecipientChipStrip, use `useRouter` + `useSearchParams` + `usePathname` from `next/navigation`. On chip click: `router.push(${pathname}?inbox=${encoded})`. Don't drop other URL params — preserve them.

For PredictedRow, import `brandColorToken` from `@/lib/swarms/brand-color` and apply: `<span style={{ background: \`var(${brandColorToken(row.entity_brand)})\`, width: 6, height: 6, borderRadius: '50%' }} aria-hidden="true" />`.

For PipelineFlow, the connecting line MUST be a CSS pseudo-element. Add a class `pipeline-flow` and define the `::before` rule in a co-located CSS module OR via a `<style>` tag wrapped at the parent in globals.css if an existing pattern exists. If unsure, inline via styled-jsx if the project uses it, or create `pipeline-flow.module.css` next to the component. Match whatever pattern the existing review surface already uses (read row-list.tsx + detail-pane.tsx for the prevailing styling pattern).

Each component: include a JSDoc block at top citing the UI-SPEC section it implements.

Run `cd web && npx tsc --noEmit` after each file to catch type errors early.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | grep -E "automations/\[swarm\]/review/(recipient-chip-strip|components/(predicted-row|pipeline-flow|stage-step|eval-type-radio|override-confirm-dialog|icontroller-info-banner))" | wc -l | grep -qE "^\s*0\s*$" && for f in web/app/\(dashboard\)/automations/\[swarm\]/review/recipient-chip-strip.tsx web/app/\(dashboard\)/automations/\[swarm\]/review/components/predicted-row.tsx web/app/\(dashboard\)/automations/\[swarm\]/review/components/pipeline-flow.tsx web/app/\(dashboard\)/automations/\[swarm\]/review/components/stage-step.tsx web/app/\(dashboard\)/automations/\[swarm\]/review/components/eval-type-radio.tsx web/app/\(dashboard\)/automations/\[swarm\]/review/components/override-confirm-dialog.tsx web/app/\(dashboard\)/automations/\[swarm\]/review/components/icontroller-info-banner.tsx; do test -f "$f" || { echo "missing $f"; exit 1; }; done && ! grep -REn "#[0-9a-fA-F]{3,8}" web/app/\(dashboard\)/automations/\[swarm\]/review/recipient-chip-strip.tsx web/app/\(dashboard\)/automations/\[swarm\]/review/components/{predicted-row,pipeline-flow,stage-step,eval-type-radio,override-confirm-dialog,icontroller-info-banner}.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files exist at the specified paths.
    - Each file starts with `"use client";`.
    - tsc clean for all 7 files (no new errors in them).
    - No raw hex (`#[0-9a-fA-F]{3,8}`) anywhere in the 7 files.
    - PredictedRow imports brandColorToken from `@/lib/swarms/brand-color`.
    - EvalTypeRadio default value = `"regression"`.
    - OverrideConfirmDialog body strings include the three UI-SPEC variants (Stage 2 re-run / Stage 3 / multi-axis).
    - IControllerInfoBanner copy includes literal text "please update the draft in iController separately".
    - RecipientChipStrip uses `useSearchParams` + `useRouter` from `next/navigation`.
    - All components include `aria-*` attributes per UI-SPEC §ARIA section.
  </acceptance_criteria>
  <done>7 foundational components ship, type-clean, token-only.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build per-stage override widgets (Stage1Widget, Stage2Widget, Stage3Widget, Stage4Widget)</name>
  <files>
    web/app/(dashboard)/automations/[swarm]/review/components/stage-1-widget.tsx,
    web/app/(dashboard)/automations/[swarm]/review/components/stage-2-widget.tsx,
    web/app/(dashboard)/automations/[swarm]/review/components/stage-3-widget.tsx,
    web/app/(dashboard)/automations/[swarm]/review/components/stage-4-widget.tsx
  </files>
  <read_first>
    - web/components/ui/select.tsx (Stage 1 + Stage 3)
    - web/components/ui/command.tsx (Stage 2 combobox primitive — Radix Command)
    - web/components/ui/switch.tsx (Plan 01 vendored — Stage 2 re-run toggle)
    - web/components/ui/textarea.tsx (Stage 4 reason)
    - web/components/ui/button.tsx (Stage 4 1-5 buttons)
    - web/lib/swarms/registry.ts (loadSwarmCategories, loadSwarmIntents server-side)
    - web/lib/swarms/types.ts (SwarmCategoryRow, SwarmIntentRow shapes)
    - web/app/(dashboard)/automations/[swarm]/review/detail-pane.tsx lines 24-32 (existing Select usage analog)
    - .planning/phases/71-bulk-review-4-axis-redesign-capability-regression-eval-split/71-UI-SPEC.md §Per-stage override widgets table (S1, S2, S3, S4)
    - 71-01-SUMMARY.md (Stage 2 customer-search source decision from Plan 01 Task 6 checkpoint)
    - .planning/phases/71-bulk-review-4-axis-redesign-capability-regression-eval-split/71-PATTERNS.md §stage-1/2/3/4-widget.tsx
  </read_first>
  <behavior>
    - **Stage1Widget:** Props: `categories: SwarmCategoryRow[]`, `value: string | null`, `onChange: (key: string) => void`. shadcn Select. Items: synthetic `{ key: 'noise', label: 'Noise / spam' }` + `{ key: 'archive', label: 'Archive without action' }` at TOP, then a separator, then enabled `categories` from registry. Placeholder `Pick a category…`.
    - **Stage2Widget:** Props: `value: { customer_account_id: string; customer_name: string } | null`, `onChange`, `reRun: boolean`, `onReRunChange`. Combobox sourced from the customer-search source approved at Plan 01 Task 6 checkpoint (option a, b, or c). 250ms debounce, min 2 chars, max 20 results, displays `{customer_name} · {nxt_account_id}`. Below the combobox: a card with a Switch labelled `Re-run downstream stages with the corrected customer` + helper `Costs additional LLM tokens. Default: off — leave existing draft as-is.` Switch uses `@/components/ui/switch`. ARIA: combobox `role="combobox"` + `aria-expanded`.
    - **Stage3Widget:** Props: `intents: SwarmIntentRow[]`, `value: string | null`, `onChange: (intent_key: string) => void`. shadcn Select. Items render `intent_key` with Tooltip on hover showing `handler_agent_key`. Placeholder `Pick a handler…`.
    - **Stage4Widget:** Props: `quality: 1|2|3|4|5 | null`, `onQualityChange`, `reason: string`, `onReasonChange`. 5 Buttons in a row, labels `1 Terrible / 2 Poor / 3 Okay / 4 Good / 5 Perfect` (or numeric labels with sr-only verbose labels). `aria-pressed` on the selected one. Below: Textarea, `maxLength={1000}`, placeholder `Why? (optional, max 1000 chars)`. Char-count footer rendered when `reason.length > 800`: `{reason.length} / 1000`.
  </behavior>
  <action>
Create the 4 widget files. Each is a `"use client";` component. Read the existing Select/Textarea usage in detail-pane.tsx to mirror import paths and prop conventions.

For Stage2Widget customer-search:
- If Plan 01 checkpoint approved option (a) — new SELECT-RPC over coordinator_runs DISTINCT — implement a Server Action OR fetch helper `searchCustomers(query: string)` calling `admin.from('coordinator_runs').select('customer_account_id, customer_name').ilike('customer_name', \`%${query}%\`).limit(20)`. Co-locate the helper in `web/app/(dashboard)/automations/[swarm]/review/components/stage-2-search.ts`.
- If option (b) — live NXT via Zapier — call the existing `nxt-zap-client.ts` lookup with debounce on the client.
- If option (c) — reuse `email_labels` denormalised customer name — query that table.

Implement debounce inline using `useEffect` + `setTimeout` (250ms) or a small `useDebouncedValue` helper. Do NOT add new npm dependency.

For Stage4Widget quality buttons, render as 5 `<Button variant={quality===n?'default':'outline'} aria-pressed={quality===n}>` buttons in a flex row with `--v7-space-1` gap. Numeric label as button text; sr-only span for verbose label.

Final tsc check.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | grep -E "components/stage-(1|2|3|4)-widget" | wc -l | grep -qE "^\s*0\s*$" && for f in web/app/\(dashboard\)/automations/\[swarm\]/review/components/stage-{1,2,3,4}-widget.tsx; do test -f "$f" || { echo "missing $f"; exit 1; }; done && grep -E "from\s+['\"]@/components/ui/switch['\"]" web/app/\(dashboard\)/automations/\[swarm\]/review/components/stage-2-widget.tsx | wc -l | grep -qE "^\s*[1-9]" && grep -E "noise|archive" web/app/\(dashboard\)/automations/\[swarm\]/review/components/stage-1-widget.tsx | wc -l | grep -qE "^\s*[2-9]" && grep -E "maxLength" web/app/\(dashboard\)/automations/\[swarm\]/review/components/stage-4-widget.tsx | wc -l | grep -qE "^\s*[1-9]" && ! grep -REn "#[0-9a-fA-F]{3,8}" web/app/\(dashboard\)/automations/\[swarm\]/review/components/stage-{1,2,3,4}-widget.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 4 widget files exist.
    - Stage1Widget includes both synthetic options `noise` and `archive`.
    - Stage2Widget imports Switch from `@/components/ui/switch`.
    - Stage2Widget customer-search implementation matches the Plan 01 Task 6 approved source (referenced in JSDoc at top of file).
    - Stage3Widget renders a Tooltip on each intent item showing handler_agent_key.
    - Stage4Widget Textarea has `maxLength={1000}` literal in source.
    - Stage4Widget renders 5 buttons with `aria-pressed`.
    - tsc clean for all 4 files.
    - No raw hex.
  </acceptance_criteria>
  <done>4 widgets ship, customer-search source aligned with Plan 01 checkpoint, types clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → component event handlers | Component `onChange` callbacks emit values that Plan 05's submit handler will pass to POST route. The route re-validates with zod (Plan 02 — defense-in-depth). |
| Component prop input → render | UI-SPEC copy is static; reason text is rendered through React text node (auto-escapes HTML). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-04-01 | Tampering | XSS via `reason` text or operator email rendered in tooltip | mitigate | D-14: React text-node renders auto-escape; NEVER use `dangerouslySetInnerHTML`. PredictedRow tooltip uses prop interpolation only. |
| T-71-04-02 | Information Disclosure | Brand-dot leaks brand mapping (no PII) | accept | Brands are non-sensitive (entity_brand list public via PROJECT.md). |
| T-71-04-03 | DoS | Customer combobox spams the search source on every keystroke | mitigate | 250ms debounce + min 2 chars + limit 20 (UI-SPEC). |
| T-71-04-04 | Tampering | Operator selects malformed customer/intent that doesn't exist in registry | mitigate | Plan 02 override-handler axis-3 throws on unknown intent_key; axis-2 only stores the value (server-side correctness preserved). UI Select is constrained to registry options. |
| T-71-04-05 | Information Disclosure | Stage-2 widget customer-search leaks customers across tenants | mitigate | Plan 01 checkpoint locks the search source against an existing tenant-scoped query (coordinator_runs is service_role-only; project_members gate at the page level). |
</threat_model>

<verification>
- `cd web && npx tsc --noEmit` clean.
- `cd web && npx vitest run "app/(dashboard)/automations/[swarm]/review"` — existing tests still green (these new components aren't imported yet by detail-pane until Plan 05).
- Manual file-existence + grep checks pass per task <verify> commands.
</verification>

<success_criteria>
- 11 NEW component files exist, type-clean, V7 token-only.
- Each component implements the contract from UI-SPEC verbatim (copy + behaviour).
- Plan 05 can mount them with no further design decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/71-bulk-review-4-axis-redesign-capability-regression-eval-split/71-04-SUMMARY.md` listing the 11 components created and any UI-SPEC ambiguities encountered (e.g. the customer-search source path actually wired up).
</output>
