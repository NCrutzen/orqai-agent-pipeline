---
phase: 84-stage-1-noise-rules-for-ap-automation-fyi-traffic
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - web/lib/debtor-email/__tests__/classify.test.ts
  - web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts
  - web/__tests__/static-checks/swarm-hard-separation.test.ts
  - .planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/CORPUS-SAMPLES.md
autonomous: true
must_haves:
  truths:
    - "Failing (RED) unit tests exist for all 8 Phase 84 category matchers in classify.ts"
    - "Failing (RED) worker tests exist for own_outbound_invoice_loopback covering D-03 direction='inbound' guard and spoofed-external-from negative"
    - "Static-check test exists asserting swarm_noise_categories ∩ swarm_intents = ∅ for all 8 Phase 84 keys"
    - "CORPUS-SAMPLES.md contains ≥10 hand-confirmed positives per category × 8 categories, with per-swarm sales-email column per D-08"
  artifacts:
    - path: "web/lib/debtor-email/__tests__/classify.test.ts"
      provides: "8 new describe-groups (one per category) × ≥3 fixtures (positive/negative/boundary)"
    - path: "web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts"
      provides: "Loopback rule group: direction=inbound + tenant-domain positive; direction=outbound negative; external-sender-spoofs-tenant-domain negative"
    - path: "web/__tests__/static-checks/swarm-hard-separation.test.ts"
      provides: "Hard-separation invariant test (Pitfall 1)"
    - path: ".planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/CORPUS-SAMPLES.md"
      provides: "Per-category corpus evidence table (email_id, subject, sender, mailbox, verdict, swarm)"
  key_links:
    - from: "classify.test.ts"
      to: "web/lib/debtor-email/classify.ts"
      via: "import { classify } from '../classify'"
      pattern: "from\\s+['\"]\\.\\./classify['\"]"
    - from: "classifier-screen-worker.test.ts"
      to: "web/lib/inngest/functions/classifier-screen-worker.ts"
      via: "import + Inngest test harness"
      pattern: "classifier-screen-worker"
---

<objective>
Wave 0: write RED tests + corpus fixtures + hard-separation static check BEFORE any migration or matcher code lands.

Purpose: enforce TDD on the 8 new category matchers per D-01, lock the hard-separation invariant (Pitfall 1) at the test layer before Wave 1 INSERTs, and produce the operator-grade corpus evidence file that D-05's corpus-evidence promotion path requires.

Output: 4 test/fixture files committed and failing (RED), ready for Wave 1+2 to turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-CONTEXT.md
@.planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-RESEARCH.md
@.planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-PATTERNS.md
@CLAUDE.md

<interfaces>
From web/lib/debtor-email/classify.ts (existing Category union — Phase 84 EXTENDS this in Wave 2):
```typescript
export type Category =
  | "auto_reply" | "ooo_temporary" | "ooo_permanent"
  | "payment_admittance" | "spam" | "unknown";
// Phase 84 will add: "coupa_invoice_paid_notification" | "coupa_invoice_approved_notification"
//   | "iss_ptp_autoreply" | "frieslandcampina_portal_reject" | "m365_quarantine"
//   | "sender_phishing_notice" | "supplier_bank_change_notification"
//   | "own_outbound_invoice_loopback"

export function classify(input: { subject: string; from: string; bodySnippet: string }):
  { category: Category; confidence: number; matchedRule: string };
```

From CONTEXT.md D-01 — the 8 LOCKED category keys (use VERBATIM):
- coupa_invoice_paid_notification
- coupa_invoice_approved_notification
- iss_ptp_autoreply
- frieslandcampina_portal_reject
- m365_quarantine
- sender_phishing_notice
- supplier_bank_change_notification
- own_outbound_invoice_loopback
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED unit tests in classify.test.ts for 7 in-classifier matchers</name>
  <read_first>
    - web/lib/debtor-email/__tests__/classify.test.ts (existing test patterns lines 16-80)
    - web/lib/debtor-email/classify.ts (existing matchers lines 44-197 for return-shape reference)
    - .planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-PATTERNS.md (lines 320-340 for describe-group structure)
    - .planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-CONTEXT.md (D-01 8 locked keys; domain evidence block lines 20-28 for fixture seed data)
  </read_first>
  <behavior>
    - 7 describe-groups (loopback excluded — that lives in worker test), one per category key from D-01
    - Each group: 1 positive fixture, 1 negative fixture (broad subject without the discriminator), 1 boundary fixture (similar template that MUST stay unknown)
    - coupa_invoice_paid_notification positive: subject "Factuur 12345678 gemarkeerd als Betaald door ISS", from "noreply@coupahost.com"
    - coupa_invoice_paid_notification boundary: subject "Factuurnummer 12345 is gemarkeerd als betwist door ISS" → MUST NOT match (dispute, not paid; per RESEARCH Open Q #2)
    - coupa_invoice_approved_notification positive: subject "Factuur 12345678 is goedgekeurd voor betaling door ISS"
    - iss_ptp_autoreply positive: from "Invoice-PtP@nl.issworld.com", subject "Automatisch antwoord: Documenten n.a.v. uitgevoerde werkzaamheden. Factuur: 12345678"
    - iss_ptp_autoreply boundary: generic "Automatisch antwoord: Out of office" from random sender → MUST match existing auto_reply, NOT iss_ptp_autoreply
    - frieslandcampina_portal_reject positive: from "Robbie.Robot@frieslandcampina.com", subject starting "FINAL_REMINDER_Invoice received for Candex related purchase(s)_"
    - m365_quarantine positive: from "q2q@apexfire.ie", subject "Microsoft 365 security: You have messages in quarantine"
    - sender_phishing_notice positive: from "melanie@rskinstallatie.nl", subject "Uitleg pishing mail" OR "Voorgaande mail niet openen"
    - supplier_bank_change_notification positive: from "info@farmplus.nl" body or subject mentioning IBAN change
    - Each positive asserts result.category === '<key>' AND result.matchedRule === '<rule_key>' (per Open Q #3 flat naming = category key)
  </behavior>
  <action>Extend web/lib/debtor-email/__tests__/classify.test.ts with 7 new describe-groups using the existing test pattern (it() with classify({subject, from, bodySnippet}) → expect(result.category).toBe(...)). Use the 8 locked category keys from CONTEXT.md D-01 verbatim as the expected category values. Tests MUST fail RED today — classify.ts has no Phase 84 matchers yet. Place each describe-group with a comment header "// Phase 84 D-01 — &lt;category_key&gt;". For the coupa_invoice_paid_notification boundary case, cite the corpus example from CONTEXT.md "Dropped from scope" block. Do NOT add the loopback group here — that lives in classifier-screen-worker.test.ts per PATTERNS.md line 339.</action>
  <verify>
    <automated>cd web && npx vitest run lib/debtor-email/__tests__/classify.test.ts --reporter=basic 2>&amp;1 | grep -E "FAIL|failed" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File web/lib/debtor-email/__tests__/classify.test.ts contains 7 new describe blocks, one per Phase 84 in-classifier category key (verify with grep -c "Phase 84 D-01" of the test file ≥ 7).
    - Running `cd web && npx vitest run lib/debtor-email/__tests__/classify.test.ts` reports ≥21 failing assertions (7 groups × ≥3 fixtures).
    - No test references the category key "own_outbound_invoice_loopback" in this file (that key belongs to the worker test).
    - Coupa-Betaald boundary case explicitly tests the `betwist`/`disputed` template and asserts it does NOT match coupa_invoice_paid_notification.
  </acceptance_criteria>
  <done>RED test file committed; 7 category groups present; vitest run shows ≥21 failures with reason "unknown received, expected &lt;phase-84-key&gt;".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED worker tests for own_outbound_invoice_loopback + hard-separation static check</name>
  <read_first>
    - web/lib/inngest/functions/classifier-screen-worker.ts (lines 167-216 step.run pattern; line 201 comment re: missing sender_email / direction on event)
    - web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts (existing test file if present; create if absent)
    - .planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-PATTERNS.md (lines 247-275 loopback placement pattern; lines 343-365 hard-separation test template)
    - .planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-CONTEXT.md (D-03 rule body; R-02 spoofing mitigation)
    - supabase/migrations/20260507_phase75_swarm_categories_data_cleanup.sql (precedent for hard-separation invariant)
  </read_first>
  <behavior>
    - Worker test group "own_outbound_invoice_loopback (Phase 84 D-03)":
      - Positive: swarmRow.tenant_domains=["fire-control.nl"], event.from="administratie@fire-control.nl", event.direction="inbound", regex outcome="unknown" → worker MUST set regexOutcome.category="own_outbound_invoice_loopback" and matchedRule="loopback_tenant_domain"
      - Negative (D-03 direction guard): same setup but direction="outbound" → MUST NOT trigger loopback (regexOutcome.category stays "unknown", falls through to LLM 2nd-pass)
      - Negative (Pitfall 3 spoofing): tenant_domains=["fire-control.nl"], from="customer@external.example", direction="inbound" → MUST NOT trigger
      - Negative (no tenant_domains): tenant_domains=[], from="administratie@fire-control.nl", direction="inbound" → MUST NOT trigger
    - Static-check test "Hard-separation invariant: swarm_noise_categories ∩ swarm_intents = ∅":
      - Queries both registries with the 8 Phase 84 keys
      - Asserts intersection returns zero rows
      - Uses createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY) with direct .from() reads (NOT exec_sql RPC — that may not exist)
  </behavior>
  <action>Create or extend web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts with a describe block for "own_outbound_invoice_loopback (Phase 84 D-03)" containing 4 it() blocks (positive + 3 negatives covering D-03 direction guard and Pitfall 3 spoofing). Mock loadSwarm to return {tenant_domains:[...]} and exercise the worker's loopback branch directly. Tests MUST fail RED — the loopback branch does not exist in classifier-screen-worker.ts yet. Separately create web/__tests__/static-checks/swarm-hard-separation.test.ts following PATTERNS.md lines 343-365: query swarm_noise_categories and swarm_intents via @supabase/supabase-js, build the intersection client-side over (swarm_type, key) tuples for the 8 Phase 84 category keys (per CONTEXT D-01), assert intersection has length 0. The static-check test is the Pitfall 1 enforcement gate — it should PASS today (the keys don't exist yet) and CONTINUE to pass after Wave 1 inserts. Add a comment line "// Phase 84 Pitfall 1 — see 84-RESEARCH.md".</action>
  <verify>
    <automated>cd web && npx vitest run lib/inngest/functions/__tests__/classifier-screen-worker.test.ts -t "own_outbound_invoice_loopback" --reporter=basic 2>&amp;1 | grep -E "FAIL|failed" | head -10 &amp;&amp; cd web && npx vitest run __tests__/static-checks/swarm-hard-separation.test.ts --reporter=basic 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts contains describe "own_outbound_invoice_loopback (Phase 84 D-03)" with exactly 4 it() blocks (verify: grep -c "it(" inside the new describe == 4).
    - Worker test RUN reports ≥4 failures (loopback branch does not exist yet).
    - File web/__tests__/static-checks/swarm-hard-separation.test.ts exists and references all 8 D-01 category keys verbatim.
    - Static-check test PASSES today (zero intersection on empty registry).
    - Neither test file uses exec_sql RPC.
  </acceptance_criteria>
  <done>Worker test file extended with loopback group (RED); static-check file created (GREEN today, must stay GREEN forever).</done>
</task>

<task type="auto">
  <name>Task 3: CORPUS-SAMPLES.md — ≥10 hand-confirmed positives per category, per-swarm column</name>
  <read_first>
    - .planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-CONTEXT.md (domain evidence block lines 20-28; D-05 corpus-evidence path; D-08 cross-swarm + R-04 sales-email FP risk)
    - .planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-RESEARCH.md (§Wave 0 Gaps line 502; Pitfall 6 storage-location enforcement; Pitfall 7 sales-email per-category corpus review)
    - web/debtor-email-analyzer/src/search-emails.ts (existing CLI for corpus search; usage in 84-RESEARCH.md line 369)
    - web/debtor-email-analyzer/src/sample-bodies.ts (existing body-dump CLI)
  </read_first>
  <action>Create .planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/CORPUS-SAMPLES.md with one ## section per category (8 sections, headings using the D-01 keys verbatim). Each section MUST contain a markdown table with columns: | email_id | mailbox | swarm | from_address | subject | direction | operator_verdict | per D-05 evidence requirements. Populate ≥10 hand-confirmed positive rows per category by querying email_pipeline.emails via `cd web/debtor-email-analyzer && npx tsx src/search-emails.ts` (use sender + subject filters derived from CONTEXT domain block: coupahost.com for both Coupa categories; Invoice-PtP@nl.issworld.com for ISS; Robbie.Robot@frieslandcampina.com; q2q@apexfire.ie; melanie@rskinstallatie.nl; info@farmplus.nl; for loopback: from_address ILIKE '%@fire-control.nl' AND mailbox = administratie@fire-control.nl). For the swarm column, record whether each email arrived in a debtor-email mailbox or a sales-email mailbox (per D-08). For sales-email rows: if a category yields <10 sales-email positives, ADD a "Sales-email coverage gap" subsection citing the count and recommending whether to drop the sales-email row per R-04 mitigation. Add a "False positives reviewed" subsection per category (0-count is fine, but must be explicit) per D-05 (2). Top of file: include a "Promotion gate (D-05) status" rollup table with columns | category | live_positives | hand_FPs | Wilson_path_eligible | corpus_path_eligible | gate_disposition |.</action>
  <verify>
    <automated>test -f .planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/CORPUS-SAMPLES.md &amp;&amp; for k in coupa_invoice_paid_notification coupa_invoice_approved_notification iss_ptp_autoreply frieslandcampina_portal_reject m365_quarantine sender_phishing_notice supplier_bank_change_notification own_outbound_invoice_loopback; do grep -v '^#' .planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/CORPUS-SAMPLES.md | grep -c "$k" || echo "MISSING:$k"; done</automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/CORPUS-SAMPLES.md.
    - File contains exactly 8 H2 sections, one per D-01 category key (verify by grep -E '^## (coupa_invoice_paid_notification|coupa_invoice_approved_notification|iss_ptp_autoreply|frieslandcampina_portal_reject|m365_quarantine|sender_phishing_notice|supplier_bank_change_notification|own_outbound_invoice_loopback)$' returns 8 lines).
    - Each section's primary table has ≥10 data rows (count pipe-delimited rows after the header separator).
    - Each section has a "False positives reviewed" subsection.
    - Top-of-file "Promotion gate (D-05) status" rollup is present.
    - Any sales-email coverage gap is flagged with a recommendation (drop or keep) per R-04.
  </acceptance_criteria>
  <done>CORPUS-SAMPLES.md committed with operator-grade evidence per category; gaps explicit; D-05 promotion-gate rollup present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → corpus file | Hand-confirmed verdicts written to CORPUS-SAMPLES.md drive D-05 promotion gate decisions |
| test → Supabase | Static-check test reads service-role credentials from env (test-env only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01-01 | T (Tampering) | CORPUS-SAMPLES.md | mitigate | File lives in repo, change-tracked via git; operator verdicts traceable per row by email_id |
| T-84-01-02 | I (Info disclosure) | swarm-hard-separation.test.ts | accept | Test uses service-role key from env; runs in CI only; no PII surfaced (only category keys) |
| T-84-01-03 | D (DoS via regex) | Phase 84 matchers (will land Wave 2) | mitigate | Tests use anchored patterns (^...$ or bounded \d+); no nested quantifiers in fixture expectations — pre-validates Wave 2 design |
</threat_model>

<verification>
- All three tasks complete; RED tests for classify (Task 1) and worker loopback (Task 2) fail loudly.
- Static-check test (Task 2) PASSES today and locks the Pitfall 1 invariant before Wave 1.
- CORPUS-SAMPLES.md is operator-reviewable evidence; per-swarm gaps explicit.
</verification>

<success_criteria>
- `cd web && npx vitest run lib/debtor-email/__tests__/classify.test.ts` reports ≥21 RED failures attributable to Phase 84 keys.
- `cd web && npx vitest run lib/inngest/functions/__tests__/classifier-screen-worker.test.ts -t "own_outbound_invoice_loopback"` reports 4 RED failures.
- `cd web && npx vitest run __tests__/static-checks/swarm-hard-separation.test.ts` PASSES (and continues to pass after Wave 1).
- `CORPUS-SAMPLES.md` covers all 8 categories with ≥10 positives each, per-swarm column, sales-email gap-flagging present.
</success_criteria>

<output>
After completion, create `.planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-01-SUMMARY.md`
</output>
