---
phase: 84-stage-1-noise-rules-for-ap-automation-fyi-traffic
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - supabase/migrations/20260520_phase84_tenant_domains.sql
  - supabase/migrations/20260520_phase84_noise_categories.sql
  - supabase/migrations/20260520_phase84_classifier_rules_seed.sql
  - web/scripts/gen-tenant-domains.ts
  - web/lib/automations/debtor-email/coordinator/tenant-domains.generated.ts
  - web/lib/swarms/types.ts
  - web/package.json
  - web/lib/inngest/functions/debtor-email-coordinator.ts
autonomous: false
must_haves:
  truths:
    - "swarms.tenant_domains jsonb NOT NULL DEFAULT '[]'::jsonb exists in production"
    - "swarms.tenant_domains is populated for every existing swarm row (per CONTEXT §6)"
    - "16 swarm_noise_categories rows (8 categories × 2 swarms) exist with action='categorize_archive'"
    - "8 classifier_rules candidate rows exist with kind='regex' (debtor-email) and kind='agent_intent' (sales-email) per Pitfall 5 / PATTERNS lines 122-133"
    - "tenant-domains.generated.ts emitted from registry; codegen idempotent"
    - "debtor-email-coordinator.ts line 50 TENANT_DOMAINS stub retired; reads from generated import"
    - "supabase db push completed successfully — schema live in production"
    - "Hard-separation static-check (Wave 0 Task 2) still passes post-migration"
  artifacts:
    - path: "supabase/migrations/20260520_phase84_tenant_domains.sql"
      provides: "ADD COLUMN tenant_domains jsonb + per-swarm UPDATE backfill"
    - path: "supabase/migrations/20260520_phase84_noise_categories.sql"
      provides: "16 idempotent UPSERTs into swarm_noise_categories"
    - path: "supabase/migrations/20260520_phase84_classifier_rules_seed.sql"
      provides: "8 candidate-status rows in classifier_rules"
    - path: "web/scripts/gen-tenant-domains.ts"
      provides: "Codegen script mirroring gen-entity-types.ts (per-swarm map output per Open Q #4)"
    - path: "web/lib/automations/debtor-email/coordinator/tenant-domains.generated.ts"
      provides: "TENANT_DOMAINS_BY_SWARM const + TenantDomain literal-union type"
  key_links:
    - from: "debtor-email-coordinator.ts:50"
      to: "tenant-domains.generated.ts"
      via: "import { TENANT_DOMAINS_BY_SWARM }"
      pattern: "tenant-domains\\.generated"
    - from: "package.json codegen script"
      to: "gen-tenant-domains.ts"
      via: "tsx web/scripts/gen-tenant-domains.ts"
      pattern: "gen-tenant-domains"
---

<objective>
Wave 1: ship the data layer — 3 migrations (column-add + 16 noise rows + 8 classifier_rules candidates), codegen for tenant_domains (mirroring gen-entity-types.ts per PATTERNS.md), retire the Phase 83 TENANT_DOMAINS stub in debtor-email-coordinator.ts line 50, and execute the BLOCKING `supabase db push`.

Purpose: deliver every CONTEXT.md D-01..D-04, D-08 registry artifact + the D-03 schema column + the codegen path that powers the Wave 2 loopback rule.

Output: live Supabase schema, generated TS file committed, classify.ts (Wave 2) and screen-worker (Wave 2) have a registry-backed tenant_domains source ready to consume.

This plan is `autonomous: false` because `supabase db push` may require interactive confirmation; the operator must observe and confirm the push completes before Wave 2 proceeds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-CONTEXT.md
@.planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-RESEARCH.md
@.planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-PATTERNS.md
@CLAUDE.md
@supabase/migrations/_template.sql
@supabase/migrations/20260511_swarm_noise_spam_key.sql
@supabase/migrations/20260504b_swarms_registry_generalisation.sql
@web/scripts/gen-entity-types.ts

<interfaces>
From web/lib/swarms/types.ts (existing SwarmRow — Phase 84 EXTENDS):
```typescript
export interface SwarmRow {
  swarm_type: string;
  // ... existing fields
  // Phase 84 D-03 adds:
  tenant_domains: string[]; // jsonb-array-of-lowercase-strings from public.swarms.tenant_domains
}
```

From web/scripts/gen-entity-types.ts (codegen precedent — copy verbatim, ONLY swap OUTPUT_PATH + DB query + renderBody shape):
- Imports: createClient, writeFileSync, mkdirSync, readFileSync, existsSync, dirname, resolve, dotenv
- Idempotency: read existing → compare → skip write
- Alphabetical sort (per swarm)
- HEADER comment "// AUTO-GENERATED ... DO NOT EDIT BY HAND. Run `npm run codegen`"

Output shape (per CONTEXT D-03 + RESEARCH Open Q #4 — per-swarm map):
```typescript
export const TENANT_DOMAINS_BY_SWARM = {
  "debtor-email": ["fire-control.nl","moyneroberts.com","smeba-fire.be","smeba.nl"],
  "sales-email":  ["smeba.nl"],
} as const;
export type TenantDomain = (typeof TENANT_DOMAINS_BY_SWARM)[keyof typeof TENANT_DOMAINS_BY_SWARM][number];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrations — tenant_domains column + 16 noise rows + 8 classifier_rules candidates</name>
  <read_first>
    - supabase/migrations/_template.sql (RLS template — column-add inherits existing RLS, no new policy needed)
    - supabase/migrations/20260504b_swarms_registry_generalisation.sql (ALTER TABLE additive jsonb precedent lines 10-15, 44-85 for per-swarm UPDATE)
    - supabase/migrations/20260511_swarm_noise_spam_key.sql (cross-swarm UPSERT pattern verbatim lines 30-41 — copy this 8 times)
    - supabase/migrations/20260428_classifier_rules.sql (schema constraints: kind in ('regex','agent_intent'); status in ('candidate','promoted','demoted','manual_block'))
    - .planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-PATTERNS.md (lines 30-95 for migration shapes; lines 100-133 for classifier_rules seed)
    - .planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-CONTEXT.md (D-01 8 keys; D-03 tenant_domains shape; D-04 action; D-08 cross-swarm)
  </read_first>
  <action>Create THREE migration files in supabase/migrations/ following the existing date prefix convention:

(a) supabase/migrations/20260520_phase84_tenant_domains.sql — `ALTER TABLE public.swarms ADD COLUMN IF NOT EXISTS tenant_domains jsonb NOT NULL DEFAULT '[]'::jsonb;` followed by per-swarm UPDATE statements. Set tenant_domains for swarm_type='debtor-email' = `'["fire-control.nl","moyneroberts.com","smeba-fire.be","smeba.nl"]'::jsonb` (matching the existing Phase 83 stub at debtor-email-coordinator.ts:50 PLUS fire-control.nl which is the new loopback case from CONTEXT §domain). Set tenant_domains for swarm_type='sales-email' = `'["smeba.nl"]'::jsonb`. Also UPDATE any other existing swarm row with `'[]'::jsonb` explicitly so CONTEXT §6 ("populated for every existing swarm row") is satisfied. No new RLS policy (column inherits public.swarms policy per RESEARCH Pattern 2). Header comment cites Phase 84 D-03.

(b) supabase/migrations/20260520_phase84_noise_categories.sql — 8 idempotent UPSERT blocks following 20260511_swarm_noise_spam_key.sql verbatim. One block per category key from D-01. Each block inserts TWO rows (one for debtor-email, one for sales-email per D-08) with action='categorize_archive', swarm_dispatch=null (per D-04). Use these display_label / outlook_label / display_order tuples:
  - coupa_invoice_paid_notification → 'Coupa: Invoice Paid' / 'Payment Admittance' / 80
  - coupa_invoice_approved_notification → 'Coupa: Invoice Approved for Payment' / 'Payment Admittance' / 81
  - iss_ptp_autoreply → 'ISS PtP Auto-Reply' / 'Auto-Reply' / 70
  - frieslandcampina_portal_reject → 'FrieslandCampina Portal Reject' / 'System Notification' / 90
  - m365_quarantine → 'Microsoft 365 Quarantine' / 'System Notification' / 91
  - sender_phishing_notice → 'Sender Phishing Notice' / 'System Notification' / 92
  - supplier_bank_change_notification → 'Supplier Bank Change Notification' / 'System Notification' / 93
  - own_outbound_invoice_loopback → 'Own Outbound Invoice Loopback' / 'System Notification' / 95
Each UPSERT uses `on conflict (swarm_type, category_key) do update set display_label=excluded.display_label, outlook_label=excluded.outlook_label, action=excluded.action, swarm_dispatch=excluded.swarm_dispatch, display_order=excluded.display_order, updated_at=now();`. Header comment block: explain the debtor-email = code-resident regex in classify.ts vs sales-email = registry-only (LLM 2nd-pass picks up the key from call-time closed list) asymmetry per RESEARCH Pitfall 5. State explicitly "Hard-separation invariant: NO row added to swarm_intents."

(c) supabase/migrations/20260520_phase84_classifier_rules_seed.sql — 8 INSERT rows for debtor-email with kind='regex' status='candidate' rule_key matching category_key verbatim (per Open Q #3 flat naming); 8 INSERT rows for sales-email with kind='agent_intent' status='candidate' (sales-email has no Pass 1 regex module per Pitfall 5). Use `on conflict (swarm_type, rule_key) do nothing`. notes column: 'Phase 84 D-01. Promotion gate per D-05 (Wilson-CI OR corpus-evidence).' Cite the seed pattern from PATTERNS.md lines 122-133.</action>
  <verify>
    <automated>test -f supabase/migrations/20260520_phase84_tenant_domains.sql &amp;&amp; test -f supabase/migrations/20260520_phase84_noise_categories.sql &amp;&amp; test -f supabase/migrations/20260520_phase84_classifier_rules_seed.sql &amp;&amp; grep -c "on conflict" supabase/migrations/20260520_phase84_noise_categories.sql &amp;&amp; grep -E "coupa_invoice_paid_notification|coupa_invoice_approved_notification|iss_ptp_autoreply|frieslandcampina_portal_reject|m365_quarantine|sender_phishing_notice|supplier_bank_change_notification|own_outbound_invoice_loopback" supabase/migrations/20260520_phase84_noise_categories.sql | grep -v '^--' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Three migration files exist with the exact filenames above.
    - tenant_domains migration adds the column with NOT NULL DEFAULT '[]'::jsonb and contains UPDATEs for at least swarm_type='debtor-email' and swarm_type='sales-email'.
    - noise_categories migration contains all 8 D-01 category keys (verify via grep returns ≥16 non-comment occurrences across the 8 UPSERT blocks).
    - noise_categories migration includes NO row referencing the table `swarm_intents` (Pitfall 1 / hard-separation).
    - classifier_rules_seed migration uses kind='regex' for debtor-email rows and kind='agent_intent' for sales-email rows (verify grep counts: 8 each).
    - Header comment in noise_categories migration explicitly mentions "Hard-separation" and "Pitfall 5" or "Pass 1 regex module" asymmetry.
  </acceptance_criteria>
  <done>All three migrations written; file count + content checks pass; ready for db push.</done>
</task>

<task type="auto">
  <name>Task 2: Codegen — gen-tenant-domains.ts + generated file + package.json wiring + SwarmRow type extension</name>
  <read_first>
    - web/scripts/gen-entity-types.ts (full file — verbatim structural mirror)
    - web/lib/automations/debtor-email/coordinator/entity.generated.ts (existing generated file — shape reference)
    - web/lib/swarms/types.ts (SwarmRow definition — add tenant_domains field)
    - web/package.json (existing `codegen` script entry)
    - .planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-PATTERNS.md (lines 137-184 for codegen mirror; lines 222-231 for output shape)
    - CLAUDE.md (Supabase patterns "Build-time codegen for registry-driven literal-union TS types" — Phase 69 D-03 precedent)
  </read_first>
  <action>Create web/scripts/gen-tenant-domains.ts as a structural mirror of web/scripts/gen-entity-types.ts. Mirror imports + dotenv loading + idempotency block (read existing → compare → skip write) verbatim. Differences from analog: OUTPUT_PATH = `web/lib/automations/debtor-email/coordinator/tenant-domains.generated.ts`; DB query = `SELECT swarm_type, tenant_domains FROM public.swarms ORDER BY swarm_type`; renderBody emits a per-swarm map shape per RESEARCH Open Q #4: `export const TENANT_DOMAINS_BY_SWARM = { "debtor-email": ["..."], "sales-email": ["..."] } as const;` followed by `export type TenantDomain = (typeof TENANT_DOMAINS_BY_SWARM)[keyof typeof TENANT_DOMAINS_BY_SWARM][number];`. Alphabetical sort applied per-swarm to keep diffs stable.

Extend web/package.json `codegen` npm script to chain both generators sequentially: `tsx scripts/gen-entity-types.ts && tsx scripts/gen-tenant-domains.ts`.

Extend web/lib/swarms/types.ts SwarmRow interface with `tenant_domains: string[];` field. Citation comment "// Phase 84 D-03".

Run `cd web && npm run codegen` to produce web/lib/automations/debtor-email/coordinator/tenant-domains.generated.ts. Commit the generated file.</action>
  <verify>
    <automated>cd web &amp;&amp; npm run codegen &amp;&amp; test -f web/lib/automations/debtor-email/coordinator/tenant-domains.generated.ts &amp;&amp; grep -q "TENANT_DOMAINS_BY_SWARM" web/lib/automations/debtor-email/coordinator/tenant-domains.generated.ts &amp;&amp; grep -q "debtor-email" web/lib/automations/debtor-email/coordinator/tenant-domains.generated.ts &amp;&amp; cd .. &amp;&amp; git diff --exit-code web/lib/automations/debtor-email/coordinator/tenant-domains.generated.ts</automated>
  </verify>
  <acceptance_criteria>
    - web/scripts/gen-tenant-domains.ts exists and imports from @supabase/supabase-js.
    - web/lib/automations/debtor-email/coordinator/tenant-domains.generated.ts exists and exports TENANT_DOMAINS_BY_SWARM as a per-swarm map (not a flat list).
    - Per-swarm domain arrays are alphabetically sorted (verify by reading the file).
    - web/package.json `codegen` script chains gen-entity-types + gen-tenant-domains.
    - web/lib/swarms/types.ts SwarmRow contains tenant_domains: string[].
    - `cd web && npm run codegen && git diff --exit-code` succeeds (idempotency).
  </acceptance_criteria>
  <done>Codegen script + generated file + package.json + type extension landed; CI gate `npm run codegen && git diff --exit-code` passes.</done>
</task>

<task type="auto">
  <name>Task 3: Retire Phase 83 TENANT_DOMAINS stub in debtor-email-coordinator.ts</name>
  <read_first>
    - web/lib/inngest/functions/debtor-email-coordinator.ts (lines 46-50, the TODO contract)
    - web/lib/automations/debtor-email/coordinator/tenant-domains.generated.ts (just-emitted, Task 2)
    - .planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-PATTERNS.md (lines 287-316, Option A vs B; Option B recommended)
    - .planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-CONTEXT.md (D-03)
  </read_first>
  <action>In web/lib/inngest/functions/debtor-email-coordinator.ts, replace the lines 46-50 stub `const TENANT_DOMAINS = ["smeba.nl", "smeba-fire.be", "moyneroberts.com"];` (and its surrounding TODO comment) with an import from the generated file (Option B per PATTERNS.md lines 287-316 — preferred over Option A registry-read because it's compile-time literal-union safe and matches the entity.generated.ts precedent). Add: `import { TENANT_DOMAINS_BY_SWARM } from "@/lib/automations/debtor-email/coordinator/tenant-domains.generated";` and replace usages with `TENANT_DOMAINS_BY_SWARM["debtor-email"]` (or whatever swarm_type identifier is in scope at the call site — verify by reading lines 50-80 surrounding context). Preserve all downstream behavior unchanged. Remove the TODO(phase-84 D-03) comment since the wiring contract is now satisfied.</action>
  <verify>
    <automated>cd web &amp;&amp; grep -n "TENANT_DOMAINS" lib/inngest/functions/debtor-email-coordinator.ts &amp;&amp; grep -q "tenant-domains.generated" lib/inngest/functions/debtor-email-coordinator.ts &amp;&amp; ! grep -q "TODO(phase-84" lib/inngest/functions/debtor-email-coordinator.ts &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "debtor-email-coordinator" || echo "tsc clean for coordinator"</automated>
  </verify>
  <acceptance_criteria>
    - Stub `const TENANT_DOMAINS = ["smeba.nl", "smeba-fire.be", "moyneroberts.com"];` no longer appears in the file (grep returns 0 non-comment occurrences of the bracketed list).
    - Import from `@/lib/automations/debtor-email/coordinator/tenant-domains.generated` present.
    - TODO(phase-84 D-03) comment removed (grep returns 0).
    - `npx tsc --noEmit` reports no new errors for debtor-email-coordinator.ts.
  </acceptance_criteria>
  <done>Stub retired; coordinator reads tenant_domains from generated registry import; tsc clean.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4 [BLOCKING]: supabase db push — apply Phase 84 schema to production</name>
  <read_first>
    - supabase/migrations/20260520_phase84_tenant_domains.sql
    - supabase/migrations/20260520_phase84_noise_categories.sql
    - supabase/migrations/20260520_phase84_classifier_rules_seed.sql
    - CLAUDE.md (Supabase patterns — pre-push hook `npm run check:supabase` runs automatically; SUPABASE_ACCESS_TOKEN env var required for non-TTY)
  </read_first>
  <action>BLOCKING — execute the live schema push. Ensure SUPABASE_ACCESS_TOKEN is set (export from web/.env.local or operator session). From repo root run `supabase db push` to apply the three Phase 84 migrations to the production database. Watch for: (a) any RLS advisor ERROR (the pre-push hook `npm run check:supabase` runs first — column-add inherits policies so this should be clean); (b) the per-swarm UPDATE statement count (≥2 UPDATEs expected); (c) ON CONFLICT path for noise_categories (re-run-safe). If the push requires interactive confirmation, the operator confirms each migration. After push completes, verify via Supabase REST API or psql: `SELECT swarm_type, jsonb_array_length(tenant_domains) FROM public.swarms;` returns rows with length ≥1 for debtor-email and sales-email; `SELECT count(*) FROM public.swarm_noise_categories WHERE category_key IN ('coupa_invoice_paid_notification', 'coupa_invoice_approved_notification', 'iss_ptp_autoreply', 'frieslandcampina_portal_reject', 'm365_quarantine', 'sender_phishing_notice', 'supplier_bank_change_notification', 'own_outbound_invoice_loopback');` returns 16. Also re-run the hard-separation static-check from Wave 0 Task 2 against live registry to confirm intersection is still empty.</action>
  <verify>
    <automated>SUPABASE_URL=$(grep -E '^SUPABASE_URL=' web/.env.local | cut -d= -f2- | tr -d '"') &amp;&amp; SUPABASE_SERVICE_ROLE_KEY=$(grep -E '^SUPABASE_SERVICE_ROLE_KEY=' web/.env.local | cut -d= -f2- | tr -d '"') &amp;&amp; curl -s "${SUPABASE_URL}/rest/v1/swarm_noise_categories?category_key=in.(coupa_invoice_paid_notification,coupa_invoice_approved_notification,iss_ptp_autoreply,frieslandcampina_portal_reject,m365_quarantine,sender_phishing_notice,supplier_bank_change_notification,own_outbound_invoice_loopback)&amp;select=swarm_type,category_key" -H "apikey: ${SUPABASE_SERVICE_ROLE_KEY}" -H "Authorization: Bearer ${SUPABASE_SERVICE_ROLE_KEY}" | python3 -c "import sys,json; d=json.load(sys.stdin); print('rows:', len(d)); assert len(d)==16, f'expected 16 got {len(d)}'"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` completes without ERROR-level advisor lint.
    - REST API confirms exactly 16 rows in public.swarm_noise_categories matching the 8 Phase 84 keys × 2 swarms.
    - public.swarms.tenant_domains exists and is populated for debtor-email and sales-email (jsonb_array_length ≥ 1).
    - Hard-separation static-check from Wave 0 Task 2 still passes against live data.
    - Pre-push hook `npm run check:supabase` returned clean (no ERROR-level lints).
  </acceptance_criteria>
  <done>Schema live; 16 rows confirmed; tenant_domains populated; hard-separation invariant holds.</done>
  <resume-signal>Type "schema live" or describe push error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → migration | Service-role write to live production schema |
| migration → Postgres | RLS already enforced on public.swarms + public.swarm_noise_categories; column-add inherits |
| codegen script → Supabase | Service-role read for tenant_domains snapshot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-02-01 | T (Tampering) | swarms.tenant_domains | mitigate | Operator-verified domain list committed in migration file; codegen CI gate `npm run codegen && git diff --exit-code` catches drift between registry and generated file (R-05 in CONTEXT) |
| T-84-02-02 | I (Info disclosure) | RLS column-add | accept | Column inherits existing service-role + authenticated-read policies on public.swarms (verified 20260429b_swarm_registry.sql:67-83); no anon grant added |
| T-84-02-03 | T (Integrity) | Hard-separation invariant | mitigate | Wave 0 Task 2 static-check test runs against live registry after this plan; Pitfall 1 enforcement |
| T-84-02-04 | D (DoS via mis-input) | tenant_domains operator input | mitigate | Domain list hardcoded in migration file (operator-reviewed); not user-supplied; lowercase + domain-shape implicit in committed values |
| T-84-02-05 | T (codegen drift) | tenant-domains.generated.ts | mitigate | CI gate `npm run codegen && git diff --exit-code`; Phase 69 D-03 precedent |
</threat_model>

<verification>
- Three migration files exist; `supabase db push` completes; live registry matches expected row count.
- Codegen produces tenant-domains.generated.ts; `npm run codegen && git diff --exit-code` passes.
- debtor-email-coordinator.ts no longer carries the Phase 83 stub.
- Wave 0 Task 2 static-check (hard-separation) still passes against live data.
</verification>

<success_criteria>
- All 4 tasks complete; BLOCKING db push succeeded.
- Verification SQL returns 16 noise-category rows + populated tenant_domains for ≥2 swarms.
- Codegen idempotent gate (`npm run codegen && git diff --exit-code`) passes.
- `npm run check:supabase` reports no ERROR-level lints attributable to Phase 84 migrations.
- Wave 0 RED tests for classify.ts still RED (Wave 2 will turn them GREEN).
</success_criteria>

<output>
After completion, create `.planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-02-SUMMARY.md`
</output>
