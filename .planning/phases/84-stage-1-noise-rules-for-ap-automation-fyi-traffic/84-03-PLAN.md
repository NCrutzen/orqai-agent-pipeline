---
phase: 84-stage-1-noise-rules-for-ap-automation-fyi-traffic
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - web/lib/debtor-email/classify.ts
  - web/lib/inngest/functions/classifier-screen-worker.ts
  - web/lib/inngest/functions/stage-0-safety-worker.ts
autonomous: true
must_haves:
  truths:
    - "classify.ts contains 7 new matchers for the in-classifier Phase 84 keys (loopback excluded), placed BEFORE existing SUBJECT_AUTO_REPLY and SENDER_PAYMENT_ROLE matchers per Pitfall 2"
    - "Coupa-Betaald matcher anchors on 'door ISS' suffix only (per RESEARCH Open Q #2) — does NOT match 'door CBRE' / 'betwist'"
    - "classifier-screen-worker.ts evaluates own_outbound_invoice_loopback in the worker (NOT classify.ts) per Pitfall 3"
    - "Loopback evaluation enforces direction='inbound' guard (D-03 / R-02 mitigation)"
    - "classifier/screen.requested event payload widened to carry direction; stage-0-safety-worker.ts producer populates it"
    - "Wave 0 RED tests (Task 1 + Task 2 loopback group) are now GREEN"
    - "Hard-separation static-check still passes"
  artifacts:
    - path: "web/lib/debtor-email/classify.ts"
      provides: "7 new regex matchers + extended Category union (8 new members)"
    - path: "web/lib/inngest/functions/classifier-screen-worker.ts"
      provides: "Loopback rule branch evaluated after Pass 1 regex, before Pass 2 LLM; reads swarmRow.tenant_domains via existing loadSwarm() cache"
    - path: "web/lib/inngest/functions/stage-0-safety-worker.ts"
      provides: "Producer-side direction passthrough on classifier/screen.requested emit"
  key_links:
    - from: "classify.ts (Phase 84 matchers)"
      to: "swarm_noise_categories.category_key (live registry)"
      via: "string equality on the 8 D-01 keys"
      pattern: "coupa_invoice_paid_notification|coupa_invoice_approved_notification|iss_ptp_autoreply|frieslandcampina_portal_reject|m365_quarantine|sender_phishing_notice|supplier_bank_change_notification"
    - from: "classifier-screen-worker.ts loopback branch"
      to: "swarms.tenant_domains via loadSwarm() registry cache"
      via: "swarmRow.tenant_domains array contains lookup"
      pattern: "tenant_domains.*includes"
    - from: "stage-0-safety-worker.ts emit"
      to: "classifier-screen-worker.ts event destructure"
      via: "direction field on classifier/screen.requested data payload"
      pattern: "direction"
---

<objective>
Wave 2: ship the regex matchers + loopback wiring. Turn Wave 0's RED tests GREEN.

Purpose: implement D-01's 7 in-classifier matchers in classify.ts (specificity-ordered per Pitfall 2), implement D-03's own_outbound_invoice_loopback in classifier-screen-worker.ts (NOT classify.ts per Pitfall 3), thread the `direction` field end-to-end from stage-0-safety-worker producer through screen-worker consumer.

Output: live Stage 1 classifier matches the 8 Phase 84 categories deterministically; loopback rule fires only on inbound + tenant-domain matches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-CONTEXT.md
@.planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-RESEARCH.md
@.planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-PATTERNS.md
@.planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-01-SUMMARY.md
@.planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-02-SUMMARY.md
@CLAUDE.md

<interfaces>
From web/lib/debtor-email/classify.ts (current — Phase 84 EXTENDS):
```typescript
export type Category =
  | "auto_reply" | "ooo_temporary" | "ooo_permanent"
  | "payment_admittance" | "spam" | "unknown";
// Phase 84 EXTENDS with 7 new union members (loopback NOT here — it's worker-side):
//   "coupa_invoice_paid_notification" | "coupa_invoice_approved_notification"
//   | "iss_ptp_autoreply" | "frieslandcampina_portal_reject" | "m365_quarantine"
//   | "sender_phishing_notice" | "supplier_bank_change_notification"
//   | "own_outbound_invoice_loopback"   ← worker assigns this; classify.ts never returns it

export function classify(input: { subject: string; from: string; bodySnippet: string }):
  { category: Category; confidence: number; matchedRule: string };
```

From web/lib/inngest/functions/classifier-screen-worker.ts (current — Wave 2 EXTENDS):
- Existing step.run("load-swarm-row", ...) at lines 167-177 loads swarmRow + categories
- Existing step.run("regex", ...) at line 181 calls classify() and produces regexOutcome
- Line 201 comment: "sender_email is not on the classifier/screen.requested payload" — confirm whether direction is on the payload today; if not, widen
- Line 227 `if (regexOutcome.category === "unknown")` is where LLM 2nd-pass invocation begins
- Phase 84 inserts loopback evaluation BETWEEN "regex" step.run completion and the LLM 2nd-pass branch

From CONTEXT D-03 loopback rule body:
`direction = 'inbound' AND lower(split_part(from_address, '@', 2)) = ANY(swarms.tenant_domains)`

From web/lib/automations/debtor-email/coordinator/tenant-domains.generated.ts (Wave 1 Task 2):
```typescript
export const TENANT_DOMAINS_BY_SWARM = {
  "debtor-email": ["fire-control.nl","moyneroberts.com","smeba-fire.be","smeba.nl"],
  "sales-email":  ["smeba.nl"],
} as const;
```
But per PATTERNS.md the worker reads via swarmRow.tenant_domains from loadSwarm() registry cache (Option A) — generated import is the coordinator's source (Wave 1 Task 3), worker reads live registry.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: classify.ts — 7 new matchers, specificity-ordered, Category union extended</name>
  <read_first>
    - web/lib/debtor-email/classify.ts (existing matchers lines 44-197; branch ordering lines 247-326; Pitfall 2 specificity-first reference)
    - web/lib/debtor-email/__tests__/classify.test.ts (Wave 0 Task 1 RED tests — these dictate the matcher behavior)
    - .planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-PATTERNS.md (lines 187-225 for matcher pattern; line 204 placement directive)
    - .planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-RESEARCH.md (Pitfall 2 ordering; Open Q #2 Coupa scope)
    - .planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-CONTEXT.md (D-01 8 keys; D-06 corpus-stable subject templates)
  </read_first>
  <behavior>
    - Extend Category union with all 8 new members (loopback included for type-completeness even though worker assigns it — keeps types coherent across the consumer side).
    - Add 7 new regex consts above the existing matchers, e.g.:
      - SUBJECT_COUPA_INVOICE_PAID = /^Factuur\s+\d{1,12}\s+gemarkeerd\s+als\s+Betaald\s+door\s+ISS$/i (anchored ^/$ — bounded \d{1,12} for ReDoS safety per V5 ASVS)
      - SUBJECT_COUPA_INVOICE_APPROVED = /^Factuur\s+\d{1,12}\s+is\s+goedgekeurd\s+voor\s+betaling\s+door\s+ISS$/i
      - SENDER_ISS_PTP = /^Invoice-PtP@nl\.issworld\.com$/i + SUBJECT_ISS_PTP_AUTOREPLY = /^Automatisch\s+antwoord:\s+Documenten\s+n\.a\.v\./i (sender+subject AND, NOT subject-only — Pitfall 2)
      - SENDER_FRIESLANDCAMPINA = /^Robbie\.Robot@frieslandcampina\.com$/i + SUBJECT_FC_CANDEX = /^FINAL_REMINDER_Invoice received for Candex related purchase\(s\)_/i
      - SENDER_M365_QUARANTINE = /^q2q@apexfire\.ie$/i + SUBJECT_M365_QUARANTINE = /Microsoft 365 security: You have messages in quarantine/i
      - SENDER_RSK_PHISHING = /^melanie@rskinstallatie\.nl$/i + SUBJECT_PHISHING_NOTICE = /(Uitleg pishing mail|Voorgaande mail niet openen)/i
      - SENDER_FARMPLUS = /^info@farmplus\.nl$/i + BODY/SUBJECT_BANK_CHANGE = /(IBAN|bankrekening).*(gewijzigd|wijziging|changed|new)/i (sender-anchored to prevent over-broad match)
    - Add 7 new branches in the matcher dispatch — placed BEFORE existing SUBJECT_AUTO_REPLY and SENDER_PAYMENT_ROLE branches per Pitfall 2.
    - Each branch returns { category: "<d-01-key>", confidence: 0.99, matchedRule: "<d-01-key>" } (rule_key = category_key per Open Q #3 flat naming).
    - Loopback branch is NOT in classify.ts. The classify.ts loopback union member exists for type-completeness only — the function itself never returns it.
  </behavior>
  <action>Open web/lib/debtor-email/classify.ts. STEP 1: extend the Category union with all 8 D-01 keys. STEP 2: declare the 7 new regex consts as listed in &lt;behavior&gt;. Use anchored patterns (^/$) and bounded quantifiers (\d{1,12}) to prevent catastrophic backtracking per V5 ASVS. STEP 3: insert 7 new branches in the matcher dispatch, placed BEFORE the existing SUBJECT_AUTO_REPLY branch (find that branch first — RESEARCH says line ~292) and BEFORE SENDER_PAYMENT_ROLE (line ~319). Each branch combines sender-anchored AND subject-anchored matches with `&&` (sender alone or subject alone risks Pitfall 2 swallowing — see ISS auto-reply boundary test). For Coupa-Betaald: explicitly comment "Anchored on 'door ISS' only per RESEARCH Open Q #2 — door CBRE / betwist stay in Stage 3 for dispute visibility." Return shape: `return { category: "<key>", confidence: 0.99, matchedRule: "<key>" };` for each. Do NOT add a loopback branch — that's Task 2 in the worker.</action>
  <verify>
    <automated>cd web &amp;&amp; npx vitest run lib/debtor-email/__tests__/classify.test.ts --reporter=basic 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Category union contains all 8 D-01 strings (verify: grep -c per key in classify.ts ≥ 1 each).
    - All Wave 0 Task 1 RED tests now PASS (vitest output: ≥21 tests in the new describe-groups pass; 0 failures attributable to Phase 84).
    - Coupa-Betaald boundary test (subject "betwist door ISS") still asserts NOT coupa_invoice_paid_notification — meaning the regex does not falsely match disputes.
    - ISS PtP boundary (generic "Automatisch antwoord:" from random sender) routes to auto_reply, NOT iss_ptp_autoreply.
    - All 7 new branches placed BEFORE the line containing SUBJECT_AUTO_REPLY in the dispatch block (verify by reading file structure).
    - No `(...)+` nested quantifier patterns introduced (ReDoS guard).
  </acceptance_criteria>
  <done>Wave 0 classify tests GREEN; classify.ts dispatch order correct; ReDoS-safe patterns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: classifier-screen-worker.ts — loopback rule + direction passthrough</name>
  <read_first>
    - web/lib/inngest/functions/classifier-screen-worker.ts (lines 82-87 SendFn cast; 136-144 passthrough fields; 167-177 step.run load-swarm-row; 181 step.run regex; 201 sender_email/direction comment; 227 LLM 2nd-pass branch entry)
    - web/lib/inngest/functions/stage-0-safety-worker.ts (find the inngest.send call that emits classifier/screen.requested — add direction to the payload)
    - web/lib/swarms/types.ts (SwarmRow.tenant_domains added in Wave 1 Task 2)
    - web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts (Wave 0 Task 2 RED tests — these dictate behavior)
    - .planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-PATTERNS.md (lines 247-275 loopback placement; lines 267-275 event-widening pattern)
    - .planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-CONTEXT.md (D-03 loopback rule body; R-02 spoofing mitigation)
    - CLAUDE.md (Inngest patterns — never destructure inngest.send; this-binding learning dae6276)
  </read_first>
  <behavior>
    - stage-0-safety-worker.ts emit of classifier/screen.requested includes `direction: <inbound|outbound>` from the email_pipeline.emails row (read in the same step.run that already loads the email).
    - classifier-screen-worker.ts destructure block (around lines 136-144) extends to read directionFromEvent.
    - Loopback evaluation inserts AFTER step.run("regex", ...) completes and BEFORE the `if (regexOutcome.category === "unknown")` LLM 2nd-pass invocation (line ~227): `if (regexOutcome.category === "unknown") { const tenantDomains = swarmRow.tenant_domains ?? []; const fromDomain = (fromFromEvent ?? "").split("@")[1]?.toLowerCase() ?? ""; if (directionFromEvent === "inbound" && fromDomain && tenantDomains.includes(fromDomain)) { regexOutcome.category = "own_outbound_invoice_loopback"; regexOutcome.matchedRule = "own_outbound_invoice_loopback"; regexOutcome.invoked = true; } }`
    - Loopback does NOT trigger when direction='outbound' (R-02 mitigation D-03 guard).
    - Loopback does NOT trigger when tenantDomains is empty.
    - Loopback does NOT trigger when fromDomain is not in tenantDomains.
    - No new inngest.send emit (loopback returns inline by mutating regexOutcome).
  </behavior>
  <action>STEP 1: Open web/lib/inngest/functions/stage-0-safety-worker.ts. Find the inngest.send call that emits `name: "classifier/screen.requested"`. Add `direction` to the data payload (source: the email_pipeline.emails row already loaded in the same step). Use the SendFn cast pattern (CLAUDE.md dae6276) — do not destructure inngest.send.

STEP 2: Open web/lib/inngest/functions/classifier-screen-worker.ts. Around lines 136-144 (the existing Phase 82.2 Plan 07 D-A passthrough block), add `const directionFromEvent = event.data.direction as "inbound" | "outbound" | undefined;`.

STEP 3: After the existing `step.run("regex", ...)` block (line ~181-216) and BEFORE the LLM 2nd-pass branch (line ~227 `if (regexOutcome.category === "unknown")`), insert the loopback evaluation per &lt;behavior&gt;. This is an in-memory check — no side effects, no step.run wrapper needed. Citation comment: `// Phase 84 D-03 own_outbound_invoice_loopback (R-02 direction guard). Worker-side because classify.ts signature lacks 'from' and 'direction' (Pitfall 3).`

STEP 4: If a Pitfall 3 fallback is needed because direction is NOT on the producer-side event (e.g., stage-0 producer reads it elsewhere), add a step.run("load-direction", ...) that does a single-row `select direction from email_pipeline.emails where id = email_id limit 1` and use that. Prefer event-widening (STEP 1) over DB lookup per RESEARCH recommendation.

STEP 5: Confirm Wave 0 Task 2 loopback tests now GREEN.</action>
  <verify>
    <automated>cd web &amp;&amp; npx vitest run lib/inngest/functions/__tests__/classifier-screen-worker.test.ts -t "own_outbound_invoice_loopback" --reporter=basic 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - stage-0-safety-worker.ts inngest.send for classifier/screen.requested includes `direction` field (verify by grep `direction` near the relevant inngest.send call).
    - classifier-screen-worker.ts destructures directionFromEvent from event.data.
    - Loopback branch present and placed BETWEEN step.run("regex") completion and the `if (regexOutcome.category === "unknown")` LLM 2nd-pass entry (verify by reading the relevant 30-line span).
    - Wave 0 Task 2 loopback tests (4 it() blocks) all PASS.
    - Loopback branch never calls inngest.send (it mutates regexOutcome inline).
    - No `const send = inngest.send` destructure pattern introduced (CLAUDE.md dae6276 — grep returns 0 new matches).
    - All Phase 84 keys land in regexOutcome.matchedRule equal to regexOutcome.category (flat rule_key naming per Open Q #3).
  </acceptance_criteria>
  <done>Loopback wired; direction threaded end-to-end; Wave 0 Task 2 loopback tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite gate + supabase advisor sweep</name>
  <read_first>
    - CLAUDE.md (pre-push hook `npm run check:supabase` + Supabase patterns)
    - web/package.json (test + check:supabase scripts)
  </read_first>
  <action>Run the full validation sweep: (1) `cd web && npm run codegen && git diff --exit-code` (codegen drift gate per Wave 1 Task 2 — must stay clean). (2) `cd web && npm run test` (full vitest suite — must pass; Wave 0 Tasks 1+2 RED tests now GREEN; static-check still GREEN). (3) `npm run check:supabase` from repo root (ERROR-level advisor sweep — must be clean for Phase 84 migrations). (4) `cd web && npx tsc --noEmit` (type-check; the extended Category union + SwarmRow.tenant_domains must propagate cleanly through consumers). If any check fails, fix the underlying issue rather than masking — common causes: type narrowing for the new Category union members in consumers (page.tsx, label-resolver), or grep-gate misses on the codegen output.</action>
  <verify>
    <automated>cd web &amp;&amp; npm run codegen &amp;&amp; cd .. &amp;&amp; git diff --exit-code web/lib/automations/debtor-email/coordinator/tenant-domains.generated.ts &amp;&amp; cd web &amp;&amp; npm run test 2>&amp;1 | tail -20 &amp;&amp; cd .. &amp;&amp; npm run check:supabase 2>&amp;1 | tail -20 &amp;&amp; cd web &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm run codegen && git diff --exit-code` clean.
    - `cd web && npm run test` reports 0 failures; Wave 0 RED tests now GREEN; hard-separation static-check GREEN.
    - `npm run check:supabase` reports 0 ERROR-level advisor findings.
    - `npx tsc --noEmit` reports 0 type errors.
  </acceptance_criteria>
  <done>Full validation sweep green; Phase 84 code+data layer integrated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inngest event → screen worker | classifier/screen.requested payload widened with `direction` |
| swarmRow.tenant_domains → loopback decision | Operator-controlled list (Wave 1 Task 1 migration) drives an auto-archive decision |
| Inbound email → classify() | Regex pass on untrusted subject + from strings (potential ReDoS surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-03-01 | S (Spoofing) | own_outbound_invoice_loopback | mitigate | D-03 direction='inbound' guard implemented in Task 2 step 3; sender-spoofed external claim auto-archived is the explicit R-02 risk; Wave 0 Task 2 negative test exercises this case |
| T-84-03-02 | D (ReDoS) | 7 new regex consts | mitigate | All patterns anchored (^/$) with bounded quantifiers (\d{1,12}); no nested (...)+; corpus-validated stability per D-06; precedent from Phase 60-08 ReDoS-safe shapes |
| T-84-03-03 | T (Integrity) | direction event-widening | mitigate | Producer (stage-0-safety-worker) reads direction from email_pipeline.emails row already in scope; no client-supplied value enters the worker payload |
| T-84-03-04 | E (Elevation) | matchedRule attribution | accept | rule_key = category_key (flat per Open Q #3); cross-table joins by string equality on a closed set — no injection surface |
| T-84-03-05 | T (specificity drift) | matcher ordering | mitigate | Placement BEFORE SUBJECT_AUTO_REPLY + SENDER_PAYMENT_ROLE per Pitfall 2; corpus-backfill in Wave 3 surfaces any regression in pre-existing rules' agree count |
</threat_model>

<verification>
- Wave 0 Task 1 (7 classify groups) + Task 2 (loopback group) tests GREEN.
- Codegen + supabase advisor + tsc + vitest full sweep green.
- Direction field flows from stage-0-safety-worker producer to classifier-screen-worker consumer.
- Loopback only fires on direction='inbound' AND tenant-domain match.
</verification>

<success_criteria>
- 7 in-classifier matchers ship in classify.ts; placement is specificity-first.
- Loopback ships in classifier-screen-worker.ts (not classify.ts); D-03 guard active.
- All Wave 0 tests GREEN; no regressions on existing test suite.
- Codegen drift gate clean.
- supabase advisor clean.
- npx tsc clean.
</success_criteria>

<output>
After completion, create `.planning/phases/84-stage-1-noise-rules-for-ap-automation-fyi-traffic/84-03-SUMMARY.md`
</output>
