---
phase: 82.7.3-detail-pane-and-stage-1-list-uat-followups-round-2
verified: 2026-05-18T13:00:00Z
status: human_needed
score: 6/6 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Open Stage 1 noise tab in dirty-branch state (activate override form). Confirm only the detail-pane footer row renders Submit/Cancel/Reject — no 'Discard changes' button visible anywhere."
    expected: "Exactly one Submit button (lime fill), one Cancel override, one Reject — all in the footer. No other Submit/Cancel pair visible."
    why_human: "DOM element visibility in the dirty-branch state requires a running browser; cannot determine which branch is active via static grep."
  - test: "At 1440×900 viewport, open the detail-pane on Stage 1 (or any stage tab 0/1/3/4). Check whether the footer 'Submit override (Stage N) · Cancel override · Reject' row fits on a single line with no truncation."
    expected: "All three buttons fully visible; right edge of Reject button strictly inside the pane content box; no horizontal scrollbar inside the pane."
    why_human: "CSS layout / bounding-box verification requires a rendered viewport; 540px grid column is coded correctly but pixel-level overflow is a visual check."
  - test: "Load Stage 1 noise tab with the current live dataset. Count what percentage of rendered rows show a mailbox chip (small mono pill before the sender text)."
    expected: "≥ 95% of rows show a non-empty mailbox chip; no console errors on rows with mailbox_id null."
    why_human: "Chip coverage depends on the server-side mailboxLabels map populated by loadMailboxLabels(); data quality cannot be verified statically."
  - test: "Load Stage 1 noise tab with no ?predictor= / ?confidence= URL params. Verify the Predictor and Confidence chip rows are NOT visible; only 'Show advanced filters' button renders. Then click it to reveal both rows."
    expected: "On first load: only toggle button visible, no chip rows. After click: both rows visible, label switches to 'Hide advanced filters'."
    why_human: "Next.js SSR / client hydration and initial render state require a running browser to confirm."
  - test: "Load Stage 1 noise tab with ?predictor=llm_2nd_pass in the URL. Verify both chip rows render expanded immediately on load."
    expected: "Predictor and Confidence chip rows are visible on first paint; toggle shows 'Hide advanced filters'."
    why_human: "URL-param auto-expand initialiser runs on mount; requires a browser with that URL."
  - test: "On the Stage 1 noise tab, check that the leftmost chip now reads 'Needs review · N' where N is visibly smaller than 459 (the pre-fix total). Confirm N excludes skip-label rows."
    expected: "Chip label is 'Needs review'; count is a strict subset of total rows (skip-topic rows subtracted)."
    why_human: "Count value depends on live dataset; can only be meaningfully confirmed against a real data snapshot."
---

# Phase 82.7.3: Detail-pane + Stage-1 list UAT follow-ups (round 2) — Verification Report

**Phase Goal:** Eliminate 6 operator-visible defects surfaced in UAT 2026-05-18: collapse three submit/cancel surfaces to one canonical footer (G-01), replace REG/CAP shorthand with plain-English copy (G-02), stop the footer button row from truncating (G-03), surface mailbox identity on every row (G-04), fix the "All N" counter to reflect review-needed subset (G-05), and hide advanced filters behind a toggle (G-06).
**Verified:** 2026-05-18T13:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | stage-step.tsx has no inline cancel-override / submit-override data-testids | VERIFIED | grep returns 0 for both testids; JSDoc comment at L27 is the only match for the word "cancel override" |
| 2  | stage-1-widget.tsx has no "Discard changes" button block | VERIFIED | grep -c 'Discard changes' = 0; Esc/escape handler present (count ≥ 1) |
| 3  | detail-pane.tsx lime Submit fill preserved | VERIFIED | grep -c 'v7-lime' in detail-pane.tsx = 1; file NOT in phase diff |
| 4  | eval-type-radio.tsx renders no literal REG / CAP as user-facing text | VERIFIED | grep -cE '>REG<\|>CAP<\|"REG"\|"CAP"\|reg</span\|cap</span' = 0 |
| 5  | eval-type-radio.tsx carries all four operator-locked strings | VERIFIED | "Recent regression" = 2, "Used to work correctly" = 2, "New case" = 2, "Never handled correctly before" = 2 |
| 6  | eval_type payload values capability/regression preserved | VERIFIED | grep -cE '"capability"\|"regression"' in eval-type-radio.tsx = 8; override route test 7/7 passed |
| 7  | gridTemplateColumns widened to minmax(640px,1fr) 540px in all 5 stage shells | VERIFIED | zero remaining "minmax(0, 1fr) 460px" matches; 5/5 files have the new template |
| 8  | row-list.tsx accepts optional mailboxLabels prop and renders chip | VERIFIED | mailboxLabels?: declared; data-testid="row-mailbox-chip" present; null guard present; flexShrink:0 present |
| 9  | mailboxLabels={mailboxLabels} threaded to RowList in all 5 stage shells | VERIFIED | grep -l 'mailboxLabels={mailboxLabels}' returns 2 matches per file (one for RowList, one for UnifiedDetailPane) across all 5 shells |
| 10 | noise-category-chip-strip.tsx leftmost chip labelled "Needs review" not "All" | VERIFIED | label: "Needs review" = 1; label: "All" = 0 |
| 11 | "Needs review" count uses topic !== "skip" predicate | VERIFIED | grep -cE 'topic\s*!==\s*"skip"' = 1; needsReviewCount referenced 3 times; totalCount = 0 |
| 12 | predictor-confidence-chip-strip.tsx chip rows are inside {expanded && ...} conditional | VERIFIED | expanded state controls both chip-row divs (lines 156+); toggle always rendered |
| 13 | URL-param auto-expand initialiser runs once on mount (useState, not useEffect) | VERIFIED | useState(() => { ... }) functional initialiser at L78; useEffect with search dep = 0 |
| 14 | No useEffect with search in deps (Pitfall 5 guard) | VERIFIED | grep -cE 'useEffect\([^)]*search' = 0 |

**Score:** 6/6 requirements — all code-level truths verified

### Deferred Items

None.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx` | Inline action row deleted; props preserved | VERIFIED | 0 inline testids; onSubmitDirty/onCancelDirty on type (4 references) |
| `web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx` | Discard/Submit block deleted; Esc wired | VERIFIED | 0 Discard changes; Esc/escape reference present |
| `web/app/(dashboard)/automations/[swarm]/stage-1/components/eval-type-radio.tsx` | Plain-English copy; no pills | VERIFIED | All 4 strings present; 0 REG/CAP user-facing text |
| `web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/stage-1-widget.test.tsx` | Tests re-targeted; 4/4 pass | VERIFIED | npm test --run stage-1-widget: 1 file, 4 tests passed |
| `web/app/(dashboard)/automations/[swarm]/stage-1/noise-category-chip-strip.tsx` | "Needs review" label + predicate | VERIFIED | label confirmed; topic!="skip" predicate confirmed |
| `web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/noise-category-chip-strip.test.tsx` | Matchers updated; 6/6 pass | VERIFIED | npm test --run noise-category-chip-strip: 1 file, 6 tests passed |
| `web/app/(dashboard)/automations/[swarm]/stage-1/predictor-confidence-chip-strip.tsx` | Collapse toggle + URL auto-expand | VERIFIED | Toggle present; aria-expanded; useState init; no useEffect(search) |
| `web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx` | mailboxLabels prop + chip render | VERIFIED | prop declared; chip testid present; guard present; brand tokens present |
| `web/app/(dashboard)/automations/[swarm]/stage-0/page.tsx` | Grid widened + mailboxLabels threaded | VERIFIED | minmax(640px,1fr) 540px present; mailboxLabels={mailboxLabels} present |
| `web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx` | Grid widened + mailboxLabels threaded | VERIFIED | minmax(640px,1fr) 540px present; mailboxLabels={mailboxLabels} present |
| `web/app/(dashboard)/automations/[swarm]/stage-2/page.tsx` | Grid widened + mailboxLabels threaded | VERIFIED | minmax(640px,1fr) 540px present; mailboxLabels={mailboxLabels} present |
| `web/app/(dashboard)/automations/[swarm]/stage-3/client-shell.tsx` | Grid widened + mailboxLabels threaded | VERIFIED | minmax(640px,1fr) 540px present; mailboxLabels={mailboxLabels} present |
| `web/app/(dashboard)/automations/[swarm]/stage-4/client-shell.tsx` | Grid widened + mailboxLabels threaded | VERIFIED | minmax(640px,1fr) 540px present; mailboxLabels={mailboxLabels} present |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| stage-1-widget.tsx Esc handler | canonical footer discard dispatch | custom event bulk-review:override-discard (L274) | WIRED | Esc/escape reference confirmed; action row deleted; event wiring preserved |
| eval-type-radio.tsx card click | eval_type=capability/regression API payload | onChange → evalType state | WIRED | 8 capability/regression value references; override route test 7/7 pass |
| 5 stage shells RowList mount | row-list.tsx mailboxLabels prop | mailboxLabels={mailboxLabels} prop threading | WIRED | 2 matches per file (RowList + UnifiedDetailPane) across all 5 |
| row-list.tsx recipient column | mailboxLabels[row.mailbox_id] chip | three-condition guard: mailboxLabels && r.mailbox_id != null && mailboxLabels[r.mailbox_id] | WIRED | guard confirmed; chip testid present |
| noise-category-chip-strip.tsx count | topic !== "skip" predicate over RPC aggregate | needsReviewCount += c.count if topic !== "skip" | WIRED | predicate confirmed; no totalCount remaining |
| predictor-confidence-chip-strip.tsx expanded state | URL params on mount | useState functional initialiser reading search.get("predictor") / search.get("confidence") | WIRED | L78-84; pActive/cActive logic confirmed |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| noise-category-chip-strip.tsx | needsReviewCount | classifier_queue_counts RPC (already filters status='predicted') | Yes — RPC confirmed by RESEARCH; topic filter applied client-side | FLOWING |
| row-list.tsx mailbox chip | mailboxLabels[r.mailbox_id] | loadMailboxLabels() server-side loader (already loaded at each stage shell) | Yes — loader pre-existing, reused unchanged | FLOWING |
| predictor-confidence-chip-strip.tsx expanded | search.get("predictor") / search.get("confidence") | Next.js useSearchParams() (existing hook) | Yes — reads real URL params on mount | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| stage-1-widget unit tests (BLOCK-02 fix) | `npm --prefix web test -- --run stage-1-widget` | 4/4 passed (1.32s) | PASS |
| noise-category-chip-strip unit tests (BLOCK-01 fix) | `npm --prefix web test -- --run noise-category-chip-strip` | 6/6 passed (0.98s) | PASS |
| override API contract test (eval_type unchanged) | `npm --prefix web test -- --run "override/__tests__/route.test"` | 7/7 passed (0.78s) | PASS |
| Visual footer fit at 540px pane width (G-03) | CSS layout check | Cannot test without browser | SKIP |
| Mailbox chip coverage ≥ 95% on live dataset (G-04) | Requires live data | Cannot test without running app | SKIP |
| "Show advanced filters" on first load — no chip rows visible (G-06) | Requires browser render | Cannot test without browser | SKIP |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| G-01 | 82.7.3-01 | Single canonical Submit/Cancel surface (footer-only) | VERIFIED (code) | inline testids = 0 in both files; Discard changes = 0; lime Submit in detail-pane.tsx preserved |
| G-02 | 82.7.3-01 | Plain-English eval-type radio cards (no REG/CAP) | VERIFIED (code) | REG/CAP user-facing text = 0; all 4 operator-locked strings present; API contract test 7/7 |
| G-03 | 82.7.3-03 | Footer action row fully visible at post-fix pane width | VERIFIED (code) / HUMAN NEEDED (visual) | 540px grid column coded in all 5 shells; pixel-level fit requires browser |
| G-04 | 82.7.3-03 | Mailbox label chip on every row, all stage tabs | VERIFIED (code) / HUMAN NEEDED (coverage %) | prop declared + chip render in row-list.tsx; threaded in all 5 shells; live coverage requires dataset |
| G-05 | 82.7.3-02 | "Needs review" chip count reflects review-needed subset | VERIFIED (code) / HUMAN NEEDED (visual count) | topic!="skip" predicate confirmed; "All" label gone; visual decrease requires live data |
| G-06 | 82.7.3-02 | Predictor + Confidence rows hidden by default behind toggle | VERIFIED (code) / HUMAN NEEDED (render) | expanded conditional confirmed; toggle present; aria-expanded; URL auto-expand logic present |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| stage-1-widget.tsx | 331 | `placeholder=` | Info | HTML input placeholder attribute — not a stub; pre-existing, unrelated to phase |
| row-list.tsx | 231, 296 | "placeholder" word | Info | L231 = style-comment reference; L296 = `case "placeholder":` in existing switch — pre-existing, unrelated to phase |

No blockers. No new stubs introduced by this phase.

### Human Verification Required

All code-level acceptance criteria pass. The following 6 items require a browser + live data to confirm:

#### 1. Single Submit/Cancel in dirty-branch state (G-01 visual)

**Test:** Activate the override form on Stage 1 (make the branch dirty). Observe the screen.
**Expected:** Only the detail-pane footer shows Submit override (Stage N) / Cancel override / Reject — no "Discard changes" button anywhere on screen, no second Submit button.
**Why human:** Dirty-branch activation requires UI interaction; static grep confirms the JSX is deleted but render state requires a browser.

#### 2. Footer fits on one line — no truncation (G-03)

**Test:** At 1440×900 viewport, open Stage 1 (or any stage tab 0/1/3/4) detail-pane. Read the footer.
**Expected:** "Submit override (Stage N) · Cancel override · Reject" all visible on a single line; right edge of Reject inside the pane; no horizontal scrollbar inside the pane.
**Why human:** The 540px CSS column is correctly coded, but pixel-level bounding-box fit against real button widths requires a rendered viewport.

#### 3. Mailbox chip coverage ≥ 95% on live dataset (G-04)

**Test:** Load Stage 1 noise tab with the current live dataset. Count chips visible on rendered rows.
**Expected:** ≥ 95% of rows show a non-empty mailbox chip before the sender text. No console errors on null-mailbox_id rows.
**Why human:** Coverage depends on `loadMailboxLabels()` server-side data; static analysis confirms the chip render path is wired but cannot verify data quality.

#### 4. "Needs review" count visibly smaller than pre-fix total (G-05 visual)

**Test:** Load Stage 1 noise tab. Observe the leftmost chip label and count.
**Expected:** Chip shows "Needs review · N" where N is visibly smaller than 459 (the pre-fix All total), confirming skip-label rows are excluded.
**Why human:** Count value depends on live dataset composition; SPEC requires visual confirmation against the same dataset.

#### 5. Advanced filters collapse / expand on first load (G-06 default state)

**Test:** Load Stage 1 noise tab with no ?predictor= or ?confidence= URL params. Verify Predictor + Confidence chip rows are NOT visible; only "Show advanced filters" button is shown. Click the button. Verify both rows appear and button label changes to "Hide advanced filters".
**Expected:** Initial state collapsed; toggle reveals chips; aria-expanded attribute flips.
**Why human:** Next.js client hydration and first-render state require a browser.

#### 6. Advanced filters auto-expand on deep-link URL (G-06 URL-param)

**Test:** Load Stage 1 noise tab with ?predictor=llm_2nd_pass in the URL. Verify both chip rows render expanded on first paint.
**Expected:** Chips visible immediately; toggle shows "Hide advanced filters".
**Why human:** useState initialiser behavior on mount with a pre-seeded URL requires a browser.

### Gaps Summary

No code-level gaps. All 6 requirements have their implementation correctly in place:

- G-01: inline action rows deleted from both stage-step.tsx and stage-1-widget.tsx; detail-pane footer is the sole Submit/Cancel surface; lime Submit preserved.
- G-02: REG/CAP pill markup removed; all 4 operator-locked strings present; API contract test passes.
- G-03: gridTemplateColumns widened from minmax(0,1fr) 460px to minmax(640px,1fr) 540px across all 5 stage shells.
- G-04: optional mailboxLabels prop added to RowListProps; three-condition chip render guard in place; prop threaded to all 5 RowList call-sites.
- G-05: "All" label replaced with "Needs review"; count derivation uses topic!=="skip" predicate; test suite 6/6 green.
- G-06: chip rows inside {expanded && ...}; useState functional initialiser reads URL params once on mount; toggle button always rendered with aria-expanded.

The 6 human verification items are confirmatory visual / live-data checks — they verify the already-correct implementation against a real browser and real dataset, not gaps that need fixing.

---

_Verified: 2026-05-18T13:00:00Z_
_Verifier: Claude (gsd-verifier)_
