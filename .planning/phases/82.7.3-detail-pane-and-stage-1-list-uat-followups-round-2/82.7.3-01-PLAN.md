---
phase: 82.7.3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/components/eval-type-radio.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/stage-1-widget.test.tsx
autonomous: true
requirements: [G-01, G-02]
tags: [ui, override-form, stage-1, detail-pane, copy-rework]

must_haves:
  truths:
    - "In the dirty-branch state on Stage 1, exactly one Submit/Cancel pair renders — the detail-pane footer pair. No 'Discard changes' button anywhere in the DOM."
    - "stage-step.tsx has no inline 'cancel override + Submit override' action row (the 82.7.2-01 block at L253-309 is gone)."
    - "stage-1-widget.tsx has no 'Discard changes' button block (the L355-422 block is gone)."
    - "eval-type-radio.tsx renders no literal 'REG' or 'CAP' as user-facing text; both cards carry sentence-case titles 'Recent regression' and 'New case' with subheads 'Used to work correctly' and 'Never handled correctly before'."
    - "The lime Submit fill (`background: var(--v7-lime)`) is preserved on the surviving canonical Submit button inside `_shell/detail-pane.tsx`."
    - "The Esc keyboard handler in `stage-1-widget.tsx` still dispatches the canonical discard flow; ⌘⏎ still submits; `c` / `g` keyboard shortcuts still toggle eval_type."
    - "API contract test at `web/app/api/automations/debtor-email/override/__tests__/route.test.ts` continues to pass — eval_type payload (`capability` | `regression`) unchanged."
    - "`stage-1-widget.test.tsx` continues to pass after the inline Submit button is deleted — submit-click assertions have been re-targeted (the harness renders Stage1Widget in isolation without the footer, so the inline button cannot be relied on)."
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx"
      provides: "Stage-1 widget with inline Discard/Submit pair deleted; canonical footer is the only Submit/Cancel surface"
      contains: "stage-1-widget"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx"
      provides: "Stage-step dirty-branch render with inline action row deleted; onSubmitDirty/onCancelDirty props preserved (unused-but-typed)"
      contains: "onSubmitDirty"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/components/eval-type-radio.tsx"
      provides: "Plain-English radio cards (Recent regression / New case) with REG/CAP pill markup removed"
      contains: "Recent regression"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/stage-1-widget.test.tsx"
      provides: "RTL tests re-targeted after inline Submit-button deletion — submit-click assertions now invoke the dirty-state submit path without depending on the deleted inline button"
      contains: "Stage1Widget"
  key_links:
    - from: "stage-1-widget.tsx Esc keyboard handler"
      to: "canonical footer discardOverride dispatch"
      via: "keyboard-shortcuts.tsx:134 Esc binding (unchanged)"
      pattern: "discardOverride|onCancelDirty"
    - from: "eval-type-radio.tsx card click"
      to: "/api/automations/debtor-email/override eval_type payload"
      via: "onChange dispatch to evalType state (capability|regression)"
      pattern: "capability|regression"
---

<objective>
Implement G-01 (single canonical Submit/Cancel surface — footer only) and
G-02 (plain-English eval-type radio cards, no REG/CAP shorthand) per
locked decisions H-01 + H-02 in CONTEXT.md.

Purpose: Operator UAT 2026-05-18 (round 2) — three concurrent
Submit/Cancel pairs render simultaneously during the dirty-branch state
(footer + stage-step inline pair from 82.7.2-01 + stage-1-widget
"Discard changes" block), and the REG/CAP pills on the eval-type radio
use engineering shorthand the operator does not parse intuitively.

Output: Three source edits — two pure JSX deletes (H-01) and one
in-file copy + markup rewrite (H-02) — PLUS one test-file update
(`stage-1-widget.test.tsx`) so the unit suite continues to pass after
the inline Submit button is deleted. No prop signatures change, no API
contract touched, no new dependencies.

**Regression guard:** `var(--v7-lime)` on the surviving Submit button
inside `_shell/detail-pane.tsx` must remain. `Esc` / `⌘⏎` / `c` / `g`
keyboard shortcuts must continue to fire end-to-end. The unit test for
Stage1Widget must remain green (BLOCK-02 from PLAN-CHECK).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/82.7.3-detail-pane-and-stage-1-list-uat-followups-round-2/82.7.3-SPEC.md
@.planning/phases/82.7.3-detail-pane-and-stage-1-list-uat-followups-round-2/82.7.3-CONTEXT.md
@.planning/phases/82.7.3-detail-pane-and-stage-1-list-uat-followups-round-2/82.7.3-RESEARCH.md
@.planning/phases/82.7.3-detail-pane-and-stage-1-list-uat-followups-round-2/82.7.3-PATTERNS.md
@.planning/phases/82.7.3-detail-pane-and-stage-1-list-uat-followups-round-2/82.7.3-PLAN-CHECK.md
@.planning/phases/82.7.2-override-form-rework-and-tooltip-fix/82.7.2-01-SUMMARY.md
@web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/components/eval-type-radio.tsx
@web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/stage-1-widget.test.tsx
</context>

<interfaces>
<!-- Block to DELETE from stage-step.tsx (L253-309, introduced by 82.7.2-01): -->

```tsx
<div className="flex items-center justify-end gap-2 mt-1">
  <button
    type="button"
    onClick={() => onCancelDirty?.()}
    aria-label={`Cancel override on Stage ${stage.n}`}
    data-testid={`stage-${stage.n}-cancel-override`}
    style={{ ...background: "transparent", border: "1px solid var(--v7-brand-secondary)", ... }}
  >cancel override</button>
  <button
    ...
    data-testid={`stage-${stage.n}-submit-override`}
    style={{ background: "var(--v7-lime)", ... }}
  >
    <span data-row="primary">Submit override</span><kbd>⌘⏎</kbd>
  </button>
</div>
```

PRESERVE: the `onSubmitDirty` / `onCancelDirty` props on `StageStepProps`
(CONTEXT H-01 — they stay on the type, the props become unused on this
component). PRESERVE the surrounding "Note and override are saved
together" muted hint at L240-245 from 82.7.2-01. PRESERVE the
`state === "ok"` and `state === "skipped"` branches entirely.

<!-- Block to DELETE from stage-1-widget.tsx (L355-422 — the WHOLE action-row div): -->

Anchor on the unique `<div className="flex items-center justify-end gap-2" style={{paddingTop: "var(--space-4)"}}>`
container around L355-358 (PATTERNS Pitfall 2). The entire `<div>...</div>`
subtree containing the `Discard changes` ghost button AND the `Submit
override` lime button is the delete target.

PRESERVE: the `Esc` keyboard handler wiring at `stage-1-widget.tsx:134`.
PRESERVE: the `notesRequired` red pill / red text warnings near L302-L325
(different concern). PRESERVE the override-form fields above L355.

<!-- Surviving canonical footer (no edit) — _shell/detail-pane.tsx:495+: -->

The detail-pane footer renders `Submit override (Stage N) · Cancel
override · Reject` and is the ONLY Submit/Cancel/Reject surface after
this plan. The lime fill (`background: var(--v7-lime)`) on the Submit
button there is the regression guard from 82.7.1-04. DO NOT EDIT this
file.

<!-- eval-type-radio.tsx target shape (H-02 final): -->

Two radio cards differentiated by TEXT only — no pills, no dots, no
colored badges. Reuse the existing `<label>` card shell + `RadioGroupItem`
(`@/components/ui/radio-group`) + the `checked` border swap
(brand-primary outline when selected). Card structure after edit:

```tsx
<label className="flex flex-col gap-2 p-3 rounded-[var(--v7-radius-sm)] border cursor-pointer transition-colors duration-150"
  style={{
    background: checked ? "var(--v7-brand-primary-soft)" : "var(--v7-panel-2)",
    borderColor: checked ? "var(--v7-brand-primary)" : "var(--v7-line)",
  }}>
  <div className="flex items-center gap-2">
    <RadioGroupItem value={option} aria-checked={checked} aria-label={`${title} — ${subhead}`} />
    <span style={{ fontWeight: 600, fontSize: 13 }}>{title}</span>
  </div>
  <span style={{ color: "var(--v7-text-muted)", fontSize: 12, fontFamily: "var(--font-mono)" }}>
    {subhead}
  </span>
</label>
```

Where (operator-locked):
- option `"regression"` → title `Recent regression`, subhead `Used to work correctly`
- option `"capability"` → title `New case`, subhead `Never handled correctly before`

KEEP: `evalType` state, `onChange` dispatchers, all `aria-*` attributes,
keyboard shortcuts `c` (capability) and `g` (regression) routed via
`_shell/keyboard-shortcuts.tsx:225-226`. The `pillClass: "reg" | "cap"`
prop and any `isReg` branch becomes dead — delete cleanly. API payload
(`eval_type: "capability" | "regression"`) stays identical.

<!-- stage-1-widget.test.tsx test scaffold (BLOCK-02 confirmation): -->

The test file at
`web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/stage-1-widget.test.tsx`
renders `<Stage1Widget>` in isolation inside a `<TestHarness>` wrapper
(L170+). The harness DOES NOT mount `detail-pane.tsx`, so the footer
Submit button is NOT in the DOM. Two test cases currently rely on the
inline button being present:

- L235-239 ("submits dispute override" path):
  ```tsx
  const submit = await screen.findByRole("button", { name: /submit override/i });
  fireEvent.click(submit);
  ```
- L302-305 ("marks the row for optimistic removal..." path):
  ```tsx
  const submit = await screen.findByRole("button", { name: /submit override/i });
  fireEvent.click(submit);
  ```

After H-01 deletes the inline button (Task 2), both `findByRole` queries
will throw. Re-target by dispatching the `⌘⏎` keyboard event the widget
already listens for (the canonical submit path that survives) — see
`stage-1-widget.tsx:134` for the keydown wiring. This keeps the test
intent (verifying that a dirty-branch submit fires `fetch(.../override)`
and `markPendingRemoval`) without depending on a deleted DOM element.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete inline action row in stage-step.tsx (H-01 part 1)</name>
  <files>web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx (full file)
    - .planning/phases/82.7.2-override-form-rework-and-tooltip-fix/82.7.2-01-SUMMARY.md (defines what was added at L253-309 in the parent phase — the delete target)
  </read_first>
  <behavior>
    - After edit, stage-step.tsx renders no inline `<button>` with
      `data-testid` of `stage-${stage.n}-submit-override` or
      `stage-${stage.n}-cancel-override`.
    - The "Note and override are saved together" muted hint from
      82.7.2-01 stays — it documents the coupling and is not part of
      the action row.
    - `onSubmitDirty` and `onCancelDirty` props remain on `StageStepProps`
      (CONTEXT H-01).
    - `state === "ok"` (override stage button) and `state === "skipped"`
      branches are untouched.
  </behavior>
  <action>
    1. Open `web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx`.
    2. Locate the dirty-branch block — the `<div className="flex items-center justify-end gap-2 mt-1">` introduced by 82.7.2-01 (currently at ~L253-309). It contains the Cancel button with `data-testid={`stage-${stage.n}-cancel-override`}` and the Submit button with `data-testid={`stage-${stage.n}-submit-override`}`.
    3. DELETE the entire `<div>...</div>` subtree. Nothing else changes.
    4. PRESERVE the surrounding "Note and override are saved together" muted hint div (the helper text at ~L240-245). PRESERVE the `{stage.widget}` render above.
    5. PRESERVE the `StageStepProps` type — do NOT remove `onSubmitDirty` or `onCancelDirty` from the prop signature even though they are now unused in this component (CONTEXT H-01).
    6. DO NOT touch `state === "ok"` (override-stage button at ~L194-225) or `state === "skipped"` (~L321+).
  </action>
  <verify>
    <automated>
    cd /Users/nickcrutzen/Developer/agent-workforce &amp;&amp; \
    test "$(grep -c 'data-testid={`stage-${stage.n}-cancel-override`}' 'web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx')" = "0" &amp;&amp; \
    test "$(grep -c 'data-testid={`stage-${stage.n}-submit-override`}' 'web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx')" = "0" &amp;&amp; \
    test "$(grep -c 'Note and override are saved together' 'web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx')" = "1" &amp;&amp; \
    test "$(grep -cE 'onSubmitDirty|onCancelDirty' 'web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx')" -ge "2" &amp;&amp; \
    echo "Task 1 grep gates green"
    </automated>
  </verify>
  <acceptance_criteria>
    - Zero matches for `stage-${stage.n}-submit-override` and `stage-${stage.n}-cancel-override` testids.
    - "Note and override are saved together" hint still present (one match).
    - `onSubmitDirty` and `onCancelDirty` still referenced in `StageStepProps` interface (prop signature intact).
    - `git diff stage-step.tsx` shows ONLY a deletion of the L253-309 action-row block (no other change).
  </acceptance_criteria>
  <done>
    stage-step.tsx no longer renders an inline Submit/Cancel pair; only the canonical footer in detail-pane.tsx will render those buttons going forward.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete Discard/Submit action row in stage-1-widget.tsx AND re-target the stage-1-widget unit test (H-01 part 2 + BLOCK-02 fix)</name>
  <files>web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx, web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/stage-1-widget.test.tsx</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx (full file; delete target at ~L355-422)
    - web/app/(dashboard)/automations/[swarm]/_shell/keyboard-shortcuts.tsx (verify Esc binding at L134 references the canonical footer's discard handler — NOT the inline pair we are deleting)
    - web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/stage-1-widget.test.tsx (BLOCK-02 — currently at L235-239 + L302-305 the test calls `findByRole("button", { name: /submit override/i })` against a `<Stage1Widget>` rendered in isolation. The harness DOES NOT mount detail-pane.tsx, so the inline button is the ONLY match today. After the source delete in step 2.1 below, this query will throw.)
  </read_first>
  <behavior>
    - After edit, stage-1-widget.tsx renders no `Discard changes` button and no sibling `Submit override` button in this widget.
    - The Esc keyboard handler wiring at line ~134 still dispatches the canonical discard flow (verified to route through the footer, not through the inline pair).
    - The `notesRequired` red warning pill / notes-area validation messages near L302-L325 are PRESERVED — they are a different concern.
    - The override-form fields ABOVE the action row are preserved.
    - `stage-1-widget.test.tsx` continues to pass: the two submit-click test cases (L235-239 "submits dispute override" + L302-305 "marks the row for optimistic removal...") are re-targeted to dispatch a `⌘⏎` keyboard event against the widget root (the canonical submit path the widget itself listens for at ~L134), instead of clicking the deleted inline button.
    - Test intent is preserved: both cases still assert `fetch("/api/automations/debtor-email/override", { method: "POST", ... })` and `markPendingRemoval("email-uuid-1")` after the submit.
  </behavior>
  <action>
    **Step 2.1 — Source delete (stage-1-widget.tsx):**
    1. Open `web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx`.
    2. Locate the action-row container at ~L355-358: a `<div>` with `className="flex items-center justify-end gap-2"` AND `style={{paddingTop: "var(--space-4)"}}` (PATTERNS Pitfall 2 — anchor on this unique container). The whole `<div>...</div>` subtree contains:
       - A `Discard changes` ghost button (left)
       - A `Submit override` lime-filled button (right) with `<kbd>⌘⏎</kbd>` keychip
    3. DELETE the entire `<div>...</div>` subtree (the action row only).
    4. PRESERVE: the Esc keyboard handler at ~L134 (it must continue to fire — it routes through the canonical footer's discard handler, not through the inline pair we are deleting). PRESERVE: the `notesRequired` red pill / red warning text near L302-L325 (different concern, NOT the action row). PRESERVE all override-form fields above L355.
    5. DO NOT modify any other widget surface or any prop signatures.

    **Step 2.2 — Test re-target (stage-1-widget.test.tsx — BLOCK-02):**
    6. Open `web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/stage-1-widget.test.tsx`.
    7. Confirm the harness scaffold (search for `function TestHarness` at ~L119) does NOT render `detail-pane.tsx`. It mounts `<Stage1Widget>` directly. Therefore the deleted inline button was the ONLY match for `/submit override/i` and the queries at L235-239 + L302-305 will throw after step 2.1.
    8. In the "submits dispute override" test case (`it(...)` covering ~L195-255), replace lines ~235-239:
       ```tsx
       // BEFORE (deletes-with-source):
       const submit = await screen.findByRole("button", { name: /submit override/i });
       fireEvent.click(submit);
       ```
       with a `⌘⏎` keyboard dispatch against the widget container — the canonical submit path the widget itself listens for at `stage-1-widget.tsx:~L134`:
       ```tsx
       // AFTER (canonical submit path — survives H-01):
       // Dispatch ⌘⏎ on document; widget's keydown listener (L~134) handles it.
       fireEvent.keyDown(document, { key: "Enter", metaKey: true });
       ```
       If the widget's keydown listener is scoped to a specific element (not `document`), dispatch against that element instead — verify via the `addEventListener` call in `stage-1-widget.tsx`.
    9. Apply the SAME substitution in the "marks the row for optimistic removal..." test case at ~L302-305.
    10. DO NOT change any of the subsequent `waitFor` / `expect(fetchSpy).toHaveBeenCalledWith(...)` / `expect(markPendingRemovalSpy).toHaveBeenCalledWith("email-uuid-1")` assertions. The test intent (the API call fires; the row is marked for optimistic removal) survives — only the submit-trigger mechanism changes.
    11. If the keydown approach proves brittle in practice (e.g. event scoping doesn't match the widget listener), the fallback is to invoke the dirty-state submit programmatically via the existing `onChange` handler chain — but try keydown first; it most closely mirrors operator behaviour.
    12. DO NOT add any import, do NOT modify the `TestHarness`, do NOT mount `detail-pane.tsx` (that would expand the test scope beyond Stage1Widget).
  </action>
  <verify>
    <automated>
    cd /Users/nickcrutzen/Developer/agent-workforce &amp;&amp; \
    test "$(grep -c 'Discard changes' 'web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx')" = "0" &amp;&amp; \
    test "$(grep -cE 'paddingTop:.*var\(--space-4\).*flex items-center justify-end|flex items-center justify-end.*paddingTop' 'web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx')" = "0" &amp;&amp; \
    test "$(grep -cE 'Esc|escape' 'web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx')" -ge "1" &amp;&amp; \
    test "$(grep -cE 'findByRole.*submit override' 'web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/stage-1-widget.test.tsx')" = "0" &amp;&amp; \
    test "$(grep -cE 'keyDown.*metaKey.*true|metaKey:\s*true.*Enter' 'web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/stage-1-widget.test.tsx')" -ge "2" &amp;&amp; \
    npm --prefix web test -- --run stage-1-widget &amp;&amp; \
    echo "Task 2 grep + unit test gates green"
    </automated>
  </verify>
  <acceptance_criteria>
    - Zero matches for the literal string `Discard changes` in `stage-1-widget.tsx`.
    - Zero matches for the action-row container's unique style fingerprint (`paddingTop: var(--space-4)` combined with `flex items-center justify-end`).
    - At least one match for `Esc` / `escape` (keyboard handler still wired).
    - Zero `findByRole(...submit override...)` queries remain in `stage-1-widget.test.tsx`.
    - At least two `⌘⏎` keyboard-dispatch sites in `stage-1-widget.test.tsx` (one per re-targeted test case).
    - `npm --prefix web test -- --run stage-1-widget` exits green.
    - `git diff stage-1-widget.tsx` shows ONLY the action-row deletion (~L355-422); no other source edits.
    - `git diff stage-1-widget.test.tsx` shows ONLY the two submit-click substitutions (L235-239 + L302-305); no harness changes, no new imports.
    - The canonical footer in `detail-pane.tsx` is UNCHANGED and still carries `var(--v7-lime)` on Submit (regression guard).
  </acceptance_criteria>
  <done>
    stage-1-widget.tsx no longer renders a `Discard changes` button or its sibling Submit pill. stage-1-widget.test.tsx submits via `⌘⏎` keydown (canonical path) and continues to pass. The detail-pane footer is the sole Submit/Cancel surface in the dirty-branch state.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Rewrite eval-type-radio.tsx with plain-English copy, no pills (H-02)</name>
  <files>web/app/(dashboard)/automations/[swarm]/stage-1/components/eval-type-radio.tsx</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/eval-type-radio.tsx (full file)
    - web/app/api/automations/debtor-email/override/__tests__/route.test.ts (verify eval_type=capability|regression payload assertion — must continue to pass without change)
    - web/app/(dashboard)/automations/[swarm]/_shell/keyboard-shortcuts.tsx (verify lines ~225-226 dispatch `c` → capability, `g` → regression — these must keep working)
  </read_first>
  <behavior>
    - Both radio cards differentiated by TEXT only; no pills, no dots, no colored badges.
    - Card 1 (option `"regression"`): title `Recent regression`, subhead `Used to work correctly`.
    - Card 2 (option `"capability"`): title `New case`, subhead `Never handled correctly before`.
    - Selection visual feedback reuses the existing `checked` border swap (brand-primary outline + brand-primary-soft background).
    - `evalType` state, `onChange` dispatchers, all `aria-*` attributes preserved.
    - Keyboard shortcuts `c` → capability, `g` → regression still dispatch through `_shell/keyboard-shortcuts.tsx:225-226`.
    - API payload (`eval_type: "capability" | "regression"`) unchanged.
    - The `pillClass: "reg" | "cap"` prop (or equivalent) and any `isReg` branching is removed cleanly — no dead code.
  </behavior>
  <action>
    1. Open `web/app/(dashboard)/automations/[swarm]/stage-1/components/eval-type-radio.tsx`.
    2. Identify the two card render blocks. Currently each card renders:
       - A `RadioGroupItem` (keep)
       - A pill `<span>` with red/lime background per L108-119 (`isReg ? "reg" : "cap"`) — **DELETE**
       - A subhead (currently `Used to work` / `New pattern`) — **REPLACE COPY**
    3. Rewrite each card to the shape in `<interfaces>` above:
       ```tsx
       <label className="flex flex-col gap-2 p-3 rounded-[var(--v7-radius-sm)] border cursor-pointer transition-colors duration-150"
         style={{
           background: checked ? "var(--v7-brand-primary-soft)" : "var(--v7-panel-2)",
           borderColor: checked ? "var(--v7-brand-primary)" : "var(--v7-line)",
         }}>
         <div className="flex items-center gap-2">
           <RadioGroupItem value={option} aria-checked={checked} aria-label={`${title} — ${subhead}`} />
           <span style={{ fontWeight: 600, fontSize: 13 }}>{title}</span>
         </div>
         <span style={{ color: "var(--v7-text-muted)", fontSize: 12, fontFamily: "var(--font-mono)" }}>
           {subhead}
         </span>
       </label>
       ```
       With operator-locked copy:
       - option `"regression"` → title `Recent regression`, subhead `Used to work correctly`
       - option `"capability"` → title `New case`, subhead `Never handled correctly before`
    4. DELETE the `pillClass` prop on the `Card` (or analogous) sub-component if it exists. DELETE the `isReg` branch entirely. DELETE the L108-119 pill `<span>` markup.
    5. PRESERVE the `RadioGroup` wrapper, the `onChange` handler that sets `evalType`, the `option` payload values (`"capability"` | `"regression"`), and all `aria-*` attributes.
    6. DO NOT touch the override API route, the route test, or any keyboard-shortcut dispatcher. Both `c` and `g` shortcuts should continue to work because they target the `evalType` state, not the card markup.
    7. DO NOT introduce new pill / dot / color affordances (CONTEXT H-02 boundary). DO NOT add new CSS tokens.
  </action>
  <verify>
    <automated>
    cd /Users/nickcrutzen/Developer/agent-workforce &amp;&amp; \
    test "$(grep -cE '>REG<|>CAP<|"REG"|"CAP"|reg</span|cap</span' 'web/app/(dashboard)/automations/[swarm]/stage-1/components/eval-type-radio.tsx')" = "0" &amp;&amp; \
    test "$(grep -c 'Recent regression' 'web/app/(dashboard)/automations/[swarm]/stage-1/components/eval-type-radio.tsx')" -ge "1" &amp;&amp; \
    test "$(grep -c 'New case' 'web/app/(dashboard)/automations/[swarm]/stage-1/components/eval-type-radio.tsx')" -ge "1" &amp;&amp; \
    test "$(grep -c 'Used to work correctly' 'web/app/(dashboard)/automations/[swarm]/stage-1/components/eval-type-radio.tsx')" -ge "1" &amp;&amp; \
    test "$(grep -c 'Never handled correctly before' 'web/app/(dashboard)/automations/[swarm]/stage-1/components/eval-type-radio.tsx')" -ge "1" &amp;&amp; \
    test "$(grep -cE '\"capability\"|\"regression\"' 'web/app/(dashboard)/automations/[swarm]/stage-1/components/eval-type-radio.tsx')" -ge "2" &amp;&amp; \
    npm --prefix web test -- --run override/__tests__/route.test &amp;&amp; \
    echo "Task 3 grep + API contract test green"
    </automated>
  </verify>
  <acceptance_criteria>
    - Zero matches for `REG` / `CAP` as user-facing text inside `eval-type-radio.tsx`.
    - All four operator-locked strings present at least once: `Recent regression`, `Used to work correctly`, `New case`, `Never handled correctly before`.
    - Both option values (`"capability"` and `"regression"`) still present in the radio dispatch payload.
    - The override API contract test (`web/app/api/automations/debtor-email/override/__tests__/route.test.ts`) passes without modification.
    - No new color/pill/dot affordance introduced; brand tokens only (`var(--v7-brand-primary)`, `var(--v7-brand-primary-soft)`, `var(--v7-line)`, `var(--v7-panel-2)`, `var(--v7-text-muted)`).
    - `pillClass` / `isReg` branching removed cleanly (no dead variables).
  </acceptance_criteria>
  <done>
    eval-type-radio.tsx renders two text-only radio cards with operator-locked copy. API payload unchanged. Keyboard shortcuts unchanged.
  </done>
</task>

</tasks>

<verification>
- All three task `<automated>` greps pass.
- `git diff --stat` shows exactly four files changed: `stage-step.tsx`, `stage-1-widget.tsx`, `eval-type-radio.tsx`, `stage-1-widget.test.tsx`.
- `_shell/detail-pane.tsx` is NOT in the diff (canonical footer untouched).
- `web/app/api/automations/debtor-email/override/__tests__/route.test.ts` passes.
- `npm --prefix web test -- --run stage-1-widget` passes (FLAG-01 + BLOCK-02 fix).
- Manual smoke (operator UAT, post-merge): in the dirty-branch state on Stage 1, only the footer Submit/Cancel/Reject row renders; `Esc` discards; `⌘⏎` submits; `c` / `g` toggle eval_type.
- `var(--v7-lime)` still present on Submit inside `_shell/detail-pane.tsx` (regression guard from 82.7.1-04 / 82.7.2-01).
</verification>

<success_criteria>
- G-01 satisfied: only the detail-pane footer renders Submit/Cancel in the dirty-branch state.
- G-02 satisfied: eval-type radio cards use plain-English title + subhead; no REG/CAP literals.
- No new dependencies; brand tokens preserved; API contract intact.
- 82.7.1-04 lime-Submit regression guard survives on the canonical footer.
- `stage-1-widget.test.tsx` continues to pass after the inline Submit button is deleted (BLOCK-02 fix).
</success_criteria>

<output>
After completion, create `.planning/phases/82.7.3-detail-pane-and-stage-1-list-uat-followups-round-2/82.7.3-01-SUMMARY.md` documenting:
- G-01 + G-02 grep evidence (delete target line counts before/after, new copy strings present).
- Confirmation that `var(--v7-lime)` survives on `_shell/detail-pane.tsx` Submit.
- Confirmation that override API contract test still passes.
- Confirmation that `npm --prefix web test -- --run stage-1-widget` passes after the test-file re-target (BLOCK-02).
- Note that `onSubmitDirty` / `onCancelDirty` props are now unused-but-typed on `StageStepProps` (per CONTEXT H-01).
</output>
</content>
</invoke>