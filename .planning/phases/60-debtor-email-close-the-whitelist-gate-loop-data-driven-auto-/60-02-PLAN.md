---
phase: 60
plan: 02
type: execute
wave: 1
depends_on: [60-01]
files_modified:
  - web/lib/inngest/events.ts
  - web/lib/inngest/functions/classifier-backfill.ts
  - web/lib/inngest/functions/classifier-promotion-cron.ts
  - web/app/api/inngest/route.ts
  - web/app/api/automations/debtor-email/ingest/route.ts
  - web/tests/classifier/backfill.test.ts
autonomous: true
requirements: [D-04, D-08, D-22, D-28]
must_haves:
  truths:
    - "After running the classifier-backfill one-shot Inngest function, public.classifier_rules contains the 6 seeded debtor-email rules with status='promoted'"
    - "Ingest route reads whitelist from public.classifier_rules via the 60s in-memory cache; falls back to FALLBACK_WHITELIST on DB error (D-28 step 3)"
    - "Predicted automation_runs rows include typed columns: swarm_type='debtor-email', topic, entity, mailbox_id"
    - "Hardcoded AUTO_ACTION_RULES Set is removed from route.ts (replaced by readWhitelist call)"
    - "classify.ts is UNCHANGED (D-22)"
  artifacts:
    - path: "web/lib/inngest/functions/classifier-backfill.ts"
      provides: "One-shot Inngest function event-triggered by classifier/backfill.run; seeds 6 hardcoded rules with computed CI-lo + ON CONFLICT upsert"
      exports: ["classifierBackfill"]
    - path: "web/app/api/automations/debtor-email/ingest/route.ts"
      provides: "MODIFIED: hardcoded Set replaced by readWhitelist; predicted/fetch_error/not_found inserts include swarm_type/topic/entity/mailbox_id typed columns"
      contains: "readWhitelist"
  key_links:
    - from: "web/app/api/automations/debtor-email/ingest/route.ts"
      to: "web/lib/classifier/cache.ts"
      via: "import { readWhitelist } from '@/lib/classifier/cache'"
      pattern: "readWhitelist\\(admin, \"debtor-email\"\\)"
    - from: "web/lib/inngest/functions/classifier-backfill.ts"
      to: "public.classifier_rules"
      via: "admin.from('classifier_rules').upsert(..., { onConflict: 'swarm_type,rule_key' })"
      pattern: "onConflict: \"swarm_type,rule_key\""
---

<objective>
Wire the data-driven whitelist into production reads. Two coupled changes:

1. **Backfill function** — One-shot Inngest function (`classifier/backfill.run` event) seeds the 6 historical debtor-email rules into `public.classifier_rules` with computed Wilson CI-lo and `status='promoted'`.
2. **Ingest-route refactor** — Replace the hardcoded `AUTO_ACTION_RULES` Set with a `readWhitelist(admin, "debtor-email")` call (60s TTL cache + FALLBACK_WHITELIST defensive path per D-28 step 3). Add typed columns to all `automation_runs.insert` callsites in this route.

Per D-28 step 3: keep `FALLBACK_WHITELIST` reachable inside `cache.ts` until Wave 4 confirms 1-day clean run. Per D-22: `web/lib/debtor-email/classify.ts` is NOT touched.

Also stub the cron function file (60-03 fills it in) so register-route can include it without breaking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-CONTEXT.md
@.planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-RESEARCH.md
@.planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-PATTERNS.md
@.planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-00-SUMMARY.md
@.planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-01-SUMMARY.md

@web/app/api/automations/debtor-email/ingest/route.ts
@web/lib/inngest/events.ts
@web/lib/inngest/client.ts
@web/lib/inngest/functions/debtor-email-bridge.ts
@web/lib/classifier/cache.ts
@web/lib/classifier/wilson.ts
@web/lib/automations/runs/emit.ts
@web/lib/supabase/admin.ts

<interfaces>
From web/lib/classifier/cache.ts (created in 60-00):
```typescript
export const FALLBACK_WHITELIST: Set<string>;
export async function readWhitelist(admin: SupabaseClient, swarmType: string): Promise<Set<string>>;
```

From web/lib/classifier/wilson.ts:
```typescript
export function wilsonCiLower(n: number, k: number, z?: number): number;
```

From web/lib/inngest/client.ts:
```typescript
export const inngest: Inngest;
```

From web/lib/inngest/events.ts (existing pattern at lines 151-166 for icontroller/cleanup.shard.requested) — extend with new event keys.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add classifier Inngest events + classifier-backfill one-shot + cron stub + register route</name>
  <files>web/lib/inngest/events.ts, web/lib/inngest/functions/classifier-backfill.ts, web/lib/inngest/functions/classifier-promotion-cron.ts, web/app/api/inngest/route.ts, web/tests/classifier/backfill.test.ts</files>
  <read_first>
    - web/lib/inngest/events.ts (full file — find the existing icontroller/cleanup.shard.requested entry pattern at ~lines 151-166)
    - web/lib/inngest/functions/debtor-email-bridge.ts (analog for cron + per-config step.run loop)
    - web/lib/inngest/functions/debtor-email-icontroller-cleanup-worker.ts (analog for event-trigger function shape)
    - web/app/api/inngest/route.ts (existing serve config, lines 22-48 — add 3 imports + 3 array entries)
    - .planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-RESEARCH.md (lines 603-648 — verbatim classifier-backfill code)
    - .planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-PATTERNS.md (lines 320-378 — events.ts and serve route additions)
  </read_first>
  <behavior>
    - classifier-backfill seeds exactly 6 rows into public.classifier_rules with status='promoted'
    - rule_keys match route.ts:39-46 verbatim: subject_paid_marker, payment_subject, payment_sender+subject, payment_system_sender+body, payment_sender+hint+body, payment_sender+body
    - Wilson CI-lo for (169,169) ≈ 0.978; (151,151) ≈ 0.976; (79,79) ≈ 0.954 (assertions in test)
    - Re-running the same backfill is idempotent (ON CONFLICT(swarm_type,rule_key) DO UPDATE)
    - Notes column populated for the 3 small-N rules (Option B per RESEARCH lines 596-601: "promoted via payment_admittance category-rollup N=415")
    - GET /api/inngest returns a function list including all 3 new function IDs (smoke check)
  </behavior>
  <action>
    1. **Modify `web/lib/inngest/events.ts`** — add two new event entries next to the existing `icontroller/cleanup.shard.requested`:
    ```typescript
    "classifier/backfill.run": {
      data: {
        triggeredBy?: string;
      };
    },
    "classifier/verdict.recorded": {
      data: {
        automation_run_id: string;
        agent_run_id: string;
        swarm_type: string;
        rule_key: string;
        decision: "approve" | "reject";
        message_id: string;
        source_mailbox: string;
        entity: string;
        predicted_category: string;
        override_category?: string;
      };
    },
    ```

    2. **Create `web/lib/inngest/functions/classifier-backfill.ts`** — Use RESEARCH lines 603-648 verbatim. Critical specifics:
    - `id: "classifier/backfill"`, `retries: 1`
    - Event-trigger: `{ event: "classifier/backfill.run" }`
    - Inside `step.run("seed-classifier-rules", ...)`:
      - Build `seeds` array exactly as RESEARCH lines 612-623 (6 rows; the 3 small-N rules have `notes` field with category-rollup explanation)
      - For each seed: compute `ci_lo = wilsonCiLower(s.n, s.agree)`, then `admin.from("classifier_rules").upsert({ swarm_type: "debtor-email", rule_key: s.rule_key, kind: "regex", status: "promoted", n: s.n, agree: s.agree, ci_lo, last_evaluated: now, promoted_at: now, notes: s.notes ?? null }, { onConflict: "swarm_type,rule_key" })`
      - Return `{ seeded: seeds.length }`
    - NO `*/N` cron strings inside `/** */` JSDoc (CLAUDE.md learning eb434cfd) — use `//` for any cron mention.

    3. **Create `web/lib/inngest/functions/classifier-promotion-cron.ts`** — STUB only (60-03 fills in). Provide a no-op cron skeleton so register-route can include it:
    ```typescript
    // cron: TZ=Europe/Amsterdam 0 6 * * 1-5 — daily 06:00 Amsterdam, Mon-Fri (D-09)
    // Implementation lands in plan 60-03.
    import { inngest } from "@/lib/inngest/client";

    export const classifierPromotionCron = inngest.createFunction(
      { id: "classifier/promotion-cron", retries: 2 },
      { cron: "TZ=Europe/Amsterdam 0 6 * * 1-5" },
      async ({ step }) => {
        return step.run("placeholder", async () => ({
          status: "stub",
          note: "implementation pending in plan 60-03",
        }));
      },
    );
    ```

    4. **Modify `web/app/api/inngest/route.ts`** — append three imports and three entries to the `functions` array:
    ```typescript
    import { classifierBackfill } from "@/lib/inngest/functions/classifier-backfill";
    import { classifierPromotionCron } from "@/lib/inngest/functions/classifier-promotion-cron";
    // classifierVerdictWorker added in plan 60-06
    ```
    Add `classifierBackfill` and `classifierPromotionCron` to the array. Leave a `// TODO 60-06: register classifierVerdictWorker` comment at the relevant slot.

    5. **Replace `it.todo` blocks in `web/tests/classifier/backfill.test.ts`** with real assertions. Mock the admin client. Verify:
    - `seeds.length === 6`
    - upsert called 6 times with `onConflict: "swarm_type,rule_key"`
    - first call payload contains `rule_key: "subject_paid_marker"`, `n: 169`, `agree: 169`, `ci_lo: closeTo(0.978, 3)`, `status: "promoted"`, `kind: "regex"`
    - 4th, 5th, 6th calls have `notes` containing `"payment_admittance"`
    - return value `{ seeded: 6 }`
  </action>
  <verify>
    <automated>cd web && pnpm vitest run tests/classifier/backfill.test.ts --reporter=basic && pnpm tsc --noEmit -p . 2>&1 | grep -E "classifier|inngest" || echo "no classifier/inngest tsc errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "classifier/backfill.run" web/lib/inngest/events.ts` == 1
    - `grep -c "classifier/verdict.recorded" web/lib/inngest/events.ts` == 1
    - `grep -c "id: \"classifier/backfill\"" web/lib/inngest/functions/classifier-backfill.ts` == 1
    - `grep -c "wilsonCiLower" web/lib/inngest/functions/classifier-backfill.ts` >= 1
    - `grep -c "onConflict: \"swarm_type,rule_key\"" web/lib/inngest/functions/classifier-backfill.ts` == 1
    - `grep -c "subject_paid_marker\|payment_subject\|payment_sender" web/lib/inngest/functions/classifier-backfill.ts` >= 6
    - `grep -c "payment_admittance" web/lib/inngest/functions/classifier-backfill.ts` >= 3
    - `grep -c "TZ=Europe/Amsterdam 0 6 \\* \\* 1-5" web/lib/inngest/functions/classifier-promotion-cron.ts` == 1
    - `grep -c "classifierBackfill\|classifierPromotionCron" web/app/api/inngest/route.ts` >= 2
    - `cd web && pnpm vitest run tests/classifier/backfill.test.ts --reporter=basic` exits 0 with assertions actually executing (no `it.todo` remaining)
    - `cd web && pnpm tsc --noEmit -p .` reports no NEW errors from these files
  </acceptance_criteria>
  <done>Backfill function shipped, cron stubbed, route registered, backfill test green with real assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Ingest route refactor — readWhitelist replaces hardcoded Set; typed columns on every automation_runs insert</name>
  <files>web/app/api/automations/debtor-email/ingest/route.ts</files>
  <read_first>
    - web/app/api/automations/debtor-email/ingest/route.ts (full file — focus lines 14-46 for hardcoded Set, lines 230-323 for insert callsites)
    - web/lib/classifier/cache.ts (the readWhitelist export to consume)
    - .planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-PATTERNS.md (lines 380-432 — exact before/after diff)
  </read_first>
  <action>
    Two surgical edits to `web/app/api/automations/debtor-email/ingest/route.ts`:

    **Edit 1 — Replace the hardcoded `AUTO_ACTION_RULES` Set (currently around lines 39-46):**
    - DELETE the `const AUTO_ACTION_RULES = new Set<string>([...])` declaration entirely.
    - Add at top of file: `import { readWhitelist } from "@/lib/classifier/cache";`
    - Inside the request handler, AFTER the admin client is created and BEFORE the per-row classification loop runs, fetch the whitelist:
    ```typescript
    const whitelist = await readWhitelist(admin, "debtor-email");
    ```
    - Replace every reference of `AUTO_ACTION_RULES.has(r.matchedRule)` with `whitelist.has(r.matchedRule)`.
    - The `whitelist` is fetched once per request (cached for 60s across requests by the module-level Map). Do NOT fetch inside the per-row loop.
    - The hardcoded JSDoc empirics block (route.ts:14-37 — describing N + CI-lo) STAYS as documentation but the `AUTO_ACTION_RULES` Set itself is gone. The fallback path lives inside `readWhitelist` (FALLBACK_WHITELIST in cache.ts) per D-28 step 3.

    **Edit 2 — Add typed columns to every `automation_runs.insert` callsite in this route:**
    - Identify ALL `admin.from("automation_runs").insert({...})` calls in the file (use grep).
    - For each, ADD these top-level columns alongside the existing fields (do NOT remove existing fields, do NOT move data out of `result` jsonb — additive per D-28 step 1):
      - `swarm_type: "debtor-email"`
      - `topic: r.category` (the classifier-output category, e.g., `payment_admittance`) — for `predicted` and `fetch_error`/`not_found` callsites where `r.category` may be undefined, use `r?.category ?? null`
      - `entity: settings.entity` — using the per-row mailbox settings already resolved in scope
      - `mailbox_id: settings.id` — FK to `debtor.labeling_settings.id`

    Apply this to ALL automation_runs.insert callsites (the `predicted` insert ~272-289, plus the `fetch_error` and `not_found` callsites ~230-243 per PATTERNS.md note).

    Do NOT modify `web/lib/debtor-email/classify.ts` (D-22). Do NOT remove the JSDoc block at lines 14-37.
  </action>
  <verify>
    <automated>cd web && pnpm tsc --noEmit -p . 2>&1 | grep -E "ingest/route|classifier/cache" || echo "ingest route typecheck clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AUTO_ACTION_RULES" web/app/api/automations/debtor-email/ingest/route.ts` == 0 (hardcoded Set removed; comment-only references also removed)
    - `grep -c "import { readWhitelist }" web/app/api/automations/debtor-email/ingest/route.ts` == 1
    - `grep -c "readWhitelist(admin, \"debtor-email\")" web/app/api/automations/debtor-email/ingest/route.ts` >= 1
    - `grep -c "whitelist.has" web/app/api/automations/debtor-email/ingest/route.ts` >= 1
    - Every `from("automation_runs").insert` block in the file contains `swarm_type:` — verify with: `awk '/from\("automation_runs"\)\.insert/,/}\)/' web/app/api/automations/debtor-email/ingest/route.ts | grep -c "swarm_type:"` >= 3
    - `grep -c "mailbox_id: settings.id" web/app/api/automations/debtor-email/ingest/route.ts` >= 3
    - `web/lib/debtor-email/classify.ts` UNCHANGED — `git diff --stat web/lib/debtor-email/classify.ts` returns nothing
    - `cd web && pnpm tsc --noEmit -p .` reports no NEW errors in `web/app/api/automations/debtor-email/ingest/route.ts`
  </acceptance_criteria>
  <done>Ingest route reads from classifier_rules with cache+fallback; predicted rows carry typed columns; classify.ts untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zapier → /ingest route | Existing X-Zapier-Secret header validates; no change here |
| ingest route → public.classifier_rules | Service-role read; cached 60s |
| backfill function → public.classifier_rules | Service-role write; idempotent upsert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-02-01 | Denial of Service | classifier_rules read on every request | mitigate | 60s in-memory cache (D-08) — at most 1 DB hit per swarm_type per minute per Vercel container |
| T-60-02-02 | Tampering | DB returns empty rows during transient failure → no auto-actions, all routed to review | mitigate | FALLBACK_WHITELIST inside cache.ts returns the 6 known-good rules so auto-action continues during DB blips per D-28 step 3 |
| T-60-02-03 | Repudiation | Backfill double-runs and overwrites notes | mitigate | ON CONFLICT(swarm_type,rule_key) DO UPDATE is idempotent; notes overwrite is acceptable since backfill is the single source of truth for this seed |
| T-60-02-04 | Information Disclosure | Predicted rows leak topic via JSONB | accept | Topic was already in `result.predicted.category`; promoting to typed column changes nothing about exposure (existing RLS unchanged) |
| T-60-02-05 | Tampering | Backfill computes wrong CI-lo silently | mitigate | Wave 0 wilson tests pin the exact values; backfill test mocks upsert and asserts payload values match expected CI-lo to 3 decimals |
</threat_model>

<verification>
- `cd web && pnpm vitest run tests/classifier --reporter=basic` — backfill test green
- `cd web && pnpm tsc --noEmit -p .` — no type errors
- Manual smoke: `pnpm dev` then `curl localhost:3000/api/inngest` lists `classifier/backfill` and `classifier/promotion-cron`
- After deploy: trigger `classifier/backfill.run` event manually via Inngest dashboard → verify `select count(*) from public.classifier_rules where swarm_type='debtor-email'` returns 6
</verification>

<success_criteria>
1. Hardcoded `AUTO_ACTION_RULES` Set removed; whitelist read via `readWhitelist` cache.
2. All `automation_runs.insert` callsites in `ingest/route.ts` write typed columns.
3. classifier-backfill function ships and seeds 6 rules with correct CI-lo on first event.
4. classifier-promotion-cron stub registered at correct cron string.
5. `classify.ts` untouched (D-22 enforced via grep).
</success_criteria>

<output>
After completion, create `.planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-02-SUMMARY.md`.
</output>
