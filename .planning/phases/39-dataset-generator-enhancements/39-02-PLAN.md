---
phase: 39-dataset-generator-enhancements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/commands/datasets.md
autonomous: true
requirements: [DSET-01, DSET-02, DSET-03, DSET-04, DSET-05, DSET-06, DSET-07, DSET-08]
must_haves:
  truths:
    - "/orq-agent:datasets command accepts --mode two-step|flat|curation|promote-trace"
    - "/orq-agent:datasets command accepts --trace-id <id> for promote-trace mode"
    - "/orq-agent:datasets command accepts --shape single|multi-turn|rag"
    - "Command dispatch logic selects the correct mode branch and documents error states"
    - "Existing `<pipeline>` SHA-256 block is preserved or only extends with documented additions (Phase 34 invariant)"
    - "All 9 SKST sections remain intact"
  artifacts:
    - path: "orq-agent/commands/datasets.md"
      provides: "CLI flag parsing + mode dispatch for DSET-01..08"
      contains: "--mode two-step, --mode curation, --mode promote-trace, --trace-id, --shape multi-turn, --shape rag"
  key_links:
    - from: "datasets.md Step 0 argument parser"
      to: "dataset-generator subagent"
      via: "mode flag passed as input in Step 5 spawn"
      pattern: "--mode (two-step|flat|curation|promote-trace)"
---

<objective>
Extend `orq-agent/commands/datasets.md` with new CLI flags (`--mode`, `--trace-id`, `--shape`) and the dispatch logic routing each mode to the dataset-generator subagent. Preserve all 9 SKST sections and the protected `<pipeline>` block integrity.

Purpose: The command is the user-facing CLI surface. Without flags here, the subagent's DSET-01..08 content is unreachable.
Output: Enhanced datasets.md with flag parsing, mode dispatch, and documented behaviors.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-dataset-generator-enhancements/39-CONTEXT.md
@.planning/phases/39-dataset-generator-enhancements/39-VALIDATION.md
@orq-agent/commands/datasets.md

<interfaces>
Existing flags (preserve):
- `--output <path>`: output directory (defaults to ./Agents/)

New flags (add):
- `--mode two-step|flat|curation|promote-trace`: generation mode. Default: `flat` (existing V2.0 behavior).
- `--trace-id <id>`: REQUIRED when `--mode promote-trace`; MCP trace ID to promote.
- `--shape single|multi-turn|rag`: dataset shape. Default: `single`.

datasets.md note: datasets.md IS a command file with a `<pipeline>` block. Phase 34 invariant: the `<pipeline>` SHA-256 is NOT a protected entry point (only orq-agent.md/prompt.md/architect.md are). Therefore we CAN edit `<pipeline>` content here, but check-protected-pipelines.sh must still exit 0 across the three protected files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --mode / --trace-id / --shape flags + dispatch to datasets.md</name>
  <files>orq-agent/commands/datasets.md</files>
  <read_first>
    - orq-agent/commands/datasets.md (full current file)
    - .planning/phases/39-dataset-generator-enhancements/39-CONTEXT.md
    - orq-agent/scripts/lint-skills.sh (to confirm rule set)
    - orq-agent/scripts/check-protected-pipelines.sh (to confirm datasets.md is NOT in the 3-file protected list)
  </read_first>
  <action>
    Edit `orq-agent/commands/datasets.md` as follows. Preserve the YAML frontmatter (update `argument-hint` only). Preserve every SKST section (Constraints, When to use, When NOT to use, Companion Skills, Done When, Destructive Actions, Anti-Patterns, Open in orq.ai, Documentation & Resolution).

    1. Update frontmatter `argument-hint` to:
       `[agent-spec-path|agent-description] [--mode two-step|flat|curation|promote-trace] [--trace-id <id>] [--shape single|multi-turn|rag] [--output <path>]`

    2. In Step 0 (Parse Arguments), extend the flag parser:
       - Add `--mode <value>`: string flag, consumes next token. Accepted values: `two-step`, `flat`, `curation`, `promote-trace`. Default: `flat`.
       - Add `--trace-id <id>`: string flag, consumes next token. REQUIRED when `--mode promote-trace`; if missing with that mode, STOP with message: "--mode promote-trace requires --trace-id <id>".
       - Add `--shape <value>`: string flag. Accepted values: `single`, `multi-turn`, `rag`. Default: `single`.
       - Store as `MODE`, `TRACE_ID`, `SHAPE`.

    3. Add a new "## Step 1b: Mode Dispatch" section BETWEEN Step 1 and Step 2. Content:
       - If `MODE == "promote-trace"`: skip Steps 2-4 entirely; call MCP `get_span` on `TRACE_ID` (root) + `list_spans` for children; construct the regression datapoint preserving input, output, intermediate_steps, metadata (session_id, user_id, customer_id, identity); write as single-datapoint dataset to `{OUTPUT_DIR}/[agent-name]/datasets/trace-promoted-{TRACE_ID}.md`; jump to Step 6.
       - If `MODE == "curation"`: require an existing dataset path as the positional argument; skip Step 2 clarifications; in Step 5 pass mode=curation to the subagent; subagent performs dedupe/rebalance/gap-fill/contradiction surfacing, confirming each proposed deletion via AskUserQuestion.
       - If `MODE == "two-step"`: in Step 5, instruct the subagent to emit `dimensions.md` and `tuples.md` intermediate artifacts before the final dataset, and to write them alongside the dataset files.
       - If `MODE == "flat"` (default): existing V2.0 behavior unchanged.

    4. In Step 5 (Spawn Dataset Generator), extend the "Input" list with items 7 and 8:
       - 7. Mode: `{MODE}` (two-step|flat|curation|promote-trace)
       - 8. Shape: `{SHAPE}` (single|multi-turn|rag)

    5. In Step 6 (Summary), add a conditional line: when `MODE == promote-trace`, show "Source trace: {TRACE_ID}" and "Datapoint preserved: input, output, intermediate_steps, metadata". When `MODE == two-step`, list the intermediate artifact paths. When `MODE == curation`, show "Deletions confirmed: N" where N is the AskUserQuestion count.

    6. Append to the Constraints block (do NOT remove existing):
       - **ALWAYS** validate `--mode promote-trace` receives `--trace-id <id>` before any MCP call (Phase 39 DSET-08).
       - **ALWAYS** preserve input, output, intermediate_steps, and metadata when promoting a production trace (Phase 39 DSET-08).

    Do NOT modify `orq-agent/commands/orq-agent.md`, `orq-agent/commands/prompt.md`, or `orq-agent/commands/architect.md` — they are the 3 protected pipelines.
  </action>
  <verify>
    <automated>
grep -q -- "--mode two-step" orq-agent/commands/datasets.md && \
grep -q -- "--mode curation" orq-agent/commands/datasets.md && \
grep -q -- "--mode promote-trace" orq-agent/commands/datasets.md && \
grep -q -- "--trace-id" orq-agent/commands/datasets.md && \
grep -q -- "--shape single" orq-agent/commands/datasets.md && \
grep -q -- "--shape multi-turn" orq-agent/commands/datasets.md && \
grep -q -- "--shape rag" orq-agent/commands/datasets.md && \
grep -q "Step 1b: Mode Dispatch" orq-agent/commands/datasets.md && \
grep -q "intermediate_steps" orq-agent/commands/datasets.md && \
bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/datasets.md && \
bash orq-agent/scripts/check-protected-pipelines.sh
    </automated>
  </verify>
  <acceptance_criteria>
    - All 9 grep anchors exit 0.
    - lint-skills.sh --file exits 0 on datasets.md.
    - check-protected-pipelines.sh exits 0 (3/3 SHA-256 matches on orq-agent.md, prompt.md, architect.md unchanged).
    - All 9 SKST headings remain.
  </acceptance_criteria>
  <done>
    datasets.md exposes 4 modes + shape + trace-id flags, documents dispatch, lint green, protected pipelines untouched.
  </done>
</task>

</tasks>

<verification>
Quick: `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/datasets.md`
Protected: `bash orq-agent/scripts/check-protected-pipelines.sh`
Anchor sweep: 9 flags + dispatch grep anchors.
</verification>

<success_criteria>
- 4 `--mode` values present verbatim.
- `--trace-id` and 3 `--shape` values present verbatim.
- Step 1b Mode Dispatch section exists.
- Protected pipelines SHA-256 3/3 intact.
</success_criteria>

<output>
After completion, create `.planning/phases/39-dataset-generator-enhancements/39-02-SUMMARY.md`.
</output>
