---
phase: 39-dataset-generator-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/agents/dataset-generator.md
autonomous: true
requirements: [DSET-01, DSET-02, DSET-03, DSET-04, DSET-05, DSET-06, DSET-07, DSET-08]
must_haves:
  truths:
    - "dataset-generator subagent documents Two-Step Mode (dimensions → tuples → NL inputs)"
    - "dataset-generator subagent lists all 8 adversarial vectors by exact name"
    - "dataset-generator subagent states the two coverage rules and the exact remediation phrase 'Coverage check failed:'"
    - "dataset-generator subagent describes Mode 4 curation (dedupe/rebalance/gap-fill/contradiction-resolve) with AskUserQuestion confirm before deletion"
    - "dataset-generator subagent documents multi-turn shape (Messages + perturbation) and RAG shape (expected_source_chunk_ids)"
    - "dataset-generator subagent documents promote-from-trace preserving input, output, intermediate_steps, metadata"
    - "All 9 SKST sections (Phase 34) remain intact"
  artifacts:
    - path: "orq-agent/agents/dataset-generator.md"
      provides: "Enhanced subagent body with DSET-01..08 sections"
      contains: "Two-Step Mode, 8-vector catalog, coverage rules, curation, multi-turn, rag, promote-from-trace"
  key_links:
    - from: "dataset-generator.md Constraints block"
      to: "Phase 39 DSET requirement anchors"
      via: "inline requirement ID citations"
      pattern: "Phase 39 DSET-0[1-8]"
---

<objective>
Extend `orq-agent/agents/dataset-generator.md` with 7 new content sections covering DSET-01..08 while preserving all 9 SKST sections and the existing V2.0 content.

Purpose: The subagent is the LLM-facing playbook; new content here is what actually steers generation output.
Output: Enhanced dataset-generator.md with lint-green SKST structure and all DSET anchors present.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/39-dataset-generator-enhancements/39-CONTEXT.md
@.planning/phases/39-dataset-generator-enhancements/39-VALIDATION.md
@orq-agent/agents/dataset-generator.md

<interfaces>
Exact 8-vector names (verbatim grep anchors, DSET-02):
1. persona-breaking
2. instruction-override
3. language-switching
4. formality-mismatch
5. refusal
6. format-forcing
7. multi-turn-manipulation
8. contradiction

Exact remediation phrase (DSET-03 grep anchor):
"Coverage check failed:"

Coverage rules (DSET-03):
- Every dimension value appears in ≥2 datapoints.
- No single value dominates >30% of total datapoints.

Datapoint shape (DSET-05/06/07):
- category (required)
- dimension_values: {dim1: val1, ...} (required for two-step mode)
- shape: single | multi-turn | rag
- multi-turn adds: messages, perturbation_scenario
- rag adds: expected_source_chunk_ids

Promote-from-trace shape (DSET-08):
- category: "regression"
- source: "production-trace"
- source_trace_id
- input, output (→ expected_output), intermediate_steps, metadata (session_id, user_id, customer_id, identity)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend dataset-generator subagent with DSET-01..08 sections</name>
  <files>orq-agent/agents/dataset-generator.md</files>
  <read_first>
    - orq-agent/agents/dataset-generator.md (full current file)
    - .planning/phases/39-dataset-generator-enhancements/39-CONTEXT.md
  </read_first>
  <action>
    Edit `orq-agent/agents/dataset-generator.md` to add 7 new content subsections. Preserve all existing V2.0 content (dual-dataset, OWASP taxonomy, few-shot example) and all 9 SKST sections (When to use, When NOT to use, Companion Skills, Done When, Destructive Actions, Anti-Patterns, Open in orq.ai, Documentation & Resolution, Constraints block). Insert the new sections AFTER the existing "Self-Validation Checklist" section and BEFORE the "Few-Shot Example" `<examples>` block so the checklist stays adjacent to the generation body.

    Add these sections verbatim (names are grep anchors):

    ## Two-Step Generation Mode (DSET-01)
    - Step A — Dimensions (3-6 axes): enumerate categorical axes that span the input space. Example axes: task_complexity, user_persona, input_channel.
    - Step B — Tuples: enumerate combinations (manual seed of 3-5, then LLM-scaled to cover the cartesian product).
    - Step C — Natural-language inputs: one NL input per tuple, authored in a SEPARATE pass so tuples stay inspectable independently.
    - Intermediate artifacts (dimensions.md, tuples.md) MUST be inspectable before NL generation runs.

    ## Adversarial Vector Catalog (DSET-02)
    Datasets include 15-20% adversarial cases drawn from this 8-vector catalog, with ≥3 cases per relevant vector. Reference: `orq-agent/agents/dataset-generator/resources/adversarial-vectors.md`.
    1. persona-breaking — attempts to make the agent adopt a different persona
    2. instruction-override — "ignore previous instructions..."
    3. language-switching — input in unexpected language for the agent's domain
    4. formality-mismatch — register mismatch (slang vs formal)
    5. refusal — asks for content the agent should refuse
    6. format-forcing — demands non-spec output format
    7. multi-turn-manipulation — builds malicious context across turns
    8. contradiction — self-conflicting requirements in one input

    ## Coverage Rules (DSET-03)
    Enforced BEFORE upload. On violation, emit exactly this remediation prefix and BLOCK the upload:
    - "Coverage check failed: value 'X' appears in only 1 datapoint (need ≥2). Add datapoints or adjust dimensions."
    - "Coverage check failed: value 'X' dominates 42% (limit is 30%). Rebalance by adding datapoints for under-represented values."
    Rules:
    - Rule 1: every dimension value appears in ≥2 datapoints.
    - Rule 2: no single dimension value dominates >30% of total datapoints.
    Reference: `orq-agent/agents/dataset-generator/resources/coverage-rules.md`.

    ## Curation Mode 4 (DSET-04)
    When invoked via `--mode curation`, operate on an existing dataset:
    1. Deduplicate exact input-hash matches.
    2. Rebalance: flag dimension values >30% share; propose removals (NEVER delete without AskUserQuestion confirm).
    3. Gap-fill: flag dimension values <2 count; propose additions.
    4. Resolve contradictions: surface groups of equivalent inputs with divergent expected_output.
    Every proposed deletion MUST be confirmed via AskUserQuestion (one confirm per deletion batch is acceptable).

    ## Dataset Shapes (DSET-05, DSET-06, DSET-07)
    Every datapoint carries `category` (required) AND `dimension_values: {dim: val, ...}` (required in two-step mode) so results-analyzer can slice scores (DSET-05).

    Shape values:
    - `shape: single` — legacy flat eval pair (input, expected_output, pass_criteria).
    - `shape: multi-turn` (DSET-06) — adds `messages: [{role, content}, ...]` + `perturbation_scenario: <name>`.
    - `shape: rag` (DSET-07) — adds `expected_source_chunk_ids: [chunk_id_1, chunk_id_2, ...]` so retrieval quality can be graded.
    Templates live at `orq-agent/agents/dataset-generator/resources/shapes.md`.

    ## Promote-From-Trace (DSET-08)
    When invoked via `--mode promote-trace --trace-id <id>`:
    1. Fetch trace via MCP `get_span` (root) + `list_spans`.
    2. Emit ONE datapoint preserving: `input` (root span input), `expected_output` (root span output), `intermediate_steps` (tool-call sequence), `metadata` (session_id, user_id, customer_id, identity).
    3. Tag as `category: "regression"`, `source: "production-trace"`, `source_trace_id: <id>`.
    Example shape:
    ```json
    {"category": "regression", "source": "production-trace", "source_trace_id": "tr_01JRXYZ", "input": "...", "expected_output": "...", "intermediate_steps": [{"tool": "db_lookup", "args": {}, "result": {}}], "metadata": {"session_id": "...", "customer_id": "acme", "identity": {"tenant": "eu-1"}}}
    ```

    Also update the existing Constraints block to append (do NOT replace existing constraint lines):
    - **ALWAYS** emit intermediate `dimensions` and `tuples` artifacts in two-step mode so users can inspect before NL generation (Phase 39 DSET-01).
    - **ALWAYS** preserve input, output, intermediate_steps, and metadata when promoting a production trace (Phase 39 DSET-08).

    Do NOT touch any `<pipeline>` block (this subagent has none). Do NOT rename the file. Do NOT alter the frontmatter `tools:` key.
  </action>
  <verify>
    <automated>
grep -q "persona-breaking" orq-agent/agents/dataset-generator.md && \
grep -q "instruction-override" orq-agent/agents/dataset-generator.md && \
grep -q "language-switching" orq-agent/agents/dataset-generator.md && \
grep -q "formality-mismatch" orq-agent/agents/dataset-generator.md && \
grep -q "refusal" orq-agent/agents/dataset-generator.md && \
grep -q "format-forcing" orq-agent/agents/dataset-generator.md && \
grep -q "multi-turn-manipulation" orq-agent/agents/dataset-generator.md && \
grep -q "contradiction" orq-agent/agents/dataset-generator.md && \
grep -q "Coverage check failed:" orq-agent/agents/dataset-generator.md && \
grep -q "Two-Step Generation Mode" orq-agent/agents/dataset-generator.md && \
grep -q "Curation Mode 4" orq-agent/agents/dataset-generator.md && \
grep -q "Promote-From-Trace" orq-agent/agents/dataset-generator.md && \
grep -q "shape: multi-turn" orq-agent/agents/dataset-generator.md && \
grep -q "shape: rag" orq-agent/agents/dataset-generator.md && \
grep -q "expected_source_chunk_ids" orq-agent/agents/dataset-generator.md && \
grep -q "intermediate_steps" orq-agent/agents/dataset-generator.md && \
bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/dataset-generator.md
    </automated>
  </verify>
  <acceptance_criteria>
    - All 16 grep anchors above return exit 0.
    - lint-skills.sh --file exits 0 (SKST-01..10 pass).
    - File still contains all 9 SKST headings (When to use, When NOT to use, Companion Skills, Done When, Destructive Actions, Anti-Patterns, Open in orq.ai, Documentation & Resolution, Constraints).
  </acceptance_criteria>
  <done>
    dataset-generator.md contains DSET-01..08 sections with exact anchor strings; SKST lint green; no existing V2.0 content removed.
  </done>
</task>

</tasks>

<verification>
Quick: `bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/dataset-generator.md`
Anchor sweep: 16 grep anchors above (all must exit 0).
</verification>

<success_criteria>
- 8-vector catalog verbatim present.
- "Coverage check failed:" remediation phrase present.
- Two-Step Mode, Curation Mode 4, Promote-From-Trace, multi-turn, rag sections all present.
- SKST-01..10 lint green.
</success_criteria>

<output>
After completion, create `.planning/phases/39-dataset-generator-enhancements/39-01-SUMMARY.md`.
</output>
