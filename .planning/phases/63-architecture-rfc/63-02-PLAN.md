---
phase: 63
plan: 02
type: execute
wave: 2
depends_on: [63-01]
files_modified:
  - docs/agentic-pipeline/stage-0-safety.md
  - docs/agentic-pipeline/stage-1-regex.md
  - docs/agentic-pipeline/stage-2-entity.md
  - docs/agentic-pipeline/stage-3-coordinator.md
  - docs/agentic-pipeline/stage-4-handler.md
autonomous: true
requirements: [RFC-01]
must_haves:
  truths:
    - "All 5 per-stage docs exist as self-contained files under docs/agentic-pipeline/"
    - "Each stage doc cites Anthropic 'Building Effective Agents' once (where relevant), uses ASCII diagrams, and links OUT only to existing pattern docs"
    - "Debtor-email is the running worked example throughout; sales-email parallel blocks (<=10 lines) appear ONLY in stage-1 and stage-3"
    - "Stage docs cross-reference (not duplicate) the Wave 1 contracts: context-shape-contract.md, override-model.md, graduated-automation.md"
    - "Stage 0 + Stage 4 honestly mark today-state vs forward-references (Stage 0 not yet shipped; Stage 4 zapier_tools exists but allowed_for_intents is Phase 64)"
  artifacts:
    - path: "docs/agentic-pipeline/stage-0-safety.md"
      provides: "Input-safety stage (prompt-injection guard) — forward-ref Phase 64 implementation"
      contains: "injection_suspected"
    - path: "docs/agentic-pipeline/stage-1-regex.md"
      provides: "Regex classifier as Anthropic 'routing' pattern; swarm_categories registry as dispatch table"
      contains: "swarm_categories"
    - path: "docs/agentic-pipeline/stage-2-entity.md"
      provides: "Entity enrichment, deterministic-first; produces context-shape contract"
      contains: "PipelineStageContext"
    - path: "docs/agentic-pipeline/stage-3-coordinator.md"
      provides: "Ranked-intent coordinator + Stage 3.5 escalation placeholder"
      contains: "ranked"
    - path: "docs/agentic-pipeline/stage-4-handler.md"
      provides: "Handler agents + zapier_tools registry, allowed_for_intents forward-ref Phase 64"
      contains: "zapier_tools"
  key_links:
    - from: "stage-1-regex.md"
      to: "web/lib/debtor-email/classify.ts + web/lib/inngest/functions/classifier-verdict-worker.ts"
      via: "cited as canonical Stage 1 reference implementation"
    - from: "stage-2-entity.md"
      to: "context-shape-contract.md"
      via: "produces the contract; cross-link only, no field duplication"
    - from: "stage-3-coordinator.md"
      to: "context-shape-contract.md"
      via: "consumes the contract"
    - from: "stage-4-handler.md"
      to: "supabase/migrations/20260429_zapier_tools_registry.sql + web/lib/automations/debtor-email/nxt-zap-client.ts"
      via: "cited as canonical handler-tool integration pattern"
---

<objective>
Write the five per-stage architecture docs that document each stage of the agentic pipeline funnel. Each doc is self-contained per CONTEXT D-02 and uses debtor-email as the running worked example per D-07.

Purpose: this is the bulk of the RFC content. Wave 1 (plan 01) established the contracts these stage docs cross-reference. Wave 3 (plan 03) writes the README that indexes them.

Output: 5 NEW markdown files. No code, no migrations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-architecture-rfc/63-CONTEXT.md
@.planning/phases/63-architecture-rfc/63-RESEARCH.md
@.planning/phases/63-architecture-rfc/63-VALIDATION.md
@CLAUDE.md
@docs/debtor-email-pipeline-architecture.md
@docs/orqai-patterns.md
@docs/zapier-patterns.md
@docs/inngest-patterns.md
@docs/browserless-patterns.md
@docs/agentic-pipeline/context-shape-contract.md
@docs/agentic-pipeline/override-model.md
@docs/agentic-pipeline/graduated-automation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write stage-0-safety.md (input safety stage)</name>
  <files>docs/agentic-pipeline/stage-0-safety.md</files>
  <read_first>
    - .planning/phases/63-architecture-rfc/63-CONTEXT.md (D-02 self-containment, D-13 ASCII)
    - .planning/phases/63-architecture-rfc/63-RESEARCH.md (sections "Anthropic Agentic Guidance Synthesis" — Constitutional Classifiers paraphrase; "Forward-References" — Stage 0 not yet shipped, Phase 64 implements SAFE-01..04)
    - .planning/REQUIREMENTS.md (search for SAFE-01..04 lines)
    - docs/orqai-patterns.md (response_format json_schema, fallback model patterns)
    - docs/debtor-email-pipeline-architecture.md (style template — ASCII glyph set ↓ ├─ │ └─ ┌─ ┐ └─ ┘)
  </read_first>
  <action>
Create `docs/agentic-pipeline/stage-0-safety.md`.

Required H2 sections in this order:
1. `## Goal` — 2-3 sentences: filter prompt-injection attempts and clearly malicious inputs BEFORE they reach the regex classifier or any LLM stage. `injection_suspected` outputs are routed to human-only review and never reach Stage 3 coordinators or Stage 4 handlers.
2. `## Anthropic Pattern Mapping` — one paragraph: cite Anthropic "Building Effective Agents" ONCE with URL `https://www.anthropic.com/engineering/building-effective-agents`. Paraphrase: workflow-first guidance recommends starting with the simplest viable pattern; safety classification is a Constitutional Classifier sitting in front of the chained workflow. Do NOT quote-bomb.
3. `## Today-State` — explicitly state: "Stage 0 is NOT YET SHIPPED. Phase 64 (SAFE-01..04) implements the prompt-injection regex + lightweight LLM classifier. This document defines the target shape." Cite REQUIREMENTS.md SAFE-01..04 by ID.
4. `## Architecture` — ASCII diagram showing flow:
   ```
   ┌──────────────┐      ┌─────────────────────┐      ┌──────────────────┐
   │ inbound mail │ ───> │ Stage 0: safety     │ ───> │ Stage 1 (regex)  │
   │   (raw)      │      │  - regex injection  │      └──────────────────┘
   └──────────────┘      │  - LLM verdict (opt)│
                         └────────┬────────────┘
                                  │ injection_suspected
                                  v
                         ┌─────────────────────┐
                         │ human-only review   │
                         │ (no downstream LLM) │
                         └─────────────────────┘
   ```
   Use the exact glyph set from `docs/debtor-email-pipeline-architecture.md` (verify by re-reading).
5. `## Decision Surface` — bulleted list of what Stage 0 outputs: `safe` -> Stage 1; `injection_suspected` -> human-only; `over_budget` -> reject (forward-ref Phase 64 BUDG-01 per-run token + cost ceilings).
6. `## Implementation Patterns (forward-ref)` — link OUT to `../orqai-patterns.md` for the LLM verdict step (response_format json_schema; primary `anthropic/claude-sonnet-4-6` + 3-4 fallbacks; XML-tagged prompts; 45s client timeout). Do NOT duplicate that doc's content.
7. `## Forward References` — bulleted list: SAFE-01..04 implementation (Phase 64), per-run token + cost ceilings BUDG-01 (Phase 64).
8. `## See Also` — relative links to `./README.md`, `./stage-1-regex.md`, `./override-model.md` (axis 1 captures category overrides), `./graduated-automation.md`.

Style: English. ATX headers. No emojis. No frontmatter. Match existing docs/ conventions.
  </action>
  <verify>
    <automated>test -f docs/agentic-pipeline/stage-0-safety.md &amp;&amp; grep -qE "Stage 0|safety" docs/agentic-pipeline/stage-0-safety.md &amp;&amp; grep -qF "anthropic.com/engineering/building-effective-agents" docs/agentic-pipeline/stage-0-safety.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/agentic-pipeline/stage-0-safety.md`
    - `grep -E "injection_suspected" docs/agentic-pipeline/stage-0-safety.md`
    - `grep -E "Phase 64|SAFE-0[1-4]" docs/agentic-pipeline/stage-0-safety.md`
    - `grep -F "../orqai-patterns.md" docs/agentic-pipeline/stage-0-safety.md`
    - `! grep -rEi "smeba-uk|smeba-ie" docs/agentic-pipeline/stage-0-safety.md`
    - `! grep -rEi "netlify|railway|firebase|neon|puppeteer" docs/agentic-pipeline/stage-0-safety.md`
  </acceptance_criteria>
  <done>File exists; today-state honest (not yet shipped, Phase 64); ASCII diagram present; Anthropic URL cited once; orqai-patterns linked, not duplicated.</done>
</task>

<task type="auto">
  <name>Task 2: Write stage-1-regex.md (regex classifier as routing pattern)</name>
  <files>docs/agentic-pipeline/stage-1-regex.md</files>
  <read_first>
    - .planning/phases/63-architecture-rfc/63-CONTEXT.md (D-02, D-07 debtor-email worked example, D-13 ASCII)
    - .planning/phases/63-architecture-rfc/63-RESEARCH.md (sections "Anthropic Agentic Guidance Synthesis / Routing pattern", "File Manifest / stage-1-regex.md" row)
    - web/lib/debtor-email/classify.ts (canonical Stage 1 reference — cite, do not duplicate)
    - web/lib/inngest/functions/classifier-verdict-worker.ts (cite for the categorize_archive vs swarm_dispatch dispatch)
    - docs/debtor-email-pipeline-architecture.md (sections "Stage 1 — Regex classifier", "Stage 2 — Per-category handlers")
    - docs/zapier-patterns.md (registry-driven routing pattern reference)
  </read_first>
  <action>
Create `docs/agentic-pipeline/stage-1-regex.md`.

Required H2 sections in this order:
1. `## Goal` — 2-3 sentences: deterministic regex classification of inbound text into a category (e.g. `invoice_copy_request`, `payment_promise`, `unknown`). Cheap, fast, auditable. `unknown` falls through to Stage 2 + Stage 3.
2. `## Anthropic Pattern Mapping` — one paragraph: cite Anthropic URL ONCE; paraphrase the "routing" pattern (classify input, dispatch to specialised follow-up). Reference how `swarm_categories.action` ('categorize_archive' vs 'swarm_dispatch') is the dispatch switch.
3. `## Architecture` — ASCII diagram showing: `inbound (post Stage 0) -> regex match -> category + action lookup in swarm_categories -> [archive | swarm_dispatch | unknown -> Stage 2]`.
4. `## Today-State (Debtor-Email Worked Example)` — H3 sub-sections:
   - `### Reference implementation` — cite `web/lib/debtor-email/classify.ts` (link path, no code copy).
   - `### Dispatch worker` — cite `web/lib/inngest/functions/classifier-verdict-worker.ts`.
   - `### Category registry` — cite `swarm_categories` table; one row per (swarm_type, category_key) with the action column.
   - `### Real category keys` — bulleted list of 4-6 real keys from the codebase (e.g. `invoice_copy_request`, `payment_promise`, `not_responsible`, `address_correction`, `unknown`). Verify keys at write-time by greping classify.ts.
5. `## Sales-Email Parallel Block` — <=10 lines. Brief illustrative-only category names: `new_lead`, `follow_up`, `unsubscribe`. Tag explicitly: `(illustrative — Phase 73 ships actual sales-email categories)`. Purpose: prove the shape is swarm-agnostic.
6. `## Override Capture` — 1 paragraph: Axis 1 is the override surface for this stage. Cross-link to `./override-model.md#axis-1--wrong-category-stage-1`. Today-state real columns: `email_labels.corrected_category` + `agent_runs.human_verdict`.
7. `## Graduated Automation` — 1 paragraph: regex-rule promotion hook consumes Axis 1 signals. Cross-link to `./graduated-automation.md#hook-taxonomy`. Wilson-CI gate (no thresholds in this doc).
8. `## Implementation Patterns (link out)` — `../zapier-patterns.md` for registry-driven routing; `../inngest-patterns.md` for the dispatch worker pattern.
9. `## Forward References` — registry-driven `swarms.entity_brand` (Phase 68); concrete promotion thresholds (Phase 71).
10. `## See Also` — relative links to `./README.md`, `./stage-0-safety.md`, `./stage-2-entity.md`, `./override-model.md`, `./graduated-automation.md`.

Style identical to stage-0-safety.md. Implements D-02, D-07.
  </action>
  <verify>
    <automated>test -f docs/agentic-pipeline/stage-1-regex.md &amp;&amp; grep -qE "swarm_categories" docs/agentic-pipeline/stage-1-regex.md &amp;&amp; grep -qF "classify.ts" docs/agentic-pipeline/stage-1-regex.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/agentic-pipeline/stage-1-regex.md`
    - `grep -F "web/lib/debtor-email/classify.ts" docs/agentic-pipeline/stage-1-regex.md`
    - `grep -F "classifier-verdict-worker.ts" docs/agentic-pipeline/stage-1-regex.md`
    - `grep -E "illustrative.*Phase 73" docs/agentic-pipeline/stage-1-regex.md` (sales-email parallel block tagged)
    - `grep -E "Axis 1" docs/agentic-pipeline/stage-1-regex.md` (override cross-ref)
    - `! grep -rEi "smeba-uk|smeba-ie" docs/agentic-pipeline/stage-1-regex.md`
    - `! grep -rEi "netlify|railway|firebase|neon|puppeteer" docs/agentic-pipeline/stage-1-regex.md`
  </acceptance_criteria>
  <done>File exists; debtor-email worked example with real category keys; <=10-line sales-email parallel block tagged illustrative; Axis 1 + graduated-automation cross-references present; classify.ts + classifier-verdict-worker.ts cited.</done>
</task>

<task type="auto">
  <name>Task 3: Write stage-2-entity.md (entity enrichment)</name>
  <files>docs/agentic-pipeline/stage-2-entity.md</files>
  <read_first>
    - .planning/phases/63-architecture-rfc/63-CONTEXT.md (D-02, D-04 contract notation)
    - .planning/phases/63-architecture-rfc/63-RESEARCH.md (sections "File Manifest / stage-2-entity.md" row, "Today-State Telemetry / Brand registry today")
    - docs/agentic-pipeline/context-shape-contract.md (Wave 1 output — DO NOT duplicate, link only)
    - docs/debtor-email-pipeline-architecture.md (resolver pipeline content for worked example)
    - docs/zapier-patterns.md (NXT lookup via Zapier SDK whitelisted IP)
  </read_first>
  <action>
Create `docs/agentic-pipeline/stage-2-entity.md`.

Required H2 sections in this order:
1. `## Goal` — 2-3 sentences: enrich the inbound message with structured customer/entity data. Deterministic-first (thread -> sender -> identifier lookups), LLM tiebreaker only when deterministic methods conflict. Output shape is the `PipelineStageContext` defined in `./context-shape-contract.md`.
2. `## Anthropic Pattern Mapping` — one paragraph: this is NOT a pattern from the Anthropic post — it's pre-LLM enrichment that feeds the chained workflow. Cite the Anthropic URL ONCE only if helpful (paraphrase: "tools-first, LLM only when tools are insufficient"). Otherwise omit citation.
3. `## Architecture` — ASCII diagram: `email -> [thread lookup] -> [sender map] -> [identifier extraction] -> [LLM tiebreaker if conflict] -> PipelineStageContext`.
4. `## Today-State (Debtor-Email Worked Example)` — H3 sub-sections:
   - `### Resolver pipeline` — cite `web/lib/automations/debtor-email/resolve-debtor.ts`.
   - `### Per-brand routing` — today's source-of-truth: `debtor.labeling_settings.brand_id` (2-letter NXT brand code) + `debtor.labeling_settings.nxt_database` (drives which NXT DB the resolver Zap queries).
   - `### Backend lookup` — NXT SQL via Zapier SDK whitelisted IP. Cross-link `../zapier-patterns.md`. Do NOT duplicate.
5. `## Cross-Swarm Pluggability` — 1 paragraph: SugarCRM-backed sales swarm (Phase 73) plugs in by implementing the same `PipelineStageContext` output shape with a different backend resolver. The contract is backend-agnostic.
6. `## Output Contract` — link to `./context-shape-contract.md`. Do NOT duplicate the TS interface. Single sentence: "Stage 2 emits `PipelineStageContext` per `./context-shape-contract.md`."
7. `## Override Capture` — Axis 2 is the override surface. Cross-link `./override-model.md#axis-2--wrong-customer-stage-2`. Today-state: PARTIAL (per-mailbox approve/reject); consolidated capture is Phase 71.
8. `## Graduated Automation` — sender-mapping promotion consumes Axis 2 signals. Cross-link `./graduated-automation.md#hook-taxonomy`.
9. `## Forward References` — `swarms.entity_brand` unified registry (Phase 68); SugarCRM resolver (Phase 73); consolidated `corrected_customer_account_id` capture (Phase 71).
10. `## See Also` — relative links to `./README.md`, `./stage-1-regex.md`, `./stage-3-coordinator.md`, `./context-shape-contract.md`, `./override-model.md`, `./graduated-automation.md`.

Style identical. Implements D-02, references Wave 1 outputs.
  </action>
  <verify>
    <automated>test -f docs/agentic-pipeline/stage-2-entity.md &amp;&amp; grep -qF "context-shape-contract.md" docs/agentic-pipeline/stage-2-entity.md &amp;&amp; grep -qE "resolve-debtor" docs/agentic-pipeline/stage-2-entity.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/agentic-pipeline/stage-2-entity.md`
    - `grep -F "./context-shape-contract.md" docs/agentic-pipeline/stage-2-entity.md`
    - `grep -E "labeling_settings.brand_id|nxt_database" docs/agentic-pipeline/stage-2-entity.md`
    - `grep -E "Axis 2" docs/agentic-pipeline/stage-2-entity.md`
    - `! grep -E "interface PipelineStageContext" docs/agentic-pipeline/stage-2-entity.md` (must NOT duplicate the TS interface)
    - `! grep -rEi "smeba-uk|smeba-ie" docs/agentic-pipeline/stage-2-entity.md`
    - `! grep -rEi "netlify|railway|firebase|neon|puppeteer" docs/agentic-pipeline/stage-2-entity.md`
  </acceptance_criteria>
  <done>File exists; resolver pipeline cited; today-state brand routing real (brand_id + nxt_database); contract linked not duplicated; Axis 2 + sender-mapping cross-refs present.</done>
</task>

<task type="auto">
  <name>Task 4: Write stage-3-coordinator.md (ranked-intent coordinator + Stage 3.5 placeholder)</name>
  <files>docs/agentic-pipeline/stage-3-coordinator.md</files>
  <read_first>
    - .planning/phases/63-architecture-rfc/63-CONTEXT.md (Claude's Discretion: Stage 3.5 escalation = principle + placeholder, full design Phase 65)
    - .planning/phases/63-architecture-rfc/63-RESEARCH.md (sections "Anthropic Agentic Guidance Synthesis / Orchestrator-worker pattern", "Open Questions" item 1 — escalation diamond recommendation)
    - .planning/REQUIREMENTS.md (CORD-01..04)
    - docs/agentic-pipeline/context-shape-contract.md (Wave 1 — Stage 3 consumes this)
    - docs/orqai-patterns.md
  </read_first>
  <action>
Create `docs/agentic-pipeline/stage-3-coordinator.md`.

Required H2 sections in this order:
1. `## Goal` — 2-3 sentences: take the `PipelineStageContext` and produce a ranked intent list (CORD-01). Single-shot default handles ~80% of cases; multi-intent or low-confidence escalates to Stage 3.5 orchestrator-worker.
2. `## Anthropic Pattern Mapping` — one paragraph citing Anthropic URL ONCE. Paraphrase: prompt-chaining (Stage 3 receives Stage 2 output, gates Stage 4 dispatch); orchestrator-worker (Stage 3.5 escalation when fan-out is needed).
3. `## Architecture` — ASCII diagram: `PipelineStageContext -> [ranked intent list (LLM)] -> [confidence + count check] -> [single-shot Stage 4 handler] OR [Stage 3.5 orchestrator-worker (Phase 65)]`.
4. `## Input Contract` — link `./context-shape-contract.md`. Single sentence — do NOT duplicate the interface.
5. `## Output: Ranked Intent List` — describe per CORD-01: list of `{intent, confidence}` ordered descending. Top intent dispatches to Stage 4 by default. Implementation pattern: Orq.ai response_format json_schema (link `../orqai-patterns.md`).
6. `## Sales-Email Parallel Block` — <=10 lines. Illustrative intents: `qualify_lead`, `schedule_demo`, `route_to_account_owner`. Tag `(illustrative — Phase 73 ships actuals)`.
7. `## Stage 3.5 Escalation (principle + placeholder)` — H3 sub-sections:
   - `### Escalation conditions` — ASCII decision diamond showing the 3 conditions: confidence < threshold (TBD Phase 71) OR intent_count >= 3 OR `requires_orchestration` flag.
   - `### Spawn orchestrator-worker` — single line: "spawn orchestrator-worker -> see Phase 65 (CORD-02..04) for full design."
   - Do NOT design the orchestrator-worker mechanics — that's Phase 65.
8. `## Override Capture` — Axis 3 is the override surface. Cross-link `./override-model.md#axis-3--wrong-intent-stage-3`. Today-state: NOT YET CAPTURED. Phase 65 ships ranked-intent surface; Phase 71 ships the override control.
9. `## Graduated Automation` — prompt-tune trigger consumes Axis 3 signals. Cross-link `./graduated-automation.md`.
10. `## Implementation Patterns (link out)` — `../orqai-patterns.md` (response_format, fallbacks, 45s timeout); do NOT duplicate.
11. `## Forward References` — Stage 3.5 full design (Phase 65, CORD-02..04); concrete confidence threshold (Phase 71); `swarm_intents` table (Phase 68 SWRM-02).
12. `## See Also` — relative links to `./README.md`, `./stage-2-entity.md`, `./stage-4-handler.md`, `./context-shape-contract.md`, `./override-model.md`, `./graduated-automation.md`.

Style identical.
  </action>
  <verify>
    <automated>test -f docs/agentic-pipeline/stage-3-coordinator.md &amp;&amp; grep -qE "ranked|intent" docs/agentic-pipeline/stage-3-coordinator.md &amp;&amp; grep -qF "Phase 65" docs/agentic-pipeline/stage-3-coordinator.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/agentic-pipeline/stage-3-coordinator.md`
    - `grep -E "Stage 3\.5|orchestrator-worker" docs/agentic-pipeline/stage-3-coordinator.md`
    - `grep -E "illustrative.*Phase 73" docs/agentic-pipeline/stage-3-coordinator.md`
    - `grep -E "Axis 3" docs/agentic-pipeline/stage-3-coordinator.md`
    - `grep -F "./context-shape-contract.md" docs/agentic-pipeline/stage-3-coordinator.md`
    - `! grep -E "interface PipelineStageContext" docs/agentic-pipeline/stage-3-coordinator.md` (must NOT duplicate)
    - `! grep -rEi "smeba-uk|smeba-ie" docs/agentic-pipeline/stage-3-coordinator.md`
    - `! grep -rEi "netlify|railway|firebase|neon|puppeteer" docs/agentic-pipeline/stage-3-coordinator.md`
  </acceptance_criteria>
  <done>File exists; ranked-intent output described; Stage 3.5 escalation = principle + placeholder; sales-email parallel block tagged illustrative; Axis 3 honest about today-state.</done>
</task>

<task type="auto">
  <name>Task 5: Write stage-4-handler.md (handler agents + zapier_tools registry)</name>
  <files>docs/agentic-pipeline/stage-4-handler.md</files>
  <read_first>
    - .planning/phases/63-architecture-rfc/63-CONTEXT.md (D-07 worked example uses real handler agent name)
    - .planning/phases/63-architecture-rfc/63-RESEARCH.md (sections "File Manifest / stage-4-handler.md" row, "Today-State Telemetry / Tool registry today" — confirms zapier_tools exists, allowed_for_intents does NOT)
    - .planning/REQUIREMENTS.md (BUDG-02, CANO-01..04)
    - supabase/migrations/20260429_zapier_tools_registry.sql (cite columns)
    - web/lib/automations/debtor-email/nxt-zap-client.ts (cite as canonical client pattern)
    - docs/zapier-patterns.md
    - docs/browserless-patterns.md
    - docs/orqai-patterns.md
    - docs/swarm-bridge-contract.md (cross-link only — Stage 4 emits automation_runs the bridge consumes)
  </read_first>
  <action>
Create `docs/agentic-pipeline/stage-4-handler.md`.

Required H2 sections in this order:
1. `## Goal` — 2-3 sentences: a handler agent takes a single dispatched intent and produces the side effect (draft reply, fetch document, update record). Bounded single-shot; not an autonomous loop. Tools are registered in `zapier_tools`.
2. `## Anthropic Pattern Mapping` — one paragraph citing Anthropic URL ONCE. Paraphrase: bounded single-shot handler is closer to "automated workflow" than autonomous agent (per Anthropic post's distinction). Tools-allowlisted.
3. `## Architecture` — ASCII diagram: `dispatched intent + PipelineStageContext -> handler agent (Orq.ai) -> tool calls (zapier_tools registry) -> side effect (iController draft, NXT lookup, etc.) -> automation_runs row`.
4. `## Today-State (Debtor-Email Worked Example)` — H3 sub-sections:
   - `### Reference handler agent` — name: `debtor-copy-document-body-agent`. Per-swarm canonicalisation forward-ref Phase 69 (CANO-01..04).
   - `### Tool registry` — cite `supabase/migrations/20260429_zapier_tools_registry.sql`. List columns from RESEARCH: `tool_id, description, backend, pattern (sync|async_callback), target_url, auth_method (body_field|header_bearer), auth_secret_env, auth_field_name, input_schema jsonb, output_schema jsonb, callback_route, enabled, notes`.
   - `### Seeded NXT tool rows (today)` — `nxt.contact_lookup`, `nxt.identifier_lookup`, `nxt.candidate_details`, `nxt.invoice_fetch`.
   - `### Canonical client` — cite `web/lib/automations/debtor-email/nxt-zap-client.ts`.
5. `## Allowed-Intents Allowlist (forward-ref Phase 64)` — 1 paragraph. Per BUDG-02, Phase 64 adds `allowed_for_intents text[]` column to `zapier_tools`. Each tool row will declare which intents may invoke it (e.g. a copy-document handler cannot reach a payment-update tool). Today: column DOES NOT EXIST. Mark explicitly as forward-reference.
6. `## Implementation Patterns (link out)` — `../zapier-patterns.md` (registry, NXT SQL via whitelisted IP, body-field auth pattern); `../browserless-patterns.md` (playwright-core, NOT playwright); `../orqai-patterns.md` (response_format json_schema, fallback model, 45s timeout). Do NOT duplicate any of these.
7. `## Stack Constraints (per CLAUDE.md)` — bulleted: Browserless via `playwright-core`; NXT SQL only via Zapier whitelisted IP; NXT-S3 documents also via Zapier SDK (not direct AWS SDK); Orq.ai LLM Router for ad-hoc LLM calls (not direct OpenAI/Anthropic keys). One line each.
8. `## Output Plumbing` — single line: handler emits `automation_runs` row consumed by the kanban surface — see `../swarm-bridge-contract.md` (sibling concern, not duplicated here).
9. `## Override Capture` — Axis 4. Cross-link `./override-model.md#axis-4--wrong-handler-output-stage-4`. Today-state real columns: `email_labels.draft_quality` + `email_labels.feedback_reason`.
10. `## Graduated Automation` — prompt-tune / handler-replacement consumes Axis 4 signals. Cross-link `./graduated-automation.md`.
11. `## Forward References` — `allowed_for_intents` column (Phase 64 BUDG-02); handler canonicalisation (Phase 69 CANO-01..04); per-run cost ceilings (Phase 64 BUDG-01).
12. `## See Also` — relative links to `./README.md`, `./stage-3-coordinator.md`, `./context-shape-contract.md`, `./override-model.md`, `./graduated-automation.md`, `../swarm-bridge-contract.md`.

Style identical. Implements D-02, D-07.
  </action>
  <verify>
    <automated>test -f docs/agentic-pipeline/stage-4-handler.md &amp;&amp; grep -qE "zapier_tools" docs/agentic-pipeline/stage-4-handler.md &amp;&amp; grep -qE "debtor-copy-document-body-agent" docs/agentic-pipeline/stage-4-handler.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/agentic-pipeline/stage-4-handler.md`
    - `grep -F "20260429_zapier_tools_registry.sql" docs/agentic-pipeline/stage-4-handler.md`
    - `grep -F "nxt-zap-client.ts" docs/agentic-pipeline/stage-4-handler.md`
    - `grep -E "allowed_for_intents" docs/agentic-pipeline/stage-4-handler.md`
    - `grep -E "Phase 64.*BUDG-02|BUDG-02" docs/agentic-pipeline/stage-4-handler.md`
    - `grep -F "playwright-core" docs/agentic-pipeline/stage-4-handler.md`
    - `grep -E "Axis 4" docs/agentic-pipeline/stage-4-handler.md`
    - `! grep -rEi "smeba-uk|smeba-ie" docs/agentic-pipeline/stage-4-handler.md`
    - `! grep -rEi "netlify|railway|firebase|neon|puppeteer" docs/agentic-pipeline/stage-4-handler.md`
  </acceptance_criteria>
  <done>File exists; zapier_tools registry columns documented; debtor-copy-document-body-agent named as worked example; allowed_for_intents marked as Phase 64 forward-ref; stack constraints stated (playwright-core); cross-link to swarm-bridge-contract.md present.</done>
</task>

</tasks>

<threat_model>
Docs-only phase; no executable code; no migrations; no network calls. Threat surface = none. Acceptable per phase scope.
</threat_model>

<verification>
After all 5 tasks complete, run from repo root:
- `for s in 0-safety 1-regex 2-entity 3-coordinator 4-handler; do test -f "docs/agentic-pipeline/stage-${s}.md" || { echo "MISS stage-${s}.md"; exit 1; }; done`
- `! grep -rEi "smeba-uk|smeba-ie" docs/agentic-pipeline/`
- `! grep -rEi "netlify|railway|firebase|neon|puppeteer" docs/agentic-pipeline/`
- Manual: open each stage doc in markdown preview; confirm ASCII diagrams align in monospace, Anthropic URL appears at most once per doc, no quote-bombs, parallel blocks <=10 lines and tagged illustrative.
</verification>

<success_criteria>
- All 5 stage files exist
- Each cites Anthropic URL at most once where relevant
- Debtor-email worked example present in stages 1-4 with real names (classify.ts, classifier-verdict-worker.ts, resolve-debtor.ts, debtor-copy-document-body-agent, zapier_tools)
- Sales-email parallel blocks present ONLY in stage-1 + stage-3, <=10 lines, tagged illustrative
- Stage 0 + Stage 4 today-state honest (Stage 0 not yet shipped; Stage 4 zapier_tools real, allowed_for_intents Phase 64)
- Wave 1 contracts cross-referenced not duplicated
- Zero speculative brand names; zero stack-violation strings
</success_criteria>

<output>
After completion, create `.planning/phases/63-architecture-rfc/63-02-SUMMARY.md` recording:
- Files created (5)
- Real handler agent names verified at write-time
- Real category keys verified from classify.ts
- Any deviations
</output>
