---
phase: 63
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/agentic-pipeline/context-shape-contract.md
  - docs/agentic-pipeline/override-model.md
  - docs/agentic-pipeline/graduated-automation.md
autonomous: true
requirements: [RFC-02, RFC-03, RFC-04]
must_haves:
  truths:
    - "Stage 2->3 context-shape contract is documented as a TS interface plus prose table with context_version: 1 and a data-driven brand list"
    - "4-axis override model is documented with one axis per stage 1-4, distinguishing today-state real columns (axes 1+4) from forward-referenced Phase 71 work (axes 2+3)"
    - "Graduated-automation hook taxonomy documents promotion direction (LLM -> deterministic) and cites Wilson-CI as precedent without pinning thresholds"
  artifacts:
    - path: "docs/agentic-pipeline/context-shape-contract.md"
      provides: "TS interface + prose table for Stage 2->3 contract"
      contains: "context_version"
    - path: "docs/agentic-pipeline/override-model.md"
      provides: "4-axis override taxonomy"
      contains: "Axis 1, Axis 2, Axis 3, Axis 4"
    - path: "docs/agentic-pipeline/graduated-automation.md"
      provides: "Promotion-ladder principles + Wilson-CI precedent (no pinned numbers)"
      contains: "Wilson"
  key_links:
    - from: "docs/agentic-pipeline/context-shape-contract.md"
      to: "swarms.entity_brand registry (Phase 68)"
      via: "data-driven brand-list comment, NOT TS literal-union"
    - from: "docs/agentic-pipeline/override-model.md"
      to: "graduated-automation.md hooks"
      via: "each axis names which hook consumes its signal"
    - from: "docs/agentic-pipeline/graduated-automation.md"
      to: "web/lib/classifier/wilson.ts"
      via: "named precedent, no inline thresholds"
---

<objective>
Establish the three foundational scaffolding documents that all five per-stage docs (Wave 2) reference: the Stage 2->3 context-shape contract (RFC-02), the 4-axis override model (RFC-03), and the graduated-automation hook taxonomy (RFC-04).

Purpose: stage docs cannot be self-contained per CONTEXT D-02 unless these contract/model documents exist first. Each stage doc cross-references one or more of these three files.

Output: 3 NEW markdown files under `docs/agentic-pipeline/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-architecture-rfc/63-CONTEXT.md
@.planning/phases/63-architecture-rfc/63-RESEARCH.md
@.planning/phases/63-architecture-rfc/63-VALIDATION.md
@CLAUDE.md
@docs/debtor-email-pipeline-architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write context-shape-contract.md (Stage 2->3 contract, RFC-02)</name>
  <files>docs/agentic-pipeline/context-shape-contract.md</files>
  <read_first>
    - .planning/phases/63-architecture-rfc/63-CONTEXT.md (decisions D-04, D-05, D-06 — TS interface + prose, context_version: 1, brand list data-driven)
    - .planning/phases/63-architecture-rfc/63-RESEARCH.md (sections "User Constraints", "File Manifest", "Today-State Telemetry / Brand registry today")
    - docs/debtor-email-pipeline-architecture.md (style template — ASCII boxes, blockquote banners, GitHub pipe tables, no frontmatter)
    - docs/swarm-bridge-contract.md (sibling — different concern; ensure new file does NOT duplicate UI-plumbing contract)
  </read_first>
  <action>
Create `docs/agentic-pipeline/context-shape-contract.md`.

Required H2 sections in this order:
1. `## Purpose` — one paragraph: this is the runtime data contract handed from Stage 2 (entity enrichment) to Stage 3 (intent coordinator); lookup-backend-agnostic so a SugarCRM-backed sales swarm produces the same shape as an NXT-backed debtor swarm.
2. `## TypeScript Interface (Canonical)` — fenced ```typescript block with EXACTLY these fields, verbatim:
   ```typescript
   // Source of truth for the cross-swarm Stage 2 -> Stage 3 contract.
   // Codified in web/lib/agentic-pipeline/types.ts in a downstream phase (Phase 64 or 70).
   export interface PipelineStageContext {
     customer_id: string | null;        // backend-stable identifier (NXT account id, SugarCRM contact id, ...)
     customer_name: string | null;      // human-readable display name
     language: 'nl' | 'fr' | 'en' | 'de'; // detected reply language
     entity_brand: string;              // data-driven from swarms.entity_brand registry (Phase 68)
                                        // today's set: smeba | smeba-fire | firecontrol | sicli-noord | sicli-sud | berki
                                        // NOT a TypeScript literal-union enum -- registry-driven
     recent_documents: DocumentRef[];   // last N documents touched by this customer (per-swarm N)
     context_version: 1;                // contract version, persisted on every pipeline_events row
   }

   export interface DocumentRef {
     id: string;
     kind: string;        // e.g. 'invoice', 'reminder', 'credit_note' -- vocabulary per swarm
     date: string;        // ISO 8601
     reference: string | null;
   }
   ```
3. `## Prose Table (Field Semantics)` — GitHub pipe table with columns: `Field | Required | Nullable | Source backend (today) | Semantic notes`. One row per field. For `customer_id` cite NXT account lookup via Zapier SQL OR SugarCRM contact-id (forward-ref Phase 73). For `entity_brand` note: today sourced from `debtor.labeling_settings.brand_id` + `nxt_database`; Phase 68 unifies into `swarms.entity_brand` registry.
4. `## Versioning Policy` — state explicitly: `context_version: 1` from day one; additive optional fields stay at major 1; breaking changes (rename, type change, removal) bump major. Each `pipeline_events` row persists the version it was produced under (forward-ref Phase 70 for the table itself).
5. `## Forward References` — bulleted list:
   - `web/lib/agentic-pipeline/types.ts` codification — Phase 64 or 70 (whichever first needs the type at runtime)
   - `swarms.entity_brand` unified registry — Phase 68 (SWRM-01)
   - `pipeline_events` persistence target — Phase 70 (TELE-01)
6. `## See Also` — relative links to `./README.md`, `./stage-2-entity.md`, `./stage-3-coordinator.md`, `../swarm-bridge-contract.md` (the sibling, different concern).

Style: English. ATX headers. No emojis. No frontmatter. Match `docs/debtor-email-pipeline-architecture.md` conventions. Implements D-04, D-05, D-06.
  </action>
  <verify>
    <automated>test -f docs/agentic-pipeline/context-shape-contract.md &amp;&amp; [ "$(grep -cE 'interface|customer_id|customer_name|language|entity_brand|recent_documents|context_version' docs/agentic-pipeline/context-shape-contract.md)" -ge 7 ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/agentic-pipeline/context-shape-contract.md`
    - `grep -E "context_version.*1" docs/agentic-pipeline/context-shape-contract.md`
    - `grep -E "data-driven" docs/agentic-pipeline/context-shape-contract.md`
    - `! grep -E "type EntityBrand =|enum EntityBrand" docs/agentic-pipeline/context-shape-contract.md`
    - `! grep -rEi "smeba-uk|smeba-ie" docs/agentic-pipeline/context-shape-contract.md`
    - `! grep -rEi "netlify|railway|firebase|neon|puppeteer" docs/agentic-pipeline/context-shape-contract.md`
  </acceptance_criteria>
  <done>File exists; TS interface present with all 6 required fields verbatim; prose table covers all fields; versioning policy stated; brand list documented as data-driven (no enum); no speculative brand names; no stack-violation strings.</done>
</task>

<task type="auto">
  <name>Task 2: Write override-model.md (4-axis override taxonomy, RFC-03)</name>
  <files>docs/agentic-pipeline/override-model.md</files>
  <read_first>
    - .planning/phases/63-architecture-rfc/63-CONTEXT.md (decisions D-10, D-11 — 4 axes independent)
    - .planning/phases/63-architecture-rfc/63-RESEARCH.md (section "Today-State Telemetry / Where overrides are captured today" — table of axis -> table.column)
    - docs/debtor-email-pipeline-architecture.md (section "Feedback model — two distinct loops"; section "Human Review surfaces")
  </read_first>
  <action>
Create `docs/agentic-pipeline/override-model.md`.

Required H2 sections in this order:
1. `## Purpose` — one paragraph: operators correct pipeline output at four independent points; each correction produces a distinct learning signal that feeds a different graduated-automation hook (cross-link to `./graduated-automation.md`).
2. `## Four Axes (Overview Table)` — GitHub pipe table with columns: `Axis | Stage | Override meaning | Today-state (source-of-truth) | Graduated-automation hook consumed`. Four rows, exactly:
   - Axis 1 | Stage 1 (regex) | Wrong category | REAL TODAY: `debtor.email_labels.corrected_category` + `agent_runs.human_verdict` (cite migrations `20260428_public_agent_runs.sql` line 85, `20260430c_email_labels_feedback_and_invoice_copy.sql` line 6) | regex-rule promotion (Wilson-CI gate)
   - Axis 2 | Stage 2 (entity) | Wrong customer | PARTIAL TODAY: per-mailbox approve/reject writes `agent_runs.human_verdict` + updates `email_labels.reviewed_by/reviewed_at`; consolidated `corrected_customer_account_id` capture forward-referenced to Phase 71 | sender-mapping promotion
   - Axis 3 | Stage 3 (coordinator) | Wrong intent | DOES NOT EXIST TODAY — Phase 65 introduces ranked-intent surface; Phase 71 ships override capture | prompt-tune trigger
   - Axis 4 | Stage 4 (handler) | Wrong handler output | REAL TODAY: `email_labels.draft_quality` + `email_labels.feedback_reason`; `agent_runs.human_verdict` includes `'edited_minor'` and `'rejected'` (cite `20260428_public_agent_runs.sql` line 68) | prompt-tune / handler-replacement
3. `## Axis 1 -- Wrong Category (Stage 1)` — H3 sub-sections: `### Definition`, `### Where captured today`, `### Telemetry row produced`, `### Graduated-automation hook`. Each section: 2-4 sentences. Cite the Bulk Review UI surface at `web/app/(dashboard)/automations/debtor-email-review/`.
4. `## Axis 2 -- Wrong Customer (Stage 2)` — same H3 structure. Mark today-state as "partial — consolidated in Phase 71". Honest: `corrected_customer_account_id` column name is `[ASSUMED — verify at write-time]`; describe the concept abstractly if column does not match.
5. `## Axis 3 -- Wrong Intent (Stage 3)` — same H3 structure. Mark "today-state: not yet captured. Phase 65 ships the ranked-intent coordinator; Phase 71 ships the Bulk Review override control."
6. `## Axis 4 -- Wrong Handler Output (Stage 4)` — same H3 structure. Cite `draft_quality` and `feedback_reason` columns by name. Reference the kanban-card review surface.
7. `## Independence of Axes` — 1 paragraph implementing D-11 verbatim: overriding axis N does NOT invalidate downstream stages' decisions on the same email; those become NON-APPLICABLE, not WRONG. Eval logic in Phase 71 must not double-count.
8. `## Forward References` — bulleted list: `pipeline_events` table (Phase 70), Bulk Review 4-axis UI (Phase 71), ranked-intent coordinator (Phase 65), consolidated axis 2/3 columns (Phase 71).
9. `## See Also` — relative links to `./README.md`, `./graduated-automation.md`, `./stage-1-regex.md`, `./stage-2-entity.md`, `./stage-3-coordinator.md`, `./stage-4-handler.md`.

Style identical to context-shape-contract.md. No emojis. ATX headers. Implements D-10, D-11.
  </action>
  <verify>
    <automated>test -f docs/agentic-pipeline/override-model.md &amp;&amp; [ "$(grep -cE 'Axis [1-4]' docs/agentic-pipeline/override-model.md)" -ge 4 ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/agentic-pipeline/override-model.md`
    - `[ "$(grep -cE 'Axis [1-4]' docs/agentic-pipeline/override-model.md)" -ge 4 ]`
    - `grep -E "draft_quality|feedback_reason" docs/agentic-pipeline/override-model.md`
    - `grep -E "Phase 71|Phase 65" docs/agentic-pipeline/override-model.md`
    - `grep -iE "non-applicable|independent" docs/agentic-pipeline/override-model.md`
    - `! grep -rEi "smeba-uk|smeba-ie" docs/agentic-pipeline/override-model.md`
  </acceptance_criteria>
  <done>File exists; all 4 axes present with H3 sub-structure; today-state honest per axis (axes 1+4 cite real columns, axes 2+3 forward-ref Phase 71); D-11 independence statement present; no speculative brands.</done>
</task>

<task type="auto">
  <name>Task 3: Write graduated-automation.md (promotion-ladder principles, RFC-04)</name>
  <files>docs/agentic-pipeline/graduated-automation.md</files>
  <read_first>
    - .planning/phases/63-architecture-rfc/63-CONTEXT.md (decision D-12 — principles + signal types only, NO concrete thresholds)
    - .planning/phases/63-architecture-rfc/63-RESEARCH.md (sections "Anthropic Agentic Guidance Synthesis", "Today-State Telemetry / Wilson-CI promotion gate")
    - web/lib/classifier/wilson.ts (verify the precedent exists; do NOT copy thresholds into the doc)
  </read_first>
  <action>
Create `docs/agentic-pipeline/graduated-automation.md`.

Required H2 sections in this order:
1. `## Purpose` — one paragraph: as patterns become deterministic, narrow the LLM's responsibility surface. Promotion direction is one-way: LLM-handled -> deterministic rule. Cite Anthropic "Building Effective Agents" once with the URL `https://www.anthropic.com/engineering/building-effective-agents` (paraphrase the evaluator-optimizer pattern in our own voice; no quote-bombs).
2. `## Hook Taxonomy` — GitHub pipe table: `Hook name | Stage | Telemetry signal consumed | Promotion direction`. Rows:
   - regex-rule promotion | Stage 1 | Axis 1 overrides over a sample window | LLM-handled `unknown` bucket -> new regex rule
   - sender-mapping promotion | Stage 2 | Axis 2 approve/reject signals | LLM-aided sender lookup -> deterministic sender->customer map
   - prompt-tune trigger | Stage 3 | Axis 3 intent corrections clustered by pattern | prompt revision proposal queued for human review
   - handler-replacement / prompt-tune | Stage 4 | Axis 4 `draft_quality` + `feedback_reason` clusters | prompt revision OR handler-agent swap proposal
3. `## Working Precedent — Wilson-CI Sender-Mapping (Phase 56)` — 1-2 paragraphs. State: a binomial confidence interval over operator approve/reject signals gates promotion from candidate to live. Concrete thresholds live in code at `web/lib/classifier/wilson.ts` and tune with phase. Phase 71 catalogues thresholds across hooks. EXPLICITLY DO NOT mention any numeric threshold value.
4. `## Anthropic Mapping` — 3-4 sentences: workflow-vs-agent distinction, evaluator-optimizer pattern as the conceptual basis. Operator overrides = the evaluator signal; promotion recommender (Phase 72) = the optimizer that proposes deterministic rules.
5. `## Forward References` — bulleted list: concrete thresholds (Phase 71, LERN-01..05); promotion recommender (Phase 72); `pipeline_events` as single source of truth for promotion math (Phase 70).
6. `## See Also` — relative links to `./README.md`, `./override-model.md`, `./stage-1-regex.md`, `./stage-2-entity.md`, `./stage-3-coordinator.md`, `./stage-4-handler.md`.

Hard constraint: file MUST NOT contain numeric promotion-threshold literals. Wilson-CI precedent named only by phase and file path. Implements D-12.
  </action>
  <verify>
    <automated>test -f docs/agentic-pipeline/graduated-automation.md &amp;&amp; grep -qE "Wilson|Phase 56" docs/agentic-pipeline/graduated-automation.md &amp;&amp; ! grep -qE "N=[0-9]+|CI-lo=[0-9]" docs/agentic-pipeline/graduated-automation.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/agentic-pipeline/graduated-automation.md`
    - `grep -E "Wilson|Phase 56" docs/agentic-pipeline/graduated-automation.md`
    - `! grep -E "N=[0-9]+|CI-lo=[0-9]" docs/agentic-pipeline/graduated-automation.md`
    - `! grep -E "0\.92|0\.95|>= ?30|>= ?50" docs/agentic-pipeline/graduated-automation.md`
    - `grep -F "anthropic.com/engineering/building-effective-agents" docs/agentic-pipeline/graduated-automation.md`
    - `! grep -rEi "smeba-uk|smeba-ie" docs/agentic-pipeline/graduated-automation.md`
  </acceptance_criteria>
  <done>File exists; hook taxonomy table present; Wilson-CI named without numeric thresholds; Anthropic URL cited once; promotion direction stated as LLM -> deterministic for all hooks.</done>
</task>

</tasks>

<threat_model>
Docs-only phase; no executable code; no migrations; no network calls. Threat surface = none. Acceptable per phase scope.
</threat_model>

<verification>
After all 3 tasks complete, run from repo root:
- `for f in docs/agentic-pipeline/{context-shape-contract,override-model,graduated-automation}.md; do test -f "$f" || { echo "MISS $f"; exit 1; }; done`
- `! grep -rEi "smeba-uk|smeba-ie" docs/agentic-pipeline/`
- `! grep -rEi "netlify|railway|firebase|neon|puppeteer" docs/agentic-pipeline/`
</verification>

<success_criteria>
- All 3 files exist under `docs/agentic-pipeline/`
- context-shape-contract.md has TS interface with all 6 required fields verbatim and `context_version: 1`
- override-model.md has all 4 axes with honest today-state callouts
- graduated-automation.md cites Wilson-CI precedent without numeric thresholds
- Zero speculative brand names anywhere
- Zero stack-violation strings anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/63-architecture-rfc/63-01-SUMMARY.md` recording:
- Files created (3)
- Any deviations from the plan (e.g., column name verification for axis 2)
- Cross-references downstream stage docs will rely on
</output>
