---
phase: 999.7
plan: 01
subsystem: stage-0
wave: 1
tags: [stage-0, tdd-red, fixtures, dependency-install, budget-counter]
requires:
  - phase: 64
    plan: 01
    why: "Source budget-counter.ts module + initial BUDGET_CEILING_TOKENS=5000 / BUDGET_CEILING_CENTS=15 contract"
provides:
  - "email-reply-parser@2.3.5 runtime dependency (exact pin)"
  - "7 sanitized Stage 0 fixtures under web/lib/stage-0/__tests__/fixtures/"
  - "RED test suite for strip-quoted-history helper (10 cases)"
  - "RED bump of BUDGET_CEILING_TOKENS assertion 5000 -> 16000"
affects:
  - "web/package.json + web/package-lock.json (one new dep)"
  - "web/lib/stage-0/__tests__/budget-counter.test.ts (single assertion + header rebase)"
tech-stack:
  added:
    - "email-reply-parser@2.3.5 (Crisp, MIT, pure-JS, peer-dep re2 declined)"
  patterns:
    - "TDD RED-first: failing tests committed before implementation"
    - "Fixture-driven Stage 0 tests (matches regex-screen.test.ts shape)"
    - "Generator-script-then-delete for synthetic regression fixture"
key-files:
  created:
    - "web/lib/stage-0/__tests__/fixtures/outlook-nl-reply.txt"
    - "web/lib/stage-0/__tests__/fixtures/gmail-nl-reply.txt"
    - "web/lib/stage-0/__tests__/fixtures/mobile-client-reply.txt"
    - "web/lib/stage-0/__tests__/fixtures/cited-quoting.txt"
    - "web/lib/stage-0/__tests__/fixtures/signature-after-quote.txt"
    - "web/lib/stage-0/__tests__/fixtures/no-quote-history.txt"
    - "web/lib/stage-0/__tests__/fixtures/12k-token-real-sample.txt"
    - "web/lib/stage-0/__tests__/strip-quoted-history.test.ts"
  modified:
    - "web/package.json"
    - "web/package-lock.json"
    - "web/lib/stage-0/__tests__/budget-counter.test.ts"
decisions:
  - "Synthetic 12k fixture (18124 bytes, 20 reps of Outlook NL Van: block) substituted for the real 2026-05-07 14:46 stranded-email body — Supabase pull out of session scope. Wave 3 may swap with the real sample."
  - "email-reply-parser pinned EXACT 2.3.5 (no caret) per supply-chain mitigation T-999.7-04. Initial npm install wrote ^2.3.5; manually corrected and re-ran npm install to refresh lockfile."
  - "re2 peer-dep NOT installed — library auto-falls back to native RegExp; ReDoS bounded by Vercel 60s + Inngest retries:0 (per RESEARCH Pitfall T-999.7-03)."
metrics:
  duration_minutes: 3
  tasks_completed: 3
  files_created: 8
  files_modified: 3
  completed: "2026-05-08T04:56:34Z"
---

# Phase 999.7 Plan 01: RED tests + fixtures + email-reply-parser install — Summary

**One-liner:** Wave 1 RED bootstrap — install email-reply-parser@2.3.5 exact-pinned, seed 7 sanitized Stage 0 reply fixtures (incl. 18 KB synthetic 12k-token regression sample), commit failing strip-quoted-history test suite, and rebase BUDGET_CEILING_TOKENS assertion 5000 → 16000.

## Commits

| Task | Hash      | Type  | Subject                                                              |
| ---- | --------- | ----- | -------------------------------------------------------------------- |
| 1    | `8f1ba49` | chore | install email-reply-parser@2.3.5 and seed Stage 0 strip fixtures     |
| 2    | `ae0997c` | test  | add failing tests for strip-quoted-history helper (10 cases)         |
| 3    | `ec88eac` | test  | bump BUDGET_CEILING_TOKENS assertion to 16000 (RED)                  |

## RED State Confirmation

`cd web && npx vitest run lib/stage-0` final state:

- `lib/stage-0/__tests__/strip-quoted-history.test.ts` → **fails** with `Failed to resolve import "../strip-quoted-history"` (helper module ships in Plan 02 — desired RED)
- `lib/stage-0/__tests__/budget-counter.test.ts` → **5/6 pass**; the single failing assertion is `expected 5000 to be 16000` on the constant-equality test (desired RED — Plan 02 bumps the source constant)
- `lib/stage-0/__tests__/regex-screen.test.ts` → unchanged, passing
- `lib/stage-0/__tests__/llm-verdict.test.ts` → unchanged

Both failure classes match the acceptance criteria's expected error shape.

## Fixture Inventory

| #   | Filename                       | Bytes  | Notes                                                              |
| --- | ------------------------------ | ------ | ------------------------------------------------------------------ |
| 1   | outlook-nl-reply.txt           | ~410   | Outlook 365 NL `Van: ... <email>` + `Verzonden:` block             |
| 2   | gmail-nl-reply.txt             | ~290   | Gmail NL `Op <datum> om <tijd> schreef <naam>:` divider + `> ` lines |
| 3   | mobile-client-reply.txt        | ~330   | `Verzonden vanaf Outlook voor Android` mobile signature            |
| 4   | cited-quoting.txt              | ~210   | Mixed-case `> ` quoted block (Thunderbird/Apple Mail shape)        |
| 5   | signature-after-quote.txt      | ~280   | Reply → quoted → signature → another quoted (deeply threaded)      |
| 6   | no-quote-history.txt           | ~210   | First-touch — no reply markers, baseline (changed=false)           |
| 7   | 12k-token-real-sample.txt      | 18124  | Synthetic regression (20 reps Outlook NL Van: block)               |

All fixtures sanitized — placeholders only (`KLANT`, `FA-12345`, `voorbeeld.nl`, project mailboxes `smeba.nl` / `smeba-fire.be` / `fire-control.nl` per CLAUDE.md scope).

## Test Coverage (10 it() cases)

- STRIP-01 Outlook NL — drops `Van: ...` block, retains operator preamble
- STRIP-01 Gmail NL — drops `Op-schreef` divider + all `> ` lines
- STRIP-01 mobile — drops `Van:` block
- STRIP-01 `>` cited-quoting — zero remaining `> ` lines after strip
- STRIP-01 signature-after-quote — visible reply preserved
- no-quote baseline — `changed===false`, `stripped===input`, `delta_chars===0`
- STRIP-03 determinism — stripQuotedHistory(input) idempotent (replay-safe)
- STRIP-04 empty input — short-circuit, no fallback_reason
- STRIP-04 parser-returned-empty — fallback to original body, `fallback_reason: 'parser_returned_empty'`
- 12k regression — `delta_chars < -8000`, stripped non-empty

## Deviations from Plan

### Synthetic regression fixture (documented up-front)

**Found during:** Task 1
**Issue:** Plan called for the actual 2026-05-07 14:46 stranded-email body from Supabase. No DB credentials available in this session.
**Fix:** Constructed a synthetic 18,124-byte body — preamble (~10 lines of operator reply) + 20 repetitions of a sanitized 27-line Outlook NL `Van:` quoted block. Total exceeds 16 KB threshold per acceptance criteria. Built via short generator script (`_gen-12k-sample.mjs`), executed once, then deleted to keep the repo clean.
**Files modified:** `web/lib/stage-0/__tests__/fixtures/12k-token-real-sample.txt`
**Commit:** `8f1ba49`
**Wave 3 follow-up:** Replace with real stranded-email body once Supabase access lands, to validate the `delta_chars < -8000` claim against the production shape (RESEARCH Assumption A2).

### Caret-pin auto-corrected to exact pin

**Found during:** Task 1
**Issue:** `npm install email-reply-parser@2.3.5` wrote `"^2.3.5"` to package.json (npm default), violating exact-pin requirement (supply-chain mitigation T-999.7-04).
**Fix:** Edited package.json to `"2.3.5"`, re-ran `npm install` to refresh lockfile. Rule 1 auto-fix per executor protocol.
**Verified:** `grep '"email-reply-parser": "2.3.5"' package.json` → match; `node_modules/email-reply-parser/package.json` version field reads `2.3.5`.
**Commit:** `8f1ba49`

## Auth Gates

None. Pure local install + file creation.

## Known Stubs

None. The strip helper module is intentionally absent — Plan 02's deliverable. Tests fail by design with module-resolution error, which is the explicit RED contract documented in the plan and in the test file's header comment.

## Threat Flags

None — Wave 1 introduces no new network surface, no auth path, no schema change. The `email-reply-parser` dependency itself is covered by T-999.7-04 (exact-pin + lockfile commit) in the plan's threat register.

## Self-Check: PASSED

- `web/package.json` contains `"email-reply-parser": "2.3.5"` (exact, no caret) — verified by grep.
- `web/node_modules/email-reply-parser/package.json` version === 2.3.5 — verified.
- 7 fixture files exist under `web/lib/stage-0/__tests__/fixtures/` — verified `ls | wc -l` returns 7.
- `12k-token-real-sample.txt` is 18,124 bytes (> 16,000 threshold) — verified `wc -c`.
- `web/lib/stage-0/__tests__/strip-quoted-history.test.ts` exists with 10 `it(` cases referencing all 7 fixtures + the inline parser-returned-empty case — verified.
- `web/lib/stage-0/__tests__/budget-counter.test.ts` contains exactly one `expect(BUDGET_CEILING_TOKENS).toBe(16000)` and zero `toBe(5000)` — verified by grep.
- All three commits present in `git log`: `8f1ba49`, `ae0997c`, `ec88eac` — verified.
- Vitest run produces expected RED failure shapes (module-not-found + 5000-vs-16000 assertion) — verified.
