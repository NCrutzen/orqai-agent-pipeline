---
phase: 88
plan: 02
type: execute
wave: 1
depends_on: [88-01]
files_modified:
  - web/app/(dashboard)/automations/[swarm]/_shell/components/stage-2-widget.tsx
  - web/app/(dashboard)/automations/[swarm]/_shell/components/stage-3-widget.tsx
  - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx
  - web/app/(dashboard)/automations/[swarm]/_shell/__tests__/detail-pane.test.tsx
  - web/app/(dashboard)/automations/[swarm]/_shell/__tests__/stage-2-widget.test.tsx
  - web/app/(dashboard)/automations/[swarm]/_shell/__tests__/stage-3-widget.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Operator can override the customer pick on Stage 2 (S2) directly from the detail-pane, attach a note inline, and submit; the picker is the existing Stage2Widget from stage-1/components/ — not a placeholder div"
    - "Operator can override the intent pick on Stage 3 (S3) directly from the detail-pane, attach a note inline, and submit; the picker is the existing Stage3Widget from stage-1/components/"
    - "Cancel-override link on each of S0/S2/S3 (per-stage dirty branch) clears the dirty state on click — same UX as Stage 1 today"
    - "Footer secondary 'Cancel override' button still calls resetAllStageFeedback across all five stages"
    - "Hard-separation lock holds: Stage 2 widget does NOT import SwarmNoiseCategoryRow or SwarmIntentRow types (customer entity is neither); Stage 3 widget does NOT import SwarmNoiseCategoryRow"
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/_shell/components/stage-2-widget.tsx"
      provides: "Stage 1-pattern wrapper around the existing Stage2 customer picker with fused note textarea + override POST handler"
      min_lines: 200
    - path: "web/app/(dashboard)/automations/[swarm]/_shell/components/stage-3-widget.tsx"
      provides: "Stage 1-pattern wrapper around the existing Stage3 intent picker with fused note textarea + override POST handler"
      min_lines: 180
    - path: "web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx"
      provides: "Stage 2 and Stage 3 slots in stagesData (lines ~449-485) render the new widgets instead of placeholders; onChange wired to real handlers"
      contains: "Stage2Widget"
  key_links:
    - from: "_shell/detail-pane.tsx Stage 2 slot"
      to: "_shell/components/stage-2-widget.tsx"
      via: "import + JSX mount in stagesData entry"
      pattern: "from \"./components/stage-2-widget\""
    - from: "_shell/detail-pane.tsx Stage 3 slot"
      to: "_shell/components/stage-3-widget.tsx"
      via: "import + JSX mount in stagesData entry"
      pattern: "from \"./components/stage-3-widget\""
    - from: "stage-2-widget.tsx submit handler"
      to: "POST /api/automations/debtor-email/override + recordVerdict"
      via: "axis='stage_2_customer', decision_details={customer_account_id, customer_name}"
      pattern: "axis:\\s*[\"']stage_2_customer[\"']"
    - from: "stage-3-widget.tsx submit handler"
      to: "POST /api/automations/debtor-email/override + recordVerdict"
      via: "axis='stage_3_intent', decision_details={intent_key}"
      pattern: "axis:\\s*[\"']stage_3_intent[\"']"
    - from: "stage-2/3 widget keyboard listeners"
      to: "window event 'bulk-review:override-submit'"
      via: "window.addEventListener mirroring stage-1-widget.tsx:271-286"
      pattern: "bulk-review:override-submit"
---

<objective>
Wire up Stage 2 and Stage 3 override+note widgets in the unified `_shell/` detail-pane (D-01a + D-01b + D-01c). Both widgets already exist as pickers under `web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-{2,3}-widget.tsx`; this plan creates the new `_shell/components/stage-{2,3}-widget.tsx` wrappers that mirror `_shell/components/stage-1-widget.tsx` verbatim — adding the inline note textarea, EvalTypeRadio, submit POST handler, and `window` keyboard listeners — and then mounts those wrappers in `detail-pane.tsx` to replace the current placeholder div + stub `onChange`.

Purpose: Phase 82.6 left S2/S3 as placeholders despite the comment "wired in Plan 06". Operators currently cannot override customer or intent picks from the unified detail-pane on Stages 2/3 — they fall back to legacy surfaces. D-01c (cancel-override on every overrideable stage) is also currently a no-op on S2/S3 because there is no widget to clear.

Output: Two new widget files, modified `detail-pane.tsx`, three test files. All consumers of the existing helpers (`resetAllStageFeedback`, `onCancelDirty(2|3)`, `onSubmitDirty(2|3)`) start working for the first time.

Per D-01a, NO shared `StageOverrideWidget` primitive — three near-copies of the Stage 1 pattern. This is locked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/88-review-surface-cleanup/88-CONTEXT.md
@.planning/phases/88-review-surface-cleanup/88-RESEARCH.md
@CLAUDE.md
@docs/agentic-pipeline/README.md

<!-- Stage 1 canonical pattern (template for verbatim copy) -->
@web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx
<!-- Existing pickers Plan 02 wraps (NOT to be rewritten) -->
@web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-2-widget.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-3-widget.tsx
<!-- Detail-pane host -->
@web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx

<interfaces>
<!-- Stage 1 widget template — KEY contract Plan 02 widgets must mirror -->
<!-- From web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx -->
- Local dirty state at line 90: useState<{ categoryKey: string } | null>(null)
- Reset on row change at line 105-111: useEffect keyed on row.id
- notesRequired logic at line 123-130: isUnknownBucket || evalType === "regression"
- submitOverride POST at line 140-236 hits:
  - POST /api/automations/debtor-email/override (axis-specific payload)
  - recordVerdict server action
  - markPendingRemoval helper
- Window event listeners at line 271-286:
  - window.addEventListener("bulk-review:override-submit", handleSubmit)
  - window.addEventListener("bulk-review:override-discard", handleDiscard)
- Inline note Textarea at line 302-353 (the fused note+override per D-01b)

<!-- Existing Stage 2 picker (DO NOT rewrite) -->
<!-- From web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-2-widget.tsx -->
- Exports: Stage2Widget — async-search combobox for customer accounts
- Output: { customer_account_id: string, customer_name: string } via onChange
- Axis literal for override POST: "stage_2_customer"

<!-- Existing Stage 3 picker (DO NOT rewrite) -->
<!-- From web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-3-widget.tsx -->
- Exports: Stage3Widget — intent picker (consumes SwarmIntentRow[])
- Output: { intent_key: string } via onChange
- Axis literal for override POST: "stage_3_intent"

<!-- detail-pane host hooks (already exist from Phase 82.7) -->
<!-- From web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx lines 333-354 -->
- resetAllStageFeedback() — clears all stage dirty state (footer Cancel button)
- onCancelDirty(stage: 0|1|2|3): () => void — per-stage cancel link
- onSubmitDirty(stage: 0|1|2|3): () => void — dispatches window event "bulk-review:override-submit"

<!-- Hard-separation prop types on UnifiedDetailPane -->
<!-- From web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx props -->
- categories: SwarmNoiseCategoryRow[]   <-- Stage 1 ONLY consumes
- intents: SwarmIntentRow[]              <-- Stage 3 ONLY consumes
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create _shell/components/stage-2-widget.tsx (wrap + fuse note + cancel-override)</name>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx (CANONICAL TEMPLATE — read entirely)
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-2-widget.tsx (the picker to wrap)
    - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx (lines 449-460 — current Stage 2 placeholder it will replace)
    - .planning/phases/88-review-surface-cleanup/88-RESEARCH.md (Implementation Map: D-01 row for stage-2-widget.tsx)
  </read_first>
  <behavior>
    - Test 1: Renders the wrapped Stage2Widget picker (async customer combobox) when given a row with `decision_details.customer_account_id` empty.
    - Test 2: Clicking "override stage" reveals an inline `<textarea>` (fused note+override per D-01b).
    - Test 3: With dirty state present, dispatching `window.dispatchEvent(new Event("bulk-review:override-submit"))` triggers a POST to `/api/automations/debtor-email/override` with body containing `axis: "stage_2_customer"` and `decision_details: { customer_account_id, customer_name }`.
    - Test 4: Dispatching `bulk-review:override-discard` clears the dirty state (textarea hides, picker resets).
    - Test 5: When `evalType === "regression"`, submit is blocked unless note text is non-empty (notesRequired branch).
    - Test 6: Hard-sep grep — file source MUST NOT contain the literal strings `SwarmNoiseCategoryRow` or `SwarmIntentRow` (customer entity is neither).
  </behavior>
  <action>
    Create `web/app/(dashboard)/automations/[swarm]/_shell/components/stage-2-widget.tsx` by copying the structural skeleton of `_shell/components/stage-1-widget.tsx` and adapting:

    1. Replace import of `Stage1CategorySelect` with import of `Stage2Widget` from `../../stage-1/components/stage-2-widget`.
    2. Local dirty state shape: `useState<{ customer_account_id: string; customer_name: string } | null>(null)` instead of `{ categoryKey }`.
    3. `axis` literal in the POST body: `"stage_2_customer"` (not `"stage_1_category"`).
    4. `decision_details` payload: `{ customer_account_id, customer_name }` (not `{ category_key }`).
    5. `notesRequired` simplifies — Stage 2 has no "unknown bucket" equivalent. Keep only: `notesRequired = evalType === "regression"`. (Drop `isUnknownBucket`.)
    6. Window event listeners: identical to Stage 1 — `"bulk-review:override-submit"` and `"bulk-review:override-discard"`.
    7. Inline `<Textarea>` block from Stage 1 lines 302-353: copy verbatim, change `placeholder` copy to "Why is this customer wrong?" (per D-01b fused-form UX).
    8. `useEffect` reset keyed on `row.id` — copy verbatim.
    9. Submit success path: same `recordVerdict` + `markPendingRemoval` calls as Stage 1.
    10. Component signature: `export function Stage2OverrideWidget({ row, swarmId, onChange, evalType, setEvalType, notes, setNotes, dirty, setDirty }: Props)` — match Stage 1's externalised dirty/notes/evalType state ownership (the host detail-pane owns these via the hooks at lines 333-354).

    Write a co-located test file `web/app/(dashboard)/automations/[swarm]/_shell/__tests__/stage-2-widget.test.tsx` covering all six behaviour cases above. Mirror the shape of `_shell/__tests__/stage-1-widget.test.tsx` (if it exists; otherwise mirror `detail-pane.test.tsx` rendering setup).

    DO NOT modify `detail-pane.tsx` in this task — that's Task 3.
  </action>
  <verify>
    <automated>cd web && npx vitest run app/\(dashboard\)/automations/\[swarm\]/_shell/__tests__/stage-2-widget.test.tsx --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - File `web/app/(dashboard)/automations/[swarm]/_shell/components/stage-2-widget.tsx` exists.
    - `grep -E "axis:\\s*[\"']stage_2_customer[\"']" web/app/\(dashboard\)/automations/\[swarm\]/_shell/components/stage-2-widget.tsx` returns at least one hit.
    - `grep -E "bulk-review:override-submit" web/app/\(dashboard\)/automations/\[swarm\]/_shell/components/stage-2-widget.tsx` returns at least one hit.
    - `grep -E "SwarmNoiseCategoryRow|SwarmIntentRow" web/app/\(dashboard\)/automations/\[swarm\]/_shell/components/stage-2-widget.tsx` returns ZERO hits (hard-sep gate).
    - Vitest file `_shell/__tests__/stage-2-widget.test.tsx` runs green and contains the six numbered `it(` cases above.
  </acceptance_criteria>
  <done>Stage 2 widget is a drop-in replacement for the placeholder div with fused note+override + window-event submit identical to Stage 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create _shell/components/stage-3-widget.tsx (wrap + fuse note + cancel-override)</name>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-3-widget.tsx (the picker to wrap)
    - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx (lines 461-485 — current Stage 3 placeholder + stub onChange at line 467)
    - .planning/phases/88-review-surface-cleanup/88-RESEARCH.md (Implementation Map: D-01 row for stage-3-widget.tsx, plus Assumption A1)
    - docs/agentic-pipeline/README.md (hard-separation rule: Stage 3 ↔ swarm_intents)
  </read_first>
  <behavior>
    - Test 1: Renders the wrapped Stage3Widget intent picker; consumes `intents: SwarmIntentRow[]` prop forwarded from detail-pane.
    - Test 2: Clicking "override stage" reveals inline note textarea (D-01b).
    - Test 3: With dirty state, `bulk-review:override-submit` event fires POST with `axis: "stage_3_intent"` and `decision_details: { intent_key }`.
    - Test 4: `bulk-review:override-discard` clears dirty.
    - Test 5: Regression branch requires note.
    - Test 6: Hard-sep grep — file source MUST NOT contain `SwarmNoiseCategoryRow` (Stage 3 is intent-only).
  </behavior>
  <action>
    Create `web/app/(dashboard)/automations/[swarm]/_shell/components/stage-3-widget.tsx` by copying the Stage 1 widget skeleton and adapting:

    1. Import `Stage3Widget` from `../../stage-1/components/stage-3-widget`.
    2. Add prop `intents: SwarmIntentRow[]` (forwarded from detail-pane). Import the type from the existing intent-row module (locate via grep on Stage 1's chip-strip or stage-3 page imports; do NOT redefine).
    3. Local dirty shape: `useState<{ intent_key: string } | null>(null)`.
    4. POST axis literal: `"stage_3_intent"`.
    5. `decision_details` payload: `{ intent_key }`.
    6. `notesRequired = evalType === "regression"`.
    7. Window event listeners + dirty reset on row.id: copy verbatim from Stage 1.
    8. Inline Textarea: same fused-form pattern, placeholder "Why is this intent wrong?".
    9. Component signature: `export function Stage3OverrideWidget({ row, swarmId, intents, onChange, evalType, setEvalType, notes, setNotes, dirty, setDirty }: Props)`.

    Co-located test file `web/app/(dashboard)/automations/[swarm]/_shell/__tests__/stage-3-widget.test.tsx` with six numbered behaviour cases.

    DO NOT modify `detail-pane.tsx` — Task 3.
  </action>
  <verify>
    <automated>cd web && npx vitest run app/\(dashboard\)/automations/\[swarm\]/_shell/__tests__/stage-3-widget.test.tsx --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - File `web/app/(dashboard)/automations/[swarm]/_shell/components/stage-3-widget.tsx` exists.
    - `grep -E "axis:\\s*[\"']stage_3_intent[\"']" web/app/\(dashboard\)/automations/\[swarm\]/_shell/components/stage-3-widget.tsx` returns ≥1 hit.
    - `grep -E "intents:\\s*SwarmIntentRow\\[\\]" web/app/\(dashboard\)/automations/\[swarm\]/_shell/components/stage-3-widget.tsx` returns ≥1 hit.
    - `grep -E "SwarmNoiseCategoryRow" web/app/\(dashboard\)/automations/\[swarm\]/_shell/components/stage-3-widget.tsx` returns ZERO hits.
    - Vitest test file green with six `it(` cases.
  </acceptance_criteria>
  <done>Stage 3 widget is a drop-in replacement for the placeholder + stub onChange; hard-sep lock preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Stage 2 + Stage 3 widgets into _shell/detail-pane.tsx; extend detail-pane.test.tsx</name>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx (full file — but especially lines 333-354 hooks, 407-511 stagesData, 547-616 handlePrimary, 745-811 footer)
    - web/app/(dashboard)/automations/[swarm]/_shell/__tests__/detail-pane.test.tsx (existing test scaffold)
    - web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx (how Stage 1 is mounted today in stagesData[1])
  </read_first>
  <behavior>
    - Test 1: Stage 2 row in detail-pane renders `Stage2OverrideWidget` (asserted by test-id `data-testid="stage-2-override-widget"`), NOT the literal string "Stage 2 customer override — wired in Plan 06.".
    - Test 2: Stage 3 row renders `Stage3OverrideWidget`, NOT a placeholder.
    - Test 3: Stage 3 widget receives `intents` prop equal to whatever was passed to `UnifiedDetailPane`'s `intents` prop (hard-sep prop forwarding).
    - Test 4: `onCancelDirty(2)` invocation clears Stage 2 dirty state (verified by re-rendering and asserting textarea hidden).
    - Test 5: `onCancelDirty(3)` clears Stage 3 dirty state.
    - Test 6: Footer "Cancel override" button calls `resetAllStageFeedback` which clears S0+S1+S2+S3 dirty in one shot (extend existing test).
  </behavior>
  <action>
    Modify `web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx`:

    1. Add imports near top:
       ```ts
       import { Stage2OverrideWidget } from "./components/stage-2-widget";
       import { Stage3OverrideWidget } from "./components/stage-3-widget";
       ```
    2. Locate the `stagesData` array entry for Stage 2 (currently at lines 449-460 per RESEARCH.md). Replace the placeholder `<div>Stage 2 customer override — wired in Plan 06.</div>` JSX with:
       ```tsx
       <Stage2OverrideWidget
         row={row}
         swarmId={swarmId}
         onChange={/* same handler shape Stage 1 uses */}
         evalType={evalTypeStage2}
         setEvalType={setEvalTypeStage2}
         notes={notesStage2}
         setNotes={setNotesStage2}
         dirty={dirtyStage2}
         setDirty={setDirtyStage2}
         data-testid="stage-2-override-widget"
       />
       ```
       Wire the dirty/notes/evalType state per the existing pattern used for Stage 1 (likely additional `useState` declarations near lines 262-354 — mirror Stage 1's naming convention).
    3. Locate the Stage 3 slot (lines 461-485, with stub `onChange: () => { /* Plan 06 */ }` at line 467). Replace with `<Stage3OverrideWidget>` mount, forwarding the `intents` prop from `UnifiedDetailPane`'s props.
    4. Confirm `onCancelDirty(2)` and `onCancelDirty(3)` already exist at lines 337-342 (per RESEARCH.md). If they only branch on stages 0/1, extend the switch/dispatch to cover 2 and 3 (clear `setDirtyStage2(null)` / `setDirtyStage3(null)`).
    5. Confirm `resetAllStageFeedback` clears Stage 2 + Stage 3 dirty state — extend if needed.
    6. Confirm `onSubmitDirty(2)` and `onSubmitDirty(3)` dispatch `bulk-review:override-submit` — extend `switch` if it only covers stages 0/1.

    Extend `_shell/__tests__/detail-pane.test.tsx` with the six numbered behaviour assertions above. Existing assertions on Stage 1 must continue to pass.

    Hard-sep self-check at end: `grep -E "categories.*Stage3OverrideWidget|intents.*Stage1Widget|intents.*Stage2Override" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` MUST return ZERO hits (no cross-prop pollution).
  </action>
  <verify>
    <automated>cd web && npx vitest run app/\(dashboard\)/automations/\[swarm\]/_shell/__tests__/detail-pane.test.tsx --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Stage2OverrideWidget" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` returns ≥2 hits (import + mount).
    - `grep "Stage3OverrideWidget" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` returns ≥2 hits.
    - `grep "Stage 2 customer override — wired in Plan 06" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` returns ZERO hits (placeholder removed).
    - `grep "/\\* Plan 06 \\*/" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` returns ZERO hits (stub removed).
    - `grep -E "categories.*Stage3OverrideWidget|intents.*Stage2OverrideWidget" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` returns ZERO hits (hard-sep).
    - `detail-pane.test.tsx` vitest run is green with the six new assertions.
    - Full `_shell` test pass: `cd web && npx vitest run app/\(dashboard\)/automations/\[swarm\]/_shell/__tests__ --no-coverage` green.
  </acceptance_criteria>
  <done>Detail-pane mounts real S2/S3 widgets; cancel-override link + footer Cancel button + window-event Submit all work for S0/S1/S2/S3 parity per D-01c; hard-separation lock holds.</done>
</task>

</tasks>

<verification>
- All three task vitest commands green individually.
- Aggregate: `cd web && npx vitest run app/\(dashboard\)/automations/\[swarm\]/_shell/__tests__ --no-coverage` green.
- Hard-separation grep gates (run from repo root):
  - `grep -E "SwarmNoiseCategoryRow|SwarmIntentRow" web/app/\(dashboard\)/automations/\[swarm\]/_shell/components/stage-2-widget.tsx` → ZERO hits
  - `grep -E "SwarmNoiseCategoryRow" web/app/\(dashboard\)/automations/\[swarm\]/_shell/components/stage-3-widget.tsx` → ZERO hits
- Placeholder removal: `grep -E "(Stage 2 customer override — wired in Plan 06|/\\* Plan 06 \\*/)" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` → ZERO hits.
- TypeScript clean: `cd web && npx tsc --noEmit` exit 0 (no new errors introduced).
</verification>

<success_criteria>
- D-01a: Three near-copies of Stage 1 pattern — S0 (already existed) + S2 (new) + S3 (new). No shared `StageOverrideWidget` primitive added.
- D-01b: Note textarea fused inline in both new widgets — clicking "override stage" reveals it alongside the picker.
- D-01c: Cancel-override link and footer button operate on S0/S2/S3 dirty state, matching Stage 1.
- Hard-separation lock holds at file-import level (grep-verified).
</success_criteria>

<output>
After completion, create `.planning/phases/88-review-surface-cleanup/88-02-SUMMARY.md` listing the three new/modified files, the six behaviour cases per widget covered by tests, and the hard-sep grep results.
</output>
