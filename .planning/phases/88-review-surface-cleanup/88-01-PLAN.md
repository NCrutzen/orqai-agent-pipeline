---
phase: 88
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/88-review-surface-cleanup/88-WAVE0-FINDINGS.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "automation_runs.email_id column shape is documented (top-level uuid OR nested in result->>'email_id') before any RPC SQL is written"
    - "Every ?needs_action=1 deeplink / saved-link reference in the repo is enumerated so D-02 deletion is safe"
    - "Detail-pane width per stage at fixed viewport is captured; whether a width regression exists is conclusively answered yes/no with the offending stage named"
  artifacts:
    - path: ".planning/phases/88-review-surface-cleanup/88-WAVE0-FINDINGS.md"
      provides: "Three Wave-0 questions answered with grep output + screenshot evidence; gates Plans 03 and 04"
  key_links:
    - from: "88-WAVE0-FINDINGS.md > Q1 answer"
      to: "Plan 03 (D-02) RPC SQL"
      via: "RPC JOIN shape locked by Wave 0 answer"
      pattern: "automation_runs\\.email_id|result->>'email_id'"
    - from: "88-WAVE0-FINDINGS.md > Q3 answer"
      to: "Plan 04 (D-03) width-fix task"
      via: "Width-regression task scoped iff Q3 confirms a regression"
      pattern: "position: sticky|minHeight|width regression"
---

<objective>
Wave 0 pre-flight that gates Plans 03 (D-02 RPC) and 04 (D-03 width fix). Three independent verification tasks: (1) confirm `automation_runs.email_id` column shape via live Supabase query so the verdict-pending RPC JOIN is locked correctly; (2) grep the repo for `?needs_action=1` / `needs_action=` deeplinks so D-02 deletion does not silently break a saved-link surface; (3) reproduce the detail-pane width-regression claim in a dev browser at fixed viewport across Stages 0/1/2/3/4 and either pinpoint the offending stage + CSS or conclusively close the question.

Purpose: RESEARCH.md flagged three LOW/MEDIUM-confidence unknowns (A2, A4, Q3). Locking these before D-02/D-03 implementation prevents thrash. VALIDATION.md Wave 0 Requirements section lists exactly these three items.

Output: `88-WAVE0-FINDINGS.md` with three sections (Q1/Q2/Q3 answers), grep output verbatim, browser screenshots referenced by path, and explicit "scope this in Plan 04" / "drop from Plan 04" calls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/88-review-surface-cleanup/88-CONTEXT.md
@.planning/phases/88-review-surface-cleanup/88-RESEARCH.md
@.planning/phases/88-review-surface-cleanup/88-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify automation_runs.email_id column shape via live Supabase</name>
  <read_first>
    - .planning/phases/88-review-surface-cleanup/88-RESEARCH.md (Q1 section, Assumption A2)
    - supabase/migrations/20260513c_email_feedback.sql (canonical email_feedback table)
    - supabase/migrations/20260428_classifier_queue_counts.sql (current RPC for reference)
  </read_first>
  <action>
    Use the Supabase MCP `list_tables` tool (schema=`public`) to retrieve the `automation_runs` table column list. Locate `email_id` (if present at top level) and capture its data type. If `email_id` is NOT a top-level column, run an `execute_sql` query `SELECT result->>'email_id' AS email_id FROM public.automation_runs WHERE status='predicted' LIMIT 1` and confirm whether `result` JSONB contains an `email_id` key.

    Write findings to `.planning/phases/88-review-surface-cleanup/88-WAVE0-FINDINGS.md` under `## Q1 — automation_runs.email_id shape` with exactly two subsections:
    - **Schema evidence** — paste the column row(s) from `list_tables` output (or the SQL+result if JSONB).
    - **RPC JOIN locked** — write the literal SQL fragment Plan 03 must use, one of:
      - `WHERE ef.email_id = ar.email_id` (if uuid top-level)
      - `WHERE ef.email_id = (ar.result->>'email_id')::uuid` (if nested)

    Per CLAUDE.md, use Supabase MCP not the anon REST key for schema introspection.
  </action>
  <verify>
    <automated>test -f .planning/phases/88-review-surface-cleanup/88-WAVE0-FINDINGS.md && grep -q "## Q1 — automation_runs.email_id shape" .planning/phases/88-review-surface-cleanup/88-WAVE0-FINDINGS.md && grep -qE "(WHERE ef\\.email_id = ar\\.email_id|result->>'email_id')" .planning/phases/88-review-surface-cleanup/88-WAVE0-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/88-review-surface-cleanup/88-WAVE0-FINDINGS.md` exists.
    - Contains a `## Q1 — automation_runs.email_id shape` heading.
    - Contains one of the two locked JOIN fragments above (grep-verifiable).
    - Contains Schema evidence subsection with column type or JSONB query output (not paraphrased).
  </acceptance_criteria>
  <done>Plan 03 author can copy the JOIN fragment verbatim into the new RPC SQL without re-querying Supabase.</done>
</task>

<task type="auto">
  <name>Task 2: Grep audit ?needs_action=1 and needs_action= deeplinks</name>
  <read_first>
    - .planning/phases/88-review-surface-cleanup/88-RESEARCH.md (Risks #1)
    - web/app/(dashboard)/automations/[swarm]/_shell/stage-list-chips.tsx (current toggle definition)
    - web/app/(dashboard)/automations/[swarm]/_shell/_lib/feedback-list-loader.ts (server-side `needsActionOnly` filter — DO NOT confuse with URL param)
  </read_first>
  <action>
    Run these greps from repo root and capture verbatim into `88-WAVE0-FINDINGS.md` under `## Q2 — ?needs_action deeplink audit`:

    1. `grep -rn "needs_action=1" web/ docs/ supabase/ scripts/ .planning/ 2>/dev/null`
    2. `grep -rn "needs_action=" web/ docs/ supabase/ scripts/ .planning/ 2>/dev/null | grep -v node_modules`
    3. `grep -rn "needsAction" web/app web/lib 2>/dev/null`

    Then classify each hit as one of:
    - **URL param producer** (writes `?needs_action=1` to a `URLSearchParams` / `qs.set` / `Link href`) — DELETE in Plan 03
    - **URL param consumer** (reads `searchParams.needs_action` / `sp.needs_action`) — DELETE in Plan 03
    - **Server-side `needsActionOnly` loader filter** (unrelated to URL) — KEEP per RESEARCH.md Risks #2
    - **Doc / planning comment** — review, likely KEEP

    Conclude with a `### Deletion targets for Plan 03 (D-02)` bullet list of file:line pairs to delete and a `### Keep` list to forbid deletion.
  </action>
  <verify>
    <automated>grep -q "## Q2 — ?needs_action deeplink audit" .planning/phases/88-review-surface-cleanup/88-WAVE0-FINDINGS.md && grep -q "### Deletion targets for Plan 03" .planning/phases/88-review-surface-cleanup/88-WAVE0-FINDINGS.md && grep -q "### Keep" .planning/phases/88-review-surface-cleanup/88-WAVE0-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - Q2 section present with all three grep outputs verbatim.
    - Deletion targets and Keep lists are file:line specific (not paraphrased).
    - `feedback-list-loader.ts` references appear in the Keep list with rationale citing RESEARCH.md Risks #2.
    - `stage-{0,1,2,3}/page.tsx` `needs_action` param producers + consumers appear in Deletion targets.
  </acceptance_criteria>
  <done>Plan 03 author has a file:line checklist for deletion with no ambiguity about server-side filter survival.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: In-browser repro of detail-pane width across Stages 0/1/2/3/4</name>
  <read_first>
    - .planning/phases/88-review-surface-cleanup/88-RESEARCH.md (Q3 section, full)
    - web/app/(dashboard)/automations/[swarm]/stage-4/client-shell.tsx (lines 435 — position:sticky wrapper)
    - web/app/(dashboard)/automations/[swarm]/stage-0/page.tsx, stage-1/client-shell.tsx, stage-2/page.tsx, stage-3/client-shell.tsx (all show `minmax(640px, 1fr) 540px`)
  </read_first>
  <what-built>
    Wave 0 has produced Q1 (RPC JOIN) and Q2 (deeplink list) answers in `88-WAVE0-FINDINGS.md`. Q3 (width regression) cannot be answered from code alone per RESEARCH.md (LOW confidence). This checkpoint asks the operator to perform the in-browser reproduction so Plan 04 either scopes a width-fix task or drops D-03c entirely.
  </what-built>
  <how-to-verify>
    1. Run `cd web && npm run dev` and open the local dev server (or use the latest Vercel preview deploy on `agent-workforce` Vercel project).
    2. Resize browser to exactly **1440×900** (use DevTools Device Mode → Responsive → 1440 × 900).
    3. For each stage in order — Stage 0, Stage 1, Stage 2, Stage 3, Stage 4 — open a swarm view that has at least one row selected (e.g. debtor-email), click any row to open the detail-pane.
    4. With DevTools Inspector, hover the right-column detail-pane container (the element that wraps `UnifiedDetailPane`) and record its computed `width` in pixels.
    5. Take a screenshot per stage at 1440×900 and save to `.planning/phases/88-review-surface-cleanup/screenshots/stage-{N}-detail-pane.png` (create the `screenshots/` dir if absent).
    6. Append findings to `88-WAVE0-FINDINGS.md` under `## Q3 — detail-pane width per stage`:
       - Table with columns `Stage | Pane width (px) | Screenshot path | Notes`.
       - Conclusion: one of
         - **No regression** — "all stages 540px; drop D-03c from Plan 04; document operator perception likely vertical-layout caused by Stage 4 position:sticky wrapper at stage-4/client-shell.tsx:435"
         - **Stage X regressed to Ypx** — name the stage, paste the offending CSS rule (DevTools Computed → Show All), propose the one-line fix
    7. Type `approved` (with the file written) OR describe what you found instead.
  </how-to-verify>
  <resume-signal>Type "approved" once `88-WAVE0-FINDINGS.md` has Q3 section + screenshots/ subdir populated, OR describe blockers.</resume-signal>
</task>

</tasks>

<verification>
- `88-WAVE0-FINDINGS.md` exists with three top-level sections: Q1, Q2, Q3.
- Q1 contains the locked RPC JOIN fragment (grep-verifiable per Task 1).
- Q2 contains deletion-target and keep lists (grep-verifiable per Task 2).
- Q3 contains per-stage width table + conclusion (manual UAT per Task 3).
- `screenshots/` subdir exists with at least 5 PNG files (one per stage) iff Task 3 produced visual evidence.

Command: `ls .planning/phases/88-review-surface-cleanup/screenshots/*.png 2>/dev/null | wc -l` returns ≥1 (5 expected).
</verification>

<success_criteria>
Wave 0 gate closed: Plans 03 and 04 can proceed with locked JOIN shape, explicit file:line deletion list, and a yes/no answer on the width regression with concrete fix scope (or explicit drop). No remaining `[VERIFY]` annotations in Plan 03/04.
</success_criteria>

<output>
After completion, create `.planning/phases/88-review-surface-cleanup/88-01-SUMMARY.md` referencing the three findings sections and any screenshots produced.
</output>
