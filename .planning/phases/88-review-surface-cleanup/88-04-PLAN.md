---
phase: 88
plan: 04
type: execute
wave: 2
depends_on: [88-01]
files_modified:
  - web/app/(dashboard)/automations/[swarm]/stage-4/client-shell.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-4/page.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-4/__tests__/page.test.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "Stage 4 renders an outcome-state chip strip (chips: All / Handler error / Needs review / Auto-archived) instead of three Collapsible sections — chips group by outcome state, NOT by handler name (D-03a locked constraint)"
    - "Active chip is driven by `?outcome=` URL param; clicking a chip writes the param via router.replace; clicking 'All' deletes the param"
    - "selectedId cross-section model survives — selecting a row in any chip's filtered view keeps the detail-pane synced; switching chips with an out-of-bucket selectedId clears appropriately (existing logic at stage-4/client-shell.tsx:188-198 preserved)"
    - "Hard-separation lock holds: Stage 4 detail-pane still receives `intents={[]}` (no swarm_intents reference introduced)"
    - "Detail-pane width regression (per Wave 0 Plan 01 Q3): either resolved by removing the position:sticky+minHeight wrapper at stage-4/client-shell.tsx:435 (one-line revert) OR documented as 'no regression' in summary (drop the fix task)"
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/stage-4/client-shell.tsx"
      provides: "Single ChipStrip + single filtered RowList; Collapsible imports removed"
      contains: "ChipStrip"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-4/page.tsx"
      provides: "?outcome= URL param parsing; pass active outcome to client-shell"
      contains: "outcome"
  key_links:
    - from: "stage-4/page.tsx"
      to: "stage-4/client-shell.tsx"
      via: "activeOutcome prop derived from searchParams.outcome"
      pattern: "activeOutcome"
    - from: "stage-4/client-shell.tsx ChipStrip mount"
      to: "_shell/chip-strip.tsx primitive"
      via: "import ChipStrip from _shell/chip-strip"
      pattern: "from \"../_shell/chip-strip\""
    - from: "stage-4/client-shell.tsx row source"
      to: "filtered union of handlerErrorRows | needsReviewRows | autoArchivedUnified"
      via: "switch on activeOutcome at render time"
      pattern: "activeOutcome"
---

<objective>
Implement D-03: Replace Stage 4's three Collapsible sections with an outcome-state chip strip mirroring the Stage 1/2/3 pattern, and apply the detail-pane width fix iff Wave 0 Plan 01 Task 3 confirmed a regression.

Three changes:
1. **Chip-strip swap**: `stage-4/client-shell.tsx` lines 339-460 — replace the three `<Collapsible>` blocks with a single `<ChipStrip>` from `_shell/chip-strip.tsx` plus a single filtered `<RowList>`. Chip set is bounded by code (per RESEARCH.md Q2): `All / Handler error / Needs review / Auto-archived`. Drop unused Collapsible imports.
2. **URL contract**: `stage-4/page.tsx` parses `?outcome=handler_error|needs_review|auto_archived|all` (default `all`) and forwards the active value as a prop to `client-shell.tsx`.
3. **selectedId preservation**: the existing cross-section selection logic (lines 188-198) must continue to resolve a single id across the unified row union. Chip filter narrows what's *visible*; selection is unchanged.
4. **(Conditional) Width regression fix**: per Wave 0 Plan 01's Q3 conclusion in `88-WAVE0-FINDINGS.md` — if "Stage 4 narrowness from sticky wrapper" was confirmed, remove the `position: sticky` + `minHeight: 320` div wrapper at stage-4/client-shell.tsx:435 (one-line revert; pane becomes a direct grid child like every other stage). If "no regression" was the conclusion, this task drops to a no-op and is documented in the SUMMARY.

Per D-03a (locked), chips group by OUTCOME STATE, NOT handler name. Per RESEARCH.md Q2 the outcome-state set is bounded by what `stage-4/page.tsx:113-135` already filters on — 3 buckets + "All".

Per D-03c, the width regression task is gated on Wave 0 findings (Plan 01 Task 3 produced `88-WAVE0-FINDINGS.md` Q3). This plan reads that file and executes the corresponding branch.

DEPENDS ON Plan 01 (Wave 0): the width fix decision.

PARALLEL TO Plan 02 + Plan 03: no file overlap (this plan touches only `stage-4/`).

Output: Chip-strip-based Stage 4 with URL-driven filter, selectedId model preserved, and either a width fix or documented no-op.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/88-review-surface-cleanup/88-CONTEXT.md
@.planning/phases/88-review-surface-cleanup/88-RESEARCH.md
@.planning/phases/88-review-surface-cleanup/88-WAVE0-FINDINGS.md
@CLAUDE.md

<!-- Files to modify -->
@web/app/(dashboard)/automations/[swarm]/stage-4/client-shell.tsx
@web/app/(dashboard)/automations/[swarm]/stage-4/page.tsx
@web/app/(dashboard)/automations/[swarm]/stage-4/__tests__/page.test.tsx
<!-- Primitive to reuse -->
@web/app/(dashboard)/automations/[swarm]/_shell/chip-strip.tsx
<!-- Reference: how Stage 1 wires its chip strip + URL contract -->
@web/app/(dashboard)/automations/[swarm]/stage-1/noise-category-chip-strip.tsx

<interfaces>
<!-- _shell/chip-strip.tsx primitive contract (from RESEARCH.md lines 145-156) -->
- ChipStrip component (lines 34-55): mount with `chips=[{key, label, count, brandToken?}]`
- ChipStripChip type with optional brandToken: --v7-red, --v7-amber, --v7-lime
- Active-key matching via prop; chip click → callback writes URL param

<!-- Stage 4 outcome state set (bounded by stage-4/page.tsx:113-135) -->
- handlerErrorRows: KanbanRow[] where result.kanban_reason === 'handler_error'
- needsReviewRows: KanbanRow[] where result.kanban_reason === 'handler_needs_review'
- autoArchivedUnified: UnifiedRow[] from loadAutoArchivedNoiseRows + autoArchivedToUnifiedRow

<!-- selectedId model to preserve (lines 188-198) -->
- selectedEmailId searches handler-error → needs-review → auto-archived in order
- Uses KanbanRow.id for first two, pipeline_events.id for auto-archived
- screenshotPathsByRowId remap (lines 208-233) spans all three

<!-- Hard-separation lock for Stage 4 (lines 9-15) -->
- UnifiedDetailPane mount on Stage 4 passes intents={[]} (Stage 4 is noise/handler space, not intent space)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read Wave 0 width-regression finding and stub the conditional fix path</name>
  <read_first>
    - .planning/phases/88-review-surface-cleanup/88-WAVE0-FINDINGS.md (Q3 section — full)
    - web/app/(dashboard)/automations/[swarm]/stage-4/client-shell.tsx (lines 430-460 — the position:sticky wrapper at line 435)
  </read_first>
  <action>
    Read the Q3 section of `88-WAVE0-FINDINGS.md`. The section has one of three conclusions:

    **Branch A — "No regression":** Stage 4 pane width is 540px; no CSS change needed. Note in the eventual SUMMARY: "D-03c dropped — Wave 0 Q3 confirmed no width regression." No code change in this task.

    **Branch B — "Stage 4 sticky wrapper":** Remove the `position: sticky` + `minHeight: 320` div wrapper at `stage-4/client-shell.tsx:435`. The pane becomes a direct grid child. Specifically: find the `<div style={{ position: "sticky", top: "var(--space-3)", minHeight: 320 }}>` (or close variant) wrapping `<UnifiedDetailPane>`, delete the wrapper but KEEP `<UnifiedDetailPane>` as a direct child of the grid container.

    **Branch C — "Different stage regressed":** Read the named stage from Q3 conclusion. This branch is out of Plan 04's scope (no longer Stage 4) — STOP and re-plan via the checkpoint in Task 4.

    Execute the chosen branch's diff at this step (do NOT touch chip-strip work yet — Tasks 2/3 handle that). If Branch B, the file edit is a single deletion of the wrapper open/close `<div>` tags + the wrapping `style` prop.

    Document the chosen branch in a comment at the top of the Task 3 diff for traceability:
    ```tsx
    // Phase 88 Plan 04 Task 1: Width fix branch = {A|B|C}.
    // See .planning/phases/88-review-surface-cleanup/88-WAVE0-FINDINGS.md Q3.
    ```
  </action>
  <verify>
    <automated>grep -q "## Q3" .planning/phases/88-review-surface-cleanup/88-WAVE0-FINDINGS.md && (grep -qE "No regression|sticky wrapper|stage [0-9]+ regressed" .planning/phases/88-review-surface-cleanup/88-WAVE0-FINDINGS.md)</automated>
  </verify>
  <acceptance_criteria>
    - Q3 section read and branch identified (recorded in Task 4 summary checkpoint).
    - If Branch B: `grep -E "position:\\s*[\"']sticky[\"'].*minHeight" web/app/\(dashboard\)/automations/\[swarm\]/stage-4/client-shell.tsx` returns ZERO hits after the edit.
    - If Branch A: file unchanged at this task.
    - If Branch C: Task 4 checkpoint blocks proceeding.
  </acceptance_criteria>
  <done>Width fix decision is locked and applied (or skipped) per Wave 0; chip-strip work in Tasks 2/3 can proceed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ?outcome= URL parsing in stage-4/page.tsx</name>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-4/page.tsx (lines 113-135 for row derivation; searchParams shape)
    - web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx (pattern for URL searchParams typing)
    - web/app/(dashboard)/automations/[swarm]/stage-4/__tests__/page.test.tsx
  </read_first>
  <behavior>
    - Test 1: When `?outcome=handler_error`, `activeOutcome` prop forwarded to client-shell equals `"handler_error"`.
    - Test 2: When `?outcome=needs_review`, equals `"needs_review"`.
    - Test 3: When `?outcome=auto_archived`, equals `"auto_archived"`.
    - Test 4: When `?outcome=all` or param missing, equals `"all"`.
    - Test 5: Unknown values (e.g. `?outcome=foo`) coerce to `"all"`.
  </behavior>
  <action>
    Modify `web/app/(dashboard)/automations/[swarm]/stage-4/page.tsx`:

    1. Add `outcome?: string` to the `searchParams` type union.
    2. Add a parser near the top of the loader:
       ```ts
       type Outcome = "all" | "handler_error" | "needs_review" | "auto_archived";
       const allowed: ReadonlySet<Outcome> = new Set(["all", "handler_error", "needs_review", "auto_archived"]);
       const rawOutcome = String(sp.outcome ?? "all");
       const activeOutcome: Outcome = (allowed as Set<string>).has(rawOutcome) ? (rawOutcome as Outcome) : "all";
       ```
    3. Forward to `<ClientShell>` as `activeOutcome={activeOutcome}`.
    4. The existing derivation `handlerErrorRows / needsReviewRows / autoArchivedUnified` (lines 113-135) is UNCHANGED — those props all still flow to client-shell.

    Extend `stage-4/__tests__/page.test.tsx` with the five behaviour cases above. Mock the loaders such that all three row sources return non-empty arrays, then exercise the parser branches via mocked `searchParams`.
  </action>
  <verify>
    <automated>cd web && npx vitest run app/\(dashboard\)/automations/\[swarm\]/stage-4/__tests__/page.test.tsx --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "activeOutcome" web/app/\(dashboard\)/automations/\[swarm\]/stage-4/page.tsx` returns ≥3 (type, parser, prop).
    - `grep "Outcome" web/app/\(dashboard\)/automations/\[swarm\]/stage-4/page.tsx` returns ≥2 hits (type alias + cast).
    - vitest cases for all five outcome branches pass.
    - `cd web && npx tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>Stage 4 page loader exposes a typed `activeOutcome` prop to client-shell; URL contract `?outcome=` is parsed and coerced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace Collapsibles with ChipStrip in stage-4/client-shell.tsx</name>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-4/client-shell.tsx (full — focus lines 47-51 imports, 165-198 selectedId logic, 339-460 the Collapsible block)
    - web/app/(dashboard)/automations/[swarm]/_shell/chip-strip.tsx (full — primitive)
    - web/app/(dashboard)/automations/[swarm]/stage-1/noise-category-chip-strip.tsx (lines 67-78 — navigate callback pattern)
    - .planning/phases/88-review-surface-cleanup/88-RESEARCH.md (Q2 — chip set table)
    - web/app/(dashboard)/automations/[swarm]/stage-4/__tests__/page.test.tsx
  </read_first>
  <behavior>
    - Test 1: With `activeOutcome="all"`, the union of handler-error + needs-review + auto-archived rows renders (count badge per chip = each subset's length; "All" badge = sum).
    - Test 2: With `activeOutcome="handler_error"`, only handler-error rows visible; the chip with key `handler_error` has visual `aria-selected="true"` (or equivalent active marker the ChipStrip primitive uses).
    - Test 3: Clicking the "Needs review" chip dispatches `router.replace` with `?outcome=needs_review` appended (test mock `router.replace`).
    - Test 4: Clicking the "All" chip dispatches `router.replace` with `outcome` removed.
    - Test 5: selectedId model: with `activeOutcome="handler_error"` and a previously selected pipeline_events.id from auto-archived bucket, the detail-pane either clears (selectedId not in visible set) or remains visible if the model decides — assert whichever the preserved logic at lines 188-198 produces (read it, write a matching test). Behaviour must be the same as before — no regression.
    - Test 6: Hard-sep — `grep "swarm_intents\\|SwarmIntentRow" web/app/(dashboard)/automations/[swarm]/stage-4/client-shell.tsx` returns ZERO hits.
    - Test 7: Collapsible imports removed — `grep "Collapsible" web/app/(dashboard)/automations/[swarm]/stage-4/client-shell.tsx` returns ZERO hits.
  </behavior>
  <action>
    Modify `web/app/(dashboard)/automations/[swarm]/stage-4/client-shell.tsx`:

    1. Add prop to `ClientShell` signature: `activeOutcome: "all" | "handler_error" | "needs_review" | "auto_archived"`.
    2. Remove `Collapsible`, `CollapsibleTrigger`, `CollapsibleContent` imports (currently lines 47-51 per RESEARCH.md).
    3. Add `import { ChipStrip } from "../_shell/chip-strip"` and `import { useRouter, useSearchParams } from "next/navigation"`.
    4. Above the row-list block (replacing the three `<Collapsible>` blocks at lines 339-460), insert:
       ```tsx
       const chips = [
         { key: "all", label: "All", count: handlerErrorRows.length + needsReviewRows.length + autoArchivedUnified.length },
         { key: "handler_error", label: "Handler error", count: handlerErrorRows.length, brandToken: "--v7-red" },
         { key: "needs_review", label: "Needs review", count: needsReviewRows.length, brandToken: "--v7-amber" },
         { key: "auto_archived", label: "Auto-archived", count: autoArchivedUnified.length, brandToken: "--v7-lime" },
       ] as const;
       const router = useRouter();
       const searchParams = useSearchParams();
       const onChipClick = (key: string) => {
         const params = new URLSearchParams(searchParams?.toString() ?? "");
         if (key === "all") params.delete("outcome");
         else params.set("outcome", key);
         router.replace(`?${params.toString()}`);
       };
       const visibleRows =
         activeOutcome === "handler_error" ? handlerErrorRows :
         activeOutcome === "needs_review" ? needsReviewRows :
         activeOutcome === "auto_archived" ? autoArchivedUnified :
         [...handlerErrorRows, ...needsReviewRows, ...autoArchivedUnified];
       ```
    5. JSX (replacing the three Collapsible blocks):
       ```tsx
       <ChipStrip chips={chips} activeKey={activeOutcome} onSelect={onChipClick} />
       <RowList
         rows={visibleRows}
         selectedId={selectedEmailId}
         onSelect={…/* existing handler */}
         /* keep existing rightEdgeSlot / screenshotPathsByRowId etc */
       />
       ```
    6. PRESERVE selectedId resolution logic at lines 188-198 — it already spans all three row sources via union search. No change to its body. The narrowing of `visibleRows` is purely presentational; selectedId may point to a row that isn't visible, and the existing logic decides whether to clear/show.
    7. PRESERVE `screenshotPathsByRowId` remap (lines 208-233) — it spans the union, unaffected.
    8. PRESERVE the `UnifiedDetailPane` mount with `intents={[]}` (hard-sep).

    Extend `stage-4/__tests__/page.test.tsx` with all seven behaviour cases above. Use the existing test scaffolding pattern (RSC page → render → mock loaders).
  </action>
  <verify>
    <automated>cd web && npx vitest run app/\(dashboard\)/automations/\[swarm\]/stage-4/__tests__/page.test.tsx --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Collapsible" web/app/\(dashboard\)/automations/\[swarm\]/stage-4/client-shell.tsx` returns ZERO hits.
    - `grep "ChipStrip" web/app/\(dashboard\)/automations/\[swarm\]/stage-4/client-shell.tsx` returns ≥2 hits (import + mount).
    - `grep "activeOutcome" web/app/\(dashboard\)/automations/\[swarm\]/stage-4/client-shell.tsx` returns ≥3 hits.
    - `grep "intents={\\[\\]}" web/app/\(dashboard\)/automations/\[swarm\]/stage-4/client-shell.tsx` returns ≥1 hit (hard-sep preserved).
    - `grep -E "swarm_intents|SwarmIntentRow" web/app/\(dashboard\)/automations/\[swarm\]/stage-4/client-shell.tsx` returns ZERO hits.
    - All seven test cases pass.
    - `cd web && npx tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>Stage 4 uses chip-strip filter + filtered RowList with brand-token colour cues; Collapsibles deleted; selectedId model preserved; hard-sep lock holds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator UAT — Stage 4 chip-strip + width parity</name>
  <what-built>
    Stage 4 now renders a `ChipStrip` of four outcome-state chips (All / Handler error / Needs review / Auto-archived) replacing the three Collapsible sections. URL contract `?outcome=` drives the active chip. Width regression branch from Wave 0 has been applied (or documented as a no-op). selectedId model survives the change.
  </what-built>
  <how-to-verify>
    1. Run `cd web && npm run dev` (or use Vercel preview deploy).
    2. Resize to **1440×900**.
    3. Open Stage 4 for a swarm with rows in all three outcome buckets (debtor-email recommended).
    4. Verify:
       a. Chip strip shows four chips with badges matching row counts per bucket (sum on "All").
       b. Click each non-All chip → URL gains `?outcome=<key>` (check address bar); row list narrows to that bucket; chip becomes visually active.
       c. Click "All" → URL `outcome` param removed; all rows visible again.
       d. Select a row in "Handler error" bucket; detail-pane opens. Switch to "Auto-archived" chip; observe selectedId behaviour matches pre-change Stage 4 (operator: confirm "feels the same" or report regression).
       e. Detail-pane width: side-by-side compare with Stage 1, 2, 3 in another tab — confirm widths visually identical (or report any narrowness).
    5. Take a screenshot at 1440×900 and save to `.planning/phases/88-review-surface-cleanup/screenshots/stage-4-chipstrip.png`.
    6. Compare to Wave 0 baseline screenshot (`screenshots/stage-4-detail-pane.png` if produced in Plan 01) — width parity must hold.

    Expected outcomes:
    - All four chips visible with correct counts.
    - URL contract works (refresh preserves filter).
    - Detail-pane width matches Stage 1/2/3 at 1440px.
    - No Collapsible vestigial UI remains.
  </how-to-verify>
  <resume-signal>Type "approved" if all five UAT items pass, OR describe what diverged.</resume-signal>
</task>

</tasks>

<verification>
- Vitest: `cd web && npx vitest run app/\(dashboard\)/automations/\[swarm\]/stage-4/__tests__ --no-coverage` green.
- `cd web && npx tsc --noEmit` exit 0.
- Grep gates (run from repo root):
  - `grep "Collapsible" web/app/\(dashboard\)/automations/\[swarm\]/stage-4/client-shell.tsx` → ZERO hits
  - `grep -E "swarm_intents|SwarmIntentRow" web/app/\(dashboard\)/automations/\[swarm\]/stage-4/client-shell.tsx` → ZERO hits (hard-sep)
  - `grep "intents={\\[\\]}" web/app/\(dashboard\)/automations/\[swarm\]/stage-4/client-shell.tsx` → ≥1 hit
- Operator UAT screenshot in `.planning/phases/88-review-surface-cleanup/screenshots/stage-4-chipstrip.png`.
</verification>

<success_criteria>
- D-03a (locked): outcome-state chips, not per-handler. Set bounded by code (4 chips fixed).
- D-03b (planner decision): chip set is `all / handler_error / needs_review / auto_archived` with `all` as default landing per RESEARCH.md Q2 recommendation.
- D-03c: width-regression branch executed per Wave 0 Q3 conclusion (Branch A/B documented in SUMMARY).
- selectedId cross-section model preserved.
- Hard-sep lock holds (`intents={[]}` on Stage 4 detail-pane).
</success_criteria>

<output>
After completion, create `.planning/phases/88-review-surface-cleanup/88-04-SUMMARY.md` documenting:
- Width-fix branch chosen (A/B/C) and Wave 0 Q3 reference
- Chip-strip behaviour assertions covered by tests
- Hard-sep grep results
- Operator UAT screenshot path
</output>
