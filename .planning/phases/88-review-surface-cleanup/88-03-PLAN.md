---
phase: 88
plan: 03
type: execute
wave: 1
depends_on: [88-01]
files_modified:
  - supabase/migrations/20260521_phase88_classifier_queue_verdict_pending.sql
  - web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/noise-category-chip-strip.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/noise-category-chip-strip.test.tsx
  - web/app/(dashboard)/automations/[swarm]/_shell/needs-action-chip.tsx
  - web/app/(dashboard)/automations/[swarm]/_shell/stage-list-chips.tsx
  - web/app/(dashboard)/automations/[swarm]/_shell/__tests__/needs-action-chip.test.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-0/page.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-2/page.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-3/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Stage 1 'Needs review' chip displays a verdict-pending count: only rows in automation_runs.status='predicted' AND no email_feedback row at stage=1 — NOT the previous 'sum of non-skip topics' aggregate"
    - "Stage 1 default landing renders with the 'Needs review' chip visually active (URL has no ?topic / ?sub — same as today — but the chip's badge now reflects verdict-pending count)"
    - "NeedsActionChip UI is fully deleted; ?needs_action=1 is no longer in the URL keyspace; server-side `needsActionOnly` loader filter is PRESERVED (Stage 0 still hardcodes it true)"
    - "MineOnlyChip survives D-02; its tests still run"
  artifacts:
    - path: "supabase/migrations/20260521_phase88_classifier_queue_verdict_pending.sql"
      provides: "New RPC classifier_queue_verdict_pending(p_swarm_type text) → bigint; LEFT-anti-JOIN automation_runs vs email_feedback at stage=1"
      contains: "create or replace function public.classifier_queue_verdict_pending"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/noise-category-chip-strip.tsx"
      provides: "Chip strip whose leftmost chip count is bound to new `verdictPendingCount` prop, not the previous `topic !== 'skip'` sum"
      contains: "verdictPendingCount"
    - path: "web/app/(dashboard)/automations/[swarm]/_shell/needs-action-chip.tsx"
      provides: "File no longer exports NeedsActionChip; only MineOnlyChip + ToggleChip helper remain (or file is renamed mine-only-chip.tsx)"
  key_links:
    - from: "stage-1/page.tsx server loader"
      to: "rpc('classifier_queue_verdict_pending', { p_swarm_type })"
      via: "supabaseAdmin.rpc call alongside existing classifier_queue_counts"
      pattern: "classifier_queue_verdict_pending"
    - from: "stage-1/page.tsx"
      to: "NoiseCategoryChipStrip"
      via: "new verdictPendingCount prop"
      pattern: "verdictPendingCount={"
    - from: "stage-list-chips.tsx"
      to: "MineOnlyChip only (NeedsActionChip removed)"
      via: "import surgery + JSX removal"
      pattern: "NeedsActionChip"
---

<objective>
Implement D-02: Stage 1 "Needs review" chip semantics + verdict-based count + Needs-action toggle removal.

Three changes land in this plan:
1. **New RPC** `classifier_queue_verdict_pending(p_swarm_type)` returning a single bigint — rows in `automation_runs.status='predicted'` AND `swarm_type=$1` AND no row in `email_feedback` at `stage=1` for that email. (Q1 from RESEARCH.md — `email_feedback` is canonical, NOT the placeholder name `feedback_reviews` in CONTEXT.md.)
2. **Chip-strip rewire**: `noise-category-chip-strip.tsx` accepts a `verdictPendingCount` prop and binds the "Needs review" chip's badge to it. The previous aggregation loop (`topic !== "skip"` sum, lines 83-93) is removed.
3. **NeedsActionChip deletion**: file `_shell/needs-action-chip.tsx` drops the `NeedsActionChip` export (keep `MineOnlyChip` + `ToggleChip`). `stage-list-chips.tsx` stops rendering it. All four stage page.tsx files (stage-0/1/2/3) stop parsing/producing the `?needs_action=1` URL param. Server-side `needsActionOnly` loader filter in `_shell/_lib/feedback-list-loader.ts` is UNCHANGED (Risk #2 in RESEARCH.md).

Purpose: Per D-02a/b/c (locked), operators get a single verdict-pending axis instead of two overlapping controls. Per D-02d (resolved by RESEARCH.md Q1), the canonical signal is absence in `email_feedback` at `stage=1`.

Output: One new migration, two modified component files, four modified page.tsx files, two updated test files.

DEPENDS ON Plan 01 (Wave 0): the RPC JOIN shape — `ar.email_id` vs `(ar.result->>'email_id')::uuid` — is locked by `88-WAVE0-FINDINGS.md` Q1. The deletion file:line list is from Q2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/88-review-surface-cleanup/88-CONTEXT.md
@.planning/phases/88-review-surface-cleanup/88-RESEARCH.md
@.planning/phases/88-review-surface-cleanup/88-WAVE0-FINDINGS.md
@CLAUDE.md

<!-- Current chip-strip — Section it edits -->
@web/app/(dashboard)/automations/[swarm]/stage-1/noise-category-chip-strip.tsx
<!-- Files to surgically modify -->
@web/app/(dashboard)/automations/[swarm]/_shell/needs-action-chip.tsx
@web/app/(dashboard)/automations/[swarm]/_shell/stage-list-chips.tsx
<!-- Server-side filter that MUST be preserved (Risk #2) -->
@web/app/(dashboard)/automations/[swarm]/_shell/_lib/feedback-list-loader.ts
<!-- Reference RPC for migration shape -->
@supabase/migrations/20260428_classifier_queue_counts.sql
@supabase/migrations/20260513c_email_feedback.sql

<interfaces>
<!-- email_feedback (canonical verdict table — NOT feedback_reviews) -->
<!-- From supabase/migrations/20260513c_email_feedback.sql -->
- Columns: email_id uuid, stage smallint CHECK BETWEEN 0 AND 3, verdict text, operator_id text, created_at
- Index: (email_id, stage)
- Stage 1 verdict = row with stage=1

<!-- automation_runs.email_id column shape -->
<!-- LOCKED BY WAVE 0 PLAN 01 — read .planning/phases/88-review-surface-cleanup/88-WAVE0-FINDINGS.md Q1 -->
- If top-level uuid: JOIN ON ef.email_id = ar.email_id
- If nested in result jsonb: JOIN ON ef.email_id = (ar.result->>'email_id')::uuid

<!-- Existing classifier_queue_counts contract (for reference, NOT modified) -->
<!-- From supabase/migrations/20260428_classifier_queue_counts.sql -->
- Returns: (swarm_type, topic, entity, mailbox_id, count)
- Filters: status='predicted'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write + apply migration — classifier_queue_verdict_pending RPC</name>
  <read_first>
    - .planning/phases/88-review-surface-cleanup/88-WAVE0-FINDINGS.md (Q1 section — has the locked JOIN fragment)
    - .planning/phases/88-review-surface-cleanup/88-RESEARCH.md (Q1 section — recommended RPC SQL)
    - supabase/migrations/20260428_classifier_queue_counts.sql (sibling RPC for style reference)
    - supabase/migrations/_template.sql (Supabase migration template per CLAUDE.md)
    - supabase/migrations/20260513c_email_feedback.sql (canonical verdict table)
  </read_first>
  <action>
    Create `supabase/migrations/20260521_phase88_classifier_queue_verdict_pending.sql` containing:

    ```sql
    -- Phase 88 D-02: verdict-pending scalar count for Stage 1 'Needs review' chip.
    -- Replaces the previous client-side aggregation (topic !== 'skip' sum) in
    -- noise-category-chip-strip.tsx with a server-side anti-join against email_feedback.

    create or replace function public.classifier_queue_verdict_pending(p_swarm_type text)
    returns bigint
    language sql
    stable
    security invoker
    set search_path = public, pg_catalog, pg_temp
    as $$
      select count(*)::bigint
      from public.automation_runs ar
      where ar.status = 'predicted'
        and ar.swarm_type = p_swarm_type
        and not exists (
          select 1 from public.email_feedback ef
          where {JOIN_FRAGMENT_FROM_WAVE0}      -- copy from 88-WAVE0-FINDINGS.md Q1
            and ef.stage = 1
        );
    $$;

    revoke execute on function public.classifier_queue_verdict_pending(text) from public, anon;
    grant execute on function public.classifier_queue_verdict_pending(text) to authenticated, service_role;
    ```

    Replace `{JOIN_FRAGMENT_FROM_WAVE0}` with the exact fragment from `88-WAVE0-FINDINGS.md` Q1 (either `ef.email_id = ar.email_id` or `ef.email_id = (ar.result->>'email_id')::uuid`).

    Apply the migration using the Supabase MCP `apply_migration` tool (project = `mvqjhlxfvtqqubqgdvhz`, migration name = `phase88_classifier_queue_verdict_pending`). Per CLAUDE.md, do NOT use `public` for new extensions (n/a here), MUST set `search_path`, MUST grant only to authenticated + service_role (not anon).

    Smoke test via Supabase MCP `execute_sql`:
    ```sql
    select public.classifier_queue_verdict_pending('debtor-email') as cnt;
    ```
    Expect a non-error bigint (value can be any number including 0).
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260521_phase88_classifier_queue_verdict_pending.sql && grep -q "create or replace function public.classifier_queue_verdict_pending" supabase/migrations/20260521_phase88_classifier_queue_verdict_pending.sql && grep -q "set search_path = public" supabase/migrations/20260521_phase88_classifier_queue_verdict_pending.sql && ! grep -qE "to (anon|public)" supabase/migrations/20260521_phase88_classifier_queue_verdict_pending.sql</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists at the path above.
    - Contains `create or replace function public.classifier_queue_verdict_pending(p_swarm_type text)`.
    - Contains `set search_path = public, pg_catalog, pg_temp` (per CLAUDE.md SECURITY DEFINER / function hygiene rule).
    - Contains `revoke execute … from public, anon` and `grant execute … to authenticated, service_role`.
    - JOIN fragment matches the one in `88-WAVE0-FINDINGS.md` Q1 verbatim (no fallback / hedging).
    - Supabase MCP `execute_sql` smoke returns a bigint for `p_swarm_type='debtor-email'` (no SQL error).
    - `npm run check:supabase` from `web/` passes (no new ERROR-level advisor lint).
  </acceptance_criteria>
  <done>RPC is live in Supabase, callable from `rpc('classifier_queue_verdict_pending', {p_swarm_type})`, secured per CLAUDE.md RLS+function policies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire noise-category-chip-strip + stage-1/page.tsx to consume verdictPendingCount</name>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/noise-category-chip-strip.tsx (lines 83-93 for the aggregation to delete; lines 107+ for the chip definition)
    - web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx (loader section that calls classifier_queue_counts)
    - web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/noise-category-chip-strip.test.tsx
    - .planning/phases/88-review-surface-cleanup/88-RESEARCH.md (Implementation Map: D-02 stage-1/page.tsx + noise-category-chip-strip.tsx rows)
  </read_first>
  <behavior>
    - Test 1: Given `verdictPendingCount=12` prop, leftmost chip renders `Needs review` label with badge `12`.
    - Test 2: Component source MUST NOT contain the previous aggregation pattern `for (const c of counts)` summing non-skip topics for the `needsReviewCount` local — grep gate.
    - Test 3: Clicking the "Needs review" chip still navigates to a URL without `?topic` / `?sub` (existing URL contract preserved).
    - Test 4: When `verdictPendingCount=0`, chip still renders with badge `0` (does not hide).
    - Test 5: When URL has no `?topic` and no `?sub`, the "Needs review" chip renders with the active marker (`aria-selected='true'` or the `data-active` attribute the ChipStrip primitive uses) — verifies D-02c default-landing visual-active state.
  </behavior>
  <action>
    Modify `web/app/(dashboard)/automations/[swarm]/stage-1/noise-category-chip-strip.tsx`:
    1. Add to the component's `Props` interface: `verdictPendingCount: number`.
    2. DELETE the aggregation block that computes `needsReviewCount` (lines ~83-93 per RESEARCH.md — the `for (const c of counts)` loop accumulating non-skip topics). Replace `needsReviewCount` references with the new `verdictPendingCount` prop.
    3. The chip object at line ~107 stays `{ key: "all", label: "Needs review", count: verdictPendingCount }`.
    4. All other behaviour (URL contract, click handlers, brand tokens) unchanged.

    Modify `web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx`:
    1. In the server loader (alongside the existing `supabase.rpc("classifier_queue_counts", ...)` call), add:
       ```ts
       const { data: verdictPendingCount } = await supabaseAdmin
         .rpc("classifier_queue_verdict_pending", { p_swarm_type: swarmType });
       ```
       Default to `0` on null/error (don't break the page).
    2. Forward to `<NoiseCategoryChipStrip>` as `verdictPendingCount={verdictPendingCount ?? 0}`.

    Update test file `stage-1/__tests__/noise-category-chip-strip.test.tsx`:
    - Rewrite the existing `needsReviewCount` assertion to feed `verdictPendingCount={N}` prop and assert badge text equals N.
    - Add Test 4 (zero case).
    - Add Test 5 (D-02c default-landing visual-active): render the strip with no `?topic` / `?sub` in the URL (or pass the equivalent `activeKey`/`activeTopic` prop the component reads as "no selection") and assert the "Needs review" chip has the active marker — `aria-selected='true'` OR the `data-active` attribute used by the underlying ChipStrip primitive. Test name MUST match the regex `/default landing.*Needs review.*active/i` so the acceptance grep below can verify it.
    - Keep all other tests unchanged.
  </action>
  <verify>
    <automated>cd web && npx vitest run app/\(dashboard\)/automations/\[swarm\]/stage-1/__tests__/noise-category-chip-strip.test.tsx --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "verdictPendingCount" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/noise-category-chip-strip.tsx` returns ≥3.
    - `grep -E "for \\(const c of counts\\)" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/noise-category-chip-strip.tsx` returns ZERO hits (old aggregation removed).
    - `grep "classifier_queue_verdict_pending" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/page.tsx` returns ≥1 hit.
    - vitest run green; assertion on `verdictPendingCount=12` → badge "12" passes; zero-case passes.
    - `noise-category-chip-strip.test.tsx` contains a test name matching `/default landing.*Needs review.*active/i` AND vitest exits 0 (D-02c default-chip visual-active assertion — checker W-2 fix).
  </acceptance_criteria>
  <done>Chip-strip badge reflects server-computed verdict-pending count; aggregation logic deleted; URL contract unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Delete NeedsActionChip + ?needs_action URL param across stages</name>
  <read_first>
    - .planning/phases/88-review-surface-cleanup/88-WAVE0-FINDINGS.md (Q2 — Deletion targets list)
    - web/app/(dashboard)/automations/[swarm]/_shell/needs-action-chip.tsx (full file — split MineOnlyChip vs NeedsActionChip)
    - web/app/(dashboard)/automations/[swarm]/_shell/stage-list-chips.tsx (consumer)
    - web/app/(dashboard)/automations/[swarm]/_shell/__tests__/needs-action-chip.test.tsx
    - web/app/(dashboard)/automations/[swarm]/stage-0/page.tsx (lines 71, 112, 203 — hardcoded needsAction=true is server-side filter, KEEP per Risk #2)
    - web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx (line 122)
    - web/app/(dashboard)/automations/[swarm]/stage-2/page.tsx (lines 64, 103, 178)
    - web/app/(dashboard)/automations/[swarm]/stage-3/page.tsx (line 59)
    - web/app/(dashboard)/automations/[swarm]/_shell/_lib/feedback-list-loader.ts (PRESERVE `needsActionOnly` loader param — server-side filter)
  </read_first>
  <behavior>
    - Test 1: `_shell/needs-action-chip.tsx` no longer exports `NeedsActionChip` (import in any consumer fails build).
    - Test 2: `MineOnlyChip` still exports and renders correctly (existing mine-only test still passes after split).
    - Test 3: Grep across `web/app/(dashboard)/automations/[swarm]/` returns ZERO hits for `needs_action=1` URL producer/consumer.
    - Test 4: Server-side filter `needsActionOnly` survives in `_shell/_lib/feedback-list-loader.ts` (grep returns ≥1 hit — the loader param is intentionally kept).
    - Test 5: Stage 0 hardcoded `needsActionOnly: true` server-side call still works (RESEARCH.md Risk #2).
  </behavior>
  <action>
    Phase A — Component surgery on `_shell/needs-action-chip.tsx`:
    1. If the existing test file mixes `NeedsActionChip` + `MineOnlyChip` cases, first split: copy `MineOnlyChip` tests into a fresh `_shell/__tests__/mine-only-chip.test.tsx`. Verify they pass against the unchanged component.
    2. Delete the `NeedsActionChip` export from `_shell/needs-action-chip.tsx`. Keep `MineOnlyChip` + shared `ToggleChip` helper.
    3. Delete the `NeedsActionChip`-specific portion of `_shell/__tests__/needs-action-chip.test.tsx`. If only `MineOnlyChip` tests remain there, delete the whole file (you copied to `mine-only-chip.test.tsx` above).

    Phase B — Consumer cleanup on `_shell/stage-list-chips.tsx`:
    1. Remove the `NeedsActionChip` import.
    2. Remove the `needsAction` prop from the component signature.
    3. Remove the `(key: "needs_action" | "mine_only", currentlyActive: boolean) => …` toggleParam branch — narrow the closure to `"mine_only"` only.
    4. Remove the `<NeedsActionChip … onToggle={() => toggleParam("needs_action", needsAction)} />` JSX.
    5. The component now renders only `<MineOnlyChip>`.

    Phase C — Page surgery on stage-0/1/2/3 page.tsx (per Wave 0 Q2 deletion list):
    For EACH of `stage-0/page.tsx`, `stage-1/page.tsx`, `stage-2/page.tsx`, `stage-3/page.tsx`:
    1. Remove `needs_action?: string;` from the `searchParams` type (if present).
    2. Remove the `const needsAction = sp.needs_action === "1"` parsing line.
    3. Remove the `if (needsAction) qs.set("needs_action", "1")` producer line(s) (these were inside Link href builders).
    4. Remove the `needsAction` prop passed to `<StageListChips>`.

    **CRITICAL — Stage 0 exception (RESEARCH.md Risk #2):**
    Stage 0's `loadFeedbackList({ …, needsActionOnly: true })` call (the SERVER-SIDE filter) is NOT a URL-param consumer. KEEP that. Specifically:
    - DELETE: URL parsing `sp.needs_action === "1"` and the URL builder `qs.set("needs_action", "1")` at stage-0/page.tsx.
    - KEEP: any `loadFeedbackList(..., { needsActionOnly: true })` invocation (the loader still accepts that flag).
    - KEEP: `_shell/_lib/feedback-list-loader.ts` `needsActionOnly?: boolean` param signature unchanged.

    Phase D — Tests:
    - Update `_shell/__tests__/needs-action-chip.test.tsx` (or its split `mine-only-chip.test.tsx`) so `MineOnlyChip` cases still pass.
    - Run full `_shell` test directory.

    Final hard-sep grep gate:
    ```bash
    grep -rE "needs_action=1|sp\\.needs_action|searchParams\\.needs_action|NeedsActionChip" web/app/\(dashboard\)/automations/\[swarm\]/ 2>/dev/null
    ```
    Must return ZERO hits across the dashboard subtree (Stage 0's server-side `needsActionOnly: true` is a different identifier and is NOT matched by this grep — verify the regex doesn't accidentally include it; if it does, refine to exclude `needsActionOnly`).
  </action>
  <verify>
    <automated>cd web && npx vitest run app/\(dashboard\)/automations/\[swarm\]/_shell/__tests__ --no-coverage && cd .. && ! grep -rE "needs_action=1|NeedsActionChip" web/app/\(dashboard\)/automations/\[swarm\]/ 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "NeedsActionChip" web/app/\(dashboard\)/automations/\[swarm\]/` returns ZERO hits.
    - `grep -r "needs_action=1" web/app/\(dashboard\)/automations/\[swarm\]/` returns ZERO hits.
    - `grep -r "sp\\.needs_action\\|searchParams\\.needs_action" web/app/\(dashboard\)/automations/\[swarm\]/` returns ZERO hits.
    - `grep "needsActionOnly" web/app/\(dashboard\)/automations/\[swarm\]/_shell/_lib/feedback-list-loader.ts` returns ≥1 hit (server-side filter preserved).
    - `grep "needsActionOnly" web/app/\(dashboard\)/automations/\[swarm\]/stage-0/page.tsx` returns ≥1 hit (Stage 0 server-side filter call preserved).
    - `_shell/__tests__/` vitest directory run green.
    - `cd web && npx tsc --noEmit` returns 0 (no orphan imports / type errors).
  </acceptance_criteria>
  <done>NeedsActionChip is gone UI- and URL-side; MineOnlyChip survives; server-side `needsActionOnly` filter intact; existing tests for unrelated chip surfaces unchanged.</done>
</task>

</tasks>

<verification>
- All three task vitest commands green.
- Migration applied + smoke `select public.classifier_queue_verdict_pending('debtor-email')` returns bigint without error.
- `npm run check:supabase` (web/) passes — no new ERROR-level advisor lint introduced by the new RPC.
- Hard-sep grep:
  - Zero hits for `NeedsActionChip` and `needs_action=1` across `web/app/(dashboard)/automations/[swarm]/`.
  - At least 1 hit for `needsActionOnly` in `_shell/_lib/feedback-list-loader.ts` AND `stage-0/page.tsx` (server-side filter survived).
- `cd web && npx tsc --noEmit` exit 0.
</verification>

<success_criteria>
- D-02a/b: "Needs review" chip count is verdict-pending (RPC-backed); previous aggregation deleted.
- D-02c: Default landing on Stage 1 = no `?topic`/`?sub` URL (existing behaviour) but chip badge now reflects verdict-pending count.
- D-02 cleanup: NeedsActionChip UI fully removed; `?needs_action=1` no longer in the URL keyspace.
- Risk #2 mitigated: server-side `needsActionOnly` loader filter preserved; Stage 0 list still pre-filters.
</success_criteria>

<output>
After completion, create `.planning/phases/88-review-surface-cleanup/88-03-SUMMARY.md` listing the new migration, all modified files, the four grep-verifiable hard-sep results, and the migration smoke test output.
</output>
