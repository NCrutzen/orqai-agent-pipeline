---
phase: 68-swarm-registry-generalisation-canonical-context-shape
plan: 09
type: execute
wave: 5
depends_on: [03, 04, 05, 06, 07, 08]
files_modified:
  - .planning/phases/68-swarm-registry-generalisation-canonical-context-shape/68-regression-report.md
autonomous: false
requirements: [SWRM-01, SWRM-02, SWRM-03, SWRM-04]
must_haves:
  truths:
    - "Phase 67 icontroller-tag smoke event still tags through the registry-driven dispatch path"
    - "Full vitest suite green"
    - "Sales-email-stub SWRM-03 test green against live Supabase"
  artifacts:
    - path: ".planning/phases/68-swarm-registry-generalisation-canonical-context-shape/68-regression-report.md"
      provides: "Phase 67 regression confirmation + full-suite gate"
      contains: "Phase 67 regression"
  key_links:
    - from: "live Inngest event"
      to: "registry-driven dispatcher"
      via: "Vercel preview deploy"
      pattern: "icontroller-tag"
---

<objective>
Run the full validation gate before `/gsd-verify-work`: full vitest suite, sales-email-stub against live DB, Phase 67 smoke regression through the new registry path.

Purpose: Final phase-level proof — registry-driven dispatch is byte-equivalent to the prior hardcoded paths.
Output: Regression report committed; phase ready to close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/68-swarm-registry-generalisation-canonical-context-shape/68-VALIDATION.md
@.planning/phases/67-stage-2-closure-icontroller-dom-tagging/67-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full vitest suite + sales-email-stub</name>
  <read_first>
    - 68-VALIDATION.md "Sampling Rate" + "Test Infrastructure"
  </read_first>
  <files>.planning/phases/68-swarm-registry-generalisation-canonical-context-shape/68-regression-report.md</files>
  <action>
1. Run: `cd web && npx vitest run` — capture exit code + summary line.
2. Run: `cd web && npx vitest run lib/swarms/__tests__/sales-email-stub.test.ts` — confirm green.
3. Append both results to `68-regression-report.md` with PASS/FAIL.
4. If any FAIL → STOP, escalate. Do not proceed to Task 2.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Full suite exit 0
    - Sales-email-stub green
    - `68-regression-report.md` contains "Full suite: PASS" and "SWRM-03 stub: PASS"
  </acceptance_criteria>
  <done>Vitest gate clear.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 67 live smoke regression</name>
  <what-built>All Phase 68 code changes merged on a Vercel preview deploy; the registry-driven dispatcher is now the live path for the Phase 67 icontroller-tag emit.</what-built>
  <how-to-verify>
1. Confirm a Vercel preview deploy is up for the Phase 68 branch.
2. Re-fire the existing Phase 67 smoke event:
   ```bash
   bash scripts/phase-67-smoke.sh
   ```
   (or whichever smoke script Phase 67 used — check `.planning/phases/67-stage-2-closure-icontroller-dom-tagging/` for the exact path).
3. Verify the tagger executes successfully:
   - Inngest dashboard shows `debtor-email/icontroller-tag.requested` fired.
   - The downstream tagger run completes (Browserless run finishes).
   - `email_labels.icontroller_tag_status` transitions to the success state for the test row.
4. Confirm in Supabase logs / Inngest dashboard that the event went through the new `evaluateSideEffects` dispatch path (no errors about missing registry rows).
5. Append outcome to `68-regression-report.md` (PASS/FAIL + run timestamps + Inngest run IDs).
  </how-to-verify>
  <resume-signal>Type "regression PASS" with the Inngest run IDs, or describe failure.</resume-signal>
  <acceptance_criteria>
    - `68-regression-report.md` contains "Phase 67 regression: PASS" with run IDs OR an explicit operator override sign-off.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Vitest full suite green; live Phase 67 smoke confirms behavioural equivalence through the registry-driven dispatcher.
</verification>

<success_criteria>
SWRM-01..04 all proven end-to-end. Phase 68 ready for `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/68-swarm-registry-generalisation-canonical-context-shape/68-09-SUMMARY.md`.
</output>
