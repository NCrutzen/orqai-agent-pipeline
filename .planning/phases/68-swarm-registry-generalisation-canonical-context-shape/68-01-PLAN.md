---
phase: 68-swarm-registry-generalisation-canonical-context-shape
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260504b_swarms_registry_generalisation.sql
autonomous: false
requirements: [SWRM-01, SWRM-02, SWRM-04]
must_haves:
  truths:
    - "public.swarms has 5 new columns: stage1_regex_module, stage2_entity_resolver, stage3_coordinator_agent_key, canonical_context_shape, entity_brand"
    - "public.swarm_intents table exists with composite PK (swarm_type, intent_key) and ON DELETE CASCADE"
    - "debtor-email row backfilled with all stage bindings + side_effects[2] descriptors using the kind discriminator"
    - "swarm_intents has 8 rows for debtor-email matching the V2 INTENT enum"
  artifacts:
    - path: "supabase/migrations/20260504b_swarms_registry_generalisation.sql"
      provides: "Schema migration + backfill"
      contains: "create table if not exists public.swarm_intents"
  key_links:
    - from: "swarm_intents.swarm_type"
      to: "swarms.swarm_type"
      via: "FK ON DELETE CASCADE"
      pattern: "references public.swarms"
---

<objective>
Create and apply the Phase 68 migration that lifts swarm-specific bindings into `public.swarms` registry rows and creates `public.swarm_intents`.

Purpose: Turn hardcoded swarm bindings into data so onboarding a new swarm is INSERT-only.
Output: One idempotent migration applied via Supabase MCP `apply_migration`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/68-swarm-registry-generalisation-canonical-context-shape/68-CONTEXT.md
@.planning/phases/68-swarm-registry-generalisation-canonical-context-shape/68-RESEARCH.md
@web/lib/swarms/types.ts
@web/lib/automations/debtor-email/coordinator/types.ts
@supabase/migrations/20260429b_swarm_registry.sql
@supabase/migrations/20260430b_swarm_categories_invoice_copy_dispatch.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration SQL file</name>
  <read_first>
    - 68-RESEARCH.md lines 204-312 (full migration backfill SQL — copy verbatim)
    - 68-CONTEXT.md lines 30-99 (D-01..D-10 locked decisions)
    - web/lib/automations/debtor-email/coordinator/types.ts lines 17-26 (V2 INTENT enum source — 8 values)
  </read_first>
  <files>supabase/migrations/20260504b_swarms_registry_generalisation.sql</files>
  <action>
Create the file with the EXACT SQL from 68-RESEARCH.md lines 209-312. Key elements:

1. `alter table public.swarms add column if not exists` for 5 columns: `stage1_regex_module text`, `stage2_entity_resolver text`, `stage3_coordinator_agent_key text`, `canonical_context_shape jsonb`, `entity_brand jsonb`.
2. `create table if not exists public.swarm_intents` with composite PK `(swarm_type, intent_key)`, `handler_agent_key text` (nullable per D-09), `handler_event text not null`, `requires_orchestration boolean not null default false`, `created_at` + `updated_at` timestamptz, FK to swarms with `on delete cascade`.
3. Index `swarm_intents_handler_event_idx` on `handler_event`.
4. `updated_at` trigger function + trigger.
5. UPDATE on `swarms WHERE swarm_type = 'debtor-email'` populating all 5 new columns. Use `jsonb_build_object` / `jsonb_build_array` per RESEARCH lines 256-292.
   - `entity_brand` = `jsonb_build_array('smeba','smeba-fire','sicli-noord','sicli-sud','berki')` (matches CONTEXT D-15 list and INTENT enum source `coordinator/types.ts` ENTITY).
   - `side_effects` = jsonb array with TWO descriptors using `kind` discriminator (R-01 locked):
     - Descriptor 1: `{kind: 'inngest_event', event: 'debtor-email/icontroller-tag.requested', trigger: 'stage2_match_live', gate: {dry_run: false, customer_account_id_present: true, icontroller_company_present: true}, phase_origin: '67'}`
     - Descriptor 2: `{kind: 'automation_run_insert', automation: 'debtor-email-cleanup', trigger: 'stage1_categorize_archive', gate: {category_action: 'categorize_archive'}, result_template: {stage: 'icontroller_delete', icontroller: 'pending'}, phase_origin: '56.7'}`
6. Backfill 8 INSERT rows into `swarm_intents` for debtor-email V2 INTENT enum. `handler_agent_key` = `'debtor-copy-document-body-agent'` ONLY for `copy_document_request`; null for other 7. `handler_event = 'debtor-email/<intent>.requested'`. `requires_orchestration = false` for all. End with `on conflict (swarm_type, intent_key) do nothing`.

DO NOT alter `swarm_categories`. DO NOT add new indexes beyond `swarm_intents_handler_event_idx`.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260504b_swarms_registry_generalisation.sql &amp;&amp; grep -c "create table if not exists public.swarm_intents" supabase/migrations/20260504b_swarms_registry_generalisation.sql</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path `supabase/migrations/20260504b_swarms_registry_generalisation.sql`
    - `grep -c "add column if not exists" supabase/migrations/20260504b_swarms_registry_generalisation.sql` returns >=5
    - `grep -c "kind" supabase/migrations/20260504b_swarms_registry_generalisation.sql` returns >=2 (both side-effect descriptors carry kind)
    - `grep -c "('debtor-email'" supabase/migrations/20260504b_swarms_registry_generalisation.sql` returns >=8 (8 swarm_intents rows)
    - `grep "on delete cascade" supabase/migrations/20260504b_swarms_registry_generalisation.sql` returns at least 1 line
    - `grep "swarm_intents_handler_event_idx" supabase/migrations/20260504b_swarms_registry_generalisation.sql` returns at least 1 line
    - `grep "stage3_coordinator_agent_key.*= 'debtor-intent-agent'" supabase/migrations/20260504b_swarms_registry_generalisation.sql` matches
  </acceptance_criteria>
  <done>Migration file exists, idempotent, backfill complete per RESEARCH lines 209-312.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Apply migration via Supabase MCP</name>
  <what-built>Migration file `supabase/migrations/20260504b_swarms_registry_generalisation.sql` ready to apply.</what-built>
  <how-to-verify>
1. Apply via `mcp__supabase__apply_migration` using the file contents (Phase 67 precedent — same pattern).
2. Verify columns landed:
   ```sql
   select column_name from information_schema.columns
   where table_schema='public' and table_name='swarms'
   and column_name in ('stage1_regex_module','stage2_entity_resolver','stage3_coordinator_agent_key','canonical_context_shape','entity_brand');
   ```
   Expected: 5 rows.
3. Verify table exists:
   ```sql
   select count(*) from public.swarm_intents where swarm_type='debtor-email';
   ```
   Expected: 8.
4. Verify backfill:
   ```sql
   select stage1_regex_module, stage2_entity_resolver, stage3_coordinator_agent_key,
          jsonb_array_length(side_effects) as se_count,
          jsonb_array_length(entity_brand) as brand_count
   from public.swarms where swarm_type='debtor-email';
   ```
   Expected: stage1='@/lib/debtor-email/classify', stage2='@/lib/automations/debtor-email/resolve-debtor', stage3='debtor-intent-agent', se_count=2, brand_count=5.
  </how-to-verify>
  <resume-signal>Type "migration applied" with the verification SQL output, or describe any error.</resume-signal>
</task>

</tasks>

<verification>
Migration applied; all 5 columns + new table + 8 backfill rows confirmed via SQL probe.
</verification>

<success_criteria>
SWRM-01 schema in place; SWRM-02 swarm_intents seeded with 8 V2 intents; debtor-email row carries 2-element `side_effects[]` with kind discriminator.
</success_criteria>

<output>
Create `.planning/phases/68-swarm-registry-generalisation-canonical-context-shape/68-01-SUMMARY.md`.
</output>
