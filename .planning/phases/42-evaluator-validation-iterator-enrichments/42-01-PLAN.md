---
phase: 42-evaluator-validation-iterator-enrichments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/agents/tester.md
autonomous: true
requirements: [ITRX-03, ESCI-03, ESCI-04, ESCI-05, ESCI-07]
must_haves:
  truths:
    - "tester.md emits a run-comparison table across iterations (Run | Date | Model | Avg Score | Cost | Key Changes)"
    - "tester.md flags suspected overfitting when new evaluator scores ≥98% on dataset <100 datapoints"
    - "tester.md tracks capability suites separately from regression suites"
    - "tester.md warns when average pass rate ≥95%"
    - "tester.md uses isolated graders per quality dimension (tool selection, argument quality, output interpretation)"
  artifacts:
    - path: "orq-agent/agents/tester.md"
      provides: "Run-comparison + overfitting warning + capability/regression split + ≥95% warn + isolated graders"
      contains: "run-comparison table, overfitting, ≥98%, <100, capability suite, regression suite, eval may be too easy, isolated grader"
  key_links:
    - from: "orq-agent/agents/tester.md"
      to: "orq-agent/scripts/lint-skills.sh"
      via: "grep anchor phrases verbatim"
      pattern: "run-comparison table|isolated grader|capability suite|regression suite|eval may be too easy"
---

<objective>
Enrich `orq-agent/agents/tester.md` with 5 eval-science enhancements so tester output carries overfitting, suite-split, and isolated-grader signals.

Purpose: close ITRX-03 + ESCI-03/04/05/07. tester ≥95% warn anchor already exists from Phase 42 ESCI-05 baseline (Constraints line "eval may be too easy"); this plan adds the remaining 4 anchors.
Output: single updated tester.md; no resource files in this plan.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-evaluator-validation-iterator-enrichments/42-CONTEXT.md
@.planning/phases/42-evaluator-validation-iterator-enrichments/42-VALIDATION.md
@orq-agent/agents/tester.md

<interfaces>
Current tester.md already has the ≥95% warn anchor in Constraints:
- `**ALWAYS** surface a warning when average pass rate ≥ 95% (Phase 42 ESCI-05 baseline — eval may be too easy).`
That phrase "eval may be too easy" is lint-anchor verbatim — DO NOT remove or paraphrase.

Existing Phase 8 (Aggregate Results and Report) is where per-iteration tables live. Run-comparison table slots between Step 8.6 (test-results.md) and Step 8.7 (terminal summary).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read tester.md, identify insertion points, add 4 new anchor-bearing sections</name>
  <files>orq-agent/agents/tester.md</files>
  <action>
Read the current tester.md in full. Preserve every existing section byte-identical.

Insertions (all additive — zero deletions except where noted):

1. **Isolated graders (ESCI-03)** — add H3 `### Isolated Graders per Quality Dimension` under Phase 6 (Infer Agent Roles and Select Evaluators), after Step 6.4 (before Phase 7 header). Body:
   - Opening sentence MUST contain the verbatim phrase "**isolated grader**" (bold, hyphen-free alt: "isolated grader" — use EXACTLY `isolated grader` somewhere in body so grep matches).
   - 3-bullet rationale: tool selection, argument quality, output interpretation scored by SEPARATE graders. Never one omnibus grader.
   - Reference to ESCI-03 in parens: `(Phase 42 ESCI-03)`.

2. **Capability vs Regression Suites (ESCI-04)** — add H3 `### Capability Suites vs Regression Suites` at the bottom of Phase 4 (Merge and Split Datasets), after Step 4.3. Body MUST contain the phrases `capability suite` and `regression suite` verbatim (case-insensitive grep tolerant; use lowercase). Rules:
   - Capability suite — expect low pass-rate initially; new-behavior probing.
   - Regression suite — expect near-100% pass-rate; items graduated from capability after 2 consecutive green runs.
   - Graduation rule explicit. `(Phase 42 ESCI-04)`.

3. **Overfitting Warning (ESCI-07)** — add H3 `### Overfitting Warning for Small Datasets` inside Phase 8, between Step 8.2 (Pass/Fail Determination) and Step 8.3 (Category-Sliced Scoring). Body MUST contain verbatim:
   - The word `overfitting`
   - The token `≥98%` (use the Unicode ≥ character)
   - The token `<100` datapoints
   - A concrete 3-line rule: if newly-iterated evaluator median ≥ 98% AND dataset size < 100, emit warning `"Suspected overfitting: evaluator scored ≥98% on <100 datapoints. Expand dataset before validating."` and flag as `overfitting_warning: true` in test-results.json.
   - `(Phase 42 ESCI-07)`.

4. **Run-comparison Table (ITRX-03)** — add H3 `### Run-Comparison Table (Iteration Trend)` between Step 8.6 (test-results.md) and Step 8.7 (terminal summary). The H3 title MUST contain the verbatim phrase `Run-Comparison Table` and the body MUST contain the phrase `run-comparison table` (lowercase, hyphenated) so the grep anchor matches.
   - Body: markdown snippet showing the canonical 6-column table:
     ```
     | Run | Date | Model | Avg Score | Cost | Key Changes |
     |-----|------|-------|-----------|------|-------------|
     | 1 | 2026-01-15 | claude-sonnet-4-5-20250929 | 0.72 | $0.14 | baseline |
     | 2 | 2026-01-16 | claude-sonnet-4-5-20250929 | 0.81 | $0.15 | tightened <constraints> |
     ```
   - Rule: tester appends one row per `/orq-agent:iterate` cycle to `test-results.md` (bottom of the file, under H2 `## Run-Comparison Trend`); iteration-runner inserts new row keyed by run ordinal. `(Phase 42 ITRX-03)`.

5. **Done When checklist** — append 4 new checkboxes (preserving existing 5):
   - [ ] Isolated graders described — tool selection, argument quality, output interpretation (Phase 42 ESCI-03)
   - [ ] Capability suites tracked separately from regression suites (Phase 42 ESCI-04)
   - [ ] Overfitting warning rule documented (≥98% on <100 datapoints) (Phase 42 ESCI-07)
   - [ ] Run-comparison table emitted per iteration (Phase 42 ITRX-03)

Do NOT touch YAML frontmatter, Phase 1-7 step numbering, Constraints (already has ESCI-05 line), Anti-Patterns, Open in orq.ai, Documentation & Resolution. Protected pipeline files (orq-agent.md / prompt.md / architect.md) are NOT touched.
  </action>
  <verify>
    <automated>bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/tester.md &amp;&amp; grep -cE "run-comparison table" orq-agent/agents/tester.md | grep -q "^[1-9]" &amp;&amp; grep -qE "isolated grader" orq-agent/agents/tester.md &amp;&amp; grep -qE "capability suite" orq-agent/agents/tester.md &amp;&amp; grep -qE "regression suite" orq-agent/agents/tester.md &amp;&amp; grep -qE "overfitting" orq-agent/agents/tester.md &amp;&amp; grep -qE "≥98%" orq-agent/agents/tester.md &amp;&amp; grep -qE "&lt;100" orq-agent/agents/tester.md &amp;&amp; grep -qE "eval may be too easy" orq-agent/agents/tester.md</automated>
  </verify>
  <done>All 8 grep anchors present; lint-skills.sh exits 0; Done When checklist has 4 new Phase-42 rows; protected pipelines untouched.</done>
</task>

</tasks>

<verification>
Run: `bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/tester.md` (exit 0)
Run: `bash orq-agent/scripts/check-protected-pipelines.sh` (3/3 SHA-256 match)
Grep 8 anchors: run-comparison table | isolated grader | capability suite | regression suite | overfitting | ≥98% | <100 | eval may be too easy
</verification>

<success_criteria>
- tester.md lint-clean across all 5 SKST rules
- 8 grep anchors present verbatim
- Protected pipeline SHA-256 3/3 intact
- 4 new Done When checkboxes added
</success_criteria>

<output>
After completion, create `.planning/phases/42-evaluator-validation-iterator-enrichments/42-01-SUMMARY.md` documenting anchors added and line-range deltas.
</output>
