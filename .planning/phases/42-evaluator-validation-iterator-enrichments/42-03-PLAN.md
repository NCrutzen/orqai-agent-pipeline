---
phase: 42-evaluator-validation-iterator-enrichments
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/agents/iterator.md
autonomous: true
requirements: [ITRX-01, ITRX-02, ITRX-05, ITRX-07, ITRX-09, EVLD-11, ESCI-06]
must_haves:
  truths:
    - "iterator assigns P0/P1/P2 priority to every proposed improvement"
    - "iterator produces structured Action Plan (Summary + Priority Improvements + Re-run Criteria)"
    - "iterator supports evaluator-version A/B (two evaluator prompts attached to same experiment as separate columns)"
    - "iterator absorbs free-text annotation comments into diff proposal reasoning"
    - "iterator refuses to re-run same optimizer on same prompt without explicit override (no-repeat rule)"
    - "iterator publishes inspectable decision trees: prompt fix vs evaluator / upgrade model? / eval good enough?"
    - "iterator ticket output includes Evidence (datapoints, scores, run ID) AND Success Criteria (target re-run score)"
  artifacts:
    - path: "orq-agent/agents/iterator.md"
      provides: "P0/P1/P2 + Action Plan + A/B + annotation + no-repeat + decision trees + Evidence/Success Criteria"
      contains: "P0, P1, P2, Action Plan, evaluator-version A/B, annotation comment, no-repeat, explicit override, prompt fix vs evaluator, upgrade model, eval good enough, Evidence, Success Criteria"
  key_links:
    - from: "orq-agent/agents/iterator.md"
      to: "iteration-log.md schema"
      via: "new Action Plan block with P0/P1/P2 + Evidence/Success Criteria fields"
      pattern: "Action Plan|P0|P1|P2"
---

<objective>
Enrich iterator.md with 7 methodology anchors so every iteration ticket has priority, evidence, success criteria, and inspectable decision trees; also adds no-repeat safety gate and evaluator-version A/B.

Purpose: close ITRX-01/02/05/07/09 + EVLD-11 + ESCI-06.
Output: updated iterator.md only.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-evaluator-validation-iterator-enrichments/42-CONTEXT.md
@.planning/phases/42-evaluator-validation-iterator-enrichments/42-VALIDATION.md
@orq-agent/agents/iterator.md

<interfaces>
Existing Constraints already seed 4 anchors:
- `NEVER re-run the same optimizer on the same prompt without explicit user override (Phase 42 ITRX-05 no-drift rule)`
- `ALWAYS assign P0/P1/P2 priority to every proposed improvement (Phase 42 ITRX-01)`
- `ALWAYS cite Evidence (datapoints affected, scores, run ID) and Success Criteria per diff (Phase 42 ITRX-07)`
These phrases MUST remain byte-identical.

Existing Phase 2 (Diagnose) and Phase 3 (Propose) are where priority labels attach. Phase 9 (iteration-log.md) is where Action Plan renders.

Resources at `orq-agent/agents/iterator/resources/{action-plan-template.md, decision-trees.md}` are created by Plan 07 (this plan references them by path).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add P0/P1/P2 priority, Action Plan schema, A/B, annotation, no-repeat, decision-tree anchors</name>
  <files>orq-agent/agents/iterator.md</files>
  <action>
Read iterator.md in full. Preserve YAML, Phase 1-9 numbering, existing Constraints, Anti-Patterns, Open in orq.ai, Documentation & Resolution.

Insertions:

1. **Phase 3.5 new section: `## Phase 3.5: Assign P0/P1/P2 Priority (ITRX-01)`** — insert between existing Phase 3 and Phase 4 (Collect Per-Agent Approval). Body MUST contain literal tokens `P0`, `P1`, `P2` (uppercase). Priority table:
   | Priority | Scope | Examples |
   |----------|-------|----------|
   | **P0** | Prompt wording, few-shot examples, constraints | Tighten `<constraints>` block, add counterexample |
   | **P1** | Decomposition, tool descriptions, RAG tuning | Split agent into two, rewrite tool schema, re-chunk KB |
   | **P2** | Model upgrade, expand eval set, fine-tuning | Swap model snapshot, add 50 new datapoints, launch FT job |
   Rule: every change in iteration-proposals.json gets a `priority: "P0"|"P1"|"P2"` field. Iteration executes P0 first; P1/P2 deferred to next cycle unless user overrides.

2. **Phase 3.6 new section: `## Phase 3.6: Action Plan Emission (ITRX-02, ITRX-07)`** — insert after 3.5. The phrase `Action Plan` MUST appear verbatim (capitalized). Body:
   - Iteration emits `{swarm_dir}/iteration-{N}-action-plan.md` with 3 H2 sections:
     - `## Summary` — 1-2 sentence diagnosis
     - `## Priority Improvements` — P0 first, table with columns: Priority | Section | Change | Evidence | Success Criteria
     - `## Re-run Criteria` — target bottleneck score threshold + required split (holdout) + stop conditions
   - Evidence field MUST cite: datapoints affected (eval_ids), current scores, run ID (literal phrase `run ID`).
   - Success Criteria field MUST cite: target re-run bottleneck score (numeric) and max iterations to achieve.
   - The phrases `Evidence` and `Success Criteria` MUST appear verbatim as column headers and body references.
   - Template: reference `orq-agent/agents/iterator/resources/action-plan-template.md` (created by Plan 07).

3. **Phase 3.7 new section: `## Phase 3.7: Absorb Annotation Comments (ITRX-09)`** — after 3.6. The phrase `annotation comment` (lowercase, unhyphenated) MUST appear verbatim.
   - Rule: when iteration-proposals.json is built, iterator fetches free-text annotation comments via MCP (`annotations-list --trace-id {id}`) for every trace cited as worst-case evidence.
   - Each diff proposal's `reason` field MUST cite the relevant annotator reasoning inline, not just pass/fail: `reason: "Annotator: 'The tone was too formal for a support context.' → soften <task_handling> register."`
   - Fallback: if no annotations exist, note `annotation comment: none (no human labels on cited traces)`.

4. **Phase 4.0 new prelude: `### No-Repeat Optimizer Rule (ITRX-05)`** — insert inside Phase 4 (Collect Per-Agent Approval), before existing Step 4.1. The phrases `no-repeat` AND `explicit override` MUST appear verbatim.
   - Rule: iterator reads `audit-trail.md` for every past iteration on the same `{agent_key, optimizer_id, prompt_hash}` triple. If the same optimizer ran against the same prompt-hash in a prior iteration, STOP with: `"No-repeat rule: optimizer {id} already ran against this prompt. Pass --override to force re-run."`
   - User must supply flag `--override` (i.e. explicit override); iterator records `override: true` in audit-trail.md for the new iteration.
   - `(Phase 42 ITRX-05)`.

5. **Phase 6.5 new section: `## Phase 6.5: Evaluator-Version A/B (EVLD-11)`** — insert between existing Phase 6 (Re-deploy) and Phase 7 (Re-test). The phrase `evaluator-version A/B` MUST appear verbatim.
   - Rule: when iteration-proposals.json includes an evaluator-quality issue, iterator attaches BOTH the current evaluator prompt AND the proposed evaluator prompt to the SAME re-test experiment as two separate columns (`eval_current`, `eval_proposed`). Each datapoint receives two judgments.
   - Output: re-test results render side-by-side per datapoint, enabling per-datapoint judgment comparison (not just aggregate score averages).
   - MCP invocation sketch (pseudocode ok): `experiments-create --evaluators '[{name: eval_current, prompt: ...}, {name: eval_proposed, prompt: ...}]'`.
   - `(Phase 42 EVLD-11)`.

6. **Phase 2.5 new section: `## Phase 2.5: Publish Decision Trees (ESCI-06)`** — insert between existing Phase 2 and Phase 3. Body MUST contain all three verbatim decision-tree names: `prompt fix vs evaluator`, `upgrade model`, `eval good enough`.
   - Rule: iterator publishes 3 inspectable decision trees at `{swarm_dir}/iteration-{N}-decision-trees.md`, rendered from the template at `orq-agent/agents/iterator/resources/decision-trees.md` (created by Plan 07).
   - 3 trees:
     - **Prompt fix vs evaluator** — "If TPR/TNR < 90% → evaluator fix. Else → prompt fix."
     - **Upgrade model?** — "If P0 exhausted AND bottleneck < target → consider P2 model upgrade."
     - **Eval good enough?** — "If TPR ≥ 90% AND TNR ≥ 90% on held-out test → evaluator validated."
   - Users inspect the decision path the iterator took. No auto-promotion without HITL.

7. **Done When checklist** — append:
   - [ ] Every proposed improvement has P0/P1/P2 priority (ITRX-01)
   - [ ] Action Plan emitted per iteration with Summary + Priority Improvements + Re-run Criteria (ITRX-02)
   - [ ] Each ticket cites Evidence (datapoints, scores, run ID) and Success Criteria (target re-run score) (ITRX-07)
   - [ ] Annotation comments absorbed into diff proposal reasons (ITRX-09)
   - [ ] No-repeat optimizer rule enforced; explicit override required (ITRX-05)
   - [ ] Evaluator-version A/B attaches current + proposed evaluators as 2 columns (EVLD-11)
   - [ ] Decision trees published: prompt fix vs evaluator / upgrade model / eval good enough (ESCI-06)

Preserve all non-listed sections byte-identical. Protected pipelines not touched.
  </action>
  <verify>
    <automated>bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/iterator.md &amp;&amp; for anchor in "P0" "P1" "P2" "Action Plan" "evaluator-version A/B" "annotation comment" "no-repeat" "explicit override" "prompt fix vs evaluator" "upgrade model" "eval good enough" "Evidence" "Success Criteria" "run ID"; do grep -q "$anchor" orq-agent/agents/iterator.md || { echo "MISSING: $anchor"; exit 1; }; done</automated>
  </verify>
  <done>All 14 grep anchors present; lint exits 0; 5 new Phases added (2.5, 3.5, 3.6, 3.7, 4.0-prelude, 6.5); 7 Done When checkboxes appended.</done>
</task>

</tasks>

<verification>
Run: `bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/iterator.md` (exit 0)
Run: `bash orq-agent/scripts/check-protected-pipelines.sh` (3/3 SHA-256 match)
Grep 14 anchors listed above.
</verification>

<success_criteria>
- iterator.md lint-clean
- 14 grep anchors verbatim
- Protected pipeline SHA-256 3/3 intact
</success_criteria>

<output>
`.planning/phases/42-evaluator-validation-iterator-enrichments/42-03-SUMMARY.md`
</output>
