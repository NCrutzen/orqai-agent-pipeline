---
phase: 37-observability-setup-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/commands/observability.md
autonomous: true
requirements: [OBSV-01, OBSV-02, OBSV-04, OBSV-05, OBSV-06, OBSV-07]
must_haves:
  truths:
    - "Skill reports detected LLM framework (OpenAI/Anthropic/LangChain/CrewAI/Vercel AI) and prior instrumentation state"
    - "Skill recommends integration mode (AI Router default / OTEL-only / both) with written rationale tied to detection"
    - "Skill emits baseline-verification test script spec (trace appears, model+tokens, span hierarchy, PII scan) user can run"
    - "Skill guides trace enrichment with session_id, user_id, customer_id, feature tags"
    - "Skill shows @traced decorator examples for all 6 span types (agent/llm/tool/retrieval/embedding/function)"
    - "Skill documents identity attribution and forward-references /orq-agent:traces --identity"
  artifacts:
    - path: "orq-agent/commands/observability.md"
      provides: "Primary observability setup skill file"
      contains: "ORQ ► OBSERVABILITY"
  key_links:
    - from: "orq-agent/commands/observability.md"
      to: "orq-agent/commands/observability/resources/<framework>.md"
      via: "Step delegation (Plan 02 creates these)"
      pattern: "observability/resources/"
    - from: "orq-agent/commands/observability.md"
      to: "/orq-agent:traces --identity"
      via: "Forward reference for identity filter"
      pattern: "identity"
---

<objective>
Create `orq-agent/commands/observability.md` — the single-file skill invoked as `/orq-agent:observability`. This file carries all 7 OBSV capabilities except Plan 02's per-framework resource content and Plan 03's traces.md wiring.

Purpose: Users instrument their LLM application so downstream Phase 38 trace-failure analysis and Phase 42 evaluator validation have signal.
Output: One SKST-conformant command skill file.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-observability-setup-skill/37-CONTEXT.md
@.planning/phases/37-observability-setup-skill/37-VALIDATION.md
@orq-agent/commands/help.md
@orq-agent/commands/traces.md
@orq-agent/scripts/lint-skills.sh

<interfaces>
From orq-agent/commands/help.md (SKST-9 template — follow its section order):
- Frontmatter: `description:`, `allowed-tools:`, optional `argument-hint:`
- H1 command name, opening paragraph
- H2 sections in this order: Constraints, When to use, When NOT to use, Companion Skills, Done When, Destructive Actions, Step 1..N (body), Anti-Patterns, Open in orq.ai, Documentation & Resolution

From orq-agent/commands/traces.md (Phase 36) — the `--identity <id>` flag currently emits a stub warning. Phase 37 Plan 03 wires it live; this skill (Plan 01) forward-references `/orq-agent:traces --identity <id>` as the retrieval surface.

Linter default file set (orq-agent/scripts/lint-skills.sh line 218-222):
  orq-agent/SKILL.md + orq-agent/commands/*.md + orq-agent/agents/*.md
  → Single-level glob; `commands/observability/resources/*.md` is NOT scanned. Safe for Plan 02.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create observability.md skeleton + detection + mode + enrichment + @traced (OBSV-01,02,04,05,06)</name>
  <files>orq-agent/commands/observability.md</files>
  <read_first>
    - orq-agent/commands/help.md (SKST-9 template exemplar)
    - orq-agent/commands/traces.md (for forward-reference wording to /orq-agent:traces --identity)
    - .planning/phases/37-observability-setup-skill/37-CONTEXT.md (locked decisions)
    - orq-agent/scripts/lint-skills.sh (required sections list lines 10-20)
  </read_first>
  <action>
Create `orq-agent/commands/observability.md` with this exact frontmatter:

```
---
description: Instrument your LLM application for Orq.ai trace capture — detect framework, recommend integration mode, emit baseline verification, guide @traced decorators and identity attribution
allowed-tools: Bash, Read, Write, Edit, Glob, Grep, AskUserQuestion, WebFetch, mcp__orqai-mcp__list_traces
argument-hint: "[--mode <ai-router|otel|both>] [--framework <openai|anthropic|langchain|crewai|vercel-ai>]"
---
```

H1: `# Observability Setup`. Opening paragraph: "You are running the `/orq-agent:observability` command. This command walks users through instrumenting their LLM application for Orq.ai trace capture: framework detection, integration mode recommendation, baseline verification, trace enrichment, @traced decorator placement, and per-tenant identity attribution."

Then emit these H2 sections in order (section headings MUST match `orq-agent/scripts/lint-skills.sh` REQUIRED_SECTIONS exactly):

**## Constraints** — NEVER/ALWAYS bullets:
- NEVER auto-inject decorators into the user's codebase — emit code for user to paste.
- NEVER claim a framework is present without grep evidence — if ambiguous, AskUserQuestion.
- NEVER skip the baseline verification step — traces that don't appear silently are worse than no instrumentation.
- NEVER recommend "both" (AI Router + OTEL) without an explicit user ask — default AI Router when a supported SDK is detected.
- ALWAYS import instrumentors BEFORE SDK clients (OBSV-03 correctness requirement).
- ALWAYS run the PII scan (email/phone/SSN/credit-card seeds) against emitted trace bodies before declaring baseline green.
- ALWAYS recommend `/orq-agent:traces --identity <id>` as the retrieval surface for per-tenant filtering.

"**Why these constraints:**" paragraph explaining instrumentation order bugs are the #1 silent failure mode, and baseline verification is the Nyquist gate for Phase 38 downstream.

**## When to use** — bullets: new LLM project onboarding; existing project without traces; adding per-tenant attribution to a traced project; verifying instrumentation after a framework upgrade.

**## When NOT to use** — bullets: user wants aggregated analytics → `/orq-agent:analytics`; user wants to inspect existing traces → `/orq-agent:traces`; user wants to build evaluators on traces → Phase 38 `/orq-agent:analyze-failures` (forward link `TODO(TFAIL)`).

**## Companion Skills** — Directional:
- → `/orq-agent:traces` — once instrumented, this is how users inspect the resulting traces (especially with `--identity`).
- → Phase 38 trace-failure analysis (forward link `TODO(TFAIL)`) — consumes the tagged traces this skill produces.
- ← `/orq-agent:quickstart` — Step 6 (analyze traces) delegates here when instrumentation is missing.
- ← user invocation — new-project onboarding entry point.

**## Done When** — falsifiable checklist:
- [ ] Detection report rendered as a table (framework | version | existing instrumentation | confidence)
- [ ] Mode recommendation printed with written rationale tied to detection output
- [ ] Per-framework integration code emitted via delegation to `orq-agent/commands/observability/resources/<framework>.md`
- [ ] Baseline verification script spec emitted (TS or Python variant chosen from detection)
- [ ] @traced decorator examples printed for all 6 span types (agent/llm/tool/retrieval/embedding/function)
- [ ] Identity attribution example emitted with forward-reference to `/orq-agent:traces --identity <id>`

**## Destructive Actions**:
- **None** — this command is read-only and emits code snippets; it never writes to the user's project without explicit paste by the user.

**## Step 1: Detect Framework (OBSV-01)** —
Use `Grep` tool across the user's project (exclude `node_modules`, `.venv`, `dist`):

```
# Python frameworks
grep -rlE '^(from|import)[[:space:]]+(openai|anthropic|langchain|crewai|llama_index)' --include='*.py' .
# TypeScript/JavaScript
grep -rlE "from ['\"]@?(openai|@anthropic-ai/sdk|langchain|@ai-sdk|openai-agents)" --include='*.ts' --include='*.js' --include='*.tsx' .
# Existing instrumentation (avoid double-instrumenting)
grep -rlE '(from[[:space:]]+traceloop|opentelemetry|@orq-ai/node|orq\.decorators)' --include='*.py' --include='*.ts' --include='*.js' .
```

Render results as a table:
```
| Framework | Language | Files Found | Existing Instrumentation |
|-----------|----------|-------------|--------------------------|
| OpenAI SDK | Python | src/agents/*.py (3 files) | none |
| Vercel AI SDK | TypeScript | app/api/chat/route.ts | traceloop (v0.18) |
```

If zero frameworks detected, use `AskUserQuestion` with choices: OpenAI SDK / Anthropic SDK / LangChain / CrewAI / Vercel AI SDK / Other.

**## Step 2: Recommend Integration Mode (OBSV-02)** —
Decision tree (hardcoded in the skill — LLM executes it):
- Detected a supported SDK (OpenAI / Anthropic / Vercel AI) AND no existing OTEL → **AI Router** (zero-code; default).
- Detected heavy existing OTEL (Grafana/Tempo/Datadog pipelines, `opentelemetry-sdk` in deps) → **OTEL-only** (unified observability).
- User explicitly asked for cost routing AND aggregation → **Both** (rare).
- Otherwise (LangChain/CrewAI without OTEL) → **AI Router** with framework-specific instrumentor.

Print the recommendation with written rationale tied to Step 1 output (1-2 sentences).

**## Step 3: Emit Integration Code (OBSV-03 — DELEGATES to Plan 02 resources)** —
Based on the detected framework + chosen mode, instruct the LLM to `Read` the matching resource file and emit its snippet verbatim:

| Framework | Resource File |
|-----------|---------------|
| OpenAI SDK | `orq-agent/commands/observability/resources/openai-sdk.md` |
| LangChain | `orq-agent/commands/observability/resources/langchain.md` |
| CrewAI | `orq-agent/commands/observability/resources/crewai.md` |
| Vercel AI SDK | `orq-agent/commands/observability/resources/vercel-ai.md` |
| OTEL-only / generic | `orq-agent/commands/observability/resources/generic-otel.md` |

Constraint reminder: instrumentors MUST be imported before SDK clients. Each resource file's snippet enforces this.

**## Step 4: Baseline Verification (OBSV-04)** —
Generate a verification test script (choose TS or Python based on Step 1 detected language). The script MUST:
1. Invoke the user's LLM once with a canned prompt containing test values (no real secrets).
2. Poll `mcp__orqai-mcp__list_traces` with `limit: 5, since: <now - 2m>` until the new trace appears (max 30s).
3. Assert `trace.model` is present and non-empty.
4. Assert `trace.usage.total_tokens > 0`.
5. Walk `trace.spans[]` and assert at least one parent-child link exists (span hierarchy).
6. Run the PII scan regex suite over the serialized trace body:
   - Email: `[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}`
   - US phone: `\(?[2-9]\d{2}\)?[ .-]?\d{3}[ .-]?\d{4}`
   - SSN: `\d{3}-\d{2}-\d{4}`
   - Credit-card-like: `\b(?:\d[ -]*?){13,16}\b`
   Print WARN (not FAIL) for any match — user decides whether it's test data or a real leak.

Emit the script to `verify-orq-traces.{ts,py}` in the user's cwd via `Write`, then instruct user to run it and paste the output.

**## Step 5: Enrich Traces (OBSV-05)** —
Emit a table of enrichment attributes + per-framework helper call:

```
| Attribute | Purpose | OpenAI SDK (Python) | Vercel AI SDK (TS) |
|-----------|---------|---------------------|---------------------|
| session_id | Group traces for one user session | `set_session(session_id=...)` | `setSession({ sessionId })` |
| user_id | End-user attribution | `set_user(user_id=...)` | `setUser({ userId })` |
| customer_id | B2B tenant attribution | `set_customer(customer_id=...)` | `setCustomer({ customerId })` |
| feature_tags | Flag experiments / A/B arms | `set_tags(["checkout-v2"])` | `setTags(['checkout-v2'])` |
```

Also guide the user to set these from their auth middleware / request context, never hardcoded.

**## Step 6: @traced Decorator Placement (OBSV-06)** —
Emit concrete Python + TypeScript examples for all 6 span types. Python block:

```python
from orq.decorators import traced

@traced(span_type="agent")
def triage_agent(query: str) -> str: ...

@traced(span_type="llm")
def call_model(prompt: str) -> str: ...

@traced(span_type="tool")
def db_lookup(key: str) -> dict: ...

@traced(span_type="retrieval")
def search_kb(query: str) -> list[dict]: ...

@traced(span_type="embedding")
def embed_text(text: str) -> list[float]: ...

@traced(span_type="function")
def compute_rollup(rows: list) -> dict: ...
```

TypeScript block:

```ts
import { traced } from '@orq-ai/node';

export const triageAgent = traced({ spanType: 'agent' }, async (query: string) => { /* ... */ });
export const callModel    = traced({ spanType: 'llm' },    async (prompt: string) => { /* ... */ });
export const dbLookup     = traced({ spanType: 'tool' },   async (key: string) => { /* ... */ });
export const searchKb     = traced({ spanType: 'retrieval' }, async (q: string) => { /* ... */ });
export const embedText    = traced({ spanType: 'embedding' }, async (t: string) => { /* ... */ });
export const computeRollup = traced({ spanType: 'function' }, async (rows: any[]) => { /* ... */ });
```

Guidance bullets: one decorator per span boundary; never nest `llm` inside `llm`; use `function` for pure utilities; use `agent` for the outermost orchestration call.

**## Step 7: Identity Attribution (OBSV-07)** —
Emit TS + Python snippets attaching per-tenant identity:

```ts
import { setIdentity } from '@orq-ai/node';
setIdentity({ customerId: 'acme-corp', tenantId: 'eu-1' });
```

```python
from orq import set_identity
set_identity(customer_id='acme-corp', tenant_id='eu-1')
```

Then forward-reference: "Once identity attributes are attached, filter traces per-tenant via `/orq-agent:traces --identity acme-corp` (wired in Phase 37 Plan 03)."

Use the literal string "per-tenant" at least once and "identity" multiple times so the VALIDATION grep anchors pass.

**## Anti-Patterns** — table with at least 5 rows:
| Pattern | Do Instead |
|---------|-----------|
| Importing SDK client before instrumentor | Instrumentors MUST be imported first — they monkey-patch the client at import time |
| Using a floating model alias in traced calls | Pin snapshots per Phase 35 MSEL-02 (`claude-sonnet-4-5-20250929`) so trace model fields are diff-able |
| Hardcoding customer_id in source | Set from auth middleware / request context; hardcoding leaks tenants across requests |
| Skipping baseline verification "because it looked right" | If traces don't appear, they don't exist — the PII scan + hierarchy check is the Nyquist gate |
| Wrapping every function in `@traced(span_type="function")` | Span noise drowns signal — decorate span BOUNDARIES (agent/llm/tool/retrieval), not every internal call |
| Treating `identity` as optional for B2B apps | Per-tenant attribution is the billing-margin signal; without it, cost reports aggregate across customers |

**## Open in orq.ai**:
- **Traces:** https://my.orq.ai/traces
- **AI Router:** https://my.orq.ai/ai-router
- **Agent Studio:** https://my.orq.ai/agents

**## Documentation & Resolution** — standard 4-entry trust order (MCP tools > docs MCP > docs.orq.ai > this skill file).

Use banner `ORQ ► OBSERVABILITY` at the top of Step 1's output (not in the skill prose itself — in the runtime output instructions, matching traces.md pattern).

REQUIRED grep anchors must all pass (these drive VALIDATION.md):
- `grep -q "AI Router" orq-agent/commands/observability.md`
- `grep -q "OTEL" orq-agent/commands/observability.md`
- `grep -q 'span_type="agent"' orq-agent/commands/observability.md`
- `grep -q 'span_type="llm"' orq-agent/commands/observability.md`
- `grep -q 'span_type="tool"' orq-agent/commands/observability.md`
- `grep -q 'span_type="retrieval"' orq-agent/commands/observability.md`
- `grep -q 'span_type="embedding"' orq-agent/commands/observability.md`
- `grep -q 'span_type="function"' orq-agent/commands/observability.md`
- `grep -q "session_id" orq-agent/commands/observability.md`
- `grep -q "customer_id" orq-agent/commands/observability.md`
- `grep -q "identity" orq-agent/commands/observability.md`
- `grep -q "per-tenant" orq-agent/commands/observability.md`

Do NOT extract a subagent unless the file exceeds ~400 lines. If it does, create `orq-agent/agents/observability-setup.md` and note this in SUMMARY.
  </action>
  <verify>
    <automated>test -f orq-agent/commands/observability.md && bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/observability.md && grep -q "AI Router" orq-agent/commands/observability.md && grep -q "OTEL" orq-agent/commands/observability.md && grep -q 'span_type="agent"' orq-agent/commands/observability.md && grep -q 'span_type="llm"' orq-agent/commands/observability.md && grep -q 'span_type="tool"' orq-agent/commands/observability.md && grep -q 'span_type="retrieval"' orq-agent/commands/observability.md && grep -q 'span_type="embedding"' orq-agent/commands/observability.md && grep -q 'span_type="function"' orq-agent/commands/observability.md && grep -q "session_id" orq-agent/commands/observability.md && grep -q "customer_id" orq-agent/commands/observability.md && grep -q "identity" orq-agent/commands/observability.md && grep -q "per-tenant" orq-agent/commands/observability.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at orq-agent/commands/observability.md
    - Per-file lint exit 0 (all 9 SKST sections present, allowed-tools non-empty)
    - All 12 grep anchors above pass
    - All 6 span types decorated (agent/llm/tool/retrieval/embedding/function)
    - Step 3 references `observability/resources/` path (delegation hook for Plan 02)
    - Step 7 forward-references `/orq-agent:traces --identity`
  </acceptance_criteria>
  <done>observability.md is a SKST-conformant command skill addressing OBSV-01, 02, 04, 05, 06, and documenting OBSV-07 prose + forward-reference. Passes per-file lint.</done>
</task>

</tasks>

<verification>
Per-file lint: `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/observability.md` exits 0.
Protected pipelines: `bash orq-agent/scripts/check-protected-pipelines.sh` still exits 0 (Plan 01 touches no protected file).
</verification>

<success_criteria>
- orq-agent/commands/observability.md exists, passes SKST lint, and contains all 12 VALIDATION grep anchors.
- Delegation hook to `observability/resources/<framework>.md` present (Plan 02 fills these).
- Forward-reference to `/orq-agent:traces --identity` present (Plan 03 wires this live).
</success_criteria>

<output>
Create `.planning/phases/37-observability-setup-skill/37-01-SUMMARY.md` recording: file created, lint exit 0, all grep anchors verified, decision on subagent extraction (file line count + whether subagent was spawned).
</output>
