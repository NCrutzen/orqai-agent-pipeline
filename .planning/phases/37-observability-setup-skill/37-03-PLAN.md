---
phase: 37-observability-setup-skill
plan: 03
type: execute
wave: 2
depends_on: [37-01, 37-02]
files_modified:
  - orq-agent/commands/traces.md
autonomous: true
requirements: [OBSV-07]
must_haves:
  truths:
    - "The TODO(OBSV-07) marker is removed from orq-agent/commands/traces.md"
    - "The --identity <id> flag is now a live MCP pass-through filter (no longer a parse-only stub)"
    - "All 9 SKST sections remain intact in traces.md (per-file lint exit 0)"
    - "Phase 36 LCMD-02 behavior (errors-first, full IDs, MCP-first with REST fallback) is preserved"
  artifacts:
    - path: "orq-agent/commands/traces.md"
      provides: "LCMD-02 skill with OBSV-07 identity filter wired live"
      contains: "identity"
  key_links:
    - from: "orq-agent/commands/traces.md (Step 2)"
      to: "mcp__orqai-mcp__list_traces"
      via: "identity filter parameter pass-through"
      pattern: "identity"
    - from: "orq-agent/commands/traces.md"
      to: "orq-agent/commands/observability.md"
      via: "Companion Skills back-reference — observability produces identity-tagged traces that this skill filters"
      pattern: "observability"
---

<objective>
Replace the Phase 36 TODO(OBSV-07) stub in `orq-agent/commands/traces.md` with a live `--identity <id>` filter that passes through to `mcp__orqai-mcp__list_traces` (and the REST `/v2/traces` fallback). Preserve LCMD-02 invariants (errors-first, full trace IDs, MCP-first, never fabricate).

Purpose: Closes OBSV-07 retrieval surface — once `observability.md` guides users to attach `identity` attributes, this filter makes per-tenant trace queries work end-to-end.
Output: Updated `orq-agent/commands/traces.md` with no TODO(OBSV-07) remaining.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-observability-setup-skill/37-CONTEXT.md
@orq-agent/commands/traces.md
@orq-agent/scripts/lint-skills.sh

<interfaces>
From orq-agent/commands/traces.md (Phase 36) — the file currently has these OBSV-07-specific locations that must change:

1. Header `argument-hint:` line already includes `[--identity <id>]` — KEEP as is.
2. Step 1 "Parse Arguments" table row: `--identity <id> ... **STUB** — parsed but currently a no-op` — UPDATE to describe live behavior.
3. Step 1 parse rule paragraph:
```
- If `--identity <id>` is provided, emit this one-line warning **before** fetching and proceed without filtering:
  ⚠ --identity is a stub (TODO(OBSV-07)); filter ignored. Wiring lands in Phase 37.
```
REPLACE with: identity is parsed and passed to the MCP tool; no warning.

4. Step 2 "Fetch Traces" ends with:
```
**Never** apply the `--identity` filter (stub — Phase 37 wires this into either the MCP param or a client-side filter...).
```
REPLACE with live wiring instructions.

5. "Done When" bullet:
```
- [ ] If `--identity <id>` was passed, the one-line `TODO(OBSV-07)` warning was emitted and the filter was ignored (parse-only stub).
```
REPLACE with live-behavior bullet.

6. Anti-Patterns row `Treating --identity as implemented` — REPLACE with the inverse (treating it as unfiltered / forgetting to pass it through).

7. Companion Skills — ADD back-reference to `/orq-agent:observability` as the producer of identity-tagged traces.

All 9 SKST sections (Constraints, When to use, When NOT to use, Companion Skills, Done When, Destructive Actions, Anti-Patterns, Open in orq.ai, Documentation & Resolution) MUST remain present and intact.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire --identity live in traces.md and remove TODO(OBSV-07)</name>
  <files>orq-agent/commands/traces.md</files>
  <read_first>
    - orq-agent/commands/traces.md (current full file — all 7 stub locations listed in interfaces)
    - .planning/phases/37-observability-setup-skill/37-CONTEXT.md (decisions: MCP pass-through; client-side fallback)
    - orq-agent/scripts/lint-skills.sh (REQUIRED_SECTIONS lines 10-20)
  </read_first>
  <action>
Edit `orq-agent/commands/traces.md` at the 7 locations listed in `<interfaces>`. Keep every other line byte-identical.

**Edit 1 — Step 1 argument table row for `--identity`:**

Replace:
```
| `--identity <id>` | optional | none | **STUB** — parsed but currently a no-op; wiring happens in Phase 37 / OBSV-07. |
```

With:
```
| `--identity <id>` | optional | none | Filter to traces tagged with this per-tenant / per-customer identity attribute. Passed through to MCP `list_traces` as `identity=<id>`. |
```

**Edit 2 — Step 1 parse rule paragraph for `--identity`:**

Replace the entire block:
```
- If `--identity <id>` is provided, emit this one-line warning **before** fetching and proceed without filtering:

  ```
  ⚠ --identity is a stub (TODO(OBSV-07)); filter ignored. Wiring lands in Phase 37.
  ```
```

With:
```
- If `--identity <id>` is provided, pass it through to the MCP `list_traces` tool as the `identity` parameter (see Step 2). If the MCP response does not expose an `identity` parameter, apply a client-side filter over the returned rows matching `trace.metadata.identity == <id>` OR `trace.attributes.identity == <id>` OR `trace.customer_id == <id>` (identity attribution surface can land on any of these per `/orq-agent:observability` Step 7). If zero rows remain after the client-side filter, print `No traces matched --identity <id> (did you attach identity via /orq-agent:observability?).`
```

**Edit 3 — Step 2 "Fetch Traces" — update MCP tool call block:**

Replace:
```
mcp__orqai-mcp__list_traces(
  since: <now - LAST, ISO-8601>,
  limit: $LIMIT,
  deployment_key: $DEPLOYMENT,      # omit if unset
  status: $STATUS                   # omit if unset
)
```

With:
```
mcp__orqai-mcp__list_traces(
  since: <now - LAST, ISO-8601>,
  limit: $LIMIT,
  deployment_key: $DEPLOYMENT,      # omit if unset
  status: $STATUS,                  # omit if unset
  identity: $IDENTITY                # omit if unset -- per-tenant filter (OBSV-07)
)
```

**Edit 4 — Step 2 REST fallback curl:**

Replace:
```bash
curl -sS -H "Authorization: Bearer $ORQ_API_KEY" \
  "https://api.orq.ai/v2/traces?limit=${LIMIT}&since=${SINCE}&deployment_key=${DEPLOYMENT}&status=${STATUS}"
```

With:
```bash
curl -sS -H "Authorization: Bearer $ORQ_API_KEY" \
  "https://api.orq.ai/v2/traces?limit=${LIMIT}&since=${SINCE}&deployment_key=${DEPLOYMENT}&status=${STATUS}&identity=${IDENTITY}"
```

**Edit 5 — Step 2 stub-disclaimer paragraph at end of Step 2:**

Replace:
```
**Never** apply the `--identity` filter (stub — Phase 37 wires this into either the MCP param or a client-side filter, once identity attributes are attached by Phase 37 OBSV-07).
```

With:
```
When the MCP tool accepts `identity` natively, rely on server-side filtering. When it does not (older MCP versions), apply the client-side filter documented in Step 1 after fetch — never fabricate trace rows, and never silently drop the filter.
```

**Edit 6 — "Done When" checklist — replace the stub bullet:**

Replace:
```
- [ ] If `--identity <id>` was passed, the one-line `TODO(OBSV-07)` warning was emitted and the filter was ignored (parse-only stub).
```

With:
```
- [ ] If `--identity <id>` was passed, the filter was applied (MCP pass-through or client-side fallback), and result rows match the identity attribute; zero-match prints the helpful `No traces matched --identity <id>` hint.
```

**Edit 7 — Anti-Patterns table row `Treating --identity as implemented`:**

Replace:
```
| Treating `--identity` as implemented | It is a stub — emit the `TODO(OBSV-07)` warning and do not filter; wiring lands in Phase 37 |
```

With:
```
| Silently dropping `--identity` when MCP does not expose it natively | Fall back to client-side filtering over `trace.metadata.identity` / `trace.attributes.identity` / `trace.customer_id` — never return unfiltered rows when the user asked for a tenant-scoped view |
```

**Edit 8 — Companion Skills — ADD a new `←` handoff:**

In the `## Companion Skills` block, add this bullet after the existing `← /orq-agent:deploy` line:

```
- ← `/orq-agent:observability` — producer of identity-tagged traces; once that skill has attached `identity` attributes via `setIdentity()` / `set_identity()`, this command filters the resulting traces per-tenant.
```

**Post-conditions (enforced by verify):**
- `grep -c 'TODO(OBSV-07)' orq-agent/commands/traces.md` returns 0.
- `grep -c 'identity' orq-agent/commands/traces.md` returns ≥ 6 (header + 2 step refs + done-when + anti-pattern + companion).
- All 9 SKST sections still present (per-file lint exit 0).
- Phase 36 LCMD-02 anchors preserved: `grep -q 'errors-first\|errors first\|full trace ID' orq-agent/commands/traces.md`.
- No protected pipeline touched (traces.md is NOT one of the 3 protected entry points orq-agent.md / prompt.md / architect.md).
  </action>
  <verify>
    <automated>! grep -q 'TODO(OBSV-07)' orq-agent/commands/traces.md && [ "$(grep -c 'identity' orq-agent/commands/traces.md)" -ge 6 ] && bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/traces.md && grep -q 'observability' orq-agent/commands/traces.md && grep -q 'per-tenant\|per-customer' orq-agent/commands/traces.md && bash orq-agent/scripts/check-protected-pipelines.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'TODO(OBSV-07)' orq-agent/commands/traces.md` == 0
    - `grep -c 'identity' orq-agent/commands/traces.md` ≥ 6
    - `grep -q 'observability' orq-agent/commands/traces.md` succeeds (Companion Skills back-reference)
    - `grep -q 'per-tenant\|per-customer' orq-agent/commands/traces.md` succeeds
    - `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/traces.md` exits 0 (all 9 SKST sections intact)
    - `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0 (3/3 SHA-256 matches; traces.md is not protected but the sanity check must still pass)
    - Phase 36 LCMD-02 semantics preserved: `grep -q 'Errors-first\|errors first' orq-agent/commands/traces.md` still succeeds
  </acceptance_criteria>
  <done>traces.md no longer contains the TODO(OBSV-07) stub; `--identity <id>` is a live MCP pass-through with client-side fallback; per-file lint green; Phase 36 invariants preserved.</done>
</task>

</tasks>

<verification>
Per-file lint: `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/traces.md` exits 0.
Full-suite lint: `bash orq-agent/scripts/lint-skills.sh` exits 0.
Protected pipelines: `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0.
</verification>

<success_criteria>
- TODO(OBSV-07) marker is absent from traces.md.
- `--identity` is wired through MCP tool call + REST fallback + client-side fallback.
- SKST 9-section contract preserved.
- Phase 36 LCMD-02 invariants (errors-first, full IDs, MCP-first) preserved byte-for-byte outside the 8 documented edits.
</success_criteria>

<output>
Create `.planning/phases/37-observability-setup-skill/37-03-SUMMARY.md` recording: 8 edit locations applied, grep counts before/after, lint exit codes, confirmation that traces.md is not a protected pipeline.
</output>
