---
phase: 82.6-footer-approve-recordverdict-wiring
plan: 02
subsystem: debtor-email/_shell/detail-pane
tags: [debtor-email, stage-1, detail-pane, approve-flow, optimistic-removal]
dependency_graph:
  requires:
    - "Plan 01 — approvePrediction server-action wrapper in stage-1/actions.ts"
    - "selection-context.tsx — markPendingRemoval setter (pre-existing)"
  provides:
    - "Footer Approve click now flips automation_runs.status (Stage 1)"
    - "Stage 0/2/3/4 footer no longer shows the Approve button"
  affects:
    - "Stage 1 Bulk Review queue — rows leave the list on Approve"
tech_stack:
  added: []
  patterns:
    - "Dual-write mirroring override-flow precedent (stage-1-widget.tsx:170-200)"
    - "Optimistic removal via pendingRemovalIds (D-04)"
    - "Non-fatal silent recovery on recordVerdict failure (D-03)"
    - "Per-stage conditional render of primary action button (D-02b)"
key_files:
  created:
    - ".planning/phases/82.6-footer-approve-recordverdict-wiring/82.6-02-SUMMARY.md"
  modified:
    - "web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx"
decisions:
  - "Sequential ordering (feedback POSTs → approvePrediction → markPendingRemoval) mirrors stage-1-widget.tsx:161→174→200 exactly"
  - "No success toast — row vanishing is the operator's signal (override path's toast would be noise on every Approve)"
  - "Hide rather than disable on Stages 0/2/3/4 — disabled button gives wrong signal per D-02b"
  - "Gate the button, not handlePrimary itself — single entry point, belt-and-suspenders not needed"
metrics:
  duration: "~5 minutes"
  completed: "2026-05-15"
  tasks_completed: 2
  files_modified: 1
---

# Phase 82.6 Plan 02: Wire footer Approve → approvePrediction + hide on non-Stage-1 — Summary

One-liner: Footer "✓ Approve (Stages X+Y)" on Stage 1 now calls `approvePrediction` (Plan 01 wrapper) and optimistically removes the row via `markPendingRemoval`; the primary Approve button is hidden entirely on Stages 0/2/3/4.

## What Was Built

Three coordinated edits inside `web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx`:

1. **Imports added** (lines 56-57):
   ```typescript
   import { approvePrediction } from "../stage-1/actions";
   import { useSelection } from "./selection-context";
   ```

2. **markPendingRemoval destructured** in `DetailPaneInner` body (line 240):
   ```typescript
   const { markPendingRemoval } = useSelection();
   ```

3. **`handlePrimary` approve branch extended** (lines ~482-512) — preserves the existing feedback POST loop, then:
   - `try { await approvePrediction({ row_id, swarm_type, decision: "approve" }) } catch {}` — non-fatal per D-03
   - `markPendingRemoval(row.id)` — optimistic removal per D-04

4. **Footer JSX guarded** (line 647) — primary `<Button data-testid="detail-pane-primary">` wrapped in `{activeStage === 1 ? (...) : null}`. Reject + Skip siblings untouched.

## Done Criteria Verification

| Check | Result |
|------|--------|
| `grep -n "import { approvePrediction }"` | 1 match (line 56) |
| `grep -n "import { useSelection }"` | 1 match (line 57) |
| `grep -n "markPendingRemoval(row.id)"` | 1 match (line 510) |
| `grep -n "await approvePrediction"` | 1 match (line 493) |
| `approvePrediction` inside `try { ... } catch { ... }` | yes (catch is empty per D-03) |
| `grep -n "activeStage === 1 ?"` | match at line 647 (footer ternary) |
| `grep -c 'data-testid="detail-pane-primary"'` | 1 (single guarded definition) |
| `npx tsc --noEmit` | no errors for detail-pane.tsx |

## Hard-Separation Lock Audit

- No `swarm_intents` reference added (zero matches in modified file).
- `approvePrediction` (Plan 01) delegates only to `recordVerdict`, which is Stage-1-pure (`swarm_noise_categories` only).
- Footer Approve is now hidden on Stage 3, eliminating any UI-level confusion about Stage 3 dispatch — Phase 80's inline widget actions remain the canonical Stage 3 path.

## Deviations from Plan

None — plan executed exactly as written. Both edits used the exact code shapes provided in the `<action>` blocks.

## Commits

- `a45fe21` feat(82.6-02): wire approvePrediction + markPendingRemoval into footer Approve
- `65e820c` feat(82.6-02): hide footer Approve button on Stages 0/2/3/4 (D-02b)

## Self-Check: PASSED

- FOUND: web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx (modified, +32/-15 lines net)
- FOUND: commit `a45fe21` in `git log` (Task 1)
- FOUND: commit `65e820c` in `git log` (Task 2)
- VERIFIED: all 4 grep done-criteria for Task 1 produce expected single matches
- VERIFIED: Task 2 done-criteria — ternary wrapper present, single primary definition
- VERIFIED: `npx tsc --noEmit` produced no errors for target file
- VERIFIED: override branch `if (anyDirty)` left untouched (window-event dispatch preserved)
