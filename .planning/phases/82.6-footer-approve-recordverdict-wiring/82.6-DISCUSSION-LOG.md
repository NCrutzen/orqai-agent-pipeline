# Phase 82.6: Footer Approve → recordVerdict wiring - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-15
**Phase:** 82.6-footer-approve-recordverdict-wiring
**Areas discussed:** D-01 Coupling, D-02 Per-stage scope, D-03 Failure UX, D-04 Optimistic UI

---

## D-01: How should detail-pane get the recordVerdict args?

| Option | Description | Selected |
|--------|-------------|----------|
| New server-action wrapper `approvePrediction(row_id, decision)` | Thin server action: takes row_id + decision, resolves automation_run + message_id + source_mailbox + rule_key + predicted_category internally, calls recordVerdict. Detail-pane calls a single function. | ✓ |
| Detail-pane fetches /api/email-body inline | Mirrors Stage 1 widget behavior. Stage-1-specific fetch code lives in the shared shell component. | |
| Page-level shell pre-resolves and passes via prop | Stage 1 page.tsx loader pre-fetches message_id + source_mailbox; threads via a new UnifiedDetailPaneProps field. | |

**User's choice:** New server-action wrapper (recommended)
**Notes:** Wrapper location (`stage-1/actions.ts` vs `_shell/_lib/`) deferred to planner.

---

## D-02: Per-stage scope

| Option | Description | Selected |
|--------|-------------|----------|
| Stage 1 only in 82.6; Stage 3 deferred to 82.7; Stage 0/2/4 no-op | Smallest unit. | (initial pick) |
| All stages, per-stage server actions wired now | Bigger scope, one coherent rollout. | |
| Stage 1 + Stage 3 now, defer 0/2/4 | Cover the two queue-stages where progression makes sense. | |

**User's choice:** Stage 1 only — confirmed after broader-vision detour
**Notes:** User initially described a full per-stage operator workflow (Stage 0 unsafe-only filter, Stage 2 95% auto-advance + learning loop, Stage 3 approve/reject with notes, Stage 4 resolve). On checking existing code + planned milestones: Stage 2 + Stage 3 work is already owned by V9.0 (Promotion Recommender + Learning Inbox) + Phase 82.4 (capture). Genuine orphans are Stage 0 safety-review surface and Stage 4 resolve action — captured in Deferred Ideas, not 82.6 scope.

---

## D-02b: What does the footer Approve button do on Stage 0/2/3/4?

| Option | Description | Selected |
|--------|-------------|----------|
| Hide the Approve button entirely | Cleanest. Operator only sees Approve where it works. Reject + Skip still show. | ✓ |
| Keep Approve, fires feedback POST only (current) | Preserves feedback substrate but operator hits the same "nothing happens" bug. | |
| Disabled with tooltip | More visual noise; same outcome as hide. | |

**User's choice:** Hide on Stage 0/2/3/4
**Notes:** Stage 3 retains its own Phase 80 dispatcher path via inline widget actions — not the footer.

---

## D-03: If recordVerdict fails after the feedback POST succeeded?

| Option | Description | Selected |
|--------|-------------|----------|
| Match existing widget pattern: non-fatal silent recovery | Precedent in stage-1-widget.tsx:188-191. Row reconciles via broadcast on next server roundtrip. | ✓ |
| Toast on failure: "Approved — row will refresh shortly" | Same recovery, explicit confirmation. | |
| Strict ordering: recordVerdict first, feedback only on success | Stricter consistency; loses operator-intent signal if recordVerdict fails. | |
| Toast on failure + retry button | Most explicit; more UX work. | |

**User's choice:** Match widget pattern (non-fatal, silent)
**Notes:** No special UX for the failure path. Feedback row is durable; status flip reconciles eventually.

---

## D-04: Should the row disappear immediately on click, or wait for the realtime broadcast?

| Option | Description | Selected |
|--------|-------------|----------|
| Optimistic local removal via pendingRemovalIds | row-list.tsx:38 already filters by pendingRemovalIds. Add row.id to the set on approve click → row vanishes instantly. | ✓ |
| Wait for realtime broadcast | 200-1000ms latency, no flicker risk on edge-case failure. | |
| Optimistic + auto-select next row | Higher polish; more wiring. | |

**User's choice:** Optimistic removal via pendingRemovalIds
**Notes:** Existing infrastructure handles the visibility filter. No new context state.

---

## Claude's Discretion

Items where the user did not specify and the planner can decide:

- Wrapper file location (`stage-1/actions.ts` extension vs `_shell/_lib/approve-prediction.ts`)
- Hide-vs-disable visual treatment for the Stage 0/2/3/4 footer (D-02b)
- Whether to show a `toast.success("Approved")` on success — precedent in stage-1-widget.tsx:193 suggests yes, but not specified
- Parallel-vs-sequential ordering of feedback POST + approvePrediction call inside handlePrimary

## Deferred Ideas

The user's broader operator-workflow vision came up during D-02 discussion. Most of it is owned by other phases:

**Owned elsewhere (do not duplicate):**
- Stage 2 confidence gate + 95% auto-advance + learning loop → **V9.0** Promotion Recommender + Learning Inbox synthesis layer
- Stage 3 approve/reject with notes → substrate by Phase 82.4 (shipped); synthesis by V9.0; dispatch by Phase 80
- Stage 2 incorrect-mapping form / Stage 3 confirm-or-correct form → Phase 82.4 (shipped)

**Orphaned (no planned phase — captured for future):**
- Stage 0 unsafe-only default + safety-review surface (stage-0/page.tsx:60-64 explicitly says "not yet built")
- Stage 4 resolve action on handler-error rows (existence + correctness unverified)

**Pre-existing tech debt:**
- detail-pane.test.tsx:167 T8 hard-separation test failure (predates Phase 82.5; tracked but unfixed)
