---
phase: 999.5
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - web/scripts/smoke-orq-deployments.ts
  - .planning/phases/999.5-switch-stage-0-1-classifiers-router-to-deployments-cost-tracking/wave-0-orq-deployments-smoke.md
  - web/lib/automations/orq-agents/__tests__/client.test.ts
  - web/lib/stage-0/__tests__/llm-verdict.test.ts
  - web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts
autonomous: false
requirements: [REQ-DEP-01, REQ-DEP-03]
tags: [phase-999.5, wave-0, orq-deployments, smoke, test-scaffold, cost-tracking]

must_haves:
  truths:
    - "POST /v2/deployments/invoke returns a parseable per-call cost field on the response body (resolves A1) — OR the phase premise is invalid and replanning is required"
    - "Two Orq Studio Deployments exist (`stage-0-safety-classifier`, `stage-1-category-classifier`) — model = Bedrock EU Haiku 4.5 primary, Sonnet 4.5 fallback, system prompt mirrored from current orq_agents.system_prompt, response_format = JSON Schema strict with anyOf for nullable per CLAUDE.md learning 3970bad9"
    - "Studio deployment keys are kebab-case and EXACTLY match the orq_agents.agent_key (Pitfall 3 mitigation)"
    - "RED test scaffolds exist for invokeOrqDeployment (T-DEP-01..06), Stage 0 cost_cents propagation (T-DEP-07, T-DEP-09), Stage 1 cost_cents propagation (T-DEP-08, T-DEP-10) — failing imports gate Wave 1+"
    - "Latency on the smoke call (single Bedrock EU Haiku 4.5 invoke, no queue) returns < 5 s end-to-end (early A3 signal — full canary lives in Plan 03)"
  artifacts:
    - path: web/scripts/smoke-orq-deployments.ts
      provides: "One-shot tsx-runnable script that POSTs a benign canary payload to /v2/deployments/invoke and pretty-prints the full JSON response, highlighting any key containing cost/price/billing"
    - path: .planning/phases/999.5-switch-stage-0-1-classifiers-router-to-deployments-cost-tracking/wave-0-orq-deployments-smoke.md
      provides: "Captured smoke evidence: HTTP status, latency, raw response body, identified cost field path, decision (Wave 1+ proceed / escalate)"
    - path: web/lib/automations/orq-agents/__tests__/client.test.ts
      provides: "Extended test scaffolds for invokeOrqDeployment + transport dispatcher + missing-deployment_key error (T-DEP-01..06)"
    - path: web/lib/stage-0/__tests__/llm-verdict.test.ts
      provides: "Test scaffolds for Stage 0 cost_cents propagation + timeout coercion via deployments path (T-DEP-07, T-DEP-09)"
    - path: web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts
      provides: "Test scaffolds for Stage 1 cost_cents propagation + D-11 catch on deployments errors (T-DEP-08, T-DEP-10)"
  key_links:
    - from: web/scripts/smoke-orq-deployments.ts
      to: "https://api.orq.ai/v2/deployments/invoke"
      via: "raw fetch with ORQ_API_KEY bearer auth, body { key: <deployment_key>, inputs: {...}, stream: false }"
      pattern: "fetch.*v2/deployments/invoke"
    - from: .planning/phases/999.5-switch-stage-0-1-classifiers-router-to-deployments-cost-tracking/wave-0-orq-deployments-smoke.md
      to: "999.5-RESEARCH.md §Pattern 2 cost-field candidates"
      via: "documents which of {billing.total_cost, cost, provider_response.usage.total_cost, ...} is the actual path"
      pattern: "cost_field_path"
---

<objective>
Wave 0 gate. Validate the single critical [ASSUMED] claim in RESEARCH.md before any production code touches the Deployments transport:

(A1) `POST /v2/deployments/invoke` returns per-call cost in the response body — and Wave 0 captures the EXACT JSON path.

If A1 fails (no cost field anywhere in the response), the entire phase premise (cost-tracking restoration) is invalid; this plan is an explicit checkpoint that escalates to the user before Wave 1 starts.

Also create the two Orq Studio Deployments (operator-action gate) and RED test scaffolds for downstream waves so Wave 1+ executors get contracts (function signatures, expected request body, expected response parse) instead of prose.

Purpose: prevent Wave 1 (transport implementation) from being designed against unverified Deployments response behavior. Same gate shape as 999.4 Plan 01 (Wave 0 smoke + RED scaffolds).
Output: smoke evidence file with cost field path locked, two Studio deployments live, three test files extended in scaffold state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/999.5-switch-stage-0-1-classifiers-router-to-deployments-cost-tracking/999.5-RESEARCH.md
@.planning/phases/999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening/999.4-CONTEXT.md
@.planning/phases/999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening/999.4-01-PLAN.md
@.planning/phases/999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening/999.4-03-SUMMARY.md
@CLAUDE.md
@web/lib/automations/orq-agents/client.ts
@web/lib/stage-0/llm-verdict.ts
@web/lib/inngest/functions/classifier-screen-worker.ts
@web/lib/stage-0/budget-counter.ts
@supabase/migrations/20260429g_orq_agents_registry.sql
@supabase/migrations/20260507e_orq_agents_system_prompt_phase994.sql

<interfaces>
<!-- Existing exports in web/lib/automations/orq-agents/client.ts (Wave 1 will ADD, not replace) -->

```typescript
export class OrqClientTimeoutError extends Error { name: "OrqClientTimeoutError" }

export type OrqAgentRow = {
  agent_key: string;
  orqai_id: string;
  output_schema: Record<string, unknown>;
  model_config: Record<string, unknown>;
  enabled: boolean;
  system_prompt?: string; // Phase 999.4 — to be deprecated in Plan 04
  // Wave 1 will ADD: deployment_key?: string; transport?: "agents" | "router" | "deployments";
};

export type InvokeResult = {
  raw: unknown;
  agent: OrqAgentRow;
  usage: { prompt_tokens: number; completion_tokens: number; total_tokens: number };
  billing: { total_cost: number };
  cost_cents: number;
};

export async function invokeOrqAgent(agent_key: string, inputs: Record<string, unknown>, _opts?: { jsonSchemaName?: string }): Promise<InvokeResult>;
export async function invokeOrqModel(agent_key: string, inputs: Record<string, unknown>): Promise<InvokeResult>;
export const REGISTRY_CACHE_TTL_MS: 60_000;
export function __resetCacheForTests(): void;
```

<!-- Wave 1 will add: -->
<!--   export async function invokeOrqDeployment(agent_key, inputs, opts?): Promise<InvokeResult> -->
<!--   export async function invoke(agent_key, inputs): Promise<InvokeResult>  // dispatcher reading agent.transport -->
<!-- Wave 1 migration: -->
<!--   ALTER TABLE public.orq_agents ADD COLUMN deployment_key text; -->
</interfaces>

<deployments_request_response>
<!-- VERIFIED from web/node_modules/@orq-ai/node SDK (RESEARCH §Sources Primary) -->

```typescript
// REQUEST — POST https://api.orq.ai/v2/deployments/invoke
type DeploymentsInvokeRequest = {
  key: string;                                    // = orq_agents.deployment_key (kebab)
  inputs?: Record<string, unknown>;               // template variables {{var}}
  stream?: boolean;                                // PASS false EXPLICITLY (Pitfall 7)
  metadata?: Record<string, unknown>;
  extraParams?: Record<string, unknown>;
  // (other optional fields not used in this phase)
};

// RESPONSE — fields confirmed by SDK type DeploymentInvokeResponseBody
type DeploymentsInvokeResponse = {
  id: string;
  created: number;
  object: string;
  model: string;
  provider: string;
  isFinal: boolean;
  telemetry?: { traceId?: string; spanId?: string };
  finalized?: unknown;
  systemFingerprint?: string;
  retrievals?: unknown[];
  providerResponse?: unknown;          // candidate cost-field nest
  usage?: {
    prompt_tokens?: number;
    completion_tokens?: number;
    total_tokens?: number;
  };
  choices: Array<{
    index?: number;
    message: { role?: string; content?: string | null };
  }>;
  // [ASSUMED A1] one of these may carry cost — Wave 0 smoke S-1 resolves which:
  //   billing?: { total_cost?: number };
  //   cost?: number;
  //   providerResponse.usage?.total_cost?: number;
  //   ... or under a Studio-specific top-level key not yet documented.
};
```
</deployments_request_response>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create live Orq Deployments smoke script</name>
  <files>web/scripts/smoke-orq-deployments.ts</files>
  <read_first>
    - .planning/phases/999.5-switch-stage-0-1-classifiers-router-to-deployments-cost-tracking/999.5-RESEARCH.md (§Pattern 2 invokeOrqDeployment sketch, §Pitfall 1 cost field is unverified, §Pitfall 7 stream default, §Open Questions Q1)
    - .planning/phases/999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening/999.4-01-PLAN.md (precedent: smoke-orq-router-direct.ts shape — copy structure)
    - CLAUDE.md (§Orq.ai — model IDs, anyOf for nullable, 45s timeout, list_models pre-flight)
    - web/lib/automations/orq-agents/client.ts (lines 317-436 — invokeOrqModel pattern to mirror for AbortController + parser)
    - web/node_modules/@orq-ai/node/src/funcs/deploymentsInvoke.ts (line 89 — verify endpoint path)
    - web/node_modules/@orq-ai/node/src/models/operations/deploymentinvoke.ts (lines 285-339 — confirm response top-level fields)
  </read_first>
  <action>
Create `web/scripts/smoke-orq-deployments.ts` — a tsx-runnable one-shot script. Mirror the shape of the 999.4 `smoke-orq-router-direct.ts`.

Behavior:
1. Read `ORQ_API_KEY` from `process.env`. Throw if missing.
2. Read `DEPLOYMENT_KEY` from `process.env` (default to `stage-0-safety-classifier`). This script is the SAME script run twice — once per deployment after Studio creation in Task 2.
3. Build a benign canary `inputs` payload appropriate for the deployment under test:
   - For `stage-0-safety-classifier`: `{ email_id: "smoke-001", email_subject: "Hi", email_body: "Hello, this is a benign test email." }`
   - For `stage-1-category-classifier`: `{ subject: "Hi", body_text: "Hello", categories: [{ category_key: "auto_reply", display_label: "Auto reply" }, { category_key: "ooo", display_label: "Out of office" }] }`
   - Decide which inputs to send based on `DEPLOYMENT_KEY`. Print the chosen payload.
4. POST to `https://api.orq.ai/v2/deployments/invoke` with:
   - Headers: `Authorization: Bearer ${ORQ_API_KEY}`, `content-type: application/json`
   - Body: `{ key: DEPLOYMENT_KEY, inputs, stream: false }` — explicit `stream: false` per Pitfall 7.
5. Time the request (start, request_sent, response_received) using `Date.now()` deltas.
6. On non-2xx: print HTTP status + body, exit 1.
7. On 2xx: print:
   - HTTP status, latency_ms.
   - `## Top-level keys` — `Object.keys(json).sort()`.
   - `## Recursive cost-field grep` — walk the JSON and print every key whose path or name matches `/cost|price|billing/i` (case-insensitive), printing both the path and the value. Use a simple recursive function (no library).
   - `## choices[0].message.content` — raw + JSON.parse attempt.
   - `## usage` — `json.usage` object.
   - `## telemetry` — `json.telemetry` object (Studio trace ID — useful for cross-checking with Studio dashboard).
   - `## Full response (truncated 4000 chars)` — `JSON.stringify(json, null, 2).slice(0, 4000)`.
8. Exit 0.

Run command:
```
cd web && ORQ_API_KEY=$(grep '^ORQ_API_KEY=' .env.local | cut -d= -f2- | tr -d '"') DEPLOYMENT_KEY=stage-0-safety-classifier npx tsx scripts/smoke-orq-deployments.ts
```

CRITICAL: this script does NOT lock the cost-field path. It SURFACES the cost candidates so Task 3 (the human-verify checkpoint) can pick one and document it.

The script is idempotent and runnable multiple times against either deployment. Both runs (one per deployment) get captured into `wave-0-orq-deployments-smoke.md` in Task 3.
  </action>
  <verify>
    <automated>cd web && test -f scripts/smoke-orq-deployments.ts && npx tsc --noEmit scripts/smoke-orq-deployments.ts 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File `web/scripts/smoke-orq-deployments.ts` exists.
    - `grep -c "v2/deployments/invoke" web/scripts/smoke-orq-deployments.ts` returns >= 1.
    - `grep -c "stream: false\|stream:false" web/scripts/smoke-orq-deployments.ts` returns >= 1 (Pitfall 7).
    - `grep -c "cost\|price\|billing" web/scripts/smoke-orq-deployments.ts` returns >= 3 (recursive grep + diagnostics).
    - `grep -c "DEPLOYMENT_KEY" web/scripts/smoke-orq-deployments.ts` returns >= 2 (env read + body).
    - `grep -c "ORQ_API_KEY" web/scripts/smoke-orq-deployments.ts` returns >= 2 (env read + auth header).
    - `grep -c "telemetry" web/scripts/smoke-orq-deployments.ts` returns >= 1.
    - `cd web && npx tsc --noEmit scripts/smoke-orq-deployments.ts` exits 0 (compiles clean).
  </acceptance_criteria>
  <done>tsx-runnable script committed; compiles clean; ready for Task 2 operator-action + Task 3 human-verify.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create two Orq Studio Deployments (operator-action gate)</name>
  <what-built>Studio configuration outside git. Operator creates `stage-0-safety-classifier` and `stage-1-category-classifier` deployments in Orq Studio per RESEARCH §Pattern 1 steps 1-10.</what-built>
  <how-to-verify>
For EACH of the two deployments (`stage-0-safety-classifier`, `stage-1-category-classifier`):

1. **Pre-flight `list_models`** (CLAUDE.md mandate) — verify in Studio Models catalog that `aws/eu.anthropic.claude-haiku-4-5-20251001-v1:0` AND `aws/eu.anthropic.claude-sonnet-4-5-20250929-v1:0` are present. If absent, halt — model IDs may have rolled.

2. **Create the deployment** in Studio:
   - Project folder → `+` → **Deployment**
   - Name (deployment_key) — MUST be EXACTLY `stage-0-safety-classifier` (kebab, lower-case, matches `orq_agents.agent_key`). For Stage 1 use EXACTLY `stage-1-category-classifier`. (Pitfall 3.)
   - Initial Variant → primary model = `aws/eu.anthropic.claude-haiku-4-5-20251001-v1:0`
   - Fallback chain → add `aws/eu.anthropic.claude-sonnet-4-5-20250929-v1:0`. EU-only — do NOT add `openai/*` or `google-ai/*` to the chain (CLAUDE.md EU residency).
   - Temperature = `0`. max_tokens = `600`.
   - Variant prompt editor (system) → paste the current Studio prompt verbatim. SOURCE: open the existing `stage-0-safety-classifier` Agent (Agents product) in Studio, copy the system prompt, paste here. (For Stage 1, use the `stage-1-category-classifier` Agent's prompt.) DO NOT use `orq_agents.system_prompt` as the source — Studio is the SSOT (RESEARCH L-04). If those Studio Agents are gone, fall back to the DB column and flag it in the smoke MD.
   - User-message template — use a SINGLE variable `{{json_input}}` (Pitfall 5 v1 recommendation; Discretion item resolved as the lower-risk choice). The script in Task 1 sends `inputs: { ... }` with discrete keys; we will adjust the call site in Wave 1 Plan 02 to send `inputs: { json_input: JSON.stringify(rawInputs) }` to match. **Document this in the smoke MD so Wave 1 picks the matching shape.**
   - Response Format → JSON Schema → paste the schema from `orq_agents.output_schema` for the matching `agent_key`. **Verify every nullable field uses `{"anyOf": [{"type":"string"},{"type":"null"}]}` — NOT array shorthand `["string","null"]`** (CLAUDE.md learning `3970bad9`, Pitfall 4). Set `strict: true`.
   - Retry → keep Studio defaults (429/500/502/503).
   - Click **Deploy** → Major version bump.

3. **Run the smoke script against the new deployment**:
   ```
   cd web && ORQ_API_KEY=$(grep '^ORQ_API_KEY=' .env.local | cut -d= -f2- | tr -d '"') \
     DEPLOYMENT_KEY=stage-0-safety-classifier npx tsx scripts/smoke-orq-deployments.ts | tee /tmp/smoke-stage0.log
   ```
   Expected: HTTP 200, latency < 5_000 ms, `choices[0].message.content` parses as JSON conforming to `VerdictSchema` (`verdict ∈ {"safe","injection_suspected"}`, `reason: string`, `matched_span: string|null`).

   Repeat for Stage 1: `DEPLOYMENT_KEY=stage-1-category-classifier ... | tee /tmp/smoke-stage1.log` — expected response conforms to `Stage1OutputSchema`.

4. If either Studio creation fails (invalid JSON schema → "Invalid JSON detected" red banner; missing model in catalog; etc.) — halt and surface to user. Do NOT silently proceed.

5. If `mcp__orqai-mcp__create_deployment` MCP tool happens to be available in this session, the executor MAY use it instead of Studio UI for the creation step, but MUST still verify all settings above via Studio UI inspection. RESEARCH A2 + Pitfall 2 keep this optional.
  </how-to-verify>
  <resume-signal>Type "deployments live" if both Studio deployments are deployed AND both smoke runs returned HTTP 200 with parseable JSON, OR "deployment failed: <reason>" otherwise.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Capture smoke evidence + identify cost field path (Wave 0 GATE)</name>
  <what-built>Smoke evidence file `wave-0-orq-deployments-smoke.md` capturing the run output from BOTH deployments and locking the JSON path that becomes `InvokeResult.cost_cents` in Wave 1.</what-built>
  <how-to-verify>
1. Create `.planning/phases/999.5-switch-stage-0-1-classifiers-router-to-deployments-cost-tracking/wave-0-orq-deployments-smoke.md` with the following sections:
   - `## Smoke run timestamp` — ISO date.
   - `## Stage 0 deployment smoke` — paste full stdout from `/tmp/smoke-stage0.log`.
   - `## Stage 1 deployment smoke` — paste full stdout from `/tmp/smoke-stage1.log`.
   - `## A1 (cost field present) — VERDICT` — PASS / FAIL.
     - If PASS: write the EXACT JSON path (e.g. `json.billing.total_cost`, or `json.cost`, or `json.providerResponse.usage.total_cost`, or whatever the recursive grep surfaced). Include the numeric value observed and confirm it converts to a sensible cents amount via `Math.round(value * 100)`.
     - If FAIL: write what the recursive grep DID find (token counts only? telemetry only?) and explicitly state "Wave 1 BLOCKED — replan required".
   - `## A3 early signal — Studio queue absent?` — note latency_ms for each smoke. If both < 5_000 ms (Bedrock EU Haiku 4.5 typical = 1.6 s), mark "early A3 signal: PASS — full A3 verification in Plan 03 canary". If either > 10_000 ms, surface as a yellow flag (does not block Wave 1 — Plan 03 canary is the real A3 gate).
   - `## Studio template variable` — record which template shape both deployments use (`{{json_input}}` vs discrete `{{email_id}}`...). This determines the `inputs` shape in Wave 1 Plan 02.
   - `## Studio system prompts source` — record where the prompt was sourced from (Studio Agent vs DB column) per Task 2 step 2.
   - `## Stage 1 cost_cents storage location` — LOCK the field choice for Wave 2 Plan 03 Task 2: either `agent_runs.result.cost_cents` (mirrors Stage 0 pattern, single jsonb column) OR `agent_runs.tool_outputs.cost_cents` (preserves tool-output ergonomics). Pick ONE based on: existing 999.4 baseline, downstream readers (`budget-counter.ts`, Bulk Review surfaces), and migration impact. Plan 03 Task 2 reads this section literally — do not leave ambiguous.
   - `## Decision` — `Wave 1+ proceed (A1 PASS)` OR `BLOCKED — replan (A1 FAIL)`.

2. **GATE LOGIC:**
   - **If A1 = PASS** → reply `cost field locked: <path>` and Wave 1 can start.
   - **If A1 = FAIL** → reply `A1 failed: <evidence>`. Do NOT start Wave 1. The phase premise (cost-tracking restoration) is invalid as designed; either:
     (a) confirm with user that we proceed with a per-model price table fallback (rejected by RESEARCH §Don't Hand-Roll, but possible if user accepts the maintenance burden), OR
     (b) cancel the phase and revise the ROADMAP entry.

3. Commit the smoke MD only after the user confirms the gate decision.
  </how-to-verify>
  <resume-signal>Type "cost field locked: &lt;path&gt;" (e.g. "cost field locked: json.billing.total_cost") if A1 passed, or "A1 failed: &lt;reason&gt;" if not.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create RED test scaffolds for Wave 1+ implementations</name>
  <files>web/lib/automations/orq-agents/__tests__/client.test.ts, web/lib/stage-0/__tests__/llm-verdict.test.ts, web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts</files>
  <read_first>
    - .planning/phases/999.5-switch-stage-0-1-classifiers-router-to-deployments-cost-tracking/wave-0-orq-deployments-smoke.md (FRESHLY WRITTEN by Task 3 — read the locked cost-field path; mock-fixtures must use the EXACT path so Wave 1 implementations validate against the live response shape)
    - .planning/phases/999.5-switch-stage-0-1-classifiers-router-to-deployments-cost-tracking/999.5-RESEARCH.md (§Phase Requirements → Test Map T-DEP-01..11, §Pattern 2 invokeOrqDeployment sketch, §Pattern 3 dispatcher sketch)
    - web/lib/automations/orq-agents/__tests__/client.test.ts (existing — extend; 999.4 added invokeOrqModel tests; this task adds invokeOrqDeployment tests)
    - web/lib/stage-0/__tests__/llm-verdict.test.ts (existing if present — see ls; if not present, create)
    - web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts (existing; extend with cost_cents propagation + D-11 on deployments errors)
    - web/lib/automations/orq-agents/client.ts (existing exports; understand fixture shape for OrqAgentRow)
    - .planning/phases/999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening/999.4-01-PLAN.md Task 3 (precedent: RED scaffold style; header comment convention)
  </read_first>
  <behavior>
    Tests intentionally fail RED until Wave 1+ implements `invokeOrqDeployment`, the dispatcher, and the migrations. The imports define the contract.
  </behavior>
  <action>
**File 1: `web/lib/automations/orq-agents/__tests__/client.test.ts`** — EXTEND. Add a new `describe("invokeOrqDeployment")` block + a `describe("invoke (dispatcher)")` block.

Header comment near the new sections: `// Phase 999.5 RED scaffolds — failing imports gate Wave 1+ implementation. Do NOT fix by stubbing — implement the contract.`

Test cases (use vitest mocks for `global.fetch` and a stubbed `loadAgentByKeyCached`):

- `describe("invokeOrqDeployment — endpoint shape (T-DEP-01)")`:
  - `it("POSTs to /v2/deployments/invoke with body { key: agent.deployment_key, inputs, stream: false }")` — fixture row `{ agent_key: "stage-0-safety-classifier", deployment_key: "stage-0-safety-classifier", transport: "deployments", enabled: true, ... }`. Mock fetch. Assert `fetch.mock.calls[0][0]` ends with `/v2/deployments/invoke`. Assert body parses to `{ key: "stage-0-safety-classifier", inputs: { email_id: "x" }, stream: false }`.

- `describe("invokeOrqDeployment — response parser (T-DEP-02)")`:
  - `it("parses choices[0].message.content as JSON")` — mock fetch returns `{ id: "...", choices: [{ message: { content: '{"verdict":"safe","reason":"ok","matched_span":null}' } }], usage: { prompt_tokens: 10, completion_tokens: 5, total_tokens: 15 }, <COST_FIELD>: <VALUE> }` where COST_FIELD/VALUE come from the locked path in `wave-0-orq-deployments-smoke.md`. Assert `result.raw.verdict === "safe"`.

- `describe("invokeOrqDeployment — cost_cents extraction (T-DEP-03)")`:
  - `it("extracts cost_cents from <LOCKED_PATH>")` — same mock as T-DEP-02. Assert `result.cost_cents === Math.round(<VALUE> * 100)`. The LOCKED_PATH literal MUST come from the smoke MD; if Wave 0 surfaced multiple plausible paths, pick the one explicitly written in the `## A1 VERDICT` section.

- `describe("invokeOrqDeployment — 45s deadline (T-DEP-04)")`:
  - `it("aborts after 45s and throws OrqClientTimeoutError")` — mock fetch never resolves; vi fake timers; `vi.advanceTimersByTime(45_001)`; expect rejection with `name === "OrqClientTimeoutError"`.

- `describe("invokeOrqDeployment — missing deployment_key (T-DEP-06)")`:
  - `it("throws clearly when registry row has no deployment_key")` — fixture row with `deployment_key: undefined`. Expect rejection with message containing `deployment_key`.

- `describe("invoke dispatcher — transport routing (T-DEP-05)")`:
  - `it("routes transport='deployments' rows to invokeOrqDeployment")` — fixture `transport: 'deployments'`. Spy on `invokeOrqDeployment` (e.g. via module-level mock). Assert called once.
  - `it("routes transport='router' rows to invokeOrqModel")` — fixture `transport: 'router'`. Spy. Assert.
  - `it("routes transport='agents' rows to invokeOrqAgent")` — fixture `transport: 'agents'`. Spy. Assert.
  - `it("defaults to invokeOrqAgent when transport is null/undefined (Pitfall 8 backstop)")` — fixture `transport: undefined`. Spy. Assert `invokeOrqAgent` called. Note: migration adds `DEFAULT 'router'` so this code path should never fire in production, but the dispatcher is defense-in-depth.

**File 2: `web/lib/stage-0/__tests__/llm-verdict.test.ts`** — EXTEND or CREATE.

Test cases:
- `describe("llmInjectionVerdict — cost_cents propagation (T-DEP-07)")`:
  - `it("propagates cost_cents from invokeOrqDeployment into StageZeroVerdict.usage.cost_cents")` — mock the dispatcher (`invoke` from client.ts) to return `{ raw: { verdict: "safe", reason: "ok", matched_span: null }, agent: <fixture>, usage: { prompt_tokens: 10, completion_tokens: 5, total_tokens: 15 }, billing: { total_cost: 0.0023 }, cost_cents: 1 }` (cents = 0.23¢ ≈ rounds to 0; pick a value where `Math.round(total_cost * 100) > 0` for a non-zero assertion — use `total_cost: 0.05` → `cost_cents: 5`). Assert returned `verdict.usage.cost_cents > 0`.

- `describe("llmInjectionVerdict — timeout coercion via deployments path (T-DEP-09)")`:
  - `it("Stage 0 worker D-02 fail-open still fires when invoke throws OrqClientTimeoutError originating from invokeOrqDeployment")` — re-use the existing 999.4 D-02 test pattern; only the throw source changes. Mock `invoke` to throw `new OrqClientTimeoutError("Orq deployments deadline exceeded for stage-0-safety-classifier after 45000ms")`. Assert worker persists `verdict: "safe"`, `result.llm_reason` contains `"timeout"`.

If the file does not exist, create it with vitest setup mirroring sibling test files. Add the `// Phase 999.5 RED scaffolds — ...` header comment.

**File 3: `web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts`** — EXTEND.

Test cases:
- `describe("Stage 1 worker — cost_cents propagation (T-DEP-08)")`:
  - `it("writes agent_runs.result.cost_cents from invokeOrqDeployment response")` — mock `invoke` (or post-Wave-1 `invokeOrqDeployment` directly) to return `{ raw: { category_key: "auto_reply", confidence: "high", reasoning: "..." }, ..., cost_cents: 5 }`. Assert `agent_runs.insert` called with payload containing `result.cost_cents === 5` (or wherever cost_cents lands in the existing schema — verify via existing `result` jsonb shape at `classifier-screen-worker.ts:223-227`; the result jsonb is `{ category_key, confidence }` today, so the test asserts the insert payload OBJECT, with cost_cents as a sibling key OR as part of `tool_outputs.cost_cents`. Document the chosen field in the test comment.)

- `describe("Stage 1 worker — D-11 on deployments errors (T-DEP-10)")`:
  - `it("D-11 catch coerces to ('unknown', 'low') when invokeOrqDeployment throws non-timeout error")` — mock `invoke` to throw `new Error("HTTP 500 from /v2/deployments/invoke")`. Assert `agent_runs.insert` called with `result: { category_key: "unknown", confidence: "low" }`, status `"failed"`.

For ALL three files: imports must reference symbols Wave 1 will add (`invokeOrqDeployment`, `invoke` dispatcher). TypeScript will compile-error on these — that is the RED state. Add the header comment in each file.

Run focused vitest to confirm RED:
```
cd web && npx vitest run lib/automations/orq-agents/__tests__/client.test.ts lib/stage-0/__tests__/llm-verdict.test.ts lib/inngest/functions/__tests__/classifier-screen-worker.test.ts 2>&1 | tail -40
```
Expected: import failures or assertion failures on the new test cases. Existing tests (999.4-era) must still pass.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit lib/automations/orq-agents/__tests__/client.test.ts lib/stage-0/__tests__/llm-verdict.test.ts lib/inngest/functions/__tests__/classifier-screen-worker.test.ts 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - All 3 test files exist and contain the new `describe` blocks.
    - `grep -c "invokeOrqDeployment" web/lib/automations/orq-agents/__tests__/client.test.ts` returns >= 5 (at least one import/use per T-DEP-01..04 + T-DEP-06).
    - `grep -c "v2/deployments/invoke" web/lib/automations/orq-agents/__tests__/client.test.ts` returns >= 1.
    - `grep -c "transport" web/lib/automations/orq-agents/__tests__/client.test.ts` returns >= 4 (one per dispatcher test case).
    - `grep -c "cost_cents" web/lib/stage-0/__tests__/llm-verdict.test.ts` returns >= 1.
    - `grep -c "OrqClientTimeoutError\|deployments deadline" web/lib/stage-0/__tests__/llm-verdict.test.ts` returns >= 1.
    - `grep -c "cost_cents" web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts` returns >= 1.
    - `grep -c "Phase 999.5 RED" web/lib/automations/orq-agents/__tests__/client.test.ts web/lib/stage-0/__tests__/llm-verdict.test.ts web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts` returns 3 (one header per file).
    - TypeScript compile errors are scoped to imports of not-yet-existing exports (`invokeOrqDeployment`, `invoke` dispatcher); other syntax clean.
  </acceptance_criteria>
  <done>Three test files extended in RED state. Wave 1+ executors will see failing imports as their contract. Cost-field path from smoke MD is hard-coded into the parser test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Smoke script → Orq Deployments endpoint | Untrusted upstream; smoke runs once with benign payload to surface response shape |
| Operator → Orq Studio (manual deployment creation) | Trusted operator; risk = misconfiguration (Pitfall 3, 4, 5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.5-01 | Tampering | Studio deployment_key drift from agent_key | mitigate | Task 2 step 2 mandates EXACT match; Pitfall 3 referenced; Wave 1 migration will add CHECK constraint enforcing kebab pattern. |
| T-999.5-02 | Information Disclosure | Smoke script logs response containing email content | accept | Benign canary payload only ("Hello, this is a benign test email."). No PII. |
| T-999.5-03 | Availability | Studio JSON-schema validation rejects strict-mode array-shorthand nullable | mitigate | Task 2 step 2 mandates `anyOf` per CLAUDE.md learning 3970bad9; existing Stage 1 schema already uses anyOf (verified in 20260506_phase74_stage1_classifier_agent.sql:47-50); Stage 0 schema must be checked before paste. |
| T-999.5-04 | Repudiation | A1 fails (no cost field) — phase premise invalid | mitigate | Task 3 hard gate; explicit BLOCKED decision path; do NOT silently proceed to Wave 1 with cost_cents=0 stub. |
</threat_model>

<verification>
- Smoke script compiles clean with `npx tsc --noEmit`.
- Both Studio deployments live (operator confirmation in resume-signal of Task 2).
- `wave-0-orq-deployments-smoke.md` contains all required sections + locked cost-field path.
- Three test files extended; TypeScript compile errors scoped to to-be-added Wave 1 exports.
- User has signed off on Task 3 gate decision.
</verification>

<success_criteria>
- Wave 0 smoke MD has all sections filled, A1 verdict = PASS with explicit cost-field JSON path, Decision = "Wave 1+ proceed".
- Both Studio deployments live, deployment_keys exactly `stage-0-safety-classifier` and `stage-1-category-classifier`.
- Test scaffolds compile-fail only on the to-be-added exports.
- Latency early signal recorded (informs Plan 03 canary thresholds).
</success_criteria>

<output>
Create `.planning/phases/999.5-switch-stage-0-1-classifiers-router-to-deployments-cost-tracking/999.5-01-SUMMARY.md`. Document:
- Smoke results (HTTP status, latency, cost field path identified).
- Studio deployment versions (Major.Minor) for both deployments — Studio's version surfaces in the Studio dashboard top-right.
- Studio template variable shape chosen (`{{json_input}}` vs discrete) — Wave 1 Plan 02 must match.
- Source of system prompts (Studio Agent vs DB column).
- Three test files created/extended in RED state.
- Any unblock decisions for Wave 1 (e.g. "discrete inputs vs blob — went with blob").
</output>
</content>
