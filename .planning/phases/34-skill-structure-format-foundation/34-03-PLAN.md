---
phase: 34-skill-structure-format-foundation
plan: 03
type: execute
wave: 1
depends_on:
  - "34-01"
files_modified:
  - orq-agent/agents/architect.md
  - orq-agent/agents/dataset-generator.md
  - orq-agent/agents/dataset-preparer.md
  - orq-agent/agents/deployer.md
  - orq-agent/agents/experiment-runner.md
  - orq-agent/agents/failure-diagnoser.md
  - orq-agent/agents/hardener.md
  - orq-agent/agents/iterator.md
  - orq-agent/agents/kb-generator.md
  - orq-agent/agents/orchestration-generator.md
  - orq-agent/agents/prompt-editor.md
  - orq-agent/agents/readme-generator.md
  - orq-agent/agents/researcher.md
  - orq-agent/agents/results-analyzer.md
  - orq-agent/agents/spec-generator.md
  - orq-agent/agents/tester.md
  - orq-agent/agents/tool-resolver.md
autonomous: true
requirements:
  - SKST-01
  - SKST-03
  - SKST-04
  - SKST-05
  - SKST-06
  - SKST-07
  - SKST-08
  - SKST-09
  - SKST-10
must_haves:
  truths:
    - "Every one of the 17 files under `orq-agent/agents/` contains all 9 required H2 sections"
    - "Every subagent file still declares a non-empty `tools:` in frontmatter (subagent equivalent of allowed-tools — SKST-01 per RESEARCH.md schema split)"
    - "`bash orq-agent/scripts/lint-skills.sh --files orq-agent/agents` exits 0"
    - "No subagent file has `allowed-tools:` added (that key is a no-op on subagents per Claude Code schema)"
  artifacts:
    - path: "orq-agent/agents/architect.md"
      provides: "Architect subagent spec with all 9 SKST sections"
      contains: "## Constraints"
    - path: "orq-agent/agents/deployer.md"
      provides: "Deployer subagent with Destructive Actions (creates/updates live Orq.ai agents)"
      contains: "## Destructive Actions"
    - path: "orq-agent/agents/iterator.md"
      provides: "Iterator subagent with Destructive Actions (modifies local specs, re-deploys)"
      contains: "## Destructive Actions"
    - path: "orq-agent/agents/hardener.md"
      provides: "Hardener subagent with Destructive Actions (attaches guardrails to live agents)"
      contains: "## Destructive Actions"
    - path: "orq-agent/agents/prompt-editor.md"
      provides: "Prompt-editor subagent with Destructive Actions (modifies spec files in place)"
      contains: "## Destructive Actions"
    - path: "orq-agent/agents/kb-generator.md"
      provides: "KB-generator subagent with Destructive Actions (creates KBs, uploads files)"
      contains: "## Destructive Actions"
  key_links:
    - from: "orq-agent/agents/*.md (frontmatter)"
      to: "Claude Code subagent schema"
      via: "`tools:` key (subagents) — NOT `allowed-tools:` (commands)"
      pattern: "^tools:"
---

<objective>
Apply the Agent Skills format to every subagent file under `orq-agent/agents/` (17 files). Each file gains the same 9 required H2 sections as Plan 02's commands. Per RESEARCH.md Standard Stack §Frontmatter, the SKST-01 requirement for subagents is satisfied by `tools:` (NOT `allowed-tools:`) — this is the Claude Code subagent schema. All 17 subagents already have `tools:` declared (verified 2026-04-20); this plan confirms that and adds the 9 body sections.

Purpose: Subagents are invoked via `Task(subagent_type=...)` from commands. They need the same contract sections so the orchestrator can reason about when to spawn them and what handoffs happen. The Companion Skills directional arrows become the inter-subagent handoff graph that Phase 36's `/orq-agent:workspace` can eventually parse.

Output: All 17 subagent files conform to the SKST format. `bash orq-agent/scripts/lint-skills.sh --files orq-agent/agents` exits 0.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-skill-structure-format-foundation/34-CONTEXT.md
@.planning/phases/34-skill-structure-format-foundation/34-RESEARCH.md
@.planning/phases/34-skill-structure-format-foundation/34-VALIDATION.md
@.planning/phases/34-skill-structure-format-foundation/34-01-SUMMARY.md

<interfaces>
<!-- Same 9 required H2 headings as Plan 02 (exact strings — grep -qF match): -->

```
## Constraints
## When to use
## When NOT to use
## Companion Skills
## Done When
## Destructive Actions
## Anti-Patterns
## Open in orq.ai
## Documentation & Resolution
```

**Positioning rule for subagents (from RESEARCH.md line 238-240):**
- Pre-body sections (Constraints through Destructive Actions) → INSERT after frontmatter + any opening XML block (<role>, <instructions>, etc.) and BEFORE the main body content
- Footer sections (Anti-Patterns, Open in orq.ai, Documentation & Resolution) → APPEND at end of file

**CRITICAL subagent frontmatter schema (from RESEARCH.md line 87-97, Pitfall 2 line 434):** Subagent YAML frontmatter uses `tools:` NOT `allowed-tools:`. The Agent Skills open-standard key is `allowed-tools`, but Claude Code subagents predate that standard. Writing `allowed-tools:` into a subagent file is a NO-OP. The lint rule `tools-declared` enforces `tools:` for subagents. Do NOT add `allowed-tools:` to any subagent file. Do NOT modify any existing frontmatter.

**Canonical Documentation & Resolution footer (VERBATIM — same as Plan 02):**

```markdown
## Documentation & Resolution

When skill content conflicts with live API behavior or official docs, trust the source higher in this list:

1. **orq MCP tools** — query live data first (`search_entities`, `get_agent`, `models-list`); API responses are authoritative.
2. **orq.ai documentation MCP** — use `search_orq_ai_documentation` or `get_page_orq_ai_documentation`.
3. **Official docs** — browse https://docs.orq.ai directly.
4. **This skill file** — may lag behind API or docs changes.
```

**Per-subagent Destructive Action Inventory (from RESEARCH.md lines 352-375):**

| Subagent | Destructive Actions content |
|----------|-----------------------------|
| `architect.md` | Creates `{OUTPUT_DIR}/[swarm-name]/blueprint.md`. **AskUserQuestion confirm required before** overwriting existing blueprint.md. |
| `spec-generator.md` | Creates agent spec files under `{OUTPUT_DIR}/[swarm-name]/agents/*.md`. **AskUserQuestion confirm required before** overwriting existing spec files. |
| `orchestration-generator.md` | Creates `{OUTPUT_DIR}/[swarm-name]/ORCHESTRATION.md`. **AskUserQuestion confirm required before** overwriting. |
| `readme-generator.md` | Creates `{OUTPUT_DIR}/[swarm-name]/README.md`. **AskUserQuestion confirm required before** overwriting. |
| `researcher.md` | Writes `{OUTPUT_DIR}/[swarm-name]/research-brief.md`. **AskUserQuestion confirm required before** overwriting. |
| `tool-resolver.md` | Writes `{OUTPUT_DIR}/[swarm-name]/TOOLS.md`. **AskUserQuestion confirm required before** overwriting. |
| `dataset-generator.md` | Writes dataset files under `{OUTPUT_DIR}/[swarm-name]/datasets/`. **AskUserQuestion confirm required before** deleting existing datapoints during Mode-4 curation. |
| `dataset-preparer.md` | Creates datasets on Orq.ai via MCP/REST. **AskUserQuestion confirm required before** overwriting an existing Orq.ai dataset with the same name. |
| `experiment-runner.md` | Creates experiments on Orq.ai. Non-destructive (experiments are append-only). |
| `results-analyzer.md` | Read-only on Orq.ai. Writes local analysis output. Non-destructive. |
| `deployer.md` | **AskUserQuestion confirm required before** creating or updating Orq.ai agents via MCP (idempotent create-or-update mutates live agent config). Attaches tools, creates KBs. |
| `tester.md` | Orchestrates dataset-preparer → experiment-runner → results-analyzer. Non-destructive itself; delegates destructive acts to dataset-preparer. |
| `failure-diagnoser.md` | Read-only on Orq.ai (reads experiment results). Writes local analysis output. Non-destructive. |
| `iterator.md` | **AskUserQuestion confirm required before** orchestrating prompt-editor + re-deploy (which modifies local spec files AND live Orq.ai agent config). |
| `prompt-editor.md` | **AskUserQuestion confirm required before** modifying agent spec files in place. |
| `hardener.md` | **AskUserQuestion confirm required before** attaching guardrails to deployed agents on Orq.ai (modifies live agent config) and before promoting evaluators to runtime guardrails. |
| `kb-generator.md` | **AskUserQuestion confirm required before** creating a KB on Orq.ai when one with the same name already exists. Uploads files. |

**Per-subagent Companion Skills (from RESEARCH.md Companion Skill Graph lines 309-345):**

| Subagent | Companion Skills bullets |
|----------|-------------------------|
| `architect.md` | → `tool-resolver` (consumes blueprint.md to produce TOOLS.md); → `researcher` (consumes blueprint.md + TOOLS.md); → `spec-generator` (final consumer); ← `/orq-agent` (Step 3); ← `/orq-agent:architect` (this command's only subagent) |
| `tool-resolver.md` | ← `architect` (receives blueprint.md); → `researcher` (emits TOOLS.md); → `spec-generator` (emits TOOLS.md); ← `/orq-agent:tools` (standalone); ← `/orq-agent` (Step 4) |
| `researcher.md` | ← `architect` + `tool-resolver`; → `spec-generator`; ← `/orq-agent:research` (standalone); ← `/orq-agent` (Step 5) |
| `spec-generator.md` | ← `architect` + `tool-resolver` + `researcher`; → `orchestration-generator`, → `dataset-generator`, → `readme-generator`, → `kb-generator` (when agent has KB); ← `/orq-agent:prompt` (only subagent) |
| `orchestration-generator.md` | ← `spec-generator`; ← `/orq-agent` (Step 7, only for multi-agent swarms); emits ORCHESTRATION.md |
| `dataset-generator.md` | ← `spec-generator`; ← `/orq-agent:datasets` (standalone); ← `/orq-agent` (Step 6); → `dataset-preparer` (downstream during /orq-agent:test) |
| `readme-generator.md` | ← `spec-generator` + `orchestration-generator` + `dataset-generator`; ← `/orq-agent` (final step); emits README.md |
| `kb-generator.md` | ← `spec-generator` (when agent has KB); ← `/orq-agent:kb`; → `deployer` (KB attached at deploy time) |
| `deployer.md` | ← `/orq-agent:deploy`; ← all generator subagents via spec files; → `tester` (typical next invocation via `/orq-agent:test`) |
| `dataset-preparer.md` | ← `tester`; ← `/orq-agent:test`; → `experiment-runner` |
| `experiment-runner.md` | ← `dataset-preparer`; ← `tester`; → `results-analyzer` |
| `results-analyzer.md` | ← `experiment-runner`; ← `tester`; emits results summary for `iterator` / `failure-diagnoser` downstream |
| `tester.md` | ← `/orq-agent:test`; → `dataset-preparer`, → `experiment-runner`, → `results-analyzer`; → `iterator` (on failure) |
| `failure-diagnoser.md` | ← `iterator`; ← `/orq-agent:iterate`; → `iterator` (with diagnosis) |
| `iterator.md` | ← `/orq-agent:iterate`; ← `failure-diagnoser`; → `prompt-editor`; → `deployer` (re-deploy); → `tester` (re-test) |
| `prompt-editor.md` | ← `iterator`; → `deployer` (for re-deploy via `/orq-agent:deploy`) |
| `hardener.md` | ← `/orq-agent:harden`; ← `iterator` (terminal after green iteration); terminal subagent in pipeline |

**Per-subagent "Open in orq.ai" URLs (from RESEARCH.md lines 397-409):**

| Subagent | Open in orq.ai bullets |
|----------|-----------------------|
| `architect.md` | - **Agent Studio:** https://my.orq.ai/agents · - **Deployments:** https://my.orq.ai/deployments |
| `spec-generator.md` | - **Agent Studio:** https://my.orq.ai/agents · - **Prompts:** https://my.orq.ai/prompts |
| `orchestration-generator.md` | - **Agent Studio:** https://my.orq.ai/agents |
| `tool-resolver.md` | - **Agent Studio (tools tab):** https://my.orq.ai/agents |
| `researcher.md` | - **AI Router / Models:** https://my.orq.ai/models |
| `readme-generator.md` | - **Deployments:** https://my.orq.ai/deployments |
| `deployer.md` | - **Deployments:** https://my.orq.ai/deployments · - **Agent Studio:** https://my.orq.ai/agents |
| `tester.md` | - **Experiments:** https://my.orq.ai/experiments · - **Datasets:** https://my.orq.ai/datasets · - **Evaluators:** https://my.orq.ai/evaluators |
| `dataset-preparer.md` | - **Datasets:** https://my.orq.ai/datasets |
| `experiment-runner.md` | - **Experiments:** https://my.orq.ai/experiments |
| `results-analyzer.md` | - **Experiments:** https://my.orq.ai/experiments |
| `iterator.md` | - **Experiments:** https://my.orq.ai/experiments · - **Prompts:** https://my.orq.ai/prompts · - **Traces:** https://my.orq.ai/traces |
| `failure-diagnoser.md` | - **Traces:** https://my.orq.ai/traces · - **Experiments:** https://my.orq.ai/experiments |
| `prompt-editor.md` | - **Prompts:** https://my.orq.ai/prompts |
| `hardener.md` | - **Deployments (guardrails tab):** https://my.orq.ai/deployments · - **Evaluators:** https://my.orq.ai/evaluators |
| `kb-generator.md` | - **Knowledge Bases:** https://my.orq.ai/knowledge-bases |
| `dataset-generator.md` | - **Datasets:** https://my.orq.ai/datasets · - **Annotation Queues:** https://my.orq.ai/annotation-queues `<!-- TODO(SKST-10): verified in Phase 37+ -->` |

**Inferred URL markers:** The `dataset-generator.md` Annotation Queues bullet is flagged inferred per RESEARCH.md line 395. Append inline HTML comment `<!-- TODO(SKST-10): verified in Phase 37+ -->`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 9 SKST sections to the 6 generator subagents (architect, spec-generator, orchestration-generator, tool-resolver, researcher, readme-generator)</name>
  <files>
    orq-agent/agents/architect.md,
    orq-agent/agents/spec-generator.md,
    orq-agent/agents/orchestration-generator.md,
    orq-agent/agents/tool-resolver.md,
    orq-agent/agents/researcher.md,
    orq-agent/agents/readme-generator.md
  </files>
  <read_first>
    - orq-agent/agents/architect.md (full read — note current body structure)
    - orq-agent/agents/spec-generator.md (full read)
    - orq-agent/agents/orchestration-generator.md (full read)
    - orq-agent/agents/tool-resolver.md (full read)
    - orq-agent/agents/researcher.md (full read)
    - orq-agent/agents/readme-generator.md (full read)
    - .planning/phases/34-skill-structure-format-foundation/34-RESEARCH.md (Section Template lines 161-236; Pitfall 2 line 434 — do not add `allowed-tools:` to any subagent)
  </read_first>
  <action>
    For each of the 6 generator subagents, apply the same 9-section template as Plan 02's commands:

    1. **Pre-body sections (insert after frontmatter + any opening XML block, BEFORE the main body):**
       - ## Constraints (with NEVER/ALWAYS + Why)
       - ## When to use
       - ## When NOT to use
       - ## Companion Skills (use bullets from <interfaces> table VERBATIM)
       - ## Done When
       - ## Destructive Actions (use row from <interfaces> Destructive Action Inventory VERBATIM)

    2. **Footer sections (append at end of file):**
       - ## Anti-Patterns (2+ rows per file, extracted from existing prose)
       - ## Open in orq.ai (use bullets from <interfaces> Open in orq.ai table VERBATIM)
       - ## Documentation & Resolution (canonical footer from <interfaces> VERBATIM)

    **Per-subagent NEVER/ALWAYS rules (write these verbatim):**

    - **architect.md:**
      - NEVER: "generate more than 5 agents in a single swarm — decompose into sub-swarms", "skip the complexity gate — each additional agent requires one of 5 justifications"
      - ALWAYS: "default to single-agent; multi-agent must be justified", "derive agent keys from `orq-agent/references/naming-conventions.md` (regex `^[A-Za-z][A-Za-z0-9]*([._-][A-Za-z0-9]+)*$`, end with `-agent`)"
      - Why: "Over-engineered swarms are harder to maintain than a well-configured single agent. The complexity gate exists because reformatter-style agents and duplicate-model agents are the two most common over-engineering failures."

    - **spec-generator.md:**
      - NEVER: "use floating model aliases (`claude-sonnet-4-5`) — pin to snapshot (`claude-sonnet-4-5-20250929`) per Phase 35 MSEL-02", "reference tools not in `orq-agent/references/tool-catalog.md`"
      - ALWAYS: "emit the full Orq.ai Agent schema (key, description, instructions, model, tools, memory, deployment variants)", "cross-reference `orq-agent/references/orqai-agent-fields.md` for every field"
      - Why: "Incomplete specs fail at deploy-time; floating aliases silently upgrade; non-catalog tools fail with opaque MCP errors."

    - **orchestration-generator.md:**
      - NEVER: "emit ORCHESTRATION.md for single-agent swarms", "couple agents via implicit shared state — use explicit message passing or orchestrator control flow"
      - ALWAYS: "cross-reference `orq-agent/references/orchestration-patterns.md` before proposing a pattern", "document the pattern choice rationale inline"
      - Why: "ORCHESTRATION.md for 1 agent is noise; implicit coupling is the source of the hardest-to-debug swarm failures."

    - **tool-resolver.md:**
      - NEVER: "invent tool names — all tools come from `orq-agent/references/tool-catalog.md`", "attach tools without schema compatibility with the agent's input/output shape"
      - ALWAYS: "use canonical tool IDs from the catalog", "document each tool's rationale in TOOLS.md (why this tool, which agent uses it)"
      - Why: "Non-catalog tools fail at deploy; unjustified attachments bloat agent context and cost."

    - **researcher.md:**
      - NEVER: "recommend models outside the AI Router without flagging activation requirements (Phase 40 KBM-02 baseline)", "start with the cheapest model — capable-first per Phase 35 MSEL-01"
      - ALWAYS: "pin model snapshots in the research brief (Phase 35 MSEL-02)", "cross-reference `orq-agent/references/orqai-model-catalog.md`"
      - Why: "Floating aliases upgrade silently; non-activated models fail at deploy; starting cheap produces biased baselines."

    - **readme-generator.md:**
      - NEVER: "document agents that are not in the spec directory", "omit the MCP invocation commands for each deployed agent"
      - ALWAYS: "document every agent with purpose, inputs, outputs, and invocation snippet", "include the orchestration-doc section when ORCHESTRATION.md exists"
      - Why: "Missing agents produce confused users who can't find capabilities; missing invocation snippets block copy-paste testing."

    **Per-subagent When to use / When NOT to use / Done When / Anti-Patterns:** Derive from each file's existing prose. Keep bullets concrete and falsifiable. Example for architect.md Done When: "[ ] blueprint.md written", "[ ] Swarm name, agent count, pattern, complexity justification present", "[ ] Every agent has Role, Responsibility, Model recommendation, Tools needed, KB classification", "[ ] Agent keys match regex", "[ ] ORCHESTRATION section present iff multi-agent".

    **Step — Verify per-file AFTER each edit:**
    ```
    bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/<file>.md
    ```

    Must exit 0. Do NOT modify frontmatter (tools: key already present on all 6). Do NOT add `allowed-tools:`.
  </action>
  <verify>
    <automated>bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/agents/architect.md && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/agents/spec-generator.md && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/agents/orchestration-generator.md && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/agents/tool-resolver.md && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/agents/researcher.md && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/agents/readme-generator.md</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 6 files contains all 9 required H2 headings
    - `bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/<each>.md` exits 0 for each
    - Each file still has `^tools:` in frontmatter (grep -c returns 1)
    - NO file has `^allowed-tools:` in frontmatter (grep -c returns 0 — subagents use `tools:`, not `allowed-tools:`)
    - Each file's Constraints section contains `**NEVER**` and `**ALWAYS**` and `**Why these constraints:**`
    - Each file's Companion Skills section contains at least one `→ ` arrow
    - Each file's Destructive Actions section content matches the row from <interfaces> (grep for the destination file path, e.g., `blueprint.md` in architect.md)
  </acceptance_criteria>
  <done>
    All 6 generator subagents have 9 SKST sections. Lint passes per-file. Frontmatter `tools:` untouched. No `allowed-tools:` added (subagent schema does not support it).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add 9 SKST sections to the 6 deployment/testing subagents (deployer, dataset-preparer, experiment-runner, results-analyzer, tester, dataset-generator)</name>
  <files>
    orq-agent/agents/deployer.md,
    orq-agent/agents/dataset-preparer.md,
    orq-agent/agents/experiment-runner.md,
    orq-agent/agents/results-analyzer.md,
    orq-agent/agents/tester.md,
    orq-agent/agents/dataset-generator.md
  </files>
  <read_first>
    - orq-agent/agents/deployer.md (full read)
    - orq-agent/agents/dataset-preparer.md (full read)
    - orq-agent/agents/experiment-runner.md (full read)
    - orq-agent/agents/results-analyzer.md (full read)
    - orq-agent/agents/tester.md (full read)
    - orq-agent/agents/dataset-generator.md (full read)
    - .planning/phases/34-skill-structure-format-foundation/34-RESEARCH.md (Section Template + Destructive Action Inventory)
  </read_first>
  <action>
    Apply the same 9-section template.

    **Per-subagent NEVER/ALWAYS rules:**

    - **deployer.md:**
      - NEVER: "deploy without verifying the spec is syntactically valid via `orqai-agent-fields.md` schema", "overwrite live Orq.ai agent config silently — AskUserQuestion confirm required"
      - ALWAYS: "use idempotent create-or-update (not create; not delete-then-create)", "emit a deployment manifest with agent IDs, URLs, and deployment-variant IDs"
      - Why: "Invalid specs fail mid-deploy leaving partial state; silent overwrite destroys rollback anchors; manifests are required by `/orq-agent:test` to target the deployed agent."

    - **dataset-preparer.md:**
      - NEVER: "upload datasets that violate coverage rules (Phase 39 DSET-03 enforced upstream by dataset-generator)", "skip the smoke test after upload"
      - ALWAYS: "run a 5-datapoint smoke test after upload to verify the dataset is queryable", "record stratified splits (train/dev/test or holdout) in the dataset metadata"
      - Why: "Silent upload failures only surface at experiment-run time. Coverage violations produce biased eval. Splits without metadata can't be reproduced."

    - **experiment-runner.md:**
      - NEVER: "run without confirming the deployed agent from deployer's manifest is still active", "poll faster than the adaptive polling schedule (starts 10s, backs off to 60s)"
      - ALWAYS: "use REST execution (not MCP — REST is the primary experiment path per STATE.md 260326-ann decision)", "emit the experiment ID + Orq.ai URL for monitoring"
      - Why: "Stale agent references cause confusing failures; aggressive polling wastes quota; REST is the primary experiment path — MCP is fallback."

    - **results-analyzer.md:**
      - NEVER: "average scores across incompatible dimensions (tool selection + output quality) — use isolated graders", "mark a regression without citing the previous run's score for context"
      - ALWAYS: "compute Student's t statistics for per-dimension deltas", "flag regressions with ⚠️ markers (Phase 42 ITRX-04)"
      - Why: "Omnibus graders hide failure modes; ungrounded regression claims confuse iterators; the ⚠️ convention is how iterator detects drift."

    - **tester.md:**
      - NEVER: "run tests without a deployed agent (use `/orq-agent:deploy` first)", "mark an experiment failed solely because one datapoint failed — require ≥5% failure rate"
      - ALWAYS: "orchestrate dataset-preparer → experiment-runner → results-analyzer in order", "surface a warning when average pass rate ≥ 95% (Phase 42 ESCI-05 baseline — eval may be too easy)"
      - Why: "Testing specs directly (not deployed agents) tests the file, not production. Single-datapoint failures are noise. 95%+ pass rates suggest over-fit evaluators."

    - **dataset-generator.md:**
      - NEVER: "upload a dataset violating coverage (Phase 39 DSET-03)", "delete datapoints during Mode-4 curation without AskUserQuestion confirm (Phase 39 DSET-04)"
      - ALWAYS: "tag every datapoint by category AND dimension (Phase 39 DSET-05)", "include 15-20% adversarial cases from the 8-vector catalog when the agent profile warrants (Phase 39 DSET-02)"
      - Why: "Coverage violations bias eval; silent deletion loses signal irreversibly; tags enable slice analysis; adversarial cases stress-test the agent."

    **Per-subagent When to use / When NOT to use / Done When / Anti-Patterns:** Derive from existing prose. Use the Destructive Actions + Companion Skills + Open in orq.ai tables from <interfaces> VERBATIM.

    **Step — Verify per-file after each edit** with `bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/<file>.md`.
  </action>
  <verify>
    <automated>bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/agents/deployer.md && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/agents/dataset-preparer.md && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/agents/experiment-runner.md && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/agents/results-analyzer.md && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/agents/tester.md && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/agents/dataset-generator.md</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 6 files contains all 9 required H2 headings
    - `bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/<each>.md` exits 0 for each
    - Each file still has `^tools:` in frontmatter
    - `deployer.md`, `dataset-preparer.md`, `dataset-generator.md` Destructive Actions sections contain `AskUserQuestion`
    - `experiment-runner.md`, `results-analyzer.md`, `tester.md` Destructive Actions sections describe non-destructive behavior (may contain "Non-destructive" or equivalent)
  </acceptance_criteria>
  <done>
    All 6 deployment/testing subagents have 9 SKST sections. Lint passes per-file. Frontmatter untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add 9 SKST sections to the 5 iteration/hardening/KB subagents (failure-diagnoser, iterator, prompt-editor, hardener, kb-generator)</name>
  <files>
    orq-agent/agents/failure-diagnoser.md,
    orq-agent/agents/iterator.md,
    orq-agent/agents/prompt-editor.md,
    orq-agent/agents/hardener.md,
    orq-agent/agents/kb-generator.md
  </files>
  <read_first>
    - orq-agent/agents/failure-diagnoser.md (full read)
    - orq-agent/agents/iterator.md (full read)
    - orq-agent/agents/prompt-editor.md (full read)
    - orq-agent/agents/hardener.md (full read)
    - orq-agent/agents/kb-generator.md (full read)
    - .planning/phases/34-skill-structure-format-foundation/34-RESEARCH.md
  </read_first>
  <action>
    Apply the same 9-section template.

    **Per-subagent NEVER/ALWAYS rules:**

    - **failure-diagnoser.md:**
      - NEVER: "classify downstream cascading effects as root causes — label only the first upstream failure (Phase 38 TFAIL-03 baseline)", "propose prompt edits without first naming the failure mode (specification / generalization / dataset / evaluator per Phase 42 ESCI-01)"
      - ALWAYS: "cite specific datapoint IDs and trace IDs as evidence", "separate dataset-quality issues from evaluator-quality issues in the output (Phase 42 ESCI-08)"
      - Why: "Labeling downstream effects creates phantom failure modes; speculative fixes without diagnosis wastes iteration budget; conflating dataset and evaluator issues proposes fixes at the wrong layer."

    - **iterator.md:**
      - NEVER: "re-run the same optimizer on the same prompt without explicit user override (Phase 42 ITRX-05 no-drift rule)", "auto-apply prompt diffs without HITL approval"
      - ALWAYS: "assign P0/P1/P2 priority to every proposed improvement (Phase 42 ITRX-01)", "cite Evidence (datapoints affected, scores, run ID) and Success Criteria per diff (Phase 42 ITRX-07)"
      - Why: "No-drift prevents infinite loops; silent auto-apply removes the HITL safety rail; priority + evidence make diffs reviewable."

    - **prompt-editor.md:**
      - NEVER: "edit outside the sections the iterator's diff proposal names", "change snapshot model pins when the iteration is about prompt wording"
      - ALWAYS: "confirm via AskUserQuestion before writing any spec modification", "emit a before/after diff summary"
      - Why: "Out-of-scope edits compound hidden regressions; silent model changes make iteration non-reproducible; diffs are the audit trail."

    - **hardener.md:**
      - NEVER: "promote an evaluator to a guardrail unless TPR ≥ 90% AND TNR ≥ 90% on the test set (Phase 42 EVLD-08)", "attach guardrails without setting a sample_rate (Phase 42 ITRX-08: 100% <1K/day, 30% 1K-100K/day, 10% ≥100K/day)"
      - ALWAYS: "confirm guardrail promotion via AskUserQuestion", "record the sample_rate decision with volume rationale"
      - Why: "Unvalidated guardrails produce false-positive rejections that block production traffic; no sample_rate produces runaway LLM-judge costs at high volume."

    - **kb-generator.md:**
      - NEVER: "wire a KB to a deployment without retrieval quality testing (Phase 40 KBM-01)", "use memory-style stores for static reference data (Phase 40 KBM-04: KB vs Memory decision rule)"
      - ALWAYS: "verify the embedding model is activated in AI Router before KB creation (Phase 40 KBM-02)", "pick the chunking strategy from content type — sentence for prose, recursive for structured docs (Phase 40 KBM-03)"
      - Why: "Non-tested KBs return irrelevant chunks silently; misused memory-for-docs pattern is a common failure; non-activated models fail at create-time."

    **Per-subagent When to use / When NOT to use / Done When / Anti-Patterns:** Derive from existing prose. Use the tables in <interfaces> VERBATIM for Destructive Actions, Companion Skills, Open in orq.ai.

    **Step — Verify per-file after each edit.**

    **Final verification (after all 17 subagents edited — across this plan's 3 tasks):**
    ```
    bash orq-agent/scripts/lint-skills.sh --files orq-agent/agents
    ```
    Must exit 0 — proving ALL 17 subagent files conform.
  </action>
  <verify>
    <automated>bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --files /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/agents</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 5 files (failure-diagnoser, iterator, prompt-editor, hardener, kb-generator) contains all 9 required H2 headings
    - `bash orq-agent/scripts/lint-skills.sh --files orq-agent/agents` exits 0 (proves ALL 17 subagent files pass lint — covers this task AND retroactively confirms Tasks 1 and 2)
    - Each of the 5 files still has `^tools:` in frontmatter (grep -c returns 1)
    - NONE of the 5 files has `^allowed-tools:` in frontmatter (grep -c returns 0)
    - Each of the 5 files' Destructive Actions section contains the literal string `AskUserQuestion`
    - Each file's Constraints section cites a future-phase requirement ID (TFAIL-03, ITRX-05, EVLD-08, ITRX-08, KBM-01, KBM-02, KBM-03, KBM-04, ESCI-01, ESCI-08) — these establish the forward-link graph that Phases 38, 40, 42 will rely on
    - `grep -c '^allowed-tools:' orq-agent/agents/*.md | awk -F: '$2 != 0 {print; exit 1}'` produces empty output (no subagent file has that key)
  </acceptance_criteria>
  <done>
    All 17 subagent files now conform to the SKST format (6 from Task 1 + 6 from Task 2 + 5 from this task). Full-directory lint passes. Every subagent's Destructive Actions flag uses AskUserQuestion per the Destructive Action Inventory. Frontmatter `tools:` keys untouched; no `allowed-tools:` added. Plan is complete — Plan 04 can now update SKILL.md in Wave 2.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. `bash orq-agent/scripts/lint-skills.sh --files orq-agent/agents` → exits 0 (all 17 subagents pass all rules applicable to subagents).
2. `for f in orq-agent/agents/*.md; do grep -qF "## Constraints" "$f" || echo "MISSING: $f"; done` → produces no output.
3. Same check for each of the other 8 required sections → produces no output.
4. `grep -l '^allowed-tools:' orq-agent/agents/*.md` → produces no output (no subagent has that key — they use `tools:` per Claude Code schema).
5. `grep -L '^tools:' orq-agent/agents/*.md` → produces no output (every subagent HAS the tools: key).
6. No files under `orq-agent/commands/` or `orq-agent/SKILL.md` are modified by this plan.
7. `bash orq-agent/scripts/check-protected-pipelines.sh` → exits 0 (untouched — this plan doesn't touch commands, but verifies no collateral damage from Plan 02 running in parallel).
</verification>

<success_criteria>
- [ ] All 17 files in `orq-agent/agents/` contain the 9 required H2 sections
- [ ] `bash orq-agent/scripts/lint-skills.sh --files orq-agent/agents` exits 0
- [ ] All 17 files still declare non-empty `tools:` in frontmatter (no regressions)
- [ ] NO subagent file has `allowed-tools:` added (Claude Code schema does not accept it on subagents)
- [ ] Destructive Actions sections reference `AskUserQuestion` for the 11 subagents with destructive operations (architect, spec-generator, orchestration-generator, readme-generator, researcher, tool-resolver, dataset-generator, dataset-preparer, deployer, iterator, prompt-editor, hardener, kb-generator) and use explicit "Non-destructive" for read-only subagents (experiment-runner, results-analyzer, tester, failure-diagnoser)
- [ ] `dataset-generator.md` contains the inline HTML comment `<!-- TODO(SKST-10): verified in Phase 37+ -->` next to the Annotation Queues URL (inferred URL marker per locked decision #3)
- [ ] Zero modifications to `orq-agent/commands/` or `orq-agent/SKILL.md`
</success_criteria>

<output>
After completion, create `.planning/phases/34-skill-structure-format-foundation/34-03-SUMMARY.md` documenting:
- List of 17 subagent files modified
- Confirmation that all 9 sections are present per file
- Confirmation that `^tools:` is present and `^allowed-tools:` is absent in every subagent file
- Confirmation that `bash orq-agent/scripts/lint-skills.sh --files orq-agent/agents` exits 0
- Count of inferred-URL TODO markers added (expected: 1, in dataset-generator.md)
- Pointer to Plan 04 (SKILL.md in Wave 2) which starts once this plan and Plan 02 both complete
</output>
