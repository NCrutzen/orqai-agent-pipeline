---
phase: 34-skill-structure-format-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - "34-01"
files_modified:
  - orq-agent/commands/architect.md
  - orq-agent/commands/datasets.md
  - orq-agent/commands/deploy.md
  - orq-agent/commands/harden.md
  - orq-agent/commands/help.md
  - orq-agent/commands/iterate.md
  - orq-agent/commands/kb.md
  - orq-agent/commands/orq-agent.md
  - orq-agent/commands/prompt.md
  - orq-agent/commands/research.md
  - orq-agent/commands/set-profile.md
  - orq-agent/commands/systems.md
  - orq-agent/commands/test.md
  - orq-agent/commands/tools.md
  - orq-agent/commands/update.md
autonomous: true
requirements:
  - SKST-01
  - SKST-03
  - SKST-04
  - SKST-05
  - SKST-06
  - SKST-07
  - SKST-08
  - SKST-09
  - SKST-10
must_haves:
  truths:
    - "Every one of the 15 files under `orq-agent/commands/` contains all 9 required H2 sections"
    - "Every command file's `<pipeline>` block (where it has one) is unchanged — pipeline SHA-256 still matches Plan 01's golden"
    - "Every command file still declares a non-empty `allowed-tools:` in frontmatter (no regressions)"
    - "`bash orq-agent/scripts/lint-skills.sh --files orq-agent/commands` exits 0"
    - "`bash orq-agent/scripts/check-protected-pipelines.sh` exits 0 (proves byte-identical pipeline for orq-agent, prompt, architect)"
  artifacts:
    - path: "orq-agent/commands/orq-agent.md"
      provides: "/orq-agent top-level command with all 9 SKST sections"
      contains: "## Constraints"
    - path: "orq-agent/commands/prompt.md"
      provides: "/orq-agent:prompt fast-path command with all 9 SKST sections"
      contains: "## Constraints"
    - path: "orq-agent/commands/architect.md"
      provides: "/orq-agent:architect standalone command with all 9 SKST sections"
      contains: "## Constraints"
    - path: "orq-agent/commands/deploy.md"
      provides: "Deploy command with SKST sections + Destructive Actions flagged for AskUserQuestion"
      contains: "## Destructive Actions"
    - path: "orq-agent/commands/iterate.md"
      provides: "Iterate command with SKST sections + Destructive Actions flagged"
      contains: "## Destructive Actions"
    - path: "orq-agent/commands/harden.md"
      provides: "Harden command with SKST sections + Destructive Actions flagged"
      contains: "## Destructive Actions"
    - path: "orq-agent/commands/update.md"
      provides: "Update command with SKST sections + Destructive Actions (overwrites local files)"
      contains: "## Destructive Actions"
  key_links:
    - from: "orq-agent/commands/orq-agent.md (<pipeline> block)"
      to: ".planning/phases/34-skill-structure-format-foundation/golden/orq-agent.sha256"
      via: "SHA-256 of extracted <pipeline> block must match golden"
      pattern: "<pipeline>.*</pipeline>"
    - from: "orq-agent/commands/prompt.md (<pipeline> block)"
      to: ".planning/phases/34-skill-structure-format-foundation/golden/prompt.sha256"
      via: "SHA-256 of extracted <pipeline> block must match golden"
      pattern: "<pipeline>.*</pipeline>"
    - from: "orq-agent/commands/architect.md (<pipeline> block)"
      to: ".planning/phases/34-skill-structure-format-foundation/golden/architect.sha256"
      via: "SHA-256 of extracted <pipeline> block must match golden"
      pattern: "<pipeline>.*</pipeline>"
---

<objective>
Apply the Agent Skills format to every command file under `orq-agent/commands/` (15 files). Each file gains 9 required H2 sections (Constraints, When to use, When NOT to use, Companion Skills, Done When, Destructive Actions, Anti-Patterns, Open in orq.ai, Documentation & Resolution). For the 3 protected entry points (`orq-agent.md`, `prompt.md`, `architect.md`), new sections sit OUTSIDE the `<pipeline>` XML block so the golden SHA-256 captured in Plan 01 remains valid. `allowed-tools:` frontmatter is already present on all 15 files (verified 2026-04-20); this plan only confirms it and does not modify frontmatter.

Purpose: Commands are the user-facing entry points; they need the contract sections above the body so users see "When NOT to use this" before reading the pipeline. Protected commands need structural stability — new sections go outside `<pipeline>` to preserve the orchestration loop the users invoke.

Output: All 15 command files conform to the SKST format. `bash orq-agent/scripts/lint-skills.sh --files orq-agent/commands` exits 0. `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-skill-structure-format-foundation/34-CONTEXT.md
@.planning/phases/34-skill-structure-format-foundation/34-RESEARCH.md
@.planning/phases/34-skill-structure-format-foundation/34-VALIDATION.md
@.planning/phases/34-skill-structure-format-foundation/34-01-SUMMARY.md

<interfaces>
<!-- Canonical section template for every command file. Copy this into each file with per-skill customization. -->

**Required H2 headings (9 total, exact strings):**

```
## Constraints
## When to use
## When NOT to use
## Companion Skills
## Done When
## Destructive Actions
## Anti-Patterns
## Open in orq.ai
## Documentation & Resolution
```

**Positioning rule (from RESEARCH.md line 238-240):**
- Constraints, When to use, When NOT to use, Companion Skills, Done When, Destructive Actions → INSERT ABOVE existing body (after frontmatter + after any opening `<role>` / `<files_to_read>` blocks, BUT BEFORE `<pipeline>`)
- Anti-Patterns, Open in orq.ai, Documentation & Resolution → APPEND at end of file (AFTER `</pipeline>` close tag)

**CRITICAL for orq-agent.md, prompt.md, architect.md:** New sections MUST NOT be inserted between `<pipeline>` and `</pipeline>`. Verify post-edit by running `bash orq-agent/scripts/check-protected-pipelines.sh` — it must exit 0.

**Canonical "Documentation & Resolution" footer (verbatim — copy this into every command):**

```markdown
## Documentation & Resolution

When skill content conflicts with live API behavior or official docs, trust the source higher in this list:

1. **orq MCP tools** — query live data first (`search_entities`, `get_agent`, `models-list`); API responses are authoritative.
2. **orq.ai documentation MCP** — use `search_orq_ai_documentation` or `get_page_orq_ai_documentation`.
3. **Official docs** — browse https://docs.orq.ai directly.
4. **This skill file** — may lag behind API or docs changes.
```

**Canonical "Constraints" block shape (per file — NEVER/ALWAYS phrasing is skill-specific but structure is fixed):**

```markdown
## Constraints

- **NEVER** <skill-specific prohibition 1>
- **NEVER** <skill-specific prohibition 2>
- **ALWAYS** <skill-specific requirement 1>
- **ALWAYS** <skill-specific requirement 2>

**Why these constraints:** <1-3 sentence explainer tied to what breaks when violated>
```

**Per-command Destructive Action Inventory (from RESEARCH.md lines 352-375) — use these verbatim in each file's `## Destructive Actions` section:**

| Command | Destructive Actions content |
|---------|-----------------------------|
| `orq-agent.md` | Creates `{OUTPUT_DIR}/[swarm-name]/` (auto-versions to `-v2`, `-v3` on collision — no prompt needed). Writes blueprint.md, TOOLS.md, research-brief.md, all specs, datasets, orchestration-doc, README in that directory. No deletions. |
| `prompt.md` | Creates `{OUTPUT_DIR}/[agent-name]/` (auto-versioned on collision). Writes a single spec file. No deletions. |
| `architect.md` | Creates `{OUTPUT_DIR}/[swarm-name]/` (auto-versioned). Writes blueprint.md only. **AskUserQuestion confirm required before** overwriting an existing blueprint.md in the current (auto-versioned) directory. |
| `deploy.md` | **AskUserQuestion confirm required before** creating or updating agents on Orq.ai via MCP (idempotent create-or-update overwrites live agent config). Attaches tools, creates KBs. |
| `test.md` | Creates datasets on Orq.ai, creates experiments. Non-destructive to existing agent config. **AskUserQuestion confirm required before** creating a new dataset when one with the same name already exists on Orq.ai. |
| `iterate.md` | **AskUserQuestion confirm required before** modifying local agent spec files (prompt-editor writes) AND before re-deploying to Orq.ai (creates new agent version, modifies live config). |
| `harden.md` | **AskUserQuestion confirm required before** attaching guardrails to deployed agents on Orq.ai (modifies live agent config) and before promoting evaluators to runtime guardrails. |
| `kb.md` | **AskUserQuestion confirm required before** creating a KB on Orq.ai when one with the same name already exists (overwrites). Uploads files. |
| `systems.md` | **AskUserQuestion confirm required before** editing `orq-agent/systems.md` (add/remove operations modify local config). |
| `set-profile.md` | **AskUserQuestion confirm required before** overwriting `.orq-agent/config.json`. |
| `update.md` | **AskUserQuestion confirm required before** overwriting local skill files with GitHub versions (destructive to any uncommitted local edits). |
| `datasets.md` | Writes dataset files under `{OUTPUT_DIR}/`. **AskUserQuestion confirm required before** overwriting an existing dataset file. |
| `research.md` | Writes `{OUTPUT_DIR}/[swarm-name]/research-brief.md`. **AskUserQuestion confirm required before** overwriting an existing research-brief.md. |
| `tools.md` | Writes `{OUTPUT_DIR}/[swarm-name]/TOOLS.md`. **AskUserQuestion confirm required before** overwriting an existing TOOLS.md. |
| `help.md` | Read-only. Destructive Actions section content: `- **None** — this command is read-only (prints help text).` |

**Per-command Companion Skills (from RESEARCH.md lines 309-345) — derive arrows from the graph:**

| Command | Companion Skills bullets |
|---------|-------------------------|
| `orq-agent.md` | → `architect` (Step 3), → `tool-resolver`, → `researcher`, → `spec-generator`, → `orchestration-generator`, → `dataset-generator`, → `readme-generator`; ← user invocation |
| `prompt.md` | → `spec-generator` (only subagent); ← user invocation (fast path, skips full pipeline) |
| `architect.md` | → `architect` subagent (produces blueprint.md); ← user invocation; downstream consumers: `/orq-agent:tools`, `/orq-agent:research`, `/orq-agent` |
| `deploy.md` | → `deployer`; ← `/orq-agent` (after spec generation); → `/orq-agent:test` (typical next step) |
| `test.md` | → `tester`, → `dataset-preparer`, → `experiment-runner`, → `results-analyzer`; ← `/orq-agent:deploy`; → `/orq-agent:iterate` (on failure) |
| `iterate.md` | → `failure-diagnoser`, → `iterator`, → `prompt-editor`; ← `/orq-agent:test`; → `/orq-agent:deploy` (re-deploy); → `/orq-agent:test` (re-test); → `/orq-agent:harden` (on green) |
| `harden.md` | → `hardener`; ← `/orq-agent:iterate` or `/orq-agent:test`; terminal step in pipeline |
| `kb.md` | → `kb-generator`; ← `/orq-agent` (when agent spec references KB); → `/orq-agent:deploy` (KB attached to agent) |
| `datasets.md` | → `dataset-generator`; ← `/orq-agent` (full-pipeline invocation) or standalone |
| `research.md` | → `researcher`; ← `/orq-agent` (Step after architect+tools) or standalone |
| `tools.md` | → `tool-resolver`; ← `/orq-agent` (Step after architect) or standalone |
| `systems.md` | No subagent — manages local `orq-agent/systems.md` directly; ← referenced by `architect`, `deployer`, `tool-resolver` |
| `set-profile.md` | No subagent — manages local `.orq-agent/config.json`; ← read by every command via `orq-agent/SKILL.md` context |
| `update.md` | No subagent — pulls from GitHub. Meta-command, no pipeline linkage. |
| `help.md` | No subagent — lists all commands. Meta-command. |

**Per-command "Open in orq.ai" URLs (from RESEARCH.md lines 397-409):**

| Command | Open in orq.ai bullets |
|---------|-----------------------|
| `orq-agent.md` | - **Agent Studio:** https://my.orq.ai/agents · - **Deployments:** https://my.orq.ai/deployments |
| `prompt.md` | - **Agent Studio:** https://my.orq.ai/agents · - **Prompts:** https://my.orq.ai/prompts |
| `architect.md` | - **Agent Studio:** https://my.orq.ai/agents · - **Deployments:** https://my.orq.ai/deployments |
| `deploy.md` | - **Deployments:** https://my.orq.ai/deployments · - **Agent Studio:** https://my.orq.ai/agents |
| `test.md` | - **Experiments:** https://my.orq.ai/experiments · - **Datasets:** https://my.orq.ai/datasets · - **Evaluators:** https://my.orq.ai/evaluators |
| `iterate.md` | - **Experiments:** https://my.orq.ai/experiments · - **Prompts:** https://my.orq.ai/prompts · - **Traces:** https://my.orq.ai/traces |
| `harden.md` | - **Deployments (guardrails tab):** https://my.orq.ai/deployments · - **Evaluators:** https://my.orq.ai/evaluators |
| `kb.md` | - **Knowledge Bases:** https://my.orq.ai/knowledge-bases |
| `datasets.md` | - **Datasets:** https://my.orq.ai/datasets · - **Annotation Queues:** https://my.orq.ai/annotation-queues `<!-- TODO(SKST-10): verified in Phase 37+ -->` |
| `research.md` | - **AI Router / Models:** https://my.orq.ai/models |
| `tools.md` | - **Agent Studio (tools tab):** https://my.orq.ai/agents |
| `systems.md` | - **N/A** — this skill manages local configuration only (no Orq.ai entities involved) |
| `set-profile.md` | - **N/A** — this skill manages local configuration only (no Orq.ai entities involved) |
| `update.md` | - **N/A** — this skill manages local configuration only (no Orq.ai entities involved) |
| `help.md` | - **N/A** — this skill is a local help command (no Orq.ai entities involved) |

**Inferred URL markers:** For `datasets.md`'s Annotation Queues bullet and any URL flagged as inferred in RESEARCH.md line 395, append inline HTML comment `<!-- TODO(SKST-10): verified in Phase 37+ -->`.

**Current frontmatter state (verified 2026-04-20):** All 15 command files already have non-empty `allowed-tools:`. Do NOT modify existing frontmatter lines; just confirm via lint.

**XML-tagged files (orq-agent.md, prompt.md, architect.md):** These files open with `<role>...</role>` or `<files_to_read>` blocks followed by `<pipeline>...</pipeline>`. Insert the 6 pre-body sections BETWEEN the closing `</role>` (or equivalent) tag and the opening `<pipeline>` tag. NEVER inside any XML block. Append the 3 footer sections AFTER the closing `</pipeline>` tag.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 9 SKST sections to the 3 protected commands (orq-agent.md, prompt.md, architect.md) WITHOUT modifying their `<pipeline>` blocks</name>
  <files>
    orq-agent/commands/orq-agent.md,
    orq-agent/commands/prompt.md,
    orq-agent/commands/architect.md
  </files>
  <read_first>
    - orq-agent/commands/orq-agent.md (full read — understand current XML structure and where `<pipeline>` starts/ends)
    - orq-agent/commands/prompt.md (full read)
    - orq-agent/commands/architect.md (full read — compare against Example 1 in RESEARCH.md lines 510-596 which is the canonical version for this file)
    - .planning/phases/34-skill-structure-format-foundation/34-RESEARCH.md (lines 161-236 for Section Template, lines 510-596 for Example 1 applied to architect.md, lines 278-283 for Anti-Pattern example)
    - .planning/phases/34-skill-structure-format-foundation/golden/orq-agent.sha256 (just to confirm baseline exists; do NOT modify)
    - .planning/phases/34-skill-structure-format-foundation/golden/prompt.sha256
    - .planning/phases/34-skill-structure-format-foundation/golden/architect.sha256
  </read_first>
  <action>
    For each of the 3 files, perform these EXACT operations in order:

    **Step 1 — Locate insertion points:** Find the line number where `<pipeline>` opens and where `</pipeline>` closes. Record both. The pre-body sections go IMMEDIATELY BEFORE `<pipeline>` (on the line above it). The footer sections go IMMEDIATELY AFTER `</pipeline>` (on the line below it).

    **Step 2 — Insert pre-body sections (above `<pipeline>`) in THIS ORDER:**

    ```markdown
    ## Constraints

    - **NEVER** <from per-file content below>
    - **NEVER** <from per-file content below>
    - **ALWAYS** <from per-file content below>
    - **ALWAYS** <from per-file content below>

    **Why these constraints:** <from per-file content below>

    ## When to use

    <from per-file content below>

    ## When NOT to use

    <from per-file content below>

    ## Companion Skills

    Directional handoffs (→ means "this skill feeds into"):

    <from Companion Skills table in <interfaces>>

    ## Done When

    <from per-file content below>

    ## Destructive Actions

    The following actions MUST confirm via `AskUserQuestion` before proceeding:

    <from Destructive Action Inventory table in <interfaces>>
    ```

    **Per-file content for orq-agent.md:**
    - NEVER rules: "generate more than 5 agents in a single swarm — decompose into sub-swarms", "skip the Blueprint → Specs → Tests → Iterate → Harden pipeline order"
    - ALWAYS rules: "default to single-agent; multi-agent must be justified via one of the 5 complexity gates", "write output to an auto-versioned `{OUTPUT_DIR}/[swarm-name]/` directory"
    - Why: "Over-engineered swarms cost more to maintain than a well-configured single agent. Skipping pipeline order skips validation gates (tool-resolver, researcher) that catch bad blueprints before specs are written."
    - When to use: "User types `/orq-agent \"build a ...\"` to get a complete swarm from a use case description", "User wants the full Build → Deploy → Test → Iterate → Harden loop"
    - When NOT to use: "User wants only a blueprint → use `/orq-agent:architect`", "User wants only a single agent spec → use `/orq-agent:prompt`", "User wants to deploy an existing spec → use `/orq-agent:deploy` directly"
    - Done When: "[ ] blueprint.md, TOOLS.md, research-brief.md, all agent specs, ORCHESTRATION.md (if multi-agent), dataset file, README.md exist in `{OUTPUT_DIR}/[swarm-name]/`", "[ ] Every agent spec has snapshot-pinned model reference", "[ ] README documents how to invoke the deployed swarm"

    **Per-file content for prompt.md:**
    - NEVER rules: "design multi-agent swarms — this is the single-agent fast path", "skip model pinning even on fast path"
    - ALWAYS rules: "produce a single agent spec matching the use case", "use snapshot-pinned model reference (e.g. `claude-sonnet-4-5-20250929`)"
    - Why: "The fast path exists for one-off agents where full pipeline overhead is not justified. Multi-agent intent belongs in `/orq-agent`. Pinning models prevents silent model upgrades from breaking production prompts."
    - When to use: "User types `/orq-agent:prompt \"build a single X agent\"` and explicitly wants fast iteration without the full pipeline", "Use case is clearly a single-agent task (extraction, classification, summarization)"
    - When NOT to use: "Use case implies multiple roles/tools/KBs → use `/orq-agent` instead", "User needs deployment + testing → use `/orq-agent` (full pipeline) or `/orq-agent:deploy` after this command"
    - Done When: "[ ] Single agent spec file exists in `{OUTPUT_DIR}/[agent-name]/`", "[ ] Spec has model, instructions, tools (if any), and snapshot-pinned model reference", "[ ] README references the spec path"

    **Per-file content for architect.md:** Use Example 1 from RESEARCH.md lines 510-596 VERBATIM for Constraints, When to use, When NOT to use, Done When, Anti-Patterns. Use the Companion Skills bullets from the <interfaces> table above.

    **Step 3 — Append footer sections (below `</pipeline>`) in THIS ORDER:**

    ```markdown
    ## Anti-Patterns

    | Pattern | Do Instead |
    |---------|-----------|
    | <from per-file content below> | <corresponding fix> |
    | <from per-file content below> | <corresponding fix> |

    ## Open in orq.ai

    <from Open in orq.ai table in <interfaces>, one bullet per line>

    ## Documentation & Resolution

    <canonical 4-item footer from <interfaces>, verbatim>
    ```

    **Per-file Anti-Patterns content:**
    - orq-agent.md: "Skipping the architect step to get specs faster → architect produces the orchestration rationale that specs reference", "Using floating model aliases in specs (`claude-sonnet-4-5`) → pin to snapshot (`claude-sonnet-4-5-20250929`) per Phase 35 MSEL-02", "Generating datasets before tool-resolver runs → tool presence affects dataset shape"
    - prompt.md: "Using `/orq-agent:prompt` for a swarm of 2+ agents → use `/orq-agent` for multi-agent", "Skipping tool resolution when the agent needs tools → even fast-path specs should cite the canonical tool from `orq-agent/references/tool-catalog.md`"
    - architect.md: Use the 4 rows from RESEARCH.md Example 1 (lines 577-581) verbatim.

    **Step 4 — Verify per-file AFTER editing each file:**
    - Run `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/<cmd>.md` — must exit 0.
    - Run `bash orq-agent/scripts/check-protected-pipelines.sh` — must exit 0 (proves `<pipeline>` block unchanged).

    **CRITICAL:** If `check-protected-pipelines.sh` fails after editing any of these 3 files, you accidentally edited inside the `<pipeline>` block. Revert and re-apply with insertions strictly OUTSIDE.

    Do NOT modify frontmatter, `<role>`, `<files_to_read>`, or `<pipeline>` blocks. Do NOT change any existing prose outside the insertion points. This is additive-only.
  </action>
  <verify>
    <automated>bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/commands/orq-agent.md && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/commands/prompt.md && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/commands/architect.md && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/check-protected-pipelines.sh</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 3 files contains ALL 9 required H2 headings (grep -qF for each of: `## Constraints`, `## When to use`, `## When NOT to use`, `## Companion Skills`, `## Done When`, `## Destructive Actions`, `## Anti-Patterns`, `## Open in orq.ai`, `## Documentation & Resolution`)
    - `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/orq-agent.md` exits 0
    - `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/prompt.md` exits 0
    - `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/architect.md` exits 0
    - `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0 — proves the `<pipeline>` block SHA-256 is unchanged for all 3 files
    - Each file's `## Documentation & Resolution` section contains the literal string `orq MCP tools — query live data first`
    - Each file's `## Open in orq.ai` section contains at least one bullet starting with `- **` followed by a label and `https://my.orq.ai/` URL (except for files mapped to N/A — orq-agent.md, prompt.md, architect.md all have real URLs)
    - `grep -c '^allowed-tools:' orq-agent/commands/orq-agent.md` returns 1 (frontmatter untouched)
    - New sections appear OUTSIDE `<pipeline>` tags: `awk '/^<pipeline>$/{flag=1;next} /^<\/pipeline>$/{flag=0} flag' orq-agent/commands/orq-agent.md | grep -c "^## "` returns 0
  </acceptance_criteria>
  <done>
    All 3 protected command files have the 9 SKST sections inserted above and below `<pipeline>`. Lint passes for all 3. Protected-pipeline SHA-256 still matches Plan 01's golden — proving the byte-identical criterion (operationally defined as "<pipeline> block unchanged"). No frontmatter modifications.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add 9 SKST sections to the 6 mid-pipeline commands (deploy, test, iterate, harden, kb, datasets)</name>
  <files>
    orq-agent/commands/deploy.md,
    orq-agent/commands/test.md,
    orq-agent/commands/iterate.md,
    orq-agent/commands/harden.md,
    orq-agent/commands/kb.md,
    orq-agent/commands/datasets.md
  </files>
  <read_first>
    - orq-agent/commands/deploy.md (full read — note current structure, may or may not use XML tags)
    - orq-agent/commands/test.md (full read)
    - orq-agent/commands/iterate.md (full read)
    - orq-agent/commands/harden.md (full read)
    - orq-agent/commands/kb.md (full read)
    - orq-agent/commands/datasets.md (full read)
    - .planning/phases/34-skill-structure-format-foundation/34-RESEARCH.md (Section Template lines 161-236; Destructive Action Inventory lines 352-375; Companion Skill Graph lines 309-345)
  </read_first>
  <action>
    For each of the 6 files, apply the same pattern as Task 1:

    **If file has XML blocks (`<role>`, `<pipeline>`, `<files_to_read>`):** Insert 6 pre-body sections between closing `</role>` / `</files_to_read>` and opening `<pipeline>` (or before body start if no `<pipeline>`). Append 3 footer sections at end of file.

    **If file is pure Markdown (no XML tags):** Insert 6 pre-body sections immediately after frontmatter + H1 title. Append 3 footer sections at end of file.

    **Use the 9-section template from Task 1's <interfaces> block.** For per-file content, use the tables in this plan's <interfaces> block (Destructive Actions, Companion Skills, Open in orq.ai).

    **Per-file NEVER/ALWAYS rules (write these verbatim):**

    - **deploy.md:**
      - NEVER: "deploy without running `orq-agent:test` on the swarm first", "silently overwrite live Orq.ai agent config without AskUserQuestion confirmation"
      - ALWAYS: "confirm via AskUserQuestion before mutating any Orq.ai entity", "emit a deployment manifest listing agent IDs, versions, and URLs"
      - Why: "Deploying untested agents puts unverified behavior into production; silent overwrite of live agent config destroys rollback anchors."

    - **test.md:**
      - NEVER: "re-run the same experiment without at least re-seeding the dataset order (prevents order-dependent cache hits)", "treat experiment completion as quality validation — `/orq-agent:iterate` interprets the results"
      - ALWAYS: "use the deployed agent from `/orq-agent:deploy`, not a local spec", "include the holdout split when the dataset preparer produced one"
      - Why: "Tests are signal-generators, not quality gates. Running without the deployed agent tests a spec file, not production. Stale cache hits hide regressions."

    - **iterate.md:**
      - NEVER: "modify a deployed agent without `/orq-agent:deploy` re-run", "auto-apply prompt diffs without AskUserQuestion HITL approval"
      - ALWAYS: "present the failure-diagnoser output before proposing any prompt edit", "capture the before/after score delta in the iteration log"
      - Why: "Silent auto-apply destroys the HITL safety rail. Skipping diagnosis produces speculative fixes; the current failure mode must be named before the edit."

    - **harden.md:**
      - NEVER: "promote an evaluator to a guardrail unless TPR ≥ 90% AND TNR ≥ 90% on its test set (Phase 42 EVLD-08 baseline — currently enforced in spirit via hardener subagent)", "attach guardrails silently to a live agent"
      - ALWAYS: "confirm guardrail promotion via AskUserQuestion", "record the sample_rate decision with rationale"
      - Why: "Unvalidated guardrails produce false-positive rejections that break production traffic. Silent attachment removes the audit trail hardener depends on."

    - **kb.md:**
      - NEVER: "wire a KB to a deployment without retrieval quality testing (Phase 40 KBM-01)", "use memory-style stores for static reference data"
      - ALWAYS: "verify the embedding model is activated in AI Router before KB creation", "record the chunking strategy chosen and why"
      - Why: "KBs that return no relevant chunks silently hurt agent quality; activation failures produce opaque API errors; chunking choice drives retrieval precision."

    - **datasets.md:**
      - NEVER: "upload a dataset that violates coverage rules (every dimension value ≥2 datapoints, no value > 30% — Phase 39 DSET-03)", "delete datapoints during Mode-4 curation without AskUserQuestion confirmation"
      - ALWAYS: "tag every datapoint by category AND dimension (Phase 39 DSET-05)", "include 15-20% adversarial cases from the 8-vector catalog when the agent profile warrants it"
      - Why: "Biased coverage produces over-fit evaluators; unconfirmed deletion loses signal irretrievably; tags enable slice analysis in results-analyzer."

    **Per-file When to use / When NOT to use / Done When — derive from each file's existing prose.** Keep each bullet concrete and falsifiable. Example for deploy.md:
    - When to use: "A swarm has been generated via `/orq-agent`", "User has reviewed specs and is ready to push to Orq.ai"
    - When NOT to use: "Specs have not been generated → run `/orq-agent` first", "User wants to test a single agent version locally → invoke directly via MCP without deploying"
    - Done When: "[ ] Agent IDs printed for every spec deployed", "[ ] Deployment variants registered on Orq.ai match `spec.deployment`", "[ ] Tools attached to agents match `spec.tools`", "[ ] KBs (if any) attached and indexable"

    **Per-file Anti-Patterns (at least 2 rows per file):** Extract from each file's existing prose — every "don't" currently in the body becomes a row.

    **Step — Verify per-file AFTER each edit:**
    ```
    bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/<file>.md
    ```

    Must exit 0. No XML-block violations (new sections stay outside any XML envelopes).
  </action>
  <verify>
    <automated>bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/commands/deploy.md && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/commands/test.md && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/commands/iterate.md && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/commands/harden.md && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/commands/kb.md && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/commands/datasets.md</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 6 files contains all 9 required H2 headings
    - `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/<each>` exits 0 for each
    - Each file's Destructive Actions section contains the literal string `AskUserQuestion` (except files where "None — read-only" applies, which is none of these 6)
    - Each file's Constraints section contains both `**NEVER**` and `**ALWAYS**` (grep -qF for each)
    - Each file's Constraints section contains `**Why these constraints:**`
    - Each file's Companion Skills section contains at least one `→ ` arrow or `← ` arrow
    - Each file's Open in orq.ai section contains at least one bullet with `https://my.orq.ai/` (these 6 files all have real URLs, not N/A)
    - `grep -c '^allowed-tools:' orq-agent/commands/<each>.md` returns 1 for each (frontmatter untouched)
    - If any of these 6 files contains a `<pipeline>` block, it is unchanged (`bash orq-agent/scripts/check-protected-pipelines.sh` unaffected — these 6 are not protected, but if they coincidentally have pipeline tags, they must not have new sections inside; verify via `awk` pipeline-block extraction has no `^## ` inside)
  </acceptance_criteria>
  <done>
    All 6 mid-pipeline command files have 9 SKST sections added. Lint passes per-file. Destructive Actions flagged with AskUserQuestion confirmation per RESEARCH.md inventory. No frontmatter modifications.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add 9 SKST sections to the 6 remaining commands (research, tools, systems, set-profile, update, help)</name>
  <files>
    orq-agent/commands/research.md,
    orq-agent/commands/tools.md,
    orq-agent/commands/systems.md,
    orq-agent/commands/set-profile.md,
    orq-agent/commands/update.md,
    orq-agent/commands/help.md
  </files>
  <read_first>
    - orq-agent/commands/research.md (full read)
    - orq-agent/commands/tools.md (full read)
    - orq-agent/commands/systems.md (full read)
    - orq-agent/commands/set-profile.md (full read)
    - orq-agent/commands/update.md (full read)
    - orq-agent/commands/help.md (full read)
    - .planning/phases/34-skill-structure-format-foundation/34-RESEARCH.md (Lint exception for local-config skills lines 407-409 — for systems, set-profile, update, help the Open in orq.ai bullet is N/A)
  </read_first>
  <action>
    Apply the same 9-section insertion pattern as Task 2. Key notes:

    **Local-config commands (systems, set-profile, update, help) use the N/A Open in orq.ai bullet:**
    ```
    ## Open in orq.ai

    - **N/A** — this skill manages local configuration only (no Orq.ai entities involved)
    ```
    The lint accepts `N/A` as satisfying SKST-10 per RESEARCH.md line 409.

    **help.md Destructive Actions is minimal:**
    ```
    ## Destructive Actions

    - **None** — this command is read-only (prints help text for all orq-agent commands).
    ```

    **Per-file NEVER/ALWAYS rules (verbatim):**

    - **research.md:**
      - NEVER: "recommend a model from outside the AI Router without flagging activation requirements", "start with the cheapest model — Phase 35 MSEL-01 requires capable-first"
      - ALWAYS: "pin model snapshots in the research brief (Phase 35 MSEL-02)", "cross-reference `orq-agent/references/orqai-model-catalog.md` before recommending a model"
      - Why: "Floating aliases silently upgrade; non-activated models fail at deploy time; starting cheap produces biased quality baselines."

    - **tools.md:**
      - NEVER: "invent tool names not present in `orq-agent/references/tool-catalog.md`", "attach tools without verifying schema compatibility with the agent's input/output shape"
      - ALWAYS: "use canonical tool IDs from the catalog", "document each tool's rationale in TOOLS.md"
      - Why: "Non-catalog tools fail at deploy with opaque MCP errors; unjustified tool attachments bloat agent context."

    - **systems.md:**
      - NEVER: "add systems without documenting their integration method (MCP / REST / manual)", "remove systems that are referenced by existing swarms without AskUserQuestion confirmation"
      - ALWAYS: "confirm via AskUserQuestion before editing systems.md", "keep system entries alphabetical"
      - Why: "Systems registry is consumed by architect and tool-resolver — silent edits break downstream blueprints. Alphabetical ordering minimizes merge conflicts."

    - **set-profile.md:**
      - NEVER: "overwrite `.orq-agent/config.json` without AskUserQuestion confirmation", "accept a profile that doesn't exist in the documented set (core / deploy / test / full)"
      - ALWAYS: "confirm the target profile before writing", "print the resulting profile contents after write"
      - Why: "Profile changes gate which commands are available; silent changes confuse users who invoke a command expecting an older profile's tooling."

    - **update.md:**
      - NEVER: "overwrite local skill edits without AskUserQuestion confirmation showing the diff", "update when there are uncommitted changes in `orq-agent/`"
      - ALWAYS: "show a summary of files to be overwritten before proceeding", "print the GitHub commit/tag being pulled"
      - Why: "Users often make local skill edits for their deployment — silent overwrite loses that work. Confirming keeps the safety rail."

    - **help.md:**
      - NEVER: "omit a command from the help index", "show deprecated commands as if they were active"
      - ALWAYS: "list commands in the pipeline-order that `/orq-agent` follows", "flag `/orq-agent:prompt` and `/orq-agent:architect` as lateral side-entries"
      - Why: "Help is the first thing new users read; pipeline-order drives mental model; mis-labeled lateral commands confuse the flow."

    **Per-file Companion Skills, Destructive Actions, Open in orq.ai:** Use the tables in this plan's <interfaces> block VERBATIM.

    **Per-file Done When — derive from each command's current success criteria in prose.** Example for research.md: "[ ] research-brief.md written to `{OUTPUT_DIR}/[swarm-name]/`", "[ ] Every agent in scope has a model recommendation with capable-first rationale", "[ ] Alternative models listed with tradeoff paragraph".

    **Per-file Anti-Patterns (at least 2 rows per file):** Extract from existing prose.

    **Step — Verify per-file after each edit:**
    ```
    bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/<file>.md
    ```

    Must exit 0.

    **Final verification (after all 6 files edited):**
    ```
    bash orq-agent/scripts/lint-skills.sh --files orq-agent/commands
    ```
    Must exit 0 — proving ALL 15 command files (3 from Task 1 + 6 from Task 2 + 6 from this task) are conformant.
  </action>
  <verify>
    <automated>bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --files /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/commands && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/check-protected-pipelines.sh</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 6 files contains all 9 required H2 headings
    - `bash orq-agent/scripts/lint-skills.sh --files orq-agent/commands` exits 0 (proves ALL 15 command files pass lint)
    - `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0 (protected entry points still byte-identical)
    - `systems.md`, `set-profile.md`, `update.md`, `help.md` Open in orq.ai section contains the literal string `N/A`
    - `help.md` Destructive Actions section contains the literal string `None` followed by `read-only`
    - `grep -c '^allowed-tools:' orq-agent/commands/<each>.md` returns 1 for each of the 6 files
  </acceptance_criteria>
  <done>
    All 15 command files now conform to the SKST format. Full directory lint passes. Protected-pipeline check still passes. No frontmatter modifications. Local-config commands correctly use the N/A pattern for Open in orq.ai. Plan is complete — downstream Plan 03 can proceed independently on subagents.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. `bash orq-agent/scripts/lint-skills.sh --files orq-agent/commands` → exits 0 (all 15 commands pass all rules).
2. `bash orq-agent/scripts/check-protected-pipelines.sh` → exits 0 (orq-agent, prompt, architect `<pipeline>` blocks unchanged).
3. `for f in orq-agent/commands/*.md; do grep -qF "## Constraints" "$f" || echo "MISSING: $f"; done` → produces no output.
4. Same check for each of the other 8 required sections → produces no output.
5. `grep -rc '^allowed-tools:' orq-agent/commands/*.md | awk -F: '$2 != 1 {print; exit 1}'` → empty (every command has exactly one allowed-tools line).
6. No files under `orq-agent/agents/` or `orq-agent/SKILL.md` are modified by this plan.
</verification>

<success_criteria>
- [ ] All 15 files in `orq-agent/commands/` contain the 9 required H2 sections
- [ ] `bash orq-agent/scripts/lint-skills.sh --files orq-agent/commands` exits 0
- [ ] `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0 (byte-identical `<pipeline>` for orq-agent, prompt, architect)
- [ ] All 15 files still declare non-empty `allowed-tools:` in frontmatter (no regressions)
- [ ] Destructive Actions sections reference `AskUserQuestion` for the 10 commands with destructive operations (deploy, test, iterate, harden, kb, datasets, systems, set-profile, update, architect) and use `None — read-only` for help.md
- [ ] Local-config commands (systems, set-profile, update, help) use the `N/A` pattern for Open in orq.ai
- [ ] Zero modifications to `orq-agent/agents/` or `orq-agent/SKILL.md`
</success_criteria>

<output>
After completion, create `.planning/phases/34-skill-structure-format-foundation/34-02-SUMMARY.md` documenting:
- List of 15 command files modified
- Confirmation that all 9 sections are present per file
- Confirmation that `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0 post-edit
- Any per-file deviations (e.g., Anti-Patterns row counts, N/A URL uses)
- Pointer to Plan 03 (subagents) which runs in the same wave
</output>
