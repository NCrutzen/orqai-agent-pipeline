---
phase: 34-skill-structure-format-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - "34-01"
  - "34-02"
  - "34-03"
files_modified:
  - orq-agent/SKILL.md
autonomous: true
requirements:
  - SKST-01
  - SKST-02
  - SKST-03
  - SKST-04
  - SKST-05
  - SKST-06
  - SKST-07
  - SKST-08
  - SKST-09
  - SKST-10
must_haves:
  truths:
    - "`orq-agent/SKILL.md` declares a non-empty `allowed-tools:` in frontmatter"
    - "`orq-agent/SKILL.md` contains all 9 required SKST H2 sections"
    - "`orq-agent/SKILL.md` contains a Resources Policy subsection documenting the `orq-agent/references/` (shared, multi-consumer) vs `<skill>/resources/` (single-consumer) rule"
    - "`bash orq-agent/scripts/lint-skills.sh --file orq-agent/SKILL.md` exits 0"
    - "`bash orq-agent/scripts/lint-skills.sh --rule references-multi-consumer` exits 0 (all 8 files under `orq-agent/references/` still have ≥2 skill consumers — no regressions from Plans 02/03)"
  artifacts:
    - path: "orq-agent/SKILL.md"
      provides: "Top-level skill index with full 9-section SKST superset + allowed-tools frontmatter + Resources Policy subsection"
      contains: "allowed-tools:"
  key_links:
    - from: "orq-agent/SKILL.md frontmatter"
      to: "Claude Code skill schema"
      via: "`allowed-tools:` key (top-level SKILL.md uses command/skill schema per RESEARCH.md lines 81-97)"
      pattern: "^allowed-tools:"
    - from: "orq-agent/SKILL.md Resources Policy"
      to: "orq-agent/scripts/lint-skills.sh --rule references-multi-consumer"
      via: "Policy text documents the invariant; lint rule enforces it mechanically"
      pattern: "Single-consumer resources|Shared references live"
---

<objective>
Apply the 9-section SKST format to `orq-agent/SKILL.md` (the top-level skill index), add `allowed-tools:` to its frontmatter (the only command/skill file currently missing this key per RESEARCH.md line 97), and add a "Resources Policy" subsection documenting the invariant that `orq-agent/references/` holds multi-consumer docs while `<skill>/resources/` holds single-consumer docs. Per locked decision #2 from the orchestrator, SKILL.md gets the FULL 9-section superset where meaningful — "When to use" / "When NOT to use" / "Companion Skills" describe the skill-suite entry point (how/when to load `orq-agent/` as context), not a single command.

Purpose: SKILL.md is the skill-suite contract. Without the SKST sections + Resources Policy, downstream V3.0 phases (36-43) have no canonical documentation for the resources migration invariant. Adding `allowed-tools:` satisfies SKST-01 for the one file in the scope that currently lacks it.

Output: `orq-agent/SKILL.md` conforms to all 10 SKST requirements. Lint passes on the file. The references-multi-consumer lint rule still exits 0 (RESEARCH.md confirms all 8 references have ≥2 consumers — Plans 02/03 did not create new reference files).
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-skill-structure-format-foundation/34-CONTEXT.md
@.planning/phases/34-skill-structure-format-foundation/34-RESEARCH.md
@.planning/phases/34-skill-structure-format-foundation/34-VALIDATION.md
@.planning/phases/34-skill-structure-format-foundation/34-01-SUMMARY.md
@.planning/phases/34-skill-structure-format-foundation/34-02-SUMMARY.md
@.planning/phases/34-skill-structure-format-foundation/34-03-SUMMARY.md

<interfaces>
<!-- SKILL.md current frontmatter (verified 2026-04-20 — has `name:` + `description:` only, missing `allowed-tools:`): -->

```
---
name: orq-agent
description: Generate complete, copy-paste-ready Orq.ai Agent specifications with orchestration logic from use case descriptions
---
```

**Target frontmatter for SKILL.md (after this plan):**

```
---
name: orq-agent
description: Generate complete, copy-paste-ready Orq.ai Agent specifications with orchestration logic from use case descriptions
allowed-tools: Read, Write, Edit, Glob, Grep, Bash, WebSearch, WebFetch, Task
---
```

Rationale for the `allowed-tools:` value: SKILL.md is loaded as context by every `/orq-agent*` command. The union of tools needed across the command suite is: Read (spec reading), Write/Edit (spec generation), Glob/Grep (codebase discovery), Bash (lint scripts + deploy helpers), WebSearch/WebFetch (researcher), Task (subagent invocation). This matches the superset declared in `orq-agent/commands/orq-agent.md` (the full-pipeline orchestrator). No MCP tools declared at top-level since MCP availability is runtime-detected per skill.

**Same 9 required H2 headings as Plans 02/03 (exact strings — grep -qF match):**

```
## Constraints
## When to use
## When NOT to use
## Companion Skills
## Done When
## Destructive Actions
## Anti-Patterns
## Open in orq.ai
## Documentation & Resolution
```

**Positioning for SKILL.md:** SKILL.md is pure Markdown (no XML tags). Insert the 6 pre-body sections immediately after the frontmatter block + file H1 title (`# Orq Agent Designer` or similar — preserve the existing H1). The 3 footer sections go at end of file.

**New Resources Policy subsection (target location: WITHIN the body, under whatever existing section documents the directory layout — likely a section titled "Directory layout" or "Structure"; if no such section exists, append a new `## Resources Policy` H2 section before the footer sections):**

Verbatim content for the Resources Policy:

```markdown
## Resources Policy

Skill documentation lives in two places. The placement rule is driven by consumer count:

- **`orq-agent/references/`** — Shared references consumed by 2+ skills. All 8 current files (`agentic-patterns.md`, `naming-conventions.md`, `orchestration-patterns.md`, `orqai-agent-fields.md`, `orqai-api-endpoints.md`, `orqai-evaluator-types.md`, `orqai-model-catalog.md`, `tool-catalog.md`) are multi-consumer (verified 2026-04-20 — see `.planning/phases/34-skill-structure-format-foundation/34-RESEARCH.md` Reference Consumer Graph).

- **`<skill>/resources/`** — Single-consumer long-form docs. When a V3.0 skill (Phases 36-43) has reference content used by exactly one skill, that skill creates its own `resources/` subdirectory instead of adding to the flat `references/` directory.

**Invariant (enforced by lint):** Every file under `orq-agent/references/` MUST be consumed by ≥2 skills. The `references-multi-consumer` rule in `orq-agent/scripts/lint-skills.sh` enforces this. If a file drops to 1 consumer, the lint fails and the file must move to that consumer's `<skill>/resources/`.

**Migration status:** No existing references qualify for migration (all 8 have ≥2 consumers). Phases 36-43 will create per-skill `resources/` directories on demand when single-consumer content appears.
```

This Resources Policy subsection satisfies SKST-02 (documenting the policy) AND is grep-verifiable by the lint rule `references-multi-consumer` (which enforces the invariant mechanically).

**Canonical footer sections (VERBATIM — same as Plans 02/03):**

```markdown
## Anti-Patterns

| Pattern | Do Instead |
|---------|-----------|
| Treating `orq-agent/` as loose scripts | Every command and subagent is a skill — follow the SKILL.md format conventions |
| Pre-creating empty `<skill>/resources/` directories | YAGNI — create on demand when single-consumer content actually appears |
| Adding a new file to `orq-agent/references/` without ≥2 consumers | Place it in the single consumer's `<skill>/resources/` instead; lint enforces this |
| Duplicating content between `references/` and a skill body | Reference the shared file via `orq-agent/references/<name>.md` — do not inline |
| Writing `allowed-tools:` into subagent frontmatter | Subagents use `tools:` (Claude Code schema); `allowed-tools:` is a no-op on subagents |

## Open in orq.ai

- **Agent Studio:** https://my.orq.ai/agents
- **Deployments:** https://my.orq.ai/deployments
- **Experiments:** https://my.orq.ai/experiments
- **Traces:** https://my.orq.ai/traces

## Documentation & Resolution

When skill content conflicts with live API behavior or official docs, trust the source higher in this list:

1. **orq MCP tools** — query live data first (`search_entities`, `get_agent`, `models-list`); API responses are authoritative.
2. **orq.ai documentation MCP** — use `search_orq_ai_documentation` or `get_page_orq_ai_documentation`.
3. **Official docs** — browse https://docs.orq.ai directly.
4. **This skill file** — may lag behind API or docs changes.
```

**Meta-framing for SKILL.md's non-trivial sections (per RESEARCH.md Open Question #2 line 652-656):**

- **Constraints** — constraints on using the skill suite as a whole
- **When to use** — "Claude loads this file as context for every `/orq-agent*` command"
- **When NOT to use** — "For non-Orq.ai agent work" (redirect to other skills)
- **Companion Skills** — the top-level pipeline (architect → spec-generator → ... → hardener)
- **Done When** — "All subagents + commands listed below match disk reality"
- **Destructive Actions** — "N/A at the suite level — individual commands/subagents declare their own destructive actions"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `allowed-tools:` to SKILL.md frontmatter + insert 9 SKST sections with meta-framing + add Resources Policy subsection</name>
  <files>orq-agent/SKILL.md</files>
  <read_first>
    - orq-agent/SKILL.md (full read — note current frontmatter, H1 title, directory layout section if any, and any existing sections that would receive new content)
    - .planning/phases/34-skill-structure-format-foundation/34-RESEARCH.md (lines 65, 81-97 for frontmatter schema; lines 285-303 for Reference Consumer Graph; Open Question #2 lines 652-656 for SKILL.md meta-framing guidance)
    - .planning/phases/34-skill-structure-format-foundation/34-CONTEXT.md (Claude's Discretion section — SKILL.md is explicitly in scope)
    - orq-agent/scripts/lint-skills.sh (from Plan 01 — check what the lint actually requires so we hit exact rule outputs)
    - orq-agent/commands/orq-agent.md (post-Plan-02 — it now has the 9 sections; use its allowed-tools value as the template for SKILL.md's allowed-tools)
  </read_first>
  <action>
    Perform these edits to `orq-agent/SKILL.md` in order:

    **Step 1 — Frontmatter update:** Add `allowed-tools:` line to the YAML frontmatter block (between the two `---` markers). Use this EXACT value:

    ```
    allowed-tools: Read, Write, Edit, Glob, Grep, Bash, WebSearch, WebFetch, Task
    ```

    Insert this line after the existing `description:` line. Do NOT modify the `name:` or `description:` lines. Resulting frontmatter:

    ```yaml
    ---
    name: orq-agent
    description: Generate complete, copy-paste-ready Orq.ai Agent specifications with orchestration logic from use case descriptions
    allowed-tools: Read, Write, Edit, Glob, Grep, Bash, WebSearch, WebFetch, Task
    ---
    ```

    **Step 2 — Insert 6 pre-body sections** immediately after the first H1 title in the file (identify via grep for `^# `). Order: Constraints → When to use → When NOT to use → Companion Skills → Done When → Destructive Actions. Content (use meta-framing per <interfaces> guidance):

    ```markdown
    ## Constraints

    - **NEVER** rename this file, any command file, or any subagent file — slash-command routing depends on these exact paths
    - **NEVER** add files to `orq-agent/references/` with fewer than 2 consumer skills — single-consumer docs go in `<skill>/resources/`
    - **ALWAYS** load this SKILL.md when any `/orq-agent*` command runs (it declares the skill suite's directory layout, profile rules, and shared conventions)
    - **ALWAYS** cross-check new skill files against `orq-agent/scripts/lint-skills.sh` before committing — the lint is the SKST-01..10 acceptance test

    **Why these constraints:** The skill suite depends on fixed file paths for slash-command routing; misplacing shared vs single-consumer references breaks the invariant the lint enforces; not running lint lets drift accumulate silently; skipping SKILL.md as context makes every command behave as if the suite's conventions don't exist.

    ## When to use

    - Claude loads this file as context for every `/orq-agent*` command (the suite's entry point)
    - Users want to understand the skill suite's scope, command inventory, or directory layout
    - Contributors to Phases 36-43 need to confirm the SKST format conventions before adding new skills

    ## When NOT to use

    - User wants to use a non-Orq.ai agent platform → no orq-agent skill applies
    - User wants to invoke a specific command → use that command's SKILL file (`orq-agent/commands/<name>.md`) directly, not this index
    - User wants to read a shared reference → use `orq-agent/references/<name>.md` directly

    ## Companion Skills

    Directional pipeline (→ means "typical next step"):

    - → `/orq-agent:architect` (blueprint creation) → `/orq-agent:tools` (tool resolution) → `/orq-agent:research` (model + pattern research) → `/orq-agent` (full pipeline: specs + orchestration + datasets + README) → `/orq-agent:deploy` → `/orq-agent:test` → `/orq-agent:iterate` → `/orq-agent:harden`
    - Lateral entry points: `/orq-agent:prompt` (single-agent fast path), `/orq-agent:architect` (blueprint-only), `/orq-agent:datasets`, `/orq-agent:kb`, `/orq-agent:research`, `/orq-agent:tools`
    - Meta-commands: `/orq-agent:systems`, `/orq-agent:set-profile`, `/orq-agent:update`, `/orq-agent:help`

    ## Done When

    - [ ] All 15 slash commands under `orq-agent/commands/` match the commands listed in the body below
    - [ ] All 17 subagents under `orq-agent/agents/` match the subagents listed in the body below
    - [ ] All 8 shared references under `orq-agent/references/` have ≥2 consumer skills (verified by `bash orq-agent/scripts/lint-skills.sh --rule references-multi-consumer`)
    - [ ] `bash orq-agent/scripts/lint-skills.sh` exits 0 across the full suite

    ## Destructive Actions

    - **N/A at the suite level** — individual commands and subagents declare their own Destructive Actions sections. This index file makes no mutations; it is loaded as context only.
    ```

    **Step 3 — Append Resources Policy subsection.** If the current SKILL.md body has a section titled "Directory layout" / "Structure" / similar, insert the Resources Policy immediately after that section. If no such section exists, append a new `## Resources Policy` H2 BEFORE the footer sections (Step 4). Use the Resources Policy content from <interfaces> VERBATIM.

    **Step 4 — Append 3 footer sections at end of file** (in this order): Anti-Patterns → Open in orq.ai → Documentation & Resolution. Use content from <interfaces> VERBATIM (including the 5-row Anti-Patterns table).

    **Step 5 — Do NOT modify any existing body content.** Preserve the current directory layout listing, command index, subagent index, profile documentation, etc. This plan is additive-only.

    **Step 6 — Verify:**
    ```
    bash orq-agent/scripts/lint-skills.sh --file orq-agent/SKILL.md
    ```
    Must exit 0. If it fails on `allowed-tools`, the frontmatter edit failed. If it fails on required-sections, a heading is missing or mis-spelled. If it fails on XML-block positioning, a section landed inside an XML block (SKILL.md should not have XML blocks, but double-check).

    **Step 7 — Verify the references-multi-consumer invariant still holds:**
    ```
    bash orq-agent/scripts/lint-skills.sh --rule references-multi-consumer
    ```
    Must exit 0. If it fails, a reference file lost a consumer — investigate the failure message and fix before proceeding.

    **Step 8 — Verify protected-pipeline is untouched** (this plan should not touch commands, but confirm):
    ```
    bash orq-agent/scripts/check-protected-pipelines.sh
    ```
    Must exit 0.
  </action>
  <verify>
    <automated>bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --file /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/SKILL.md && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --rule references-multi-consumer && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/check-protected-pipelines.sh</automated>
  </verify>
  <acceptance_criteria>
    - `orq-agent/SKILL.md` frontmatter contains the literal line `allowed-tools: Read, Write, Edit, Glob, Grep, Bash, WebSearch, WebFetch, Task` (grep -qF)
    - `orq-agent/SKILL.md` still contains `name: orq-agent` and `description: Generate complete, copy-paste-ready` (no existing frontmatter lines deleted)
    - `orq-agent/SKILL.md` contains all 9 required H2 headings (grep -qF for each)
    - `orq-agent/SKILL.md` contains the literal string `## Resources Policy`
    - `orq-agent/SKILL.md` contains the literal string `Single-consumer resources` (as the locked discovery from <interfaces>) — this is what the VALIDATION.md Task 34-04-02 specifically greps for
    - `orq-agent/SKILL.md` contains the literal string `references-multi-consumer` (explicit mention of the lint rule)
    - `orq-agent/SKILL.md` Destructive Actions section contains the literal string `N/A at the suite level`
    - `bash orq-agent/scripts/lint-skills.sh --file orq-agent/SKILL.md` exits 0
    - `bash orq-agent/scripts/lint-skills.sh --rule references-multi-consumer` exits 0 (no regression: all 8 reference files still multi-consumer)
    - `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0 (protected commands untouched)
    - `wc -l orq-agent/SKILL.md` shows the file grew (was roughly 50-100 lines; now should be at least 150 — sanity check that sections were actually added)
  </acceptance_criteria>
  <done>
    `orq-agent/SKILL.md` has `allowed-tools:` in frontmatter, all 9 SKST sections with meta-framing suitable for an index file, and a Resources Policy subsection documenting the shared-vs-single-consumer invariant with an explicit pointer to the `references-multi-consumer` lint rule. All three lint invariants (per-file lint, references-multi-consumer, protected-pipelines) still exit 0. No existing content was removed; this is additive-only.
  </done>
</task>

</tasks>

<verification>
After the task completes:

1. `bash orq-agent/scripts/lint-skills.sh --file orq-agent/SKILL.md` → exits 0.
2. `bash orq-agent/scripts/lint-skills.sh --rule allowed-tools` → exits 0 (proves all 15 commands + SKILL.md have the key).
3. `bash orq-agent/scripts/lint-skills.sh --rule references-multi-consumer` → exits 0 (invariant holds).
4. `bash orq-agent/scripts/check-protected-pipelines.sh` → exits 0 (no commands touched).
5. `grep -q "Single-consumer resources" orq-agent/SKILL.md` → exits 0.
6. `grep -q "^allowed-tools:" orq-agent/SKILL.md` → exits 0.
7. No files under `orq-agent/commands/`, `orq-agent/agents/`, or `orq-agent/scripts/` are modified by this plan.
</verification>

<success_criteria>
- [ ] `orq-agent/SKILL.md` frontmatter declares `allowed-tools:` (SKST-01)
- [ ] `orq-agent/SKILL.md` contains all 9 required H2 SKST sections (SKST-03..10 + SKST-06)
- [ ] `orq-agent/SKILL.md` contains the Resources Policy subsection documenting the multi-consumer vs single-consumer rule (SKST-02)
- [ ] `bash orq-agent/scripts/lint-skills.sh --file orq-agent/SKILL.md` exits 0
- [ ] `bash orq-agent/scripts/lint-skills.sh --rule references-multi-consumer` exits 0
- [ ] `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0
- [ ] Zero modifications to `orq-agent/commands/`, `orq-agent/agents/`, or `orq-agent/scripts/`
</success_criteria>

<output>
After completion, create `.planning/phases/34-skill-structure-format-foundation/34-04-SUMMARY.md` documenting:
- The diff applied to SKILL.md's frontmatter (added `allowed-tools:`)
- The 9 SKST sections added (with meta-framing rationale for Destructive Actions = N/A at suite level)
- The Resources Policy subsection (quote the key invariant paragraph)
- Confirmation that lint + references-multi-consumer + check-protected-pipelines all exit 0
- Pointer to Plan 05 (final verification sweep)
</output>
