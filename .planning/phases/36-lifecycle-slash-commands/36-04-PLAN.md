---
phase: 36-lifecycle-slash-commands
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/commands/models.md
autonomous: true
requirements:
  - LCMD-04
must_haves:
  truths:
    - "Running /orq-agent:models prints an ORQ ► MODELS banner with models grouped by provider"
    - "Models are broken out by type (chat, embedding, image, rerank, speech, etc.) within each provider"
    - "Optional positional [search-term] filters the listing (case-insensitive substring match on model id)"
    - "Output ends with an Open in orq.ai model-garden deep link + MCP tools used footer"
    - "Any example model IDs shown in prose are dated snapshots (Phase 35 MSEL-02 discipline)"
  artifacts:
    - path: "orq-agent/commands/models.md"
      provides: "LCMD-04 — models lookup slash command"
      contains: "## Constraints"
      contains_all:
        - "ORQ ► MODELS"
        - "allowed-tools:"
        - "argument-hint:"
        - "chat"
        - "embedding"
        - "image"
        - "rerank"
        - "provider"
        - "MCP tools used"
        - "my.orq.ai/model-garden"
  key_links:
    - from: "orq-agent/commands/models.md"
      to: "Orq.ai MCP server (list_models, search_entities)"
      via: "Step-by-step MCP tool invocations"
      pattern: "mcp__orqai-mcp__list_models"
    - from: "orq-agent/commands/models.md"
      to: "Phase 35 MSEL-02 snapshot-pinning rule"
      via: "Any prose example model ID must be a dated snapshot"
      pattern: "claude-sonnet-4-5-20250929|gpt-4o-[0-9]{4}-[0-9]{2}-[0-9]{2}"
---

<objective>
Create the `/orq-agent:models` slash command at `orq-agent/commands/models.md` — a read-only MCP-backed command that lists Orq.ai Model Garden models grouped by provider, broken out by type (chat / embedding / image / rerank / speech / etc.), with optional positional search-term filter.

Purpose: LCMD-04. Give users direct Model Garden visibility from Claude Code without opening Studio.

Output: One new file passing Phase 34 SKST lint AND Phase 35 MSEL-02 snapshot-pinning rule (all example model IDs dated).
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-lifecycle-slash-commands/36-CONTEXT.md
@.planning/phases/36-lifecycle-slash-commands/36-VALIDATION.md

<interfaces>
<!-- Same copy-paste template shape as Plans 01/02/03. -->

Argument conventions (locked in 36-CONTEXT.md):
- `[search-term]` positional, optional; case-insensitive substring match on model id.

MCP tools (from 36-CONTEXT.md catalog):
- `mcp__orqai-mcp__list_models` — full catalog
- `mcp__orqai-mcp__search_entities` — alternate path (type=model)

Model types to render as subsections (per LCMD-04): chat, embedding, image, rerank, speech (if present), completion (if present).
Providers are grouped as top-level H3 headings (OpenAI, Anthropic, Google, Mistral, Cohere, etc.).

Phase 35 MSEL-02 rule: any `model:` YAML line must use a dated snapshot (e.g., `claude-sonnet-4-5-20250929`). Prose example model IDs follow the same discipline for consistency.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author orq-agent/commands/models.md</name>
  <files>orq-agent/commands/models.md</files>
  <read_first>
    - orq-agent/commands/help.md (template — banner + 9 SKST sections)
    - orq-agent/references/orqai-model-catalog.md (existing provider × type layout; reuse grouping taxonomy)
    - .planning/phases/36-lifecycle-slash-commands/36-CONTEXT.md (locked: Open-in link = https://my.orq.ai/&lt;workspace&gt;/model-garden; positional search-term)
    - .planning/phases/36-lifecycle-slash-commands/36-VALIDATION.md (Task 36-04-01 verification map — grep chat / embedding / rerank)
    - orq-agent/scripts/lint-skills.sh (rules required-sections + allowed-tools + snapshot-pinned-models — this command WILL be linted by the MSEL-02 rule)
  </read_first>
  <behavior>
    - `/orq-agent:models` (no args): prints ORQ ► MODELS banner + one H3 per provider + one H4 per type under each provider + table of models under each type.
    - `/orq-agent:models <search-term>`: filter the listing to models whose ID case-insensitive-substring-matches `<search-term>`.
    - Types shown (when present): chat, embedding, image, rerank, speech, completion. Types with zero hits under a provider are omitted.
    - Read-only.
  </behavior>
  <action>
    Create `orq-agent/commands/models.md`. Every H2 heading must appear verbatim.

    **Frontmatter (exact):**
    ```yaml
    ---
    description: List Orq.ai Model Garden models grouped by provider, broken out by type (chat / embedding / image / rerank / speech / completion)
    allowed-tools: Bash, Read, mcp__orqai-mcp__list_models, mcp__orqai-mcp__search_entities
    argument-hint: "[search-term]"
    ---
    ```

    **Body outline (each H2 present verbatim):**

    1. `# Model Garden Lookup` + one-paragraph purpose line.

    2. `## Constraints`:
       - NEVER mutate model activation state — this command is read-only.
       - NEVER show deprecated model IDs without the deprecated flag annotation.
       - NEVER print a floating alias (e.g., `claude-sonnet-latest`) as an example; always use a dated snapshot like `claude-sonnet-4-5-20250929` (MSEL-02).
       - ALWAYS group output provider → type → model rows.
       - ALWAYS call `list_models` first; fall back to `search_entities type=model` or REST `GET /v2/models` on MCP failure.
       + "Why these constraints:" paragraph tying the rules to MSEL-02 discipline (snapshot pinning prevents silent model drift downstream).

    3. `## When to use` (3+ bullets: checking availability before wiring a spec, confirming a provider's dated snapshot surface, post-activation smoke check).

    4. `## When NOT to use` (2+ bullets: need to activate a model → Studio Model Garden page; need recommendation for a use case → /orq-agent:research or the researcher subagent).

    5. `## Companion Skills` (→ /orq-agent:research for recommendations; → /orq-agent:workspace for workspace-scope overview; ← user invocation).

    6. `## Done When`:
       - [ ] Terminal shows ORQ ► MODELS banner
       - [ ] Output grouped by provider then by type
       - [ ] Search term (when provided) echoed in banner sub-line and used to filter
       - [ ] "MCP tools used:" footer lists list_models
       - [ ] No floating-alias model IDs in prose (MSEL-02 lint clean)

    7. `## Destructive Actions`: `- **None** — this command is read-only (reads Orq.ai Model Garden via MCP).`

    8. `## Step 1: Parse Argument` — read `$ARGUMENTS`; treat whole string as case-insensitive search-term filter. Empty → unfiltered.

    9. `## Step 2: Fetch Model Catalog` — call `mcp__orqai-mcp__list_models`. On MCP error fall back to `curl -H "Authorization: Bearer $ORQ_API_KEY" "https://api.orq.ai/v2/models"`.

    10. `## Step 3: Group Results` — bucket by `provider` then by `type`. Accepted types rendered in this order: chat, embedding, image, rerank, speech, completion. Other types → bucket under H4 `Other`.

    11. `## Step 4: Apply Filter (optional)` — if search-term present, keep only models whose `id` contains the term (case-insensitive). Providers / types that empty after filter are omitted.

    12. `## Step 5: Render` — banner:
        ```
        ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
         ORQ ► MODELS                         [filter {term}]
        ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        ```
        Then per provider:
        ```
        ### {Provider}

        #### chat
        | Model ID | Activated | Context | Cost (in/out per 1K) |
        |---|---|---|---|
        | claude-sonnet-4-5-20250929 | yes | 200K | $0.003 / $0.015 |
        ```
        Column choices at Claude's discretion per 36-CONTEXT.md (example above shows a reasonable shape).

    13. `## Step 6: Print Open-in-orq.ai + MCP Footer`:
        ```
        Open in orq.ai:
          https://my.orq.ai/model-garden

        MCP tools used: list_models
        ```

    14. `## Anti-Patterns` (min 4 rows):
        - Printing `claude-sonnet-latest` as an example | Always use a dated snapshot (MSEL-02) — e.g. `claude-sonnet-4-5-20250929`
        - Flattening the output into one table without provider/type grouping | Users scan by provider first; preserve the hierarchy
        - Omitting the activated-state column | Activation status drives whether a model can be wired to a deployment
        - Hiding deprecated models silently | Surface with explicit `(deprecated)` flag — users need to see what's expiring

    15. `## Open in orq.ai`:
        - **Model Garden:** https://my.orq.ai/model-garden
        - **Agents:** https://my.orq.ai/agents

    16. `## Documentation & Resolution` — standard 4-line trust order.

    **MSEL-02 lint note:** This file WILL be linted by the `snapshot-pinned-models` rule. The rule targets YAML `model:` lines with floating-alias suffixes (`-latest`, `:latest`, `-beta`). Do NOT emit YAML `model:` lines in this file. Prose / table model IDs are not matched by the rule regex, but still use dated snapshots for consistency.
  </action>
  <verify>
    <automated>test -f orq-agent/commands/models.md &amp;&amp; grep -q "^allowed-tools:" orq-agent/commands/models.md &amp;&amp; grep -q "ORQ ► MODELS" orq-agent/commands/models.md &amp;&amp; grep -qi "chat" orq-agent/commands/models.md &amp;&amp; grep -qi "embedding" orq-agent/commands/models.md &amp;&amp; grep -qi "image" orq-agent/commands/models.md &amp;&amp; grep -qi "rerank" orq-agent/commands/models.md &amp;&amp; grep -qi "provider" orq-agent/commands/models.md &amp;&amp; grep -q "MCP tools used" orq-agent/commands/models.md &amp;&amp; grep -q "my.orq.ai/model-garden" orq-agent/commands/models.md &amp;&amp; bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/models.md</automated>
  </verify>
  <acceptance_criteria>
    - File orq-agent/commands/models.md exists.
    - Frontmatter contains `description:` and `allowed-tools:` non-empty.
    - All 9 required H2 sections present.
    - Banner `ORQ ► MODELS` appears.
    - All 4 type tokens appear (chat, embedding, image, rerank — case-insensitive).
    - `provider` token appears (grouping dimension).
    - `MCP tools used` footer present.
    - `my.orq.ai/model-garden` Open-in link present.
    - `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/models.md` exits 0 (includes MSEL-02 `snapshot-pinned-models` rule — no floating aliases on `model:` lines).
    - `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0.
  </acceptance_criteria>
  <done>File orq-agent/commands/models.md exists, passes SKST + MSEL-02 lint, groups models by provider × type, supports positional search-term filter. LCMD-04 file-level requirement satisfied; manual MCP smoke deferred to /gsd:verify-work 36.</done>
</task>

</tasks>

<verification>
- `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/models.md` exits 0 (SKST + MSEL-02).
- `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0.
- All type + provider + footer + link greps hit.
</verification>

<success_criteria>
LCMD-04 file-level coverage complete: `/orq-agent:models [search-term]` groups by provider × type per ROADMAP Phase 36 Success Criterion #4, passes MSEL-02 snapshot-pinning lint. Manual MCP smoke deferred to /gsd:verify-work 36.
</success_criteria>

<output>
After completion, create `.planning/phases/36-lifecycle-slash-commands/36-04-SUMMARY.md` using the standard summary template.
</output>
