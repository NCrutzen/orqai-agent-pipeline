---
phase: 36-lifecycle-slash-commands
plan: 07
type: execute
wave: 2
depends_on:
  - "36-01"
  - "36-02"
  - "36-03"
  - "36-04"
  - "36-05"
  - "36-06"
files_modified:
  - orq-agent/SKILL.md
  - orq-agent/commands/help.md
autonomous: true
requirements:
  - LCMD-01
  - LCMD-02
  - LCMD-03
  - LCMD-04
  - LCMD-05
  - LCMD-06
  - LCMD-07
must_haves:
  truths:
    - "orq-agent/SKILL.md lists all 6 new commands in the directory listing AND in the Commands table"
    - "orq-agent/commands/help.md includes the 6 new commands in the pipeline-order help index"
    - "Neither file touches a `<pipeline>` block — protected-pipeline hash check still exits 0"
    - "SKILL.md + help.md both still pass SKST lint after the edit"
  artifacts:
    - path: "orq-agent/SKILL.md"
      provides: "Updated skill index including 6 new Phase 36 commands"
      contains_all:
        - "workspace.md"
        - "traces.md"
        - "analytics.md"
        - "models.md"
        - "quickstart.md"
        - "automations.md"
    - path: "orq-agent/commands/help.md"
      provides: "Help output listing the 6 new slash commands in pipeline-order"
      contains_all:
        - "/orq-agent:workspace"
        - "/orq-agent:traces"
        - "/orq-agent:analytics"
        - "/orq-agent:models"
        - "/orq-agent:quickstart"
        - "/orq-agent:automations"
  key_links:
    - from: "orq-agent/SKILL.md Commands table"
      to: "Each new orq-agent/commands/<name>.md"
      via: "One row per new command"
      pattern: "commands/(workspace|traces|analytics|models|quickstart|automations).md"
    - from: "orq-agent/commands/help.md printed help banner"
      to: "Each new slash command"
      via: "Pipeline-order command list"
      pattern: "/orq-agent:(workspace|traces|analytics|models|quickstart|automations)"
---

<objective>
Wire the 6 new Phase 36 commands into the skill index (`orq-agent/SKILL.md`) and the printed help banner (`orq-agent/commands/help.md`) so users discover them through the standard discovery paths.

Purpose: Close the "I created a command but no one can find it" gap. Plans 01-06 author the command files; this plan surfaces them to users via the two discovery entry points established in Phase 34.

Output: Two edited files. Zero new files. Zero changes to `<pipeline>` blocks of any protected entry point.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-lifecycle-slash-commands/36-CONTEXT.md
@.planning/phases/36-lifecycle-slash-commands/36-VALIDATION.md
@.planning/phases/36-lifecycle-slash-commands/36-01-SUMMARY.md
@.planning/phases/36-lifecycle-slash-commands/36-02-SUMMARY.md
@.planning/phases/36-lifecycle-slash-commands/36-03-SUMMARY.md
@.planning/phases/36-lifecycle-slash-commands/36-04-SUMMARY.md
@.planning/phases/36-lifecycle-slash-commands/36-05-SUMMARY.md
@.planning/phases/36-lifecycle-slash-commands/36-06-SUMMARY.md

<interfaces>
<!-- Current structure of SKILL.md and help.md (to understand where edits go). -->

From orq-agent/SKILL.md (existing):

```
## Directory Structure

orq-agent/
  commands/
    orq-agent.md
    prompt.md
    architect.md
    tools.md
    research.md
    datasets.md
    deploy.md
    kb.md
    test.md
    iterate.md
    harden.md
    set-profile.md
    systems.md
    update.md
    help.md
  ...
```

And:

```
## Commands

### Orchestrator

| Command | File | Purpose |
|---------|------|---------|
| /orq-agent | commands/orq-agent.md | ... |
| /orq-agent:help | commands/help.md | ... |
```

From orq-agent/commands/help.md (existing printed help block):

```
Commands:
  /orq-agent "description"     Generate agent specs from a use case
  /orq-agent:prompt "desc"     Quick single-agent generator
  /orq-agent:architect "desc"  Standalone architecture design
  ...
  /orq-agent:help              Show this help
```

NOTE: help.md is NOT a protected pipeline file — it's a standard SKST-formatted command file. Editing its body is safe. Only `orq-agent.md`, `prompt.md`, `architect.md` are byte-identical-protected on their `<pipeline>` blocks.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update SKILL.md directory listing + Commands table, update help.md pipeline-order help block</name>
  <files>orq-agent/SKILL.md, orq-agent/commands/help.md</files>
  <read_first>
    - orq-agent/SKILL.md (current Directory Structure block + Commands section — see exact line locations before editing)
    - orq-agent/commands/help.md (current printed-help banner block — Step 2 body with the Commands list)
    - .planning/phases/36-lifecycle-slash-commands/36-CONTEXT.md (locked: 6 new command filenames + their purposes)
    - .planning/phases/36-lifecycle-slash-commands/36-01-SUMMARY.md through 36-06-SUMMARY.md (for one-line purpose summaries to insert into the Commands table)
    - orq-agent/scripts/check-protected-pipelines.sh (to confirm what is protected — only the 3 entry points' <pipeline> blocks; SKILL.md and help.md are NOT in that set)
    - orq-agent/scripts/lint-skills.sh (both edited files must still pass SKST lint)
  </read_first>
  <behavior>
    - SKILL.md Directory Structure block: add 6 lines under `commands/` listing the 6 new files in alphabetical order after existing entries.
    - SKILL.md Commands section: insert a new subsection `### Phase 36 (Lifecycle Slash Commands)` with a 6-row table (Command | File | Purpose) — placed AFTER the existing `### V2.0 Commands (Phase 5+)` subsection.
    - help.md printed-help banner: insert 6 new command lines into the Commands block, in pipeline-order (Discovery / Monitoring first, then Onboarding): quickstart → workspace → models → traces → analytics → automations. Place BEFORE the `/orq-agent:help` line (help remains last).
    - No changes to protected pipeline files (orq-agent.md / prompt.md / architect.md).
    - No changes to `<pipeline>` blocks anywhere.
  </behavior>
  <action>
    **Part A — Edit orq-agent/SKILL.md:**

    1. In the `## Directory Structure` code block, under `commands/`, insert the 6 new filenames alphabetically:
       ```
       analytics.md                 # Phase 36: Analytics summary (LCMD-03)
       automations.md               # Phase 36: Trace Automations list + create (LCMD-06)
       models.md                    # Phase 36: Model Garden lookup (LCMD-04)
       quickstart.md                # Phase 36: 12-step onboarding tour (LCMD-05 + LCMD-07)
       traces.md                    # Phase 36: Production traces query (LCMD-02)
       workspace.md                 # Phase 36: Single-screen workspace overview (LCMD-01)
       ```
       Place them in whatever natural ordering matches the existing block's alphabetical pattern (existing lines are grouped by Phase number — you MAY instead group Phase 36 at the end of the commands block as a contiguous `# Phase 36: ...` cluster for readability — Claude's discretion, both patterns are acceptable as long as all 6 filenames appear).

    2. In the `## Commands` section, after the existing `### V2.0 Commands (Phase 5+)` table, insert a new H3:

       ```markdown
       ### Phase 36 (Lifecycle Slash Commands)

       | Command | File | Tier Required | Purpose |
       |---------|------|---------------|---------|
       | `/orq-agent:workspace` | `commands/workspace.md` | any | Single-screen Orq.ai workspace overview -- agents / deployments / prompts / datasets / experiments / projects / KBs / evaluators + analytics summary |
       | `/orq-agent:traces` | `commands/traces.md` | any | Query production traces; errors first; supports `--deployment / --status / --last / --limit` + `--identity` stub (Phase 37) |
       | `/orq-agent:analytics` | `commands/analytics.md` | any | Requests / cost / tokens / error-rate summary with `--last` and `--group-by model|deployment|agent|status` |
       | `/orq-agent:models` | `commands/models.md` | any | List Model Garden models grouped by provider × type (chat / embedding / image / rerank / speech / completion) |
       | `/orq-agent:quickstart` | `commands/quickstart.md` | any | 12-step interactive onboarding tour covering the full Build → Evaluate → Optimize lifecycle |
       | `/orq-agent:automations` | `commands/automations.md` | any | List / create Orq.ai Trace Automation rules (auto-kick-off experiments on matching traces) |
       ```

       **Tier note:** All Phase 36 commands are `any` tier since they only READ workspace data via MCP (except `automations --create` which mutates but still needs only MCP/REST access — no separate tier gate defined). Keep `any` unless reading existing `.orq-agent/config.json` reveals a different convention.

    **Part B — Edit orq-agent/commands/help.md:**

    1. Locate the printed-help Commands block inside Step 2 (the fenced code block that starts with `Commands:` and ends with `/orq-agent:help              Show this help`).

    2. Insert 6 new lines in pipeline-order BEFORE the `/orq-agent:help` line. Pipeline order groups discovery/monitoring first, then onboarding:

       ```
         /orq-agent:workspace         Single-screen workspace overview
         /orq-agent:traces            Query production traces (errors first)
         /orq-agent:analytics         Requests/cost/tokens/error-rate summary
         /orq-agent:models            List Model Garden models by provider & type
         /orq-agent:quickstart        12-step onboarding tour
         /orq-agent:automations       Trace Automation rules (list / --create)
       ```

       Maintain existing column alignment (command left-aligned, description separated by sufficient spaces — match the existing pattern).

    3. Keep `/orq-agent:help              Show this help` as the LAST line in the Commands block.

    **Constraints throughout:**
    - Do NOT edit `orq-agent/commands/orq-agent.md`, `prompt.md`, or `architect.md` (protected).
    - Do NOT edit any `<pipeline>` block anywhere.
    - Do NOT delete existing rows / lines — only INSERT.
    - Both edited files MUST still pass the SKST lint after the edit.
  </action>
  <verify>
    <automated>grep -q "workspace.md" orq-agent/SKILL.md &amp;&amp; grep -q "traces.md" orq-agent/SKILL.md &amp;&amp; grep -q "analytics.md" orq-agent/SKILL.md &amp;&amp; grep -q "models.md" orq-agent/SKILL.md &amp;&amp; grep -q "quickstart.md" orq-agent/SKILL.md &amp;&amp; grep -q "automations.md" orq-agent/SKILL.md &amp;&amp; grep -q "/orq-agent:workspace" orq-agent/SKILL.md &amp;&amp; grep -q "/orq-agent:traces" orq-agent/SKILL.md &amp;&amp; grep -q "/orq-agent:analytics" orq-agent/SKILL.md &amp;&amp; grep -q "/orq-agent:models" orq-agent/SKILL.md &amp;&amp; grep -q "/orq-agent:quickstart" orq-agent/SKILL.md &amp;&amp; grep -q "/orq-agent:automations" orq-agent/SKILL.md &amp;&amp; grep -q "/orq-agent:workspace" orq-agent/commands/help.md &amp;&amp; grep -q "/orq-agent:traces" orq-agent/commands/help.md &amp;&amp; grep -q "/orq-agent:analytics" orq-agent/commands/help.md &amp;&amp; grep -q "/orq-agent:models" orq-agent/commands/help.md &amp;&amp; grep -q "/orq-agent:quickstart" orq-agent/commands/help.md &amp;&amp; grep -q "/orq-agent:automations" orq-agent/commands/help.md &amp;&amp; bash orq-agent/scripts/lint-skills.sh --file orq-agent/SKILL.md &amp;&amp; bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/help.md &amp;&amp; bash orq-agent/scripts/check-protected-pipelines.sh</automated>
  </verify>
  <acceptance_criteria>
    - All 6 new filenames (`workspace.md`, `traces.md`, `analytics.md`, `models.md`, `quickstart.md`, `automations.md`) appear in orq-agent/SKILL.md.
    - All 6 new slash-command literals (`/orq-agent:workspace`, `/orq-agent:traces`, `/orq-agent:analytics`, `/orq-agent:models`, `/orq-agent:quickstart`, `/orq-agent:automations`) appear in BOTH orq-agent/SKILL.md AND orq-agent/commands/help.md.
    - `bash orq-agent/scripts/lint-skills.sh --file orq-agent/SKILL.md` exits 0.
    - `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/help.md` exits 0.
    - `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0 (byte-identical protected `<pipeline>` blocks untouched).
  </acceptance_criteria>
  <done>SKILL.md index lists all 6 new commands in Directory Structure + Commands table. help.md printed help block includes all 6 in pipeline-order. SKST lint green on both edited files. Protected-pipeline hash check still green. LCMD-01..07 discoverability wired.</done>
</task>

</tasks>

<verification>
- `bash orq-agent/scripts/lint-skills.sh` (full suite) exits 0.
- `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0.
- All 12 grep anchors above hit (6 filenames in SKILL.md + 6 command literals in each of SKILL.md and help.md).
</verification>

<success_criteria>
Discoverability wiring complete: users running `/orq-agent:help` see all 6 new commands; users reading SKILL.md see all 6 in the directory listing + Commands table. Protected pipelines remain byte-identical. LCMD-01..07 cross-cutting index wiring satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/36-lifecycle-slash-commands/36-07-SUMMARY.md` using the standard summary template.
</output>
