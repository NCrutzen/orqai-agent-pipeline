---
phase: 36-lifecycle-slash-commands
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/commands/traces.md
autonomous: true
requirements:
  - LCMD-02
must_haves:
  truths:
    - "Running /orq-agent:traces prints an ORQ ► TRACES banner with errors-first-grouped trace rows"
    - "All four documented flags exist: --deployment, --status, --last, --limit"
    - "A --identity flag stub exists and is documented as a no-op TODO for OBSV-07 wiring in Phase 37"
    - "Trace IDs are rendered in full (not truncated) so they can be copy-pasted into Studio"
    - "Output ends with an Open in orq.ai deep link + MCP tools used footer"
  artifacts:
    - path: "orq-agent/commands/traces.md"
      provides: "LCMD-02 — traces query slash command"
      contains: "## Constraints"
      contains_all:
        - "ORQ ► TRACES"
        - "allowed-tools:"
        - "argument-hint:"
        - "--deployment"
        - "--status"
        - "--last"
        - "--limit"
        - "--identity"
        - "TODO(OBSV-07)"
        - "errors first"
        - "MCP tools used"
        - "my.orq.ai/traces"
  key_links:
    - from: "orq-agent/commands/traces.md"
      to: "Orq.ai MCP server (list_traces, list_spans)"
      via: "Step-by-step MCP tool invocations in the command body"
      pattern: "mcp__orqai-mcp__list_traces"
    - from: "orq-agent/commands/traces.md"
      to: "Phase 37 OBSV-07"
      via: "--identity stub with TODO(OBSV-07) forward link"
      pattern: "TODO\\(OBSV-07\\)"
---

<objective>
Create the `/orq-agent:traces` slash command at `orq-agent/commands/traces.md` — a thin MCP-backed, read-only command that queries and summarizes Orq.ai production traces with four filter flags (`--deployment`, `--status`, `--last`, `--limit`) plus an `--identity` stub for future per-tenant filtering (OBSV-07, Phase 37).

Purpose: LCMD-02. Let users inspect production traces from Claude Code without opening Studio. Errors surface first with full trace IDs for copy-paste debugging.

Output: One new file `orq-agent/commands/traces.md` passing Phase 34 SKST lint, matching house-style banner, and declaring the `--identity` stub as a documented no-op with a forward-link to Phase 37.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-lifecycle-slash-commands/36-CONTEXT.md
@.planning/phases/36-lifecycle-slash-commands/36-VALIDATION.md

<interfaces>
<!-- Copy-paste template shape (same as Plan 01; every Phase 36 command uses it). -->

From orq-agent/commands/help.md (read-only template):
- Frontmatter: `description:` + `allowed-tools:` (+ optional `argument-hint:`)
- 9 required H2 sections, banner format `ORQ ► {VERB} {OBJECT}` box-char wrapped.

Argument conventions (locked in 36-CONTEXT.md):
- Long-form flags only: `--flag value`.
- `--last` values: `5m | 1h | 24h | 7d | 30d`; default `24h` for traces.
- `--limit` default `20`.
- `--deployment <key>` filters to a specific deployment key.
- `--status <ok|error>` filters by trace status.
- `--identity <id>` is a stub (OBSV-07 wiring in Phase 37) — parse, document, but do not use.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author orq-agent/commands/traces.md</name>
  <files>orq-agent/commands/traces.md</files>
  <read_first>
    - orq-agent/commands/help.md (template for read-only, banner + 9 SKST sections)
    - orq-agent/commands/systems.md (template for multi-branch argument parsing — Step 2: Parse Arguments)
    - .planning/phases/36-lifecycle-slash-commands/36-CONTEXT.md (locked flag conventions: --last 24h default, --limit 20 default, errors-first sort, Open-in link = https://my.orq.ai/&lt;workspace&gt;/traces)
    - .planning/phases/36-lifecycle-slash-commands/36-VALIDATION.md (Task 36-02-01 verification map — the 4 flag greps are blocking)
    - orq-agent/SKILL.md Commands table (confirm no existing traces command)
    - orq-agent/scripts/lint-skills.sh (rules required-sections + allowed-tools)
  </read_first>
  <behavior>
    - `/orq-agent:traces` (no args): prints ORQ ► TRACES banner + last 24h of traces, errors first (status=error rows), then status=ok rows, limit 20.
    - `--deployment <key>`: filters to one deployment.
    - `--status <ok|error>`: filters by status.
    - `--last <duration>`: accepts 5m|1h|24h|7d|30d (default 24h).
    - `--limit <N>`: integer, default 20.
    - `--identity <id>`: parse-only stub — logs "identity filter not yet implemented (OBSV-07, Phase 37) — TODO" and proceeds without filtering.
    - Row format: full trace ID (no truncation) + deployment key + status + timestamp + duration + model used.
    - Errors first: stable sort on status (error before ok) then by timestamp desc.
    - Read-only.
  </behavior>
  <action>
    Create `orq-agent/commands/traces.md`. Every H2 heading must appear verbatim.

    **Frontmatter (exact):**
    ```yaml
    ---
    description: Query and summarize Orq.ai production traces — errors first, full trace IDs, with deployment/status/duration/limit filters
    allowed-tools: Bash, Read, mcp__orqai-mcp__list_traces, mcp__orqai-mcp__list_spans, mcp__orqai-mcp__search_entities
    argument-hint: "[--deployment <key>] [--status <ok|error>] [--last <5m|1h|24h|7d|30d>] [--limit <N>] [--identity <id>]"
    ---
    ```

    **Body outline (each H2 present verbatim):**

    1. `# Traces Query` — one-paragraph purpose: "You are running the `/orq-agent:traces` command. This command queries Orq.ai production traces and renders them errors first with full trace IDs for copy-paste debugging."

    2. `## Constraints`:
       - NEVER mutate traces — this is read-only query surface.
       - NEVER truncate trace IDs — they are copy-paste targets for Studio / support.
       - NEVER silently swallow MCP errors — surface them inline.
       - ALWAYS sort errors first (status=error rows above status=ok rows).
       - ALWAYS call MCP `list_traces` first; fall back to REST `GET /v2/traces` with `$ORQ_API_KEY` on MCP failure.
       + "Why these constraints:" paragraph linking to MCP-first PROJECT decision + debugging workflow priority (SKST-06).

    3. `## When to use` (3+ bullets: post-deploy spot-check, investigating a user-reported error, tracking error-rate trend vs analytics).

    4. `## When NOT to use` (2+ bullets: need aggregated stats → /orq-agent:analytics; need per-step span drill-down → Studio span viewer).

    5. `## Companion Skills` (directional: → /orq-agent:analytics for aggregates; → Phase 38 trace-failure analysis skill (forward link TFAIL); ← user invocation).

    6. `## Done When`:
       - [ ] Terminal shows ORQ ► TRACES banner
       - [ ] At least one row rendered (or explicit "no traces matched" message) with full trace IDs
       - [ ] Errors-first ordering observable when both status classes present
       - [ ] Window (--last) echoed in the banner sub-line
       - [ ] "MCP tools used:" footer lists list_traces (and list_spans if span detail was requested)

    7. `## Destructive Actions`: `- **None** — this command is read-only (reads Orq.ai traces via MCP).`

    8. `## Step 1: Parse Arguments` — parse `$ARGUMENTS` for the 5 flags. Document each flag with the exact string prefix:
       - `--deployment <key>` — optional, filters to one deployment
       - `--status <ok|error>` — optional, filters by status
       - `--last <5m|1h|24h|7d|30d>` — optional, default `24h`
       - `--limit <N>` — optional integer, default `20`
       - `--identity <id>` — **STUB** — parsed but currently a no-op; wiring happens in Phase 37 / OBSV-07. Emit a one-line warning: `⚠ --identity is a stub (TODO(OBSV-07)); filter ignored.`

       Unknown flag → STOP with usage hint.

    9. `## Step 2: Fetch Traces` — call `mcp__orqai-mcp__list_traces` with the parsed filters (map --last to `since=<timestamp>`, --limit to `limit=`, --deployment to `deployment_key=`, --status to `status=`). On MCP error fall back to: `curl -H "Authorization: Bearer $ORQ_API_KEY" "https://api.orq.ai/v2/traces?limit=$LIMIT&since=$SINCE&deployment_key=$DEPLOYMENT_KEY&status=$STATUS"`.

    10. `## Step 3: Sort Errors First` — stable-sort rows: primary key `status` (`error` before `ok`), secondary key `started_at` descending.

    11. `## Step 4: Render` — emit banner block:
        ```
        ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
         ORQ ► TRACES                         last {--last}
        ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        ```
        Then a pipe-separated Markdown table with columns: `| Trace ID | Deployment | Status | Started | Duration | Model |`. Errors-first rows above ok rows. Do NOT truncate trace IDs.

        If zero rows: print `No traces matched the filter.` and STOP gracefully.

    12. `## Step 5: Print Open-in-orq.ai + MCP Footer`:
        ```
        Open in orq.ai:
          https://my.orq.ai/traces

        MCP tools used: list_traces
        ```

    13. `## Anti-Patterns` (min 4 rows):
        - Truncating trace IDs to save horizontal space | Full IDs are copy-paste targets; let tables overflow
        - Sorting by timestamp only (errors buried below successes) | Errors-first is non-negotiable per LCMD-02
        - Treating --identity as implemented | It is a stub; emit the TODO(OBSV-07) warning and do not filter
        - Fabricating a response when MCP errors | Surface the error and the fallback attempt inline

    14. `## Open in orq.ai`:
        - **Traces:** https://my.orq.ai/traces
        - **Deployments:** https://my.orq.ai/deployments
        - **Studio span viewer:** https://my.orq.ai/traces (click any trace)

    15. `## Documentation & Resolution` — standard 4-line trust order (MCP > docs MCP > docs.orq.ai > this file).

    **Phase 35 snapshot-pinning check:** If any example model ID appears in prose (e.g., in sample table row), use dated snapshot `claude-sonnet-4-5-20250929`.

    The exact strings `--deployment`, `--status`, `--last`, `--limit`, `--identity`, and `TODO(OBSV-07)` MUST be present literally — they are grep-verified acceptance anchors.
  </action>
  <verify>
    <automated>test -f orq-agent/commands/traces.md &amp;&amp; grep -q "^allowed-tools:" orq-agent/commands/traces.md &amp;&amp; grep -q "ORQ ► TRACES" orq-agent/commands/traces.md &amp;&amp; grep -q "\-\-deployment" orq-agent/commands/traces.md &amp;&amp; grep -q "\-\-status" orq-agent/commands/traces.md &amp;&amp; grep -q "\-\-last" orq-agent/commands/traces.md &amp;&amp; grep -q "\-\-limit" orq-agent/commands/traces.md &amp;&amp; grep -q "\-\-identity" orq-agent/commands/traces.md &amp;&amp; grep -q "TODO(OBSV-07)" orq-agent/commands/traces.md &amp;&amp; grep -qi "errors first\|errors-first" orq-agent/commands/traces.md &amp;&amp; grep -q "MCP tools used" orq-agent/commands/traces.md &amp;&amp; grep -q "my.orq.ai/traces" orq-agent/commands/traces.md &amp;&amp; bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/traces.md</automated>
  </verify>
  <acceptance_criteria>
    - File orq-agent/commands/traces.md exists.
    - Frontmatter contains `description:` and `allowed-tools:` with non-empty values.
    - All 9 required H2 sections present (SKST via `required-sections` rule).
    - Banner `ORQ ► TRACES` appears at least once.
    - All 5 flag strings appear verbatim: `--deployment`, `--status`, `--last`, `--limit`, `--identity`.
    - `TODO(OBSV-07)` forward-link token appears (Phase 37 hook).
    - `errors first` or `errors-first` phrase appears.
    - `MCP tools used` footer present.
    - `my.orq.ai/traces` Open-in link present.
    - `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/traces.md` exits 0.
    - Protected pipelines unchanged: `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0.
  </acceptance_criteria>
  <done>File orq-agent/commands/traces.md exists, passes SKST lint, declares all 4 LCMD-02 flags + --identity stub with TODO(OBSV-07), renders errors-first ordering with full trace IDs. LCMD-02 file-level requirement satisfied; manual MCP smoke deferred to /gsd:verify-work 36.</done>
</task>

</tasks>

<verification>
- `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/traces.md` exits 0.
- `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0.
- All 5 flag greps + TODO(OBSV-07) grep hit.
</verification>

<success_criteria>
LCMD-02 file-level coverage complete: `/orq-agent:traces` supports the four required flags plus the documented `--identity` stub forward-linked to Phase 37 (OBSV-07). Errors-first ordering and full trace IDs documented per ROADMAP Phase 36 Success Criterion #2. Manual MCP smoke deferred to /gsd:verify-work 36.
</success_criteria>

<output>
After completion, create `.planning/phases/36-lifecycle-slash-commands/36-02-SUMMARY.md` using the standard summary template.
</output>
