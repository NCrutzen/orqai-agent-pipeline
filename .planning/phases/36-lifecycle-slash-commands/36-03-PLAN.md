---
phase: 36-lifecycle-slash-commands
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/commands/analytics.md
autonomous: true
requirements:
  - LCMD-03
must_haves:
  truths:
    - "Running /orq-agent:analytics prints an ORQ ► ANALYTICS banner with requests / cost / tokens / error-rate summary"
    - "Supports --last flag (5m|1h|24h|7d|30d, default 24h)"
    - "Supports --group-by flag with accepted values: model, deployment, agent, status"
    - "Group-by output renders one row per group value in a pipe-separated Markdown table"
    - "Output ends with an Open in orq.ai analytics deep link + MCP tools used footer"
  artifacts:
    - path: "orq-agent/commands/analytics.md"
      provides: "LCMD-03 — analytics summary slash command"
      contains: "## Constraints"
      contains_all:
        - "ORQ ► ANALYTICS"
        - "allowed-tools:"
        - "argument-hint:"
        - "--last"
        - "--group-by"
        - "model"
        - "deployment"
        - "agent"
        - "status"
        - "requests"
        - "cost"
        - "tokens"
        - "error rate"
        - "MCP tools used"
        - "my.orq.ai/analytics"
  key_links:
    - from: "orq-agent/commands/analytics.md"
      to: "Orq.ai MCP server (get_analytics_overview, query_analytics)"
      via: "Step-by-step MCP tool invocations in the command body"
      pattern: "mcp__orqai-mcp__get_analytics_overview|mcp__orqai-mcp__query_analytics"
---

<objective>
Create the `/orq-agent:analytics` slash command at `orq-agent/commands/analytics.md` — a read-only MCP-backed command that reports requests / cost / tokens / error-rate for a time window, with optional grouping by model | deployment | agent | status.

Purpose: LCMD-03. Give users aggregate production metrics from Claude Code without opening Studio.

Output: One new file passing Phase 34 SKST lint with the exact flag strings `--last` and `--group-by` plus the 4 accepted group values (model, deployment, agent, status).
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-lifecycle-slash-commands/36-CONTEXT.md
@.planning/phases/36-lifecycle-slash-commands/36-VALIDATION.md

<interfaces>
<!-- Same copy-paste template shape as Plans 01/02. -->

Argument conventions (locked in 36-CONTEXT.md):
- `--last <5m|1h|24h|7d|30d>` — default `24h`.
- `--group-by <model|deployment|agent|status>` — optional; no default (flat total when omitted).

MCP tools available (from 36-CONTEXT.md §Existing MCP tool catalog):
- `mcp__orqai-mcp__get_analytics_overview` — overall totals for a window
- `mcp__orqai-mcp__query_analytics` — drill-down with group-by dimension
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author orq-agent/commands/analytics.md</name>
  <files>orq-agent/commands/analytics.md</files>
  <read_first>
    - orq-agent/commands/help.md (template — banner + 9 SKST sections)
    - orq-agent/commands/systems.md (template for argument parsing w/ STOP branches)
    - .planning/phases/36-lifecycle-slash-commands/36-CONTEXT.md (locked: Open-in link = https://my.orq.ai/&lt;workspace&gt;/analytics; --last default 24h; 4 group-by values)
    - .planning/phases/36-lifecycle-slash-commands/36-VALIDATION.md (Task 36-03-01 verification map — `grep -q "\-\-group-by"` is blocking)
    - orq-agent/scripts/lint-skills.sh (rules required-sections + allowed-tools)
  </read_first>
  <behavior>
    - `/orq-agent:analytics` (no args): prints ORQ ► ANALYTICS banner + single-line summary: `Requests: {N} | Cost: {USD} | Tokens: {N} | Error rate: {N}% (last 24h)`.
    - `--last <duration>`: changes the window and echoes it in banner sub-line.
    - `--group-by <dim>` where `dim ∈ {model, deployment, agent, status}`: renders one row per group value: `| {Dim} | Requests | Cost | Tokens | Error Rate |`.
    - Unknown --group-by value → STOP with usage hint listing the 4 accepted values.
    - Read-only.
  </behavior>
  <action>
    Create `orq-agent/commands/analytics.md`. Every H2 heading must appear verbatim.

    **Frontmatter (exact):**
    ```yaml
    ---
    description: Orq.ai analytics summary — requests, cost, tokens, error rate — with optional group-by model / deployment / agent / status
    allowed-tools: Bash, Read, mcp__orqai-mcp__get_analytics_overview, mcp__orqai-mcp__query_analytics
    argument-hint: "[--last <5m|1h|24h|7d|30d>] [--group-by <model|deployment|agent|status>]"
    ---
    ```

    **Body outline (each H2 present verbatim):**

    1. `# Analytics Summary` + one-paragraph purpose line.

    2. `## Constraints`:
       - NEVER mutate analytics config.
       - NEVER omit the time window from the banner — the number is meaningless without it.
       - NEVER fabricate totals when an MCP tool errors — surface the error inline.
       - ALWAYS call `get_analytics_overview` first for the flat summary; only call `query_analytics` when `--group-by` is present.
       - ALWAYS echo USD currency symbol on the cost line; Orq.ai returns USD-normalized cost.
       + "Why these constraints:" paragraph (cost interpretation matters; users act on these numbers).

    3. `## When to use` (3+ bullets: daily cost check, model-mix analysis via --group-by model, post-deploy error-rate spike investigation).

    4. `## When NOT to use` (2+ bullets: need per-trace detail → /orq-agent:traces; need a single entity list → /orq-agent:workspace).

    5. `## Companion Skills` (→ /orq-agent:traces when error rate spikes; → /orq-agent:models for model-mix context; ← user invocation).

    6. `## Done When`:
       - [ ] Terminal shows ORQ ► ANALYTICS banner with the window
       - [ ] Summary line present with all 4 metrics (requests, cost, tokens, error rate)
       - [ ] When --group-by is supplied, the group table has one row per group value
       - [ ] "MCP tools used:" footer lists get_analytics_overview (+ query_analytics if grouped)

    7. `## Destructive Actions`: `- **None** — this command is read-only (reads Orq.ai analytics via MCP).`

    8. `## Step 1: Parse Arguments` — parse `$ARGUMENTS` for:
       - `--last <5m|1h|24h|7d|30d>` (default `24h`). Any other value → STOP with usage hint.
       - `--group-by <model|deployment|agent|status>`. Any other value → STOP with usage hint enumerating the 4 accepted values verbatim.

    9. `## Step 2: Fetch Totals` — call `mcp__orqai-mcp__get_analytics_overview` with `window=<--last>`. On error fall back to `curl -H "Authorization: Bearer $ORQ_API_KEY" "https://api.orq.ai/v2/analytics/overview?window=$LAST"`.

    10. `## Step 3: Fetch Group-By (optional)` — if `--group-by` present, call `mcp__orqai-mcp__query_analytics` with `window=<--last>` and `group_by=<dim>`. Each group row must carry: name, requests, cost, tokens, error_rate.

    11. `## Step 4: Render` — banner:
        ```
        ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
         ORQ ► ANALYTICS                      window {--last}
        ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        ```
        Then the flat summary line (always):
        ```
        Requests: {N} | Cost: ${USD} | Tokens: {N} | Error rate: {N}%
        ```
        If `--group-by` set, follow with an H3 `Breakdown by {dim}` and a pipe-separated Markdown table with columns `| {Dim} | Requests | Cost | Tokens | Error Rate |`.

    12. `## Step 5: Print Open-in-orq.ai + MCP Footer`:
        ```
        Open in orq.ai:
          https://my.orq.ai/analytics

        MCP tools used: get_analytics_overview[, query_analytics]
        ```

    13. `## Anti-Patterns` (min 4 rows):
        - Rendering cost without currency | Always prefix with `$` (Orq.ai returns USD-normalized)
        - Omitting the window from the banner | Metrics are meaningless without a window
        - Accepting arbitrary --group-by values | Restrict to the 4 documented dimensions
        - Fabricating a group table when MCP errors | Surface the error and the REST fallback inline

    14. `## Open in orq.ai`:
        - **Analytics:** https://my.orq.ai/analytics
        - **Traces:** https://my.orq.ai/traces
        - **Deployments:** https://my.orq.ai/deployments

    15. `## Documentation & Resolution` — standard 4-line trust order.

    The four group-by values `model`, `deployment`, `agent`, `status` MUST each appear at least once in the file (grep-verified).
  </action>
  <verify>
    <automated>test -f orq-agent/commands/analytics.md &amp;&amp; grep -q "^allowed-tools:" orq-agent/commands/analytics.md &amp;&amp; grep -q "ORQ ► ANALYTICS" orq-agent/commands/analytics.md &amp;&amp; grep -q "\-\-last" orq-agent/commands/analytics.md &amp;&amp; grep -q "\-\-group-by" orq-agent/commands/analytics.md &amp;&amp; grep -q "model" orq-agent/commands/analytics.md &amp;&amp; grep -q "deployment" orq-agent/commands/analytics.md &amp;&amp; grep -q "agent" orq-agent/commands/analytics.md &amp;&amp; grep -q "status" orq-agent/commands/analytics.md &amp;&amp; grep -qi "requests" orq-agent/commands/analytics.md &amp;&amp; grep -qi "cost" orq-agent/commands/analytics.md &amp;&amp; grep -qi "tokens" orq-agent/commands/analytics.md &amp;&amp; grep -qi "error rate" orq-agent/commands/analytics.md &amp;&amp; grep -q "MCP tools used" orq-agent/commands/analytics.md &amp;&amp; grep -q "my.orq.ai/analytics" orq-agent/commands/analytics.md &amp;&amp; bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/analytics.md</automated>
  </verify>
  <acceptance_criteria>
    - File orq-agent/commands/analytics.md exists.
    - Frontmatter contains `description:` and `allowed-tools:` non-empty.
    - All 9 required H2 sections present.
    - Banner `ORQ ► ANALYTICS` appears.
    - Flag strings `--last` and `--group-by` appear verbatim.
    - All 4 group-by values appear (`model`, `deployment`, `agent`, `status`).
    - All 4 metric labels appear (requests, cost, tokens, error rate — case-insensitive).
    - `MCP tools used` footer present.
    - `my.orq.ai/analytics` Open-in link present.
    - `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/analytics.md` exits 0.
    - `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0.
  </acceptance_criteria>
  <done>File orq-agent/commands/analytics.md exists, passes SKST lint, declares both flags and all 4 group dimensions, and renders the standard metrics quadruple. LCMD-03 file-level requirement satisfied; manual MCP smoke deferred to /gsd:verify-work 36.</done>
</task>

</tasks>

<verification>
- `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/analytics.md` exits 0.
- `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0.
- All flag + group-value + metric greps hit.
</verification>

<success_criteria>
LCMD-03 file-level coverage complete: `/orq-agent:analytics` supports `--last` and `--group-by (model|deployment|agent|status)` with the 4-metric summary per ROADMAP Phase 36 Success Criterion #3. Manual MCP smoke deferred to /gsd:verify-work 36.
</success_criteria>

<output>
After completion, create `.planning/phases/36-lifecycle-slash-commands/36-03-SUMMARY.md` using the standard summary template.
</output>
