---
phase: 82.3
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - web/components/automations/bulk-review/audit/Stage0EvidencePanel.tsx
  - web/components/automations/bulk-review/audit/__tests__/Stage0EvidencePanel.test.tsx
autonomous: true
requirements: [82.3-STAGE0-RENDERER]
must_haves:
  truths:
    - "Stage 0 expander renders regex patterns triggered, LLM injection verdict, and budget headroom from decision_details"
    - "Empty/missing inputs render the locked empty-state copy without throwing"
  artifacts:
    - path: "web/components/automations/bulk-review/audit/Stage0EvidencePanel.tsx"
      provides: "Pure Stage 0 evidence renderer consumed by Plan 11"
      contains: "export function Stage0EvidencePanel"
  key_links:
    - from: "Stage 0 page audit-payload mapper (Plan 11)"
      to: "Stage0EvidencePanel"
      via: "props.payload: Stage0AuditPayload"
      pattern: "&lt;Stage0EvidencePanel payload="
---

<objective>
Build the pure Stage 0 evidence renderer per UI-SPEC §Component Inventory. Input: `Stage0AuditPayload` (from Plan 02 types). Output: ReactNode with REASONING + EVIDENCE sections + optional raw JSON sub-toggle (`RawJsonToggle` lands in Plan 09; this plan accepts `raw` as a prop slot but renders a placeholder until 09 ships).

Purpose: Surface Stage 0 safety verdict evidence (regex patterns triggered, LLM injection verdict, budget headroom) so operators understand WHY Stage 0 passed or blocked an email — without forcing them to open Supabase.
Output: One pure-functional component file + co-located vitest suite covering: present-payload, empty-arrays, all-null fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-UI-SPEC.md
@.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-CONTEXT.md
@web/app/(dashboard)/automations/[swarm]/_shell/_lib/audit-types.ts
@web/components/ui/badge.tsx
@docs/agentic-pipeline/README.md

<interfaces>
From audit-types.ts (Plan 02):
```typescript
export interface Stage0AuditPayload {
  stage: 0;
  regex_patterns_fired: string[];
  llm_injection_verdict: "clean" | "flagged" | "unknown" | null;
  llm_reasoning: string | null;
  budget_headroom_cents: number | null;
  raw: Record&lt;string, unknown&gt;;
}
```

UI-SPEC locked tokens:
- Section header: 11px 600 uppercase, label `REASONING` / `EVIDENCE`
- Body: 13px 400 line-height 1.5
- Mono (regex strings, cents): 12px Geist Mono
- Chip-teal for regex match label; destructive (`#ef4444`) for `flagged`; muted for `null`/`clean`/`unknown`
- Empty copy: `No evidence captured for this stage.` muted italic
- Spacing: --space-3 internal padding, --space-5 between sub-sections
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stage0EvidencePanel pure component + tests</name>
  <files>web/components/automations/bulk-review/audit/Stage0EvidencePanel.tsx, web/components/automations/bulk-review/audit/__tests__/Stage0EvidencePanel.test.tsx</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/_shell/_lib/audit-types.ts
    - web/components/ui/badge.tsx (chip styling analog)
    - .planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-UI-SPEC.md §Typography, §Color, §Copywriting Contract, §Spacing Scale
  </read_first>
  <behavior>
    Tests (vitest + @testing-library/react):
    - renders `REASONING` and `EVIDENCE` section headers when payload has any data
    - renders each `regex_patterns_fired[]` entry as a mono chip with the literal pattern string
    - renders `llm_injection_verdict === "flagged"` with `data-testid="stage0-verdict-flagged"` and color `var(--v7-danger)` (or `#ef4444` inline)
    - renders `llm_injection_verdict === "clean"` with `data-testid="stage0-verdict-clean"` (muted color)
    - renders `llm_injection_verdict === null` as muted text `Injection check skipped` (locked copy)
    - renders `llm_reasoning` body when non-null in the REASONING block
    - renders `budget_headroom_cents` formatted as `Budget headroom: $0.12` (cents → dollars, two decimals) in mono
    - renders `budget_headroom_cents === null` as muted `Budget headroom: —`
    - empty payload (no patterns, all nulls) renders `No evidence captured for this stage.` and nothing else
    - includes `raw` slot rendered as a `data-testid="stage0-raw-json-slot"` placeholder (Plan 09 replaces internals)
  </behavior>
  <action>
    Create `web/components/automations/bulk-review/audit/Stage0EvidencePanel.tsx` as a server-component-safe pure function:
    ```typescript
    import type { Stage0AuditPayload } from "@/app/(dashboard)/automations/[swarm]/_shell/_lib/audit-types";

    interface Props { payload: Stage0AuditPayload; }
    export function Stage0EvidencePanel({ payload }: Props) { /* … */ }
    ```

    Render order per UI-SPEC:
    1. REASONING block (11px 600 uppercase header `REASONING` + 13px body — `llm_reasoning` or muted italic `No evidence captured for this stage.` when all-null)
    2. EVIDENCE block (uppercase header + chip row of regex patterns + verdict chip + budget headroom mono line)
    3. Raw JSON slot — render `<div data-testid="stage0-raw-json-slot" />` (Plan 09 swaps content)

    Use inline styles with `var(--v7-*)` tokens (same convention as `detail-pane.tsx`). NO new CSS module. No Tailwind beyond what's already used in `stage-step.tsx`.

    Empty-state predicate: `regex_patterns_fired.length === 0 && llm_injection_verdict === null && llm_reasoning === null && budget_headroom_cents === null` → render the muted italic empty-state line ONLY, skip section headers.

    Currency format: `"$" + (cents / 100).toFixed(2)` rendered in `font-family: var(--font-geist-mono, monospace); font-size: 12px`.

    Co-located test file `__tests__/Stage0EvidencePanel.test.tsx` with vitest. Use `@testing-library/react`. Build payload fixtures inline (do NOT import production fixtures).
  </action>
  <verify>
    <automated>test -f web/components/automations/bulk-review/audit/Stage0EvidencePanel.tsx &amp;&amp; cd web &amp;&amp; npx vitest run --reporter=dot components/automations/bulk-review/audit/__tests__/Stage0EvidencePanel.test.tsx 2>&amp;1 | tail -20 &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `web/components/automations/bulk-review/audit/Stage0EvidencePanel.tsx` exists
    - `grep -c "export function Stage0EvidencePanel" Stage0EvidencePanel.tsx` = 1
    - `grep "Stage0AuditPayload" Stage0EvidencePanel.tsx` matches (typed input)
    - `grep "No evidence captured for this stage." Stage0EvidencePanel.tsx` matches (locked copy)
    - `grep "REASONING" Stage0EvidencePanel.tsx` matches; `grep "EVIDENCE" Stage0EvidencePanel.tsx` matches
    - `grep "stage0-raw-json-slot" Stage0EvidencePanel.tsx` matches
    - Test file exists and ALL describe/it blocks pass (vitest exit 0)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Stage0EvidencePanel renders all four data sources + empty-state; vitest green.</done>
</task>

</tasks>

<verification>
Vitest green for `Stage0EvidencePanel.test.tsx`; tsc clean.
</verification>

<success_criteria>
Plan 11 can `import { Stage0EvidencePanel }` and pass a `Stage0AuditPayload` derived from `pipeline_events.decision_details` where `stage = 0`.
</success_criteria>

<output>
After completion, create `.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-03-SUMMARY.md`.
</output>
