---
status: complete
phase: 82.3-per-stage-audit-surface-v8-stabilisation
source:
  - 82.3-01-SUMMARY.md
  - 82.3-02-SUMMARY.md
  - 82.3-03-SUMMARY.md
  - 82.3-04-SUMMARY.md
  - 82.3-05-SUMMARY.md
  - 82.3-06-SUMMARY.md
  - 82.3-07-SUMMARY.md
  - 82.3-08-SUMMARY.md
  - 82.3-09-SUMMARY.md
  - 82.3-10-SUMMARY.md
  - 82.3-11-SUMMARY.md
started: 2026-05-13
updated: 2026-05-13
mode: live-walkthrough
operator: Nick Crutzen
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: |
  Kill any running dev server. Restart `npm run dev` from `web/`. Server boots without errors, Stage 1 page loads with row list populated, and the audit expander affordance appears on every Stage 0/1/2/3 entry in the detail pane.
result: pass
notes: |
  Required a pre-existing latent bug fix (chunked .in() query in stage-1 loader, commit e54e39d) before rows would populate. Once fixed, dev server cold start is clean; rows render; expander affordance present.

### 2. Expander Affordance on Stages 0–3, Absent on Stage 4
expected: |
  Click any row in Bulk Review. The detail pane shows a "Show details" expander under each Stage 0, 1, 2, and 3 entry. Stage 4 row does NOT show an expander.
result: pass

### 3. Hide / Show + Multi-Open
expected: |
  Click "Show details" on Stage 0 — panel opens with chevron flipped + "Hide details" label. Click "Show details" on Stage 1 — both panels open simultaneously (multi-open allowed, no accordion semantics). Click "Hide details" on either — that panel closes; the other stays open.
result: pass

### 4. Row-Switch Reactivity
expected: |
  With Stage 1 expander open on row A (e.g., Remco / unknown / LLM Pass-2), click a different row B (e.g., brocacef / auto_reply / regex Pass-1). The detail pane top section (subject, sender, Current value) updates AND the audit content inside the expander refreshes to match row B's data — predictor chip, rule chip, REASONING text, raw JSON all reflect row B.
result: pass
notes: |
  Required fix in commit a06f004 — the effectiveStageAudit useMemo now always rebuilds from the live timeline prop on selection change, ignoring the stale server-side stageAudit snapshot.

### 5. Stage 1 Evidence — Regex Pass-1 Row
expected: |
  On a row with `regex` predictor chip in the list (e.g., Stichting Pluryn / payment_admittance), open Stage 1 expander. REASONING reads "Regex Pass-1 matched — no LLM reasoning produced." EVIDENCE chips show: matched rule name (e.g., `payment_subject`) + `regex` predictor chip + dash for confidence (regex doesn't produce confidence).
result: pass

### 6. Stage 1 Evidence — LLM Pass-2 Row
expected: |
  On a row with `LLM · medium` predictor chip in the list (e.g., Remco / unknown), open Stage 1 expander. REASONING reads the actual LLM-generated reasoning text. EVIDENCE chips show: rule name (e.g., `no_match`) + `llm` predictor chip + `medium` confidence chip.
result: pass
notes: |
  Required fixes in commits 03e0952 (read from actual decision_details schema) and bd1e271 (accept "llm_2nd_pass" predictor value).

### 7. Raw JSON Sub-Toggle
expected: |
  Inside any Stage expander, click "Show raw JSON". A collapsible code block reveals with Geist Mono 12px font and 240px max-height with internal scroll. Contents show the full decision_details JSON payload (regex.invoked, category, llm_reasoning, llm_confidence, etc.).
result: pass

### 8. Override Controls Untouched
expected: |
  Override dropdown on Stage 1 (e.g., showing `payment_admittance` or `unknown`) still works — opens, lets you pick a different category. `override stage` links on Stages 0/2/3/4 remain clickable. Approve / Reject / Skip footer is still present (Approve is broken as a known pre-existing bug, separate from 82.3 surface).
result: pass

### 9. Display Fallbacks (Plan 10)
expected: |
  Rows where the upstream Outlook display-name artifact was "Planning" render as "(unknown sender)". Rows with missing/empty subject render as "(no subject)".
result: pass

### 10. Stage 2 Audit Content
expected: |
  On a row that went through Stage 2 customer resolution, Stage 2 expander shows identifier source, confidence, top-3 candidate list, and iController before/after screenshots via signed URL.
result: skipped
reason: |
  Scope reduction documented in Plan 11 SUMMARY: shallow wiring passes empty agent_runs + automation_runs arrays. Stage 2 currently renders the empty-state copy ("Stage didn't run") on rows where Stage 2 did run. Original Plan 11 envisioned per-row server-side agent_runs + automation_runs loading; deferred to Phase 82.4 (which already needs richer per-row data for prose-notes capture). Not a 82.3 blocker — expander structure and signed-URL API both ship correctly.

### 11. Stage 3 Audit Content
expected: |
  On a row that went through Stage 3 intent classification, Stage 3 expander shows the ranked intent list with confidences, coordinator one-line reasoning, and the selected-intent highlight.
result: skipped
reason: |
  Same as Test 10 — per-row agent_runs.tool_outputs not threaded server-side. Deferred to Phase 82.4. Expander structure renders correctly; payload-side enrichment is the follow-up.

### 12. Cross-Page Entry Coverage (Stage 0 / Stage 2 / Stage 3 / Stage 4 pages)
expected: |
  Navigate to `/stage-0`, `/stage-2`, `/stage-3`, `/stage-4` pages in turn. Each page's detail pane mounts UnifiedDetailPane with the audit expanders. Affordance present on Stages 0/1/2/3 entries.
result: skipped
reason: |
  Stage 1 page coverage explicitly verified during live UAT. Cross-page wiring code shipped in Plan 11 Task 2 with grep-verified acceptance (`stageAudit={buildStageAuditMap` present in all 5 stage-N/page.tsx). Same UnifiedDetailPane → effectiveStageAudit useMemo path applies; manual click-through deferred to first real cross-page operator session.

## Summary

total: 12
passed: 9
issues: 0
pending: 0
skipped: 3
blocked: 0

## Gaps

[none — 3 skipped tests are scope-reductions documented in Plan 11 SUMMARY, deferred to Phase 82.4, not code defects]

## Acknowledged Follow-Ups (documented in 82.3-11-SUMMARY.md)

1. Stage 2/3 richer audit content — needs per-row agent_runs + automation_runs server-side loading. Fold into Phase 82.4.
2. Stage 1 "(no mailbox)" header — pre-existing display quirk; address in the next display-fidelity pass.
3. Approve button no-op — pre-existing bulk-review action bug unrelated to 82.3; open separate `/gsd-debug` session when ready.
