---
phase: 82.3
plan: 07
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - web/components/automations/bulk-review/audit/StageDetailExpander.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx
  - web/components/automations/bulk-review/audit/__tests__/StageDetailExpander.test.tsx
autonomous: true
requirements: [82.3-EXPANDER-WIRING]
must_haves:
  truths:
    - "Every Stage 0–3 row in the detail-pane sidebar renders a Show details/Hide details toggle BELOW the Current line and ABOVE the existing control area"
    - "Stage 4 row renders unchanged (no expander affordance per CONTEXT <out_of_scope>)"
    - "Multiple expanders can be open simultaneously (no accordion)"
    - "Default state is collapsed"
  artifacts:
    - path: "web/components/automations/bulk-review/audit/StageDetailExpander.tsx"
      provides: "Generic Collapsible wrapper consumed by StageStep when auditDetails is non-null and n ≠ 4"
      contains: "export function StageDetailExpander"
  key_links:
    - from: "stage-step.tsx control area"
      to: "StageDetailExpander"
      via: "render BETWEEN line containing Current and existing state==='ok' / 'dirty' / 'skipped' control block"
      pattern: "&lt;StageDetailExpander"
---

<objective>
Wire the per-stage expander into the existing `StageStep` row in the detail-pane sidebar. Inserts a `Show details` / `Hide details` Collapsible between the "Current: {value}" line (line 85) and the override control area (line 87) of `stage-step.tsx`. Does NOT touch the override controls. Stage 4 (n === 4) is EXPLICITLY excluded.

Purpose: Make the audit panels actually visible. This is the integration point where Plans 03–06's renderers (threaded via Plan 02's `auditDetails` slot) become operator-visible.
Output: New `StageDetailExpander` wrapper + 2-line edit to `stage-step.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-UI-SPEC.md
@web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx
@web/components/ui/collapsible.tsx

<interfaces>
StageData (post-Plan-02) gains:
```typescript
auditDetails?: ReactNode;
```

UI-SPEC interaction contract:
- Inline placement (NOT side-popover)
- Multiple-open allowed (NO accordion)
- Default closed
- Affordance: `Show details` collapsed / `Hide details` open
- Chevron-down / chevron-up icon (lucide-react)
- Open-state chevron uses brand-accent color (`var(--v7-brand-primary)` / `#ff6a34`)
- 11px uppercase 600 button label
- Stage 4 row: NEVER renders the expander affordance
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StageDetailExpander generic Collapsible wrapper + tests</name>
  <files>web/components/automations/bulk-review/audit/StageDetailExpander.tsx, web/components/automations/bulk-review/audit/__tests__/StageDetailExpander.test.tsx</files>
  <read_first>
    - web/components/ui/collapsible.tsx (from Plan 01)
    - .planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-UI-SPEC.md §Interaction Contract
    - .planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-UI-SPEC.md §Copywriting Contract
  </read_first>
  <behavior>
    - Client component
    - Props: `stage: 0 | 1 | 2 | 3`, `children: ReactNode`
    - Renders a `<Collapsible>` with internal `open` state (uncontrolled). Default `open=false`.
    - Trigger button: `Show details` when closed, `Hide details` when open. ChevronDown icon when closed (muted color); ChevronUp icon when open (`var(--v7-brand-primary)` color).
    - Button styling: 11px uppercase 600, var(--space-2) gap, transparent background, no border, focus-visible outline `var(--v7-brand-secondary)`.
    - Content panel: padded `var(--space-3)`, background `var(--v7-panel-2)`, 1px solid `var(--v7-border)` top divider, margin-top `var(--space-2)`.
    - `data-testid="stage-detail-expander-{stage}"` on Collapsible root.
    - Children render inside `CollapsibleContent`.
    - `aria-controls` / `aria-expanded` correctly threaded (Radix handles this; test asserts behavior).
  </behavior>
  <action>
    Create `web/components/automations/bulk-review/audit/StageDetailExpander.tsx` as a `"use client"` component:
    ```typescript
    "use client";
    import { useState, type ReactNode } from "react";
    import { ChevronDown, ChevronUp } from "lucide-react";
    import { Collapsible, CollapsibleTrigger, CollapsibleContent } from "@/components/ui/collapsible";

    interface Props { stage: 0 | 1 | 2 | 3; children: ReactNode; }
    export function StageDetailExpander({ stage, children }: Props) { /* … */ }
    ```

    Locked copy: `Show details` (closed) / `Hide details` (open).

    Test cases (vitest + RTL + userEvent):
    - default state: button labeled "Show details", content NOT in DOM (or has hidden attribute)
    - click button: label flips to "Hide details", content visible (testid for child renders)
    - click again: collapses back
    - keyboard: focus button, press Enter → toggles open
    - aria: button has `aria-expanded="false"` initially, `aria-expanded="true"` when open
    - testid `stage-detail-expander-2` present when `stage={2}`
  </action>
  <verify>
    <automated>test -f web/components/automations/bulk-review/audit/StageDetailExpander.tsx &amp;&amp; head -1 web/components/automations/bulk-review/audit/StageDetailExpander.tsx | grep -q '"use client"' &amp;&amp; cd web &amp;&amp; npx vitest run --reporter=dot components/automations/bulk-review/audit/__tests__/StageDetailExpander.test.tsx 2>&amp;1 | tail -15 &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File starts with `"use client";`
    - `grep "Show details" StageDetailExpander.tsx` matches
    - `grep "Hide details" StageDetailExpander.tsx` matches
    - `grep "ChevronDown" StageDetailExpander.tsx` matches
    - `grep "ChevronUp" StageDetailExpander.tsx` matches
    - `grep "from \"@/components/ui/collapsible\"" StageDetailExpander.tsx` matches
    - vitest exit 0 with at least 5 cases
    - tsc clean
  </acceptance_criteria>
  <done>StageDetailExpander reusable Collapsible wrapper passes tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire expander into stage-step.tsx (Stage 0–3 only)</name>
  <files>web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx (full file)
    - web/components/automations/bulk-review/audit/StageDetailExpander.tsx (just created)
    - .planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-CONTEXT.md `<out_of_scope>` Stage 4
  </read_first>
  <behavior>
    - Stage 0–3 rows with `stage.auditDetails != null && stage.n !== 4` render `<StageDetailExpander stage={stage.n}>{stage.auditDetails}</StageDetailExpander>` between the `Current: …` line (currently lines 78–85) and the `{/* Control area */}` div (currently line 87)
    - Stage 4 NEVER renders the expander (compile-time exclusion + runtime guard)
    - When `auditDetails == null`, no expander renders (clean fallback for stages without telemetry)
    - Existing override controls + state lines render unchanged below the expander
    - Existing tests for stage-step still pass
  </behavior>
  <action>
    Edit `web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx`:

    1. Add import at top: `import { StageDetailExpander } from "@/components/automations/bulk-review/audit/StageDetailExpander";`

    2. Between the closing `</div>` of the "Current value" block (line 85) and the `<div className="mt-1">` of the "Control area" block (line 87), INSERT:
    ```tsx
    {stage.auditDetails != null && stage.n !== 4 && (
      <div className="mt-2">
        <StageDetailExpander stage={stage.n as 0 | 1 | 2 | 3}>
          {stage.auditDetails}
        </StageDetailExpander>
      </div>
    )}
    ```

    Do NOT modify:
    - The 30×30 node circle
    - The Title + axis tag row
    - The `Current:` line
    - The control area branches (ok / dirty / skipped)
    - The override-stage button

    Add a co-located test (or extend existing — check `__tests__` neighbor) confirming:
    - When `stage.auditDetails` is provided AND `stage.n !== 4` → expander testid `stage-detail-expander-{n}` is in DOM
    - When `stage.n === 4` → expander testid absent even if auditDetails non-null (defensive)
    - When `auditDetails` is undefined → expander absent
  </action>
  <verify>
    <automated>grep -n "StageDetailExpander" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/components/stage-step.tsx &amp;&amp; grep -n "stage.n !== 4" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/components/stage-step.tsx &amp;&amp; cd web &amp;&amp; npx vitest run --reporter=dot app/\\(dashboard\\)/automations/\\[swarm\\]/stage-1/components 2>&amp;1 | tail -20 &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "StageDetailExpander" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/components/stage-step.tsx` ≥ 2 (import + use)
    - `grep "stage.n !== 4" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/components/stage-step.tsx` matches (Stage 4 exclusion)
    - `grep "stage.auditDetails != null" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/components/stage-step.tsx` matches
    - Existing override-stage button regex `grep "override stage" stage-step.tsx` still matches (control area untouched)
    - vitest exit 0; existing stage-step tests still pass
    - tsc clean
  </acceptance_criteria>
  <done>Expander renders for Stage 0–3 rows when auditDetails present; Stage 4 untouched.</done>
</task>

</tasks>

<verification>
- Manual smoke (deferred to Plan 11): open `/automations/debtor-email/stage-1`, select a row, expect Stage 0/1/2/3 each show a "Show details" toggle, Stage 4 shows nothing extra
- Vitest green
- Existing stage-step + detail-pane tests still pass
</verification>

<success_criteria>
Every Stage 0–3 row in the sidebar renders a Show details toggle when `auditDetails` is provided. Click expands inline. Multiple expanders may be open. Stage 4 untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-07-SUMMARY.md`.
</output>
