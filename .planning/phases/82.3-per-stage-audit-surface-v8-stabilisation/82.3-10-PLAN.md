---
phase: 82.3
plan: 10
type: execute
wave: 3
depends_on: []
files_modified:
  - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx
  - web/app/(dashboard)/automations/[swarm]/_shell/_lib/display-fallbacks.ts
  - web/app/(dashboard)/automations/[swarm]/_shell/_lib/__tests__/display-fallbacks.test.ts
autonomous: true
requirements: [82.3-DISPLAY-QUALITY-FIX]
must_haves:
  truths:
    - "Bulk Review detail-pane never displays the literal string 'Planning' as a sender_name — it renders '(unknown sender)' instead"
    - "Missing subject renders '(no subject)' verbatim"
    - "The fix is display-layer only — no DB write, no row-mapper schema change"
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/_shell/_lib/display-fallbacks.ts"
      provides: "displaySender() + displaySubject() pure helpers used by detail-pane header"
      contains: "export function displaySender"
  key_links:
    - from: "detail-pane.tsx header (lines ~370–380)"
      to: "display-fallbacks.ts"
      via: "displaySender(row.from_name, row.from_email) / displaySubject(row.subject)"
      pattern: "displaySender(\|displaySubject("
---

<objective>
Fix the display-layer bug flagged in 2026-05-11 smoke: sender_name = "Planning" leaks through to Bulk Review rows on Stages 1 and 3. CONTEXT.md confirmed this is a row-mapper display bug, not a persistence bug — operator-confirmed.

Purpose: Per UI-SPEC §Copywriting Contract, missing sender renders `(unknown sender)` muted italic; the literal `"Planning"` (an Outlook display-name artifact from a system mailbox) is treated as missing.
Output: One small pure helper module + tests + a 2-line swap in `detail-pane.tsx` header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-CONTEXT.md
@.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-UI-SPEC.md
@web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx

Note: per the MEMORY note `feedback_email_pipeline_lookup_keys`, `email_pipeline.emails.sender_name` is the source column (NOT `sender_first_name`). The "Planning" string is an Outlook display-name artifact for system mailboxes — treat it as a sentinel for "no real sender".
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: displaySender + displaySubject helpers + tests</name>
  <files>web/app/(dashboard)/automations/[swarm]/_shell/_lib/display-fallbacks.ts, web/app/(dashboard)/automations/[swarm]/_shell/_lib/__tests__/display-fallbacks.test.ts</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx lines 362–380 (existing header render)
    - .planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-UI-SPEC.md §Copywriting Contract → Data-quality fix rows
  </read_first>
  <behavior>
    - `displaySender(from_name: string | null | undefined, from_email: string | null | undefined): string`:
      - Trim whitespace on both inputs
      - If trimmed `from_name` is non-empty AND not equal to the sentinel `"Planning"` (case-sensitive — that's the literal Outlook artifact) → return it
      - Else if trimmed `from_email` is non-empty → return it
      - Else return `"(unknown sender)"`
    - `displaySubject(subject: string | null | undefined): string`:
      - Trim whitespace
      - If trimmed non-empty → return it
      - Else return `"(no subject)"`
    - Both functions pure, no side effects
  </behavior>
  <action>
    Create `web/app/(dashboard)/automations/[swarm]/_shell/_lib/display-fallbacks.ts`:
    ```typescript
    // Phase 82.3 Plan 10 — display-layer fallbacks for the detail-pane header.
    // CONTEXT.md §Data-quality fix: "Planning" is an Outlook display-name artifact
    // from a system mailbox; treat it as a sentinel for "no real sender".

    const PLANNING_SENTINEL = "Planning";

    export function displaySender(
      from_name: string | null | undefined,
      from_email: string | null | undefined,
    ): string {
      const name = (from_name ?? "").trim();
      if (name.length > 0 && name !== PLANNING_SENTINEL) return name;
      const email = (from_email ?? "").trim();
      if (email.length > 0) return email;
      return "(unknown sender)";
    }

    export function displaySubject(subject: string | null | undefined): string {
      const s = (subject ?? "").trim();
      return s.length > 0 ? s : "(no subject)";
    }
    ```

    Test cases (vitest):
    - displaySender("Alice", "a@b.com") → "Alice"
    - displaySender("Planning", "real@b.com") → "real@b.com" (sentinel rejected, falls through to email)
    - displaySender("Planning", null) → "(unknown sender)"
    - displaySender(null, "fallback@b.com") → "fallback@b.com"
    - displaySender("  ", "  ") → "(unknown sender)"
    - displaySender(null, null) → "(unknown sender)"
    - displaySender(undefined, undefined) → "(unknown sender)"
    - displaySubject("Hello") → "Hello"
    - displaySubject("") → "(no subject)"
    - displaySubject("   ") → "(no subject)"
    - displaySubject(null) → "(no subject)"
    - displaySubject(undefined) → "(no subject)"
  </action>
  <verify>
    <automated>test -f web/app/\(dashboard\)/automations/\[swarm\]/_shell/_lib/display-fallbacks.ts &amp;&amp; grep -q "PLANNING_SENTINEL" web/app/\(dashboard\)/automations/\[swarm\]/_shell/_lib/display-fallbacks.ts &amp;&amp; cd web &amp;&amp; npx vitest run --reporter=dot app/\\(dashboard\\)/automations/\\[swarm\\]/_shell/_lib/__tests__/display-fallbacks.test.ts 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `web/app/(dashboard)/automations/[swarm]/_shell/_lib/display-fallbacks.ts`
    - `grep "PLANNING_SENTINEL" display-fallbacks.ts` matches
    - `grep -c "^export function" display-fallbacks.ts` = 2
    - `grep "(unknown sender)" display-fallbacks.ts` matches
    - `grep "(no subject)" display-fallbacks.ts` matches
    - vitest exit 0 with ≥12 cases
  </acceptance_criteria>
  <done>Display helpers ready.</done>
</task>

<task type="auto">
  <name>Task 2: Wire helpers into detail-pane.tsx header</name>
  <files>web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx lines 358–382
    - web/app/(dashboard)/automations/[swarm]/_shell/_lib/display-fallbacks.ts (Task 1)
  </read_first>
  <action>
    Edit `detail-pane.tsx`:

    1. Add import near top of file: `import { displaySender, displaySubject } from "./_lib/display-fallbacks";`

    2. Replace the existing subject line (currently `{row.subject ?? "(no subject)"}` around line 370) with `{displaySubject(row.subject)}`.

    3. Replace the existing from line (currently `From {row.from_name ?? row.from_email ?? "(unknown)"}` around line 379) with `From {displaySender(row.from_name, row.from_email)}`.

    Do NOT change any other JSX, styling, or behavior. Do NOT modify the row-mapper / loader — the fix is display-layer only per CONTEXT.md §Data-quality fix.
  </action>
  <verify>
    <automated>grep -n "displaySender\|displaySubject" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx &amp;&amp; ! grep "row.subject ?? \"(no subject)\"" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx &amp;&amp; ! grep "From {row.from_name ??" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx &amp;&amp; cd web &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -5 &amp;&amp; npx vitest run --reporter=dot app/\\(dashboard\\)/automations/\\[swarm\\]/_shell 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "displaySender\|displaySubject" detail-pane.tsx` ≥ 3 (1 import + 2 uses)
    - Old `row.subject ?? "(no subject)"` literal absent (grep returns 0)
    - Old `From {row.from_name ?? row.from_email ?? "(unknown)"}` literal absent
    - tsc clean
    - existing detail-pane tests still pass
  </acceptance_criteria>
  <done>Sender + subject render through the locked fallback helpers.</done>
</task>

</tasks>

<verification>
Vitest green; tsc clean; visual rendering smoke deferred to Plan 11 final pass.
</verification>

<success_criteria>
Operator never sees "Planning" as a sender in Bulk Review. Missing subjects render "(no subject)" consistently.
</success_criteria>

<output>
After completion, create `.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-10-SUMMARY.md`.
</output>
