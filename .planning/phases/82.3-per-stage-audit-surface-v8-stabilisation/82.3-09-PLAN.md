---
phase: 82.3
plan: 09
type: execute
wave: 3
depends_on: [01, 03, 04, 05, 06]
files_modified:
  - web/components/automations/bulk-review/audit/RawJsonToggle.tsx
  - web/components/automations/bulk-review/audit/Stage0EvidencePanel.tsx
  - web/components/automations/bulk-review/audit/Stage1EvidencePanel.tsx
  - web/components/automations/bulk-review/audit/Stage2EvidencePanel.tsx
  - web/components/automations/bulk-review/audit/Stage3EvidencePanel.tsx
  - web/components/automations/bulk-review/audit/__tests__/RawJsonToggle.test.tsx
autonomous: true
requirements: [82.3-RAW-JSON-TOGGLE]
must_haves:
  truths:
    - "Each Stage 0–3 evidence panel renders a Show raw JSON / Hide raw JSON sub-toggle that reveals decision_details / tool_outputs"
    - "Raw JSON block uses Geist Mono 12px, max-height 240px with internal scroll"
    - "Default closed; click toggles open"
  artifacts:
    - path: "web/components/automations/bulk-review/audit/RawJsonToggle.tsx"
      provides: "Sub-collapsible code block for raw JSON inspection"
      contains: "export function RawJsonToggle"
  key_links:
    - from: "Stage{0,1,2,3}EvidencePanel"
      to: "RawJsonToggle"
      via: "swap data-testid='stage{n}-raw-json-slot' placeholder for <RawJsonToggle raw={payload.raw} />"
      pattern: "RawJsonToggle raw="
---

<!--
File-count justification (per checker review 2026-05-12): Task 2 touches 4 stage-panel files
plus introduces RawJsonToggle (6 files total). The 4-panel swap is mechanically identical
across stages (same import, same placeholder-to-component replacement) — splitting would
add coordination churn without reducing risk. Kept in one plan deliberately.
-->

<objective>
Implement the "Show raw JSON / Hide raw JSON" sub-toggle and swap the placeholders left in Plans 03–06.

Purpose: 20% escape hatch for ambiguous audit stories — inspect raw payload without leaving the row.
Output: One component + tests + 4 placeholder-to-real swaps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-UI-SPEC.md
@web/components/ui/collapsible.tsx
@web/components/automations/bulk-review/audit/StageDetailExpander.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RawJsonToggle component + tests</name>
  <files>web/components/automations/bulk-review/audit/RawJsonToggle.tsx, web/components/automations/bulk-review/audit/__tests__/RawJsonToggle.test.tsx</files>
  <read_first>
    - web/components/ui/collapsible.tsx
    - web/components/automations/bulk-review/audit/StageDetailExpander.tsx
    - .planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-UI-SPEC.md §Interaction Contract → Raw JSON sub-toggle
  </read_first>
  <behavior>
    - Client component, props `{ raw: Record<string, unknown> }`
    - Trigger label `Show raw JSON` (closed) / `Hide raw JSON` (open) — mono 12px, muted color closed, accent (`var(--v7-brand-primary)`) on hover/focus
    - Content `<pre><code>` rendering `JSON.stringify(raw, null, 2)` with font-family Geist Mono 12px, max-height 240px, overflow-y auto, background `var(--v7-panel-2)`, border 1px solid `var(--v7-border)`, padding `var(--space-3)`, border-radius 4px
    - Default closed
    - testids: `raw-json-toggle` on root, `raw-json-content` on `<pre>`
  </behavior>
  <action>
    Create `web/components/automations/bulk-review/audit/RawJsonToggle.tsx` as `"use client"`. Use the same Radix Collapsible primitives as StageDetailExpander.

    Locked copy strings: `Show raw JSON`, `Hide raw JSON`.

    Test cases:
    - default closed: trigger label is `Show raw JSON`; content not visible
    - click: label flips to `Hide raw JSON`; content visible
    - rendered JSON includes 2-space indent (assert content includes `"foo": 1` for `{ foo: 1 }`)
    - empty object: renders `{}`
    - Enter key on trigger toggles
  </action>
  <verify>
    <automated>test -f web/components/automations/bulk-review/audit/RawJsonToggle.tsx &amp;&amp; head -1 web/components/automations/bulk-review/audit/RawJsonToggle.tsx | grep -q '"use client"' &amp;&amp; cd web &amp;&amp; npx vitest run --reporter=dot components/automations/bulk-review/audit/__tests__/RawJsonToggle.test.tsx 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File starts with `"use client";`
    - `grep "Show raw JSON" RawJsonToggle.tsx` matches; `grep "Hide raw JSON" RawJsonToggle.tsx` matches
    - `grep "JSON.stringify" RawJsonToggle.tsx` matches with `, null, 2`
    - `grep "max-height: 240px\|maxHeight.*240" RawJsonToggle.tsx` matches
    - vitest exit 0 with ≥4 cases
  </acceptance_criteria>
  <done>RawJsonToggle ready for use in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Swap stage{0..3}-raw-json-slot placeholders for the real component</name>
  <files>web/components/automations/bulk-review/audit/Stage0EvidencePanel.tsx, web/components/automations/bulk-review/audit/Stage1EvidencePanel.tsx, web/components/automations/bulk-review/audit/Stage2EvidencePanel.tsx, web/components/automations/bulk-review/audit/Stage3EvidencePanel.tsx</files>
  <read_first>
    - web/components/automations/bulk-review/audit/RawJsonToggle.tsx (from Task 1)
    - The four panel files (each has a `data-testid="stage{n}-raw-json-slot"` placeholder div from Plans 03/04/05/06)
  </read_first>
  <action>
    In each of the four panel files:
    1. Add import: `import { RawJsonToggle } from "./RawJsonToggle";`
    2. Replace the placeholder `<div data-testid="stage{n}-raw-json-slot" />` with `<div data-testid="stage{n}-raw-json-slot"><RawJsonToggle raw={payload.raw} /></div>` (keep the testid wrapper so existing render tests still pass; the wrapper now contains the real toggle)

    Apply identically to Stage0/1/2/3 panels. No other change to the panels.

    Re-run each panel's existing vitest suite — they should still pass because the slot testid is preserved.
  </action>
  <verify>
    <automated>grep -c "RawJsonToggle" web/components/automations/bulk-review/audit/Stage0EvidencePanel.tsx web/components/automations/bulk-review/audit/Stage1EvidencePanel.tsx web/components/automations/bulk-review/audit/Stage2EvidencePanel.tsx web/components/automations/bulk-review/audit/Stage3EvidencePanel.tsx &amp;&amp; cd web &amp;&amp; npx vitest run --reporter=dot components/automations/bulk-review/audit 2>&amp;1 | tail -20 &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Each of Stage0/1/2/3 EvidencePanel.tsx contains exactly 2 `RawJsonToggle` references (1 import + 1 use)
    - Each panel still contains the `stage{n}-raw-json-slot` testid wrapper
    - All existing audit/* vitest suites pass
    - tsc clean
  </acceptance_criteria>
  <done>All 4 stage panels surface raw JSON via the locked sub-toggle.</done>
</task>

</tasks>

<verification>
Vitest green; tsc clean; locked copy strings present.
</verification>

<success_criteria>
Every Stage 0–3 expander includes a Show raw JSON sub-toggle that reveals the source payload at Geist Mono 12px with 240px scroll.
</success_criteria>

<output>
After completion, create `.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-09-SUMMARY.md`.
</output>
