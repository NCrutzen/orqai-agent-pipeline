---
phase: 82.3
plan: 05
type: execute
wave: 2
depends_on: [02]
files_modified:
  - web/components/automations/bulk-review/audit/Stage2EvidencePanel.tsx
  - web/components/automations/bulk-review/audit/ScreenshotThumb.tsx
  - web/components/automations/bulk-review/audit/__tests__/Stage2EvidencePanel.test.tsx
  - web/components/automations/bulk-review/audit/__tests__/ScreenshotThumb.test.tsx
autonomous: true
requirements: [82.3-STAGE2-RENDERER, 82.3-SCREENSHOT-THUMB]
must_haves:
  truths:
    - "Stage 2 expander shows identifier source, confidence, top-3 candidates with scores, and iController before/after screenshots"
    - "ScreenshotThumb renders 128px-max thumbnail; click toggles inline full-width render below"
    - "Missing screenshot path renders 'No iController capture for this run.' (locked copy)"
  artifacts:
    - path: "web/components/automations/bulk-review/audit/Stage2EvidencePanel.tsx"
      provides: "Stage 2 evidence renderer"
      contains: "export function Stage2EvidencePanel"
    - path: "web/components/automations/bulk-review/audit/ScreenshotThumb.tsx"
      provides: "Screenshot thumbnail with click-to-inline-expand and signed-URL fetch"
      contains: "export function ScreenshotThumb"
  key_links:
    - from: "ScreenshotThumb"
      to: "Plan 08 signed-URL API route"
      via: "fetch('/api/automations/audit/signed-url?path=…')"
      pattern: "/api/automations/audit/signed-url"
---

<objective>
Build the Stage 2 evidence renderer + the `ScreenshotThumb` helper it depends on. Per CONTEXT.md the Stage 2 expander surfaces the iController before/after screenshot pair (Supabase Storage bucket `automation-screenshots`) via signed URLs refreshed on every open.

This plan builds the renderer + thumb. The signed-URL endpoint itself lands in Plan 08 (Wave 3) — `ScreenshotThumb` calls `/api/automations/audit/signed-url` and degrades gracefully when 404 (Plan 08 not yet deployed in dev).

Purpose: Stage 2 is the highest-value evidence surface (operators want visual proof of what iController showed).
Output: Two components + their tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-UI-SPEC.md
@.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-CONTEXT.md
@web/app/(dashboard)/automations/[swarm]/_shell/_lib/audit-types.ts
@web/components/automations/bulk-review/audit/Stage0EvidencePanel.tsx
@web/components/ui/skeleton.tsx

<interfaces>
From audit-types.ts:
```typescript
export interface Stage2AuditPayload {
  stage: 2;
  identifier_source: "thread" | "sender" | "identifier" | "unresolved" | null;
  confidence: "high" | "medium" | "low" | null;
  top_candidates: Array&lt;{ account_id: string; name: string; score: number }&gt;;
  screenshot_paths: { before: string | null; after: string | null };
  raw: Record&lt;string, unknown&gt;;
}
```

UI-SPEC tokens:
- Identifier source chip: chip-pink
- Confidence chip: chip-lime/chip-amber/text-muted
- Unresolved verdict: destructive (`#ef4444`)
- Screenshot thumb: max-height 128px, click → inline expand (NOT modal)
- Loading: skeleton shimmer at thumbnail dimensions (use existing `@/components/ui/skeleton`)
- Empty-state copies (locked):
  - `Customer could not be resolved. No candidates returned.`
  - `No iController capture for this run.`
  - `Screenshot unavailable. Refresh to retry.`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ScreenshotThumb client component + tests</name>
  <files>web/components/automations/bulk-review/audit/ScreenshotThumb.tsx, web/components/automations/bulk-review/audit/__tests__/ScreenshotThumb.test.tsx</files>
  <read_first>
    - web/components/ui/skeleton.tsx
    - .planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-UI-SPEC.md §Interaction Contract → Screenshot rendering + Loading state + Error state
    - .planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-CONTEXT.md §Screenshots
  </read_first>
  <behavior>
    - Client component (`"use client"`)
    - Props: `path: string` (Supabase Storage object path), `label: string` (e.g., "Before", "After")
    - On mount: fetch `/api/automations/audit/signed-url?path={encodeURIComponent(path)}` → expect JSON `{ url: string, expires_at: string }`
    - While loading: render `&lt;Skeleton /&gt;` at 128px height + label
    - On success: render `&lt;img&gt;` with `style="max-height: 128px; height: auto; width: auto;"`, alt=label
    - On click (img): toggle internal `expanded` state. When `expanded=true`, render a SECOND `&lt;img&gt;` below at `width: 100%; height: auto;` with `data-testid="screenshot-full-{label}"`. Second click collapses. NO modal/lightbox.
    - On fetch error (network OR non-200): render muted text `Screenshot unavailable. Refresh to retry.` + a `&lt;button&gt;Refresh&lt;/button&gt;` that re-runs the fetch
    - Client-side cache: keep a module-level `Map<string, { url: string; cachedAt: number }>` with 200ms TTL per UI-SPEC §Interaction Contract. If same `path` requested within 200ms, return cached URL without re-fetch.
    - Loading aria: `aria-label="Loading screenshot…"` on the skeleton
  </behavior>
  <action>
    Create `web/components/automations/bulk-review/audit/ScreenshotThumb.tsx` as a `"use client"` component. Use `useState` for `{ url, loading, error, expanded }`. Use `useEffect` to trigger fetch on mount with proper cleanup (AbortController). Module-level `Map` for the 200ms cache.

    Locked copy strings (must appear verbatim):
    - `Screenshot unavailable. Refresh to retry.`
    - `Loading screenshot…`

    Test file: mock `global.fetch` per test case.
    - happy path: fetch resolves → img renders with returned URL
    - click thumb → second img appears with testid `screenshot-full-{label}`; click again → it disappears
    - 404 response → error state renders + Refresh button visible
    - Refresh button click re-triggers fetch
    - cache: rapid second instance with same path within 200ms does NOT issue a second fetch (assert `fetch.mock.calls.length === 1`)
  </action>
  <verify>
    <automated>test -f web/components/automations/bulk-review/audit/ScreenshotThumb.tsx &amp;&amp; head -1 web/components/automations/bulk-review/audit/ScreenshotThumb.tsx | grep -q '"use client"' &amp;&amp; cd web &amp;&amp; npx vitest run --reporter=dot components/automations/bulk-review/audit/__tests__/ScreenshotThumb.test.tsx 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File starts with `"use client";` directive (line 1)
    - `grep "/api/automations/audit/signed-url" ScreenshotThumb.tsx` matches
    - `grep "Screenshot unavailable. Refresh to retry." ScreenshotThumb.tsx` matches
    - `grep "Loading screenshot" ScreenshotThumb.tsx` matches
    - Module-level Map declared (grep `new Map`)
    - 200ms TTL constant defined (grep `200`)
    - vitest exit 0 with at least 5 passing test cases
  </acceptance_criteria>
  <done>ScreenshotThumb fetches signed URLs, caches 200ms, click-toggles inline expand, handles errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stage2EvidencePanel pure component + tests</name>
  <files>web/components/automations/bulk-review/audit/Stage2EvidencePanel.tsx, web/components/automations/bulk-review/audit/__tests__/Stage2EvidencePanel.test.tsx</files>
  <read_first>
    - web/components/automations/bulk-review/audit/Stage0EvidencePanel.tsx
    - web/components/automations/bulk-review/audit/ScreenshotThumb.tsx (from Task 1)
    - .planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-UI-SPEC.md §Copywriting Contract Stage 2 rows
  </read_first>
  <behavior>
    - EVIDENCE block with: identifier-source chip (chip-pink), confidence chip, candidate table (top 3, mono `account_id` + name + score formatted `0.87`)
    - `identifier_source === "unresolved"` → render destructive-colored chip + muted body `Customer could not be resolved. No candidates returned.` AND skip candidate table even if non-empty
    - `top_candidates.length === 0 && identifier_source !== "unresolved"` → muted `No candidates returned.`
    - SCREENSHOTS block: renders `&lt;ScreenshotThumb path={screenshot_paths.before} label="Before" /&gt;` + `&lt;ScreenshotThumb path={screenshot_paths.after} label="After" /&gt;` side-by-side
    - If BOTH `screenshot_paths.before` and `.after` are null → SCREENSHOTS block renders muted `No iController capture for this run.` ONLY (no thumbs, no headers eaten — header still shows)
    - If only one path null → render the present thumb + muted dash for the missing side
    - All-null payload → only top-level muted empty-state line
    - `data-testid="stage2-raw-json-slot"` placeholder
  </behavior>
  <action>
    Create `web/components/automations/bulk-review/audit/Stage2EvidencePanel.tsx`. Pure component (no `"use client"` needed at the panel itself — ScreenshotThumb is the client boundary).

    Locked copy strings (must appear verbatim):
    - `Customer could not be resolved. No candidates returned.`
    - `No iController capture for this run.`
    - `No candidates returned.`

    Candidate table: simple `&lt;ul&gt;` with rows of `&lt;li&gt;` containing `&lt;span class="mono"&gt;{account_id}&lt;/span&gt; — {name} ({score.toFixed(2)})`. No `&lt;table&gt;` element needed; UI-SPEC names it "mono list with score".

    Test file mocks `ScreenshotThumb` so Panel tests don't hit fetch (use `vi.mock("../ScreenshotThumb", () => ({ ScreenshotThumb: ({ path, label }: any) => &lt;div data-testid={\`mock-thumb-${label}\`}&gt;{path ?? "null"}&lt;/div&gt; }))`).

    Test cases:
    - full payload (identifier_source="identifier", high confidence, 3 candidates, both screenshots) → all visible; mock thumbs render twice
    - unresolved (identifier_source="unresolved", empty candidates) → destructive chip + locked unresolved copy
    - no screenshots (both null) → locked "No iController capture for this run." appears
    - top_candidates with 5 entries → only first 3 rendered (rest silently truncated)
    - all-null → only empty-state line
  </action>
  <verify>
    <automated>test -f web/components/automations/bulk-review/audit/Stage2EvidencePanel.tsx &amp;&amp; cd web &amp;&amp; npx vitest run --reporter=dot components/automations/bulk-review/audit/__tests__/Stage2EvidencePanel.test.tsx 2>&amp;1 | tail -20 &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function Stage2EvidencePanel" Stage2EvidencePanel.tsx` = 1
    - `grep "Customer could not be resolved. No candidates returned." Stage2EvidencePanel.tsx` matches
    - `grep "No iController capture for this run." Stage2EvidencePanel.tsx` matches
    - `grep "ScreenshotThumb" Stage2EvidencePanel.tsx` matches (import + use)
    - top_candidates truncation: `grep "slice(0, 3)" Stage2EvidencePanel.tsx` matches
    - vitest exit 0 with at least 5 cases
    - tsc clean
  </acceptance_criteria>
  <done>Stage2EvidencePanel renders all sub-blocks; handles unresolved + missing-screenshots correctly.</done>
</task>

</tasks>

<verification>
Both vitest suites green; tsc clean; locked copy strings present verbatim.
</verification>

<success_criteria>
Plan 11 can `import { Stage2EvidencePanel }` and feed `Stage2AuditPayload` derived from `agent_runs.context` + `automation_runs.result`. ScreenshotThumb calls Plan 08's API route.
</success_criteria>

<output>
After completion, create `.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-05-SUMMARY.md`.
</output>
