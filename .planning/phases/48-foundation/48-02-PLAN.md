---
phase: 48-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260415_v7_foundation.sql
autonomous: true
requirements: [RT-02, RT-03, RT-04]

must_haves:
  truths:
    - "agent_events table exists with correct columns and captures thinking/tool_call/waiting/done event types"
    - "swarm_jobs table exists with 5 Kanban stages (backlog, ready, progress, review, done)"
    - "swarm_agents table exists with status, metrics JSONB, skills JSONB, and unique(swarm_id, agent_name)"
    - "All 4 tables have Realtime enabled (REPLICA IDENTITY FULL) and RLS policies for authenticated read"
  artifacts:
    - path: "supabase/migrations/20260415_v7_foundation.sql"
      provides: "4 tables, indexes, RLS policies, Realtime config"
      contains: "CREATE TABLE agent_events"
  key_links:
    - from: "agent_events"
      to: "projects"
      via: "swarm_id FK references projects(id)"
      pattern: "REFERENCES projects"
    - from: "swarm_jobs"
      to: "projects"
      via: "swarm_id FK references projects(id)"
      pattern: "REFERENCES projects"
    - from: "swarm_agents"
      to: "projects"
      via: "swarm_id FK references projects(id)"
      pattern: "REFERENCES projects"
---

<objective>
Create the 4 V7 foundation database tables (agent_events, swarm_jobs, swarm_agents, swarm_briefings) with indexes, RLS policies, and Supabase Realtime publication.

Purpose: All real-time UI features in Phases 49-53 depend on these tables. Setting up Realtime publication now means no schema changes are needed later.
Output: Migration file executed against Supabase, 4 tables live with Realtime enabled.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-foundation/48-CONTEXT.md
@.planning/phases/48-foundation/48-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and execute V7 foundation database migration</name>
  <files>supabase/migrations/20260415_v7_foundation.sql</files>
  <action>
1. Create migration directory if not exists: `mkdir -p supabase/migrations`

2. Create `supabase/migrations/20260415_v7_foundation.sql` with all 4 tables. Use the exact schema from the RESEARCH.md code example:

**agent_events table:**
- id UUID PK, swarm_id UUID FK to projects(id) ON DELETE CASCADE, agent_name TEXT NOT NULL
- event_type TEXT NOT NULL with CHECK constraint: 'thinking', 'tool_call', 'tool_result', 'waiting', 'done', 'error', 'delegation'
- span_id TEXT, parent_span_id TEXT, content JSONB DEFAULT '{}'
- started_at TIMESTAMPTZ, ended_at TIMESTAMPTZ, created_at TIMESTAMPTZ DEFAULT NOW()
- Indexes: (swarm_id, created_at DESC), (span_id)

**swarm_jobs table:**
- id UUID PK, swarm_id UUID FK to projects(id) ON DELETE CASCADE, title TEXT NOT NULL, description TEXT
- stage TEXT NOT NULL DEFAULT 'backlog' with CHECK: 'backlog', 'ready', 'progress', 'review', 'done'
- priority TEXT DEFAULT 'normal' with CHECK: 'low', 'normal', 'high', 'urgent'
- assigned_agent TEXT, tags JSONB DEFAULT '[]', position INTEGER DEFAULT 0
- created_at TIMESTAMPTZ DEFAULT NOW(), updated_at TIMESTAMPTZ DEFAULT NOW()
- Index: (swarm_id, stage)

**swarm_agents table:**
- id UUID PK, swarm_id UUID FK to projects(id) ON DELETE CASCADE, agent_name TEXT NOT NULL, role TEXT
- status TEXT DEFAULT 'idle' with CHECK: 'idle', 'active', 'waiting', 'error', 'offline'
- parent_agent TEXT, metrics JSONB DEFAULT '{}', skills JSONB DEFAULT '[]', orqai_deployment_id TEXT
- created_at TIMESTAMPTZ DEFAULT NOW(), updated_at TIMESTAMPTZ DEFAULT NOW()
- UNIQUE(swarm_id, agent_name)
- Index: (swarm_id)

**swarm_briefings table:**
- id UUID PK, swarm_id UUID FK to projects(id) ON DELETE CASCADE, narrative TEXT NOT NULL
- metrics_snapshot JSONB DEFAULT '{}', generated_at TIMESTAMPTZ DEFAULT NOW()
- expires_at TIMESTAMPTZ DEFAULT (NOW() + INTERVAL '30 minutes')
- Index: (swarm_id, generated_at DESC)

**Realtime:**
- ALTER TABLE ... REPLICA IDENTITY FULL on all 4 tables

**Realtime publication:**
- Include this SQL in the migration (uncommented):
```sql
ALTER PUBLICATION supabase_realtime ADD TABLE agent_events, swarm_jobs, swarm_agents, swarm_briefings;
```

**RLS:**
- ENABLE ROW LEVEL SECURITY on all 4 tables
- SELECT policy for authenticated role (USING true) on all 4 tables
- No INSERT/UPDATE/DELETE policies for authenticated -- service role handles writes

3. Execute the migration against the Supabase database using the **Supabase Management API** (proven approach -- used successfully for project table consolidation earlier):

```bash
# Read the migration SQL file
SQL=$(cat supabase/migrations/20260415_v7_foundation.sql)

# Execute via Supabase Management API
curl -X POST "https://api.supabase.com/v1/projects/mvqjhlxfvtqqubqgdvhz/database/query" \
  -H "Authorization: Bearer sbp_f441f401fadff59cdf27d78e19c6d3727c240f15" \
  -H "Content-Type: application/json" \
  -d "{\"query\": \"$SQL\"}"
```

If the SQL is too large for a single request, split into logical chunks (tables, then indexes, then RLS, then Realtime) and execute sequentially via the same Management API endpoint.

4. After execution, verify ALL 4 tables exist by querying the REST API for each table (see verify command below).

5. Confirm Realtime publication is enabled. Run this query via the Management API:
```sql
SELECT tablename FROM pg_publication_tables WHERE pubname = 'supabase_realtime' AND tablename IN ('agent_events', 'swarm_jobs', 'swarm_agents', 'swarm_briefings');
```
This must return all 4 table names. If any are missing, run the ALTER PUBLICATION command again via the Management API.
  </action>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/Agent\ Workforce && ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im12cWpobHhmdnRxcXVicWdkdmh6Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzM1NzkzMzAsImV4cCI6MjA4OTE1NTMzMH0.8mKWUcA5o_0g0GKBc9OVBcA9MtHeo6I5kUOtbEfbK1U" && BASE="https://mvqjhlxfvtqqubqgdvhz.supabase.co/rest/v1" && for TABLE in agent_events swarm_jobs swarm_agents swarm_briefings; do CODE=$(curl -s -o /dev/null -w "%{http_code}" "$BASE/$TABLE?select=id&limit=0" -H "apikey: $ANON_KEY" -H "Authorization: Bearer $ANON_KEY"); echo "$TABLE: $CODE"; done</automated>
  </verify>
  <done>All 4 tables (agent_events, swarm_jobs, swarm_agents, swarm_briefings) exist in Supabase with correct columns, constraints, indexes, RLS policies, and REPLICA IDENTITY FULL. Authenticated users can SELECT from all tables. Realtime publication is confirmed enabled for all 4 tables (verified via pg_publication_tables query).</done>
</task>

</tasks>

<verification>
1. REST API query to each table returns 200 (not 404): agent_events, swarm_jobs, swarm_agents, swarm_briefings
2. Migration SQL file exists at supabase/migrations/20260415_v7_foundation.sql
3. Each table has correct CHECK constraints (verified by attempting invalid INSERT)
4. RLS is active (anonymous key can SELECT, cannot INSERT)
5. Realtime publication confirmed: `SELECT tablename FROM pg_publication_tables WHERE pubname = 'supabase_realtime'` includes all 4 tables
</verification>

<success_criteria>
- 4 tables exist in Supabase with all columns, constraints, and indexes from the schema
- Authenticated users can read all tables
- Service role can write to all tables
- REPLICA IDENTITY FULL is set on all 4 tables
- Realtime publication is enabled for all 4 tables (confirmed via pg_publication_tables query)
- Migration file committed for reproducibility
</success_criteria>

<output>
After completion, create `.planning/phases/48-foundation/48-02-SUMMARY.md`
</output>
