---
phase: 48-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/public/fonts/Satoshi-Variable.woff2
  - web/public/fonts/Satoshi-VariableItalic.woff2
  - web/public/fonts/CabinetGrotesk-Variable.woff2
  - web/app/layout.tsx
  - web/app/globals.css
  - web/components/theme-provider.tsx
  - web/components/theme-toggle.tsx
  - web/components/ui/glass-card.tsx
autonomous: true
requirements: [DS-01, DS-02, DS-03, DS-04]

must_haves:
  truths:
    - "App loads Satoshi Variable for body text and Cabinet Grotesk Variable for headings via CSS variables"
    - "User can toggle between dark and light theme and preference persists across page refresh with no FOUC"
    - "All V7 tokens use --v7-* prefix and existing shadcn tokens remain completely unchanged"
    - "GlassCard component renders with backdrop-blur, semi-transparent background, and subtle border"
  artifacts:
    - path: "web/public/fonts/Satoshi-Variable.woff2"
      provides: "Body font file"
    - path: "web/public/fonts/CabinetGrotesk-Variable.woff2"
      provides: "Heading font file"
    - path: "web/app/globals.css"
      provides: "V7 token namespace alongside existing shadcn tokens"
      contains: "--v7-bg"
    - path: "web/app/layout.tsx"
      provides: "Font loading and ThemeProvider wrapping"
      contains: "ThemeProvider"
    - path: "web/components/theme-provider.tsx"
      provides: "next-themes wrapper with data-theme attribute"
      contains: "attribute"
    - path: "web/components/theme-toggle.tsx"
      provides: "Dark/light toggle button"
      contains: "setTheme"
    - path: "web/components/ui/glass-card.tsx"
      provides: "Glassmorphism utility component"
      contains: "glass"
  key_links:
    - from: "web/app/layout.tsx"
      to: "web/components/theme-provider.tsx"
      via: "ThemeProvider wrapping children"
      pattern: "ThemeProvider"
    - from: "web/app/globals.css"
      to: "web/components/ui/glass-card.tsx"
      via: "CSS custom properties consumed by component"
      pattern: "--v7-glass"
    - from: "web/app/globals.css"
      to: "@custom-variant dark"
      via: "data-theme attribute selector"
      pattern: "data-theme"
---

<objective>
Establish the V7 design system: self-hosted fonts (Satoshi + Cabinet Grotesk), next-themes dark/light toggle with data-theme attribute, parallel --v7-* CSS token namespace, and GlassCard utility component.

Purpose: Every subsequent V7 phase depends on these tokens, fonts, and theme infrastructure. Existing V6 pages must remain visually untouched.
Output: Fonts loaded, theme toggle working, all V7 CSS tokens available, GlassCard component ready.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-foundation/48-CONTEXT.md
@.planning/phases/48-foundation/48-UI-SPEC.md
@.planning/phases/48-foundation/48-RESEARCH.md

@web/app/layout.tsx
@web/app/globals.css
</context>

<interfaces>
<!-- Existing code the executor needs to understand -->

From web/app/layout.tsx:
```typescript
import { Geist, Geist_Mono } from "next/font/google";
// Geist fonts use --font-geist-sans and --font-geist-mono CSS variables
// <html> has suppressHydrationWarning, <body> has font variable classNames
```

From web/app/globals.css (line 5):
```css
@custom-variant dark (&:is(.dark *));
/* This line MUST change to (&:is([data-theme="dark"] *)) */
/* Existing :root and .dark blocks with oklch tokens must NOT be modified */
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install next-themes, download fonts, add V7 tokens to globals.css</name>
  <files>web/package.json, web/public/fonts/Satoshi-Variable.woff2, web/public/fonts/Satoshi-VariableItalic.woff2, web/public/fonts/CabinetGrotesk-Variable.woff2, web/app/globals.css</files>
  <action>
1. Install next-themes: `cd web && npm install next-themes@^0.4`

2. Download font files from Fontshare into `web/public/fonts/`:
   - Satoshi Variable: download from https://www.fontshare.com/fonts/satoshi (get the woff2 variable files)
   - Cabinet Grotesk Variable: download from https://www.fontshare.com/fonts/cabinet-grotesk (get the woff2 variable file)
   - Use curl to download. If Fontshare requires a zip download, download the zip, extract woff2 files, place in `web/public/fonts/`.
   - Expected files: `Satoshi-Variable.woff2`, `Satoshi-VariableItalic.woff2`, `CabinetGrotesk-Variable.woff2`

3. Update `globals.css` line 5 -- change `@custom-variant dark (&:is(.dark *));` to `@custom-variant dark (&:is([data-theme="dark"] *));`

4. Add V7 token blocks AFTER the existing `:root` and `.dark` blocks in `globals.css`. Do NOT modify any existing token values. Add a clear comment separator:

```css
/* ============================================================
   V7 Design System — Parallel namespace (--v7-*)
   These tokens coexist with existing shadcn tokens above.
   NEVER modify existing :root or .dark token values.
   ============================================================ */

:root, [data-theme="light"] {
  /* Backgrounds */
  --v7-bg: #f6f3ee;
  --v7-bg-2: #fbfaf7;
  --v7-panel: #f9f6f1;
  --v7-panel-2: #f2eee8;
  --v7-line: #d5d1ca;
  /* Text */
  --v7-text: #231f19;
  --v7-muted: #6f6a61;
  --v7-faint: #958f84;
  --v7-inverse: #fbfaf7;
  /* Accents */
  --v7-teal: #01696f;
  --v7-blue: #0c6aa7;
  --v7-lime: #4d8d1c;
  --v7-amber: #b66a11;
  --v7-pink: #b93e8a;
  --v7-red: #b5454e;
  /* Soft accent backgrounds */
  --v7-teal-soft: rgba(1,105,111,0.12);
  --v7-blue-soft: rgba(12,106,167,0.12);
  --v7-amber-soft: rgba(182,106,17,0.13);
  --v7-pink-soft: rgba(185,62,138,0.13);
  /* Glass */
  --v7-glass-bg: #f9f6f1;
  --v7-glass-border: #d5d1ca;
  --v7-glass-blur: 18px;
  --v7-glass-shadow: 0 8px 24px rgba(28,24,18,0.08);
  --v7-glass-shadow-heavy: 0 24px 70px rgba(28,24,18,0.14);
  /* Radius */
  --v7-radius: 22px;
  --v7-radius-lg: 28px;
  --v7-radius-card: 24px;
  --v7-radius-sm: 14px;
  --v7-radius-inner: 16px;
  --v7-radius-kpi: 20px;
  --v7-radius-mini: 18px;
  --v7-radius-pill: 999px;
}

[data-theme="dark"] {
  /* Backgrounds */
  --v7-bg: #0c1117;
  --v7-bg-2: #101722;
  --v7-panel: rgba(19,26,36,0.82);
  --v7-panel-2: #151d29;
  --v7-line: rgba(255,255,255,0.08);
  /* Text */
  --v7-text: #edf3fb;
  --v7-muted: #9fadc4;
  --v7-faint: #6b7688;
  --v7-inverse: #091018;
  /* Accents */
  --v7-teal: #3ac7c9;
  --v7-blue: #69a8ff;
  --v7-lime: #8ad05e;
  --v7-amber: #ffb547;
  --v7-pink: #ff78cf;
  --v7-red: #ff6b7a;
  /* Soft accent backgrounds */
  --v7-teal-soft: rgba(58,199,201,0.13);
  --v7-blue-soft: rgba(105,168,255,0.13);
  --v7-amber-soft: rgba(255,181,71,0.13);
  --v7-pink-soft: rgba(255,120,207,0.13);
  /* Glass */
  --v7-glass-bg: rgba(19,26,36,0.82);
  --v7-glass-border: rgba(255,255,255,0.08);
  --v7-glass-blur: 18px;
  --v7-glass-shadow: 0 10px 30px rgba(0,0,0,0.22);
  --v7-glass-shadow-heavy: 0 30px 100px rgba(0,0,0,0.42);
  /* Radius — same as light, inherited from :root */
}
```

All values come directly from 48-UI-SPEC.md. Copy them exactly.
  </action>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/Agent\ Workforce/web && node -e "require('next-themes')" && ls public/fonts/*.woff2 && grep 'data-theme' app/globals.css && grep '\-\-v7-bg' app/globals.css</automated>
  </verify>
  <done>next-themes installed, 3 font woff2 files in public/fonts/, @custom-variant updated to data-theme, all V7 tokens present in globals.css with light and dark variants, existing shadcn tokens completely untouched</done>
</task>

<task type="auto">
  <name>Task 2: Wire fonts and ThemeProvider in layout, create theme-toggle and GlassCard</name>
  <files>web/app/layout.tsx, web/components/theme-provider.tsx, web/components/theme-toggle.tsx, web/components/ui/glass-card.tsx</files>
  <action>
1. Create `web/components/theme-provider.tsx`:
```typescript
"use client";
import { ThemeProvider as NextThemesProvider } from "next-themes";

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  return (
    <NextThemesProvider
      attribute="data-theme"
      defaultTheme="dark"
      enableSystem
      disableTransitionOnChange
    >
      {children}
    </NextThemesProvider>
  );
}
```

2. Create `web/components/theme-toggle.tsx`:
```typescript
"use client";
import { useTheme } from "next-themes";
import { Moon, Sun } from "lucide-react";
import { Button } from "@/components/ui/button";

export function ThemeToggle() {
  const { theme, setTheme } = useTheme();
  return (
    <Button
      variant="ghost"
      size="icon"
      onClick={() => setTheme(theme === "dark" ? "light" : "dark")}
      aria-label="Toggle theme"
    >
      <Sun className="h-4 w-4 rotate-0 scale-100 transition-all dark:rotate-90 dark:scale-0" />
      <Moon className="absolute h-4 w-4 rotate-90 scale-0 transition-all dark:rotate-0 dark:scale-100" />
    </Button>
  );
}
```

3. Create `web/components/ui/glass-card.tsx`:
```typescript
import { cn } from "@/lib/utils";

export function GlassCard({
  className,
  ...props
}: React.ComponentProps<"div">) {
  return (
    <div
      className={cn(
        "rounded-[var(--v7-radius)]",
        "bg-[var(--v7-glass-bg)]",
        "border border-[var(--v7-glass-border)]",
        "backdrop-blur-[18px]",
        "shadow-[var(--v7-glass-shadow)]",
        "transition-all duration-200",
        className
      )}
      {...props}
    />
  );
}
```

4. Update `web/app/layout.tsx`:
   - Add `localFont` import from `next/font/local`
   - Add Satoshi and Cabinet Grotesk font declarations (keep existing Geist fonts)
   - Import ThemeProvider from `@/components/theme-provider`
   - Add all 4 font CSS variable classes to `<html>` className
   - Wrap children with ThemeProvider inside body (wrap the existing TooltipProvider)

Satoshi config:
```typescript
const satoshi = localFont({
  src: [
    { path: "../public/fonts/Satoshi-Variable.woff2", style: "normal" },
    { path: "../public/fonts/Satoshi-VariableItalic.woff2", style: "italic" },
  ],
  variable: "--font-satoshi",
  display: "swap",
  weight: "300 900",
});
```

Cabinet Grotesk config:
```typescript
const cabinetGrotesk = localFont({
  src: "../public/fonts/CabinetGrotesk-Variable.woff2",
  variable: "--font-cabinet",
  display: "swap",
  weight: "100 900",
});
```

The `<html>` tag should have: `className={\`${satoshi.variable} ${cabinetGrotesk.variable} ${geistSans.variable} ${geistMono.variable}\`}`

Layout body structure should be:
```tsx
<body className={`${satoshi.variable} ${cabinetGrotesk.variable} ${geistSans.variable} ${geistMono.variable} antialiased`}>
  <ThemeProvider>
    <TooltipProvider>{children}</TooltipProvider>
    <Toaster richColors position="bottom-right" />
  </ThemeProvider>
</body>
```

IMPORTANT: Do NOT remove Geist fonts or change existing font variable names. The new fonts are additive.
  </action>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/Agent\ Workforce/web && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>App builds without errors. ThemeProvider wraps app with data-theme attribute. 4 font CSS variables available (--font-satoshi, --font-cabinet, --font-geist-sans, --font-geist-mono). ThemeToggle component exists. GlassCard component exists with correct V7 token references. Existing pages render unchanged.</done>
</task>

</tasks>

<verification>
1. `cd web && npx next build` completes without errors
2. `grep -c '\-\-v7-' web/app/globals.css` returns 40+ (all tokens present)
3. `grep 'data-theme' web/app/globals.css` shows updated @custom-variant
4. `grep 'ThemeProvider' web/app/layout.tsx` confirms provider is wired
5. `ls web/public/fonts/*.woff2 | wc -l` returns 3
6. Existing shadcn token values in :root and .dark blocks are byte-identical to before
</verification>

<success_criteria>
- App builds and all existing pages render with no visual changes
- Theme can be toggled between dark and light (verifiable by data-theme attribute on html element)
- V7 CSS tokens are consumable by any component via var(--v7-*)
- GlassCard renders with glassmorphism styling
- Font CSS variables --font-satoshi and --font-cabinet are injected on html element
</success_criteria>

<output>
After completion, create `.planning/phases/48-foundation/48-01-SUMMARY.md`
</output>
