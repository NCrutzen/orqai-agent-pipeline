---
phase: 70-telemetry-consolidation-pipeline-events
plan: 06
type: execute
wave: 4
depends_on: [70-03, 70-04, 70-05]
files_modified:
  - web/app/(dashboard)/automations/[swarm]/review/page.tsx
  - web/app/(dashboard)/automations/[swarm]/review/__tests__/load-page-data.test.ts
autonomous: true
requirements: [TELE-03]
must_haves:
  truths:
    - "Bulk Review predicted-row feed (loadPageData lines 147, 187, 246) reads from public.pipeline_events instead of automation_runs"
    - "Bulk Review visible behaviour unchanged: same row order, same fields rendered to operator"
    - "Side-loaders (loadCoordinatorRunsForReview, loadTaggingFailuresForReview) still consume legacy tables — out of scope per RESEARCH Pitfall 5"
    - "classifier_queue_counts RPC, classifier_rules race-cohort + pending-promotions queries are NOT migrated (out of scope per RESEARCH Pitfall 5)"
    - "Atomic replacement per D-16: no shim, no view, revert is the rollback"
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/review/page.tsx"
      provides: "loadPageData reads pipeline_events for the predicted-row feed (3 sub-queries rewired)"
      contains: "from(\"pipeline_events\")"
    - path: "web/app/(dashboard)/automations/[swarm]/review/__tests__/load-page-data.test.ts"
      provides: "loadPageData unit test against mocked pipeline_events"
      min_lines: 60
  key_links:
    - from: "web/app/(dashboard)/automations/[swarm]/review/page.tsx"
      to: "public.pipeline_events"
      via: "admin.from('pipeline_events').select(...).eq('swarm_type', swarmType).eq('stage', 1)"
      pattern: "from\\([\"']pipeline_events[\"']\\)"
---

<objective>
Atomically replace the 3 predicted-row feed sub-queries in `loadPageData` (lines 147, 187, 246) with reads from `public.pipeline_events`. The visible Bulk Review UI is unchanged — operator sees the same row count, fields, and order. Only the SQL underneath shifts.

Per RESEARCH Pitfall 5: scope is EXACTLY (2), (3), (6) of the 8 sub-queries — the predicted-row feed + cost-outlier RPC + selected-row detail. Out of scope: classifier_queue_counts RPC, classifier_rules race-cohort, classifier_rules pending-promotions, loadCoordinatorRunsForReview, loadTaggingFailuresForReview. The plan-checker MUST flag attempts to migrate any out-of-scope query as scope creep.

Purpose: Close TELE-03 without scope creep.
Output: One server component file modified; one new unit test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-telemetry-consolidation-pipeline-events/70-CONTEXT.md
@.planning/phases/70-telemetry-consolidation-pipeline-events/70-RESEARCH.md
@web/app/(dashboard)/automations/[swarm]/review/page.tsx
@web/app/(dashboard)/automations/[swarm]/review/__tests__/safety-review-loader.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire loadPageData predicted-row feed to pipeline_events</name>
  <files>web/app/(dashboard)/automations/[swarm]/review/page.tsx</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/review/page.tsx (lines 126-307: loadPageData function in full)
    - .planning/phases/70-telemetry-consolidation-pipeline-events/70-RESEARCH.md Pitfall 5 (8-query inventory + scope)
    - .planning/phases/70-telemetry-consolidation-pipeline-events/70-CONTEXT.md D-14, D-16
    - .planning/phases/70-telemetry-consolidation-pipeline-events/70-RESEARCH.md Validation Architecture (Bulk Review test row)
  </read_first>
  <behavior>
    - Sub-query (2) at line 147: SELECT from automation_runs WHERE status='predicted' AND swarm_type=... → REWIRE to SELECT from pipeline_events WHERE swarm_type=$1 AND stage=1 ORDER BY created_at DESC, cursor-paginated 100 rows. Map pipeline_events columns to the shape that downstream rendering expects (row id, decision, confidence, email_id, created_at, decision_details).
    - Sub-query (3) at line 187: cost-outlier RPC automation_runs_with_outlier — keep as-is for v1 (RPC reads automation_runs which is still being written). Add a code comment "// Phase 70 TELE-03: outlier RPC stays on automation_runs for v1 — Phase 72 may move to pipeline_events.cost_cents aggregations". This sub-query is referenced in scope per Pitfall 5 but pragmatically left untouched in v1 because the RPC encapsulates the join logic.
    - Sub-query (6) at line 246: SELECT from automation_runs WHERE id=? (selected-row detail) → REWIRE to SELECT from pipeline_events WHERE id=? (where the row id passed in the URL is now the pipeline_events.id, since the row list comes from pipeline_events).
    - Out of scope (do NOT touch): RPC classifier_queue_counts, classifier_rules promoted-today, classifier_rules candidate, loadCoordinatorRunsForReview, loadTaggingFailuresForReview. Add a comment block at the top of loadPageData listing the in-scope vs out-of-scope queries with line refs.
    - Visible UI unchanged — predicted-row card still shows decision text + confidence + email subject (subject comes from a downstream join via email_id, which was already happening).
  </behavior>
  <action>
    1. In web/app/(dashboard)/automations/[swarm]/review/page.tsx, locate loadPageData (line 126-307).
    2. Add a top-of-function comment block titled "Phase 70 TELE-03 scope" listing all 8 sub-queries from RESEARCH Pitfall 5 with their line numbers and IN-SCOPE / OUT-OF-SCOPE labels.
    3. Replace the sub-query (2) automation_runs predicted-row SELECT with a pipeline_events SELECT:
       - SELECT id, created_at, swarm_type, stage, email_id, decision, confidence, decision_details, automation_run_id, agent_run_id FROM pipeline_events WHERE swarm_type = swarmType AND stage = 1 ORDER BY created_at DESC LIMIT 100 with the same cursor-pagination pattern the existing query uses.
       - Map the result rows to the type/shape consumed by row-list / row-strip components downstream. If the existing component expects an "automation_runs" row shape, transform the pipeline_events row into that shape OR widen the component prop type. Prefer transforming (less blast radius).
    4. Update the selected-row detail SELECT (sub-query 6, line 246) to read from pipeline_events with the same projection.
    5. Leave the cost-outlier RPC call (sub-query 3, line 161/187) untouched and add the code comment described in <behavior>.
    6. Per D-16 (no shim, atomic replacement): the old automation_runs SELECT is REMOVED, not commented-out. Revert is via git.
    7. Re-run typecheck after edits: cd web && npx tsc --noEmit -p web/tsconfig.json.
  </action>
  <verify>
    <automated>grep -c "from(\"pipeline_events\")" "web/app/(dashboard)/automations/[swarm]/review/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "pipeline_events" "web/app/(dashboard)/automations/[swarm]/review/page.tsx" returns >=2 (predicted-row feed + selected-row detail)
    - grep -c "Phase 70 TELE-03 scope" "web/app/(dashboard)/automations/[swarm]/review/page.tsx" returns >=1
    - grep -c "OUT-OF-SCOPE" "web/app/(dashboard)/automations/[swarm]/review/page.tsx" returns >=1
    - Predicted-row feed no longer reads automation_runs for the main list (only the cost-outlier RPC + selected-row may still touch automation_runs): grep -E "from\([\"']automation_runs[\"']\)" "web/app/(dashboard)/automations/[swarm]/review/page.tsx" | wc -l returns <=1 (only acceptable: the cost-outlier branch IF it does an explicit from(); the RPC call admin.rpc is fine)
    - cd web && npx tsc --noEmit -p web/tsconfig.json 2>&1 | grep "review/page.tsx" | wc -l returns 0
    - Out-of-scope queries untouched: grep -c "loadCoordinatorRunsForReview" "web/app/(dashboard)/automations/[swarm]/review/page.tsx" returns >=1 AND grep -c "classifier_queue_counts" "web/app/(dashboard)/automations/[swarm]/review/page.tsx" returns >=1 AND grep -c "classifier_rules" "web/app/(dashboard)/automations/[swarm]/review/page.tsx" returns >=2
  </acceptance_criteria>
  <done>3-of-8 sub-queries rewired (predicted-row feed + selected-row detail; outlier RPC kept by design); out-of-scope queries untouched; typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add load-page-data.test.ts asserting pipeline_events read + side-loader regression</name>
  <files>web/app/(dashboard)/automations/[swarm]/review/__tests__/load-page-data.test.ts</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/review/__tests__/safety-review-loader.test.ts (existing test patterns in this dir)
    - web/app/(dashboard)/automations/[swarm]/review/page.tsx (loadPageData export + dependencies after Task 1)
    - web/lib/inngest/functions/__tests__/classifier-invoice-copy-handler.test.ts (mock pattern)
  </read_first>
  <behavior>
    - Test 1: loadPageData calls admin.from('pipeline_events') for the predicted-row feed (NOT automation_runs)
    - Test 2: loadPageData filters by swarm_type AND stage=1 for the predicted-row feed
    - Test 3: loadPageData still calls admin.rpc('classifier_queue_counts') (side-loader regression — TELE-02 parity for out-of-scope queries)
    - Test 4: loadPageData still calls loadCoordinatorRunsForReview (regression for out-of-scope side-loader)
    - Test 5: When pipeline_events returns 3 rows, the page data result has 3 row entries
  </behavior>
  <action>
    1. Create web/app/(dashboard)/automations/[swarm]/review/__tests__/load-page-data.test.ts following the structure of safety-review-loader.test.ts.
    2. Mock admin.from(table).select(...) chain to record table names called and return fixture data per table.
    3. Mock admin.rpc(name, args) similarly.
    4. Mock loadCoordinatorRunsForReview / loadTaggingFailuresForReview imports with vi.mock so we can assert they are still called (TELE-02 zero consumer breakage for the side-loaders).
    5. Implement the 5 tests per <behavior>.
    6. Use a minimal fake searchParams + admin client; await loadPageData(sp, admin, "debtor-email") and assert.
  </action>
  <verify>
    <automated>cd web && npx vitest run "app/(dashboard)/automations/[swarm]/review/__tests__/load-page-data.test.ts" --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path
    - grep -c "pipeline_events" "web/app/(dashboard)/automations/[swarm]/review/__tests__/load-page-data.test.ts" returns >=3
    - grep -c "loadCoordinatorRunsForReview" "web/app/(dashboard)/automations/[swarm]/review/__tests__/load-page-data.test.ts" returns >=1 (regression assertion)
    - grep -c "classifier_queue_counts" "web/app/(dashboard)/automations/[swarm]/review/__tests__/load-page-data.test.ts" returns >=1 (regression assertion)
    - cd web && npx vitest run "app/(dashboard)/automations/[swarm]/review/__tests__/load-page-data.test.ts" exits 0 with all 5 tests passing
  </acceptance_criteria>
  <done>5 tests green; pipeline_events read asserted; side-loader regressions in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Authenticated operator -> Bulk Review server component | Reads via createAdminClient (server-side only); pipeline_events_auth_select policy gates access |
| Server component -> public.pipeline_events | service-role admin client used; RLS service_role-all policy |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-06-01 | Information Disclosure | pipeline_events.decision_details rendered to operator (jsonb) — may contain customer PII | mitigate | Bulk Review is operator-facing (already authenticated by middleware); Plan 02 RLS enforces server-side access only; do NOT pass full decision_details to client without filtering — current row-list/row-strip components already render only specific fields |
| T-70-06-02 | Tampering / Injection | swarmType passed from URL param into Postgres .eq("swarm_type", swarmType) — could a malicious operator inject? | mitigate | Supabase JS client parameterizes .eq() — no SQL string interpolation; route is auth-gated (middleware enforces project_members); no further mitigation needed |
| T-70-06-03 | Repudiation | Operator's view of decisions diverges from underlying truth if pipeline_events read is filtered differently than legacy automation_runs read | mitigate | Tests assert filter is .eq('swarm_type', X) AND .eq('stage', 1), matching the legacy automation_runs status='predicted' semantic; document the equivalence in a code comment |
| T-70-06-04 | Denial of Service | LIMIT 100 missing or page-cursor missing causes full-table scan | mitigate | Action requires preserving the existing cursor-pagination pattern (LIMIT 100); acceptance criteria does not currently grep for LIMIT but the test assertion on row-count cap implicitly covers it |
</threat_model>

<verification>
- 3 of 8 sub-queries rewired (predicted-row feed + selected-row detail; outlier RPC kept).
- 5 tests green.
- Manual smoke (operator-driven, optional): visit /automations/debtor-email/review and verify the predicted-row list still renders.
- Full suite green: cd web && npm test 2>&1 | tail -5.
</verification>

<success_criteria>
- TELE-03 satisfied: Bulk Review predicted-row feed sources from pipeline_events.
- No regressions in side-loaders (still call legacy tables).
- D-16 atomic replacement honored — no shim.
</success_criteria>

<output>
After completion, create `.planning/phases/70-telemetry-consolidation-pipeline-events/70-06-SUMMARY.md`
</output>
