---
status: testing
phase: 34-40-cross-phase
source: 34-01-SUMMARY.md, 34-02-SUMMARY.md, 34-03-SUMMARY.md, 35-01-SUMMARY.md, 35-02-SUMMARY.md, 35-03-SUMMARY.md, 35-04-SUMMARY.md, 36-01-SUMMARY.md, 36-02-SUMMARY.md, 36-03-SUMMARY.md, 37-01-SUMMARY.md, 37-02-SUMMARY.md, 37-03-SUMMARY.md, 39-01-SUMMARY.md, 39-02-SUMMARY.md, 40-01-SUMMARY.md, 40-02-SUMMARY.md, 40-03-SUMMARY.md, 40-04-SUMMARY.md
started: 2026-03-23T15:00:00Z
updated: 2026-03-23T16:41:15Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

status: COMPLETE
all 26 tests evaluated

## Tests

### 1. Cold Start Smoke Test
expected: Navigate to Vercel deployment URL. Redirected to /login. Login page renders with email/password form.
result: pass
note: Testing via Vercel deployment. Microsoft SSO deferred.

### 2. Email/Password Login
expected: Enter email and password. Redirected to dashboard. Collapsible sidebar visible with nav items. User info in sidebar.
result: pass

### 3. Create a Project
expected: Click create project button. Modal with name/description fields. Submit. Project appears in list.
result: pass

### 4. Project Detail & Invite Member
expected: Click into the newly created project. You see a tabbed layout (Overview | Runs | Swarm Graph | Settings). The Overview tab shows project info and member list with you as sole member. An "Invite" button is available. Clicking it opens a modal with Directory/Email toggle for inviting members.
result: pass
note: Member list shows raw user ID instead of display name/email — UX gap to fix.

### 5. Start a Pipeline Run
expected: From the project detail, go to the Runs tab. Click "New Pipeline Run". A form appears with a use case textarea (with placeholder example), optional run name, and file upload area. Enter a use case description (e.g., "Customer onboarding chatbot") and click Start Pipeline. You're redirected to the run detail page.
result: pass
note: "New Pipeline Run" label is confusing — users are creating an agent swarm from a use case, not starting a "pipeline run". Needs user-facing terminology rework.

### 6. Run Detail - Graph Primary Layout
expected: The run detail page shows a graph-primary layout: the SwarmGraph fills the main viewport area. On the right side, a terminal panel (400px) shows pipeline step entries as cards with status-colored left borders and timestamps. The graph starts populating with agent nodes as the architect stage completes.
result: issue
note: |
  1. Graph layout: PASS — SwarmGraph fills main viewport
  2. Terminal panel: PASS — cards with status borders and timestamps visible
  3. Agent nodes: ISSUE — architect stage shows "Complete" but graph still shows grey placeholders with "Building swarm..." text. Nodes don't populate after architect completes.
  4. UX: ISSUE — user expects interactive back-and-forth with agents during pipeline (like CLI skill does: discussion, confirmations, choices). Currently the pipeline runs silently with no user interaction between stages. This is a significant expectation gap.

### 7. Real-time Pipeline Progress
expected: As the pipeline executes stages (architect, dataset-preparer, etc.), you see live updates WITHOUT page refresh: graph nodes appear and change status (idle -> running -> complete with green borders), terminal entries update in real-time via Broadcast, and the step progress is visible. No 5-second polling flicker.
result: pass
note: Real-time terminal updates work (steps transition complete/running/pending live). Graph nodes still stuck on grey placeholders with "Building swarm..." — same bug as Test 6. Steps counter updates (2/7). No polling flicker.

### 8. HITL Approval Gate
expected: When the pipeline reaches a stage that needs approval (e.g., spec-generator), the pipeline pauses. The graph node for that agent shows an amber pulsing border with PauseCircle icon. A terminal entry appears with "waiting" status (amber left border). An ApprovalPanel renders inline showing the explanation, a diff viewer comparing old vs new content, a comment field, and Approve/Reject buttons.
result: issue
note: Pipeline runs straight through all 7 stages without ever pausing for approval. No stage emits an approval request. The HITL infrastructure (DB schema, Inngest events, UI components) exists but is never triggered. Related to the broader gap: pipeline has no interactive dialogue — unlike the CLI skill which has discussion, confirmations, and user choices throughout.

### 9. Approve/Reject Decision
expected: Click Approve (or Reject) on the approval panel. The decision is applied optimistically (UI updates immediately). A toast notification confirms the action. The pipeline resumes (or stops if rejected). The approval entry in the terminal shows the decision badge (green for approved, red for rejected). The graph node transitions from amber/waiting back to running/complete.
result: skip
note: Cannot test — pipeline never pauses (see Test 8). Blocked by missing approval emission in pipeline stages.

### 10. Pipeline Completion & Celebration
expected: When all pipeline stages complete, the SwarmGraph triggers a confetti celebration animation (dual burst). A brief overlay appears and auto-dismisses. All graph nodes show green "complete" borders. The run status badge shows "complete" in the terminal panel and run list.
result: pass
note: |
  1. Status badge: PASS — shows green "Complete" badge in header and "Pipeline Complete" entry in terminal (8 entries total)
  2. Terminal: PASS — all 7 stages show green Complete badges with durations
  3. Graph nodes: ISSUE — still grey placeholders, never populated. Shows "Waiting for pipeline to start" even after completion. Graph-mapper never received/processed swarm data.
  4. Confetti: triggered but user wants it bolder/more visible
  5. Duration shown (15m 18s) — good for user awareness

### 11. Run List - Live Updates
expected: Navigate to the global Runs page (sidebar -> Runs). You see all runs across projects as RunCards with status badge, step progress, agent count, timestamps. If a pipeline is currently running, updates appear in real-time (status badge changes, progress updates) without page refresh.
result: pass
note: Terminology gap — "Runs" should be "Agent Workforce Creations" or similar user-facing label. Same terminology issue as Test 5.

### 12. Project Swarm Graph Tab
expected: Go back to the project detail page. Click the "Swarm Graph" tab. If the project has a completed run, the graph from the latest successful run renders. If no completed runs, an appropriate empty state message shows.
result: issue
note: |
  Graph renders with green-bordered nodes, BUT the nodes are completely wrong. Shows internal pipeline reference data ("Orchestration Patterns Reference", "Prompt Keys", "Examples", "Swarm Names", "Agent Keys", "Naming Conventions Reference", "Usage Notes", "Mistral", "Meta") instead of the actual agents designed by the pipeline. User asked for 3 poetry-writing agents — graph shows ~10+ reference/metadata artifacts. Graph-mapper is parsing the wrong data from pipeline output (likely reading file/reference metadata instead of actual agent specs from the architect stage).

### 13. Settings - Credential Management
expected: Navigate to Settings (sidebar). You see a tabbed page with Credentials, Auth Profiles, Health, Systems tabs. On the Credentials tab, click "Add Credential". A modal appears with a form including name, auth profile type selector (radio cards with icons), and paste-only secret input fields. The secret fields only accept paste (no typing), showing bullet masks.
result: pass
note: |
  1. Tabs render correctly (Credentials, Auth Profiles, Health, Systems)
  2. Page loads extremely slowly — performance issue
  3. Sidebar has duplicate navigation: Credentials and Health appear both as top-level sidebar items AND as tabs inside Settings. Should be one or the other.
  4. Sidebar still says "Pipeline Manager" — terminology gap (see Tests 5, 11)

### 14. Create & Store a Credential
expected: Select an auth profile type (e.g., API Key). Paste a test value into the secret field. Submit. The credential appears in the credentials list table with a status badge (not-tested). Credential values are NOT visible in the list -- only name, type, and status show.
result: pass

### 15. Health Dashboard
expected: Switch to the Health tab in Settings. You see 3 service health cards: Browserless.io, Supabase Storage, and MCP Adapter. Each shows a status indicator. Click "Run Health Check" button. The check triggers (via Inngest) and results update in real-time via Broadcast -- cards show updated status without page refresh.
result: pass
note: All 3 cards render with status indicators and color-coded borders. Supabase Storage shows red "Unreachable — Bucket not found" — likely needs storage bucket provisioning. Browserless.io and MCP Adapter both green/connected.

### 16. Systems Registry
expected: Switch to the Systems tab in Settings. Click "Add System". A form appears with system name, URL, and integration method selection (radio group with descriptive subtitles for each method). Create a system with "Browser Automation" integration method. The system appears in the systems list with an integration method badge.
result: pass

### 17. Project Credential & System Linking
expected: Go to a project detail page -> Settings tab. You can link/unlink credentials and systems to this specific project. Link the credential and browser-automation system you created. They appear as linked to this project.
result: pass

### 18. Pipeline with Automation Detection
expected: Start a new pipeline run on the project that has a browser-automation system linked. After the spec-generator stage completes, the pipeline should detect the linked browser-automation system and enter the automation sub-pipeline. A terminal entry prompts you to upload/paste an SOP (Standard Operating Procedure).
result: skip
note: Automation detection depends on updated orqai-agent-pipeline repo integration (Phase 43). Skip until upstream sync is complete.

### 19. SOP Upload & Preview
expected: In the SOP upload prompt, paste or type SOP content (markdown text describing steps). A markdown preview renders below using react-markdown with formatted headings, lists, etc. Click "Looks good" to confirm. The pipeline proceeds and prompts for screenshot upload.
result: skip
note: Depends on automation sub-pipeline (Phase 43 upstream sync).

### 20. Screenshot Upload & Vision Analysis
expected: A screenshot upload area appears in the terminal. Drag-and-drop or click to upload a screenshot. The image is resized client-side (max 1568px) and uploaded to Supabase Storage via signed URL. The pipeline sends the SOP + screenshot to Orq.ai vision for analysis.
result: skip
note: Depends on automation sub-pipeline (Phase 43 upstream sync).

### 21. Annotation Review Overlay
expected: After vision analysis completes, a terminal entry appears with a "Review Steps" button. Clicking it opens a full-width Dialog overlay (90vw x 85vh) with a 40/60 split: left side shows SOP steps as cards, right side shows the annotated screenshot with CSS overlay highlights (numbered bounding boxes). Each step card has confirm (checkmark) and edit (pencil) buttons.
result: skip
note: Depends on automation sub-pipeline (Phase 43 upstream sync).

### 22. Annotation Edit & Re-analysis
expected: Click the edit (pencil) button on a step card. An inline text field appears for editing the step description. Make a change and save. Click "Re-analyze" to send corrections to Orq.ai. Changed steps are detected and their confirmations reset. Updated annotations appear on the screenshot.
result: skip
note: Depends on automation sub-pipeline (Phase 43 upstream sync).

### 23. Confirm Annotations & Resume Pipeline
expected: Confirm all steps (each gets a green checkmark and green-highlighted bounding box). Click "Finalize" or confirm all. The pipeline resumes via Inngest event (automation/annotation.confirmed). The overlay closes and the pipeline continues to completion.
result: skip
note: Depends on automation sub-pipeline (Phase 43 upstream sync).

### 24. Failed Pipeline - Retry
expected: If a pipeline run failed (or simulate by stopping Inngest), navigate to the failed run detail. The failed step shows a red status badge and error message. A "Retry" button is available. Clicking retry resets the failed step and all subsequent steps, then re-triggers the pipeline from the failed step.
result: skip
note: No failed runs available to test. Retry flow untested.

### 25. Approval Email Notification
expected: When a pipeline hits an approval gate, an email is sent to team members via Resend. The email contains a deep-link URL. Clicking the link opens the run detail page with the approval panel focused (via ?approval= query param).
result: skip
note: Blocked — HITL approval never triggers (see Test 8).

### 26. Credential Failure Detection
expected: If a pipeline stage fails with an auth-related error (e.g., invalid API key), the credential failure detection system flags the credential as needs_rotation. The credential status badge changes to "needs_rotation" in the credentials list. A failure notification email is sent via Resend.
result: skip
note: No failed runs to trigger credential failure detection.

## Summary

total: 26
passed: 13
issues: 3
pending: 0
skipped: 10
skipped: 2
skipped: 10

## Gaps

- **UX: Member list shows user ID** — Project overview member list displays raw UUID instead of display name or email. Needs profile lookup or join query.
- **UX: "New Pipeline Run" terminology** — Users are creating an agent swarm from a use case description, not running a pipeline. Button/page labels should reflect user intent (e.g., "Create Agent Swarm").
- **BUG: Agent nodes don't populate after architect completes** — Graph shows grey placeholders with "Building swarm..." even after architect stage is marked Complete. Graph-mapper may not be receiving/processing the architect output correctly.
- **UX: No interactive agent dialogue during pipeline** — CLI skill has rich back-and-forth (discussion step, confirmations, choices). Web UI runs silently with no user interaction between stages. Users expect to engage with the agents, not just watch status cards. This is a significant design gap — the pipeline needs conversational interaction points beyond just HITL approval gates.
- **BUG: HITL approval never triggers** — Pipeline runs all 7 stages straight through without pausing. No stage emits an approval request. The approval infrastructure (DB, Inngest events, UI components) is built but never wired into the pipeline execution. Tests 8-9 blocked.
- **BUG: Graph shows "Waiting for pipeline to start" after completion** — Even after all 7 stages complete, graph area shows placeholder boxes and "Waiting for pipeline to start" text. Graph-mapper never receives swarm architecture data from the pipeline. This persists through the entire run lifecycle (running → complete).
- **UX: Confetti celebration too subtle** — User wants bolder/more visible celebration on pipeline completion.
- **BUG: Graph-mapper shows wrong nodes** — Swarm Graph tab renders internal pipeline reference data (Orchestration Patterns Reference, Prompt Keys, Examples, Swarm Names, Agent Keys, etc.) instead of actual designed agents. Graph-mapper is parsing the wrong data from pipeline output — likely reading file/reference metadata instead of agent specs from the architect stage. User asked for 3 agents, got ~10+ reference artifacts.
- **PERF: Settings page loads extremely slowly** — Noticeable delay when navigating to Settings.
- **UX: Duplicate nav for Credentials and Health** — Both appear as top-level sidebar items AND as tabs inside Settings. Should consolidate to one location.
- **UX: Sidebar title "Pipeline Manager"** — Should match the product identity (Agent Workforce). Part of broader terminology cleanup.
