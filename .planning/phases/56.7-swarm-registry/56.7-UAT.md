---
status: complete
phase: 56.7-swarm-registry
source:
  - 56.7-01-SUMMARY.md
  - 56.7-02-SUMMARY.md
  - 56.7-03-SUMMARY.md
started: 2026-04-29T12:37:02Z
updated: 2026-04-29T13:11:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: |
  Fresh `next build` succeeds, `tsc --noEmit` clean, migration
  applied with 7 swarm_categories rows for debtor-email.
result: pass
notes: |
  - `tsc --noEmit` exit 0
  - `next build` exit 0; route table shows `ƒ /automations/[swarm]/review`
    and no `debtor-email-review` route
  - Supabase REST (service role): `swarms` has 1 debtor-email row
    with full `ui_config` (tree_levels, drawer_fields, row_columns)
  - `swarm_categories` debtor-email returns 7 rows ordered by
    display_order: payment(10), payment_admittance(15), auto_reply(20),
    ooo_temporary(30), ooo_permanent(40), invoice_copy_request(50),
    unknown(60). `unknown` has action=reject + outlook_label=null;
    all others have action=categorize_archive
  - Anon-key query returns [] — RLS posture (service-role-all +
    authenticated-select) working as designed

### 2. Old debtor-email-review URL redirects
expected: |
  /automations/debtor-email-review → 307 →
  /automations/debtor-email/review; subpaths forwarded.
result: pass
notes: |
  - `/automations/debtor-email-review` → HTTP 307,
    Location: `/automations/debtor-email/review`
  - `/automations/debtor-email-review/foo` → HTTP 307,
    Location: `/automations/debtor-email/review/foo`
  - Verified against `next start` on port 3055 (production build)

### 3. New [swarm]/review page is registered
expected: |
  /automations/debtor-email/review resolves through middleware to
  the registry-driven page handler.
result: pass
notes: |
  - Route appears in `next build` output as `ƒ /automations/[swarm]/review`
  - Unauthenticated request returns 307 to /login?next=/automations/debtor-email/review,
    confirming the dynamic segment resolves and the auth middleware engages
  - Visual queue rendering not verifiable from CLI (requires authenticated
    session) — covered structurally by the 77/77 vitest pass in 56.7-03

### 4. Verdict actions dispatch correctly via registry
expected: |
  Categorize/reject/manual_review actions fire registry-driven
  side-effects through classifier-verdict-worker; iController-delete
  still gated on swarm_type=debtor-email (D-12).
result: pass
notes: |
  Live dispatch chain verified 2026-04-29 13:08 by approving the
  "Automatisch antwoord: Factuur 17338318 PO 4503009670" row
  (message_id …f4SsQwAA, predicted ooo_temporary, overridden to
  payment_admittance). Three downstream automation_runs rows
  prove the registry-driven dispatch fired:

  1. categorize+archive run (e4b499d5) status=completed,
     applied_category="Payment Admittance" — that string is the
     outlook_label column from the swarm_categories registry row,
     proving the action lookup hit the DB and not a constant
  2. debtor-email-cleanup / icontroller_delete run (0435f59f)
     triggered_by="classifier-verdict-worker", source_automation_run_id
     points back to the original predicted row — proves the worker
     fired the D-12-gated cleanup dispatch
  3. Follow-up icontroller_delete review-side run (f2f67a43)
     status=pending — cleanup queue picked it up

  Override flow also verified: predicted topic=ooo_temporary in the
  original row, but the categorize+archive run shows
  topic=payment_admittance and applied_category="Payment Admittance"
  — registry-driven override path (Pitfall 5) working end-to-end.

### 5. Override category dropdown matches registry
expected: |
  Detail-pane override-category dropdown populated from
  swarm_categories (entries for debtor-email, ordered by
  display_order); no leftover OVERRIDE_CATEGORIES const.
result: pass
notes: |
  Verified visually via authenticated screenshot 2026-04-29 15:02.
  Dropdown shows 6 entries in exact display_order:
  Payment(10), Payment Admittance(15), Auto-reply(20),
  OOO (temporary)(30), OOO (permanent)(40), Invoice copy request(50).
  Labels = outlook_label column; `unknown` (action=reject) correctly
  omitted from override choices.

  Bonus confirmations from same screenshot:
  - Page title "Debtor Email — Bulk Review" = swarms.display_name
    resolved via loadSwarm('debtor-email') from [swarm] segment
  - Queue-by-topic tree built from ui_config.tree_levels
    (unknown=49, ooo temporary=1)
  - Detail-pane field labels match ui_config.drawer_fields
  - Subtitle is action-aware ("categorize+archive, downstream
    cleanup, or swarm dispatch") — matches the four SwarmAction
    branches from 56.7-02

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — both blocked items are environmental, not code issues]
