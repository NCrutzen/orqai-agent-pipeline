# Phase 56.7 — Plan Verification

**Date:** 2026-04-29
**Status:** PASS WITH WARNINGS
**Plans verified:** 3 (Wave 1 migration+loader, Wave 2 verdict-worker, Wave 3 queue UI + redirect + 60-05 amendment)

## Goal-Backward Result

Completing all 3 waves DOES achieve the phase boundary in CONTEXT.md:
- Adding new swarm = `INSERT swarms` + `INSERT swarm_categories` + (optional) Inngest dispatch worker. **Verified by Wave 3 Verification step 3** (Sales-swarm dry-run inserts a row, page renders without code change).
- No new route required: dynamic `[swarm]/review` segment delivered Wave 3 Task 1.
- No new component required: existing components generalized in-place via `ui_config` jsonb.
- No Vercel deploy for routing: `loadSwarm` 60s TTL + DB read covers it.

## Decision Coverage (D-00 .. D-17)

| Decision | Wave | Task | Status |
|----------|------|------|--------|
| D-00 schema | 1 | T1 | Covered (DDL block lines 416-448 of RESEARCH copied verbatim) |
| D-01 action CHECK enum | 1 | T1 | Covered (CHECK in DDL + literal union in types.ts) |
| D-02 swarm_dispatch event name | 1+2 | T1+T2 | Covered (column seeded null for Outlook actions; worker fires inngest.send when set) |
| D-03 ui_config jsonb shape | 1 | T1 | Covered (default jsonb_build_object in DDL) |
| D-04 RLS service+authenticated | 1 | T1 | Covered (4 policies in DDL) |
| D-05 seed debtor-email + 6 categories | 1 | T1 | Covered + extended to 7 rows (payment_admittance alias per Q4) |
| D-06 60s in-memory cache | 1 | T2 | Covered (mirrors web/lib/classifier/cache.ts) |
| D-07 [swarm]/review route + redirect | 3 | T1+T2 | Covered |
| D-08 ui_config.tree_levels | 3 | T1 Step D | Covered |
| D-09 counts RPC unchanged | 3 | T1 Step B | Covered (threads swarmType into p_swarm_type) |
| D-10 worker switch on action | 2 | T2 | Covered (4 branches + exhaustive default) |
| D-11 outlook_label nullable skip | 2 | T2 | Covered (if guard wraps both categorize+archive) |
| D-12 side_effects deferred | 2 | T2 | Covered (iController gated on swarm_type==='debtor-email') |
| D-13 INSERT-only swarm onboarding | 3 | T1 | Covered (validated by Wave 3 Verification step 3) |
| D-14 classifier-rules already cross-swarm | — | — | Pre-existing; no action needed |
| D-15 ship 56.7 BEFORE queue rewrite | 3 | T4 | **WARNING — see below** |
| D-16 swarm_type canonical, no FK | 1 | T1 | Covered (no FK in DDL per explicit instruction) |
| D-17 one seeded swarm | 1 | T1 | Covered (debtor-email only) |

**100% decision coverage.** No locked decision is dropped. No deferred ideas (declarative side_effects, FK enforcement, Realtime invalidation, admin UI, Sales swarm itself) are introduced.

## Wave Dependency Order

Correct: Wave 1 (depends_on=[]) → Wave 2 (depends_on=[56.7-01]) → Wave 3 (depends_on=[56.7-01, 56.7-02]). Wave 2 imports `loadSwarmCategories` from Wave 1; Wave 3 imports both loaders and consumes the seeded rows.

## Material Concerns

### WARNING 1 — D-15 amendment target is already-executed code (Wave 3 Task 4)

`60-05-PLAN.md` and `61-01..03-PLAN.md` have **SUMMARY.md files present**, meaning they are already shipped. The current `web/app/(dashboard)/automations/debtor-email-review/` directory contains the 60-05/61 outputs (page.tsx, queue-tree.tsx, detail-pane.tsx etc., last modified 2026-04-29).

Implication: D-15's instruction to "ship 56.7 BEFORE the queue rewrite" is **moot** — the queue rewrite shipped first. The amendment Wave 3 Task 4 performs is **planning-document hygiene only**, not a real dependency that affects execution.

Additionally: Wave 3 Task 4 Step B prescribes replacing `OVERRIDE_CATEGORIES` references in 60-05's body. **Grep confirms 60-05-PLAN.md contains zero `OVERRIDE_CATEGORIES` references** — Task 4 Step B is a no-op for that symbol. The path-rename portion of Step A/C is the only meaningful change.

**Severity: WARNING.** Execution will not break, but the planner should know the amendment is documentation-only. Consider downscoping Task 4 to "frontmatter `depends_on` + `files_modified` path renames only" and noting that the queue rewrite already consumed pre-registry constants (which Wave 3 Task 1 supersedes by deleting `categories.ts` and rewriting actions.ts/page.tsx in the new directory).

### WARNING 2 — Atomic delete of `debtor-email-review/` (Wave 3 Task 1 Step G)

Risk flagged in the prompt. Assessment:
- `next.config.ts` redirects (Wave 3 Task 2) are **edge-level 307s** in Next 15 — they fire BEFORE any route matching, so even if the new dynamic route hadn't deployed yet, old bookmarks would 307 to a 404 (transient).
- The redirect and dynamic route ship in the **same commit/deploy** per Task 1 Step G + Task 2 sequencing — atomic at the deploy level.
- `web/.next/types/routes.d.ts` shows the old route was previously typed; a stale build artifact. Will rebuild on deploy.

**Risk acceptable.** No execution change required. Recommend deploying Wave 3's Task 1 + Task 2 commits together (don't split).

### WARNING 3 — Pre-existing test imports point at debtor-email-review/

`web/tests/queue/{page,rule-filter,race-cohort,keyboard-shortcuts}.test.tsx` import from `@/app/(dashboard)/automations/debtor-email-review/...` (5+ test files). Wave 3 Task 1 Step G deletes that directory but Task 3 only edits `page.test.tsx` and `actions.test.ts`. The other test files will fail to import.

**Severity: WARNING (could become BLOCKER at execution time).** Wave 3 Task 1 Step H (link search-replace) covers `*.ts/*.tsx` source but the verify glob excludes `web/.next/` correctly — it WILL still catch test imports. However, Task 1's Done criteria say "zero literals remain" which would force the executor to fix all test imports inline. Task 3 should explicitly enumerate the additional test files needing import path updates: `keyboard-shortcuts.test.tsx`, `rule-filter.test.tsx`, `race-cohort.test.tsx`. Otherwise Task 1's grep-zero check will fail and the executor will scramble.

**Fix recommendation:** Add to Wave 3 Task 1 Step H: "Also update import paths in `web/tests/queue/*.test.tsx` from `debtor-email-review/` to `[swarm]/review/`."

### INFO — payment_admittance alias preservation (Wave 2)

Verified: current worker (`web/lib/inngest/functions/classifier-verdict-worker.ts`) maps both `payment` and `payment_admittance` to `'Payment Admittance'`. Wave 1 seeds 7 rows including `payment_admittance` (display_order=15). Wave 2 worker switch removes `CATEGORY_LABEL` and looks up by `category_key`. Behavior preserved: predicted_category='payment_admittance' resolves to the alias row directly (no normalization), outlook_label='Payment Admittance' applied. Test 9 in Wave 2 covers this. **No concern.**

## Stack Rule Audit (CLAUDE.md)

- ✅ Migration via Supabase migration file: `20260429b_swarm_registry.sql`.
- ✅ No `*/N` JSDoc patterns — Wave 2 worker is event-triggered, no cron strings.
- ✅ 60s in-memory cache pattern matches `web/lib/classifier/cache.ts`.
- ✅ RLS posture: service-role full + authenticated SELECT.
- ✅ No new env vars required; service-role key already in Vercel.
- ✅ Inngest `step.run` granularity preserved in worker rewrite.
- ✅ No direct LLM API calls (N/A for this phase).

## Out-of-Scope Items — Not Crept In

- ❌ Sales swarm provisioning: confirmed deferred (D-17), only debtor-email seeded.
- ❌ Declarative side_effects beyond Outlook: column reserved (D-12), not consumed; iController stays gated on `swarm_type==='debtor-email'`.
- ❌ FK enforcement: explicitly NOT added (D-16).
- ❌ Realtime invalidation: only TTL Map (D-06 Discretion).
- ❌ Admin UI for swarm management: not introduced.

## Scope Sanity

| Plan | Tasks | Files | Wave | Within budget? |
|------|-------|-------|------|----------------|
| 56.7-01 | 2 | 4 | 1 | Yes |
| 56.7-02 | 2 | 2 | 2 | Yes |
| 56.7-03 | 4 | 13 | 3 | Borderline — 13 file mods + plan amendments. Scoped via "move + tweak" not "rewrite", so realistic. |

Wave 3 is the heavy plan but the per-file genericization is mostly mechanical (search-replace + prop threading). Acceptable.

## Verdict

**PASS** with three warnings:
1. D-15 amendment is documentation-only (queue rewrite already shipped) — downscope Task 4 expectations.
2. Atomic Wave 3 Task 1 Step G + Task 2 deploy required (no separate commits).
3. Wave 3 Task 1 Step H must enumerate the 4 additional test files needing import path fixes (keyboard-shortcuts.test.tsx, rule-filter.test.tsx, race-cohort.test.tsx — and verify page.test.tsx/actions.test.tsx covered by Task 3).

No blockers. Phase boundary is achievable on first execution if the warnings are folded in.
