---
phase: 999.8-stage-1-llm-confidence-gate-and-predictor-attribution
plan: 07
name: Wave 5 — Stage 1 chip-strip filter chips (predictor + confidence) + loadPageData filters
type: tdd
wave: 5
depends_on: [01, 02]
files_modified:
  - web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/predictor-confidence-chip-strip.tsx
autonomous: true
gap_closure: false
requirements: [REQ-CHIP-05]
must_haves:
  truths:
    - "/stage-1?predictor=llm_2nd_pass filters the row list to only LLM-predicted rows"
    - "/stage-1?predictor=regex filters to only regex-predicted rows"
    - "/stage-1?confidence=medium filters to LLM medium-conf rows"
    - "/stage-1 (no params) shows everything status=predicted (D-06 default)"
    - "Filter chips render below the existing noise-category chip-strip as a sibling component (per Phase 81 anti-pattern lock: duplicate styling, do NOT extract generic ChipStrip)"
    - "Predictor filter shape is denormalized (D-11) — reads decision_details->>predictor on pipeline_events, NO JOIN to agent_runs at query time"
    - "This plan depends on Phase 81 being merged on main — confirm before starting"
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/predictor-confidence-chip-strip.tsx"
      provides: "New chip-strip sibling component for predictor + confidence filters (D-05)"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx"
      provides: "PageSearchParams extended with predictor + confidence; loadPageData filter logic; chip-strip mounted in JSX"
  key_links:
    - from: "predictor chip click"
      to: "URL ?predictor= param"
      via: "router.push(qs)"
      pattern: "?predictor=(regex|llm_2nd_pass)"
    - from: "loadPageData with ?predictor=X"
      to: "pipeline_events.decision_details JSON path"
      via: ".eq('decision_details->>predictor', X)"
      pattern: "decision_details->>predictor"
---

<objective>
Add two filter chip rows to the `/stage-1` shell (Phase 81) so operators can slice by `predictor` and `confidence`. This is the UI half of D-05 + D-06.

Filter chips sit BELOW the existing noise-category chip strip as a second sibling chip strip (per Phase 81 D-07 lock: "do NOT extract a generic ChipStrip — duplicate the styling, keep types per surface"). URL state is `?predictor=` and `?confidence=`. Default view shows everything (D-06).

The filter shape is denormalized per D-11: the chip filter reads `pipeline_events.decision_details->>predictor` (populated by Plan 02 Task 3). NO JOIN to `agent_runs` from `loadPageData`.

**CRITICAL DEPENDENCY:** This plan depends on Phase 81 being merged. Phase 81 is code-shipped on local main (4 plans with SUMMARY files) but if it gets reverted before this plan executes, the `/stage-1/` directory + chip-strip extension points won't exist. Verify on `main` before starting.

Output: One new chip-strip component + extensions to `page.tsx`'s `PageSearchParams` interface, `loadPageData` filter, and JSX mount point.

Implements D-05, D-06, D-11. Honors Plan 01 RED tests for `load-page-data.predictor`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/999.8-stage-1-llm-confidence-gate-and-predictor-attribution/999.8-CONTEXT.md
@.planning/phases/999.8-stage-1-llm-confidence-gate-and-predictor-attribution/999.8-RESEARCH.md
@.planning/phases/999.8-stage-1-llm-confidence-gate-and-predictor-attribution/999.8-PATTERNS.md
@.planning/phases/999.8-stage-1-llm-confidence-gate-and-predictor-attribution/999.8-01-SUMMARY.md
@.planning/phases/999.8-stage-1-llm-confidence-gate-and-predictor-attribution/999.8-02-SUMMARY.md
@.planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-CONTEXT.md
@web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/noise-category-chip-strip.tsx

<interfaces>
Phase 81 chip-strip pattern (analog at `noise-category-chip-strip.tsx`):
- Server-rendered chip array, client `onClick` writes URL via `router.push`
- URL writer uses `useSearchParams()` + `URLSearchParams.set()` / `.delete()`
- "All" chip deletes the param; specific chip writes it

Existing `loadPageData` filter pattern (per page.tsx:478-503 — already filters on `decision_details->>entity` and `decision_details->>mailbox_id` via JOIN-back to pipeline_events):
```typescript
.eq("decision_details->>predictor", params.predictor)
.eq("decision_details->>llm_confidence", params.confidence)
```

D-11 denormalization source: Plan 02 Task 3 added `predictor: llmInvoked ? "llm_2nd_pass" : "regex"` to `decision_details` at `emit-pipeline-event`. So new rows have the field; historical rows don't. The chip filter naturally excludes historical rows for which `decision_details->>predictor IS NULL`, which is fine — historical rows are typically already verdicted and not in the predicted-row list anyway.

Anti-pattern (verbatim from `noise-category-chip-strip.tsx:14-17`):
> Do NOT extract a generic ChipStrip — duplicate the styling, keep types per surface.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PredictorConfidenceChipStrip component + URL writers</name>
  <files>web/app/(dashboard)/automations/[swarm]/stage-1/predictor-confidence-chip-strip.tsx</files>
  <behavior>
    - Renders two chip rows: predictor (All / regex / llm) and confidence (All / high / medium / low)
    - Each chip click writes the corresponding `?predictor=` or `?confidence=` URL param
    - "All" click deletes the param
    - Active-state derivation reads `useSearchParams()` and highlights the matching chip
    - Styling: copy the existing `noise-category-chip-strip.tsx` visual idiom (don't extract a shared component per anti-pattern lock)
    - Per-D-11: this is a UI component only; the actual filtering happens server-side in `loadPageData`
    - Per-D-06: chips are unset by default; no chip is active when URL has no params
    - Empty-strip behaviour mirrors Phase 81 ("All" chip always present even when no specific options exist)
  </behavior>
  <action>
    Create `web/app/(dashboard)/automations/[swarm]/stage-1/predictor-confidence-chip-strip.tsx` by copying the noise-category chip-strip's visual + URL-writer structure. Adapt to:

    1. Two horizontal chip rows stacked vertically (predictor on top, confidence below), or one row with grouped chips — planner's call. Recommendation: two short rows so the layout stays compact and operators see what each axis controls.

    2. Predictor chips (3): "All", "regex", "LLM"
       - "All" deletes `?predictor=`
       - "regex" writes `?predictor=regex`
       - "LLM" writes `?predictor=llm_2nd_pass`

    3. Confidence chips (4): "All", "high", "medium", "low"
       - "All" deletes `?confidence=`
       - others write `?confidence=high|medium|low` literally

    4. Both filters can be active simultaneously (e.g. `?predictor=llm_2nd_pass&confidence=medium`).

    5. The component is a client component (`"use client"`) since it uses `useRouter` / `useSearchParams` / `usePathname`.

    6. Visual styling: gap, chip border-radius, active-state token must match the noise-category chip strip exactly. Per Phase 81 D-07 / CONTEXT.md "Claude's Discretion": ride v7.css tokens, no new tokens.

    Anti-pattern reminder (from noise-category-chip-strip.tsx:14-17): keep types per surface. The chip data type for predictor/confidence is INLINE, not shared with the noise-category strip.
  </action>
  <verify>
    <automated>cd web && npm run test -- predictor-confidence-chip-strip 2>&1 || echo "(component may not have a dedicated test; falls back to RTL render in load-page-data tests)"</automated>
  </verify>
  <done>Component renders. Click writes URL. Active state matches URL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend PageSearchParams + loadPageData filter + mount chip strip in page.tsx</name>
  <files>web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx</files>
  <behavior>
    - `PageSearchParams` interface adds `predictor?: string` and `confidence?: string`
    - `loadPageData` validates params server-side: predictor ∈ {regex, llm_2nd_pass}, confidence ∈ {high, medium, low}; invalid values are silently ignored (no 400)
    - The predicted-row query (per Phase 81's `loadPageData` JOIN-back pattern at lines ~478-503) adds:
      - `.eq("decision_details->>predictor", params.predictor)` when set
      - `.eq("decision_details->>llm_confidence", params.confidence)` when set
    - The Pending Promotion sub-view (`?sub=pending`) is unaffected — predictor/confidence chips only filter the predicted-row list, not the candidate-rule list
    - JSX mounts `<PredictorConfidenceChipStrip />` directly below `<NoiseCategoryChipStrip />` in the `/stage-1` page render
  </behavior>
  <action>
    1. Edit the `PageSearchParams` interface in page.tsx to add the two optional fields per `<interfaces>` above.

    2. In `loadPageData`, add server-side validation:
       ```typescript
       const validatedPredictor =
         params.predictor === "regex" || params.predictor === "llm_2nd_pass"
           ? params.predictor
           : undefined;
       const validatedConfidence =
         params.confidence === "high" || params.confidence === "medium" || params.confidence === "low"
           ? params.confidence
           : undefined;
       ```

    3. Apply the filters to the existing predicted-row query (mirror the existing `.eq("decision_details->>entity", ...)` / `.eq("decision_details->>mailbox_id", ...)` pattern at page.tsx:478-503):
       ```typescript
       if (validatedPredictor) {
         q = q.eq("decision_details->>predictor", validatedPredictor);
       }
       if (validatedConfidence) {
         q = q.eq("decision_details->>llm_confidence", validatedConfidence);
       }
       ```

    4. Mount the new chip strip in the page JSX. Find the existing render of `<NoiseCategoryChipStrip>` in page.tsx and render `<PredictorConfidenceChipStrip>` directly below it (sibling, not wrapped).

    5. The `?sub=pending` branch in `loadPageData` is NOT affected — the candidate-rule list does not gain predictor/confidence filters (those are predicted-row concepts, not rule concepts).

    Run Plan 01 load-page-data tests — all predictor/confidence assertions should GREEN.
  </action>
  <verify>
    <automated>cd web && npm run test -- load-page-data.predictor</automated>
  </verify>
  <done>All Plan 01 load-page-data.predictor tests GREEN. Visiting `/automations/debtor-email/stage-1?predictor=llm_2nd_pass` filters to LLM-only rows. No regression.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual + interaction check in dev</name>
  <what-built>
    Two new chip strips render below the noise-category strip. URL params drive filtering. Operators can slice the Stage 1 row list by predictor and confidence.
  </what-built>
  <how-to-verify>
    1. `cd web && npm run dev` and visit `/automations/debtor-email/stage-1` in a dev session.
    2. Confirm both chip strips render below the noise-category strip with consistent styling.
    3. Click "LLM" in the predictor strip → URL becomes `?predictor=llm_2nd_pass` and the row list narrows.
    4. Click "medium" in the confidence strip → URL adds `&confidence=medium` and the list narrows further.
    5. Click "All" in either strip → corresponding param is removed.
    6. Confirm the Therese Hendriks reference row (if any predicted rows exist) is visible under `?predictor=llm_2nd_pass&confidence=medium`.
    7. Visit `/automations/debtor-email/stage-1?sub=pending` → predictor/confidence chips do not affect the candidate-rule list.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/interaction issues.</resume-signal>
</task>

</tasks>

<verification>
- All Plan 01 load-page-data.predictor tests GREEN.
- Dev visual check approved.
- Cross-swarm visit (e.g. `/automations/sales-email/stage-1`) — chip strip renders and filters work even with empty predictor data (since the underlying read shape is unchanged).
- `?sub=pending` branch unaffected.
</verification>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| URL param → loadPageData filter | server-side validated against enum allowlist |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9998-13 | Tampering | predictor/confidence URL params | mitigate | Server-side enum allowlist validation; invalid values are silently ignored (no SQL injection vector via Supabase's `.eq()` API) |
| T-9998-14 | Information Disclosure | filter exposes which emails the LLM was uncertain about | accept | Same access scope as the rest of /stage-1; if operator can see the page they can already see all predicted rows |
</threat_model>

<success_criteria>
- Chip strips render and filter correctly
- URL state survives refresh
- Dev visual check approved
- No regression in Phase 81 surfaces (chip-strip, row list, detail pane, ?sub=pending)
</success_criteria>

<output>
Create `.planning/phases/999.8-stage-1-llm-confidence-gate-and-predictor-attribution/999.8-07-SUMMARY.md` with:
- Screenshots / GIF of the new chip strips in action
- The final chip layout decision (two rows vs one)
- Confirmation that the `?sub=pending` flow is unaffected
- Test results
</output>
