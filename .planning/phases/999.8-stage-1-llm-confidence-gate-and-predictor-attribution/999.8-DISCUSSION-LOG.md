# Phase 999.8: Stage 1 LLM confidence gate + predictor attribution — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-11
**Phase:** 999.8-stage-1-llm-confidence-gate-and-predictor-attribution
**Areas discussed:** Confidence gate scope, Low-confidence routing, Predictor attribution model, Backfill strategy

---

## Confidence gate scope

### Q1: How should the confidence threshold be expressed for the LLM 2nd-pass `categorize_archive` action?

| Option | Description | Selected |
|--------|-------------|----------|
| Uniform `high`-only | Hard rule across all swarm_noise_categories: only `llm_confidence === 'high'` triggers categorize_archive. medium/low always route to the low-confidence surface. | ✓ |
| Per-category `min_confidence` column | Add `min_llm_confidence` column to swarm_noise_categories (default 'high', overridable per row). | |
| Score gate not bucket gate | Bypass the low/medium/high enum entirely. Require LLM to return top-2 ranked list + numeric margin. | |

**User's choice:** Uniform `high`-only, with the operator constraint that "high should be really high confidence like 95%"
**Notes:** Operator added a calibration constraint on top of the code-level uniform gate. Code gate enforces the boundary; the Orq agent prompt must be re-tuned so "high" means ~95%+ certainty.

### Q2: When a human later marks a `high`-confidence LLM prediction as wrong, should the system surface that as a calibration-drift signal?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — alerting threshold | Track high-conf FP rate separately. Above threshold (e.g. >2% over 50 high-conf predictions per mailbox) emit Inngest event / dashboard flag. | ✓ |
| Telemetry only, no alert | Record high-conf FPs in the same Wilson-CI stream as everything else. | |
| Defer to a later phase | Out of scope for 999.8. | |

**User's choice:** Yes — alerting threshold (recommended)
**Notes:** Concrete threshold values and emission mechanism (Inngest event vs dashboard flag) left to planning. Principle locked.

---

## Low-confidence routing

### Q3: When the LLM 2nd-pass returns `medium` or `low`, where should the prediction land for human review?

(Initial framing — superseded after user flagged Phase 81 shell work in flight.)

| Option | Description | Selected |
|--------|-------------|----------|
| Stage 1 Bulk Review with discriminator | Reuse Stage 1 Bulk Review, add predictor column/chip + low_confidence filter. | (superseded) |
| New dedicated surface | Separate route + queue + triage habit. | |
| Reuse escalate-to-Kanban pattern | Insert automation_runs row mirroring Stage 0 safety escalation. | |

**User's response:** "Before you implement item three, check the code we are building because I think we are rebuilding the whole Kanban review style with a review pane for every stage."
**Notes:** Operator surfaced active Phase 81 work — fold Stage 1 into the stage-keyed shell with chip-strip + row list + detail pane per Sketch 005. Routing question re-framed in the new shell's vocabulary.

### Q3-rephrased: Inside the new `/stage-1` shell (Phase 81), where do `medium`/`low` LLM predictions land?

| Option | Description | Selected |
|--------|-------------|----------|
| New sub-view `?sub=low-confidence` | Mirror Pending Promotion sub-view pattern from Phase 81 D-09..D-11. | |
| Predictor + confidence filters on main row list | Same Stage 1 row list. Add chip-strip filters for `predictor` and `confidence`. | ✓ |
| Predictor-aware view of same row list, gated by status | No new sub-view; medium/low keep `status=predicted` and appear in default row list under their noise-category chip. | |

**User's choice:** Predictor + confidence filters on main row list
**Notes:** Single surface, no new sub-view, leverages the chip-strip Phase 81 is already building.

### Q4: What's the default filter state when an operator lands on `/stage-1` with no query params?

| Option | Description | Selected |
|--------|-------------|----------|
| Show everything `status=predicted` | Default = no predictor/confidence filter applied. | ✓ |
| Hide regex shadow-mode rows by default | Default filter = `predictor=llm OR (predictor=regex AND not_in_shadow)`. | |
| Default to highest-leverage (LLM medium-conf only) | Default filter = `predictor=llm, confidence=medium`. | |

**User's choice:** Show everything `status=predicted`
**Notes:** Filters are opt-in for slicing. Matches today's Bulk Review mental model: "here's the inbox."

---

## Predictor attribution model

### Q5: How granular should predictor attribution be on the verdict row and in the feedback-aggregation math?

| Option | Description | Selected |
|--------|-------------|----------|
| Two streams: `regex` vs `llm_2nd_pass` | `agent_runs.predictor text` carries one of two values. Wilson-CI groups on `(mailbox, predictor)`. | ✓ |
| N streams: per-regex-rule + `llm_2nd_pass` | `predictor` carries `regex:<rule_key>` or `llm_2nd_pass`. Full cardinality. | |
| Both — store rule_key but aggregate at 2 streams initially | Persist `predictor` + `predictor_rule` columns. Phase 71 reads rule-level. | |

**User's choice:** Two streams: `regex` vs `llm_2nd_pass` (recommended)
**Notes:** Per-rule granularity deferred to Phase 71 which already owns regex-rule promotion math. Per-rule data is reconstructable from `pipeline_events.decision_details.regex.matchedRule` if Phase 71 needs it.

### Q6: How should predictor attribution surface in the Stage 1 row UI and the verdict-recording workflow?

| Option | Description | Selected |
|--------|-------------|----------|
| Predictor chip + plain approve/reject | Row card shows chip (`LLM · medium` or `regex · rule_key`). Verdict UI unchanged. Server-side captures predictor from pipeline_events. | ✓ |
| Predictor chip + verdict subclasses | Dedicated buttons: `LLM was wrong`, `Regex rule X was wrong`, `Not noise — should reach Stage 3`. | |
| Chip only; verdict subclass derived server-side | Plain verdict UI; server derives subclass from (predictor, decision, override) into `verdict_subclass` column. | |

**User's choice:** Predictor chip + plain approve/reject (recommended)
**Notes:** Operator UX stays simple. No new verdict buttons to teach. Attribution is automatic from pipeline_events on recordVerdict.

---

## Backfill strategy

### Q7: For the new `agent_runs.predictor` column, how do we handle historical rows?

| Option | Description | Selected |
|--------|-------------|----------|
| Forward-only cutover | Migration adds `predictor text NULL`. New verdicts populate forward. Historical rows stay NULL. Feedback math filters `predictor IS NOT NULL`. | ✓ |
| Backfill from pipeline_events | One-shot SQL/script reads decision_details for every historical agent_run. | |
| Hybrid: forward + lazy backfill on read | labeling-flip-cron joins agent_runs to pipeline_events on the fly when `predictor IS NULL`. | |

**User's choice:** Forward-only cutover (recommended)
**Notes:** Accepts cold-start window before new per-predictor Wilson-CI streams have N=50. Cron shadow-mode during cold-start is a planning-time decision; principle of forward-only is locked.

---

## Claude's Discretion

- Exact mechanism for "keep row queued without firing classifier/verdict.recorded" (new event name vs `decision: "defer"` shape).
- Predictor chip rendering details (whether regex variant shows `<rule_key>` or just `regex`).
- Exact threshold and emission mechanism for the high-conf FP calibration-drift signal (D-03).
- Whether labeling-flip-cron remains in shadow mode during the forward-only cold-start window.

## Deferred Ideas

- Per-regex-rule attribution (`regex:<rule_key>` in `agent_runs.predictor`) — Phase 71 owns rule-level math.
- Numeric top-2 + margin LLM output contract — explicit non-goal of 999.8.
- Dedicated `?sub=low-confidence` Stage 1 sub-view — rejected in favor of filter chips; revive if filter-chip UX proves insufficient.
- Operator verdict subclasses — verdict UI stays plain; revisit in Phase 72 (Promotion recommender) if richer intent capture is needed.
- Historical predictor backfill — forward-only locked; backfill is a follow-on if cold-start window proves unacceptable.

---

## Follow-up pass (2026-05-11) — Claude's Discretion items reviewed

A second discuss-phase pass picked up three of the four open items and locked them; the fourth (D-03 thresholds) stays as Claude's Discretion for the planner.

### Q-fp1: Mechanism for keeping a row queued without firing `verdict.recorded`?

| Option | Description | Selected |
| -- | -- | -- |
| (a) New event `classifier/screen.requires_review` | verdict-worker untouched. Row stays at `status=predicted`. Optional future subscriber. | ✓ |
| (b) Reuse `verdict.recorded` with `decision: "defer"` | Verdict-worker grows a defer arm (no-op). | |
| Defer to planner | | |

**User's choice:** (a) new dedicated event. Captured as **D-10**.
**Notes:** Cleaner separation, low Inngest namespace cost, no verdict-worker change.

### Q-fp2: Concrete threshold + signal type for D-03 high-conf FP alert?

| Option | Description | Selected |
| -- | -- | -- |
| 2% rolling-50, dashboard flag only | Soft signal. | |
| 2% rolling-50, Inngest event + dashboard | Active signal, automatable. | |
| Defer to planner | | ✓ |

**User's choice:** defer. Planner reads existing alert patterns and proposes both numbers and emission shape.
**Notes:** Principle (D-03) locked in original pass; concrete values intentionally left to planning.

### Q-fp3: Predictor filter query shape in `loadPageData`?

| Option | Description | Selected |
| -- | -- | -- |
| Denormalized `agent_runs.predictor` | Matches D-07 migration. One column, one filter. | ✓ |
| JOIN `pipeline_events` at query time | Lighter schema; extra JOIN per page load. | |
| Defer to planner | | |

**User's choice:** denormalized. Captured as **D-11**.
**Notes:** Consistent with the migration and Wilson-CI cron's read shape.

### Q-fp4: Regex predictor chip text on the Stage 1 row card?

| Option | Description | Selected |
| -- | -- | -- |
| `regex · <rule_key>` | Instant context; longer chip. | |
| `regex` only | Compact; detail pane shows rule. | ✓ |
| Defer to planner | | |

**User's choice:** `regex` only. Captured as **D-12**.
