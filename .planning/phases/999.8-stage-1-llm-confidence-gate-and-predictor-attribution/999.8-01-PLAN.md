---
phase: 999.8-stage-1-llm-confidence-gate-and-predictor-attribution
plan: 01
name: Wave 0 — RED test scaffolds for gate, predictor write, cron split, page filter
type: tdd
wave: 0
depends_on: []
files_modified:
  - web/lib/inngest/functions/__tests__/classifier-screen-worker.gate.test.ts
  - web/lib/inngest/functions/__tests__/labeling-flip-cron.predictor.test.ts
  - web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/actions.predictor.test.ts
  - web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/load-page-data.predictor.test.ts
autonomous: true
gap_closure: false
requirements: [REQ-GATE-01, REQ-VERDICT-08, REQ-CRON-09, REQ-FP-03, REQ-CHIP-05]
must_haves:
  truths:
    - "RED test files exist describing each behaviour that later waves must satisfy"
    - "All RED tests fail with a clear assertion message (not a setup error)"
    - "Each test covers the exact Pitfall surfaces from RESEARCH §Pitfalls (regex no_match abstain, swarm_type mismatch, email_id vs automation_run_id)"
  artifacts:
    - path: "web/lib/inngest/functions/__tests__/classifier-screen-worker.gate.test.ts"
      provides: "RED tests for D-01 gate: high→approve, medium/low→requires_review, unknown→approve, regex-only→approve"
    - path: "web/lib/inngest/functions/__tests__/labeling-flip-cron.predictor.test.ts"
      provides: "RED tests for D-07 per-predictor Wilson-CI split + D-09 predictor IS NOT NULL filter + D-03 high-conf FP alert"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/actions.predictor.test.ts"
      provides: "RED tests for recordVerdict writing predictor='llm_2nd_pass' / 'regex' / NULL"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/load-page-data.predictor.test.ts"
      provides: "RED tests for ?predictor= and ?confidence= URL filters"
  key_links:
    - from: "gate.test.ts"
      to: "classifier-screen-worker.ts emit-verdict step"
      via: "mocked inngest.send capturing event name"
      pattern: "classifier/(verdict.recorded|screen.requires_review)"
    - from: "actions.predictor.test.ts"
      to: "recordVerdict agent_runs.insert"
      via: "Supabase admin mock capturing insert payload"
      pattern: "predictor: 'llm_2nd_pass'|'regex'|null"
---

<objective>
Establish the test contracts for Phase 999.8 BEFORE any implementation lands. RED tests define the exact behaviour each later wave must satisfy. No production code in this plan.

Purpose: TDD discipline (per `<tdd_integration>`) — the gate, predictor write, cron split, and page filter are all logic with crisp I/O contracts. Writing the tests first locks the contract and prevents the executor from drifting into a different shape during implementation.

Output: Four test files, all RED. Each test asserts a single observable behaviour. CI shows them failing with the assertion message (not a missing-import or setup error).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/999.8-stage-1-llm-confidence-gate-and-predictor-attribution/999.8-CONTEXT.md
@.planning/phases/999.8-stage-1-llm-confidence-gate-and-predictor-attribution/999.8-RESEARCH.md
@.planning/phases/999.8-stage-1-llm-confidence-gate-and-predictor-attribution/999.8-PATTERNS.md
@web/lib/inngest/functions/classifier-screen-worker.ts
@web/lib/inngest/functions/labeling-flip-cron.ts
@web/app/(dashboard)/automations/[swarm]/stage-1/actions.ts
@web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx
@web/vitest.config.ts

<interfaces>
<!-- Key contracts later waves must satisfy. Tests assert these. -->

Event payload (new, from RESEARCH §4):
```typescript
"classifier/screen.requires_review": {
  data: {
    automation_run_id: string;
    agent_run_id: string | null;
    email_id: string;
    message_id: string;
    source_mailbox: string;
    swarm_type: string;
    entity?: string | null;
    llm_category_key: string;
    llm_confidence: "medium" | "low";
    final_category_key: string;
  };
}
```

Gate decision (RESEARCH §1):
```
gateClearsForAutoArchive =
  !llmInvoked                       // regex path: always clears
  || llmConfidence === "high"       // high LLM conf: clears
  || finalCategoryKey === "unknown" // unknown: label-only-skip path (action=reject)
```

Predictor derivation (RESEARCH §7, Pitfall 2):
```
predictor =
  decision_details.llm_invoked === true ? "llm_2nd_pass"
  : decision_details.regex?.invoked === true ? "regex"
  : null
// NEVER infer from regex.matchedRule === "no_match" — that is abstain, not a prediction
```

`agent_runs.predictor` enum: `'regex' | 'llm_2nd_pass' | null` (NULL = pre-cutover row).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED gate tests for classifier-screen-worker (D-01, D-10)</name>
  <files>web/lib/inngest/functions/__tests__/classifier-screen-worker.gate.test.ts</files>
  <behavior>
    - regex path (llmInvoked=false): emits `classifier/verdict.recorded` with `decision="approve"` (unchanged from today)
    - LLM high confidence: emits `classifier/verdict.recorded` with `decision="approve"`
    - LLM medium confidence: emits `classifier/screen.requires_review` (D-10), DOES NOT emit `verdict.recorded`
    - LLM low confidence: emits `classifier/screen.requires_review`, DOES NOT emit `verdict.recorded`
    - LLM returns `unknown` (regardless of confidence): emits `classifier/verdict.recorded` (label-only-skip path, action=reject preserved)
    - `emit-pipeline-event` step is called unconditionally on all branches (RESEARCH §1 "pipeline_events row still emitted")
  </behavior>
  <action>
    Create the test file per the analog patterns in existing `web/lib/inngest/functions/__tests__/`. Mock `inngest.send` via a vi.fn() that captures all calls. Mock the Orq agent invocation via the existing `invokeOrqAgent` mock pattern (grep `__tests__` for the canonical mock). Use 6 test cases (one per behaviour bullet above) — each test calls the worker with a fixture event, then asserts the captured `inngest.send` calls.

    NOTE: the gate is implemented per D-01 in plan 03 (Wave 2). For now, every test MUST fail with the assertion "expected 'classifier/screen.requires_review' but got 'classifier/verdict.recorded'" (or similar). If a test fails with a setup error (missing import, mock not wired), fix the setup so it fails on the assertion instead.

    Use the existing fixture-data style from the surrounding test files in the same directory — do NOT invent a new test harness.
  </action>
  <verify>
    <automated>cd web && npm run test -- classifier-screen-worker.gate</automated>
  </verify>
  <done>Test file exists with 6 RED tests. Each fails with a clear assertion message, not a setup error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED cron + recordVerdict + page filter tests</name>
  <files>
    web/lib/inngest/functions/__tests__/labeling-flip-cron.predictor.test.ts,
    web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/actions.predictor.test.ts,
    web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/load-page-data.predictor.test.ts
  </files>
  <behavior>
    **labeling-flip-cron.predictor.test.ts** (D-07 + D-09 + D-03):
    - Per-mailbox query is now per-(mailbox, predictor): two queries per mailbox, one with `.eq("predictor", "regex")`, one with `.eq("predictor", "llm_2nd_pass")`
    - Both queries include `.not("predictor", "is", null)` filter (D-09 forward-only)
    - Each predictor stream computes its own Wilson-CI lower bound independently
    - Audit row `rule_key` is `mailbox_flip:${mailbox}:${predictor}` (two streams per mailbox in `classifier_rule_evaluations`)
    - Bucket with n<50 returns `no_change` (cold-start floor, RESEARCH §5)
    - **D-03 high-conf FP alert**: when predictor='llm_2nd_pass' and n_high_conf>=50 and high_conf_fp_rate > 0.05 → emits `classifier/calibration_drift.detected` event with action='calibration_alarm'
    - D-03 warn tier: high_conf_fp_rate > 0.02 but <= 0.05 → audit row with action='calibration_warn', NO event emission
    - **Critical (RESEARCH Pitfall 1, verified pre-flight 2026-05-11)**: cron filter is `swarm_type='debtor-email'` (the value `recordVerdict` actually writes; production count = 404 rows, all 'debtor-email', zero 'debtor-email-labeling'). Test asserts the eq filter argument is `'debtor-email'`.

    **actions.predictor.test.ts** (D-08 + Pitfall 9):
    - `recordVerdict` reads `pipeline_events` for the email (filtered by `email_id`, `stage=1`, `swarm_type`)
    - Predictor derived as `llm_2nd_pass` when `decision_details.llm_invoked === true`
    - Predictor derived as `regex` when `decision_details.llm_invoked !== true` AND `decision_details.regex?.invoked === true`
    - Predictor is `null` when neither (edge case — defensive)
    - `regex.matchedRule === "no_match"` is NOT treated as a regex prediction when `llm_invoked === true` (Pitfall 2 — abstain, not prediction)
    - `VerdictInput` schema now requires a real `email_id` (Pitfall 9 — don't rely on `email_id===automation_run_id` aliasing)
    - The `agent_runs.insert` payload includes the derived `predictor` field

    **load-page-data.predictor.test.ts** (D-05 + D-11):
    - `?predictor=llm_2nd_pass` adds an eq filter on `decision_details->>predictor` (denormalized chip filter per RESEARCH §6)
    - `?predictor=regex` adds the analogous filter
    - `?confidence=medium` adds eq filter on `decision_details->>llm_confidence`
    - Invalid `?predictor=garbage` is silently ignored (no filter applied; not a 400)
    - Default (no params) returns rows unfiltered by predictor/confidence (D-06)
  </behavior>
  <action>
    Create the three test files following the analog patterns at `web/lib/inngest/functions/__tests__/*.test.ts` and `web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/*.test.ts`. Mock the Supabase admin client per the existing test conventions (grep the test dir for the canonical `createAdminClient` mock).

    For load-page-data tests: extend the existing `load-page-data.test.ts` mock chain if there is one; otherwise mirror its query-builder mocking idiom.

    All tests MUST fail RED with assertion errors (not setup errors). Pre-write the production code SHAPE the test expects (function signature, returned shape) by importing the symbol the implementation will export — the test should fail because the symbol doesn't yet behave as expected, not because it's undefined. If the import is undefined, stub a minimal export in the production file so the test fails on assertion. Document any stubs added.

    Per CLAUDE.md "Build-time codegen" pattern: this plan does NOT need codegen — the `predictor` enum is a 2-value union which is fine as a TS string-literal.
  </action>
  <verify>
    <automated>cd web && npm run test -- labeling-flip-cron.predictor stage-1.*predictor</automated>
  </verify>
  <done>Three test files exist. All tests RED with assertion errors. CI command above shows ≥10 failing tests, none with setup errors.</done>
</task>

</tasks>

<verification>
- All four test files exist under `__tests__/` directories adjacent to their implementation targets.
- `cd web && npm run test -- predictor gate` returns ≥16 failing tests (6 gate + 10 across the other three files), all with assertion messages.
- No test fails with `TypeError: Cannot read property 'X' of undefined` or `Cannot find module` — those are setup errors and must be fixed.
- No production code modified except minimal stubs (documented) needed to keep imports resolving.
</verification>

<success_criteria>
- RED tests committed and pushable
- Each later wave (02, 03, 05, 06, 07) has at least one corresponding RED test that turns GREEN on completion
- No flaky setup; tests pass when the implementation lands and fail without it
</success_criteria>

<output>
Create `.planning/phases/999.8-stage-1-llm-confidence-gate-and-predictor-attribution/999.8-01-SUMMARY.md` listing each test file + the count of RED tests in each + the symbol each test expects from the implementation file (so executors of later plans know what they need to export).
</output>
