---
phase: 82.7-detail-pane-post-approve-polish
plan: 03
type: execute
wave: 3
depends_on:
  - "82.7-02"
files_modified:
  - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/components/pipeline-flow.tsx
autonomous: true
requirements:
  - D-03
tags:
  - per-stage-cancel-override
  - stage-step
  - pipeline-flow
must_haves:
  truths:
    - "When a stage card's state is `dirty`, the operator sees a `cancel override` link/button adjacent to the override-coupling helper, styled like the existing `override stage` link."
    - "Clicking `cancel override` on stage N clears ONLY stage N's dirty flag (via the `onCancelDirty(N)` callback from Plan 02), leaving other dirty stages and the draft prose note intact."
    - "PipelineFlow's prop signature formally declares `onCancelDirty: (stageN: number) => void`, retiring the `@ts-expect-error` directive (if any) added in Plan 02."
    - "StageStepProps declares `onCancelDirty?: () => void` as an optional prop (callers without override flows pass nothing)."
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx"
      provides: "Per-stage cancel-override link rendered when stage.state === 'dirty'"
      contains: "cancel override"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/components/pipeline-flow.tsx"
      provides: "onCancelDirty prop thread from parent detail-pane down to StageStep"
      contains: "onCancelDirty"
  key_links:
    - from: "stage-step.tsx dirty-state render branch"
      to: "<button onClick={() => onCancelDirty?.()}>cancel override</button>"
      via: "in-component conditional render under stage.state === 'dirty'"
      pattern: "cancel override"
    - from: "pipeline-flow.tsx PipelineFlowProps"
      to: "onCancelDirty: (stageN: number) => void"
      via: "interface declaration"
      pattern: "onCancelDirty: \\(stageN: number\\) => void"
    - from: "pipeline-flow.tsx <StageStep ... /> per-stage render"
      to: "onCancelDirty={() => onCancelDirty(s.n)}"
      via: "JSX prop passthrough"
      pattern: "onCancelDirty={\\(\\) => onCancelDirty"
---

<objective>
Complete the D-03 per-stage half by extending the prop thread `detail-pane.tsx → pipeline-flow.tsx → stage-step.tsx` so that each stage card with `state === 'dirty'` renders a `cancel override` affordance the operator can click to undo a single accidental override without affecting other dirty stages or their draft notes.

Purpose: Plan 02 added `onCancelDirty(stageN)` on the parent and passed it to `<PipelineFlow>`. This plan formally declares the prop on PipelineFlowProps, passes it through to each `<StageStep>`, and renders the `cancel override` link inside `stage-step.tsx` when the stage's local state is `'dirty'`.

Output: Two modified files. Removes the `@ts-expect-error` directive added in Plan 02 (if used).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/82.7-detail-pane-post-approve-polish/82.7-CONTEXT.md
@.planning/phases/82.7-detail-pane-post-approve-polish/82.7-PATTERNS.md
@.planning/phases/82.7-detail-pane-post-approve-polish/82.7-02-SUMMARY.md

<interfaces>
<!-- pipeline-flow.tsx + stage-step.tsx surface points (from PATTERNS.md). -->

From web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx:
- L23-26: `interface StageStepProps { stage: StageData; onMarkDirty: () => void; }` — extend with `onCancelDirty?: () => void`.
- L71: `const [proseNotes, setProseNotes] = useState<...>(...);` — DO NOT clear on cancel (drafts are valuable per CONTEXT D-03 note).
- L180-213: 'ok' branch — current `override stage` link (text-button, `var(--v7-brand-secondary)` color, underline-on-hover). This is the visual template for `cancel override`.
- L216-234: 'dirty' branch — currently renders `stage.widget` + override-coupling helper `⤓ Override + note save together`. This is where `cancel override` is added.
- L246-277: 'skipped' branch — also has an `override stage` link; NOT the site for cancel (skipped is not dirty).

From web/app/(dashboard)/automations/[swarm]/stage-1/components/pipeline-flow.tsx:
- `PipelineFlowProps` declares `onMarkDirty: (stageN: number) => void` — extend with `onCancelDirty: (stageN: number) => void`.
- L106 (approx): `<StageStep stage={s} onMarkDirty={() => onMarkDirty(s.n)} />` — extend with sibling `onCancelDirty={() => onCancelDirty(s.n)}`.

V7 tokens reused (no new tokens):
- `--v7-brand-secondary` — link color (mirror `override stage` styling)
- `--v7-amber` — "override in progress" copy color
- `--font-mono` — typography
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend pipeline-flow.tsx prop signature + pass onCancelDirty to each StageStep</name>
  <files>web/app/(dashboard)/automations/[swarm]/stage-1/components/pipeline-flow.tsx</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/pipeline-flow.tsx (full file — small, <200 lines)
    - .planning/phases/82.7-detail-pane-post-approve-polish/82.7-PATTERNS.md §`stage-step.tsx + pipeline-flow.tsx — D-03 per-stage cancel-override` (full template)
    - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx (lines around the `<PipelineFlow ... onCancelDirty={onCancelDirty} />` call site landed in Plan 02 — confirm caller shape)
  </read_first>
  <behavior>
    - `PipelineFlowProps` (or whatever the props interface is named) declares `onCancelDirty: (stageN: number) => void` immediately after the existing `onMarkDirty: (stageN: number) => void` declaration.
    - The component function destructures `onCancelDirty` from props.
    - Each `<StageStep>` render passes `onCancelDirty={() => onCancelDirty(s.n)}` as a sibling to the existing `onMarkDirty={() => onMarkDirty(s.n)}` callback.
  </behavior>
  <action>
    1. Read `pipeline-flow.tsx` end-to-end. Locate the props interface.
    2. Add the new prop to the interface, alphabetical or sibling-position with `onMarkDirty`:
       ```typescript
       onCancelDirty: (stageN: number) => void;
       ```
       Keep `onMarkDirty` as-is.
    3. In the component function signature, add `onCancelDirty` to the destructure: `({ ..., onMarkDirty, onCancelDirty }: PipelineFlowProps)`.
    4. Locate the `<StageStep>` render (approximately L106). Currently it passes `onMarkDirty={() => onMarkDirty(s.n)}`. Add a sibling prop:
       ```typescript
       <StageStep
         stage={s}
         onMarkDirty={() => onMarkDirty(s.n)}
         onCancelDirty={() => onCancelDirty(s.n)}
       />
       ```
       Preserve existing key prop and any other props verbatim.
    5. Do NOT change any other logic in this file.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | grep "pipeline-flow" || echo "tsc clean for pipeline-flow"</automated>
    <grep_checks>
      - `grep -n "onCancelDirty: (stageN: number) => void" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/components/pipeline-flow.tsx` → MUST return ≥1 match
      - `grep -n "onCancelDirty={() => onCancelDirty" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/components/pipeline-flow.tsx` → MUST return ≥1 match
      - `grep -n "onMarkDirty" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/components/pipeline-flow.tsx` → MUST return ≥3 matches (interface + destructure + JSX usage; unchanged)
    </grep_checks>
  </verify>
  <acceptance_criteria>
    - `PipelineFlowProps` declares `onCancelDirty: (stageN: number) => void`.
    - Component destructures `onCancelDirty` from props.
    - `<StageStep>` receives `onCancelDirty={() => onCancelDirty(s.n)}` alongside existing `onMarkDirty`.
    - tsc clean.
  </acceptance_criteria>
  <done>PipelineFlow forwards per-stage cancel callbacks. Plan 02's `@ts-expect-error` (if added) is now obsolete and removed in Task 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Render cancel-override link in stage-step.tsx dirty branch + retire Plan 02 ts-expect-error</name>
  <files>
    web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx,
    web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx
  </files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx L23-30 (StageStepProps)
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx L180-235 ('ok' branch override stage link as styling template + 'dirty' branch render site)
    - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx (locate the `// @ts-expect-error` directive from Plan 02, if present, near the `<PipelineFlow>` call site)
    - .planning/phases/82.7-detail-pane-post-approve-polish/82.7-PATTERNS.md §`stage-step.tsx + pipeline-flow.tsx` — exact cancel-link JSX template
  </read_first>
  <behavior>
    - `StageStepProps` declares `onCancelDirty?: () => void` (optional — callers without override flows omit it).
    - Component destructures `onCancelDirty` from props.
    - 'dirty' branch (`stage.state === 'dirty'`) renders a new sub-row containing an `Override in progress` label (`var(--v7-amber)` color, 12px mono) on the left and a `cancel override` text-button on the right (`var(--v7-brand-secondary)` color, underline-on-hover, mirroring the 'ok' branch `override stage` link styling).
    - Click on `cancel override` invokes `onCancelDirty?.()` (optional-chained so component is safe if prop omitted by hypothetical caller).
    - `proseNotes` local state is NOT cleared — drafts persist per CONTEXT D-03 discretion.
    - `stage.widget` and the override-coupling helper `⤓ Override + note save together` remain rendered (this is additive, not replacing).
    - `@ts-expect-error` directive added in Plan 02 around `<PipelineFlow onCancelDirty={...}>` is REMOVED (PipelineFlowProps now declares the prop officially).
  </behavior>
  <action>
    1. Read `stage-step.tsx` end-to-end. Locate `StageStepProps` interface at L23-26.
    2. Extend the interface:
       ```typescript
       interface StageStepProps {
         stage: StageData;
         onMarkDirty: () => void;
         onCancelDirty?: () => void;
       }
       ```
    3. In the component function signature (or the `({ stage, onMarkDirty })` destructure), add `onCancelDirty` to the destructure.
    4. Locate the 'dirty' branch render at L216-234. The current shape is approximately:
       ```typescript
       {stage.state === "dirty" && (
         <>
           {stage.widget}
           <div
             data-testid="override-coupling-helper-picker"
             style={{ marginTop: 4, color: "var(--v7-amber)", fontFamily: "var(--font-mono)", fontSize: 11 }}
           >
             ⤓ Override + note save together
           </div>
         </>
       )}
       ```
    5. Insert a new sub-row INSIDE the fragment, AFTER the override-coupling helper (so the visual order is: widget → coupling helper → cancel row). Use the exact JSX from PATTERNS.md:
       ```typescript
       <div className="flex items-center justify-between gap-3 mt-1">
         <span
           className="text-[12px] leading-[1.3]"
           style={{ color: "var(--v7-amber)", fontFamily: "var(--font-mono)" }}
         >
           Override in progress
         </span>
         <button
           type="button"
           onClick={() => onCancelDirty?.()}
           className="text-[12px] leading-[1.3] underline-offset-2 hover:underline transition-colors duration-150 focus:outline-none focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2"
           style={{
             color: "var(--v7-brand-secondary)",
             outlineColor: "var(--v7-brand-secondary)",
             fontFamily: "var(--font-mono)",
           }}
           aria-label={`Cancel override on Stage ${stage.n}`}
           data-testid={`stage-${stage.n}-cancel-override`}
         >
           cancel override
         </button>
       </div>
       ```
    6. Do NOT touch the 'ok' branch (L180-213) or 'skipped' branch (L246-277) — those keep their existing `override stage` link verbatim.
    7. Do NOT touch the `proseNotes` useState (L71).
    8. Open `web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx`. Grep for `// @ts-expect-error` near the `<PipelineFlow` call site. If present (added in Plan 02 to bridge the prop gap), DELETE the directive line(s) AND the explanatory comment immediately above (the `// wired in 82.7-03 — ...` line). The prop now type-checks cleanly because Task 1 declared it on PipelineFlowProps.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | grep -E "stage-step|detail-pane|pipeline-flow" || echo "tsc clean for Plan 03 surface"</automated>
    <grep_checks>
      - `grep -n "onCancelDirty?: () => void" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/components/stage-step.tsx` → MUST return ≥1 match
      - `grep -n ">cancel override<" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/components/stage-step.tsx` → MUST return ≥1 match
      - `grep -n "Override in progress" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/components/stage-step.tsx` → MUST return ≥1 match
      - `grep -n "data-testid={\`stage-\${stage.n}-cancel-override\`}" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/components/stage-step.tsx` → MUST return ≥1 match
      - `grep -n "onCancelDirty?\\.()" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/components/stage-step.tsx` → MUST return ≥1 match
      - `grep -nE "@ts-expect-error.*onCancelDirty|wired in 82\\.7-03" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` → MUST return 0 matches (directive retired)
    </grep_checks>
  </verify>
  <acceptance_criteria>
    - `StageStepProps` includes `onCancelDirty?: () => void` (optional).
    - Dirty-branch render contains an `Override in progress` label + `cancel override` text-button with `var(--v7-brand-secondary)` color and `--font-mono`.
    - Button click invokes `onCancelDirty?.()` (optional-chained).
    - `proseNotes` state untouched.
    - `@ts-expect-error` from Plan 02 retired from detail-pane.tsx.
    - tsc clean across the three involved files.
  </acceptance_criteria>
  <done>Per-stage cancel-override link live for every dirty stage. Plan 02 stub directive removed.</done>
</task>

</tasks>

<verification>
- `cd web && npx tsc --noEmit` → zero errors across the touched files.
- Operator smoke: open Stage 1, select a row. Click `override stage` on Stage 0 only. The stage card now shows `Override in progress | cancel override`. Click `cancel override` → stage flips back to 'ok' state, footer returns from `Submit override (Stage 0)` to `Approve (Stages 0+1)`. Repeat with two stages dirty → cancelling one leaves the other dirty (footer shows `Submit override (Stage 1)` after cancelling Stage 0).
- Verify draft notes persist: type prose in StageFeedbackPanel, then click `cancel override` → re-clicking `override stage` should NOT re-seed an empty textarea (parent owns the seeding per Phase 82.5; Plan 03 only mutates the dirty flag).
- Hard-separation: no `swarm_intents` references introduced; reset path is local state only.
</verification>

<success_criteria>
- D-03 fully satisfied (both footer-level from Plan 02 AND per-stage from this plan).
- PipelineFlowProps and StageStepProps formally declare the cancel callback (no stub directives).
- All prior data-testids preserved + one new (`stage-{n}-cancel-override`) added.
</success_criteria>

<output>
After completion, create `.planning/phases/82.7-detail-pane-post-approve-polish/82.7-03-SUMMARY.md` per template.
</output>
