---
phase: 82.7-detail-pane-post-approve-polish
plan: 02
type: execute
wave: 2
depends_on: ["82.7-01"]
files_modified:
  - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx
autonomous: true
requirements:
  - D-02
  - D-03
tags:
  - detail-pane
  - footer-cancel-override
  - label-fix
must_haves:
  truths:
    - "Primary button renders ONE checkmark glyph (the lucide `<Check />` icon), not two."
    - "When at least one stage is `dirty` (anyDirty === true), the footer shows a secondary `Cancel override` button next to the primary, on Stage 1 only."
    - "Clicking the footer Cancel override button clears the entire `dirty` map to `{}` and the footer returns to Approve mode without any server traffic."
    - "Per-stage cancel callback (`onCancelDirty(stageN)`) is defined and passed down to PipelineFlow, ready for Plan 03 to consume."
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx"
      provides: "Footer cancel-override button + label de-duplication + per-stage reset callback"
      contains: "resetAllStageFeedback"
  key_links:
    - from: "primaryLabel construction"
      to: "label strings without leading `✓ ` glyph"
      via: "ternary at the original L450-454 site"
      pattern: "Approve \\(Stages"
    - from: "footer JSX (anyDirty branch)"
      to: "secondary Cancel button rendering"
      via: "conditional inside `activeStage === 1` ternary"
      pattern: "Cancel override"
    - from: "resetAllStageFeedback callback"
      to: "setDirty({})"
      via: "useCallback hook"
      pattern: "setDirty\\(\\{\\}\\)"
    - from: "onCancelDirty(stageN) callback"
      to: "setDirty(prev => omit stageN)"
      via: "useCallback hook"
      pattern: "onCancelDirty"
---

<objective>
Land three changes in `_shell/detail-pane.tsx` in a single batched edit (single file, single wave) so subsequent plans can build on them without re-touching the file:

1. **D-02:** Drop the literal `✓ ` prefix from all three `primaryLabel` branches (Approve, single-stage override, multi-stage override). The lucide `<Check />` icon already renders inside the Button; the string prefix is the duplicate.
2. **D-03 footer-level:** Add a secondary `Cancel override` button rendered alongside the primary inside the `activeStage === 1` ternary, gated by `anyDirty`. Wires to a new `resetAllStageFeedback` useCallback that does `setDirty({})`. Pure client-side; no server traffic.
3. **D-03 prep (per-stage callback):** Define `onCancelDirty(stageN)` useCallback (sibling to existing `onMarkDirty` at ~L286-288). Pass it through `<PipelineFlow onCancelDirty={onCancelDirty} ... />` at the existing PipelineFlow call site (~L610-616). Plan 03 wires the receiving side.

Purpose: Operator UAT (2026-05-16) reported a double-checkmark on the Approve label (D-02), and that accidentally clicking `override stage` traps the footer in "Submit override" mode with no exit (D-03). Both fix-sites live in detail-pane.tsx; batching them avoids serial re-edits and avoids a same-wave file conflict.

Output: One modified file. Plan 03 (Wave 3) extends the prop thread into stage-step.tsx. Plan 04 (Wave 4) extends `handlePrimary` for auto-advance and touches selection-context.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/82.7-detail-pane-post-approve-polish/82.7-CONTEXT.md
@.planning/phases/82.7-detail-pane-post-approve-polish/82.7-PATTERNS.md

<interfaces>
<!-- detail-pane.tsx surface points (Phase 82.6 baseline, file is 685 lines). -->

From web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx:
- Line ~35: lucide-react imports — `import { Check, MailOpen, SkipForward, X } from "lucide-react";` — Plan 02 ADDS one icon (`Undo2` or `RotateCcw`, pick `Undo2` for consistency with sketch findings).
- Line ~37: `import { Button } from "@/components/ui/button";` — reused.
- Line ~228: `const [dirty, setDirty] = useState<Record<number, boolean>>({});` — state to reset.
- Line ~237: `const { markPendingRemoval } = useSelection();` — destructure point (Plan 04 will extend).
- Line ~286-288: existing `onMarkDirty` useCallback — new `onCancelDirty` sits next to it.
- Line ~440-454: `primaryLabel` ternary (D-02 fix site).
- Line ~610-616: `<PipelineFlow ... onMarkDirty={onMarkDirty} ... />` — add `onCancelDirty` prop here.
- Line ~647-672: footer JSX inside `activeStage === 1 ? (...) : null` ternary — D-03 footer Cancel button goes here.
- `anyDirty` boolean computed at ~L447 from `dirty` map — reused as the cancel-button render gate.

Existing analog patterns:
- Reject button (L664-672) is the styling template for the secondary footer Cancel button: `variant="outline"`, `size="sm"`, lucide icon + label.
- `onMarkDirty` useCallback at L286-288 is the template for `onCancelDirty`.
- `setDirty` is a `useState` setter; safe to call as `setDirty({})` (full clear) or `setDirty(prev => { const { [n]: _, ...rest } = prev; return rest; })` (omit one key).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: De-duplicate primary button label (D-02) — drop literal ✓ prefix</name>
  <files>web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx L440-470 (the `primaryLabel` ternary site)
    - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx L640-672 (the footer JSX where Button renders `<Check />` icon + `{primaryLabel}`)
    - .planning/phases/82.7-detail-pane-post-approve-polish/82.7-PATTERNS.md §`detail-pane.tsx — D-02` (exact current strings)
  </read_first>
  <behavior>
    - `primaryLabel` ternary at L450-454 produces strings WITHOUT a leading `✓ ` glyph.
    - All three branches: Approve / single-stage Submit override / multi-stage Submit overrides.
    - The Button JSX continues to render the lucide `<Check />` icon as its first child — only ONE checkmark visible per button in the live UI.
    - No change to `handlePrimary` logic, no change to which branch is selected.
  </behavior>
  <action>
    1. Read the `primaryLabel` declaration at L450-454. The current form is:
       ```typescript
       const primaryLabel = anyDirty
         ? dirtyStages.length === 1
           ? `✓ Submit override (Stage ${dirtyStages[0]})`
           : `✓ Submit overrides (Stages ${dirtyStages.join(", ")})`
         : `✓ Approve (Stages ${visibleOkStages.join("+")})`;
       ```
    2. Replace with the `✓ ` prefix removed from each template literal:
       ```typescript
       const primaryLabel = anyDirty
         ? dirtyStages.length === 1
           ? `Submit override (Stage ${dirtyStages[0]})`
           : `Submit overrides (Stages ${dirtyStages.join(", ")})`
         : `Approve (Stages ${visibleOkStages.join("+")})`;
       ```
    3. Do NOT touch the `<Check className="h-4 w-4 mr-1" aria-hidden="true" />` icon JSX inside the Button at ~L660. That's the surviving single checkmark.
    4. Do NOT touch any other `✓` glyphs elsewhere in the file (e.g. `✓ Looks correct`, `✓ Saved at`, audit-row chips) — those are NOT next to a lucide icon and stay as-is.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | grep "detail-pane" || echo "tsc clean for detail-pane"</automated>
    <grep_checks>
      - `grep -n "\`✓ Submit override (Stage" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` → MUST return 0 matches
      - `grep -n "\`✓ Submit overrides (Stages" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` → MUST return 0 matches
      - `grep -n "\`✓ Approve (Stages" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` → MUST return 0 matches
      - `grep -n "\`Approve (Stages" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` → MUST return ≥1 match
      - `grep -n "\`Submit override (Stage" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` → MUST return ≥1 match
      - `grep -n "<Check className=\"h-4 w-4 mr-1\"" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` → MUST return ≥1 match (icon preserved)
    </grep_checks>
  </verify>
  <acceptance_criteria>
    - All three `primaryLabel` template literals start with the action verb (`Approve` / `Submit override` / `Submit overrides`), NOT with `✓ `.
    - The lucide `<Check />` icon JSX in the Button is unchanged.
    - File compiles.
  </acceptance_criteria>
  <done>Label strings de-duplicated. Single checkmark rendered.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add resetAllStageFeedback + onCancelDirty callbacks + footer Cancel button (D-03 footer)</name>
  <files>web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx L33-40 (imports — lucide-react line)
    - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx L225-300 (state declarations + existing useCallbacks `onMarkDirty`)
    - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx L605-625 (the PipelineFlow call site)
    - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx L645-680 (footer JSX, especially the Reject button at L664-672 as the secondary-button styling template)
    - .planning/phases/82.7-detail-pane-post-approve-polish/82.7-PATTERNS.md §`detail-pane.tsx — D-03 footer-level cancel-override` (full template)
  </read_first>
  <behavior>
    - New `Undo2` import from `lucide-react` added to existing import statement at ~L35.
    - New `resetAllStageFeedback` useCallback declared near the other useCallbacks (sibling to `onMarkDirty`). Body: `setDirty({})`. Empty dep array.
    - New `onCancelDirty` useCallback declared sibling to `resetAllStageFeedback`. Body: `setDirty(prev => { const { [stageN]: _omit, ...rest } = prev; return rest; })`. Empty dep array.
    - `onCancelDirty` is passed to `<PipelineFlow>` as a new prop alongside the existing `onMarkDirty`. (Plan 03 extends PipelineFlow's prop types to accept it — for this plan, just pass it; TS may complain about an unknown prop until Plan 03 lands, so cast with `// @ts-expect-error — wired in Plan 03 (82.7-03)` if necessary, OR pass it conditionally and let Plan 03 finalize. Preferred: pass it; if TS errors, add the inline `// @ts-expect-error: wired in 82.7-03` directive.)
    - Footer JSX gains a secondary Cancel button inside the existing `activeStage === 1 ? (...) : null` ternary, rendered immediately before or after the primary Button, gated by `{anyDirty && (...)}`.
  </behavior>
  <action>
    1. Read the imports line at L35. It currently is approximately:
       ```typescript
       import { Check, MailOpen, SkipForward, X } from "lucide-react";
       ```
       Add `Undo2`:
       ```typescript
       import { Check, MailOpen, SkipForward, Undo2, X } from "lucide-react";
       ```
       Keep alphabetical ordering (Undo2 between SkipForward and X).
    2. Locate the existing `onMarkDirty` useCallback (around L286-288). Immediately after it, add two new useCallback declarations:
       ```typescript
       const resetAllStageFeedback = useCallback(() => {
         setDirty({});
       }, []);

       const onCancelDirty = useCallback((stageN: number) => {
         setDirty((prev) => {
           const { [stageN]: _omit, ...rest } = prev;
           return rest;
         });
       }, []);
       ```
    3. Locate the `<PipelineFlow ... />` call site (around L610-616). Add `onCancelDirty={onCancelDirty}` as a new prop, sibling to the existing `onMarkDirty={onMarkDirty}`. If TypeScript complains because PipelineFlowProps doesn't declare `onCancelDirty` yet, add a single-line comment ABOVE the prop:
       ```typescript
       // wired in 82.7-03 — PipelineFlow prop signature extended in Plan 03
       // @ts-expect-error
       onCancelDirty={onCancelDirty}
       ```
       Plan 03 will add the prop to `PipelineFlowProps` and remove this directive.
    4. Locate the footer JSX inside the `activeStage === 1` ternary (around L647-672). Identify the primary Button (`data-testid="detail-pane-primary"`) and the Reject Button (`variant="outline"`). Insert a new secondary Cancel button INSIDE the same flex container, rendered conditionally on `anyDirty`. Place it immediately AFTER the primary Button and BEFORE Reject (so the visual order is `[Submit override] [Cancel override] [Reject]`):
       ```typescript
       {anyDirty && (
         <Button
           type="button"
           variant="outline"
           size="sm"
           onClick={resetAllStageFeedback}
           data-testid="detail-pane-cancel-override"
         >
           <Undo2 className="h-4 w-4 mr-1" aria-hidden="true" />
           Cancel override
         </Button>
       )}
       ```
       The button MUST sit INSIDE the `activeStage === 1` ternary so it disappears on other stages, matching the Stage 1 footer guard pattern (D-02b from Phase 82.6).
    5. Do NOT touch `handlePrimary`, `approvePrediction`, `markPendingRemoval`, or any other logic. This task is purely additive (one import, two callbacks, one prop, one button).
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | grep "detail-pane" | grep -v "ts-expect-error" || echo "tsc clean for detail-pane (apart from expected directives)"</automated>
    <grep_checks>
      - `grep -n "resetAllStageFeedback" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` → MUST return ≥2 matches (declaration + onClick wire)
      - `grep -n "onCancelDirty" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` → MUST return ≥2 matches (declaration + PipelineFlow prop)
      - `grep -n "Cancel override" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` → MUST return ≥1 match (button label)
      - `grep -n "data-testid=\"detail-pane-cancel-override\"" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` → MUST return ≥1 match
      - `grep -n "Undo2" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` → MUST return ≥2 matches (import + JSX usage)
      - `grep -n "setDirty({})" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` → MUST return ≥1 match
    </grep_checks>
  </verify>
  <acceptance_criteria>
    - `Undo2` imported from lucide-react in alphabetical position.
    - `resetAllStageFeedback` useCallback exists, body `setDirty({})`.
    - `onCancelDirty(stageN: number)` useCallback exists, omits `stageN` key from dirty map.
    - `<PipelineFlow>` receives `onCancelDirty` prop (with `@ts-expect-error` if needed; Plan 03 removes the directive).
    - Footer JSX contains a `<Button variant="outline" data-testid="detail-pane-cancel-override">` rendered conditionally on `anyDirty`, INSIDE the `activeStage === 1` ternary.
    - `tsc --noEmit` reports zero errors EXCEPT the explicitly-marked `@ts-expect-error` directive (if used), which Plan 03 will resolve.
  </acceptance_criteria>
  <done>Footer Cancel override button live for Stage 1 + anyDirty state. Per-stage cancel callback ready for Plan 03 to consume.</done>
</task>

</tasks>

<verification>
- `cd web && npx tsc --noEmit` → zero errors (apart from the single allowed `@ts-expect-error` if used; Plan 03 removes it).
- Operator smoke: open Stage 1, click `override stage` on any stage → the footer now shows a secondary `Cancel override` button next to `Submit override (Stage N)`. Click it → footer returns to `Approve (Stages X+Y)` mode without any network call (verify with DevTools Network tab — empty).
- Visual: only ONE `✓` glyph visible on the primary button (the lucide icon), never two.
- Hard-separation: zero new `swarm_intents` references; reset path is pure local state.
</verification>

<success_criteria>
- D-02 satisfied: single checkmark on primary button.
- D-03 footer half satisfied: footer Cancel override button works and clears dirty map.
- D-03 per-stage half PREPARED: `onCancelDirty` callback defined and threaded into PipelineFlow ready for Plan 03 to consume.
</success_criteria>

<output>
After completion, create `.planning/phases/82.7-detail-pane-post-approve-polish/82.7-02-SUMMARY.md` per template.
</output>
