---
phase: 35-pipeline-engine
plan: 04
type: execute
wave: 3
depends_on: [35-02, 35-03]
files_modified:
  - web/app/(dashboard)/projects/[id]/page.tsx
  - web/app/(dashboard)/runs/page.tsx
  - web/components/run-card.tsx
  - web/components/app-sidebar.tsx
autonomous: false
requirements: [FOUND-05]

must_haves:
  truths:
    - "User sees a list of pipeline runs inside their project with status, progress, and timestamps"
    - "User sees a global runs page showing all runs across their projects"
    - "Project detail page has tabs: Overview and Runs"
    - "Run cards show: name, status badge, step progress, started time, duration, last error if failed"
    - "New Pipeline Run button navigates to the new-run form"
  artifacts:
    - path: "web/app/(dashboard)/projects/[id]/page.tsx"
      provides: "Refactored project detail with tabbed layout"
      contains: "Runs"
    - path: "web/app/(dashboard)/runs/page.tsx"
      provides: "Global runs page across all projects"
      min_lines: 40
    - path: "web/components/run-card.tsx"
      provides: "Run card component for run lists"
      exports: ["RunCard"]
  key_links:
    - from: "web/components/run-card.tsx"
      to: "/projects/[id]/runs/[runId]"
      via: "Link to run detail page"
      pattern: "href.*runs"
    - from: "web/app/(dashboard)/projects/[id]/page.tsx"
      to: "/projects/[id]/new-run"
      via: "New Pipeline Run button"
      pattern: "new-run"
---

<objective>
Run list UI: project-scoped run list with tabs, global runs page, and end-to-end verification checkpoint.

Purpose: Complete the pipeline visibility story -- users can see all their pipeline runs both within a project (tabbed view) and globally (sidebar nav). This plan also refactors the project detail page to a tabbed layout (Overview | Runs) as specified in CONTEXT.md.

Output: Tabbed project detail page, global runs page, reusable run card component, and verified end-to-end flow.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-pipeline-engine/35-CONTEXT.md
@.planning/phases/35-pipeline-engine/35-02-SUMMARY.md
@.planning/phases/35-pipeline-engine/35-03-SUMMARY.md

<interfaces>
<!-- Existing project detail page to refactor -->

From web/app/(dashboard)/projects/[id]/page.tsx (current):
```typescript
// Currently: single page with members card + pipeline runs placeholder
// Refactor to: tabbed layout with Overview (members) and Runs (run list)
```

From supabase/schema-pipeline.sql:
```sql
-- pipeline_runs columns available for display:
-- id, project_id, name, use_case, status, step_count, steps_completed,
-- agent_count, started_at, completed_at, created_at
```

From web/components/step-status-badge.tsx (Plan 03):
```typescript
export function StepStatusBadge({ status }: { status: string }): JSX.Element;
```

From web/components/app-sidebar.tsx (Phase 34):
```typescript
// Sidebar with nav items: Dashboard, Projects, Runs, Settings, Profile
// Runs item currently links to placeholder page
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run card component, tabbed project detail, and global runs page</name>
  <files>web/components/run-card.tsx, web/app/(dashboard)/projects/[id]/page.tsx, web/app/(dashboard)/runs/page.tsx</files>
  <action>
First, install shadcn Tabs component: `cd web && npx shadcn@latest add tabs --yes` (skip if already installed).

**web/components/run-card.tsx:**
Client component for displaying a pipeline run in list views.

Props: `run` object with pipeline_runs fields.

Card layout (per CONTEXT.md: "Detailed run cards showing: run name, status badge, step progress, agent count, started timestamp, duration, last error if failed"):
- Left side:
  - Run name (bold, truncated)
  - Use case description (muted, truncated to 1 line)
- Right side:
  - Status badge (reuse Badge component with color variants: pending=gray, running=blue, complete=green, failed=red)
- Bottom row:
  - Step progress: "{steps_completed}/{step_count} steps" with a subtle progress bar
  - Started: relative timestamp (e.g., "5 min ago", "2 hours ago") -- use simple relative time function, no library needed
  - Duration: if completed_at exists, show human-readable duration
  - If failed: show last error in red muted text, truncated

Entire card is wrapped in a Link to `/projects/{project_id}/runs/{run_id}`.
Use Card component from shadcn/ui. Hover state with subtle border color change.

**web/app/(dashboard)/projects/[id]/page.tsx:**
Refactor the existing project detail page to a tabbed layout.

Use shadcn Tabs component:
- Tab 1: **Overview** -- existing content (project info, members list, invite button)
- Tab 2: **Runs** -- pipeline run list for this project

The Runs tab:
1. Fetch pipeline_runs for this project, ordered by created_at DESC
2. Header row with "Pipeline Runs" title and a "New Pipeline Run" button (links to `/projects/{id}/new-run`, uses Plus icon)
3. If runs exist: render RunCard for each
4. If no runs: empty state with Play icon and "No pipeline runs yet" + "Start your first pipeline run" CTA button linking to new-run

Preserve all existing Overview content (members, invite modal, project info). Just wrap in Tabs structure.

Default tab: "runs" (users come here to see/start pipeline runs).

**web/app/(dashboard)/runs/page.tsx:**
Replace the placeholder with a real global runs page (per CONTEXT.md: "Sidebar 'Runs' item can show all runs across projects").

1. Fetch all pipeline_runs the user can see (RLS handles scoping), ordered by created_at DESC
2. Page title: "All Pipeline Runs"
3. Subtitle: "Pipeline runs across all your projects"
4. Render RunCard for each run (include project name in a muted label above or next to the run name)
5. Empty state if no runs

For project name display on global runs: join pipeline_runs with projects table:
```typescript
const { data: runs } = await supabase
  .from("pipeline_runs")
  .select("*, projects(name)")
  .order("created_at", { ascending: false });
```
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | tail -5 && test -f "components/run-card.tsx" && grep -q "Tabs" "app/(dashboard)/projects/[id]/page.tsx" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Project detail has Overview and Runs tabs, run list shows RunCard components with status/progress/timestamps, global runs page shows all runs across projects, New Pipeline Run button links to form</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify end-to-end pipeline flow</name>
  <files>none</files>
  <action>
Verify the complete Phase 35 pipeline engine end-to-end:
- Database schema for pipeline state (pipeline_runs, pipeline_steps, pipeline_files)
- Inngest durable function with step-per-stage execution
- Prompt adapter that reads pipeline .md files from GitHub and calls Claude API
- New pipeline run form with use case textarea and Start Pipeline button
- Run detail page with vertical step timeline, status badges, expandable logs
- Retry-from-failed-step functionality
- Project detail page with Overview/Runs tabs
- Global runs page in sidebar

Steps to verify:
1. Prerequisites: Ensure Supabase is configured (run schema-pipeline.sql), Inngest dev server is running (cd web and npx inngest-cli@latest dev), ANTHROPIC_API_KEY is set in web/.env.local
2. Start the app: cd web and npm run dev
3. Navigate to a project, click "New Pipeline Run", enter a use case description, click "Start Pipeline" -- verify redirect to run detail page
4. Verify step timeline shows all 7 stages transitioning from pending to running to complete (or failed)
5. Verify page updates automatically (5-second polling)
6. Click on a completed step to expand and see log output
7. If a step fails: verify plain-English error message (not stack trace) and retry button works
8. Go back to project page Runs tab -- verify run appears with status badge, step progress, timestamps
9. Navigate to sidebar Runs page -- verify same run appears in global view with project name
10. Visual check: run detail page looks like a build pipeline (GitHub Actions / Vercel deploy feel)
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && echo "TypeScript PASS"</automated>
  </verify>
  <done>End-to-end pipeline flow verified: create run, watch execution, see in lists, retry works, UI is clean and consistent</done>
</task>

</tasks>

<verification>
- [ ] RunCard component shows name, status badge, step progress, timestamps, and error (if failed)
- [ ] Project detail page has tabbed layout with Overview and Runs tabs
- [ ] Runs tab shows project-scoped run list with New Pipeline Run button
- [ ] Global runs page shows all runs across all user's projects
- [ ] End-to-end flow works: create run -> watch execution -> see in lists
- [ ] `cd web && npx tsc --noEmit` passes
</verification>

<success_criteria>
Users can submit a use case, watch the pipeline execute via the run detail page, see their runs in both project-scoped and global views, and retry failed pipelines from the exact failure point. The pipeline engine phase is functionally complete.
</success_criteria>

<output>
After completion, create `.planning/phases/35-pipeline-engine/35-04-SUMMARY.md`
</output>
