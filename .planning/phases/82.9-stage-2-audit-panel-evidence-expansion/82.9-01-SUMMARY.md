---
phase: 82.9
plan: 01
subsystem: debtor-email / stage-2 entity-resolver / zapier-registry
tags: [debtor-email, stage-2, zapier-registry, zod, nxt-candidate-details, d-03]
requires:
  - phase-56 zapier_tools registry (migration 20260429)
  - phase-56 async_callback pattern (migration 20260429c)
provides:
  - CandidateDetailMatchSchema.contact_person (z.string().nullable().optional())
  - CandidateDetailMatchSchema.recent_invoices (z.array(z.string()).max(5).optional().default([]))
  - public.zapier_tools.output_schema['nxt.candidate_details'] declares both new fields
affects:
  - Plan 02 (resolver/writer) — can now persist rich Candidate shape
  - Plan 03 (mapper) — types align with discriminated decision_details
  - Plan 04 (panel UI) — CANDIDATES section renders contact_person + recent_invoices
tech-stack:
  added: []
  patterns:
    - "Three-tier defense-in-depth bound on recent_invoices: SQL LIMIT 5 (Tier 1, Zap) + Zod .max(5) (Tier 2, code) + mapper .slice(0,5) (Tier 3, future Plan 03)"
    - "Zapier-tools registry one-row UPDATE (no new tool, no env-var change)"
    - "Back-compat via .optional() — old Zap responses without new fields still parse (Pitfall 5)"
key-files:
  created:
    - supabase/migrations/20260519_phase82_9_candidate_details_schema_extension.sql
  modified:
    - web/lib/automations/debtor-email/nxt-zap-client.ts
decisions:
  - "D-03 resolved in-place: extend existing Zap + registry row, no sibling endpoint"
  - "contact_person + recent_invoices both .optional() for forward-compat with not-yet-edited Zap and back-compat with legacy responses"
  - "max-5 bound declared at all three tiers; Tier 3 (mapper slice) deferred to Plan 03"
metrics:
  duration: "~10 minutes"
  completed: "2026-05-20T04:13:44Z"
  tasks_completed: 2
  tasks_pending_human_action: 1
---

# Phase 82.9 Plan 01: Extend nxt.candidate_details Zap response with contact_person + recent_invoices

Staged the Zod schema and registry-side declaration for the two new `nxt.candidate_details` output fields (`contact_person`, `recent_invoices[]`); Zap edit + migration application are the remaining human-action checkpoint (Task 3) that requires Zapier UI access and prod Supabase write.

## Tasks

| # | Task | Status | Commit |
|---|------|--------|--------|
| 1 | Extend `CandidateDetailMatchSchema` (Zod) with `contact_person` + `recent_invoices` | ✅ done | `74fa737` |
| 2 | Create migration `20260519_phase82_9_candidate_details_schema_extension.sql` (jsonb_set UPDATE on registry) | ✅ done | `df59c0e` |
| 3 | **CHECKPOINT (human-action)** — operator edits Zap, smoke-tests against acceptance NXT, applies migration to prod Supabase | ⏸️ pending operator | — |

## Files Changed

**Modified:**
- `web/lib/automations/debtor-email/nxt-zap-client.ts` — appended two new optional fields to `CandidateDetailMatchSchema` (inside the existing `z.object({...})`, after `modified_on`). Exported `CandidateDetail` type widens automatically via `z.infer`.

**Created:**
- `supabase/migrations/20260519_phase82_9_candidate_details_schema_extension.sql` — single `UPDATE public.zapier_tools` with nested `jsonb_set` on `output_schema` for `tool_id = 'nxt.candidate_details'`. Includes a `DO $$` smoke-check that raises if the row is missing or the update was a no-op. Zero `INSERT` statements (verified by grep).

## Verification

| Check | Result |
|-------|--------|
| `grep -c "contact_person: z.string().nullable().optional()" web/lib/automations/debtor-email/nxt-zap-client.ts` | `1` ✅ |
| `grep -c "recent_invoices: z.array(z.string()).max(5).optional().default(\[\])"` | `1` ✅ |
| `grep -c "CandidateDetailMatchSchema = z.object"` | `1` (no duplicate) ✅ |
| `npx tsc --noEmit` — grep for nxt-zap-client / CandidateDetail / resolve-debtor errors | zero hits ✅ |
| Migration `grep -c "tool_id = 'nxt.candidate_details'"` | `2` (UPDATE WHERE + DO SELECT) ✅ |
| Migration `grep -c contact_person` / `recent_invoices` | `3` / `3` (≥2 each) ✅ |
| Migration `grep -c '"maxItems": 5'` | `1` ✅ |
| Migration `grep -ic 'INSERT INTO public.zapier_tools'` | `0` ✅ |
| Migration `grep -ic 'UPDATE public.zapier_tools'` | `1` ✅ |

## Deviations from Plan

None — plan executed exactly as written. The two auto tasks (1 and 2) match the planner's `<action>` blocks verbatim, including comment text, field names, default values, jsonb path keys, and the DO-block smoke check.

## Authentication Gates

Task 3 is a structural human-action checkpoint, not an auth gate: the operator needs Zapier UI write access (no API path exposed to Claude for this workflow) AND Supabase Management API/`supabase db push` access to apply the migration to the production project (`mvqjhlxfvtqqubqgdvhz`). Both pre-existed; no new credentials were required by this plan.

## Pending Human Action (Task 3)

Operator must, in order:

1. **Edit existing Zap "candidate_details"** (tool_id `nxt.candidate_details`, target hook `https://hooks.zapier.com/hooks/catch/15380147/uv5ju6h/`) to extend the SQL step (Find Multiple Rows / Run Code) to JOIN the contact-persons relation and the invoices relation, projecting:
   - `contact_person` (single string, primary contact name, NULL allowed)
   - `recent_invoices` (top 5 invoice numbers newest-first; `ORDER BY invoice_date DESC LIMIT 5` — Tier 1 bound)
2. **Smoke-test** against acceptance NXT (`nxt_benelux_prod` read-only SELECT) using a known customer ID from a recent Stage 2 `sender_match` row. Confirm the response includes both new fields and Zod doesn't throw.
3. **Apply migration** `20260519_phase82_9_candidate_details_schema_extension.sql` to prod Supabase via Management API or `supabase db push`.
4. **A1 fallback note**: If `contact_person` is unreachable from the Zap's SQL connection, persist `contact_person: null` (schema accepts it) and document the limitation so Plan 02 knows the field will be reliably null.

Smoke verification SQL after migration apply:
```sql
select output_schema #> '{properties,matches,items,properties,contact_person}' as cp,
       output_schema #> '{properties,matches,items,properties,recent_invoices}' as ri,
       updated_at
from public.zapier_tools
where tool_id = 'nxt.candidate_details';
```

Expected: `cp` non-null with `type: ["string", "null"]`; `ri` non-null with `maxItems: 5`; `updated_at` recent.

## Final Zap Response Shape (target post-Task-3)

Example redacted payload (one match):
```json
{
  "matches": [
    {
      "id": "12345",
      "name": "Acme Holding B.V.",
      "status": "active",
      "brand_id": "NL",
      "country_id": "NL",
      "city": "Amsterdam",
      "classification": "key-account",
      "email": "ap@acme.example",
      "modified_on": "2026-04-12T08:31:00Z",
      "contact_person": "Jan de Vries",
      "recent_invoices": ["NL-2026-001234", "NL-2026-001190", "NL-2026-001150", "NL-2026-001108", "NL-2026-001067"]
    }
  ]
}
```

If A1 falls back, `contact_person: null` is valid and `recent_invoices` may be `[]` if the customer has no invoices yet.

## Migration Application Timestamp

⏸️ Pending — operator applies migration in Task 3. To be recorded in STATE.md / ROADMAP.md by orchestrator after operator signs off ("approved" or "approved (contact_person degraded to null — A1 fallback)").

## Known Stubs

None — no UI-rendered placeholders introduced. Both new fields flow through to consumers (resolver, writer, mapper, panel) only after downstream plans 02-04 wire them; until then they are simply persisted on Zod parse and ignored.

## Threat Flags

None — no new trust boundaries introduced. Same Zap → callback transport, same auth secret, same registry row. PII surface (`contact_person`) is at the same trust level as existing `customer_name` and `email` fields already on the schema.

## Self-Check: PASSED

- `web/lib/automations/debtor-email/nxt-zap-client.ts` — FOUND (modified, contains new schema fields)
- `supabase/migrations/20260519_phase82_9_candidate_details_schema_extension.sql` — FOUND (created)
- Commit `74fa737` (Task 1) — FOUND in `git log --oneline`
- Commit `df59c0e` (Task 2) — FOUND in `git log --oneline`
