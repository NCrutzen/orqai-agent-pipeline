---
phase: 82.5-feedback-ux-deepdive-v8-stabilisation
plan: 04
type: execute
wave: 2
depends_on: [82.5-02]
files_modified:
  - web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx
autonomous: true
requirements: [R3]
requirements_addressed: [R3]

must_haves:
  truths:
    - "Every row with ≥1 email_feedback row for the active stage renders an 8px filled dot in the correct color"
    - "Rows with no feedback render the empty dashed dot"
    - "The selected-row left border treatment (--brand-primary) is preserved (still reads visually)"
    - "RowList accepts feedbackMap prop without changing existing column layout"
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx"
      provides: "RowList that consumes feedbackMap + mounts RowVerdictDot"
      contains: "RowVerdictDot"
  key_links:
    - from: "RowList <li>"
      to: "<RowVerdictDot>"
      via: "first child mount before StageBadge"
      pattern: "<RowVerdictDot"
    - from: "<li> style"
      to: "position: relative"
      via: "absolute-positioned dot anchor"
      pattern: "position: \"relative\""
---

<objective>
Mount the `<RowVerdictDot>` (from Plan 02) into every row of the existing RowList, and thread the new `feedbackMap` prop from page → shell → list. Pure additive change — no grid template touched (per CONTEXT D-2).

Purpose: deliver R3 visible behavior; the dot is now wired to render in every Stage 0–3 row strip.
Output: 1 modified file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82.5-feedback-ux-deepdive-v8-stabilisation/82.5-SPEC.md
@.planning/phases/82.5-feedback-ux-deepdive-v8-stabilisation/82.5-CONTEXT.md
@.planning/phases/82.5-feedback-ux-deepdive-v8-stabilisation/82.5-PATTERNS.md
@web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx
@web/app/(dashboard)/automations/[swarm]/_shell/components/row-verdict-dot.tsx

<interfaces>
<!-- Contract this plan creates that Plan 06 consumes -->

```typescript
export type RowVerdictMap = Record<string /* row.id */, "confirm" | "override" | "unclear" | null>;

// RowList new prop:
type RowListProps = {
  // ... existing ...
  feedbackMap?: RowVerdictMap;
};
```

The page-level prefetch in Plan 06 will reduce a `FeedbackMap` to a `RowVerdictMap` via:
```typescript
const verdictMap: RowVerdictMap = {};
for (const [id, entry] of Object.entries(feedbackMap)) {
  verdictMap[id] = entry.own_latest?.verdict ?? entry.others[0]?.verdict ?? null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire RowVerdictDot into RowList</name>
  <files>web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx (full file)
    - web/app/(dashboard)/automations/[swarm]/_shell/components/row-verdict-dot.tsx (Plan 02 output)
    - .planning/phases/82.5-feedback-ux-deepdive-v8-stabilisation/82.5-PATTERNS.md section 11
    - .planning/phases/82.5-feedback-ux-deepdive-v8-stabilisation/82.5-CONTEXT.md D-2
  </read_first>
  <behavior>
    - Import `RowVerdictDot` from `./components/row-verdict-dot`
    - Add `feedbackMap?: Record<string, "confirm" | "override" | "unclear" | null>` to `RowListProps`
    - Inside the row map, render `<RowVerdictDot verdict={feedbackMap?.[r.id] ?? null} />` as the first child of each `<li>`
    - Ensure each `<li>` style block includes `position: "relative"` so the absolutely-positioned dot anchors correctly
    - DO NOT modify `gridTemplateColumns` (line ~84-86 of current file)
    - DO NOT modify the selected-row left-border treatment (line ~78-80) — the dot's `zIndex: 0` ensures the border still reads above
    - When `feedbackMap` is undefined, every row gets `verdict={null}` (empty dashed dot) — graceful default
  </behavior>
  <action>
Open `web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx`.

**1. Add import near top of file:**
```typescript
import { RowVerdictDot } from "./components/row-verdict-dot";
```

**2. Extend `RowListProps`** (find the existing type/interface and append):
```typescript
feedbackMap?: Record<string, "confirm" | "override" | "unclear" | null>;
```

**3. Destructure the new prop** in the component function signature (wherever existing props are destructured):
```typescript
function RowList({ /* existing */, feedbackMap }: RowListProps) {
```

**4. Verify each row `<li>` has `position: "relative"`** in its inline style block.
- If the current `<li>` inline style does NOT include `position`, add `position: "relative"` to that style object.
- If `position` is already set to something else (e.g., `"static"`), change it to `"relative"`.

**5. Mount `<RowVerdictDot>` as the first child inside each `<li>`** (immediately after the `<li ...>` opening tag, BEFORE any existing children like `<StageBadge>`):
```tsx
<RowVerdictDot verdict={feedbackMap?.[r.id] ?? null} />
```

The variable that holds each row in the map is currently `r` (per PATTERNS section 11 — verify by reading the file; if the parameter name differs, use the actual name).

CRITICAL constraints:
- DO NOT touch `gridTemplateColumns`. The dot is absolute-positioned and consumes zero grid space.
- DO NOT change the existing left-border-on-selection logic. The dot's `zIndex: 0` + the border's higher stacking keeps the orange selection line readable.
- DO NOT add a new column. CONTEXT D-2 is explicit: overlay only.
- DO NOT change row click behavior. `<RowVerdictDot>` has `pointerEvents: 'none'` per Plan 02 — verify it's still there if you re-read row-verdict-dot.tsx.
  </action>
  <verify>
    <automated>grep -q 'import { RowVerdictDot }' "web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx" && grep -q "feedbackMap" "web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx" && grep -q "<RowVerdictDot" "web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx" && grep -q 'position: "relative"' "web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx" && cd web && npx tsc --noEmit 2>&1 | (! grep -q "_shell/row-list")</automated>
  </verify>
  <acceptance_criteria>
    - Import statement for RowVerdictDot present
    - `feedbackMap?` field present in props type
    - `<RowVerdictDot>` mounted as the FIRST JSX child of each `<li>` in the row map
    - `verdict={feedbackMap?.[r.id] ?? null}` (null-default for missing entries)
    - Every `<li>` style has `position: "relative"`
    - `gridTemplateColumns` value is byte-for-byte unchanged from pre-change (verify via git diff inspection — no changes to that property)
    - tsc --noEmit passes
    - When feedbackMap is undefined (or not passed yet by parent), the component still renders without runtime error (every row shows the dashed empty dot)
  </acceptance_criteria>
  <done>RowList renders the verdict dot in every row; absolute positioning preserves the existing layout; the dashed empty state shows for rows without feedback.</done>
</task>

</tasks>

<verification>
- Manual smoke: load any `/automations/[swarm]/stage-1` page (after Plan 06 wires the map) → every row has a dot in the leftmost slot
- Manual: rows with prior `email_feedback` show filled dot in correct color; rows without show dashed empty dot
- Manual: select a row → the orange `--brand-primary` left border still reads on top of the dot
- Manual: click a row → row click handler still fires (no interception)
- tsc --noEmit passes
</verification>

<success_criteria>
- R3 acceptance from SPEC: every row with ≥1 email_feedback row for the active stage renders an 8px filled dot in the correct color; rows with no feedback render the empty dashed dot
- Selected-row left border treatment preserved
- No grid layout regression
</success_criteria>

<output>
After completion, create `.planning/phases/82.5-feedback-ux-deepdive-v8-stabilisation/82.5-04-SUMMARY.md`.
</output>
