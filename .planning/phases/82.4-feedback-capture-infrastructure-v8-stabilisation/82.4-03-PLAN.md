---
phase: 82.4
plan: 03
type: execute
wave: 3
depends_on: [82.4-01, 82.4-02]
files_modified:
  - web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx
  - web/components/automations/bulk-review/audit/__tests__/StageFeedbackPanel.test.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx
autonomous: true
requirements: [FB-04, FB-05]
must_haves:
  truths:
    - "Each Stage 0–3 sidebar entry has a prose-notes textarea (optional, max 4000 chars)"
    - "Each Stage 0–3 sidebar entry has a ✓ Confirm chip alongside the existing 'Looks correct' status line"
    - "Save button POSTs /api/automations/debtor-email/feedback and shows inline error on non-2xx"
    - "Stage 4 sidebar entries do NOT render the feedback panel (per CONTEXT out_of_scope)"
    - "After successful confirm POST, the 82.3 StageDetailExpander auto-collapses (operator-momentum)"
    - "OKLCH inline-style tokens — NO Tailwind colour classes (matches 82.3 audit-surface convention)"
  artifacts:
    - path: "web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx"
      provides: "Prose-notes textarea + Save button + ✓ Confirm chip"
      min_lines: 60
      exports: ["StageFeedbackPanel"]
    - path: "web/components/automations/bulk-review/audit/__tests__/StageFeedbackPanel.test.tsx"
      provides: "RTL coverage for textarea/save/confirm flows"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx"
      provides: "Mounts StageFeedbackPanel inside StageDetailExpander for Stages 0–3"
      contains: "StageFeedbackPanel"
  key_links:
    - from: "StageFeedbackPanel Save button"
      to: "/api/automations/debtor-email/feedback"
      via: "fetch POST"
      pattern: "fetch.*debtor-email/feedback"
    - from: "stage-step.tsx"
      to: "StageFeedbackPanel"
      via: "render inside StageDetailExpander when stage.n !== 4"
      pattern: "StageFeedbackPanel"
---

<objective>
Add the prose-notes textarea + Save button + ✓ Confirm chip beneath each Stage 0–3 sidebar entry. Mount inside the existing 82.3 `StageDetailExpander`. Wire the Save button to POST to `/api/automations/debtor-email/feedback`. Wire the Confirm chip to POST with `verdict='confirm'` and then auto-collapse the expander.

Purpose: This is the operator-facing capture surface. Day 1 of the debtor-person's onboarding (2026-05-18) depends on this being live.
Output: New `StageFeedbackPanel` component + test + modification to `stage-step.tsx` to mount it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82.4-feedback-capture-infrastructure-v8-stabilisation/82.4-CONTEXT.md
@.planning/phases/82.4-feedback-capture-infrastructure-v8-stabilisation/82.4-PATTERNS.md
@web/components/automations/bulk-review/audit/StageDetailExpander.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx

<interfaces>
<!-- Component contract for StageFeedbackPanel -->

```typescript
interface StageFeedbackPanelProps {
  stage: 0 | 1 | 2 | 3;
  emailId: string;
  onAfterConfirm?: () => void;  // Used by stage-step.tsx to auto-collapse the StageDetailExpander
}

export function StageFeedbackPanel(props: StageFeedbackPanelProps): JSX.Element;
```

Behaviour:
- Renders a `<textarea>` (4 rows, 4000 char max, placeholder "Add a note about this stage's decision...").
- "Save" button below textarea — disabled when textarea empty, enabled when non-empty. On click: POST `{ email_id, stage, verdict: 'unclear', prose_notes: <text> }`. Shows inline 12px error text on non-2xx; clears textarea on 2xx.
- "✓ Confirm" chip alongside textarea (right-aligned). On click: POST `{ email_id, stage, verdict: 'confirm', prose_notes: <text or undefined> }`. On 2xx: clear textarea AND call `onAfterConfirm()` (parent collapses expander).
- During in-flight POST, both buttons disabled + button label changes to "Saving…".
- V7 OKLCH inline-style tokens only — match the StageDetailExpander idiom (11px uppercase 600 labels, --v7-panel-2 background, --v7-brand-secondary outline focus).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create StageFeedbackPanel component + RTL tests</name>
  <read_first>
    - web/components/automations/bulk-review/audit/StageDetailExpander.tsx (token/spacing pattern)
    - .planning/phases/82.4-feedback-capture-infrastructure-v8-stabilisation/82.4-PATTERNS.md (lines 173-259, StageFeedbackPanel section)
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx (lines 100-128, confirm-affordance pattern)
  </read_first>
  <files>web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx, web/components/automations/bulk-review/audit/__tests__/StageFeedbackPanel.test.tsx</files>
  <behavior>
    - Renders textarea + Save button + Confirm chip
    - Save disabled when textarea empty
    - Save click → fetch POST to /api/automations/debtor-email/feedback with body `{email_id, stage, verdict:'unclear', prose_notes}`
    - Confirm click → fetch POST with `verdict:'confirm'` and (prose_notes if any)
    - On 2xx confirm → calls `onAfterConfirm()`
    - On non-2xx → inline error text rendered
    - During in-flight → "Saving…" label, both buttons disabled
    - Uses V7 inline-style tokens (test grep for `var(--v7-`)
    - data-testid `stage-feedback-panel-{stage}` on root
  </behavior>
  <action>
Create `StageFeedbackPanel.tsx` as a `"use client"` component implementing the interface above. Match the V7 token idiom from `StageDetailExpander.tsx`:
- Container: `padding: var(--space-3); background: var(--v7-panel-2); border-top: 1px solid var(--v7-border); margin-top: var(--space-3)`.
- Label "Notes": 11px uppercase 600, color `var(--v7-muted)`, letterSpacing 0.04em.
- Textarea: `width: 100%; min-height: 80px; font-size: 12px; line-height: 1.4; padding: var(--space-2); background: var(--v7-panel-1); border: 1px solid var(--v7-line); border-radius: var(--v7-radius-md); color: var(--v7-text); resize: vertical; outline-color: var(--v7-brand-secondary)`. `maxLength={4000}`.
- Row below textarea (flex justify-between): left = Save button, right = ✓ Confirm chip.
- Save button: 12px, `padding: var(--space-1) var(--space-3)`, `background: var(--v7-brand-secondary-soft); color: var(--v7-brand-secondary); border: 1px solid var(--v7-brand-secondary); border-radius: var(--v7-radius-pill)`. Disabled state: `opacity: 0.4; cursor: not-allowed`.
- Confirm chip: same pill shape but `background: var(--v7-lime-soft); color: var(--v7-lime); border: 1px solid var(--v7-lime)`. Label: `✓ Confirm`.
- Inline error: 12px, `color: var(--v7-warn)`, role="alert".
- POST: `fetch("/api/automations/debtor-email/feedback", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({...}) })`.

Test file (`__tests__/StageFeedbackPanel.test.tsx`) — vitest + @testing-library/react. Stub global.fetch. 6 tests:
1. Renders textarea + Save + Confirm chip with data-testid `stage-feedback-panel-2`.
2. Save disabled when textarea empty; enables after typing.
3. Save click triggers POST with `verdict: "unclear"` and the typed prose.
4. Confirm click triggers POST with `verdict: "confirm"`. With empty textarea, body has no `prose_notes` (or `prose_notes: undefined`).
5. On 2xx confirm response, `onAfterConfirm` mock is called.
6. On non-2xx response (mocked 500), inline error with role="alert" appears.
  </action>
  <verify>
    <automated>cd web && npx vitest run components/automations/bulk-review/audit/__tests__/StageFeedbackPanel.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `StageFeedbackPanel.tsx` exists and exports `StageFeedbackPanel`
    - File grep `var(--v7-` returns ≥5 matches (token usage)
    - File grep `className=.*bg-` returns 0 (no Tailwind colour classes)
    - data-testid `stage-feedback-panel-${stage}` present
    - All 6 tests pass
    - Component props match `{ stage: 0|1|2|3, emailId: string, onAfterConfirm?: () => void }`
  </acceptance_criteria>
  <done>Component + tests both pass.</done>
</task>

<task type="auto">
  <name>Task 2: Mount StageFeedbackPanel inside stage-step.tsx with auto-collapse wiring</name>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx (current StageDetailExpander mount block)
    - web/components/automations/bulk-review/audit/StageDetailExpander.tsx (Collapsible structure)
    - .planning/phases/82.4-feedback-capture-infrastructure-v8-stabilisation/82.4-PATTERNS.md (lines 262-280)
  </read_first>
  <files>web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx</files>
  <action>
Currently `stage-step.tsx` renders:
```tsx
{stage.auditDetails != null && stage.n !== 4 && (
  <div className="mt-2">
    <StageDetailExpander stage={stage.n as 0 | 1 | 2 | 3}>
      {stage.auditDetails}
    </StageDetailExpander>
  </div>
)}
```

Refactor this to host BOTH the audit details AND a `StageFeedbackPanel` inside the same expander. The expander needs to be controllable from inside so the Confirm chip can auto-collapse it.

Steps:
1. Add prop `emailId: string` to `StageStepProps` (read `email_id` upstream — `pipeline-flow.tsx` or whichever parent threads `stage.auditDetails`). If a clean upstream thread is hard, accept `emailId?: string` and skip the feedback panel when undefined.
2. Lift the `open` state of the expander up: introduce local `useState<boolean>(false)` named `auditOpen` inside `StageStep`. Modify `StageDetailExpander` props to accept `open?: boolean; onOpenChange?: (v: boolean) => void` (controlled mode). Update `StageDetailExpander.tsx` to fall through to these when supplied, else fall back to its own internal state.
3. Replace the existing audit block with:

```tsx
{stage.auditDetails != null && stage.n !== 4 && (
  <div className="mt-2">
    <StageDetailExpander
      stage={stage.n as 0 | 1 | 2 | 3}
      open={auditOpen}
      onOpenChange={setAuditOpen}
    >
      {stage.auditDetails}
      {emailId && (
        <div style={{ marginTop: "var(--space-3)" }}>
          <StageFeedbackPanel
            stage={stage.n as 0 | 1 | 2 | 3}
            emailId={emailId}
            onAfterConfirm={() => setAuditOpen(false)}
          />
        </div>
      )}
    </StageDetailExpander>
  </div>
)}
```

4. Import `StageFeedbackPanel` from `@/components/automations/bulk-review/audit/StageFeedbackPanel`.
5. Thread `emailId` from the parent that already provides `auditDetails`. Use grep to find the parent: `grep -rn "auditDetails:" web/app/\(dashboard\)/automations/\[swarm\]/`. The parent that builds `stage.auditDetails` (the `stageAudit` map from `buildStageAuditMap`) already has the `email_id` in scope. Thread it through.
6. Run typecheck. Run existing `stage-step` tests to confirm nothing broke.

If `StageDetailExpander.tsx` needs the controlled-mode patch, add this minimal patch (additive — preserves uncontrolled fallback):
```tsx
interface Props {
  stage: 0 | 1 | 2 | 3;
  children: ReactNode;
  open?: boolean;
  onOpenChange?: (v: boolean) => void;
}

export function StageDetailExpander({ stage, children, open: openProp, onOpenChange }: Props) {
  const [internalOpen, setInternalOpen] = useState(false);
  const isControlled = openProp !== undefined;
  const open = isControlled ? openProp! : internalOpen;
  const setOpen = (v: boolean) => {
    if (!isControlled) setInternalOpen(v);
    onOpenChange?.(v);
  };
  // … rest unchanged: pass `open` and `setOpen` to Collapsible
}
```

Note: this modifies `StageDetailExpander.tsx` — record it in `files_modified` if needed. Verify all existing 82.3 tests for StageDetailExpander still pass.
  </action>
  <verify>
    <automated>cd web && grep -q "StageFeedbackPanel" app/\(dashboard\)/automations/\[swarm\]/stage-1/components/stage-step.tsx && grep -q "onAfterConfirm" app/\(dashboard\)/automations/\[swarm\]/stage-1/components/stage-step.tsx && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `stage-step.tsx` imports `StageFeedbackPanel`
    - `StageFeedbackPanel` is rendered inside the existing `StageDetailExpander` block
    - Guard `stage.n !== 4` preserved
    - `onAfterConfirm` is wired to close the expander (`setAuditOpen(false)`)
    - `emailId` is threaded from parent (or panel is skipped when undefined)
    - TypeScript compiles
    - Existing 82.3 StageDetailExpander tests still pass
  </acceptance_criteria>
  <done>Mount + auto-collapse wiring functional.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82.4-03-01 | XSS via prose_notes | StageFeedbackPanel render | mitigate | Textarea is input-only; prose_notes is never rendered back from server in this plan. React text rendering escapes by default if displayed later. |
| T-82.4-03-02 | DoS via spam-click Save | repeated POSTs | accept | Per-row budget at the API layer + button disabled during in-flight call. Operator is single trusted human; no rate-limit needed. |
</threat_model>

<verification>
- Component test file passes (6 tests)
- stage-step.tsx test file (if any) still passes
- Manual smoke: dev server, open detail-pane on a row, click Show details, type prose, click Save → row appears in `email_feedback` (verify via Supabase MCP `execute_sql`)
- Manual smoke: click Confirm → row with `verdict='confirm'` written AND expander collapses
</verification>

<success_criteria>
- Operator can type prose + save per Stage 0–3 entry
- Confirm chip writes verdict='confirm' AND auto-collapses the expander
- No Tailwind colour classes; only V7 inline tokens
- Stage 4 entries unchanged (no feedback panel)
</success_criteria>

<output>
After completion, create `.planning/phases/82.4-feedback-capture-infrastructure-v8-stabilisation/82.4-03-SUMMARY.md`
</output>
