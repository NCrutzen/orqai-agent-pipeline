---
phase: 35-model-selection-discipline
plan: 05
type: execute
wave: 3
depends_on:
  - "35-01"
  - "35-02"
  - "35-03"
  - "35-04"
files_modified:
  - .planning/phases/35-model-selection-discipline/35-05-VERIFICATION.md
autonomous: true
requirements:
  - MSEL-01
  - MSEL-02
  - MSEL-03
must_haves:
  truths:
    - "Full-suite `bash orq-agent/scripts/lint-skills.sh` exits 0 across all 5 rules (allowed-tools, tools-declared, required-sections, references-multi-consumer, snapshot-pinned-models) on the default file set (1 SKILL.md + 15 commands + 17 agents = 33 files)"
    - "Isolated `snapshot-pinned-models` rule exits 1 on negative fixture, 0 on positive fixture, and 0 on default file set (3 invocations recorded)"
    - "`bash orq-agent/scripts/check-protected-pipelines.sh` exits 0 (3 protected entry points byte-identical on `<pipeline>` block)"
    - "Four mandatory phrases present in researcher.md: capable-first, after quality baseline run, cascade-candidate, quality-equivalence experiment"
    - "Four mandatory elements present in spec-generator.md: snapshot-pinned, regex reject, `alias-only -- pinning unavailable`, cascade-candidate"
    - "Capable Tier Lookup section present in orqai-model-catalog.md with 4 task-category rows"
    - "`35-05-VERIFICATION.md` exists with a MSEL-{01,02,03}-to-check traceability table, captured green lint output, and the 4-row ROADMAP Phase 35 Success Criteria checklist"
  artifacts:
    - path: ".planning/phases/35-model-selection-discipline/35-05-VERIFICATION.md"
      provides: "Full-suite green proof + per-requirement traceability + ROADMAP-criteria checklist (evidence trail for /gsd:verify-work)"
      contains: "MSEL-01|MSEL-02|MSEL-03"
  key_links:
    - from: "35-05-VERIFICATION.md traceability table"
      to: "lint rules + phrase-presence greps"
      via: "Every MSEL-0X row names the check (lint rule name, grep pattern, or protected-pipeline hash) and the command to reproduce"
      pattern: "MSEL-0"
    - from: "35-05-VERIFICATION.md ROADMAP criteria checklist"
      to: "ROADMAP.md Phase 35 Success Criteria lines 138-142"
      via: "4-row checklist, each row quoting the ROADMAP criterion verbatim + evidence command + ✓"
      pattern: "Success Criteria"
---

<objective>
Run the full Phase 35 verification sweep and capture green output in `35-05-VERIFICATION.md`. This is the final quality gate before `/gsd:verify-work`. Produce a requirement-to-check traceability table (MSEL-01, MSEL-02, MSEL-03) and a 4-row ROADMAP Phase 35 Success Criteria checklist proving the three MSEL requirements and the four ROADMAP success criteria are mechanically satisfied.

Purpose: Plans 01-04 produced infrastructure (lint rule + fixtures) and per-file policy edits (researcher, spec-generator, catalog). This plan proves the whole system holds together at phase-close. The verification document is the evidence trail for `/gsd:verify-work` and becomes the reference for downstream V3.0 phases (36-43) that inherit the snapshot-pinning invariant.

Output: `.planning/phases/35-model-selection-discipline/35-05-VERIFICATION.md` with captured green lint output (full suite + isolated rule), captured protected-pipelines green output, per-MSEL traceability table, and a ROADMAP Phase 35 Success Criteria checklist — all three sections green.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-model-selection-discipline/35-CONTEXT.md
@.planning/phases/35-model-selection-discipline/35-VALIDATION.md
@.planning/phases/35-model-selection-discipline/35-01-SUMMARY.md
@.planning/phases/35-model-selection-discipline/35-02-SUMMARY.md
@.planning/phases/35-model-selection-discipline/35-03-SUMMARY.md
@.planning/phases/35-model-selection-discipline/35-04-SUMMARY.md

<interfaces>
<!-- The 4 ROADMAP Phase 35 Success Criteria (from ROADMAP.md lines 138-142): -->

1. Running researcher on a sample use case returns capable-tier as Primary; budget-profile downgrades appear only as alternatives tagged "after quality baseline run." **[Grep-verifiable at the file level; full LLM smoke is manual.]**
2. Every generated agent spec contains a snapshot-pinned model reference (e.g., `claude-sonnet-4-5-20250929`), never a floating alias. **[Lint rule `snapshot-pinned-models` enforces; default-set run exits 0.]**
3. When user requests cost optimization, researcher proposes model-cascade pattern with mandatory quality-equivalence experiment step before cascade is marked approved. **[Grep-verifiable at the file level; full LLM smoke is manual.]**
4. Existing `/orq-agent`, `/orq-agent:prompt`, `/orq-agent:architect` produce functionally equivalent output (no generator-loop regression). **[`check-protected-pipelines.sh` SHA-256 check exits 0; full semantic equivalence is a manual smoke deferred to `/gsd:verify-work`.]**

<!-- The 3 MSEL requirements (from REQUIREMENTS.md lines 118-120): -->

| Req | Description | Check | Expected |
|-----|-------------|-------|----------|
| MSEL-01 | Researcher capable-first; budget only as "after quality baseline run" alternative | `grep -q "capable-first" orq-agent/agents/researcher.md` + `grep -q "after quality baseline run" orq-agent/agents/researcher.md` + Capable Tier Lookup table present in catalog | All exit 0 |
| MSEL-02 | Spec-generator pins snapshots, never floating aliases | `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models` on default set + 2 fixture invocations + grep-check for embedded regex in spec-generator.md | Default set + good fixture exit 0, bad fixture exit 1 |
| MSEL-03 | Researcher proposes cascade with mandatory quality-equivalence experiment | `grep -q "cascade-candidate" orq-agent/agents/researcher.md` + `grep -q "quality-equivalence experiment" orq-agent/agents/researcher.md` + `grep -q "cascade-candidate" orq-agent/agents/spec-generator.md` | All exit 0 |

**Files in scope for lint (unchanged from Phase 34 — 33 total):**
- 1 × `orq-agent/SKILL.md`
- 15 × `orq-agent/commands/*.md`
- 17 × `orq-agent/agents/*.md`

Fixtures (`tests/fixtures/35-{bad,good}-pin.md`) are OUT of the default set by design; they are invoked explicitly via `--file` to exercise the rule's negative/positive paths.

**Verification document template (`35-05-VERIFICATION.md`):**

```markdown
# Phase 35 Verification

**Verified:** <DATE>
**Phase:** 35-model-selection-discipline
**Status:** COMPLETE / BLOCKED

## Captured green output

### Full lint suite
$ bash orq-agent/scripts/lint-skills.sh
<stdout verbatim>
<exit: 0>

### Isolated snapshot-pinned-models rule on default set
$ bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models
<stdout verbatim>
<exit: 0>

### Snapshot-pinned-models rule on positive fixture
$ bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file tests/fixtures/35-good-pin.md
<stdout verbatim>
<exit: 0>

### Snapshot-pinned-models rule on negative fixture (intentional FAIL)
$ bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file tests/fixtures/35-bad-pin.md
<stdout verbatim>
<exit: 1>   # intentional — proves rule catches violations

### Protected-pipeline hash check
$ bash orq-agent/scripts/check-protected-pipelines.sh
<stdout verbatim>
<exit: 0>

### Pre-existing rule regression checks
$ bash orq-agent/scripts/lint-skills.sh --rule allowed-tools   → exit 0
$ bash orq-agent/scripts/lint-skills.sh --rule tools-declared   → exit 0
$ bash orq-agent/scripts/lint-skills.sh --rule required-sections   → exit 0
$ bash orq-agent/scripts/lint-skills.sh --rule references-multi-consumer   → exit 0

### Phrase-presence greps
$ grep -q "capable-first" orq-agent/agents/researcher.md   → exit 0
$ grep -q "after quality baseline run" orq-agent/agents/researcher.md   → exit 0
$ grep -q "cascade-candidate" orq-agent/agents/researcher.md   → exit 0
$ grep -q "quality-equivalence experiment" orq-agent/agents/researcher.md   → exit 0
$ grep -q "snapshot-pinned" orq-agent/agents/spec-generator.md   → exit 0
$ grep -q "regex reject" orq-agent/agents/spec-generator.md   → exit 0
$ grep -q "alias-only -- pinning unavailable" orq-agent/agents/spec-generator.md   → exit 0
$ grep -q "cascade-candidate" orq-agent/agents/spec-generator.md   → exit 0
$ grep -q "## Capable Tier Lookup" orq-agent/references/orqai-model-catalog.md   → exit 0

## MSEL requirement traceability

<3-row table: MSEL-01 | MSEL-02 | MSEL-03>

## ROADMAP Phase 35 Success Criteria

<4-row checklist quoting ROADMAP verbatim + evidence + ✓>

## Manual-only deferred verifications

- End-to-end researcher LLM smoke: invoke `/orq-agent:research "<canned use case>"` and confirm Primary is capable-tier with budget alternatives carrying "after quality baseline run" tag. Deferred to /gsd:verify-work.
- Semantic byte-identical check on 3 protected entry points: hash check proves `<pipeline>` block unchanged; full spec-JSON equivalence diff deferred to /gsd:verify-work.

## File inventory

- Default-set skill files: 33 (1 SKILL.md + 15 commands + 17 agents) — all pass lint
- Fixtures: 2 (tests/fixtures/35-bad-pin.md, tests/fixtures/35-good-pin.md) — out of default set by design
- Script modified: orq-agent/scripts/lint-skills.sh (gained 1 rule: snapshot-pinned-models)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full Phase 35 verification sweep and produce 35-05-VERIFICATION.md</name>
  <files>.planning/phases/35-model-selection-discipline/35-05-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/35-model-selection-discipline/35-VALIDATION.md (Per-Task Verification Map, especially 35-05-01 + Manual-Only Verifications table)
    - .planning/phases/35-model-selection-discipline/35-01-SUMMARY.md (confirm lint rule + fixtures landed)
    - .planning/phases/35-model-selection-discipline/35-02-SUMMARY.md (confirm researcher edits landed)
    - .planning/phases/35-model-selection-discipline/35-03-SUMMARY.md (confirm spec-generator edits landed)
    - .planning/phases/35-model-selection-discipline/35-04-SUMMARY.md (confirm catalog edits landed)
    - orq-agent/scripts/lint-skills.sh (confirm the rule IDs used in the verification doc match reality)
    - orq-agent/scripts/check-protected-pipelines.sh
  </read_first>
  <action>
    Execute the full verification suite in this EXACT order, capturing stdout+stderr of each command into `/tmp/35-*.log` files. Do NOT modify any file under `orq-agent/`, `tests/fixtures/`, or any Phase 35 PLAN/SUMMARY file. This plan is read-only on code; it writes only the VERIFICATION.md document.

    **Step 1 — Full lint suite:**
    ```bash
    cd /Users/nickcrutzen/developer/orqai-agent-pipeline
    bash orq-agent/scripts/lint-skills.sh > /tmp/35-lint-full.log 2>&1
    echo "exit: $?" >> /tmp/35-lint-full.log
    ```
    Exit code MUST be 0. If non-zero, STOP and diagnose. Do NOT write VERIFICATION.md with red output; instead, file a BLOCKED status summary and name the offending file + rule.

    **Step 2 — Isolated snapshot-pinned-models rule on default set:**
    ```bash
    bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models > /tmp/35-rule-default.log 2>&1
    echo "exit: $?" >> /tmp/35-rule-default.log
    ```
    Exit code MUST be 0.

    **Step 3 — Rule on positive fixture (MUST exit 0):**
    ```bash
    bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file tests/fixtures/35-good-pin.md > /tmp/35-fixture-good.log 2>&1
    echo "exit: $?" >> /tmp/35-fixture-good.log
    ```

    **Step 4 — Rule on negative fixture (MUST exit 1; this is the intentional-failure check):**
    ```bash
    bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file tests/fixtures/35-bad-pin.md > /tmp/35-fixture-bad.log 2>&1
    echo "exit: $?" >> /tmp/35-fixture-bad.log
    ```
    Exit code MUST be 1. Record both the FAIL message and the exit 1 as evidence that the rule catches violations.

    **Step 5 — Protected-pipelines SHA-256 check:**
    ```bash
    bash orq-agent/scripts/check-protected-pipelines.sh > /tmp/35-protected.log 2>&1
    echo "exit: $?" >> /tmp/35-protected.log
    ```
    Exit code MUST be 0. If non-zero, one of the protected command files was perturbed during Plans 02/03/04 — diagnose, revert, re-run before writing VERIFICATION.md.

    **Step 6 — Pre-existing 4-rule regression sweep:**
    ```bash
    for rule in allowed-tools tools-declared required-sections references-multi-consumer; do
      echo "=== --rule $rule ===" >> /tmp/35-per-rule.log
      bash orq-agent/scripts/lint-skills.sh --rule "$rule" >> /tmp/35-per-rule.log 2>&1
      echo "exit: $?" >> /tmp/35-per-rule.log
    done
    ```
    Every rule MUST exit 0.

    **Step 7 — Phrase-presence greps (capture outcomes for the traceability table):**
    ```bash
    {
      echo "=== researcher.md phrase checks ==="
      for phrase in "capable-first" "after quality baseline run" "cascade-candidate" "quality-equivalence experiment" "## Model Selection Policy"; do
        if grep -q "$phrase" orq-agent/agents/researcher.md; then echo "✓ $phrase"; else echo "✗ $phrase"; fi
      done
      echo "=== spec-generator.md phrase checks ==="
      for phrase in "snapshot-pinned" "regex reject" "alias-only -- pinning unavailable" "cascade-candidate" "Snapshot Pinning Rule (MSEL-02)" "Cascade Block Emission (MSEL-03)"; do
        if grep -q "$phrase" orq-agent/agents/spec-generator.md; then echo "✓ $phrase"; else echo "✗ $phrase"; fi
      done
      echo "=== catalog phrase checks ==="
      for phrase in "## Capable Tier Lookup" "claude-sonnet-4-5-20250929" "claude-opus-4-20250514" "claude-haiku-4-5-20251001" "gpt-4o-2024-11-20"; do
        if grep -q "$phrase" orq-agent/references/orqai-model-catalog.md; then echo "✓ $phrase"; else echo "✗ $phrase"; fi
      done
    } > /tmp/35-phrases.log 2>&1
    ```
    Every line in `/tmp/35-phrases.log` MUST start with `✓`. Any `✗` means a Plan-02/03/04 edit was incomplete or regressed — STOP and diagnose.

    **Step 8 — File inventory:**
    ```bash
    {
      echo "SKILL.md count: $(ls orq-agent/SKILL.md 2>/dev/null | wc -l | tr -d ' ')"
      echo "commands/*.md count: $(ls orq-agent/commands/*.md 2>/dev/null | wc -l | tr -d ' ')"
      echo "agents/*.md count: $(ls orq-agent/agents/*.md 2>/dev/null | wc -l | tr -d ' ')"
      echo "fixtures count: $(ls tests/fixtures/35-*.md 2>/dev/null | wc -l | tr -d ' ')"
    } > /tmp/35-inventory.log
    ```
    Expect: SKILL.md=1, commands=15, agents=17, fixtures=2.

    **Step 9 — Write `35-05-VERIFICATION.md`** with the following structure (use the template from `<interfaces>` above as the skeleton):

    - Header: `# Phase 35 Verification`, dated (use `2026-04-20` or actual run date), status `COMPLETE`.
    - **Captured green output section:**
      - Paste `/tmp/35-lint-full.log` verbatim in a fenced code block.
      - Paste `/tmp/35-rule-default.log` verbatim.
      - Paste `/tmp/35-fixture-good.log` verbatim.
      - Paste `/tmp/35-fixture-bad.log` verbatim (explicitly annotate `exit: 1 — INTENTIONAL; rule correctly rejects floating-alias fixture`).
      - Paste `/tmp/35-protected.log` verbatim.
      - Paste `/tmp/35-per-rule.log` verbatim.
      - Paste `/tmp/35-phrases.log` verbatim.
      - Paste `/tmp/35-inventory.log` verbatim.
    - **MSEL requirement traceability table** — 3 rows (MSEL-01, MSEL-02, MSEL-03) with columns:
      - Req
      - Description (verbatim from REQUIREMENTS.md lines 118-120)
      - Check (lint rule name + specific grep patterns)
      - Evidence command (exactly reproducible)
      - Green? ✓ (cite the log line)
    - **ROADMAP Phase 35 Success Criteria checklist** — 4 rows, one per criterion from ROADMAP.md lines 138-142. Each row: criterion text verbatim (quoted), evidence (log reference), status ✓ (or note if manual smoke is deferred per 35-VALIDATION.md Manual-Only Verifications table).
    - **Manual-only deferred verifications** — reproduce the 2 rows from `35-VALIDATION.md` §Manual-Only Verifications table. Label them `DEFERRED to /gsd:verify-work`; they are NOT blockers for Phase 35 mechanical close.
    - **File inventory** — counts from Step 8 with pass markers.
    - **Downstream consumer note** — one paragraph stating: "Phases 36-43 must call `bash orq-agent/scripts/lint-skills.sh` on any new skill files before marking themselves complete (Phase 34 lateral invariant). Phase 35 adds the invariant that any new `model:` line in any new skill file must pass the `snapshot-pinned-models` rule. CI wiring is owned by Phase 43 (DIST)."
    - **Deferred/open items:**
      - "Embedding/speech alias-only models: none currently appear in the default file set. If a future skill file adds an alias-only embedding or speech model, it MUST carry the comment `# alias-only -- pinning unavailable <YYYY-MM-DD>` per the spec-generator rule. An explicit allow-list in the lint rule is NOT implemented (YAGNI) — revisit if such a model surfaces."
      - "LLM-output semantic verification (Success Criteria 1 + 4 require LLM-level smoke): deferred to /gsd:verify-work. Grep-level checks prove the policy text is in place; LLM adherence at runtime is a runtime property validated by manual canned-input smoke during verify-work."

    **Step 10 — Self-verify the VERIFICATION.md is well-formed:**
    ```bash
    test -s .planning/phases/35-model-selection-discipline/35-05-VERIFICATION.md
    grep -q "MSEL-01" .planning/phases/35-model-selection-discipline/35-05-VERIFICATION.md
    grep -q "MSEL-02" .planning/phases/35-model-selection-discipline/35-05-VERIFICATION.md
    grep -q "MSEL-03" .planning/phases/35-model-selection-discipline/35-05-VERIFICATION.md
    grep -q "COMPLETE" .planning/phases/35-model-selection-discipline/35-05-VERIFICATION.md
    grep -q "exit: 0" .planning/phases/35-model-selection-discipline/35-05-VERIFICATION.md
    grep -q "exit: 1" .planning/phases/35-model-selection-discipline/35-05-VERIFICATION.md  # from the negative fixture
    ```
    All must exit 0.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/nickcrutzen/developer/orqai-agent-pipeline && bash orq-agent/scripts/lint-skills.sh && bash orq-agent/scripts/check-protected-pipelines.sh && bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models && bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file tests/fixtures/35-good-pin.md && ! bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file tests/fixtures/35-bad-pin.md && test -s .planning/phases/35-model-selection-discipline/35-05-VERIFICATION.md && grep -q "MSEL-01" .planning/phases/35-model-selection-discipline/35-05-VERIFICATION.md && grep -q "MSEL-02" .planning/phases/35-model-selection-discipline/35-05-VERIFICATION.md && grep -q "MSEL-03" .planning/phases/35-model-selection-discipline/35-05-VERIFICATION.md && grep -q "COMPLETE" .planning/phases/35-model-selection-discipline/35-05-VERIFICATION.md && grep -q "exit: 0" .planning/phases/35-model-selection-discipline/35-05-VERIFICATION.md && grep -q "exit: 1" .planning/phases/35-model-selection-discipline/35-05-VERIFICATION.md'</automated>
  </verify>
  <acceptance_criteria>
    - `bash orq-agent/scripts/lint-skills.sh` (no args, full suite across all 5 rules and 33 files) exits 0
    - `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models` (default set) exits 0
    - `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file tests/fixtures/35-good-pin.md` exits 0
    - `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file tests/fixtures/35-bad-pin.md` exits 1 (intentional FAIL)
    - `bash orq-agent/scripts/lint-skills.sh --rule allowed-tools` exits 0
    - `bash orq-agent/scripts/lint-skills.sh --rule tools-declared` exits 0
    - `bash orq-agent/scripts/lint-skills.sh --rule required-sections` exits 0
    - `bash orq-agent/scripts/lint-skills.sh --rule references-multi-consumer` exits 0
    - `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0
    - All 4 researcher.md phrases present (`capable-first`, `after quality baseline run`, `cascade-candidate`, `quality-equivalence experiment`): greps exit 0
    - All 4 spec-generator.md phrases present (`snapshot-pinned`, `regex reject`, `alias-only -- pinning unavailable`, `cascade-candidate`): greps exit 0
    - Catalog `## Capable Tier Lookup` present with 4 category rows: greps exit 0
    - `.planning/phases/35-model-selection-discipline/35-05-VERIFICATION.md` exists, non-empty
    - VERIFICATION.md contains literal strings `MSEL-01`, `MSEL-02`, `MSEL-03` (all 3 requirement IDs)
    - VERIFICATION.md contains literal string `COMPLETE` (phase status)
    - VERIFICATION.md contains `exit: 0` at least 6 times (captured green outputs)
    - VERIFICATION.md contains `exit: 1` at least once (intentional negative-fixture FAIL)
    - VERIFICATION.md contains a traceability table with 3 rows (MSEL-01, MSEL-02, MSEL-03)
    - VERIFICATION.md contains the 4-row ROADMAP Phase 35 Success Criteria checklist
    - VERIFICATION.md cites Manual-Only Verifications as deferred to /gsd:verify-work
    - File counts: 1 × SKILL.md, 15 × commands/*.md, 17 × agents/*.md, 2 × fixtures (33 + 2 total)
    - Zero modifications under `orq-agent/`, `tests/fixtures/`, or any Phase 35 PLAN/SUMMARY file (other than this task's own VERIFICATION.md write)
  </acceptance_criteria>
  <done>
    Phase 35 is mechanically verified: full-suite lint passes on all 33 skill files across all 5 rules, snapshot-pinning rule correctly differentiates positive/negative fixtures, protected-pipeline hash check confirms byte-identical `<pipeline>` blocks for the 3 protected entry points, all 3 MSEL requirements are traceable to lint rules or grep-verifiable phrases with green output, and all 4 ROADMAP Phase 35 Success Criteria are checked off with evidence (grep/lint green at the file level; LLM-level smoke deferred to `/gsd:verify-work` per Phase 35's Manual-Only Verifications table). Downstream phases (36-43) inherit the snapshot-pinning invariant.
  </done>
</task>

</tasks>

<verification>
This plan IS the verification step. Its own verify step runs the full suite:

1. `bash orq-agent/scripts/lint-skills.sh` → exits 0 (all 33 files, all 5 rules).
2. `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models` (default set) → exits 0.
3. `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file tests/fixtures/35-good-pin.md` → exits 0.
4. `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file tests/fixtures/35-bad-pin.md` → exits 1 (intentional).
5. `bash orq-agent/scripts/check-protected-pipelines.sh` → exits 0.
6. `35-05-VERIFICATION.md` exists with all captured output, 3-row MSEL traceability, 4-row ROADMAP criteria checklist, and deferred-items section.

No code files are modified by this plan.
</verification>

<success_criteria>
- [ ] `bash orq-agent/scripts/lint-skills.sh` exits 0 (full suite, 5 rules × 33 files)
- [ ] `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models` (default set) exits 0
- [ ] Positive fixture exits 0, negative fixture exits 1
- [ ] 4 pre-existing rules (allowed-tools, tools-declared, required-sections, references-multi-consumer) all exit 0
- [ ] `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0
- [ ] `.planning/phases/35-model-selection-discipline/35-05-VERIFICATION.md` exists with all required sections
- [ ] 3-row MSEL traceability table in VERIFICATION.md
- [ ] 4-row ROADMAP Phase 35 Success Criteria checklist in VERIFICATION.md, all ✓
- [ ] Manual-only verifications (LLM smoke, semantic spec-diff) explicitly deferred to /gsd:verify-work
- [ ] File counts confirmed (33 skill files + 2 fixtures)
- [ ] Zero modifications under `orq-agent/`, `tests/fixtures/`, or any Phase 35 PLAN/SUMMARY file
</success_criteria>

<output>
After completion, create `.planning/phases/35-model-selection-discipline/35-05-SUMMARY.md` documenting:
- Phase 35 status: COMPLETE / BLOCKED
- Quoted exit codes for all 9+ verification commands (full lint, isolated snapshot-pinned-models on default set + 2 fixtures, protected-pipelines, 4 per-rule regressions, phrase-presence sweep)
- Count of files in scope (33 default-set + 2 fixtures) and count passing
- Pointer to `35-05-VERIFICATION.md` as the full evidence file
- Downstream handoff: "Phases 36-43 inherit the snapshot-pinning invariant: any new `model:` line in any new skill file must pass `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models` before the phase's own /gsd:verify-work. CI wiring owned by Phase 43 (DIST)."
- Manual-smoke TODOs for /gsd:verify-work: (1) invoke `/orq-agent:research "<canned use case>"` and confirm capable-tier Primary + budget alternatives tagged; (2) invoke `/orq-agent "<canned use case>"` before/after Phase 35 and diff spec-JSON to confirm only `model:` fields differ and differences are pin-snapshots of previously-floating aliases.
</output>
