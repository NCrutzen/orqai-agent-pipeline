---
phase: 35-model-selection-discipline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/scripts/lint-skills.sh
  - tests/fixtures/35-bad-pin.md
  - tests/fixtures/35-good-pin.md
autonomous: true
requirements:
  - MSEL-02
must_haves:
  truths:
    - "`bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models` is a valid invocation (does not exit 1 with 'Unknown rule')"
    - "On a fixture containing `model: openai/gpt-4o-latest`, the rule exits 1 and prints a FAIL line naming the offending file"
    - "On a fixture containing `model: anthropic/claude-sonnet-4-5-20250929`, the rule exits 0"
    - "The embedding/speech alias exception is documented inline as a bash comment in the rule block"
    - "All 4 pre-existing rules (allowed-tools, tools-declared, required-sections, references-multi-consumer) still exit 0 on the default file set"
  artifacts:
    - path: "orq-agent/scripts/lint-skills.sh"
      provides: "New rule `snapshot-pinned-models` that rejects floating-alias suffixes (-latest|:latest|-beta) on any `model:` line"
      contains: "snapshot-pinned-models"
    - path: "tests/fixtures/35-bad-pin.md"
      provides: "Negative fixture with `model: openai/gpt-4o-latest` proving the rule catches the failure"
      contains: "model: openai/gpt-4o-latest"
    - path: "tests/fixtures/35-good-pin.md"
      provides: "Positive fixture with a dated snapshot `model: anthropic/claude-sonnet-4-5-20250929` proving the rule passes on good pins"
      contains: "model: anthropic/claude-sonnet-4-5-20250929"
  key_links:
    - from: "orq-agent/scripts/lint-skills.sh rule dispatcher (case statement)"
      to: "new function `check_snapshot_pinned_models`"
      via: "case branch `snapshot-pinned-models) check_snapshot_pinned_models \"$f\" ;;` added in both `run_rule_on_file` and `run_rule_on_default_set`"
      pattern: "snapshot-pinned-models\\)"
    - from: "rule implementation"
      to: "negative fixture"
      via: "rule runs `grep -En 'model:[[:space:]]*[^[:space:]]+(-latest|:latest|-beta)[[:space:]]*$'` and emits FAIL when a match is found"
      pattern: "\\(\\-latest\\|:latest\\|\\-beta\\)"
---

<objective>
Extend `orq-agent/scripts/lint-skills.sh` with a new rule `snapshot-pinned-models` that enforces MSEL-02: every `model:` line in a skill file MUST pin to a dated snapshot. Floating-alias suffixes (`-latest`, `:latest`, `-beta`) are rejected with exit 1. Ship two fixtures (`tests/fixtures/35-bad-pin.md` negative, `tests/fixtures/35-good-pin.md` positive) that prove the rule behaves correctly, and document the embedding/speech alias exception inline.

Purpose: Phase 35 lands the text policy in `researcher.md` and `spec-generator.md` (Plans 02/03), but without a mechanical enforcement we cannot guarantee that future edits preserve snapshot pinning. This plan ships the enforcement layer first so downstream plans' edits can be validated against it.

Output: An extended lint script that accepts `--rule snapshot-pinned-models`, two test fixtures, and a lateral invariant that the pre-existing 4 rules remain green.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-model-selection-discipline/35-CONTEXT.md
@.planning/phases/35-model-selection-discipline/35-VALIDATION.md
@.planning/phases/34-skill-structure-format-foundation/34-01-SUMMARY.md
@orq-agent/scripts/lint-skills.sh

<interfaces>
<!-- Existing lint-skills.sh structure (extracted from the current script). The new rule -->
<!-- MUST follow the same dispatcher pattern: usage block update + check function + -->
<!-- case branches in BOTH run_rule_on_file AND run_rule_on_default_set. -->

Rule registration points in `orq-agent/scripts/lint-skills.sh`:

1. `usage()` — `Rule IDs:` block (add one line for the new rule so `--help` documents it)
2. `check_snapshot_pinned_models()` — new function (follow the pattern of `check_allowed_tools`/`check_tools_declared`)
3. `run_rule_on_file()` — add case branch `snapshot-pinned-models) check_snapshot_pinned_models "$f" ;;`
4. `run_rule_on_default_set()` — add case branch that iterates the default file set and calls `check_snapshot_pinned_models`
5. `lint_file_all_rules()` — call `check_snapshot_pinned_models "$f"` so it runs by default on every file

Existing `emit_fail()` helper prints `FAIL: ...` and sets `fail=1`. Use it unchanged.

**Regex for the rule** (MUST use this exact BRE-compatible grep pattern):
`model:[[:space:]]*[^[:space:]]+(-latest|:latest|-beta)[[:space:]]*$`

Invoke as `grep -En` (extended regex, line numbers) so the FAIL message can include the line number.

**Embedding/speech alias exception** (from 35-CONTEXT.md §decisions):
Orq.ai only exposes some embedding + speech models as aliases. The rule documents this via an inline bash comment listing known-alias-only provider prefixes. For Phase 35 we do NOT implement an allow-list (YAGNI — no such alias currently appears in any skill file); the comment + the `# alias-only — pinning unavailable 2026-04-20` convention for spec-generator is sufficient. Documenting here keeps audit-trail clean when a future alias surfaces.

**Default file set** (unchanged, defined in `default_file_set()`):
- `orq-agent/SKILL.md`
- `orq-agent/commands/*.md` (15 files)
- `orq-agent/agents/*.md` (17 files)

Fixture files live OUTSIDE the default file set (under `tests/fixtures/`) so they do NOT pollute the full-suite green run. The rule is invoked against them explicitly via `--rule snapshot-pinned-models --file <fixture>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 fixtures for snapshot-pinning rule</name>
  <files>tests/fixtures/35-bad-pin.md, tests/fixtures/35-good-pin.md</files>
  <read_first>
    - .planning/phases/35-model-selection-discipline/35-CONTEXT.md (locked decisions, especially the regex and exception)
    - .planning/phases/35-model-selection-discipline/35-VALIDATION.md (Wave 0 Requirements + Per-Task map)
    - orq-agent/scripts/lint-skills.sh (existing rule patterns)
  </read_first>
  <behavior>
    - Negative fixture (`tests/fixtures/35-bad-pin.md`): MUST contain exactly one `model:` line with a floating alias suffix so a future run of `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file tests/fixtures/35-bad-pin.md` exits 1.
    - Positive fixture (`tests/fixtures/35-good-pin.md`): MUST contain exactly one `model:` line with a dated snapshot so the same invocation exits 0.
    - Both files use minimal skill-file shape (YAML frontmatter delimiters + a `model:` line) — they do NOT need the 9 SKST sections because they are test inputs, not skill files in the default set.
  </behavior>
  <action>
    Create the `tests/fixtures/` directory if it does not exist (`mkdir -p tests/fixtures`).

    **Write `tests/fixtures/35-bad-pin.md` with this EXACT content:**

    ```
    ---
    name: test-bad-pin
    description: Negative fixture for snapshot-pinned-models rule (Phase 35 MSEL-02). Contains a floating-alias model ID that the rule MUST reject.
    tools: Read
    model: openai/gpt-4o-latest
    ---

    # Test fixture — bad pin

    This file intentionally uses a floating alias (`openai/gpt-4o-latest`) to prove the `snapshot-pinned-models` lint rule catches non-pinned model IDs.
    ```

    **Write `tests/fixtures/35-good-pin.md` with this EXACT content:**

    ```
    ---
    name: test-good-pin
    description: Positive fixture for snapshot-pinned-models rule (Phase 35 MSEL-02). Contains a dated-snapshot model ID that the rule MUST accept.
    tools: Read
    model: anthropic/claude-sonnet-4-5-20250929
    ---

    # Test fixture — good pin

    This file uses a dated snapshot (`anthropic/claude-sonnet-4-5-20250929`) to prove the `snapshot-pinned-models` lint rule passes on correctly pinned model IDs.
    ```

    Do NOT add these files to `default_file_set()` in `lint-skills.sh` — they stay out-of-band so the full-suite run remains green.
  </action>
  <verify>
    <automated>test -s /Users/nickcrutzen/developer/orqai-agent-pipeline/tests/fixtures/35-bad-pin.md && test -s /Users/nickcrutzen/developer/orqai-agent-pipeline/tests/fixtures/35-good-pin.md && grep -qF "model: openai/gpt-4o-latest" /Users/nickcrutzen/developer/orqai-agent-pipeline/tests/fixtures/35-bad-pin.md && grep -qF "model: anthropic/claude-sonnet-4-5-20250929" /Users/nickcrutzen/developer/orqai-agent-pipeline/tests/fixtures/35-good-pin.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -s tests/fixtures/35-bad-pin.md` exits 0 (file exists, non-empty)
    - `test -s tests/fixtures/35-good-pin.md` exits 0
    - `grep -qF "model: openai/gpt-4o-latest" tests/fixtures/35-bad-pin.md` exits 0
    - `grep -qF "model: anthropic/claude-sonnet-4-5-20250929" tests/fixtures/35-good-pin.md` exits 0
    - `grep -cE "^model:" tests/fixtures/35-bad-pin.md` returns `1` (exactly one model line)
    - `grep -cE "^model:" tests/fixtures/35-good-pin.md` returns `1`
    - Neither fixture file path appears anywhere inside `orq-agent/scripts/lint-skills.sh` (stays out of default set): `grep -q "tests/fixtures" orq-agent/scripts/lint-skills.sh` exits 1
  </acceptance_criteria>
  <done>Fixtures exist, contain the exact model lines required by the rule regex, and live OUTSIDE the default lint file set so the pre-existing full-suite run remains unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend lint-skills.sh with snapshot-pinned-models rule</name>
  <files>orq-agent/scripts/lint-skills.sh</files>
  <read_first>
    - .planning/phases/35-model-selection-discipline/35-CONTEXT.md (locked decisions on regex + exception)
    - .planning/phases/35-model-selection-discipline/35-VALIDATION.md (Per-Task Verification Map 35-01-01, 35-01-02)
    - orq-agent/scripts/lint-skills.sh (the file being modified — study the `check_allowed_tools`, `check_tools_declared`, `run_rule_on_file`, `run_rule_on_default_set`, `lint_file_all_rules` patterns)
  </read_first>
  <behavior>
    - `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file tests/fixtures/35-bad-pin.md` MUST exit 1 and emit a FAIL line containing the path of the fixture.
    - `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file tests/fixtures/35-good-pin.md` MUST exit 0 with no FAIL lines.
    - `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models` (no file — default set) MUST exit 0 because no file under `orq-agent/` currently uses a floating alias in a `model:` line (confirm with a pre-edit grep).
    - `bash orq-agent/scripts/lint-skills.sh --help` MUST document the new rule in the `Rule IDs:` block.
    - The 4 pre-existing rules MUST still exit 0 on the default file set (no regressions).
    - `bash orq-agent/scripts/check-protected-pipelines.sh` MUST still exit 0 (this plan touches a script, not a command file, so protected pipelines are unaffected).
  </behavior>
  <action>
    Edit `orq-agent/scripts/lint-skills.sh` with FOUR coordinated changes. Do NOT reformat unrelated lines.

    **Change 1 — Update `usage()` heredoc.** In the `Rule IDs:` block (currently listing 4 rules), add ONE new line AFTER the `references-multi-consumer` line:

    ```
      snapshot-pinned-models     every 'model:' line pins a dated snapshot; rejects -latest/:latest/-beta aliases (MSEL-02)
    ```

    Keep column alignment consistent with the existing 4 entries (spaces, no tabs).

    **Change 2 — Add the `check_snapshot_pinned_models` function.** Insert this function BLOCK immediately after `check_references_multi_consumer()` ends (before the `# --- apply one rule to one file ---` header):

    ```bash
    # --- rule: snapshot-pinned-models (MSEL-02) ---
    # Rejects floating-alias suffixes on any 'model:' line (model: provider/name-latest etc.).
    # Exception: some embedding + speech models Orq.ai only exposes as aliases. When such a
    # model legitimately cannot be pinned, spec-generator emits the alias with the comment
    # '# alias-only -- pinning unavailable <date>' (see orq-agent/agents/spec-generator.md).
    # This rule does NOT grant automatic allow-list bypass; any alias-only line must either
    # (a) not match the floating-suffix regex below, or (b) be explicitly grep-excluded in a
    # future iteration when a concrete alias-only model surfaces in the skill set.
    check_snapshot_pinned_models() {
      local f="$1"
      local hits
      hits=$(grep -nE '^[[:space:]]*-?[[:space:]]*model:[[:space:]]*[^[:space:]]+(-latest|:latest|-beta)[[:space:]]*$' "$f" || true)
      if [ -n "$hits" ]; then
        while IFS= read -r line; do
          emit_fail "${f}:${line%%:*} — floating-alias model ID (use a dated snapshot, e.g. claude-sonnet-4-5-20250929) [MSEL-02]"
        done <<< "$hits"
      fi
    }
    ```

    **Change 3 — Register the rule in `run_rule_on_file()`.** Add this case branch inside the `case "$rule" in` block, BEFORE the final `*)` (unknown rule) branch:

    ```bash
        snapshot-pinned-models)
          check_snapshot_pinned_models "$f"
          ;;
    ```

    **Change 4 — Register the rule in `run_rule_on_default_set()`.** Add this case branch in the same spot:

    ```bash
        snapshot-pinned-models)
          while IFS= read -r f; do check_snapshot_pinned_models "$f"; done < <(default_file_set)
          ;;
    ```

    **Change 5 — Wire the rule into `lint_file_all_rules()`.** Inside `lint_file_all_rules()`, AFTER the existing `is_subagent`/`check_allowed_tools`/`check_tools_declared` branch, add one line:

    ```bash
      check_snapshot_pinned_models "$f"
    ```

    This makes the rule run by default (no-flags invocation) on every file, same as `required-sections`.

    **Do NOT add fixtures to `default_file_set()`.** The function must remain unchanged.

    **Verification sequence after edit:**

    1. Pre-edit grep for existing floating aliases in the default set (should be 0 hits):
       ```bash
       grep -rnE '^[[:space:]]*-?[[:space:]]*model:[[:space:]]*[^[:space:]]+(-latest|:latest|-beta)[[:space:]]*$' orq-agent/SKILL.md orq-agent/commands orq-agent/agents || echo "clean"
       ```
       If this returns hits, STOP — the default-set run will go red; log the hit and escalate via 35-SUMMARY for Plans 02/03/04 to fix.

    2. Post-edit rule-on-fixture checks:
       ```bash
       bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file tests/fixtures/35-bad-pin.md ; echo "exit: $?"   # must be 1
       bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file tests/fixtures/35-good-pin.md ; echo "exit: $?"  # must be 0
       ```

    3. Post-edit full-suite check (must stay green):
       ```bash
       bash orq-agent/scripts/lint-skills.sh ; echo "exit: $?"   # must be 0
       ```

    4. Post-edit protected-pipelines check:
       ```bash
       bash orq-agent/scripts/check-protected-pipelines.sh ; echo "exit: $?"   # must be 0
       ```

    5. Per-rule green on default set:
       ```bash
       bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models ; echo "exit: $?"   # must be 0
       ```
  </action>
  <verify>
    <automated>bash -c 'cd /Users/nickcrutzen/developer/orqai-agent-pipeline && bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file tests/fixtures/35-good-pin.md && ! bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file tests/fixtures/35-bad-pin.md && bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models && bash orq-agent/scripts/lint-skills.sh && bash orq-agent/scripts/check-protected-pipelines.sh && grep -q "snapshot-pinned-models" orq-agent/scripts/lint-skills.sh && grep -q "check_snapshot_pinned_models" orq-agent/scripts/lint-skills.sh && grep -q "MSEL-02" orq-agent/scripts/lint-skills.sh'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "snapshot-pinned-models" orq-agent/scripts/lint-skills.sh` exits 0 (rule ID registered)
    - `grep -q "check_snapshot_pinned_models" orq-agent/scripts/lint-skills.sh` exits 0 (function defined)
    - `grep -q "MSEL-02" orq-agent/scripts/lint-skills.sh` exits 0 (requirement ID traceable inline)
    - `grep -q "alias-only" orq-agent/scripts/lint-skills.sh` exits 0 (embedding/speech exception documented inline)
    - `grep -c "snapshot-pinned-models)" orq-agent/scripts/lint-skills.sh` returns at least `2` (registered in BOTH run_rule_on_file AND run_rule_on_default_set case blocks)
    - `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file tests/fixtures/35-bad-pin.md` exits 1 and prints a line starting with `FAIL:` containing `tests/fixtures/35-bad-pin.md`
    - `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file tests/fixtures/35-good-pin.md` exits 0 with no `FAIL:` output
    - `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models` (default set, no --file) exits 0
    - `bash orq-agent/scripts/lint-skills.sh` (full suite, no args) exits 0 — all 5 rules pass on all 33 files
    - `bash orq-agent/scripts/lint-skills.sh --rule allowed-tools` exits 0 (no regression)
    - `bash orq-agent/scripts/lint-skills.sh --rule tools-declared` exits 0 (no regression)
    - `bash orq-agent/scripts/lint-skills.sh --rule required-sections` exits 0 (no regression)
    - `bash orq-agent/scripts/lint-skills.sh --rule references-multi-consumer` exits 0 (no regression)
    - `bash orq-agent/scripts/lint-skills.sh --help 2>&1 | grep -q "snapshot-pinned-models"` exits 0 (help text updated)
    - `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0 (protected pipelines untouched)
    - `default_file_set()` unchanged: `grep -q "tests/fixtures" orq-agent/scripts/lint-skills.sh` exits 1 (fixtures stay out of default set)
  </acceptance_criteria>
  <done>
    The lint script gains a fifth rule `snapshot-pinned-models` that mechanically enforces MSEL-02 on every skill file (SKILL.md + commands + agents). The rule rejects `-latest`, `:latest`, `-beta` suffixes, documents the embedding/speech alias exception inline, and keeps the full-suite green baseline intact. Plans 02 and 03 can now edit `researcher.md` and `spec-generator.md` with confidence that any accidental floating-alias introduction will be caught by `--file` linting after each commit.
  </done>
</task>

</tasks>

<verification>
Wave 1 verification — all commands must exit 0 except where explicitly stated:

1. `bash orq-agent/scripts/lint-skills.sh` (full suite) → exit 0
2. `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models` (default set) → exit 0
3. `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file tests/fixtures/35-good-pin.md` → exit 0
4. `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file tests/fixtures/35-bad-pin.md` → exit 1 (intentional FAIL)
5. `bash orq-agent/scripts/check-protected-pipelines.sh` → exit 0
6. All 4 pre-existing per-rule invocations → exit 0
</verification>

<success_criteria>
- [ ] `orq-agent/scripts/lint-skills.sh` has new function `check_snapshot_pinned_models`
- [ ] Rule registered in `usage()`, `run_rule_on_file()`, `run_rule_on_default_set()`, and `lint_file_all_rules()`
- [ ] MSEL-02 traceable inline via grep
- [ ] Embedding/speech alias exception documented inline
- [ ] `tests/fixtures/35-bad-pin.md` exists with `model: openai/gpt-4o-latest`
- [ ] `tests/fixtures/35-good-pin.md` exists with `model: anthropic/claude-sonnet-4-5-20250929`
- [ ] Rule returns exit 1 on bad fixture, exit 0 on good fixture
- [ ] Rule returns exit 0 on default file set (no pre-existing violations)
- [ ] Full lint suite still exits 0 (no regressions on 4 pre-existing rules)
- [ ] Protected-pipelines check still exits 0
- [ ] `--help` documents the new rule
</success_criteria>

<output>
After completion, create `.planning/phases/35-model-selection-discipline/35-01-SUMMARY.md` documenting:
- Rule `snapshot-pinned-models` added to `orq-agent/scripts/lint-skills.sh`
- Exact regex embedded: `model:[[:space:]]*[^[:space:]]+(-latest|:latest|-beta)[[:space:]]*$`
- Fixtures created at `tests/fixtures/35-bad-pin.md` + `tests/fixtures/35-good-pin.md`
- Captured exit codes for all 6 verification commands
- Pre-edit grep confirmed 0 existing floating aliases in the default set
- Downstream handoff: "Plans 02, 03, 04 can now run `bash orq-agent/scripts/lint-skills.sh --file <edited-file>` after each commit; Plan 05 captures the full-suite evidence."
</output>
