---
phase: 35-model-selection-discipline
plan: 02
type: execute
wave: 2
depends_on:
  - "35-01"
files_modified:
  - orq-agent/agents/researcher.md
autonomous: true
requirements:
  - MSEL-01
  - MSEL-03
must_haves:
  truths:
    - "Researcher subagent documents a capable-first policy (primary rec = capable tier) before any cost-driven downgrade"
    - "Budget-profile downgrades are labeled `after quality baseline run` as an alternative, not as the primary"
    - "Researcher proposes a cascade pattern (cheap-first + capable escalation) only in response to explicit cost-optimization requests, tagged `cascade-candidate`"
    - "Every cascade proposal includes a `quality-equivalence experiment` step with explicit Pass-rate comparison guidance; cascade stays `approved: false` until that experiment runs (Phase 42 wires runtime)"
    - "All 9 SKST sections remain intact (`bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/researcher.md` exits 0)"
    - "No floating-alias model IDs introduced (`bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file orq-agent/agents/researcher.md` exits 0)"
  artifacts:
    - path: "orq-agent/agents/researcher.md"
      provides: "Capable-first model recommendation policy + cascade pattern with quality-equivalence experiment instructions"
      contains: "capable-first"
  key_links:
    - from: "researcher.md Model Selection Policy section"
      to: "spec-generator.md Instructions field"
      via: "Researcher emits tagged recommendation (`cascade-candidate: true/false`); spec-generator reads the tag and either emits a single pinned `model:` line or a cascade block with quality-equivalence test instructions"
      pattern: "cascade-candidate"
    - from: "researcher.md cascade block"
      to: "Phase 42 runtime"
      via: "Researcher writes the quality-equivalence experiment instructions as text; Phase 42 (EVLD) wires evaluator infra to execute them. `approved: false` remains the default until Phase 42 closes the loop."
      pattern: "quality-equivalence experiment"
---

<objective>
Update `orq-agent/agents/researcher.md` to embed the capable-first, cascade-aware model selection policy (MSEL-01 + MSEL-03). Add a new Model Selection Policy section that (a) mandates the capable tier as the primary recommendation, (b) labels budget-profile downgrades as alternatives tagged `after quality baseline run`, (c) defines the cascade proposal format with a mandatory `quality-equivalence experiment` step, and (d) preserves all 9 SKST sections that Phase 34 validated.

Purpose: The researcher currently lists capable/cost/vision model categories as options but does NOT enforce ordering or cascade discipline. Without this policy, generated swarms start from the wrong baseline (cheap → biased Pass rate → biased iteration). Phase 35 ships the text policy; the LLM applies it at runtime.

Output: A researcher subagent file that passes `lint-skills.sh --file`, `snapshot-pinned-models --file`, AND contains four specific phrases verifiable by grep: `capable-first`, `after quality baseline run`, `cascade-candidate`, `quality-equivalence experiment`.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-model-selection-discipline/35-CONTEXT.md
@.planning/phases/35-model-selection-discipline/35-VALIDATION.md
@.planning/phases/34-skill-structure-format-foundation/34-03-SUMMARY.md
@orq-agent/agents/researcher.md

<interfaces>
<!-- Key structural constraints from the existing researcher.md (must preserve) -->

Researcher subagent YAML frontmatter (lines 1-7, must remain byte-identical on the required keys):
- `name: orq-researcher`
- `tools: Read, Glob, Grep, WebSearch, WebFetch`
- `model: inherit`

9 SKST sections (must remain intact — Phase 34 validated):
1. `## Constraints` (line ~388) — already mentions `Phase 35 MSEL-01` + `MSEL-02`; this plan REINFORCES that policy with an upstream authoritative section but does NOT remove existing Constraints bullets.
2. `## When to use` (line ~397)
3. `## When NOT to use` (line ~403)
4. `## Companion Skills` (line ~409)
5. `## Done When` (line ~420)
6. `## Destructive Actions` (line ~430)
7. `## Anti-Patterns to Avoid` (line ~434)
8. `## Open in orq.ai` (line ~450)
9. `## Documentation & Resolution` (line ~454)

**Insertion point for the new section:**
Place `## Model Selection Policy` AFTER `## Knowledge Base Design Training Knowledge` (ends around line 123) and BEFORE `## Output Format` (starts around line 124). This positions the policy BEFORE the agent learns the output shape, so the generated recommendation structure reflects the policy.

**Required verbatim phrases** (each MUST appear at least once; grep-verifiable):
1. `capable-first` — in section heading text or inline policy statement
2. `after quality baseline run` — tag applied to budget-profile alternative
3. `cascade-candidate` — tag used when user requested cost optimization
4. `quality-equivalence experiment` — mandatory step inside cascade proposal

**Required structural elements for the new section:**
- A Policy Rules numbered list (MSEL-01 + MSEL-03 + forward-link to MSEL-02 enforced by spec-generator)
- A Capable Tier Lookup subsection referencing `orq-agent/references/orqai-model-catalog.md §Capable Tier Lookup` (the table is added by Plan 04 — forward reference is fine)
- A Cascade Pattern subsection with the exact 4 elements of a cascade proposal (cheap tier, capable tier, confidence-threshold trigger, quality-equivalence experiment)
- A Worked Example showing how to tag a `cascade-candidate` recommendation
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Insert Model Selection Policy section into researcher.md (MSEL-01 + MSEL-03)</name>
  <files>orq-agent/agents/researcher.md</files>
  <read_first>
    - .planning/phases/35-model-selection-discipline/35-CONTEXT.md (all locked decisions, especially §decisions and §specifics)
    - .planning/phases/35-model-selection-discipline/35-VALIDATION.md (Per-Task 35-02-01, 35-02-02, 35-02-03)
    - orq-agent/agents/researcher.md (the file being modified — study existing Constraints block + Output Format rules)
  </read_first>
  <behavior>
    - After edit: `grep -q "capable-first" orq-agent/agents/researcher.md` exits 0
    - After edit: `grep -q "after quality baseline run" orq-agent/agents/researcher.md` exits 0
    - After edit: `grep -q "cascade-candidate" orq-agent/agents/researcher.md` exits 0
    - After edit: `grep -q "quality-equivalence experiment" orq-agent/agents/researcher.md` exits 0
    - After edit: `bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/researcher.md` exits 0 (9 SKST sections intact, `tools:` frontmatter intact)
    - After edit: `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file orq-agent/agents/researcher.md` exits 0 (no floating aliases introduced; illustrative model IDs in the new section MUST use dated snapshots)
    - After edit: full-suite `bash orq-agent/scripts/lint-skills.sh` still exits 0
    - After edit: `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0 (this plan does NOT touch command files)
  </behavior>
  <action>
    Edit `orq-agent/agents/researcher.md`. Insert a new H2 section titled `## Model Selection Policy` IMMEDIATELY AFTER the `## Knowledge Base Design Training Knowledge` section ends (i.e., after the embedding-model-constraint paragraph, before the `## Output Format` H2).

    **Section content** — write this EXACT text (preserve markdown structure, the required verbatim phrases are intentional anchors for grep-based validation):

    ```markdown
    ## Model Selection Policy

    <!-- Phase 35 MSEL-01 + MSEL-03: capable-first, cascade-aware policy enforced in the researcher. -->
    <!-- MSEL-02 (snapshot pinning of emitted IDs) is enforced downstream in spec-generator + the -->
    <!-- `snapshot-pinned-models` lint rule. This section focuses on recommendation ORDERING and -->
    <!-- CASCADE DISCIPLINE; pinning is a write-time concern handled by the spec-generator. -->

    Apply this policy to every Model Recommendation section you emit. It overrides any default "cheapest-first" instinct from training data.

    ### Policy Rules

    1. **capable-first ordering** (MSEL-01): The Primary recommendation MUST be the most capable tier model for the task category (chat-heavy, tool-calling, code/RAG, vision). Never start with a budget-tier model, even when the user has requested cost optimization. Capable-first establishes a quality baseline; cost-driven downgrades are proposed as alternatives, not as primaries.

    2. **Budget alternatives tagged `after quality baseline run`** (MSEL-01): When you include budget-profile alternatives in the Alternatives list, label them explicitly: `-- after quality baseline run: [trade-off]`. This signals to the user (and to downstream spec-generator + iterator) that the cheaper model is a downgrade path to be taken AFTER the capable-tier baseline has produced a measured Pass rate, not BEFORE.

    3. **Cascade-aware pattern** (MSEL-03): Only when the user explicitly requests cost optimization during discussion (e.g., budget_profile=cost-first, or user said "cheapest possible" / "minimize cost"), propose a two-tier cascade. Tag the recommendation `cascade-candidate: true` in the Model Recommendation section so spec-generator knows to emit the cascade block + quality-equivalence test instructions. For normal use cases (no explicit cost-optimization request), tag `cascade-candidate: false` and emit a single capable-tier Primary.

    4. **Quality-equivalence gate** (MSEL-03): Every cascade proposal MUST include a `quality-equivalence experiment` step. The cascade is `approved: false` until that experiment runs, produces a measurable Pass-rate comparison between the cheap tier and the capable tier on the same test dataset, and shows that the cheap tier is within an acceptable tolerance (user-defined; suggest 5 percentage points as a default). Do NOT mark a cascade `approved: true` in your output — that flag flips downstream after Phase 42 (Evaluator Validation) runtime wires the experiment.

    ### Capable Tier Lookup

    Use the table at `orq-agent/references/orqai-model-catalog.md §Capable Tier Lookup` as your starting point for selecting the capable-tier Primary. This table maps task categories (chat-heavy, tool-calling, code/RAG, vision) to dated-snapshot model IDs (e.g., `anthropic/claude-sonnet-4-5-20250929`). **Always validate the chosen ID against the live workspace via the MCP `models-list` tool** before committing to it — the static table is a seed, not authoritative.

    When the live list does not expose the recommended capable-tier ID, fall back to the next-best available capable-tier option from the same provider family, then flag the substitution as `Confidence: MEDIUM — capable-tier substitute applied (original: <id>)`.

    ### Cascade Pattern

    When `cascade-candidate: true`, structure the Model Recommendation as follows:

    ```
    **Primary (cascade, cheap tier):** `provider/cheap-model-DATED-SNAPSHOT`
    **Escalation (cascade, capable tier):** `provider/capable-model-DATED-SNAPSHOT`
    **Trigger:** escalate when cheap-tier confidence score < 0.7 (or task-specific threshold; suggest in the Rationale)
    **Rationale:** [why this split is viable for this role -- specifically, what signals predict when the cheap tier will fail]
    **cascade-candidate:** true
    **approved:** false
    **quality-equivalence experiment:**
      - Run the swarm's test dataset through BOTH tiers independently.
      - Use the same evaluator suite (see Phase 42 EVLD wiring) for both runs.
      - Compute Pass-rate delta (capable_tier_pass_rate - cheap_tier_pass_rate).
      - If delta <= 5 percentage points (default tolerance; user may override in discussion), cascade is viable. Mark `approved: true` only after this experiment closes; Phase 42 runtime flips the flag.
      - If delta > tolerance, fall back to capable-tier Primary and record the delta in the research brief's Confidence field.
    **Confidence:** LOW until quality-equivalence experiment runs
    ```

    For non-cascade (the default path):

    ```
    **Primary:** `provider/capable-model-DATED-SNAPSHOT`
    **cascade-candidate:** false
    **Rationale:** [capable-first rationale -- specific capability match, not generic praise]
    **Alternatives:**
    1. `provider/capable-alt-DATED-SNAPSHOT` -- [capable-tier alternative, different provider, same quality band]
    2. `provider/budget-model-DATED-SNAPSHOT` -- after quality baseline run: [cost trade-off + latency impact]
    3. `provider/fast-model-DATED-SNAPSHOT` -- after quality baseline run: [latency trade-off]
    ```

    Note the budget alternative carries the `after quality baseline run` tag verbatim.

    ### Why this policy

    Starting with cheap models produces biased baselines: the iterator's first Pass-rate measurement reflects the cheap model's ceiling, not the task's inherent difficulty. Downstream iteration then confuses "this task is hard" with "this model is weak." The capable-first policy forces a true baseline; the cascade discipline provides a principled, test-backed path to cost reduction without sacrificing signal.
    ```

    (The inner code fences above use four backticks in Claude's view; when writing the actual file, use three-backtick fences as shown in the markdown content.)

    **Update the existing Constraints section** (already near line ~388) by REPLACING the current line:
    > `- **NEVER** start with the cheapest model — capable-first per Phase 35 MSEL-01.`

    with the slightly reinforced version (adds the `cascade-candidate` + `after quality baseline run` hooks by reference so the Constraints block points into the new section for detail):

    ```
    - **NEVER** start with the cheapest model — capable-first per Phase 35 MSEL-01. Budget alternatives carry the `after quality baseline run` tag. Cascade proposals (`cascade-candidate: true`) only in response to explicit cost-optimization requests and MUST include a `quality-equivalence experiment` step. See `## Model Selection Policy` above for the full protocol.
    ```

    **Do NOT modify** any other Constraints bullet, any frontmatter key, or the `<files_to_read>` block. Those are covered by Phase 34 SKST invariants.

    **Verification after edit:**

    1. Four phrase-presence greps (all must exit 0):
       ```bash
       grep -q "capable-first" orq-agent/agents/researcher.md
       grep -q "after quality baseline run" orq-agent/agents/researcher.md
       grep -q "cascade-candidate" orq-agent/agents/researcher.md
       grep -q "quality-equivalence experiment" orq-agent/agents/researcher.md
       ```

    2. SKST + snapshot-pin lint on just the modified file (must exit 0):
       ```bash
       bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/researcher.md
       bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file orq-agent/agents/researcher.md
       ```

    3. Full suite (must still exit 0):
       ```bash
       bash orq-agent/scripts/lint-skills.sh
       ```

    4. Protected pipelines (must still exit 0; this plan does not touch command files):
       ```bash
       bash orq-agent/scripts/check-protected-pipelines.sh
       ```
  </action>
  <verify>
    <automated>bash -c 'cd /Users/nickcrutzen/developer/orqai-agent-pipeline && grep -q "capable-first" orq-agent/agents/researcher.md && grep -q "after quality baseline run" orq-agent/agents/researcher.md && grep -q "cascade-candidate" orq-agent/agents/researcher.md && grep -q "quality-equivalence experiment" orq-agent/agents/researcher.md && grep -q "## Model Selection Policy" orq-agent/agents/researcher.md && bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/researcher.md && bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file orq-agent/agents/researcher.md && bash orq-agent/scripts/lint-skills.sh && bash orq-agent/scripts/check-protected-pipelines.sh'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "## Model Selection Policy" orq-agent/agents/researcher.md` exits 0 (new H2 section present)
    - `grep -q "capable-first" orq-agent/agents/researcher.md` exits 0 (MSEL-01 ordering phrase)
    - `grep -q "after quality baseline run" orq-agent/agents/researcher.md` exits 0 (budget-alternative tag)
    - `grep -q "cascade-candidate" orq-agent/agents/researcher.md` exits 0 (MSEL-03 cascade tag)
    - `grep -q "quality-equivalence experiment" orq-agent/agents/researcher.md` exits 0 (mandatory cascade step)
    - `grep -q "MSEL-01" orq-agent/agents/researcher.md` exits 0 (requirement ID traceable)
    - `grep -q "MSEL-03" orq-agent/agents/researcher.md` exits 0 (requirement ID traceable)
    - `grep -q "Capable Tier Lookup" orq-agent/agents/researcher.md` exits 0 (forward-ref to catalog table added by Plan 04)
    - `grep -q "approved: false" orq-agent/agents/researcher.md` exits 0 (cascade-approval guard)
    - `bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/researcher.md` exits 0 (9 SKST sections intact)
    - `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file orq-agent/agents/researcher.md` exits 0 (no floating aliases introduced in illustrative IDs)
    - `bash orq-agent/scripts/lint-skills.sh` (full suite) exits 0 (no regressions anywhere)
    - `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0 (command files untouched)
    - Frontmatter `tools: Read, Glob, Grep, WebSearch, WebFetch` unchanged: `grep -c "^tools: Read, Glob, Grep, WebSearch, WebFetch$" orq-agent/agents/researcher.md` returns `1`
    - `<files_to_read>` block unchanged: `grep -c "^<files_to_read>$" orq-agent/agents/researcher.md` returns `1`
  </acceptance_criteria>
  <done>
    Researcher subagent now enforces MSEL-01 (capable-first ordering + `after quality baseline run` alternative tag) and MSEL-03 (`cascade-candidate` flag + `quality-equivalence experiment` mandatory step). All 9 SKST sections pass lint. No floating aliases introduced. Frontmatter and `<files_to_read>` unchanged. Ready for Plan 03 (spec-generator snapshot pinning) and Plan 04 (catalog Capable Tier table) to execute in parallel within Wave 2.
  </done>
</task>

</tasks>

<verification>
Wave 2 (Plan 02) verification — researcher.md edits pass all phrase-presence + SKST + snapshot-pin checks:

1. 4 required verbatim phrases present (`capable-first`, `after quality baseline run`, `cascade-candidate`, `quality-equivalence experiment`).
2. `lint-skills.sh --file` exits 0 (9 SKST sections intact).
3. `lint-skills.sh --rule snapshot-pinned-models --file` exits 0 (no floating aliases in illustrative IDs).
4. Full-suite lint + protected-pipeline check still exit 0.
5. Frontmatter and `<files_to_read>` unchanged.
</verification>

<success_criteria>
- [ ] New `## Model Selection Policy` section inserted before `## Output Format`
- [ ] 4 mandatory phrases present and grep-verifiable
- [ ] MSEL-01 + MSEL-03 requirement IDs traceable inline
- [ ] Capable Tier Lookup subsection forward-references the catalog table (Plan 04 delivers)
- [ ] Cascade Pattern subsection has all 4 elements (cheap, capable, trigger, quality-equivalence experiment)
- [ ] Existing Constraints bullet reinforced without breaking SKST
- [ ] `tools:` frontmatter + `<files_to_read>` unchanged
- [ ] `lint-skills.sh --file` exits 0
- [ ] `lint-skills.sh --rule snapshot-pinned-models --file` exits 0
- [ ] Full-suite lint + protected-pipeline check still exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/35-model-selection-discipline/35-02-SUMMARY.md` documenting:
- New `## Model Selection Policy` section added to researcher.md with line number
- 4 verbatim phrases confirmed via grep with line numbers
- Captured exit codes for all verification commands
- Confirmation that `tools:` frontmatter and `<files_to_read>` are unchanged
- Downstream handoff: "Plan 03 (spec-generator) and Plan 04 (catalog) can now run in parallel within Wave 2. Plan 05 captures the full verification evidence."
</output>
