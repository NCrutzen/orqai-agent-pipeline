---
phase: 35-model-selection-discipline
plan: 04
type: execute
wave: 2
depends_on:
  - "35-01"
files_modified:
  - orq-agent/references/orqai-model-catalog.md
autonomous: true
requirements:
  - MSEL-01
must_haves:
  truths:
    - "`orq-agent/references/orqai-model-catalog.md` has a `## Capable Tier Lookup` H2 section"
    - "The Capable Tier Lookup table maps 4 task categories (chat-heavy, tool-calling, code/RAG, fast-triage) to dated-snapshot model IDs"
    - "The WARNING at the top of the file is preserved (live selection via MCP `models-list`); the new section does NOT contradict it"
    - "All model IDs in the new section are dated snapshots (no `-latest`, `:latest`, `-beta` suffixes) — lint snapshot-pinned-models rule passes on this file"
    - "The references-multi-consumer rule remains green (file is consumed by researcher.md and spec-generator.md, both of which list it in `<files_to_read>`)"
  artifacts:
    - path: "orq-agent/references/orqai-model-catalog.md"
      provides: "Capable Tier Lookup table: 4 task categories x dated-snapshot model IDs, with a preserved WARNING that MCP `models-list` is authoritative"
      contains: "## Capable Tier Lookup"
  key_links:
    - from: "orq-agent/agents/researcher.md Capable Tier Lookup subsection (Plan 02)"
      to: "orq-agent/references/orqai-model-catalog.md §Capable Tier Lookup"
      via: "Researcher references this table by section heading; executor reads it when selecting the capable-tier Primary"
      pattern: "Capable Tier Lookup"
---

<objective>
Add a `## Capable Tier Lookup` H2 section to `orq-agent/references/orqai-model-catalog.md` using the seed table from 35-CONTEXT.md §Specifics. The table maps task categories to dated-snapshot model IDs (illustrative — authoritative list comes from MCP `models-list` at runtime). Preserve the file's existing WARNING that live selection MUST validate against `models-list`.

Purpose: Plan 02 (researcher.md) references `orq-agent/references/orqai-model-catalog.md §Capable Tier Lookup` as the seed lookup for capable-first recommendations. Without this section present, the forward reference dangles. Plan 04 closes that reference in parallel with Plans 02/03 within Wave 2.

Output: A model catalog file with a new `## Capable Tier Lookup` section containing 4 rows (one per task category), preserved WARNING at top, and zero floating-alias model IDs.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-model-selection-discipline/35-CONTEXT.md
@.planning/phases/35-model-selection-discipline/35-VALIDATION.md
@orq-agent/references/orqai-model-catalog.md

<interfaces>
<!-- Key constraints from the existing catalog file (must preserve) -->

1. File opens with a NOTE + WARNING at lines 5-9 stating the file is a format reference only, live data comes from MCP `models-list`. This MUST remain verbatim.
2. File is referenced by 2 consumers (lint passes references-multi-consumer): `orq-agent/agents/researcher.md` + `orq-agent/agents/spec-generator.md` (both list it in `<files_to_read>`). Adding a section does NOT change the consumer count, so the rule remains green.
3. File is NOT a skill file (lives under `references/`, not `commands/` or `agents/`). It does NOT need the 9 SKST sections. It is NOT linted for SKST structure; only the `references-multi-consumer` rule applies to it, and only indirectly (the rule checks that every file in `references/` has >=2 consumers in SKILL.md/commands/agents).
4. The existing `## Recommended Models by Use Case` section (line 32) already lists categories (Reasoning, Classification, Generation, Fast, Vision). The new `## Capable Tier Lookup` section is a more prescriptive subset — it explicitly names the capable-tier primary per category for MSEL-01 lookup.

**Placement:** Insert `## Capable Tier Lookup` immediately AFTER the existing `## Recommended Models by Use Case` section (and all its H3 subsections) and BEFORE the existing `## How to Choose` H2 section. This ordering presents categories first, then the capable-first lookup that MSEL-01 references, then the choice heuristics.

**Seed table** (from 35-CONTEXT.md §specifics — verbatim with dated snapshots):

| Task category | Capable-tier Primary (illustrative; validate via MCP models-list) | Alternative Primary (same tier, different provider) |
|---|---|---|
| Chat-heavy / conversational | `anthropic/claude-sonnet-4-5-20250929` | `openai/gpt-4o-2024-11-20` |
| Tool-calling / agentic | `anthropic/claude-sonnet-4-5-20250929` | `openai/gpt-4o-2024-11-20` |
| Code / RAG synthesis | `anthropic/claude-opus-4-20250514` | `openai/gpt-4o-2024-11-20` |
| Fast triage (NOT a default — only cascade cheap tier) | `anthropic/claude-haiku-4-5-20251001` | `google-ai/gemini-2-5-flash` |

All IDs above are intentionally dated snapshots. The lint rule MUST NOT fire on this file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `## Capable Tier Lookup` section to the model catalog reference</name>
  <files>orq-agent/references/orqai-model-catalog.md</files>
  <read_first>
    - .planning/phases/35-model-selection-discipline/35-CONTEXT.md (locked decisions — especially §specifics for the seed table)
    - .planning/phases/35-model-selection-discipline/35-VALIDATION.md (Per-Task 35-04-01)
    - orq-agent/references/orqai-model-catalog.md (the file being modified — study the WARNING block and the existing `## Recommended Models by Use Case` section for style consistency)
  </read_first>
  <action>
    Edit `orq-agent/references/orqai-model-catalog.md`. Insert the new H2 section `## Capable Tier Lookup` IMMEDIATELY AFTER the existing `### Vision and Multimodal` H3 subsection (the last subsection of `## Recommended Models by Use Case`, ending around line 81) and BEFORE the existing `## How to Choose` H2 (around line 83).

    **Write this EXACT markdown** (preserve the inline comments as audit trail for MSEL-01 + Phase 42 forward-link):

    ```markdown
    ## Capable Tier Lookup

    <!-- Phase 35 MSEL-01: capable-first seed table for researcher.md §Model Selection Policy. -->
    <!-- This is a STATIC SEED. Live model availability MUST be validated via MCP `models-list` -->
    <!-- before any spec is deployed. See the WARNING at the top of this file. -->

    Researcher starts every Model Recommendation with the capable-tier primary for the task category. Use this table as the default lookup; override via `--model` flag, discussion input, or MCP `models-list` output when the listed snapshot is not enabled in the user's workspace.

    | Task category | Capable-tier Primary (illustrative; validate via MCP `models-list`) | Alternative Primary (same tier, different provider) |
    |---|---|---|
    | Chat-heavy / conversational | `anthropic/claude-sonnet-4-5-20250929` | `openai/gpt-4o-2024-11-20` |
    | Tool-calling / agentic | `anthropic/claude-sonnet-4-5-20250929` | `openai/gpt-4o-2024-11-20` |
    | Code / RAG synthesis | `anthropic/claude-opus-4-20250514` | `openai/gpt-4o-2024-11-20` |
    | Fast triage (NOT a default — only cascade cheap tier) | `anthropic/claude-haiku-4-5-20251001` | `google-ai/gemini-2-5-flash` |

    **Why these IDs are dated snapshots:** Phase 35 MSEL-02 (snapshot-pinning) requires every emitted `model:` field to pin to a dated snapshot. The lookup table therefore seeds dated snapshots; floating aliases (`claude-sonnet-4-5`, `gpt-4o`) appear elsewhere in this file ONLY as provider/format-pattern examples, NOT as recommendations.

    **How to use the table:**

    1. Read the agent's role from the architect blueprint.
    2. Map the role to one of the four task categories above (Chat-heavy, Tool-calling, Code/RAG, Fast triage).
    3. Pick the Capable-tier Primary as your recommendation starting point.
    4. Call MCP `models-list` to confirm the dated snapshot is enabled in the workspace.
       - If enabled → use it as Primary.
       - If not enabled → use the Alternative Primary column; if that is also not enabled, pick the closest capable-tier option from the MCP output and flag `Confidence: MEDIUM — capable-tier substitute applied`.
    5. NEVER substitute a Fast-triage model as the Primary unless the discussion explicitly requested a cost cascade (see researcher.md §Cascade Pattern for when Fast triage is a legitimate choice).

    **Relationship to this file's other sections:**

    - `## Recommended Models by Use Case` (above) is a broader listing of options by strength.
    - `## Capable Tier Lookup` (this section) is the prescriptive subset enforcing MSEL-01 capable-first ordering.
    - `## Fallback Model Strategy` (below) handles the `fallback_models` array shape; use the Alternative Primary column from this table as the first fallback entry when the Primary is chosen.
    ```

    **Do NOT modify** the existing WARNING block at lines 5-9, the Provider Format table (line 11+), any of the existing `## Recommended Models by Use Case` subsections, the `## How to Choose` section, or the `## Fallback Model Strategy` section.

    **Verification after edit:**

    1. Section presence:
       ```bash
       grep -q "## Capable Tier Lookup" orq-agent/references/orqai-model-catalog.md
       ```

    2. All 4 category rows present:
       ```bash
       grep -q "Chat-heavy / conversational" orq-agent/references/orqai-model-catalog.md
       grep -q "Tool-calling / agentic" orq-agent/references/orqai-model-catalog.md
       grep -q "Code / RAG synthesis" orq-agent/references/orqai-model-catalog.md
       grep -q "Fast triage" orq-agent/references/orqai-model-catalog.md
       ```

    3. Dated-snapshot IDs present:
       ```bash
       grep -q "claude-sonnet-4-5-20250929" orq-agent/references/orqai-model-catalog.md
       grep -q "claude-opus-4-20250514" orq-agent/references/orqai-model-catalog.md
       grep -q "claude-haiku-4-5-20251001" orq-agent/references/orqai-model-catalog.md
       grep -q "gpt-4o-2024-11-20" orq-agent/references/orqai-model-catalog.md
       ```

    4. Existing WARNING preserved:
       ```bash
       grep -q "WARNING: \`models-list\` returns ALL models" orq-agent/references/orqai-model-catalog.md
       ```

    5. `references-multi-consumer` rule still green (this file is still referenced by researcher.md + spec-generator.md):
       ```bash
       bash orq-agent/scripts/lint-skills.sh --rule references-multi-consumer
       ```

    6. Full-suite lint still green:
       ```bash
       bash orq-agent/scripts/lint-skills.sh
       ```

    7. `snapshot-pinned-models` rule (default set — catalog is NOT in the default set, so this confirms the rule is unaffected by the catalog edit):
       ```bash
       bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models
       ```

    8. Protected pipelines unaffected:
       ```bash
       bash orq-agent/scripts/check-protected-pipelines.sh
       ```
  </action>
  <verify>
    <automated>bash -c 'cd /Users/nickcrutzen/developer/orqai-agent-pipeline && grep -q "## Capable Tier Lookup" orq-agent/references/orqai-model-catalog.md && grep -q "Chat-heavy / conversational" orq-agent/references/orqai-model-catalog.md && grep -q "Tool-calling / agentic" orq-agent/references/orqai-model-catalog.md && grep -q "Code / RAG synthesis" orq-agent/references/orqai-model-catalog.md && grep -q "Fast triage" orq-agent/references/orqai-model-catalog.md && grep -q "claude-sonnet-4-5-20250929" orq-agent/references/orqai-model-catalog.md && grep -q "claude-opus-4-20250514" orq-agent/references/orqai-model-catalog.md && grep -q "claude-haiku-4-5-20251001" orq-agent/references/orqai-model-catalog.md && grep -q "WARNING:" orq-agent/references/orqai-model-catalog.md && bash orq-agent/scripts/lint-skills.sh && bash orq-agent/scripts/check-protected-pipelines.sh'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "## Capable Tier Lookup" orq-agent/references/orqai-model-catalog.md` exits 0 (new H2 present)
    - `grep -q "Chat-heavy / conversational" orq-agent/references/orqai-model-catalog.md` exits 0
    - `grep -q "Tool-calling / agentic" orq-agent/references/orqai-model-catalog.md` exits 0
    - `grep -q "Code / RAG synthesis" orq-agent/references/orqai-model-catalog.md` exits 0
    - `grep -q "Fast triage" orq-agent/references/orqai-model-catalog.md` exits 0
    - `grep -q "claude-sonnet-4-5-20250929" orq-agent/references/orqai-model-catalog.md` exits 0 (dated snapshot for chat-heavy + tool-calling)
    - `grep -q "claude-opus-4-20250514" orq-agent/references/orqai-model-catalog.md` exits 0 (dated snapshot for code/RAG)
    - `grep -q "claude-haiku-4-5-20251001" orq-agent/references/orqai-model-catalog.md` exits 0 (dated snapshot for fast-triage cascade cheap tier)
    - `grep -q "gpt-4o-2024-11-20" orq-agent/references/orqai-model-catalog.md` exits 0 (alternative dated snapshot)
    - `grep -q "MSEL-01" orq-agent/references/orqai-model-catalog.md` exits 0 (requirement traceable inline via HTML comment)
    - `grep -q "MSEL-02" orq-agent/references/orqai-model-catalog.md` exits 0 (cross-reference to pinning rule)
    - `grep -q "WARNING:" orq-agent/references/orqai-model-catalog.md` exits 0 (original WARNING block preserved)
    - `grep -q "validate via MCP" orq-agent/references/orqai-model-catalog.md` exits 0 (live-selection invariant documented)
    - Counted rows in the new table: `awk '/^## Capable Tier Lookup/,/^## How to Choose/' orq-agent/references/orqai-model-catalog.md | grep -c "^|" | awk '{print ($1>=6) ? "ok" : "fail"}'` prints `ok` (header + separator + 4 data rows = 6 pipe-lines minimum)
    - `bash orq-agent/scripts/lint-skills.sh --rule references-multi-consumer` exits 0 (file still has 2 consumers)
    - `bash orq-agent/scripts/lint-skills.sh` (full suite) exits 0
    - `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0
  </acceptance_criteria>
  <done>
    The model catalog reference gains a prescriptive `## Capable Tier Lookup` section that researcher.md can reference when selecting the capable-tier Primary. All 4 task-category rows are present with dated-snapshot model IDs. The existing WARNING block is preserved. The references-multi-consumer rule remains green. The forward reference from Plan 02's researcher.md now resolves to a concrete table.
  </done>
</task>

</tasks>

<verification>
Wave 2 (Plan 04) verification:

1. `## Capable Tier Lookup` H2 section present with 4 category rows.
2. All 4 category labels grep-verifiable.
3. All 4 dated-snapshot model IDs present.
4. Original WARNING block preserved.
5. `references-multi-consumer` lint rule still exits 0.
6. Full-suite lint + protected-pipeline check still exit 0.
</verification>

<success_criteria>
- [ ] `## Capable Tier Lookup` H2 inserted between `## Recommended Models by Use Case` and `## How to Choose`
- [ ] 4 task category rows present with dated-snapshot IDs
- [ ] WARNING about MCP `models-list` authority preserved
- [ ] MSEL-01 + MSEL-02 requirement IDs traceable inline
- [ ] references-multi-consumer lint rule still green
- [ ] Full-suite lint + protected-pipeline check still exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/35-model-selection-discipline/35-04-SUMMARY.md` documenting:
- New `## Capable Tier Lookup` section inserted with line numbers
- 4 task categories + 4 dated-snapshot model IDs confirmed via grep
- WARNING block preservation confirmed (byte-unchanged)
- Captured exit codes for verification commands
- Downstream handoff: "Plan 05 runs full verification sweep and writes VERIFICATION.md with MSEL-01/02/03-to-check traceability."
</output>
