# Phase 67: Stage 2 closure (iController DOM tagging) — Discussion Log

> **Audit trail only.** Decisions in `67-CONTEXT.md`.

**Date:** 2026-05-04
**Mode:** `--auto`
**Areas discussed:** Dispatch topology, URL construction, Selector probe, Failure surface, Live-mode gate, Idempotency, Cutover

---

## Dispatch topology (TAG-01, TAG-02 — non-blocking)

| Option | Pros | Cons | Selected |
|---|---|---|---|
| New Inngest event + dedicated tagger function | True non-blocking; independent failure domain; matches Phase 65 fan-out shape | One more function to maintain | ✓ |
| Inline `step.run` inside label-resolver, ignore-on-error | Simpler topology | Couples label-resolver health to Browserless availability; Inngest run shows red on Browserless outage | |
| Piggyback on `debtor-email/coordinator.requested` | Zero new events | Forces coordinator to either block or re-fan-out; muddles Stage 3 vs Stage 2 concerns | |

**Auto-selected:** Dedicated tagger function with new event.
**Why:** Independent failure domain is the only design that satisfies TAG-02 ("non-blocking for downstream Stage 3+4") without forcing the coordinator to handle Browserless errors.

---

## URL construction site

| Option | Pros | Cons | Selected |
|---|---|---|---|
| Build URL in label-resolver, pass on event payload | Saves two Supabase queries per tag; label-resolver already has both inputs loaded | URL format change requires label-resolver update | ✓ |
| Build URL in tagger function | Tagger fully self-contained | Re-loads `labeling_settings` + email row per tag | |

**Auto-selected:** Build in label-resolver. Tagger receives a pre-resolved URL on the event payload.

---

## Selector probe / Wave 0

| Option | Pros | Cons | Selected |
|---|---|---|---|
| Run probe in acceptance, commit selectors as Wave 0 | Blocks correctly; test-first per CLAUDE.md | Adds a manual step | ✓ |
| Skip probe, pull selectors from Phase 56 archive | No manual step | iController DOM may have drifted since 2026-04-29 walkthrough | |
| Defer selector completion to a follow-up phase | Phase 67 ships sooner | Tagger throws on every call; phase doesn't satisfy TAG-01 | |

**Auto-selected:** Run probe Wave 0.

---

## Failure surface (TAG-02 deferred run flag)

| Option | Pros | Cons | Selected |
|---|---|---|---|
| Catch in tagger, write `email_labels.icontroller_tag_status='failed'` + error, return ok | Recovery in operator hands; doesn't pollute Inngest retry queue | New column needed | ✓ |
| Bubble error, let Inngest retry up to N times | Built-in retry | A stuck page burns through retries before fix lands; cost amplification | |
| Reuse existing `email_labels.status` column | Zero schema change | Conflates resolver outcome with tagging outcome; ambiguous Bulk Review queries | |

**Auto-selected:** New `icontroller_tag_status` column with explicit value space.

---

## Live-mode gate

| Option | Pros | Cons | Selected |
|---|---|---|---|
| Reuse `labeling_settings.dry_run` | One source of truth; per-mailbox already; operator UX unchanged | None — this is what `dry_run=false` was always for | ✓ |
| New `labeling_settings.tag_in_icontroller` toggle | Granular control | Confuses operators ("which flag turns on tagging?") | |

**Auto-selected:** Reuse `dry_run`.

---

## Idempotency

`labelEmailInIcontroller` already implements idempotency via `readCurrentLabel(page)` (Phase 56). Phase 67 inherits — re-tagging the same email_label_id maps `'already_labeled'` → `icontroller_tag_status='tagged'`. No new logic.

---

## Cutover

| Option | Pros | Cons | Selected |
|---|---|---|---|
| Single PR; per-mailbox `dry_run` controls rollout | Operator already has the lever; rollback is a flag flip | None | ✓ |
| Global feature flag wrapped around the dispatch | Belt-and-braces | Two flags doing the same job | |

**Auto-selected:** Single PR; rely on existing `dry_run` per-mailbox lever.

---

## Claude's Discretion

- Helper module path for `buildIcontrollerMessageUrl`.
- Exact Bulk Review query change to surface failed tags.
- Tagger function id naming (recommendation: `automations/debtor-email-icontroller-tagger`).
- Whether to type `IcontrollerTagStatus` in events.ts.

## Deferred Ideas

- Stage 1 worker (carried from Phase 66).
- `swarms.side_effects[]` registry generalisation (Phase 68).
- Bulk Review "retry tagging" button (Phase 71).
- Cross-swarm canonical handler input shape (Phase 69).
- `pipeline_events` telemetry (Phase 70).
- iController API integration (permanently out of scope — no such API).

---

*Generated by `/gsd-discuss-phase 67 --auto`. Each "Selected" mark reflects the recommended option Claude chose; user can revise `67-CONTEXT.md` directly before `/gsd-plan-phase`.*
