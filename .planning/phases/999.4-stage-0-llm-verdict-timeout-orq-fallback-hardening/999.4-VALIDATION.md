---
phase: 999.4
slug: stage-0-llm-verdict-timeout-orq-fallback-hardening
status: planned
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-07
updated: 2026-05-07
---

# Phase 999.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (existing — `web/vitest.config.ts`) |
| **Config file** | `web/vitest.config.ts` |
| **Quick run command** | `cd web && npx vitest run lib/stage-0 lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts lib/inngest/functions/__tests__/classifier-screen-worker.test.ts lib/inngest/functions/__tests__/automation-runs-sweeper.test.ts lib/automations/orq-agents/__tests__/client.test.ts` |
| **Full suite command** | `cd web && npx vitest run` |
| **Estimated runtime** | ~30 seconds (focused) / ~3 min (full) |

---

## Sampling Rate

- **After every task commit:** Run quick command (focused on touched modules)
- **After every plan wave:** Run quick command across all Phase 999.4 modules
- **Before `/gsd-verify-work`:** Full suite must be green + Wave 0 smoke against Orq Router must have completed (PASS on A1+A2+A3)
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Decision Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|--------------|-----------------|-----------|-------------------|-------------|--------|
| 01-T1 | 999.4-01 | 0 | D-06, D-07, D-08 | Smoke script compiles + targets `/v2/router/chat/completions` with strict json_schema + fallback_models + Bedrock-prefixed model | smoke (live, manual run) | `cd web && ORQ_API_KEY=... npx tsx scripts/smoke-orq-router-direct.ts` | ❌ pending | ⬜ |
| 01-T2 | 999.4-01 | 0 | D-06, D-07, D-08 | A1+A2+A3 PASS captured in `wave-0-orq-router-smoke.md` | checkpoint:human-verify | manual operator confirmation in chat | ❌ pending | ⬜ |
| 01-T3 | 999.4-01 | 0 | (test scaffolds for D-01..D-09) | 4 test files exist with failing imports of not-yet-existing exports (RED state) | unit (compile + targeted run) | `cd web && npx vitest run lib/automations/orq-agents/__tests__/client.test.ts lib/inngest/functions/__tests__/automation-runs-sweeper.test.ts --reporter=verbose` (expect RED on missing exports) | ❌ pending | ⬜ |
| 02-T1 | 999.4-02 | 1 | D-01, D-04 | `OrqClientTimeoutError` class exported; 45s `AbortController` deadline at the existing `invokeOrqAgent` fetch boundary | unit | `cd web && npx vitest run lib/automations/orq-agents/__tests__/client.test.ts -t "OrqClientTimeoutError\|deadline"` | ❌ pending | ⬜ |
| 02-T2 | 999.4-02 | 1 | D-02 | Stage 0 worker catches ONLY `OrqClientTimeoutError`, persists `verdict='safe'` + `result.llm_reason` starting `"timeout: client_deadline_exceeded"`, forwards to Stage 1 (NOT safety_review); non-timeout errors propagate; `safety_overridden` short-circuit unchanged | unit (T-B2, T-B3, T-B4) | `cd web && npx vitest run lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts` | ❌ pending | ⬜ |
| 03-T1 | 999.4-03 | 2 | D-06, D-07, D-08 | `invokeOrqModel` POSTs to `/v2/router/chat/completions` with model + fallback_models + response_format json_schema strict from registry; OpenAI-style response parsed into `InvokeResult`; system prompt cached 60s; inherits 45s `OrqClientTimeoutError` | unit (T-C1, T-C2, T-C3 + inherited T-B1) | `cd web && npx vitest run lib/automations/orq-agents/__tests__/client.test.ts` | ❌ pending | ⬜ |
| 03-T2 | 999.4-03 | 2 | D-03, D-06 | Both classifiers swapped to `invokeOrqModel`; Stage 1 D-11 catch fires on `OrqClientTimeoutError` (T-B5); registry pre-deploy check both agents `enabled=true` | unit + manual registry check | `cd web && npx vitest run lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts lib/inngest/functions/__tests__/classifier-screen-worker.test.ts lib/stage-0` + Supabase MCP `SELECT agent_key, enabled FROM public.orq_agents WHERE agent_key IN ('stage-0-safety-classifier', 'stage-1-category-classifier')` | ❌ pending | ⬜ |
| 04-T1 | 999.4-04 | 3 | D-09 | Sweeper SELECT/UPDATE preserves `result` JSONB, marks stale Stage 0 rows failed, emits per-swarm stale channel; `cutoff` inside `step.run`; cron string only in `createFunction` config (NOT in JSDoc) | unit (T-D1, T-D2, T-D3, T-D4) | `cd web && npx vitest run lib/inngest/functions/__tests__/automation-runs-sweeper.test.ts` | ❌ pending | ⬜ |
| 04-T2 | 999.4-04 | 3 | D-09 | `stage0StaleSweeper` registered in `web/app/api/inngest/route.ts` `serve({ functions: [...] })`; full vitest suite green (no regression) | unit + integration | `cd web && npx vitest run` (full suite) | ❌ pending | ⬜ |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `web/lib/automations/orq-agents/__tests__/client.test.ts` — created in 01-T3 with RED scaffolds for `OrqClientTimeoutError`, `invokeOrqModel`, `deadline`, `router endpoint shape`, `response parser`, `prompt cache` (covers D-01, D-04, D-06, D-07, D-08).
- [ ] `web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts` — created/extended in 01-T3 with RED scaffolds for `timeout coercion`, `non-timeout error rethrown`, `safety_overridden short-circuit` (covers D-02).
- [ ] `web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts` — extended in 01-T3 with RED scaffold for `deadline triggers D-11` (covers D-03).
- [ ] `web/lib/inngest/functions/__tests__/automation-runs-sweeper.test.ts` — created in 01-T3 with RED scaffolds for `cutoff`, `result jsonb merge`, `cutoff inside step.run`, `stale channel emit` (covers D-09).
- [ ] `web/scripts/smoke-orq-router-direct.ts` + `wave-0-orq-router-smoke.md` — created in 01-T1; 5-min curl-equivalent against the live Orq Router endpoint validates the three [ASSUMED] items (A1 strict json_schema, A2 fallback_models array, A3 Bedrock-prefixed model ID returns valid JSON).
- [ ] **GATE:** 01-T2 checkpoint must record A1+A2+A3 PASS before any Wave 1 task starts. If FAIL, halt and amend CONTEXT.md with an alternate Fix C transport.

---

## Manual-Only Verifications

| Behavior | Decision Ref | Why Manual | Test Instructions |
|----------|--------------|------------|-------------------|
| Real-world latency win on direct chat-completion path | D-06, D-07 | Cannot be unit-tested — Orq queue-wait only manifests under production load | After Fix C ships (Wave 2), sample 50 stage-0/email.received traces over 24h. Verify p99 trace duration < 30 s (was 6–17 min observed). Capture in a follow-up `.planning/phases/999.4-.../latency-validation.md` post-deploy. |
| Sweeper closes existing stuck rows in production | D-09 | DB write to `automation_runs` with real data; needs operator review before flipping policy from mark-failed to auto-re-emit | After first sweeper cron tick post-deploy: `SELECT id, swarm_type, status, result->>'llm_reason', completed_at FROM automation_runs WHERE status='failed' AND result->>'llm_reason'='inngest_cancelled_stale' AND completed_at > <deploy>`. Operator confirms swept rows match the known-stuck inventory. |
| `safety_overridden=true` short-circuit unchanged | D-02 (negative) | End-to-end Inngest event flow with operator-driven re-emit | Re-emit a `safety_overridden=true` event from Bulk Review; confirm Stage 0 worker skips `llmInjectionVerdict` (no Orq trace; emits `classifier/screen.requested`; stage_0_safety run NOT created). |
| Pre-deploy: both target agents `enabled=true` in production registry | D-06 | Phase 74 activation may have been deferred for `stage-1-category-classifier` (Pitfall 6) | Via Supabase MCP: `SELECT agent_key, enabled FROM public.orq_agents WHERE agent_key IN ('stage-0-safety-classifier', 'stage-1-category-classifier')` — both rows MUST be `enabled=true` before Plan 03 ships. |
| Post-deploy: Router-path traces visible in Orq dashboard | D-06 (observability) | Dashboard inspection — proves observability not regressed by transport swap | Open Orq dashboard → traces; filter on the two agent keys; confirm new traces appear with `chat-completion` spans on `/v2/router/chat/completions` instead of `/v2/agents/.../responses`. |
| `get_agent` system-prompt field path identified | D-07 | Field path determined empirically from production response shape | During Plan 03 Task 1: `curl https://api.orq.ai/v2/agents/<orqai_id> -H "Authorization: Bearer $ORQ_API_KEY" | jq` → identify which of `prompts.system`, `system_prompt`, or `messages[role=system].content` carries the prompt. Document in 999.4-03-SUMMARY.md. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (every task has either `<automated>` or is the explicit human-verify checkpoint)
- [x] Wave 0 covers all MISSING references (Router-path tests + live smoke + sweeper tests)
- [x] No watch-mode flags (`vitest run`, NOT `vitest`)
- [x] Feedback latency < 30 s (focused vitest run is ~10s per file)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** planned
