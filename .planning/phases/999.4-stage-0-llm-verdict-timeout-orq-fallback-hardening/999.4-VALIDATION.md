---
phase: 999.4
slug: stage-0-llm-verdict-timeout-orq-fallback-hardening
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-07
---

# Phase 999.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (existing — `web/vitest.config.ts`) |
| **Config file** | `web/vitest.config.ts` |
| **Quick run command** | `cd web && npx vitest run lib/stage-0 lib/inngest/functions/stage-0-safety-worker lib/inngest/functions/classifier-screen-worker lib/automations/orq-agents` |
| **Full suite command** | `cd web && npm run test` |
| **Estimated runtime** | ~30 seconds (focused) / ~3 min (full) |

---

## Sampling Rate

- **After every task commit:** Run quick command (focused on touched modules)
- **After every plan wave:** Run quick command across all Phase 999.4 modules
- **Before `/gsd-verify-work`:** Full suite must be green + Wave 0 smoke against Orq Router must have completed
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

To be filled by gsd-planner with one row per task — each task ID, the plan it belongs to, the wave, the canonical decision/requirement reference (D-01..D-09 from CONTEXT.md), the test type, and the exact command that proves the task done.

| Task ID | Plan | Wave | Decision Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|--------------|-----------------|-----------|-------------------|-------------|--------|
| TBD by planner | | | | | | | | |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `web/lib/stage-0/__tests__/llm-verdict.test.ts` — extend (or create) for deadline + timeout-error path (D-01, D-04)
- [ ] `web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts` — extend (or create) for graceful coercion (`verdict='safe'` on timeout, persist `llm_reason='timeout: client_deadline_exceeded'`) (D-02)
- [ ] `web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts` — extend for deadline triggering existing D-11 catch (D-03)
- [ ] `web/lib/automations/orq-agents/__tests__/client.test.ts` — new tests covering `invokeOrqModel` (Router transport), prompt-cache TTL, response-shape mapping (D-06, D-07)
- [ ] `web/lib/inngest/functions/__tests__/automation-runs-sweeper.test.ts` — fixture rows: stuck (>15 min, status='pending', triggered_by LIKE 'stage-0/%'), fresh (<15 min — must NOT match), already-failed (must NOT match) (D-09)
- [ ] **Wave 0 smoke (live):** 5-min curl against Orq Router endpoint to verify (a) `response_format` json_schema strict mode is honored on the Router path, (b) `fallback_models: ["aws/eu.anthropic.claude-sonnet-4-5-20250929-v1:0"]` is accepted, (c) Bedrock-prefixed `model: "aws/eu.anthropic.claude-haiku-4-5-20251001-v1:0"` returns valid JSON conforming to the existing `stage_0_safety_verdict` schema. Captured in `.planning/phases/999.4-.../wave-0-orq-router-smoke.md` (or similar).

---

## Manual-Only Verifications

| Behavior | Decision Ref | Why Manual | Test Instructions |
|----------|--------------|------------|-------------------|
| Real-world latency win on direct chat-completion path | D-06, D-07 | Cannot be unit-tested — Orq queue-wait only manifests under production load. | After Fix C ships, sample 50 stage-0/email.received traces over 24h. Verify p99 trace duration < 30 s (was 6–17 min observed). Capture in a follow-up `.planning/phases/999.4-.../latency-validation.md` post-deploy. |
| Sweeper closes existing stuck rows in production | D-09 | DB write to `automation_runs` with real data; needs operator review before flipping policy from mark-failed to auto-re-emit. | After first sweeper cron tick post-deploy, query `automation_runs WHERE status='failed' AND result->>'llm_reason'='inngest_cancelled_stale' AND completed_at > <deploy>`. Operator confirms the swept rows match the known-stuck inventory at deploy time. |
| `safety_overridden=true` short-circuit unchanged | D-02 (negative) | End-to-end Inngest event flow with operator-driven re-emit. | Re-emit a `safety_overridden=true` event from Bulk Review; confirm Stage 0 worker skips the LLM call entirely (no Orq trace; emits `classifier/screen.requested`; stage_0_safety run NOT created). |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (Router-path tests + live smoke)
- [ ] No watch-mode flags (`vitest run`, NOT `vitest`)
- [ ] Feedback latency < 30 s
- [ ] `nyquist_compliant: true` set in frontmatter (after planner fills the per-task map)

**Approval:** pending
