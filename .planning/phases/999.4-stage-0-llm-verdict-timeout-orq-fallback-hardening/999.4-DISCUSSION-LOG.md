# Phase 999.4: Stage 0 LLM-verdict timeout / Orq queue-wait hardening — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-07
**Phase:** 999.4 — Stage 0 LLM-verdict timeout / Orq fallback-chain hardening
**Areas discussed:** Scope, Stage 1 mirror, Coercion verdict, Client deadline, Fix C scope (v1 + v2 after trace evidence), Cleanup of stuck rows

---

## Scope — Which fixes should this phase ship?

| Option | Description | Selected |
|--------|-------------|----------|
| Fix B only | In-tree timeout + graceful degrade only; file Fix C separately. | |
| Fix B + Fix C in one phase | Ship both per ROADMAP recommendation. | ✓ |
| Fix C only | Skip the code change, only Orq-side tuning. | |

---

## Stage 1 — Apply same Fix B pattern to stage-1-category-classifier?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — same fix, both stages | Mirror the deadline + graceful coercion in Stage 1 (D-11 already coerces on thrown errors; missing the deadline so hangs go through). | ✓ |
| No — Stage 0 only | Keep scope tight; revisit Stage 1 if it persists. | |

---

## Coerce to — On Stage 0 LLM timeout, what verdict should we coerce to?

| Option | Description | Selected |
|--------|-------------|----------|
| `'safe'` — let email through | Assume non-injection; persist timeout reason for audit. | ✓ |
| `'injection_suspected'` — fail-closed | Route to safety_review HITL queue. | |
| Persist as `'failed'` | Insert with status='failed', no forward, no classification. | |

---

## Timeout — What client-side deadline for the Orq llm-verdict call?

| Option | Description | Selected |
|--------|-------------|----------|
| 45 s (per ROADMAP) | Aligns with CLAUDE.md "45s client timeout (Orq.ai intern retry = 31s)" pattern. | ✓ |
| 30 s — tighter | Faster recovery; risk of cutting off legit slow completions. | |
| 60 s — match Pro Inngest step ceiling | Maximum LLM headroom; less margin for downstream steps. | |

---

## Fix C v1 — Original framing (fallback chain tuning)

| Option | Description | Selected |
|--------|-------------|----------|
| Trim to 2-hop fallback + EU-only | Reduce surface area of the fallback list. | |
| Keep chain, swap primary to faster model | Lower-risk single change. | |
| Defer Fix C, ship only Fix B | Lowest blast radius. | |

**User's choice:** "I'm not sure if fallback is the problem — check some traces first before we conclude this is the issue: 01KR0TZSWB6P1NSAAS5TAWAXVZ // 01KR0SWX88SZ3NB02M6EEF6Z1T // 01KR0RMTYM6X0SP3TGPA1P6PY9 → maybe changing servers?"

**Investigation result:** All 3 traces used Bedrock EU Haiku 4.5 as primary; the chat-completion span took 1.6 s in every case; the 6–17 minute trace duration was queue-wait inside the Orq Agents-product orchestration layer, NOT fallback activation. User's instinct was correct — original Fix C framing is wrong.

---

## Fix C v2 — Reframed after trace evidence (queue-wait root cause)

| Option | Description | Selected |
|--------|-------------|----------|
| Switch from Agents product → direct chat-completion / LLM Router | Bypass the Agents queue; the two classifiers are single-shot JSON-only and don't need Agents-product orchestration. | ✓ |
| Contact Orq support, keep Agents product | File a ticket with the trace IDs; no in-tree change beyond Fix B. | |
| Defer Fix C entirely | Ship only Fix B; investigate Fix C after a week of data. | |

---

## Backfill — Cleanup of historical stuck `automation_runs.status='pending'` rows

| Option | Description | Selected |
|--------|-------------|----------|
| One-shot SQL script in this phase | Closes existing stuck rows now. | |
| Recurring sweeper Inngest cron | Self-healing for future spikes too. | ✓ |
| No cleanup | Manual triage only. | |

---

## Claude's Discretion

- Implementation shape of the deadline (`AbortController` vs `Promise.race`).
- Helper-name choice for the direct chat-completion path (`invokeOrqModel` vs `mode` param on `invokeOrqAgent`).
- Sweeper's mark-failed vs auto-re-emit policy (start with mark-failed, upgrade later).

## Deferred Ideas

- Migrate other Orq agents (Stage 2/3, handlers) off Agents product — only after Fix C proves out.
- Body-size truncation upstream (the 11,758-token Stage 0 outlier).
- OData / ZapierRelayError ReadTimeout investigation in the `categorize` Inngest function.
- Auto-re-emit policy upgrade in the sweeper.
