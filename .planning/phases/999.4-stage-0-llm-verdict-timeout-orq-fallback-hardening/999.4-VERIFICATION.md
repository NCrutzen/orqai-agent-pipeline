---
phase: 999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening
verified: 2026-05-07T14:05:00Z
status: human_needed
score: 3/3 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Apply migrations 20260507d (agent_runs.result jsonb) + 20260507e (orq_agents.system_prompt) to production Supabase"
    expected: "Both columns present; pre-existing rows backfilled with default '{}' / null"
    why_human: "DDL migration to shared production DB requires explicit operator action; cannot run from verifier"
  - test: "Sync Studio system_prompt for stage-0-safety-classifier and stage-1-category-classifier into orq_agents.system_prompt"
    expected: "Both rows have non-null system_prompt matching current Studio editorial; first invocation of invokeOrqModel does NOT throw 'registry row missing system_prompt'"
    why_human: "Operations action documented in 999.4-03-SUMMARY deploy gate #2; without this, first prod call after deploy fails"
  - test: "Deploy current main to Vercel and confirm /v2/router/chat/completions traces appear in Orq dashboard for both classifiers"
    expected: "Router-path traces show up alongside (legacy) Agents-product traces for stage-0/stage-1; no 6–17 min queue-wait tail"
    why_human: "Requires live prod traffic + Orq dashboard visual inspection (T-999.4-05 mitigation per Plan 03)"
  - test: "Observe stage0StaleSweeper cron firing every 10 min during business hours (06:00-19:58 Amsterdam, Mon-Fri)"
    expected: "Inngest dashboard shows scheduled runs of 'stage-0/stale-sweeper'; sweeper output reports swept count + emitted channels"
    why_human: "Cron schedule cannot be exercised by unit tests; needs live Inngest run observation post-deploy"
---

# Phase 999.4: Stage 0 LLM-verdict Timeout / Orq Fallback Hardening — Verification Report

**Phase Goal:** Harden Stage 0 LLM-verdict timeout + Orq fallback by (a) bounding client deadline, (b) escaping the Agents-product queue via Router direct path, (c) sweeping leftover stuck rows.
**Verified:** 2026-05-07T14:05:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Client deadline bounds Orq.ai calls so Stage 0 cannot strand a row indefinitely | VERIFIED | `CLIENT_DEADLINE_MS = 45_000` + `AbortController + setTimeout` at both `invokeOrqAgent` (client.ts:183-210) and `invokeOrqModel` (client.ts:366-387). Stage 0 worker selectively coerces `OrqClientTimeoutError` → synthetic `verdict='safe'` with audit `result.llm_reason='timeout: client_deadline_exceeded'` (stage-0-safety-worker.ts:113-133). Tests T-B2/T-B3/T-B4 + class-instantiation GREEN. |
| 2 | Stage 0 + Stage 1 single-shot JSON classifiers bypass the Agents-product queue via Router direct path | VERIFIED | `invokeOrqModel` posts to `https://api.orq.ai/v2/router/chat/completions` (client.ts:27,317,370). `web/lib/stage-0/llm-verdict.ts` and `web/lib/inngest/functions/classifier-screen-worker.ts` both import and invoke `invokeOrqModel`; zero remaining `invokeOrqAgent` references for these classifiers. Wave 0 live smoke PASS A1/A2/A3 (1201 ms). Tests T-B1/T-B5/T-C1/T-C2/T-C3 GREEN. |
| 3 | Stale `pending` automation_runs rows are swept on a recurring cron and marked failed with audit | VERIFIED | `web/lib/inngest/functions/automation-runs-sweeper.ts` exports `stage0StaleSweeper` with id `"stage-0/stale-sweeper"`, cron `TZ=Europe/Amsterdam */10 6-19 * * 1-5`, 15-min cutoff inside `step.run("compute-cutoff", ...)`, per-row JSONB merge writing `llm_reason='inngest_cancelled_stale'`, distinct-channel emit. Registered in `web/app/api/inngest/route.ts:37,72`. Tests T-D1/T-D2/T-D3/T-D4 GREEN. |

**Score:** 3/3 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `web/lib/automations/orq-agents/client.ts` | `OrqClientTimeoutError` class + `CLIENT_DEADLINE_MS=45_000` + `invokeOrqModel` Router-direct helper | VERIFIED | All three exports present; AbortController pattern at both fetch boundaries; `clearTimeout` in finally. |
| `web/lib/inngest/functions/stage-0-safety-worker.ts` | Selective `OrqClientTimeoutError` → `verdict='safe'` coercion inside `step.run("llm-verdict", ...)` | VERIFIED | `instanceof OrqClientTimeoutError || err.name === "OrqClientTimeoutError"` discriminator; non-timeout errors rethrow. |
| `web/lib/stage-0/llm-verdict.ts` | Uses `invokeOrqModel` (not `invokeOrqAgent`) | VERIFIED | Single import + call site; legacy reference count 0. |
| `web/lib/inngest/functions/classifier-screen-worker.ts` | Uses `invokeOrqModel`; D-11 catch handles `OrqClientTimeoutError` transitively | VERIFIED | Import + call site present; existing D-11 catch unchanged but now covers timeout via typed error. |
| `web/lib/inngest/functions/automation-runs-sweeper.ts` | `stage0StaleSweeper` cron, replay-safe cutoff, JSONB merge | VERIFIED | All 7 documented steps present; 3.6 KB file. |
| `web/app/api/inngest/route.ts` | Sweeper registered in `serve({ functions })` | VERIFIED | Import line 37, entry line 72. |
| `supabase/migrations/20260507d_agent_runs_result_jsonb_phase994.sql` | Adds `agent_runs.result jsonb default '{}'` | VERIFIED (file present) | DDL not applied yet — see human verification. |
| `supabase/migrations/20260507e_orq_agents_system_prompt_phase994.sql` | Adds `orq_agents.system_prompt text` | VERIFIED (file present) | DDL not applied yet — see human verification. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `stage-0-safety-worker` | `OrqClientTimeoutError` | `import` + `instanceof` check | WIRED | Coercion executes on timeout, propagates other errors. |
| `llm-verdict.ts` | Router endpoint | `invokeOrqModel` → `fetch(ROUTER_URL)` | WIRED | Confirmed via grep + Wave 0 live smoke. |
| `classifier-screen-worker` | Router endpoint | `invokeOrqModel` | WIRED | D-11 catch path covers `OrqClientTimeoutError` transitively. |
| `inngest/route.ts` | `stage0StaleSweeper` | `serve({ functions: [...stage0StaleSweeper] })` | WIRED | Registered alongside `stage0SafetyWorker`. |
| Sweeper | `automation_runs` | `select().eq().like().lt()` + per-row `update()` | WIRED | T-D1 cutoff filter + T-D2 JSONB merge pass. |
| Sweeper | Bulk Review realtime | `emitAutomationRunStale(admin, channel)` per distinct `swarm_type` | WIRED | T-D4 distinct-channel assertion passes. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Wave 0 Router direct path live smoke | `tsx web/scripts/smoke-orq-router-direct.ts` (executed during Plan 01) | A1+A2+A3 PASS, latency 1201 ms | PASS |
| Phase-relevant unit tests | `npx vitest run lib/automations/orq-agents/__tests__/client.test.ts lib/inngest/functions/__tests__/{stage-0-safety-worker,classifier-screen-worker,automation-runs-sweeper}.test.ts lib/stage-0` | 7 files / 45 tests passed | PASS |
| TypeScript clean for new/changed files | `npx tsc --noEmit` (per Plan 02 + Plan 04 summaries) | No new TS errors | PASS |
| Live cron observation | (cannot run without Inngest) | n/a | SKIP — routed to human_verification |

### Anti-Patterns Found

None blocking. All side effects in `step.run()`; cron string lives in `createFunction` config (not in `/** */` JSDoc, per CLAUDE.md rule); replay-safe cutoff inside `step.run("compute-cutoff", ...)`. No TODO/FIXME/placeholder anti-patterns introduced by phase commits.

### Deferred Items

Per `deferred-items.md`: 22 pre-existing repo-wide test failures (queue/rule-filter, labeling, pipeline stages, v7 graph, dashboard load-page-data, safety-review-loader). None caused by this phase; baseline net moved -1 (632 passed). Explicitly out of scope per the user's verification brief.

### Gaps Summary

No goal-blocking gaps. The three observable truths (client deadline, Router-direct transport, stale-row sweeper) are end-to-end wired in code, tests are GREEN (45/45 phase-relevant), and Wave 0 live smoke confirmed Router contract before implementation.

The phase is **code-complete** but **not yet activated in production**: two SQL migrations and one Operations prompt-sync UPDATE must be applied before traffic hits the new Router path, plus a Vercel deploy. These are documented deploy gates (Plan 03 SUMMARY §"Deploy gates") and surfaced in `human_verification` above. Status is `human_needed` because cron observation, dashboard trace verification, and DDL application can only be confirmed by an operator.

---

_Verified: 2026-05-07T14:05:00Z_
_Verifier: Claude (gsd-verifier)_
