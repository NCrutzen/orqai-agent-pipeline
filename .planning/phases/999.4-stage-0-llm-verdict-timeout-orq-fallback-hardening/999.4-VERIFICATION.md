---
phase: 999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening
verified: 2026-05-07T15:55:00Z
status: human_needed
score: 2/2 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 3/3
  scope_change: "Plan 03 (Router transport swap) reverted in commit 31fc9ce. Truth #2 (Router-direct transport) removed from must-haves; Stage 0/1 stay on Agents-product path. Phase trims to 2 truths: (1) client deadline + selective coercion, (2) stale-row sweeper."
  gaps_closed: []
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Observe stage0StaleSweeper cron firing every 10 min during business hours (06:00-19:58 Amsterdam, Mon-Fri)"
    expected: "Inngest dashboard shows scheduled runs of 'stage-0/stale-sweeper'; sweeper output reports swept count + emitted channels"
    why_human: "Cron schedule cannot be exercised by unit tests; needs live Inngest run observation post-deploy"
  - test: "Confirm 45s client deadline activates in production for any genuinely slow Orq Agents call"
    expected: "Stage 0 worker logs OrqClientTimeoutError → synthetic verdict='safe' with audit `result.llm_reason='timeout: client_deadline_exceeded'`; row does not strand at status='pending'"
    why_human: "Requires live prod traffic against the Agents-product queue; cannot be reproduced deterministically from unit tests"
---

# Phase 999.4: Stage 0 LLM-verdict Timeout / Orq Fallback Hardening — Verification Report (Post-Revert)

**Phase Goal (post-revert):** Bound Stage 0 LLM-verdict latency client-side (45s `AbortController` deadline + selective fail-open coercion on `OrqClientTimeoutError`) and backstop any stuck rows that escape the deadline (Inngest cron sweeper). Stage 0/1 remain on the Agents-product transport (`/v2/agents/{key}/responses`) — Router swap from Plan 03 was reverted on 2026-05-07 after evidence showed the queue-stuck tail is not chronic.

**Verified:** 2026-05-07T15:55:00Z
**Status:** human_needed
**Re-verification:** Yes — scope-change re-verify after Plan 03 revert (commit `31fc9ce`).

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Client deadline bounds Orq.ai Agents calls so Stage 0 cannot strand a row indefinitely | VERIFIED | `web/lib/automations/orq-agents/client.ts`: `CLIENT_DEADLINE_MS = 45_000` (line 32), `OrqClientTimeoutError` class (line 42), `AbortController` + `setTimeout` wrapping the `invokeOrqAgent` fetch (lines 164, 184–185), `clearTimeout` in finally. Stage 0 worker selectively coerces `OrqClientTimeoutError` → synthetic `verdict='safe'` with audit `result.llm_reason='timeout: client_deadline_exceeded'` (`stage-0-safety-worker.ts:113-133`); non-timeout errors rethrow. Test `client.test.ts` + `stage-0-safety-worker.test.ts` GREEN. |
| 2 | Stale `pending` automation_runs rows are swept on a recurring cron and marked failed with audit | VERIFIED | `web/lib/inngest/functions/automation-runs-sweeper.ts:30` exports `stage0StaleSweeper`; cron `TZ=Europe/Amsterdam */10 6-19 * * 1-5` (line 33). Replay-safe cutoff inside `step.run("compute-cutoff", ...)`; per-row JSONB merge writes `llm_reason='inngest_cancelled_stale'`; distinct-channel realtime emit. Registered in `web/app/api/inngest/route.ts:37,72`. `automation-runs-sweeper.test.ts` GREEN. |

**Score:** 2/2 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `web/lib/automations/orq-agents/client.ts` | `OrqClientTimeoutError` class + `CLIENT_DEADLINE_MS=45_000` + `invokeOrqAgent` wrapped in AbortController | VERIFIED | All present at lines 32, 42, 143–210. `invokeOrqModel` correctly removed by revert. `invokeOrqAgentWithUsage` alias retained (line 245). |
| `web/lib/inngest/functions/stage-0-safety-worker.ts` | Selective `OrqClientTimeoutError` → `verdict='safe'` coercion inside `step.run("llm-verdict", ...)` | VERIFIED | `instanceof OrqClientTimeoutError \|\| err.name === "OrqClientTimeoutError"` discriminator at lines 126–127. |
| `web/lib/stage-0/llm-verdict.ts` | Uses `invokeOrqAgent` (Agents path) | VERIFIED | Single import + call site (lines 18, 40). No `invokeOrqModel` references. |
| `web/lib/inngest/functions/classifier-screen-worker.ts` | Uses `invokeOrqAgent`; D-11 catch handles `OrqClientTimeoutError` transitively | VERIFIED | Import + call site at lines 45, 188. |
| `web/lib/inngest/functions/automation-runs-sweeper.ts` | `stage0StaleSweeper` cron, replay-safe cutoff, JSONB merge | VERIFIED | Cron `*/10 6-19 * * 1-5` Amsterdam; all documented steps present. |
| `web/app/api/inngest/route.ts` | Sweeper registered in `serve({ functions })` | VERIFIED | Import line 37, registration line 72. |
| `supabase/migrations/20260507f_drop_orq_agents_system_prompt_phase994.sql` | Drops `orq_agents.system_prompt` (Plan 03 cleanup) | VERIFIED | File present; migration text confirms already applied to prod via MCP on 2026-05-07. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `stage-0-safety-worker` | `OrqClientTimeoutError` | `import` + `instanceof` check | WIRED | Coercion executes on timeout, propagates other errors. |
| `llm-verdict.ts` | Agents endpoint | `invokeOrqAgent` → `fetch(/v2/agents/{key}/responses)` | WIRED | Same import path as pre-Plan-03; cost_cents preserved on response. |
| `classifier-screen-worker` | Agents endpoint | `invokeOrqAgent` | WIRED | D-11 catch path covers `OrqClientTimeoutError` transitively (typed error instance preserves `.name`). |
| `inngest/route.ts` | `stage0StaleSweeper` | `serve({ functions: [...stage0StaleSweeper] })` | WIRED | Registered alongside `stage0SafetyWorker`. |
| Sweeper | `automation_runs` | `select().eq().like().lt()` + per-row `update()` | WIRED | Cutoff filter + JSONB merge tested. |
| Sweeper | Bulk Review realtime | `emitAutomationRunStale(admin, channel)` per distinct `swarm_type` | WIRED | Distinct-channel assertion passes. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Phase-relevant unit tests (5 files / 29 tests) | `npx vitest run lib/automations/orq-agents/__tests__/client.test.ts lib/inngest/functions/__tests__/{stage-0-safety-worker,classifier-screen-worker,automation-runs-sweeper}.test.ts lib/stage-0/__tests__/llm-verdict.test.ts` | 5 files / 29 tests passed | PASS |
| No leftover `invokeOrqModel` references in source | `grep -rn "invokeOrqModel" web/lib web/app web/scripts` | 1 hit, in `client.test.ts` comment header (documentation only — no symbol use) | PASS |
| No leftover `system_prompt` registry references | `grep -rn "system_prompt" web/lib web/app` | 1 hit in `regex-patterns.ts` (unrelated regex pattern name `reveal_system_prompt`) | PASS |
| Live cron observation | (cannot run without Inngest) | n/a | SKIP — routed to human_verification |
| Live timeout coercion | (requires prod traffic) | n/a | SKIP — routed to human_verification |

### Anti-Patterns Found

None blocking. All side effects in `step.run()`; cron string lives in `createFunction` config (not in `/** */` JSDoc); replay-safe cutoff inside `step.run("compute-cutoff", ...)`. No TODO/FIXME/placeholder anti-patterns introduced.

### Revert Cleanup Audit (Plan 03)

| Item | Expected Post-Revert State | Status |
|------|---------------------------|--------|
| `invokeOrqModel` helper removed from `client.ts` | Symbol absent in source | VERIFIED (zero source references; `.next` build cache excluded) |
| `orq_agents.system_prompt` column dropped | DDL drop migration present | VERIFIED (`20260507f...sql` applied via MCP) |
| Stage 0/1 workers reverted to `invokeOrqAgent` | `llm-verdict.ts` + `classifier-screen-worker.ts` import `invokeOrqAgent` | VERIFIED |
| 999.5 (Router cost-reconciliation) cancelled | Phase directory removed | VERIFIED (commit `c92f778`) |
| Phase 999.4 plan trimmed to 2 truths | Plans 01, 02, 04 active; Plan 03 marked REVERTED | VERIFIED |

### Deferred Items

Per `deferred-items.md`: 22 pre-existing repo-wide test failures (unrelated to this phase). Not goal-blocking.

### Gaps Summary

No goal-blocking gaps. The two observable truths (45s client deadline + selective fail-open coercion; stale-row sweeper) are end-to-end wired in code, all 5 phase-relevant test files (29 tests) GREEN, and Plan 03 revert artifacts are cleanly removed (`invokeOrqModel` gone from source, `orq_agents.system_prompt` dropped in migration `20260507f`).

The phase is **code-complete and live-ready**: no DDL gates remain (Plan 03's two migrations 20260507d/e are no longer required — `agent_runs.result jsonb` was already in 20260507d, and the system_prompt column has been dropped). Status remains `human_needed` only because cron firing and live timeout coercion can only be observed against running infrastructure.

---

_Verified: 2026-05-07T15:55:00Z_
_Verifier: Claude (gsd-verifier)_
