---
phase: 999.4
plan: "01"
subsystem: stage-0-safety / orq-router-transport
tags: [phase-999.4, wave-0, orq-router, smoke, test-scaffold, RED]
provides:
  - "Validated Router transport for Wave 2 Fix C (A1+A2+A3 PASS)"
  - "RED contracts for OrqClientTimeoutError, invokeOrqModel, stage0StaleSweeper"
requires:
  - "ORQ_API_KEY in web/.env.local"
affects:
  - web/lib/automations/orq-agents/client.ts (Wave 1+ targets)
  - web/lib/inngest/functions/stage-0-safety-worker.ts (Wave 1 target)
  - web/lib/inngest/functions/classifier-screen-worker.ts (Wave 2 target)
  - web/lib/inngest/functions/automation-runs-sweeper.ts (Wave 3 target — new)
tech_stack:
  added: []
  patterns:
    - "Raw fetch (no SDK) against /v2/router/chat/completions with bearer auth"
    - "anyOf-for-nullable in JSON Schema strict mode (CLAUDE.md learning 3970bad9)"
    - "RED-state vitest scaffolds: failing imports = contract for downstream wave"
key_files:
  created:
    - web/lib/automations/orq-agents/__tests__/client.test.ts
    - web/lib/inngest/functions/__tests__/automation-runs-sweeper.test.ts
  modified:
    - web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts
    - web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts
    - .planning/phases/999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening/wave-0-orq-router-smoke.md
decisions:
  - "Wave 1+ proceed without CONTEXT.md amendment (A1+A2+A3 PASS, latency 1201ms)"
  - "Fix C transport confirmed: POST /v2/router/chat/completions with fallback_models array + json_schema strict"
metrics:
  duration: ~2h (incl. live smoke)
  completed: 2026-05-07
  tasks: 3
  commits: 3
---

# Phase 999.4 Plan 01: Wave 0 — Orq Router Smoke + RED Scaffolds Summary

Validated the three [ASSUMED] Router behaviors from RESEARCH.md against the live `/v2/router/chat/completions` endpoint and committed RED contracts for the four downstream wave units.

## What Shipped

1. **`web/scripts/smoke-orq-router-direct.ts`** (commit `4c261c7`) — one-shot tsx smoke harness that POSTs a strict json_schema request with Bedrock primary + Bedrock fallback to the Router endpoint, validates the response against `stage_0_safety_verdict`.
2. **`wave-0-orq-router-smoke.md` evidence file** (commit `45f2b2d`) — full request/response capture, latency, A1/A2/A3 PASS markers and decision = "Wave 1+ PROCEED".
3. **Four RED test scaffolds** (commit `b1f2a4a`) — failing imports of `OrqClientTimeoutError`, `invokeOrqModel`, and `stage0StaleSweeper` define Wave 1+ contracts.

## Wave 0 Gate Results (Live Smoke)

| ID | Item | Result |
|----|------|--------|
| A1 | `response_format: { type: "json_schema", strict: true }` honored | **PASS** |
| A2 | `fallback_models: [...]` accepted on Router path | **PASS** (no 400; primary resolved so fallback not exercised) |
| A3 | `aws/eu.anthropic.claude-haiku-4-5-20251001-v1:0` resolves on Router | **PASS** — 1201 ms |

Decision: **Wave 1+ proceed.** No CONTEXT.md amendment. Fix B's 45 s deadline budget has > 40 s headroom over observed primary latency.

## RED Contracts Committed

### `web/lib/automations/orq-agents/__tests__/client.test.ts` (new)
Covers T-B1, T-C1, T-C2, T-C3.
- `OrqClientTimeoutError` typed error class.
- `invokeOrqModel` 45 s `AbortController` deadline → throws `OrqClientTimeoutError`.
- POST URL = `/v2/router/chat/completions`; body merges registry `model_config.primary`, `fallback_models`, registry `output_schema` under `response_format.json_schema.strict=true`.
- Response parser: OpenAI-style `choices[0].message.content` → `InvokeResult.raw`; usage fields populated.
- 60 s system-prompt + registry cache: `loadAgent` chain hit exactly once across two invocations.

### `web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts` (extended, +3 describes)
Covers T-B2, T-B3, T-B4.
- `OrqClientTimeoutError` from `llmInjectionVerdict` → `automation_runs` row written with `result.verdict='safe'`, `result.llm_reason='timeout: client_deadline_exceeded'`, `topic=null`, `classifier/screen.requested` emitted.
- Non-timeout errors (e.g. parse failures) are NOT coerced — handler rejects.
- `safety_overridden=true` short-circuit unchanged: skips `llmInjectionVerdict`, still emits `classifier/screen.requested`.

### `web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts` (extended, +1 describe)
Covers T-B5.
- `OrqClientTimeoutError` from `invokeOrqModel` → existing D-11 catch coerces to `category_key='unknown'`, `confidence='low'`; `classifier/verdict.recorded` carries `predicted_category='unknown'`.

### `web/lib/inngest/functions/__tests__/automation-runs-sweeper.test.ts` (new)
Covers T-D1..T-D4.
- 15-min cutoff filter: only stuck `pending` rows from `stage-0/safety-worker` get UPDATEd.
- JSONB merge: read-modify-write preserves `email_id`, `regex_matched`, adds `llm_reason='inngest_cancelled_stale'`, `status='failed'`, `completed_at`.
- Replay-safe: cutoff timestamp computed inside `step.run('compute-cutoff', ...)`.
- `emitAutomationRunStale` called once per distinct `swarm_type` (`'debtor-email-review'`, `'sales-email-review'`).

## RED-State Verification

Vitest output confirms expected failures:
- `client.test.ts` — 5 of 6 fail: `invokeOrqModel is not a function`, `OrqClientTimeoutError is not a constructor` (the typed-error structural assertion does fail since the class is not exported).
- `automation-runs-sweeper.test.ts` — all 4 fail: cannot import `stage0StaleSweeper` (the file `automation-runs-sweeper.ts` does not yet exist).
- `stage-0-safety-worker.test.ts` — 7 existing pass; 1 new (T-B2) fails RED at `new OrqClientTimeoutError(...)`. T-B3 and T-B4 currently pass coincidentally because the existing worker rethrows / short-circuits unconditionally; they will become real RED gates once Wave 1 introduces selective `OrqClientTimeoutError` coercion.
- `classifier-screen-worker.test.ts` — 11 existing pass; 1 new (T-B5) fails RED at `new OrqClientTimeoutError(...)`.

## Deviations from Plan

**None.** Plan executed exactly as written:

- Task 1 (smoke script + evidence skeleton) committed `4c261c7` (pre-checkpoint, by prior turn).
- Task 2 (run smoke + capture evidence) — user ran the smoke with `ORQ_API_KEY`, returned `smoke green`. Evidence MD filled in and committed `45f2b2d`.
- Task 3 (4 RED scaffolds) committed `b1f2a4a`. All grep acceptance criteria pass; vitest confirms RED state on the to-be-added exports without breaking the 18 existing tests in the two extended files.

## Commits

| Hash | Message |
|------|---------|
| `4c261c7` | feat(999.4-01): add Orq Router direct-path smoke script and evidence skeleton |
| `45f2b2d` | docs(999.4-01): capture smoke PASS evidence — A1/A2/A3 green |
| `b1f2a4a` | test(999.4-01): add Wave 0 RED scaffolds for Wave 1+ contracts |

## Next

Wave 1 (`999.4-02-PLAN.md`) — implement `OrqClientTimeoutError` + 45 s `AbortController` at existing `invokeOrqAgent` fetch boundary; Stage 0 worker selective coercion (T-B2 turns green; T-B3 stays green for the right reason).

## Self-Check: PASSED

- `web/scripts/smoke-orq-router-direct.ts` — FOUND.
- `.planning/phases/999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening/wave-0-orq-router-smoke.md` — FOUND, contains `## Result PASS`, `## A1`, `## A2`, `## A3`, `## Decision Wave 1+ PROCEED`.
- `web/lib/automations/orq-agents/__tests__/client.test.ts` — FOUND.
- `web/lib/inngest/functions/__tests__/automation-runs-sweeper.test.ts` — FOUND.
- `web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts` — modified, contains `Phase 999.4` describes.
- `web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts` — modified, contains `Phase 999.4` describe.
- Commits `4c261c7`, `45f2b2d`, `b1f2a4a` — all FOUND in `git log`.
