---
phase: 999.4
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - web/scripts/smoke-orq-router-direct.ts
  - .planning/phases/999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening/wave-0-orq-router-smoke.md
  - web/lib/automations/orq-agents/__tests__/client.test.ts
  - web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts
  - web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts
  - web/lib/inngest/functions/__tests__/automation-runs-sweeper.test.ts
autonomous: false
decisions_addressed: [D-06, D-07, D-08]
tags: [phase-999.4, wave-0, orq-router, smoke, test-scaffold]

must_haves:
  truths:
    - "POST /v2/router/chat/completions accepts response_format json_schema strict mode"
    - "POST /v2/router/chat/completions accepts fallback_models array with Bedrock-prefixed IDs"
    - "Bedrock EU Haiku 4.5 (aws/eu.anthropic.claude-haiku-4-5-20251001-v1:0) returns valid JSON conforming to stage_0_safety_verdict schema via Router"
    - "Test files for Wave 1+ exist with failing-spec scaffolds (RED state) so executors implement against contracts"
  artifacts:
    - path: web/scripts/smoke-orq-router-direct.ts
      provides: "One-shot live smoke against Orq Router endpoint"
    - path: .planning/phases/999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening/wave-0-orq-router-smoke.md
      provides: "Captured smoke evidence with curl/script output, latency, response shape"
    - path: web/lib/automations/orq-agents/__tests__/client.test.ts
      provides: "Test scaffolds for invokeOrqModel + OrqClientTimeoutError + prompt cache"
    - path: web/lib/inngest/functions/__tests__/automation-runs-sweeper.test.ts
      provides: "Test scaffolds for sweeper SELECT/UPDATE/replay-safety"
  key_links:
    - from: web/scripts/smoke-orq-router-direct.ts
      to: "https://api.orq.ai/v2/router/chat/completions"
      via: "raw fetch with ORQ_API_KEY bearer auth"
      pattern: "fetch.*v2/router/chat/completions"
---

<objective>
Wave 0 gate. Validate the three [ASSUMED] items in RESEARCH.md before any production code changes:
(A1) `response_format: { type: "json_schema", strict: true, ... }` honored on `/v2/router/chat/completions`,
(A2) `fallback_models` array accepted on the Router path,
(A3) Bedrock-prefixed model IDs (`aws/eu.anthropic.claude-*`) resolve correctly on the Router endpoint.

Also create RED test scaffolds for the units that downstream waves will implement, so Wave 1+ executors get contracts (function signatures, expected behavior) and not just prose.

Purpose: prevent Fix C (Wave 2) from being designed against unverified Orq Router behavior.
Output: smoke evidence file + 4 test files in scaffold state (importing not-yet-existing exports → expected red).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening/999.4-CONTEXT.md
@.planning/phases/999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening/999.4-RESEARCH.md
@.planning/phases/999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening/999.4-VALIDATION.md
@CLAUDE.md
@web/lib/automations/orq-agents/client.ts
@web/lib/stage-0/llm-verdict.ts

<interfaces>
<!-- From web/lib/automations/orq-agents/client.ts (existing exports — Wave 1+ will add to these) -->

```typescript
export type OrqAgentRow = {
  agent_key: string;
  orqai_id: string;
  output_schema: unknown;
  model_config: unknown; // { primary, fallbacks?, temperature?, max_tokens? }
  enabled: boolean;
  // ...
};

export type InvokeResult = {
  raw: unknown;
  agent: OrqAgentRow;
  usage: { prompt_tokens: number; completion_tokens: number; total_tokens: number };
  billing: { total_cost: number };
  cost_cents: number;
};

export async function invokeOrqAgent(
  agent_key: string,
  inputs: Record<string, unknown>,
  opts?: { jsonSchemaName?: string },
): Promise<InvokeResult>;

export const REGISTRY_CACHE_TTL_MS = 60_000;
```

<!-- Wave 1 will add: -->
<!--   export class OrqClientTimeoutError extends Error { name = "OrqClientTimeoutError" } -->
<!-- Wave 2 will add: -->
<!--   export async function invokeOrqModel(agent_key, inputs): Promise<InvokeResult> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create live Orq Router smoke script and capture evidence</name>
  <files>web/scripts/smoke-orq-router-direct.ts, .planning/phases/999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening/wave-0-orq-router-smoke.md</files>
  <read_first>
    - .planning/phases/999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening/999.4-RESEARCH.md (§Open Questions, §Assumptions Log A1–A3, §Pattern 2)
    - .planning/phases/999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening/999.4-CONTEXT.md (§decisions D-06, D-07, D-08)
    - CLAUDE.md (§Orq.ai — Anthropic model routing, anyOf for nullable, 45s client timeout)
    - web/lib/automations/orq-agents/client.ts (existing fetch shape lines 130–187)
    - supabase/migrations/20260506_phase74_stage1_classifier_agent.sql (registry row shape — output_schema with anyOf for nullable)
  </read_first>
  <action>
Create `web/scripts/smoke-orq-router-direct.ts` — a tsx-runnable one-shot script.

Behavior:
1. Read `ORQ_API_KEY` from `process.env`. Throw if missing.
2. Build a minimal `stage_0_safety_verdict` request body matching the existing schema (mirrors `web/lib/stage-0/llm-verdict.ts` `VerdictSchema`):
   - `model: "aws/eu.anthropic.claude-haiku-4-5-20251001-v1:0"` (D-08 primary)
   - `fallback_models: ["aws/eu.anthropic.claude-sonnet-4-5-20250929-v1:0"]` (D-08 fallback)
   - `temperature: 0`
   - `max_tokens: 600`
   - `messages: [{ role: "system", content: "You output strict JSON conforming to the schema..." }, { role: "user", content: "{\"email_subject\":\"Hi\",\"email_body\":\"Hello\"}" }]`
   - `response_format: { type: "json_schema", json_schema: { name: "stage_0_safety_verdict", strict: true, schema: { type: "object", additionalProperties: false, required: ["verdict","reason","matched_span"], properties: { verdict: { type: "string", enum: ["safe","injection_suspected"] }, reason: { type: "string", maxLength: 280 }, matched_span: { anyOf: [{ type: "string" }, { type: "null" }] } } } } }` (anyOf for nullable per CLAUDE.md learning `3970bad9`).
3. POST to `https://api.orq.ai/v2/router/chat/completions` with `Authorization: Bearer ${ORQ_API_KEY}`, `content-type: application/json`. Use raw `fetch` (no SDK) — matches RESEARCH.md "Alternatives Considered" recommendation.
4. Time the request with `Date.now()` deltas (start, request_sent, response_received).
5. Parse `json.choices[0].message.content`, `JSON.parse`, validate the parsed JSON conforms to `stage_0_safety_verdict`.
6. Print to stdout: HTTP status, latency_ms, raw body (truncated to 1000 chars), parsed verdict, fallback_used (if response surfaces it).
7. Exit code 0 on full success (HTTP 200 + valid JSON conforming to schema), exit code 1 on any failure.

Run command: `cd web && ORQ_API_KEY=$(grep '^ORQ_API_KEY=' .env.local | cut -d= -f2-) npx tsx scripts/smoke-orq-router-direct.ts`

Capture the full stdout/stderr to `.planning/phases/999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening/wave-0-orq-router-smoke.md` with the following sections:
- `## Smoke run timestamp` — ISO date
- `## Result` — PASS / FAIL
- `## A1 (response_format json_schema strict)` — PASS / FAIL + evidence
- `## A2 (fallback_models accepted)` — PASS / FAIL + evidence (HTTP 400 with "fallback_models" in error → FAIL)
- `## A3 (Bedrock-prefixed model ID resolves)` — PASS / FAIL + latency_ms
- `## Raw request body` — JSON
- `## Raw response body` — JSON (full)
- `## Decision` — Wave 1+ proceed (PASS) or escalate (FAIL — describe required CONTEXT.md amendment)

CRITICAL: this is a CHECKPOINT gate. If A1, A2, or A3 fails, escalate to user before Wave 1 starts — do NOT silently proceed. The blocker is recorded in the smoke MD's `## Decision` section.
  </action>
  <verify>
    <automated>cd web && test -f scripts/smoke-orq-router-direct.ts && npx tsc --noEmit scripts/smoke-orq-router-direct.ts 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File `web/scripts/smoke-orq-router-direct.ts` exists.
    - `grep -c "v2/router/chat/completions" web/scripts/smoke-orq-router-direct.ts` returns >= 1.
    - `grep -c "aws/eu.anthropic.claude-haiku-4-5-20251001-v1:0" web/scripts/smoke-orq-router-direct.ts` returns >= 1.
    - `grep -c "fallback_models" web/scripts/smoke-orq-router-direct.ts` returns >= 1.
    - `grep -c '"strict": true\|strict: true' web/scripts/smoke-orq-router-direct.ts` returns >= 1.
    - `grep -c "anyOf" web/scripts/smoke-orq-router-direct.ts` returns >= 1.
    - File `.planning/phases/999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening/wave-0-orq-router-smoke.md` exists with `## Result`, `## A1`, `## A2`, `## A3`, `## Decision` headers.
  </acceptance_criteria>
  <done>Script compiles; smoke MD captured; manual run executed and result section filled in.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run smoke and confirm PASS — gate Wave 1</name>
  <what-built>Live smoke script + evidence file. This task RUNS it and gates the rest of the phase.</what-built>
  <how-to-verify>
1. From repo root: `cd web && ORQ_API_KEY=$(grep '^ORQ_API_KEY=' .env.local | cut -d= -f2- | tr -d '"') npx tsx scripts/smoke-orq-router-direct.ts`
2. Expected: exit 0, HTTP 200, latency_ms < 5000 (Router path should be fast — no queue), parsed JSON has `verdict ∈ {"safe","injection_suspected"}`, `reason` is a string, `matched_span` is `string | null`.
3. Paste full stdout into `wave-0-orq-router-smoke.md` `## Raw response body` section. Fill in `## A1`, `## A2`, `## A3`, `## Decision`.
4. If any of A1/A2/A3 FAIL: stop. Surface the failure to user. Wave 1 must NOT start until either (a) the smoke is rerun green, or (b) CONTEXT.md is amended with an alternate Fix C transport (e.g., raw chat-completion without fallback_models, or fall back to Agents-product path with deadline-only Fix B).
  </how-to-verify>
  <resume-signal>Type "smoke green" if A1+A2+A3 PASS, or "smoke failed: <reason>" otherwise.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Create test scaffolds (RED) for Wave 1+ implementations</name>
  <files>web/lib/automations/orq-agents/__tests__/client.test.ts, web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts, web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts, web/lib/inngest/functions/__tests__/automation-runs-sweeper.test.ts</files>
  <read_first>
    - .planning/phases/999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening/999.4-RESEARCH.md (§Phase Requirements → Test Map T-B1..T-D4, §Pattern 2 invokeOrqModel sketch, §Pattern 4 sweeper sketch)
    - .planning/phases/999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening/999.4-VALIDATION.md (§Wave 0 Requirements)
    - web/lib/automations/orq-agents/client.ts (existing exports + AbortController pattern lines 130–147)
    - web/lib/inngest/functions/stage-0-safety-worker.ts (existing step.run("llm-verdict") site lines 109–111)
    - web/lib/inngest/functions/classifier-screen-worker.ts (existing D-11 catch block lines 240–276)
    - web/lib/inngest/functions/__tests__ — sibling test files for vitest setup conventions, mock fetch patterns
  </read_first>
  <action>
Create four vitest test files. Each test must import the symbol it tests; tests will fail RED until Wave 1+ implements them. That is intentional — the imports define the contract.

**File 1: `web/lib/automations/orq-agents/__tests__/client.test.ts`** — covers T-B1, T-C1, T-C2, T-C3.

Test cases (use `describe`/`it` blocks; mock `global.fetch` via vitest):
- `describe("OrqClientTimeoutError")`:
  - `it("is a typed error class with name='OrqClientTimeoutError'")` — `import { OrqClientTimeoutError } from "../client"; expect(new OrqClientTimeoutError("x").name).toBe("OrqClientTimeoutError"); expect(new OrqClientTimeoutError("x") instanceof Error).toBe(true)`.
- `describe("invokeOrqModel — deadline (T-B1)")`:
  - `it("aborts after 45s and throws OrqClientTimeoutError")` — mock fetch to never resolve; use vitest fake timers; `vi.advanceTimersByTime(45_001)`; expect rejection with `name === "OrqClientTimeoutError"`.
- `describe("invokeOrqModel — router endpoint shape (T-C1)")`:
  - `it("POSTs to /v2/router/chat/completions with merged model + fallback_models + response_format from registry")` — mock supabase `loadAgent` to return a fixture row with `model_config: { primary: "aws/eu.anthropic.claude-haiku-4-5-20251001-v1:0", fallbacks: ["aws/eu.anthropic.claude-sonnet-4-5-20250929-v1:0"], temperature: 0, max_tokens: 600 }`, `output_schema: { type: "object", ... }`. Mock fetch. Capture `fetch.mock.calls[0][0]` URL and `mock.calls[0][1].body`. Assert URL ends with `/v2/router/chat/completions`. Assert parsed body `model === "aws/eu.anthropic.claude-haiku-4-5-20251001-v1:0"`, `fallback_models[0] === "aws/eu.anthropic.claude-sonnet-4-5-20250929-v1:0"`, `response_format.type === "json_schema"`, `response_format.json_schema.strict === true`.
- `describe("invokeOrqModel — response parser (T-C2)")`:
  - `it("parses OpenAI-style choices[0].message.content into InvokeResult.raw")` — mock fetch returns `{ choices: [{ message: { content: '{"verdict":"safe","reason":"ok","matched_span":null}' } }], usage: { prompt_tokens: 10, completion_tokens: 5, total_tokens: 15 } }`. Assert `result.raw.verdict === "safe"`, `result.usage.prompt_tokens === 10`, `result.usage.total_tokens === 15`.
- `describe("invokeOrqModel — system prompt cache (T-C3)")`:
  - `it("fetches system prompt once within 60s window across multiple invocations")` — spy on the prompt-fetch path (e.g., a `loadSystemPromptForAgent` internal helper, exported via a `__resetCacheForTests` hook); call `invokeOrqModel` twice in quick succession; assert prompt-fetch invoked exactly 1.

**File 2: `web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts`** — extend or create; covers T-B2, T-B3, T-B4.

Test cases:
- `describe("Stage 0 worker — timeout coercion (T-B2)")`:
  - `it("catches OrqClientTimeoutError and persists verdict='safe' with result.llm_reason='timeout: client_deadline_exceeded'")` — use `@inngest/test` (or in-tree harness) to invoke worker; mock `llmInjectionVerdict` to throw `new OrqClientTimeoutError("Orq router deadline exceeded after 45000ms")`. Assert `automation_runs.insert` called with `{ status: "completed", result: { verdict: "safe", llm_reason: expect.stringContaining("timeout") } }`. Assert `inngest.send("classifier/screen.requested", ...)` was emitted (forward to Stage 1). Assert NO emit to `safety_review` topic.
- `describe("Stage 0 worker — non-timeout errors rethrown (T-B3)")`:
  - `it("does NOT coerce on parse / schema errors (only OrqClientTimeoutError)")` — mock `llmInjectionVerdict` to throw `new Error("Stage 0 verdict parse failed: ...")`. Assert step.run rejects (does not coerce); assert no `verdict='safe'` insert.
- `describe("Stage 0 worker — safety_overridden short-circuit unchanged (T-B4)")`:
  - `it("safety_overridden=true emits classifier/screen.requested without invoking llmInjectionVerdict")` — invoke worker with `safety_overridden: true` in event data. Spy on `llmInjectionVerdict`; assert NOT called. Assert `classifier/screen.requested` emitted.

**File 3: `web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts`** — extend; covers T-B5.

Test case:
- `describe("Classifier screen worker — deadline triggers existing D-11 catch (T-B5)")`:
  - `it("when invokeOrqModel throws OrqClientTimeoutError, D-11 catch coerces to category_key='unknown', confidence='low'")` — mock `invokeOrqModel` (post-Wave 2 swap target) to throw `new OrqClientTimeoutError("...")`. Assert `agent_runs.insert` row has `result.category_key === "unknown"`, `result.confidence === "low"`. Assert verdict event emitted downstream with `category_key='unknown'`.

**File 4: `web/lib/inngest/functions/__tests__/automation-runs-sweeper.test.ts`** — new; covers T-D1..T-D4.

Test cases (mock supabase admin client + fake timers):
- `describe("Sweeper — cutoff (T-D1)")`:
  - `it("SELECT picks rows older than 15 min, ignores fresher rows")` — fixture three rows: stuck (`status='pending'`, `triggered_by='stage-0/safety-worker'`, `created_at = now - 16min`), fresh (`created_at = now - 5min`), already-failed (`status='failed'`). Assert sweeper UPDATE only targets the stuck row's `id`.
- `describe("Sweeper — JSONB merge (T-D2)")`:
  - `it("merges llm_reason into result without losing existing keys (email_id, regex_matched)")` — fixture stuck row with `result: { email_id: "abc", regex_matched: false }`. After sweep, expect UPDATE payload `{ status: "failed", completed_at: <iso>, result: { email_id: "abc", regex_matched: false, llm_reason: "inngest_cancelled_stale" } }` (per-row read-modify-write).
- `describe("Sweeper — replay-safe cutoff (T-D3)")`:
  - `it("cutoff timestamp is computed inside step.run('compute-cutoff', ...)")` — assert via Inngest test harness that `step.run` is called with id `"compute-cutoff"` before any DB query.
- `describe("Sweeper — emit stale channel (T-D4)")`:
  - `it("calls emitAutomationRunStale once per distinct swarm_type after marking rows")` — fixture two stuck rows: one `swarm_type='debtor-email'`, one `swarm_type='sales-email'`. Spy on `emitAutomationRunStale`; assert called exactly twice with channels `'debtor-email-review'` and `'sales-email-review'`.

For all four files: do NOT implement any production code. Tests must compile against vitest types (`import { describe, it, expect, vi, beforeEach } from "vitest"`). Tests will fail RED — that is the desired Wave 0 state. Add a header comment in each file: `// Phase 999.4 RED scaffold — failing imports gate Wave 1+ implementation. Do not fix by stubbing — implement the contract.`
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit lib/automations/orq-agents/__tests__/client.test.ts lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts lib/inngest/functions/__tests__/classifier-screen-worker.test.ts lib/inngest/functions/__tests__/automation-runs-sweeper.test.ts 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - All 4 test files exist.
    - `grep -l "OrqClientTimeoutError" web/lib/automations/orq-agents/__tests__/client.test.ts` returns the path.
    - `grep -l "invokeOrqModel" web/lib/automations/orq-agents/__tests__/client.test.ts` returns the path.
    - `grep -c "v2/router/chat/completions" web/lib/automations/orq-agents/__tests__/client.test.ts` returns >= 1.
    - `grep -c "client_deadline_exceeded\|timeout: client_deadline" web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts` returns >= 1.
    - `grep -c "category_key.*unknown" web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts` returns >= 1.
    - `grep -c "inngest_cancelled_stale" web/lib/inngest/functions/__tests__/automation-runs-sweeper.test.ts` returns >= 1.
    - `grep -c "compute-cutoff" web/lib/inngest/functions/__tests__/automation-runs-sweeper.test.ts` returns >= 1.
    - TypeScript expects compile errors only on imports of not-yet-existing exports (`OrqClientTimeoutError`, `invokeOrqModel`, `stage0StaleSweeper`); other syntax is clean.
  </acceptance_criteria>
  <done>Four test files committed in RED state. Wave 1+ executors will see the failing imports as their contract.</done>
</task>

</tasks>

<verification>
- Smoke evidence MD answers A1+A2+A3 with PASS.
- All 4 test files exist with the expected import shape.
- `cd web && npx vitest run --reporter=verbose lib/automations/orq-agents/__tests__/client.test.ts lib/inngest/functions/__tests__/automation-runs-sweeper.test.ts 2>&1 | head -30` shows expected RED failures (cannot import `OrqClientTimeoutError`, `invokeOrqModel`, `stage0StaleSweeper`).
</verification>

<success_criteria>
- Wave 0 smoke MD has all 4 PASS markers (A1, A2, A3, Decision = "Wave 1+ proceed").
- Test scaffolds compile-fail only on the to-be-added exports.
- User has signed off on the smoke result.
</success_criteria>

<output>
Create `.planning/phases/999.4-stage-0-llm-verdict-timeout-orq-fallback-hardening/999.4-01-SUMMARY.md` with smoke results, test files created, and any unblock decisions for Wave 1.
</output>
