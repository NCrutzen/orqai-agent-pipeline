---
phase: 74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260506_phase74_agent_runs_entity_nullable.sql
  - supabase/migrations/20260506_phase74_sales_email_seed.sql
  - supabase/migrations/20260506_phase74_stage1_classifier_agent.sql
  - web/lib/automations/debtor-email/mailboxes.ts
autonomous: false
requirements:
  - SPEC-REQ-1
  - SPEC-REQ-5
  - SPEC-REQ-6
  - SPEC-REQ-7
notes: |
  Wave 1 foundation. Addresses RESEARCH blockers #1 (agent_runs.entity CHECK),
  #4 (ICONTROLLER_MAILBOXES), and seeds the registry rows the worker (Plan 04)
  reads. orq_agents row created with enabled=false; flipped true by Plan 03
  after Studio + MCP ritual completes. Includes a checkpoint for the operator
  to confirm firecontrol@/fire@ mailbox iController IDs and entity_brand for
  sales-email seed.
must_haves:
  truths:
    - "agent_runs INSERT with swarm_type='sales-email', entity=null succeeds (CHECK dropped, NOT NULL removed)"
    - "swarms row exists for swarm_type='sales-email' with stage1_regex_module=null"
    - "Five swarm_categories rows exist for swarm_type='sales-email' (auto_reply, ooo_temporary, ooo_permanent, payment_admittance, unknown)"
    - "orq_agents row exists for agent_key='stage-1-category-classifier' with swarm_type='cross-cutting' (enabled=false placeholder)"
    - "ICONTROLLER_MAILBOXES contains the operator-confirmed firecontrol@ + SMEBA fire@ entries (or operator-stated descope if not debtor)"
  artifacts:
    - path: "supabase/migrations/20260506_phase74_agent_runs_entity_nullable.sql"
      provides: "Drops agent_runs_entity_check; alters entity drop not null"
    - path: "supabase/migrations/20260506_phase74_sales_email_seed.sql"
      provides: "swarms + 5 swarm_categories rows for sales-email"
    - path: "supabase/migrations/20260506_phase74_stage1_classifier_agent.sql"
      provides: "orq_agents row for stage-1-category-classifier (enabled=false)"
    - path: "web/lib/automations/debtor-email/mailboxes.ts"
      provides: "Updated ICONTROLLER_MAILBOXES per operator confirmation"
  key_links:
    - from: "Plan 04 worker (loadSwarm)"
      to: "public.swarms row swarm_type='sales-email'"
      via: "registry helper"
      pattern: "loadSwarm\\(admin, ['\"]sales-email['\"]\\)"
    - from: "Plan 04 worker (loadSwarmCategories)"
      to: "public.swarm_categories rows swarm_type='sales-email'"
      via: "registry helper"
      pattern: "loadSwarmCategories\\(admin, ['\"]sales-email['\"]\\)"
    - from: "Plan 04 worker (agent_runs INSERT)"
      to: "public.agent_runs (entity nullable)"
      via: "supabase admin client"
      pattern: "from\\(['\"]agent_runs['\"]\\)\\.insert"
---

<objective>
Land all DB schema/registry foundation needed by the Stage-1 worker before any
worker code is written. Three migrations + one TS update. Includes an operator
checkpoint to confirm two unknowns: (1) firecontrol@ + SMEBA fire@ mailbox
iController IDs (or descope), (2) entity_brand value for sales-email seed.

Purpose: Plan 04's `classifier-screen-worker` cannot exercise the sales-email
LLM-only path without `agent_runs.entity` being nullable; cannot pass the SPEC
REQ-6 acceptance check (5 sales-email swarm_categories rows) without the seed;
cannot persist its agent_runs telemetry without the orq_agents registry row.

Output: Three applied Supabase migrations + a CLAUDE.md-compliant TS edit. All
verifiable by SQL count queries + grep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-SPEC.md
@.planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-CONTEXT.md
@.planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-RESEARCH.md
@CLAUDE.md
@supabase/migrations/20260428_public_agent_runs.sql
@supabase/migrations/20260429b_swarm_registry.sql
@supabase/migrations/20260429g_orq_agents_registry.sql
@supabase/migrations/20260504b_swarms_registry_generalisation.sql
@supabase/migrations/20260505b_orq_agents_cross_cutting.sql
@web/lib/automations/debtor-email/mailboxes.ts

<interfaces>
<!-- agent_runs schema (current — Phase 60-00 era) -->
From supabase/migrations/20260428_public_agent_runs.sql:13-22:
```sql
swarm_type text not null check (swarm_type in ('debtor-email','sales-email')),
entity text not null check (entity in ('smeba','berki','sicli-noord','sicli-sud','smeba-fire')),
-- ^^ this CHECK + NOT NULL is the blocker for sales-email LLM agent_runs writes.
```

<!-- swarms row shape (Phase 68 generalisation) -->
From supabase/migrations/20260504b_swarms_registry_generalisation.sql:
- stage1_regex_module text null   -- D-03: null = LLM-only
- stage2_entity_resolver text null
- stage3_coordinator_agent_key text null
- side_effects jsonb default '[]'::jsonb
- canonical_context_shape jsonb null
- entity_brand jsonb default '[]'::jsonb

<!-- swarm_categories row shape -->
Columns: swarm_type, category_key, display_label, outlook_label, action,
swarm_dispatch, display_order, enabled (default true).
PK: (swarm_type, category_key).

<!-- orq_agents row shape (cross-cutting precedent) -->
From 20260505b: agent_key, orqai_id, description, swarm_type, version,
input_schema (jsonb), output_schema (jsonb), model_config (jsonb),
timeout_ms, enabled, notes.

<!-- ICONTROLLER_MAILBOXES (current — debtor-email mailboxes only) -->
From web/lib/automations/debtor-email/mailboxes.ts:8-14:
debiteuren@smeba.nl=4, debiteuren@berki.nl=171, debiteuren@sicli-noord.nl=15,
debiteuren@sicli-sud.nl=16, debiteuren@smeba-fire.nl=5.
**firecontrol@ and fire@ NOT present** — operator must confirm IDs or descope.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator confirmations (mailbox IDs + sales-email entity_brand)</name>
  <what-needed>
    Two pieces of information that the planner cannot derive:
    1. iController mailbox IDs for `firecontrol@` and SMEBA `fire@`. RESEARCH
       Pitfall 5: without these, `classifier-label-resolver`'s
       `isKnownMailbox` gate silently no-ops the iController side-effect for
       these two debtor mailboxes, and SPEC REQ-7 acceptance passes only on
       the literal `pipeline_events` count check while live tagging never
       fires. Operator may also confirm "descope — these are not debtor
       mailboxes; route Stage-1-only" — in which case Task 4 is a no-op.
    2. `entity_brand` value for the new `sales-email` swarms row. CONTEXT
       D-17 leaves discretion: `[]` (truly cross-brand) vs starter brand
       list. RESEARCH recommendation: `[]` for v1 since sales-email has no
       Stage 3 chain that consumes brand metadata yet (D-18). Operator
       confirms `[]` or supplies a list.
  </what-needed>
  <how-to-verify>
    Operator answers in chat. Record the answers in the resume signal.
  </how-to-verify>
  <resume-signal>
    Provide both answers in this format:
      - firecontrol@ icontroller_mailbox_id: <number> | DESCOPE
      - SMEBA fire@ address (e.g. fire@smeba.nl) and icontroller_mailbox_id: <addr>=<number> | DESCOPE
      - sales-email entity_brand: [] | ["brand1","brand2",...]
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migration — agent_runs.entity nullable + drop CHECK</name>
  <files>supabase/migrations/20260506_phase74_agent_runs_entity_nullable.sql</files>
  <read_first>
    - supabase/migrations/20260428_public_agent_runs.sql (lines 13-91 — current schema, named `agent_runs_entity_check`)
    - .planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-RESEARCH.md (Pitfall 1 + Code Example C)
    - CLAUDE.md (Supabase section — Management API / apply_migration MCP idiom)
  </read_first>
  <action>
    Create `supabase/migrations/20260506_phase74_agent_runs_entity_nullable.sql`
    with EXACTLY this body (drop existing CHECK, drop NOT NULL; idempotent
    via `if exists`):

    ```sql
    -- Phase 74 — Stage 1 LLM classifier needs agent_runs to accept rows for
    -- sales-email LLM calls. Sales-email has no debtor entity; entity is a
    -- debtor-specific concept that does not generalize. Drop the CHECK and
    -- the NOT NULL — swarm_type column already provides cross-swarm
    -- discrimination.
    -- Source: 74-RESEARCH.md Pitfall 1, Open Question 4.

    alter table public.agent_runs
      drop constraint if exists agent_runs_entity_check;

    alter table public.agent_runs
      alter column entity drop not null;

    -- Phase 74 D-11 contract: classifier-screen-worker writes a top-level
    -- error_message on LLM error/timeout/schema-fail. Ensure the column
    -- exists (idempotent — no-op if 20260428 schema already has it).
    alter table public.agent_runs
      add column if not exists error_message text null;
    ```

    Apply via Supabase Management API (`apply_migration` MCP if available, else
    Studio SQL editor as fallback per CLAUDE.md). Confirm by running a
    one-shot test INSERT in a SQL session:

    ```sql
    -- transient verification (do NOT commit a fixture row)
    begin;
      insert into public.agent_runs (
        id, swarm_type, email_id, inngest_run_id, status, entity
      ) values (
        gen_random_uuid(), 'sales-email', '00000000-0000-0000-0000-000000000000',
        'verify-only', 'predicted', null
      );
    rollback;
    ```

    The transaction rolling back must NOT raise a CHECK violation. If it does,
    investigate before committing.
  </action>
  <acceptance_criteria>
    - File `supabase/migrations/20260506_phase74_agent_runs_entity_nullable.sql` exists.
    - `grep -c "drop constraint if exists agent_runs_entity_check" supabase/migrations/20260506_phase74_agent_runs_entity_nullable.sql` returns `1`.
    - `grep -c "alter column entity drop not null" supabase/migrations/20260506_phase74_agent_runs_entity_nullable.sql` returns `1`.
    - SQL probe: `select column_name, is_nullable from information_schema.columns where table_schema='public' and table_name='agent_runs' and column_name='entity'` returns `entity, YES`.
    - SQL probe: `select conname from pg_constraint where conrelid='public.agent_runs'::regclass and conname='agent_runs_entity_check'` returns zero rows.
    - SQL probe: `select column_name from information_schema.columns where table_schema='public' and table_name='agent_runs' and column_name='error_message'` returns `error_message` (column exists post-migration).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/agent-workforce && grep -c "drop constraint if exists agent_runs_entity_check" supabase/migrations/20260506_phase74_agent_runs_entity_nullable.sql && grep -c "alter column entity drop not null" supabase/migrations/20260506_phase74_agent_runs_entity_nullable.sql</automated>
  </verify>
  <done>Migration file committed; applied to Supabase; SQL probes above pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migration — sales-email registry seed (swarms + 5 swarm_categories)</name>
  <files>supabase/migrations/20260506_phase74_sales_email_seed.sql</files>
  <read_first>
    - supabase/migrations/20260429b_swarm_registry.sql (current swarms + swarm_categories shape + debtor seed for column reference)
    - supabase/migrations/20260504b_swarms_registry_generalisation.sql (Phase 68 columns added: stage1_regex_module, stage2_entity_resolver, stage3_coordinator_agent_key, side_effects, canonical_context_shape, entity_brand)
    - .planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-RESEARCH.md (Code Example B — exact migration body)
    - CONTEXT.md D-17 (5-row seed spec)
    - Operator answer from Task 1 for `entity_brand` value (`[]` or supplied list)
  </read_first>
  <action>
    Create `supabase/migrations/20260506_phase74_sales_email_seed.sql` using
    Code Example B from RESEARCH.md verbatim, with the operator-supplied
    `entity_brand` substituted in. Migration MUST:

    1. INSERT one `public.swarms` row with:
       - swarm_type='sales-email'
       - display_name='Sales Email'
       - description='Sales-email Stage 0->1 classification (Phase 74). LLM-only Stage 1 (no regex module).'
       - review_route='/automations/[swarm]/review'
       - source_table='automation_runs'
       - enabled=true
       - ui_config (jsonb): tree_levels=['topic','entity','mailbox_id'], row_columns=[received_at, sender, subject, rule], drawer_fields=['subject','sender','received_at','rule','predicted_category','body_html'], default_sort='created_at desc'
       - side_effects='[]'::jsonb (D-18)
       - stage1_regex_module=null (D-03 — drives LLM-only path)
       - stage2_entity_resolver=null
       - stage3_coordinator_agent_key=null
       - canonical_context_shape=null
       - entity_brand=<operator answer from Task 1, default '[]'::jsonb>
       - `on conflict (swarm_type) do update set description, ui_config, updated_at`

    2. INSERT exactly 5 `public.swarm_categories` rows for swarm_type='sales-email':
       - ('auto_reply', 'Auto-reply', 'Auto-Reply', 'categorize_archive', null, 10)
       - ('ooo_temporary', 'OOO (temporary)', 'OoO — Temporary', 'categorize_archive', null, 20)
       - ('ooo_permanent', 'OOO (permanent)', 'OoO — Permanent', 'categorize_archive', null, 30)
       - ('payment_admittance', 'Payment Admittance', 'Payment Admittance', 'categorize_archive', null, 40)
       - ('unknown', 'Unknown (manual review)', null, 'manual_review', null, 50)
       - `on conflict (swarm_type, category_key) do update set display_label, outlook_label, action, swarm_dispatch, display_order, updated_at`

    Apply via Supabase Management API. The migration is idempotent via the
    on-conflict clauses, so re-applying is safe.
  </action>
  <acceptance_criteria>
    - File exists at the specified path.
    - `grep -c "swarm_type" supabase/migrations/20260506_phase74_sales_email_seed.sql` returns >= 7 (one for swarms INSERT + 5 in swarm_categories VALUES + on-conflict mentions).
    - `grep -c "'sales-email'" supabase/migrations/20260506_phase74_sales_email_seed.sql` returns >= 6 (1 swarms + 5 swarm_categories).
    - `grep -c "stage1_regex_module" supabase/migrations/20260506_phase74_sales_email_seed.sql` returns >= 1 (set to null).
    - SQL probe: `select count(*) from public.swarms where swarm_type='sales-email'` returns 1.
    - SQL probe: `select count(*) from public.swarm_categories where swarm_type='sales-email'` returns exactly 5 (SPEC REQ-6 acceptance).
    - SQL probe: `select category_key from public.swarm_categories where swarm_type='sales-email' order by display_order` returns `auto_reply, ooo_temporary, ooo_permanent, payment_admittance, unknown` in that order.
    - SQL probe: `select stage1_regex_module from public.swarms where swarm_type='sales-email'` returns NULL.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/agent-workforce && test -f supabase/migrations/20260506_phase74_sales_email_seed.sql && grep -c "'sales-email'" supabase/migrations/20260506_phase74_sales_email_seed.sql</automated>
  </verify>
  <done>Migration file committed; applied to Supabase; all SQL probes pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Migration — orq_agents row for stage-1-category-classifier (enabled=false placeholder)</name>
  <files>supabase/migrations/20260506_phase74_stage1_classifier_agent.sql</files>
  <read_first>
    - supabase/migrations/20260429g_orq_agents_registry.sql (current orq_agents schema + 3-row debtor seed)
    - supabase/migrations/20260505b_orq_agents_cross_cutting.sql (cross-cutting swarm_type precedent)
    - .planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-RESEARCH.md (Code Example D — exact migration body)
    - CONTEXT.md D-05, D-06 (agent contract)
  </read_first>
  <action>
    Create `supabase/migrations/20260506_phase74_stage1_classifier_agent.sql`
    using Code Example D from RESEARCH.md verbatim. Key fields:

    - agent_key='stage-1-category-classifier'
    - orqai_id='PLACEHOLDER_STAGE1_CLASSIFIER_SLUG' (Plan 03 UPDATEs after Studio create_agent succeeds)
    - swarm_type='cross-cutting' (D-05)
    - version='2026-05-06.v1'
    - input_schema (jsonb): object with subject, body_text, categories (array of {category_key, display_label}); required=[subject, body_text, categories]; additionalProperties default
    - output_schema (jsonb): D-06 strict object with category_key (string), confidence (enum low|medium|high), reasoning (anyOf [string, null]); additionalProperties=false
    - model_config (jsonb): primary='aws/eu.anthropic.claude-haiku-4-5-20251001-v1:0', fallbacks=['openai/gpt-4o-mini','google-ai/gemini-2.5-flash'], temperature=0, max_tokens=400
    - timeout_ms=45000 (CLAUDE.md 45s ceiling)
    - enabled=false (Plan 03 flips true after `get_agent` verification confirms persisted strict response_format)
    - notes describing the Plan 03 ritual that activates this row

    Migration must use `on conflict (agent_key) do update set description, swarm_type, version, input_schema, output_schema, model_config, timeout_ms, notes, updated_at` (idempotent, but does NOT overwrite `orqai_id` or `enabled` so Plan 03's flip survives re-applies).

    Apply via Supabase Management API.
  </action>
  <acceptance_criteria>
    - File exists.
    - `grep -c "stage-1-category-classifier" supabase/migrations/20260506_phase74_stage1_classifier_agent.sql` returns >= 1.
    - `grep -c "cross-cutting" supabase/migrations/20260506_phase74_stage1_classifier_agent.sql` returns >= 1.
    - `grep -c "aws/eu.anthropic.claude-haiku-4-5-20251001-v1:0" supabase/migrations/20260506_phase74_stage1_classifier_agent.sql` returns >= 1.
    - `grep -c "anyOf" supabase/migrations/20260506_phase74_stage1_classifier_agent.sql` returns >= 1 (D-06 nullable reasoning).
    - `grep -c "PLACEHOLDER_STAGE1_CLASSIFIER_SLUG" supabase/migrations/20260506_phase74_stage1_classifier_agent.sql` returns >= 1.
    - On-conflict clause MUST exclude `orqai_id` and `enabled` from the update set (verify with grep that those two column names do not appear inside the `do update set ...` clause).
    - SQL probe: `select agent_key, swarm_type, enabled from public.orq_agents where agent_key='stage-1-category-classifier'` returns `stage-1-category-classifier, cross-cutting, false`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/agent-workforce && grep -c "stage-1-category-classifier" supabase/migrations/20260506_phase74_stage1_classifier_agent.sql && grep -c "cross-cutting" supabase/migrations/20260506_phase74_stage1_classifier_agent.sql && grep -c "anyOf" supabase/migrations/20260506_phase74_stage1_classifier_agent.sql</automated>
  </verify>
  <done>Migration file committed; applied to Supabase; orq_agents row present with enabled=false.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Extend ICONTROLLER_MAILBOXES per operator answer</name>
  <files>web/lib/automations/debtor-email/mailboxes.ts</files>
  <read_first>
    - web/lib/automations/debtor-email/mailboxes.ts (full file — line 8 ICONTROLLER_MAILBOXES const)
    - web/lib/inngest/functions/classifier-label-resolver.ts:294 (isKnownMailbox gate context — informational)
    - Operator answer from Task 1
  </read_first>
  <action>
    If operator answered DESCOPE for both firecontrol@ and SMEBA fire@:
      - Skip this task. Add a single comment line in `mailboxes.ts` at the
        bottom of the const recording the descope, e.g. `// firecontrol@ /
        SMEBA fire@ deferred per Phase 74 operator decision (2026-05-06)`.

    Otherwise, edit `web/lib/automations/debtor-email/mailboxes.ts` and add
    one or two new entries to the ICONTROLLER_MAILBOXES const using the
    operator-supplied IDs. Preserve the existing entries verbatim. Example
    shape (replace `<id>` with operator answer):

    ```typescript
    export const ICONTROLLER_MAILBOXES = {
      "debiteuren@smeba.nl": 4,
      "debiteuren@berki.nl": 171,
      "debiteuren@sicli-noord.nl": 15,
      "debiteuren@sicli-sud.nl": 16,
      "debiteuren@smeba-fire.nl": 5,
      "firecontrol@<domain>.nl": <id>,         // Phase 74
      "fire@<smeba-domain>.nl": <id>,          // Phase 74
    } as const;
    ```

    Do NOT modify MAILBOX_BRAND_PATTERNS unless operator explicitly requests
    a new entity entry. The brand patterns are entity-keyed, not
    mailbox-keyed; new mailboxes can map to existing entities (e.g.
    `firecontrol@…` may map to `smeba-fire` for brand purposes).

    Run `cd web && pnpm tsc --noEmit` to confirm no type regressions in the
    `SourceMailbox` union consumers (this widens the union, all consumers
    are still narrowing).
  </action>
  <acceptance_criteria>
    - If operator descoped: file contains a comment recording the decision; no constant change.
    - Else: `grep -c "firecontrol@" web/lib/automations/debtor-email/mailboxes.ts` returns >= 1 AND/OR `grep -c "fire@" web/lib/automations/debtor-email/mailboxes.ts` returns >= 2 (one in original `smeba-fire`, one in new `fire@…`).
    - `cd web && pnpm tsc --noEmit` exits 0.
    - All previously-listed mailboxes (smeba/berki/sicli-noord/sicli-sud/smeba-fire) still present unchanged: `grep -c "debiteuren@smeba.nl" web/lib/automations/debtor-email/mailboxes.ts` returns 1.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/agent-workforce/web && pnpm tsc --noEmit</automated>
  </verify>
  <done>File updated per operator answer; tsc clean; existing entries unchanged.</done>
</task>

</tasks>

<verification>
- All three migration files exist at the specified paths.
- All three migrations applied to Supabase (Management API success or operator-confirmed Studio apply).
- SQL probes pass:
  - `select count(*) from public.swarms where swarm_type='sales-email'` = 1
  - `select count(*) from public.swarm_categories where swarm_type='sales-email'` = 5
  - `select enabled from public.orq_agents where agent_key='stage-1-category-classifier'` = false
  - `select is_nullable from information_schema.columns where table_schema='public' and table_name='agent_runs' and column_name='entity'` = YES
- `cd web && pnpm tsc --noEmit` clean.
</verification>

<success_criteria>
- All four blocker-class artifacts in place: agent_runs.entity nullable, sales-email registry seeded, orq_agents placeholder row, mailbox registry decision recorded.
- No worker code touched (Plan 04 territory).
- Operator decisions for firecontrol@/fire@ + entity_brand recorded in commit message and SUMMARY.
</success_criteria>

<output>
After completion, create `.planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-01-SUMMARY.md` recording:
- The three migration filenames + apply timestamps.
- Operator answers verbatim (mailbox IDs and entity_brand value).
- SQL probe results.
</output>
