---
phase: 74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/lib/inngest/events.ts
  - web/lib/inngest/functions/stage-0-safety-worker.ts
  - web/app/api/automations/debtor-email/ingest/route.ts
autonomous: true
requirements:
  - SPEC-REQ-2
  - SPEC-REQ-7
notes: |
  Wave 1, parallel with Plan 01 (zero file overlap). Addresses RESEARCH
  blocker #2 (Stage 0 hardcodes swarm_type='debtor-email'). Threads
  swarm_type from the ingest route → stage-0/email.received event →
  stage-0-safety-worker → classifier/screen.requested event so the new
  Stage-1 worker (Plan 04) receives it deterministically. SPEC.md
  "Constraints" reads "Stage 0 contract preserved" — that locked text
  refers to BEHAVIOR (verdict logic, budget logic). Adding two
  passthrough fields to two event payloads + threading them is contract
  EXTENSION, not contract change, and is explicitly mandated by D-01
  and D-02. No verdict/budget logic touched.
must_haves:
  truths:
    - "stage-0/email.received event payload carries swarm_type: string (required) and entity?: string|null"
    - "classifier/screen.requested event payload carries swarm_type: string (required) and entity?: string|null"
    - "Debtor-email ingest route emits swarm_type='debtor-email' in stage-0/email.received"
    - "stage-0-safety-worker no longer hardcodes 'debtor-email' literals at line 130 / line 156 — instead reads swarm_type from event.data and threads to both classifier/screen.requested emit sites (lines 75 and 177)"
    - "STALE_CHANNEL is derived from swarm_type instead of being a hardcoded string"
  artifacts:
    - path: "web/lib/inngest/events.ts"
      provides: "Extended event schemas for stage-0/email.received and classifier/screen.requested"
    - path: "web/lib/inngest/functions/stage-0-safety-worker.ts"
      provides: "Threaded swarm_type via event.data; no debtor-email literals in emit sites or DB inserts"
    - path: "web/app/api/automations/debtor-email/ingest/route.ts"
      provides: "Ingest route emits swarm_type='debtor-email' in stage-0/email.received"
  key_links:
    - from: "web/app/api/automations/debtor-email/ingest/route.ts"
      to: "stage-0-safety-worker (via stage-0/email.received)"
      via: "inngest.send with swarm_type"
      pattern: "swarm_type:\\s*['\"]debtor-email['\"]"
    - from: "stage-0-safety-worker"
      to: "classifier-screen-worker (via classifier/screen.requested)"
      via: "inngest.send with swarm_type"
      pattern: "swarm_type:\\s*[A-Za-z_][A-Za-z0-9_]*\\s*[,}]"
---

<objective>
Thread `swarm_type` through the Stage 0 → Stage 1 seam so the new
classifier-screen-worker (Plan 04) gets a deterministic value without
introducing any new mailbox→swarm map. Three coordinated edits:
(1) extend two event payload types in events.ts; (2) source swarm_type
from event.data in stage-0-safety-worker and remove all `'debtor-email'`
hardcodes there; (3) populate swarm_type in the debtor-email ingest
route's `stage-0/email.received` emit.

Purpose: D-01 + D-02 rely on this; Plan 04's worker reads `event.data.swarm_type`
on the `classifier/screen.requested` payload directly.

Output: events.ts + stage-0-safety-worker.ts + debtor-email/ingest route.ts
edits, all backwards-compatible (new fields required on the event type but
the only existing emit site is the one we're modifying).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-SPEC.md
@.planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-CONTEXT.md
@.planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-RESEARCH.md
@CLAUDE.md
@web/lib/inngest/events.ts
@web/lib/inngest/functions/stage-0-safety-worker.ts
@web/app/api/automations/debtor-email/ingest/route.ts

<interfaces>
<!-- Current stage-0/email.received schema (events.ts:259-269) -->
```typescript
"stage-0/email.received": {
  data: {
    automation_run_id: string;
    email_id: string;
    message_id: string;
    source_mailbox: string;
    subject: string;
    body_text: string;
    safety_overridden?: boolean;
  };
};
```

<!-- Current classifier/screen.requested schema (events.ts:297-307) -->
```typescript
"classifier/screen.requested": {
  data: {
    automation_run_id: string;
    email_id: string;
    message_id: string;
    source_mailbox: string;
    subject: string;
    body_text: string;
    safety_overridden?: boolean;
  };
};
```

<!-- Stage 0 worker hotspots (verified via grep) -->
- stage-0-safety-worker.ts:36 — `const STALE_CHANNEL = "debtor-email-review";`
- stage-0-safety-worker.ts:75 — first `inngest.send({name:"classifier/screen.requested", data:{...}})` (safe-verdict path)
- stage-0-safety-worker.ts:120 — `await emitAutomationRunStale(admin, STALE_CHANNEL);`
- stage-0-safety-worker.ts:128 — `automation: "debtor-email-review"` (hardcoded literal in DB write)
- stage-0-safety-worker.ts:130 — `swarm_type: "debtor-email"` (hardcoded in DB write)
- stage-0-safety-worker.ts:156 — `swarm_type: "debtor-email"` (second hardcoded in DB write)
- stage-0-safety-worker.ts:177 — second `inngest.send({name:"classifier/screen.requested", ...})` (safety_overridden path)
- stage-0-safety-worker.ts:190 — `await emitAutomationRunStale(admin, STALE_CHANNEL);`

<!-- Debtor ingest emit site -->
- web/app/api/automations/debtor-email/ingest/route.ts:600-652 — `inngest.send({name:"stage-0/email.received", data:{...}})` — does NOT currently include swarm_type.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend events.ts schemas with swarm_type + entity</name>
  <files>web/lib/inngest/events.ts</files>
  <read_first>
    - web/lib/inngest/events.ts:259-307 (the two event payloads to extend)
    - web/lib/inngest/events.ts:165-180 (existing classifier/verdict.recorded shape — reference for swarm_type field placement convention)
  </read_first>
  <action>
    Edit `web/lib/inngest/events.ts`. Two changes only:

    1. In `"stage-0/email.received"` payload (currently lines ~259-269), add
       two fields immediately after `body_text`:

       ```typescript
       swarm_type: string;
       entity?: string | null;
       ```

       `swarm_type` is REQUIRED (the ingest route owns it deterministically
       from the route path). `entity` is optional/nullable because sales-email
       has no entity concept.

    2. In `"classifier/screen.requested"` payload (currently lines ~297-307),
       add the same two fields in the same position (after `body_text`):

       ```typescript
       swarm_type: string;
       entity?: string | null;
       ```

       Per CONTEXT D-01 (swarm_type) and D-02 (entity passthrough so Plan 04
       can write pipeline_events without re-deriving entity).

    Do NOT modify any other event payload in this file. Do NOT change the
    JSDoc above the two payloads (the documentation about Stage 0 trust
    boundary is still accurate — we're extending the data shape, not
    renaming or repurposing the event).

    Run `cd web && pnpm tsc --noEmit` after the edit. The compiler will
    flag every emit site that doesn't yet supply `swarm_type`. Expected
    flagged sites: (a) `web/app/api/automations/debtor-email/ingest/route.ts`
    around the `stage-0/email.received` emit; (b) `web/lib/inngest/functions/stage-0-safety-worker.ts`
    at the two `classifier/screen.requested` emits (lines 75, 177). These
    are addressed in Tasks 2 and 3. NO other call sites should be flagged
    (verify via the tsc output).
  </action>
  <acceptance_criteria>
    - `grep -A 15 '"stage-0/email.received":' web/lib/inngest/events.ts | grep -c "swarm_type: string"` returns 1.
    - `grep -A 15 '"classifier/screen.requested":' web/lib/inngest/events.ts | grep -c "swarm_type: string"` returns 1.
    - `grep -A 15 '"stage-0/email.received":' web/lib/inngest/events.ts | grep -c "entity?: string \\| null"` returns 1.
    - `grep -A 15 '"classifier/screen.requested":' web/lib/inngest/events.ts | grep -c "entity?: string \\| null"` returns 1.
    - `cd web && pnpm tsc --noEmit` flags only the three known emit sites listed in `<action>` (debtor-email/ingest, stage-0-safety-worker @ ~line 75, stage-0-safety-worker @ ~line 177). Any other failure is a regression — investigate before continuing.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/agent-workforce && grep -A 15 '"stage-0/email.received":' web/lib/inngest/events.ts | grep -c "swarm_type: string" && grep -A 15 '"classifier/screen.requested":' web/lib/inngest/events.ts | grep -c "swarm_type: string"</automated>
  </verify>
  <done>events.ts compiles standalone (against unmodified callers tsc only flags the 3 expected sites); both payloads carry swarm_type + entity.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: De-hardcode stage-0-safety-worker — thread swarm_type through</name>
  <files>web/lib/inngest/functions/stage-0-safety-worker.ts</files>
  <read_first>
    - web/lib/inngest/functions/stage-0-safety-worker.ts (full file — focus on lines 36, 75, 120, 128-130, 156, 177, 190)
    - .planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-RESEARCH.md (Pitfall 2 — exact list of hardcoded sites)
    - CLAUDE.md (Inngest section — never destructure `inngest.send`, `step.run` discipline)
  </read_first>
  <action>
    Edit `web/lib/inngest/functions/stage-0-safety-worker.ts`. Six coordinated changes:

    1. **Destructure swarm_type + entity from event.data** at the top of the
       handler body (alongside the existing destructure of automation_run_id,
       email_id, etc.). Add:
       ```typescript
       const swarm_type = event.data.swarm_type;
       const entity = event.data.entity ?? null;
       ```
       Place these after the existing destructure block.

    2. **Replace the `STALE_CHANNEL` constant at line 36** with a derived
       per-call value. Either:
       - Delete the line `const STALE_CHANNEL = "debtor-email-review";` and
         replace its two consumer sites (lines ~120 and ~190) with a local
         constant computed inside the handler:
         ```typescript
         const staleChannel = `${swarm_type}-review`;
         ```
         Place the assignment after the `swarm_type` destructure. Then update
         lines ~120 and ~190 to use `staleChannel` instead of `STALE_CHANNEL`.
       - This eliminates the module-level hardcode AND makes the value
         swarm-agnostic for sales-email/future swarms.

    3. **Line 75 emit (`classifier/screen.requested`, safe-verdict path):**
       add `swarm_type` and `entity` to the `data` object passed to
       `inngest.send`:
       ```typescript
       (inngest.send as unknown as SendFn)({
         name: "classifier/screen.requested",
         data: {
           automation_run_id,
           email_id,
           message_id,
           source_mailbox,
           subject,
           body_text,
           swarm_type,        // NEW per Phase 74 D-01
           entity,            // NEW per Phase 74 D-02
           // …existing fields preserved (e.g. safety_overridden if present here)
         },
       });
       ```
       Preserve the existing `inngest.send` cast pattern (`SendFn` type alias
       — CLAUDE.md never-destructure rule). If the file does not already
       have a `SendFn` alias, add one at the top following the
       `classifier-label-resolver.ts:34` template.

    4. **Line 128 (`automation: "debtor-email-review"` literal in a DB write):**
       replace the literal with `${swarm_type}-review` (template string) so
       the stale-marker DB write reflects the actual swarm.

    5. **Lines 130 and 156 (two `swarm_type: "debtor-email"` literals in DB
       writes):** replace BOTH with `swarm_type` (the destructured variable).

    6. **Line 177 emit (`classifier/screen.requested`, safety_overridden
       path):** apply the same change as line 75 — add `swarm_type` and
       `entity` to the data object.

    After editing, run `grep -n '"debtor-email"' web/lib/inngest/functions/stage-0-safety-worker.ts`. **The output must be empty.** Any remaining literal is a regression.

    Run `cd web && pnpm tsc --noEmit`. Errors at the two emit sites must be
    resolved by the changes above. The final tsc output should only flag
    `web/app/api/automations/debtor-email/ingest/route.ts` (Task 3 fixes that).

    Run `cd web && pnpm vitest run lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts` if the test file exists. Adjust mocks/fixtures only if compilation requires it (do NOT change test assertions — they must still cover the existing behavior).
  </action>
  <acceptance_criteria>
    - `grep -c '"debtor-email"' web/lib/inngest/functions/stage-0-safety-worker.ts` returns 0 (zero hardcoded debtor-email string literals).
    - `grep -c "STALE_CHANNEL" web/lib/inngest/functions/stage-0-safety-worker.ts` returns 0 OR returns the 2 consumer-site references with the constant renamed/derived from swarm_type — operator's choice between full delete vs rename, both acceptable as long as the value is no longer a module-level literal.
    - `grep -n "swarm_type," web/lib/inngest/functions/stage-0-safety-worker.ts` shows >= 4 occurrences (2 emit sites passing it through + 2 DB write sites using the variable).
    - `grep -n "entity," web/lib/inngest/functions/stage-0-safety-worker.ts` shows >= 2 occurrences (2 emit sites passing it through).
    - `cd web && pnpm tsc --noEmit` flags ONLY the debtor-email ingest route (Task 3 fixes that).
    - The destructured `swarm_type` is read from `event.data` before being passed downstream (verify by grep: `grep -n "event.data.swarm_type\\|swarm_type = event.data" web/lib/inngest/functions/stage-0-safety-worker.ts` returns >= 1).
    - Vitest stage-0 test suite (if exists) still green: `cd web && pnpm vitest run lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts` exits 0 (or fixture updates limited to threading the new fields through mocks).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/agent-workforce && test "$(grep -c '"debtor-email"' web/lib/inngest/functions/stage-0-safety-worker.ts)" = "0"</automated>
  </verify>
  <done>Zero `"debtor-email"` literals; both emit sites + DB writes thread swarm_type from event.data; tsc errors at this file resolved; existing tests still green (or fixtures updated minimally).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Debtor-email ingest route emits swarm_type in stage-0/email.received</name>
  <files>web/app/api/automations/debtor-email/ingest/route.ts</files>
  <read_first>
    - web/app/api/automations/debtor-email/ingest/route.ts (lines 600-652 — the `stage-0/email.received` emit site)
    - .planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-RESEARCH.md (Pitfall 2 — coordinated change list)
  </read_first>
  <action>
    Edit `web/app/api/automations/debtor-email/ingest/route.ts`. One change:
    add `swarm_type: "debtor-email"` and `entity: <existing entity variable
    if available, else null>` to the `data` object passed to the
    `inngest.send({name:"stage-0/email.received", data:{...}})` call around
    line 600-652.

    The route is `/api/automations/debtor-email/ingest`, so `swarm_type` is
    deterministically `"debtor-email"` — this is the ONE location where the
    literal is correct (it's the source-of-truth derivation point, mirroring
    how the route path identifies the swarm). Per CLAUDE.md registry-driven
    principle: literals at ingest boundaries are acceptable when the route
    path itself encodes the swarm; literals INSIDE shared workers are not.

    For `entity`: if the route already computes an `entity` variable (e.g.
    from labeling_settings or the source_mailbox→entity map), pass it
    through. If not, pass `null` — Plan 04's worker handles null entity
    fine (it writes `null` to agent_runs.entity, which Plan 01 made
    nullable).

    Run `cd web && pnpm tsc --noEmit` — the file should now compile.
  </action>
  <acceptance_criteria>
    - `grep -A 25 'name: "stage-0/email.received"' web/app/api/automations/debtor-email/ingest/route.ts | grep -c 'swarm_type:'` returns >= 1.
    - The added value is the literal string `"debtor-email"` (verify: `grep -A 25 'name: "stage-0/email.received"' web/app/api/automations/debtor-email/ingest/route.ts | grep -c 'swarm_type: "debtor-email"'` returns 1).
    - `grep -A 25 'name: "stage-0/email.received"' web/app/api/automations/debtor-email/ingest/route.ts | grep -c 'entity'` returns >= 1.
    - `cd web && pnpm tsc --noEmit` exits 0 across the entire repo (events.ts type extension fully satisfied).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/agent-workforce/web && pnpm tsc --noEmit</automated>
  </verify>
  <done>Ingest route emits swarm_type='debtor-email' + entity in stage-0/email.received; full repo tsc clean.</done>
</task>

</tasks>

<verification>
- `grep -rn '"debtor-email"' web/lib/inngest/functions/stage-0-safety-worker.ts` returns zero matches.
- `cd web && pnpm tsc --noEmit` exits 0.
- `cd web && pnpm vitest run lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts` (if file exists) exits 0.
- Two extended event payloads in events.ts each carry swarm_type (required) + entity (optional/nullable).
- Debtor-email ingest route emits swarm_type='debtor-email' in stage-0/email.received data.
</verification>

<success_criteria>
- Stage 0 → Stage 1 seam carries swarm_type deterministically from ingest route through both events.
- No `"debtor-email"` literal remains in stage-0-safety-worker.ts.
- Plan 04 can rely on `event.data.swarm_type` being present and string-typed on classifier/screen.requested.
</success_criteria>

<output>
After completion, create `.planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-02-SUMMARY.md` recording:
- The exact list of literals removed and replaced (with line numbers).
- The shape of the SendFn type alias used at the two emit sites.
- Whether the existing stage-0 test fixture file required an update (and what).
</output>
