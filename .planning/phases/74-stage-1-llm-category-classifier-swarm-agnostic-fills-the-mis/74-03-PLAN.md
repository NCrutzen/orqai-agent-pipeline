---
phase: 74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis
plan: 03
type: execute
wave: 2
depends_on: [74-01]
files_modified:
  - supabase/migrations/20260506_phase74_stage1_classifier_agent_activate.sql
autonomous: false
requirements:
  - SPEC-REQ-1
notes: |
  Wave 2. Depends on Plan 01 only (orq_agents placeholder row must exist
  before Studio create_agent + UPDATE-orqai_id step). The Orq.ai create →
  Studio JSON Schema → update → get_agent ritual is documented in CLAUDE.md
  (cba7352b learning + 2026-05-01 Studio observation). MCP exposes agent
  CRUD but NOT JSON-schema-tool CRUD — the Response Format dropdown step
  is operator-manual in Studio.
must_haves:
  truths:
    - "Orq.ai catalog list_models contains all 3 IDs: aws/eu.anthropic.claude-haiku-4-5-20251001-v1:0, openai/gpt-4o-mini, google-ai/gemini-2.5-flash"
    - "Orq agent stage-1-category-classifier exists in Orq.ai with primary model verified, fallback chain set, and strict json_schema response_format persisted (visible in get_agent and Studio Response Format dropdown)"
    - "public.orq_agents row for stage-1-category-classifier has orqai_id=<real slug>, enabled=true"
    - "Smoke test: invokeOrqAgent('stage-1-category-classifier', auto-reply fixture) returns parseable JSON with category_key='auto_reply' and confidence in {medium,high}"
  artifacts:
    - path: "supabase/migrations/20260506_phase74_stage1_classifier_agent_activate.sql"
      provides: "UPDATE statement that flips enabled=true and sets orqai_id to the real Studio slug after Studio + MCP ritual completes"
  key_links:
    - from: "Plan 04 worker (invokeOrqAgent)"
      to: "Orq.ai agent stage-1-category-classifier"
      via: "registry lookup (public.orq_agents.orqai_id)"
      pattern: "invokeOrqAgent\\(['\"]stage-1-category-classifier['\"]"
---

<objective>
Provision the Stage-1 LLM agent in Orq.ai following CLAUDE.md's
list_models → create_agent → Studio JSON Schema tool → update_agent →
get_agent verify ritual (D-09). End state: a functioning Orq agent +
DB row that Plan 04's worker can invoke via the standard
`invokeOrqAgent("stage-1-category-classifier", ...)` registry path,
with strict response_format json_schema enforcement persisted
server-side.

Purpose: D-05/D-06 + the Orq.ai workflow learnings demand this exact
sequence. Per CLAUDE.md `cba7352b`, `create_agent` silently drops
`response_format` so the Studio + PATCH step is non-negotiable. Per
CLAUDE.md `f980a2a1`, `update_agent` accepts unknown model IDs
silently — `list_models` pre-flight is the contracted defense.

Output: Operator-driven ritual + a small UPDATE migration that flips
`public.orq_agents.enabled=true` and writes the real `orqai_id` once
the Studio steps complete and `get_agent` verifies persistence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-SPEC.md
@.planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-CONTEXT.md
@.planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-RESEARCH.md
@CLAUDE.md
@docs/orqai-patterns.md
@.planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-01-SUMMARY.md
@web/lib/automations/orq-agents/client.ts

<interfaces>
<!-- Locked agent contract (D-05, D-06) -->
- agent_key: stage-1-category-classifier
- swarm_type: cross-cutting
- primary model: aws/eu.anthropic.claude-haiku-4-5-20251001-v1:0
- fallbacks: openai/gpt-4o-mini → google-ai/gemini-2.5-flash
- response_format: strict json_schema with anyOf nullable for `reasoning`
- output: { category_key: string, confidence: low|medium|high, reasoning: string|null }
- input: { subject, body_text, categories: [{category_key, display_label}, ...] }
- temperature: 0
- max_tokens: 400
- timeout_ms: 45000

<!-- Prompt structure (D-08, planner-drafted; reference debtor-intent-agent) -->
The agent prompt MUST follow CLAUDE.md XML convention:
  <role>You are a Stage-1 email category classifier. You read an inbound message and pick exactly one category_key from the supplied list (or "unknown" if no listed category fits with at least medium confidence).</role>
  <task>Classify the message into one of the listed category_keys. Output strict JSON matching the response_format schema.</task>
  <categories>{{ injected at call time as JSON of (category_key, display_label) pairs }}</categories>
  <input><subject>{{subject}}</subject><body>{{body_text}}</body></input>
  <constraints>
    - You MUST pick a category_key from the listed categories OR the literal string "unknown".
    - confidence "high" = explicit phrase signaling the category (e.g. "Out of office until X", "Auto-reply", invoice payment confirmation reference).
    - confidence "medium" = strong contextual signal but no explicit phrase.
    - confidence "low" = ambiguous; the worker will coerce this to "unknown".
    - reasoning is optional; null if no concise reason fits.
  </constraints>
  <output_format>
    Strict JSON. No markdown. No prose outside the JSON object.
  </output_format>
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Orq.ai MCP + Studio ritual</name>
  <what-needed>
    Operator (or Claude with Orq MCP access) executes the following sequence
    in order. The MCP `mcp__orq__*` tools are the canonical path; the
    Studio JSON Schema tool resource step is the only manual UI step
    (CLAUDE.md 2026-05-01 Studio observation: Orq MCP exposes NO tool CRUD).

    **STEP 1 — list_models pre-flight (CLAUDE.md `f980a2a1`):**
      Call `mcp__orq__list_models` and confirm ALL THREE model IDs are
      present in the catalog response:
        - aws/eu.anthropic.claude-haiku-4-5-20251001-v1:0
        - openai/gpt-4o-mini
        - google-ai/gemini-2.5-flash
      If ANY ID is missing, STOP. Either the Orq catalog has shifted (consult
      CLAUDE.md "Anthropic model routing in Orq.ai" section for current
      Haiku/Sonnet IDs) or pick a substitute and update Plan 01's
      orq_agents migration `model_config` accordingly.

    **STEP 2 — create_agent (bare, no parameters):**
      Call `mcp__orq__create_agent` with:
        - key: stage-1-category-classifier
        - description: "Stage 1 LLM category classifier (Phase 74). Swarm-agnostic. Reads enabled swarm_categories at call time and emits one of (category_key, 'unknown') with confidence low/medium/high."
        - model: aws/eu.anthropic.claude-haiku-4-5-20251001-v1:0  (bare ID; per CLAUDE.md `cba7352b` `create_agent` drops response_format silently — that's why this step is bare)
        - prompt: full XML-tagged prompt from the `<interfaces>` block above (role, task, categories, input, constraints, output_format)
      Capture the returned slug (it goes into Task 2's UPDATE).

    **STEP 3 — Studio: create JSON Schema tool resource:**
      In Orq.ai Studio → Tools → Add → JSON Schema:
        - name: stage_1_classifier_v1
        - description: "Strict output schema for stage-1-category-classifier (Phase 74)."
        - schema: paste the EXACT JSON below:
          ```json
          {
            "type": "object",
            "additionalProperties": false,
            "required": ["category_key", "confidence", "reasoning"],
            "properties": {
              "category_key": { "type": "string" },
              "confidence":   { "type": "string", "enum": ["low", "medium", "high"] },
              "reasoning":    { "anyOf": [{"type": "string"}, {"type": "null"}] }
            }
          }
          ```
        - strict: true (CLAUDE.md `3970bad9` — anyOf for nullable, NOT array shorthand)

    **STEP 4 — Studio: set Response Format on the agent:**
      Open the agent → Model parameters → Response Format dropdown =
      "JSON Schema" → select the resource named `stage_1_classifier_v1` from
      the right-hand picker. Save.

    **STEP 5 — update_agent (full model.parameters + fallback_models):**
      Call `mcp__orq__update_agent` with:
        - key: stage-1-category-classifier
        - model.parameters:
            - temperature: 0
            - max_tokens: 400
            - response_format: (the json_schema reference picked in Step 4 — pass it through if MCP carries it back; else Studio's setting persists and the PATCH only carries temperature + max_tokens)
        - fallback_models: ["openai/gpt-4o-mini", "google-ai/gemini-2.5-flash"]
          (NOTE: per CLAUDE.md fallback_models is at AGENT ROOT level, NOT nested in model.parameters — Phase 65-02 learning.)

    **STEP 6 — get_agent verify (CLAUDE.md `cba7352b`):**
      Call `mcp__orq__get_agent` for `stage-1-category-classifier`. Verify
      the response shows:
        - model.parameters.response_format is populated AND references the
          json_schema (not empty / not null)
        - fallback_models = ["openai/gpt-4o-mini", "google-ai/gemini-2.5-flash"]
        - model = "aws/eu.anthropic.claude-haiku-4-5-20251001-v1:0"
      ALSO: open the Studio agent UI and confirm the Model dropdown is
      populated (NOT empty). An empty Studio dropdown means an unknown
      model ID slipped through despite Step 1 — abort and re-list.

    **STEP 7 — smoke fixture call:**
      Send a sample input via Studio Playground OR via `invokeOrqAgent`:
        ```
        subject: "Out of office until 15 May 2026"
        body_text: "I am out of office until Friday 15 May 2026 and will respond on my return. For urgent matters contact xyz."
        categories: [
          {category_key:"auto_reply",         display_label:"Auto-reply"},
          {category_key:"ooo_temporary",      display_label:"OOO (temporary)"},
          {category_key:"ooo_permanent",      display_label:"OOO (permanent)"},
          {category_key:"payment_admittance", display_label:"Payment Admittance"},
          {category_key:"unknown",            display_label:"Unknown (manual review)"}
        ]
        ```
      Expected: `{ "category_key": "ooo_temporary", "confidence": "medium" or "high", "reasoning": <string|null> }`. JSON parses cleanly. (REQ-1 acceptance.)

    Capture the real Orq agent slug (Step 2 return) — Task 2 needs it.
  </what-needed>
  <how-to-verify>
    Operator pastes:
    1. The output of `mcp__orq__list_models` filtered for the three IDs (or screenshot/JSON excerpt).
    2. The slug returned by `mcp__orq__create_agent`.
    3. The relevant excerpt of `mcp__orq__get_agent` showing model.parameters.response_format populated and fallback_models set.
    4. The smoke fixture response.
  </how-to-verify>
  <resume-signal>
    Provide:
      - Orq agent slug for stage-1-category-classifier: <slug>
      - get_agent verify: response_format persisted = YES/NO
      - smoke fixture result: <pasted JSON>
    If any verification fails, describe what failed and STOP — do not
    proceed to Task 2.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Activation migration — UPDATE orq_agents.orqai_id + enabled=true</name>
  <files>supabase/migrations/20260506_phase74_stage1_classifier_agent_activate.sql</files>
  <read_first>
    - .planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-01-SUMMARY.md (Plan 01 confirmation that orq_agents row exists with enabled=false placeholder)
    - Operator answer from Task 1 with the real Orq slug
  </read_first>
  <action>
    Create `supabase/migrations/20260506_phase74_stage1_classifier_agent_activate.sql`
    with EXACTLY this body, substituting the operator-supplied slug:

    ```sql
    -- Phase 74 — activate the stage-1-category-classifier agent registry row
    -- after Studio + MCP ritual confirms get_agent shows persisted strict
    -- response_format and the smoke fixture returns parseable JSON.
    -- Follow-up to: 20260506_phase74_stage1_classifier_agent.sql
    -- Operator-supplied slug: <PASTE FROM TASK 1 RESUME SIGNAL>

    update public.orq_agents
       set orqai_id  = '<REAL_SLUG_FROM_TASK_1>',
           enabled   = true,
           updated_at = now()
     where agent_key = 'stage-1-category-classifier';

    -- Sanity: confirm exactly 1 row updated.
    do $$
    declare cnt int;
    begin
      select count(*) into cnt
        from public.orq_agents
       where agent_key='stage-1-category-classifier'
         and enabled=true
         and orqai_id is not null
         and orqai_id <> 'PLACEHOLDER_STAGE1_CLASSIFIER_SLUG';
      if cnt <> 1 then
        raise exception 'orq_agents activation failed: expected 1 active row, got %', cnt;
      end if;
    end$$;
    ```

    Apply via Supabase Management API. The DO block raises if the slug
    isn't actually live, providing a deploy-time guard.
  </action>
  <acceptance_criteria>
    - File exists at the specified path.
    - `grep -c "PLACEHOLDER_STAGE1_CLASSIFIER_SLUG" supabase/migrations/20260506_phase74_stage1_classifier_agent_activate.sql` returns 0 (the placeholder is the value being REPLACED, not a value the migration writes).
    - `grep -c "stage-1-category-classifier" supabase/migrations/20260506_phase74_stage1_classifier_agent_activate.sql` returns >= 2 (UPDATE WHERE + DO block check).
    - `grep -c "enabled\\s*=\\s*true" supabase/migrations/20260506_phase74_stage1_classifier_agent_activate.sql` returns >= 1.
    - SQL probe post-apply: `select agent_key, swarm_type, enabled, orqai_id from public.orq_agents where agent_key='stage-1-category-classifier'` returns the real slug and `enabled=true`.
    - SQL probe post-apply: `select count(*) from public.orq_agents where agent_key='stage-1-category-classifier' and orqai_id='PLACEHOLDER_STAGE1_CLASSIFIER_SLUG'` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/agent-workforce && test -f supabase/migrations/20260506_phase74_stage1_classifier_agent_activate.sql && grep -c "stage-1-category-classifier" supabase/migrations/20260506_phase74_stage1_classifier_agent_activate.sql</automated>
  </verify>
  <done>Activation migration file committed; applied; SQL probes pass; the agent is invokable from app code via the standard registry route.</done>
</task>

</tasks>

<verification>
- Studio + MCP ritual completed (Task 1 resume signal recorded).
- `public.orq_agents` row for stage-1-category-classifier has real slug + enabled=true.
- Smoke fixture call returns valid JSON matching the strict schema.
- `mcp__orq__get_agent` shows persisted strict response_format.
</verification>

<success_criteria>
- Plan 04 can rely on `invokeOrqAgent("stage-1-category-classifier", ...)` working without further setup.
- All three model IDs (primary + 2 fallbacks) verified present in Orq catalog at the time of provisioning.
- D-09 ritual completed in the documented order (no skipped steps).
</success_criteria>

<output>
After completion, create `.planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-03-SUMMARY.md` recording:
- The exact list of all three confirmed model IDs from list_models.
- The real Orq agent slug.
- A copy/paste of the smoke fixture response.
- The applied activation migration filename.
</output>
