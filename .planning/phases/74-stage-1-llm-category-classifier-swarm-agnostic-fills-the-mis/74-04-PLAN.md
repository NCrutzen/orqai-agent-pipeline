---
phase: 74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis
plan: 04
type: execute
wave: 3
depends_on: [74-01, 74-02, 74-03]
files_modified:
  - web/lib/inngest/functions/classifier-screen-worker.ts
  - web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts
  - web/app/api/inngest/route.ts
autonomous: true
requirements:
  - SPEC-REQ-2
  - SPEC-REQ-3
  - SPEC-REQ-4
  - SPEC-REQ-5
  - SPEC-REQ-6
notes: |
  Wave 3. Depends on Plan 01 (DB rows), Plan 02 (event swarm_type), and
  Plan 03 (live Orq agent). This is the heart of Phase 74: the
  classifier-screen-worker that consumes classifier/screen.requested,
  runs registry-driven regex-then-LLM, applies confidence gate, dual-writes
  pipeline_events, and emits classifier/verdict.recorded. All side effects
  in step.run; non-deterministic IDs INSIDE step.run; inngest.send via
  SendFn cast (never destructured); retries:0.

  TDD: tdd="true" — RED tests first per D-19.
must_haves:
  truths:
    - "classifier-screen-worker.ts exists, exports classifierScreenWorker, is registered in app/api/inngest/route.ts"
    - "Worker triggers on event 'classifier/screen.requested' with retries:0"
    - "Worker reads swarm_type + entity from event.data (no event-payload literal fallback)"
    - "Worker resolves regex module via swarms.stage1_regex_module dynamic import (no swarm_type literal in worker code)"
    - "When swarms.stage1_regex_module is null (sales-email): regex skipped → LLM invoked unconditionally"
    - "When regex returns non-unknown category: LLM is NOT invoked; agent_runs row NOT inserted"
    - "When LLM returns confidence='low': final predicted_category coerced to 'unknown'"
    - "When LLM throws / schema-fails: agent_runs.status='failed' with error_message; verdict still emits with predicted_category='unknown'"
    - "Each invocation writes exactly one pipeline_events row via emitPipelineEvent"
    - "LLM-invoked branch additionally writes exactly one agent_runs row"
    - "Worker emits classifier/verdict.recorded with swarm_type, predicted_category, source_mailbox passed through"
  artifacts:
    - path: "web/lib/inngest/functions/classifier-screen-worker.ts"
      provides: "The worker function — full D-16 step chain"
      min_lines: 150
    - path: "web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts"
      provides: "RED tests covering D-19's 7 cases + Pitfalls 4 (replay-id) and 6 (empty categories)"
      min_lines: 200
    - path: "web/app/api/inngest/route.ts"
      provides: "Updated to import + register classifierScreenWorker"
  key_links:
    - from: "web/lib/inngest/functions/classifier-screen-worker.ts"
      to: "swarms + swarm_categories registry"
      via: "loadSwarm + loadSwarmCategories"
      pattern: "loadSwarm\\(admin"
    - from: "web/lib/inngest/functions/classifier-screen-worker.ts"
      to: "Orq agent stage-1-category-classifier"
      via: "invokeOrqAgent"
      pattern: "invokeOrqAgent\\(['\"]stage-1-category-classifier['\"]"
    - from: "web/lib/inngest/functions/classifier-screen-worker.ts"
      to: "pipeline_events table"
      via: "emitPipelineEvent helper"
      pattern: "emitPipelineEvent\\(admin"
    - from: "web/lib/inngest/functions/classifier-screen-worker.ts"
      to: "classifier-verdict-worker (existing)"
      via: "inngest.send classifier/verdict.recorded"
      pattern: "classifier/verdict\\.recorded"
    - from: "web/app/api/inngest/route.ts"
      to: "Inngest serve handler registration"
      via: "import + functions array"
      pattern: "classifierScreenWorker"
---

<objective>
Implement the classifier-screen-worker per D-16 step ordering:
load-swarm-row → regex (registry-driven dynamic import, skipped if
stage1_regex_module=null) → llm-call (only on regex='unknown', with
agent_runs INSERT inside step.run, ID generated inside step.run, error
coerced to unknown/low) → emit-pipeline-event (Phase 70 dual-write) →
emit-verdict (classifier/verdict.recorded). Add RED tests covering D-19's
7 cases. Register the function with the Inngest serve handler.

Purpose: This is THE seam closure. After this plan, Stage 0 → Stage 1 is
no longer empty. Cross-swarm reuse works because the worker has zero
swarm-type literals and reads everything from registry rows seeded in
Plan 01.

Output: One worker file, one test file, one route registration edit.
SPEC REQ-2..REQ-6 verifiable by automated tests + grep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-SPEC.md
@.planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-CONTEXT.md
@.planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-RESEARCH.md
@CLAUDE.md
@.planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-01-SUMMARY.md
@.planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-02-SUMMARY.md
@.planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-03-SUMMARY.md
@web/lib/inngest/functions/classifier-label-resolver.ts
@web/lib/inngest/functions/classifier-verdict-worker.ts
@web/lib/inngest/functions/__tests__/classifier-verdict-worker.test.ts
@web/lib/swarms/registry.ts
@web/lib/swarms/types.ts
@web/lib/pipeline-events/emit.ts
@web/lib/pipeline-events/types.ts
@web/lib/automations/orq-agents/client.ts
@web/lib/inngest/client.ts
@web/lib/inngest/events.ts
@web/lib/debtor-email/classify.ts
@web/lib/automations/debtor-email/llm-tiebreaker.ts
@web/app/api/inngest/route.ts

<interfaces>
<!-- inngest function shape (template: classifier-verdict-worker.ts) -->
```typescript
import { inngest } from "@/lib/inngest/client";

type SendFn = (p: { name: string; data: Record<string, unknown> }) => Promise<unknown>;

export const classifierScreenWorker = inngest.createFunction(
  { id: "classifier/screen-worker", retries: 0 },
  { event: "classifier/screen.requested" },
  async ({ event, step }) => { /* ... */ },
);
```

<!-- registry helpers (web/lib/swarms/registry.ts) -->
- `loadSwarm(admin, swarm_type): Promise<SwarmRow | null>` — 60s cache, last-known-good on transient error.
- `loadSwarmCategories(admin, swarm_type): Promise<SwarmCategoryRow[]>` — same caching.

<!-- SwarmRow shape (web/lib/swarms/types.ts) -->
- swarm_type: string
- stage1_regex_module: string | null      ← null = LLM-only (D-03)
- enabled: boolean
- ...other Phase 68 columns

<!-- SwarmCategoryRow -->
- swarm_type, category_key, display_label, outlook_label, action, swarm_dispatch, display_order, enabled

<!-- emitPipelineEvent (web/lib/pipeline-events/emit.ts) -->
```typescript
emitPipelineEvent(admin, {
  swarm_type, stage: 1, email_id, decision, confidence,
  decision_details, agent_run_id, automation_run_id, triggered_by,
})
```

<!-- numericConfidence (pipeline-events/types.ts) -->
- maps low|medium|high|none → 0.4|0.7|0.9|null

<!-- invokeOrqAgent (orq-agents/client.ts:113) -->
```typescript
const result = await invokeOrqAgent("stage-1-category-classifier", inputs);
// result.raw is the parsed JSON; pass through zod for type safety
```

<!-- regex module contract (web/lib/debtor-email/classify.ts) -->
```typescript
classify(input: { subject: string; from: string; bodySnippet: string }): {
  category: string;        // 'auto_reply'|'ooo_temporary'|'ooo_permanent'|'payment_admittance'|'unknown'
  matchedRule: string | null;
}
```

<!-- agent_runs INSERT shape (template: classifier-invoice-copy-handler) -->
columns used by the worker: id (uuid generated INSIDE step.run),
swarm_type, automation_run_id, email_id, inngest_run_id, entity (nullable
post Plan 01), status ('predicted' | 'failed'), confidence (text low|medium|high),
reasoning, tool_outputs (jsonb), error_message (on failure path).

<!-- classifier/verdict.recorded payload (events.ts:165-180) -->
```typescript
"classifier/verdict.recorded": {
  data: {
    automation_run_id: string;
    swarm_type: string;
    decision: "approve" | "reject" | "manual_review";
    message_id: string;
    source_mailbox: string;
    predicted_category: string;
    override_category: string | null;
    // ...optional fields preserved per existing emit sites
  };
};
```

<!-- Inngest registration (web/app/api/inngest/route.ts:32, 65) -->
- import line at top: `import { classifierVerdictWorker } from "@/lib/inngest/functions/classifier-verdict-worker";`
- functions array passed to `serve(...)`: contains `classifierVerdictWorker,`
- ADD: import classifierScreenWorker; add it to the same functions array.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write classifier-screen-worker.test.ts (RED tests, D-19 + Pitfalls 4 & 6)</name>
  <files>web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts</files>
  <read_first>
    - web/lib/inngest/functions/__tests__/classifier-verdict-worker.test.ts (full file — primary mock pattern template)
    - web/lib/inngest/functions/__tests__/classifier-label-resolver.test.ts (extended mock pattern with side-effect mocks)
    - .planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-RESEARCH.md (Validation Architecture section — Phase Requirements → Test Map table)
    - CONTEXT.md D-19 (test fixture list)
  </read_first>
  <behavior>
    Test file MUST contain at least these test cases (vitest `describe` /
    `it` blocks). Each maps to a SPEC REQ:

    1. **REQ-3 / regex-hit-skip-LLM (debtor-email):**
       Mock `loadSwarm` returns `{stage1_regex_module:'@/lib/debtor-email/classify', ...}`,
       mock the dynamic import to a `classify` returning `{category:'payment_admittance', matchedRule:'payment_kw_v1'}`.
       Invoke handler. Assert: `invokeOrqAgent` NOT called; supabase
       `from('agent_runs').insert` NOT called; `emitPipelineEvent` called
       once with `decision='payment_admittance'`; `inngest.send` called
       once with name=`classifier/verdict.recorded` and
       `predicted_category='payment_admittance'`.

    2. **REQ-3 / sales-email no-regex → LLM-only:**
       Mock `loadSwarm` returns `{stage1_regex_module:null, ...}`. Mock
       `invokeOrqAgent` returns `{raw:{category_key:'auto_reply', confidence:'high', reasoning:null}}`.
       Invoke handler with `swarm_type='sales-email'`. Assert:
       `invokeOrqAgent` called exactly once; `agent_runs.insert` called
       exactly once with `swarm_type='sales-email'`; verdict event has
       `predicted_category='auto_reply'`.

    3. **REQ-4 / LLM low-confidence → unknown coercion:**
       Same mock as #2 but `invokeOrqAgent` returns
       `{raw:{category_key:'auto_reply', confidence:'low', reasoning:'unsure'}}`.
       Assert verdict event has `predicted_category='unknown'`. Assert
       agent_runs row inserted with confidence='low'.

    4. **REQ-4 / LLM medium pass-through:**
       Confidence 'medium' returns whatever the LLM picked. Verify
       `predicted_category` matches the LLM's `category_key`.

    5. **D-11 / LLM error → unknown coercion:**
       `invokeOrqAgent` throws. Worker MUST NOT throw. Assert:
       `agent_runs.insert` called once with status='failed' and a non-null
       error_message; `emitPipelineEvent` called once; verdict event emitted
       with `predicted_category='unknown'`. NO retries (function is retries:0).

    6. **REQ-5 / pipeline_events row count = 1:**
       In any of the above paths assert `emitPipelineEvent` mock call
       count === 1 per worker invocation.

    7. **REQ-5 / agent_runs row only when LLM invoked:**
       For the regex-hit case (#1), assert `from('agent_runs').insert` was
       NEVER called. For the LLM cases (#2-#5), assert it was called exactly
       once.

    8. **REQ-6 / sales-email payload processes without throwing:**
       Re-use case #2; additionally assert the handler returns
       `{ok:true, regex_category:'unknown', llm_invoked:true, final_category_key:'auto_reply'}`.

    9. **Pitfall 4 / replay-id stability:**
       Re-invoke the handler twice with the SAME `event.id` (mocking the
       step.run cache to return the cached value on second call). Assert
       `from('agent_runs').insert` total call count is 1, not 2.
       (Mock approach: assert the UUID returned by the `crypto.randomUUID`
       call is captured inside the step.run boundary — mirror the pattern
       in `classifier-verdict-worker.test.ts`.)

    10. **Pitfall 6 / empty categories list:**
       Mock `loadSwarmCategories` returns `[]`. Worker MUST coerce to
       `predicted_category='unknown'` and SHOULD NOT call `invokeOrqAgent`
       (since there's nothing to pick from). Assert no LLM call; verdict
       emitted with `predicted_category='unknown'`.

    11. **REQ-6 (static check) / no swarm_type literals:**
       Add a small fs.readFileSync-based assertion at the bottom of the
       test file:
       ```typescript
       it("contains zero swarm_type === 'X' branches (REQ-6 static check)", () => {
         const src = readFileSync(
           resolve(__dirname, "../classifier-screen-worker.ts"),
           "utf8",
         );
         expect(src).not.toMatch(/swarm_type\s*===\s*['"](sales-email|debtor-email)['"]/);
       });
       ```

    Mock conventions (template from classifier-verdict-worker.test.ts):
    - vi.mock the helpers (loadSwarm, loadSwarmCategories, emitPipelineEvent, invokeOrqAgent, createAdminClient).
    - Mock supabase admin chain: `from(table).insert(row).select(cols).single()` returning `{data, error}`.
    - Use `vi.fn()` to track call counts.
    - Construct an Inngest event via the same factory pattern used in the verdict-worker test.
  </behavior>
  <action>
    Create `web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts`
    with the above tests. ALL 11 cases listed in `<behavior>`. Tests MUST
    fail RED initially because the worker file (Task 2) doesn't exist yet —
    that's the TDD entry point.

    Run `cd web && pnpm vitest run lib/inngest/functions/__tests__/classifier-screen-worker.test.ts`. Expected: tests fail with "module not found" or similar import error. That's the RED state.
  </action>
  <acceptance_criteria>
    - File exists at the specified path.
    - File contains at least 11 `it(...)` blocks (one per case above) + 1 static-check `it`.
    - `grep -c "describe\\|it(" web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts` returns >= 12.
    - Static-check `it` literally tests against the regex `swarm_type\\s*===\\s*['"](sales-email|debtor-email)['"]` (verify by grep).
    - Initial test run is RED (module-not-found acceptable, OR all-fail acceptable). Capture the failing run output for the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/agent-workforce && test -f web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts && grep -c "it(" web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts</automated>
  </verify>
  <done>Test file committed with all 12 it-blocks; running vitest shows RED for the right reason (worker not yet implemented).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement classifier-screen-worker.ts (GREEN — make tests pass)</name>
  <files>web/lib/inngest/functions/classifier-screen-worker.ts</files>
  <read_first>
    - web/lib/inngest/functions/classifier-label-resolver.ts (full file — primary template; SendFn cast at line 34, Phase 70 dual-write at lines 151-228, emit-coordinator at 270-287)
    - web/lib/inngest/functions/classifier-verdict-worker.ts (registry idiom at lines 26-90, loadSwarmCategories use at 71-74)
    - web/lib/automations/debtor-email/llm-tiebreaker.ts (zod-validated invokeOrqAgent pattern at lines 18-80)
    - web/lib/swarms/registry.ts:30-76 (loadSwarm + loadSwarmCategories signatures)
    - web/lib/pipeline-events/emit.ts (emitPipelineEvent shape)
    - web/lib/pipeline-events/types.ts:107 (numericConfidence helper)
    - .planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-RESEARCH.md (Code Example A — full worker skeleton)
  </read_first>
  <behavior>
    The worker MUST satisfy ALL test cases from Task 1. Critical
    invariants (from D-13/14/15/16, RESEARCH Pitfalls):

    - `inngest.createFunction({ id: "classifier/screen-worker", retries: 0 }, { event: "classifier/screen.requested" }, ...)`.
    - Type-cast `inngest.send` via `SendFn` alias; never destructure.
    - All side effects in `step.run()`. Five steps named `load-swarm-row`,
      `regex`, `llm-call`, `emit-pipeline-event`, `emit-verdict` (D-16
      naming; Inngest step names appear in trace UI).
    - UUID for agent_runs.id generated INSIDE `step.run("llm-call")` via
      `crypto.randomUUID()` (or omit `id` and let DB default fill it —
      but be consistent with `classifier-invoice-copy-handler` pattern
      which generates explicitly).
    - swarm_type read from `event.data.swarm_type` directly. NO fallback
      to `?? "debtor-email"` — if missing, throw clearly (registry-driven
      principle: missing swarm_type = bug, not silent default).
    - entity read from `event.data.entity ?? null`.
    - Empty `categories.length === 0` short-circuits to
      `final_category_key='unknown'` BEFORE the LLM call (Pitfall 6
      defense).
    - Regex resolution: `await import(swarmRow.stage1_regex_module)` only
      when non-null. The dynamic `import()` of a registry-supplied path
      is acceptable here (D-04 explicitly mandates it). Errors during
      import propagate (D-12 — run fails).
    - Confidence gate: `parsed.confidence === 'low' ? 'unknown' : parsed.category_key`.
    - LLM error path: catch inside step.run, write agent_runs with
      `status='failed'`, error_message in `tool_outputs.error` AND set
      `error_message` column if it exists (verify via the Plan 01 schema /
      `20260428_public_agent_runs.sql`); re-coerce final_category_key to
      'unknown' and confidence to 'low'. Do NOT rethrow.
    - emitPipelineEvent payload includes:
      - swarm_type, stage:1, email_id
      - decision: final_category_key
      - confidence: numericConfidence(llmConfidence ?? null) — null when regex-hit
      - decision_details: { regex: <regexOutcome>, llm_invoked, llm_category_key, llm_confidence, llm_reasoning, llm_error, final_category_key }
      - agent_run_id (when LLM invoked) or null
      - automation_run_id passthrough
      - triggered_by: 'pipeline'
    - emit-verdict via inline `(inngest.send as unknown as SendFn)({...})`:
      - name: 'classifier/verdict.recorded'
      - data: { automation_run_id, swarm_type, decision: 'approve', message_id, source_mailbox, predicted_category: final_category_key, override_category: null }
      - `decision: 'approve'` matches the existing classifier-verdict-worker
        contract (the verdict-worker dispatches via swarm_categories.action,
        not via decision; preserve existing semantics).
    - Returns `{ ok: true, regex_category, llm_invoked, final_category_key }` for trace observability (test #8 asserts this shape).

    Output schema validation: import zod, define `Stage1OutputSchema` mirroring D-06 (category_key string, confidence enum low|medium|high, reasoning nullable). Parse `result.raw` from invokeOrqAgent.

    NO `swarm_type === 'X'` branches anywhere. The static-check test (Task 1, #11) enforces this.
  </behavior>
  <action>
    Create `web/lib/inngest/functions/classifier-screen-worker.ts` using
    Code Example A from RESEARCH.md as the literal starting point, then
    refine to satisfy every test case in Task 1. Diverge from the example
    only as needed to make the tests green:

    - Add zod schema definition.
    - Add `SendFn` type alias.
    - Add the empty-categories short-circuit (Pitfall 6) — Code Example A
      omits this; tests #10 require it.
    - Verify the regex-step's input shape matches the actual
      `web/lib/debtor-email/classify.ts` `classify` signature
      (`{subject, from, bodySnippet}`); `from` is sender_email and may not
      be on the event payload — read `web/lib/debtor-email/classify.ts`
      first and adjust the inputs accordingly. If `from` isn't in
      `classifier/screen.requested.data`, pass `""` (debtor regex doesn't
      require `from` for auto_reply / OOO patterns; `payment_admittance`
      is body+subject driven).
    - Use Phase 65's exact `inngest.send` cast pattern (template from
      `classifier-label-resolver.ts:34`).
    - Use the `event.id ?? \`local-${message_id}\`` pattern for
      `inngest_run_id` (template from `classifier-invoice-copy-handler`).

    Run iteratively:
      1. `cd web && pnpm vitest run lib/inngest/functions/__tests__/classifier-screen-worker.test.ts`
      2. Read failures; fix worker.
      3. Repeat until all 12 it-blocks GREEN.

    Then run `cd web && pnpm tsc --noEmit` and `cd web && pnpm vitest run` to confirm no broader regressions.
  </action>
  <acceptance_criteria>
    - File exists at the specified path.
    - `grep -c "id: \"classifier/screen-worker\"" web/lib/inngest/functions/classifier-screen-worker.ts` returns 1.
    - `grep -c "retries: 0" web/lib/inngest/functions/classifier-screen-worker.ts` returns 1.
    - `grep -c "event: \"classifier/screen.requested\"" web/lib/inngest/functions/classifier-screen-worker.ts` returns 1.
    - `grep -c "step.run(" web/lib/inngest/functions/classifier-screen-worker.ts` returns >= 5 (5 named steps).
    - `grep -E "swarm_type\\s*===\\s*['\"](sales-email|debtor-email)['\"]" web/lib/inngest/functions/classifier-screen-worker.ts` returns NOTHING (REQ-6).
    - `grep -c "invokeOrqAgent" web/lib/inngest/functions/classifier-screen-worker.ts` returns >= 1 with arg `"stage-1-category-classifier"`.
    - `grep -c "emitPipelineEvent" web/lib/inngest/functions/classifier-screen-worker.ts` returns >= 1.
    - `grep -c "loadSwarm\\|loadSwarmCategories" web/lib/inngest/functions/classifier-screen-worker.ts` returns >= 2.
    - `grep -c "inngest.send as unknown as SendFn" web/lib/inngest/functions/classifier-screen-worker.ts` returns >= 1.
    - `grep -E "const\\s+\\w+\\s*=\\s*inngest\\.send" web/lib/inngest/functions/classifier-screen-worker.ts` returns NOTHING (Pitfall: never destructure).
    - `cd web && pnpm vitest run lib/inngest/functions/__tests__/classifier-screen-worker.test.ts` exits 0 (all 12 it-blocks GREEN).
    - `cd web && pnpm tsc --noEmit` exits 0.
    - `cd web && pnpm vitest run` exits 0 (no broader regressions).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/agent-workforce/web && pnpm vitest run lib/inngest/functions/__tests__/classifier-screen-worker.test.ts</automated>
  </verify>
  <done>Worker file implements D-16 step ordering; all 12 it-blocks GREEN; full vitest + tsc clean; zero swarm_type literals.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Register classifierScreenWorker in app/api/inngest/route.ts</name>
  <files>web/app/api/inngest/route.ts</files>
  <read_first>
    - web/app/api/inngest/route.ts (full file — focus on line 32 import + line 65 functions array)
  </read_first>
  <action>
    Edit `web/app/api/inngest/route.ts`. Two minimal changes:

    1. Add import alongside the existing classifierVerdictWorker import
       (around line 32):
       ```typescript
       import { classifierScreenWorker } from "@/lib/inngest/functions/classifier-screen-worker";
       ```

    2. Add `classifierScreenWorker,` to the `functions: [...]` array passed
       to `serve(...)` (around line 65), placed adjacent to
       `classifierVerdictWorker,` for readability.

    Run `cd web && pnpm tsc --noEmit`. Run `cd web && pnpm vitest run` (no
    test directly hits this file but a smoke pass-through is healthy).
  </action>
  <acceptance_criteria>
    - `grep -c "classifierScreenWorker" web/app/api/inngest/route.ts` returns >= 2 (one import, one functions-array entry).
    - The import path is `@/lib/inngest/functions/classifier-screen-worker` (verify exact string).
    - `cd web && pnpm tsc --noEmit` exits 0.
    - `cd web && pnpm vitest run` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/agent-workforce && grep -c "classifierScreenWorker" web/app/api/inngest/route.ts</automated>
  </verify>
  <done>Route registers the new worker; tsc + tests clean; on next deploy Inngest picks up classifier/screen.requested.</done>
</task>

</tasks>

<verification>
- `cd web && pnpm vitest run lib/inngest/functions/__tests__/classifier-screen-worker.test.ts` exits 0 with 12 passing tests.
- `cd web && pnpm tsc --noEmit` exits 0.
- `cd web && pnpm vitest run` exits 0 (full suite).
- `grep -E "swarm_type\\s*===\\s*['\"](sales-email|debtor-email)['\"]" web/lib/inngest/functions/classifier-screen-worker.ts` returns nothing.
- `grep -c "classifierScreenWorker" web/app/api/inngest/route.ts` >= 2.
</verification>

<success_criteria>
- Worker implementation passes all D-19 cases + Pitfall 4 + Pitfall 6 unit tests.
- Worker registered with Inngest; classifier/screen.requested has its first consumer in production.
- Zero swarm_type literals in worker code (REQ-6 acceptance).
- Phase 70 dual-write contract honored (one pipeline_events row per call; one agent_runs row when LLM invoked).
</success_criteria>

<output>
After completion, create `.planning/phases/74-stage-1-llm-category-classifier-swarm-agnostic-fills-the-mis/74-04-SUMMARY.md` recording:
- Total LOC of the new worker.
- Final test count (should be >= 12).
- Sample trace shape returned from a manual invoke (paste the `{ok, regex_category, llm_invoked, final_category_key}` return).
- Confirmation of zero swarm_type literals (paste the grep output).
</output>
