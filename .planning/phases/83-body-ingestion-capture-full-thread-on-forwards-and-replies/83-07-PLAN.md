---
phase: 83-body-ingestion-capture-full-thread-on-forwards-and-replies
plan: 07
type: execute
wave: 7
depends_on: [83-01, 83-02, 83-03, 83-04, 83-05, 83-06, 83-06b]
files_modified:
  - web/scripts/verify-phase83.ts
  - .planning/phases/83-body-ingestion-capture-full-thread-on-forwards-and-replies/83-07-VERIFICATION.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Read-only verification script samples N FW: and Re: rows post-backfill and asserts non-null body_full_text in ≥19/20 (≥95%, matching 83-05's permanent-Graph-404 tolerance) and logs any failing row_ids to stdout (CONTEXT verification §1)"
    - "Script asserts length(body_full_text) > length(body_unique_text) on ≥95% of FW:/Re: rows (CONTEXT verification §2)"
    - "Script reports coordinator_runs.decision_details.input_size distribution post-deploy (CONTEXT verification §3 — D-09 telemetry sanity)"
    - "Operator confirms no regression on direct-debtor emails: spot-check 10 non-FW rows for unchanged top-1 intent (CONTEXT verification §5)"
    - "R-03 PII expansion check: sample 100 post-deploy Stage 0 outputs; injection_suspected false-positive rate is not materially higher than pre-deploy"
  artifacts:
    - path: web/scripts/verify-phase83.ts
      provides: "Read-only verification harness; exits 0 only when all CONTEXT verification gates pass"
      contains: "body_full_text"
    - path: .planning/phases/83-body-ingestion-capture-full-thread-on-forwards-and-replies/83-07-VERIFICATION.md
      provides: "Operator-facing checklist with SQL snippets + sign-off"
      contains: "Acceptance threshold"
  key_links:
    - from: "web/scripts/verify-phase83.ts"
      to: "email_pipeline.emails + email_pipeline.conversation_context + coordinator_runs"
      via: "service-role supabase client; read-only SELECTs"
      pattern: "SELECT"
---

<objective>
Build the read-only verification harness that proves Phase 83 actually delivered what
CONTEXT promised: full-thread bodies persisted, conversation_context populated, Stage 3
telemetry showing wider input, no Stage 0 false-positive spike, and operator sign-off
on no regression for direct-debtor emails.

Purpose: CONTEXT §verification §1-§5. Note that §4 (≥50% reclassification threshold)
belongs to Phase 87 retro-classification — this plan checks §1, §2, §3, §5 plus the
R-03 PII expansion sanity check.

Output: Runnable verification script + a markdown checklist for the operator-facing
sign-off (human-verify checkpoint at the end).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/83-body-ingestion-capture-full-thread-on-forwards-and-replies/83-CONTEXT.md
@web/scripts/backfill-bodies.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verification harness script</name>
  <files>web/scripts/verify-phase83.ts</files>
  <read_first>
    - web/scripts/backfill-bodies.ts (style + service-role client setup reference)
    - .planning/phases/83-body-ingestion-capture-full-thread-on-forwards-and-replies/83-CONTEXT.md (verification §1-§5)
  </read_first>
  <action>
Create web/scripts/verify-phase83.ts as a read-only tsx script. It runs four
verifications and prints a verdict; exits 0 only when all four pass.

Run command: npx tsx web/scripts/verify-phase83.ts [--days=30] [--sample=20]

Verifications:

V1. Body coverage on FW:/Re: rows (CONTEXT §1).
SELECT id, source_id, mailbox, subject, body_full_text, body_unique_text
FROM email_pipeline.emails
WHERE received_at >= now() - interval '{days} days'
  AND (subject ILIKE 'FW:%' OR subject ILIKE 'Re:%')
ORDER BY received_at DESC
LIMIT {sample};
ASSERT n_with_body >= ceil(0.95 * sample) (i.e. ≥19/20 when --sample=20). The 5% slack
matches 83-05's acceptance criterion for permanent Graph 404s — rows whose Graph message
has been deleted upstream cannot be backfilled and must not block verification.
Collect the row_ids of any failing rows (body_full_text IS NULL OR empty) and print them
to stdout so the operator can cross-reference them against 83-05-SUMMARY.md's
permanent-failure list.
Print: "V1 body coverage: {n_with_body}/{sample} rows have body_full_text. Required ≥95%. PASS/FAIL."
Print (only if any failed): "V1 failing row_ids: <comma-separated list>"

V2. body_full_text vs body_unique_text length (CONTEXT §2).
From the same sample as V1, count rows where char_length(body_full_text) > char_length(body_unique_text).
ASSERT count >= ceil(0.95 * sample).
Print: "V2 thread expansion: {n_wider}/{sample} rows have full > unique. Required ≥95%. PASS/FAIL."

V3. Stage 3 telemetry (CONTEXT §3, D-09).
SELECT
  count(*) AS runs,
  count(*) FILTER (WHERE (decision_details->'input_size'->>'truncated')::boolean = true) AS truncated_runs,
  percentile_disc(0.5) WITHIN GROUP (ORDER BY (decision_details->'input_size'->>'input_chars')::int) AS median_chars
FROM coordinator_runs
WHERE created_at >= now() - interval '24 hours';
ASSERT runs > 0 AND median_chars > 500 (heuristic: pre-deploy median was lower for FW:/Re:).
Print the row + verdict.

V4. R-03 PII expansion sanity check — Stage 0 false-positive rate.
SELECT
  count(*) AS stage0_runs,
  count(*) FILTER (WHERE result->>'verdict' = 'injection_suspected') AS suspected
FROM automation_runs
WHERE automation IN ('debtor-email-review','sales-email-review')
  AND result->>'stage' = 'safety_check'
  AND completed_at >= now() - interval '24 hours';
ASSERT suspected/stage0_runs ≤ 0.05 (5% ceiling — operator adjusts threshold if pre-deploy baseline differs; the script accepts a --pii-ceiling=N CLI flag, default 0.05).
Print: "V4 PII expansion: {suspected}/{stage0_runs} ({pct}%) Stage 0 injection_suspected. Ceiling {ceiling}%. PASS/FAIL."

Final summary:
- Print one line per V1..V4 with PASS/FAIL.
- If any fail, process.exit(1); otherwise exit(0).

Implementation notes:
- Use the same dotenv + createClient pattern as web/scripts/backfill-bodies.ts.
- READ-ONLY: no INSERT/UPDATE anywhere. No service-role write should appear in this file.
- Print SQL used for each check (transparency for the operator running it).
- Do NOT call Graph; do NOT invoke Stage 3; pure Supabase queries.

The script does NOT verify CONTEXT §4 (the ≥50% reclassification threshold). That is
Phase 87's deliverable. Print a closing reminder: "Phase 87 retro-classification covers
the ≥50% reclassification gate."
  </action>
  <verify>
    <automated>cd web && npx tsx scripts/verify-phase83.ts --days=30 --sample=20 — exits 0 with PASS for V1..V4 (post-deploy); grep -c "INSERT\|UPDATE\|DELETE\|UPSERT" web/scripts/verify-phase83.ts returns 0 (read-only invariant)</automated>
  </verify>
  <acceptance_criteria>
    - File exists at web/scripts/verify-phase83.ts
    - grep -c "body_full_text" web/scripts/verify-phase83.ts is at least 2
    - grep -c "V1 failing row_ids" web/scripts/verify-phase83.ts is at least 1 (failing row_ids logged on partial-coverage outcome)
    - grep -E "ceil\(0\.95|>= ?0\.95|n_with_body >= " web/scripts/verify-phase83.ts returns at least one match (95% slack honored)
    - grep -c "conversation_context\|coordinator_runs" web/scripts/verify-phase83.ts is at least 1
    - grep -v '^#' web/scripts/verify-phase83.ts | grep -Ec '\.(insert|update|delete|upsert)\b' returns 0 (no writes)
    - Script exits 0 when all four verifications pass; exits 1 when any fail
    - Script prints the SQL it runs (transparency)
  </acceptance_criteria>
  <done>Verification harness exists, is read-only, and provides a single command to gate Phase 83 closure.</done>
</task>

<task type="auto">
  <name>Task 2: Operator-facing VERIFICATION.md checklist</name>
  <files>.planning/phases/83-body-ingestion-capture-full-thread-on-forwards-and-replies/83-07-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/83-body-ingestion-capture-full-thread-on-forwards-and-replies/83-CONTEXT.md (verification §1-§5 + R-03)
  </read_first>
  <action>
Create the markdown checklist with these sections:

1. Pre-flight: confirm Plans 83-01..83-06 shipped and Plan 83-05 backfill completed (link to 83-05-SUMMARY.md row counts).

2. Automated checks: instructions to run `npx tsx web/scripts/verify-phase83.ts --days=30 --sample=20` and paste the output here.

3. Manual spot-checks (CONTEXT §5 — the human-only gate):
   - Pick 10 direct (non-FW) debtor emails received in the last 7 days from Bulk Review.
   - For each, compare the top-1 ranked intent BEFORE Phase 83 deploy (look up coordinator_runs prior to deploy timestamp) vs AFTER (most recent coordinator_runs row).
   - Acceptable: top-1 intent unchanged OR confidence delta within ±0.05.
   - Unacceptable: ≥3 of 10 rows shift to a different top-1 intent — surfaces a Stage 3 regression on direct-debtor traffic. STOP, surface to /gsd-debug.

4. Sign-off table:
   | Check | Result | Notes |
   |-------|--------|-------|
   | V1 body coverage | PASS / FAIL | … |
   | V2 thread expansion | PASS / FAIL | … |
   | V3 telemetry median | PASS / FAIL | … |
   | V4 PII expansion ceiling | PASS / FAIL | … |
   | Manual §5 direct-debtor regression | PASS / FAIL | … |

5. Closure note: Phase 87 retro-classification owns the ≥50% reclassification gate
   (CONTEXT §4). Phase 83 closes after V1..V4 + §5 pass. Phase 83 ROADMAP entry gets
   checkbox flipped only after sign-off.

6. v8.2 backlog: add a one-line item "Drop email_pipeline.emails.body_text column after
   one stable release of Phase 83 (CONTEXT D-10 follow-up)."
  </action>
  <verify>
    <automated>Verify file exists; grep -c "PASS / FAIL" .planning/phases/83-body-ingestion-capture-full-thread-on-forwards-and-replies/83-07-VERIFICATION.md is at least 5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the specified path
    - Contains a sign-off table with rows for V1 V2 V3 V4 and manual §5
    - Contains literal "Acceptance threshold" or equivalent operator-facing language for each check
    - Contains the v8.2 backlog reminder line about dropping body_text
  </acceptance_criteria>
  <done>Operator has a single document to follow to close Phase 83.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator sign-off — Phase 83 verification</name>
  <what-built>
    Phase 83 ingestion fix (Plans 83-01..83-06) + verification harness (Task 1 above) + checklist (Task 2 above). All automated checks should already be PASS before this checkpoint.
  </what-built>
  <how-to-verify>
    1. Run: `cd web && npx tsx scripts/verify-phase83.ts --days=30 --sample=20` and confirm exit code 0 with all four V1..V4 reporting PASS.
    2. Open `.planning/phases/83-body-ingestion-capture-full-thread-on-forwards-and-replies/83-07-VERIFICATION.md`.
    3. Walk through the 10-row manual §5 direct-debtor regression spot-check in Bulk Review.
    4. Fill in the sign-off table at the bottom of 83-07-VERIFICATION.md and commit it.
    5. Update ROADMAP.md: flip Phase 83 checkbox to [x].
  </how-to-verify>
  <resume-signal>
    Type "approved" once the sign-off table is filled in and committed. If any check fails, describe which one and surface to /gsd-debug.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator script → Supabase (read-only) | Standard service-role read posture; no new write surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-22 | Information disclosure | verification script prints sample row contents (subjects, body lengths) | accept | Operator-local stdout only; not persisted. Subjects only, not body content. |
| T-83-23 | Tampering | accidental write from verification script | mitigate | Acceptance criterion enforces zero insert/update/delete/upsert calls in the file (grep gate). |
</threat_model>

<verification>
- Automated harness exits 0 with PASS on all four checks against post-deploy data.
- Manual §5 spot-check finds no direct-debtor regression beyond noise.
- Sign-off table filled in by operator and committed.
</verification>

<success_criteria>
- Phase 83 verification harness shipped.
- Operator has run it and signed off.
- ROADMAP Phase 83 checkbox flipped.
</success_criteria>

<output>
After completion, create `.planning/phases/83-body-ingestion-capture-full-thread-on-forwards-and-replies/83-07-SUMMARY.md` with verification outcomes + sign-off date.
</output>
