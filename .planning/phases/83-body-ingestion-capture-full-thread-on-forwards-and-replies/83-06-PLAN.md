---
phase: 83-body-ingestion-capture-full-thread-on-forwards-and-replies
plan: 06
type: execute
wave: 6
depends_on: [83-01, 83-03, 83-05]
files_modified:
  - web/lib/automations/debtor-email/coordinator/invoke-intent.ts
  - web/lib/automations/debtor-email/coordinator/assemble-input.ts
  - web/lib/automations/debtor-email/coordinator/__tests__/assemble-input.test.ts
  - web/lib/inngest/functions/debtor-email-coordinator.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Stage 3 coordinator reads body_full_text with fallback to body_text (D-10)"
    - "Stage 3 input passed to invokeIntentAgent is the wrapped inbound_message + quoted_thread XML structure (D-04, D-06)"
    - "conversation_context rows are inlined as prior elements ordered by position ASC"
    - "Tenant-domain detection uses a static list (D-08 fallback)"
    - "Assembled input capped at 8000 chars (D-09); D-08 truncation preserves oldest non-tenant inbound + newest reply"
    - "input_chars + truncated flag persisted on coordinator_runs.decision_details.input_size (D-09 telemetry)"
  artifacts:
    - path: web/lib/automations/debtor-email/coordinator/assemble-input.ts
      provides: "assembleInput helper that builds the wrapped XML structure with truncation"
      contains: "inbound_message"
    - path: web/lib/inngest/functions/debtor-email-coordinator.ts
      provides: "Reads body_full_text + conversation_context; assembles XML input; passes to invokeIntentAgent; logs telemetry"
      contains: "body_full_text"
  key_links:
    - from: "debtor-email-coordinator (Inngest)"
      to: "email_pipeline.conversation_context"
      via: "supabase select with eq email_id, ordered by position ASC"
      pattern: "conversation_context"
    - from: "invoke-intent buildUserMessage"
      to: "Orq.ai debtor-intent-agent"
      via: "wrapped XML assembled_input string in the user message"
      pattern: "assembled_input"
---

<objective>
Make Stage 3 read body_full_text with body_text fallback, and rebuild the coordinator
Orq.ai prompt input as the wrapped XML structure described in CONTEXT D-04 / D-06.
Apply D-08 truncation + D-09 hard cap with telemetry.

Purpose: D-06 (Stage 3 portion) wires the new persisted data into Stage 3 so the
classifier finally sees what humans see. D-10 fallback (coalesce body_full_text,
body_text) keeps not-yet-backfilled rows working during the transition.

Output: New assembleInput helper, updated coordinator Inngest function, updated
invoke-intent prompt builder. (Stage 1 screen worker reader switch lives in sibling
plan 83-06b — runs in parallel within wave 6.)

RFC compliance: Stage 1 vs Stage 3 hard separation rule UNCHANGED. This plan only swaps
the INPUT column from body_text to body_full_text on Stage 3; registry writes
(swarm_intents) are untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/83-body-ingestion-capture-full-thread-on-forwards-and-replies/83-CONTEXT.md
@docs/agentic-pipeline/stage-3-coordinator.md
@web/lib/automations/debtor-email/coordinator/invoke-intent.ts
@web/lib/inngest/functions/debtor-email-coordinator.ts

Static tenant-domain list for Phase 83 (D-08 fallback until Phase 84 ships
swarms.tenant_domains): smeba.nl, smeba-fire.be, moyneroberts.com. Add others
discovered during implementation grep against swarms.mailboxes.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: assembleInput helper (RED then GREEN)</name>
  <files>web/lib/automations/debtor-email/coordinator/assemble-input.ts, web/lib/automations/debtor-email/coordinator/__tests__/assemble-input.test.ts</files>
  <behavior>
    - Test 1: no priors — text contains the inbound_message opening tag (literal substring), the subject, the body, and a present-but-empty quoted_thread block.
    - Test 2: 2 priors — text contains 2 prior elements ordered by position ASC.
    - Test 3: below cap — truncated equals false.
    - Test 4: over 8000 chars AND a non-tenant inbound prior exists — output keeps the oldest non-tenant prior plus the inbound message, drops middle priors, inserts the marker substring "truncated: N messages dropped from middle of thread" where N is the dropped count, sets truncated=true.
    - Test 5: over 8000 chars AND no non-tenant prior is identifiable — fallback marker substring "kept most recent" appears and truncated=true.
    - Test 6: tenant detection — elger@smeba-fire.be with tenantDomains containing smeba-fire.be is tenant; someone@cbre.com is non-tenant.
    - Test 7: inputChars equals final string length.
  </behavior>
  <read_first>
    - .planning/phases/83-body-ingestion-capture-full-thread-on-forwards-and-replies/83-CONTEXT.md (D-04, D-08, D-09)
    - web/lib/automations/debtor-email/coordinator/types.ts (type-style reference)
  </read_first>
  <action>
A. Create the RED test at the test path. The test imports assembleInput from ../assemble-input which does not yet exist — tests fail with module-not-found. Commit: test(83-06): pin assembleInput contract (RED).

B. Implement assemble-input.ts:

Exported types:
- AssembleInputArgs: subject string, bodyFull string, priors array of objects (position, senderEmail, subject, receivedAt, bodyText), tenantDomains string[], capChars number.
- AssembledInput: text string, inputChars number, truncated boolean.

Implementation rules:
1. Render two concatenated XML-tagged blocks: inbound_message (with subject + body children, all values xml-escaped) and quoted_thread (with one prior element per prior, ordered by position ASC, attributes position/from/received, inner text the bodyText).
2. xmlEscape: replace ampersand first (with the named entity), then less-than, then greater-than. Order matters.
3. If text.length is at or below capChars: return truncated=false.
4. If over cap: scan priors in REVERSE position order (highest position = oldest chronologically). Pick the first whose senderEmail domain is NOT in tenantDomains (case-insensitive exact equality). If found, re-render with that prior as the only kept prior and insert a literal marker line "truncated: N messages dropped from middle of thread" where N = priors.length - 1. Return truncated=true.
5. If no non-tenant prior exists: greedily keep priors from position 1 upward until adding the next would exceed cap; insert marker "kept most recent {N} priors"; if still over cap, hard-slice the rendered text to capChars and append "kept most recent {chars} chars". Return truncated=true.
6. Tenant detection helper: lowercase domain part of email (substring after @), compare equality against each lowercased tenantDomains entry.

Commit GREEN: feat(83-06): assembleInput — D-04 wrap + D-08/D-09 truncation.
  </action>
  <verify>
    <automated>cd web && npx vitest run lib/automations/debtor-email/coordinator/__tests__/assemble-input.test.ts — 7/7 pass; cd web && npx tsc --noEmit — clean</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist at the specified paths
    - grep -c "inbound_message" web/lib/automations/debtor-email/coordinator/assemble-input.ts is at least 1
    - grep -c "quoted_thread" web/lib/automations/debtor-email/coordinator/assemble-input.ts is at least 1
    - grep -c "messages dropped from middle of thread" web/lib/automations/debtor-email/coordinator/assemble-input.ts equals 1
    - grep -c "kept most recent" web/lib/automations/debtor-email/coordinator/assemble-input.ts is at least 1
    - 7/7 vitest assertions pass
    - tsc --noEmit clean
  </acceptance_criteria>
  <done>Helper unit-tested and ready to plug into the coordinator.</done>
</task>

<task type="auto">
  <name>Task 2: Switch invoke-intent prompt to assembled_input</name>
  <files>web/lib/automations/debtor-email/coordinator/invoke-intent.ts</files>
  <read_first>
    - web/lib/automations/debtor-email/coordinator/invoke-intent.ts (full file, ~110 lines)
    - .planning/phases/83-body-ingestion-capture-full-thread-on-forwards-and-replies/83-CONTEXT.md (D-06)
  </read_first>
  <action>
1. Modify InvokeIntentInput (lines 14-25): ADD required field `assembled_input: string` immediately after body_text. Keep body_text for trace-only visibility.

2. Modify buildUserMessage (lines 53-60): REPLACE the JSON.stringify(promptVars) body with a multi-line string that emits a context block (email_id, run_id, sender_email, sender_domain, mailbox, entity each wrapped in their own XML tags) followed by promptVars.assembled_input on its own line, followed by "Return the JSON object. No preamble."

3. Modify the invokeIntentAgent body (lines 71-86): construct promptVars including `assembled_input: input.assembled_input`. The Orq variables payload continues to include subject, body_text, received_at for trace visibility — those are no longer interpolated into the prompt text.

4. Add a one-line comment: replay-safety holds because assembled_input is deterministic for a given (email_id, conversation_context state) pair.

5. If __tests__/invoke-intent.test.ts exists, extend fixtures to pass assembled_input (a short XML-wrapped string suffices). Do NOT create a new test file — Task 1 already pins the assembly contract.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit — clean; grep -c "assembled_input" web/lib/automations/debtor-email/coordinator/invoke-intent.ts is at least 3; cd web && npx vitest run lib/automations/debtor-email/coordinator/__tests__ — green</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "assembled_input" web/lib/automations/debtor-email/coordinator/invoke-intent.ts is at least 3 (type field, prompt builder, call site)
    - grep -c "JSON.stringify" web/lib/automations/debtor-email/coordinator/invoke-intent.ts in the buildUserMessage function returns 0 (the old key-sorted JSON blob is gone)
    - tsc --noEmit clean
    - Existing coordinator vitest suite passes after fixture update
  </acceptance_criteria>
  <done>Stage 3 prompt is the wrapped XML structure; body_text no longer interpolated into the prompt body.</done>
</task>

<task type="auto">
  <name>Task 3: Wire reader switch + assembly + telemetry into the coordinator Inngest function</name>
  <files>web/lib/inngest/functions/debtor-email-coordinator.ts</files>
  <read_first>
    - web/lib/inngest/functions/debtor-email-coordinator.ts (lines 110-160: email row fetch and invokeIntentAgent call)
    - web/lib/automations/debtor-email/coordinator/assemble-input.ts (from Task 1)
    - .planning/phases/83-body-ingestion-capture-full-thread-on-forwards-and-replies/83-CONTEXT.md (D-09 telemetry)
  </read_first>
  <action>
A. Extend the email-row select to include body_full_text. Preserve every other column already selected.

B. Immediately after the email row loads, fetch conversation_context rows for this email_id with select fields (position, sender_email, subject, received_at, body_text), filtered by eq email_id, ordered by position ASC. This SELECT lives inside the same step.run boundary that constructs the invokeIntentAgent input (CLAUDE.md Inngest §step.run rule — side effects inside step.run).

C. Import assembleInput from @/lib/automations/debtor-email/coordinator/assemble-input. Declare a constant TENANT_DOMAINS = ["smeba.nl", "smeba-fire.be", "moyneroberts.com"] with a TODO comment pointing at Phase 84 D-03 (swap to swarms.tenant_domains JSONB lookup once that ships).

D. Build bodyFull = email.body_full_text ?? email.body_text ?? "" (D-10 fallback). Call assembleInput with subject, bodyFull, priors mapped from conversation_context rows, tenantDomains, capChars=8000.

E. Pass assembled.text to invokeIntentAgent as assembled_input. Keep body_text passing email.body_text ?? "" (trace-only).

F. Telemetry (D-09): merge into the existing coordinator_runs UPDATE the field decision_details.input_size = { input_chars: assembled.inputChars, truncated: assembled.truncated }. If decision_details is built via a shared helper, extend the helper; do not write decision_details from two places.

G. Replay-safety: assembleInput is pure and deterministic given the SAME conversation_context state. The two SELECTs (email + conversation_context) and the UPDATE all live inside step.run boundaries per CLAUDE.md. Do not generate UUIDs or use Date.now outside step.run.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit — clean; grep -c "body_full_text" web/lib/inngest/functions/debtor-email-coordinator.ts is at least 2; grep -c "assembleInput" web/lib/inngest/functions/debtor-email-coordinator.ts is at least 1; grep -c "conversation_context" web/lib/inngest/functions/debtor-email-coordinator.ts is at least 1; grep -c "input_size" web/lib/inngest/functions/debtor-email-coordinator.ts is at least 1; cd web && npx vitest run lib/inngest/functions/__tests__ — green</automated>
  </verify>
  <acceptance_criteria>
    - Select string includes body_full_text
    - conversation_context fetched + ordered by position ASC
    - assembleInput called exactly once with TENANT_DOMAINS and capChars=8000
    - assembled.text passed as assembled_input to invokeIntentAgent
    - decision_details.input_size persisted with input_chars + truncated keys
    - Existing coordinator vitest suite passes
    - tsc --noEmit clean
  </acceptance_criteria>
  <done>Stage 3 sees the wrapped XML thread; telemetry pinned for D-09 observability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| email_pipeline.emails / conversation_context → Orq.ai | Wider classifier input (full thread + 2 priors) flows to Orq.ai; same trust posture as today, just more content per call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-18 | Information disclosure | wider PII surface in Stage 3 prompt | accept | Orq.ai data residency unchanged (EU); the corpus that reached the classifier on direct-send paths already carried similar PII. R-03 spot-check belongs to Plan 83-07. |
| T-83-19 | Tampering | tenant-domain misclassification (false-tenant elides a non-tenant prior from truncation preservation) | mitigate | Static list is auditable in code; Phase 84 D-03 supersedes with registry-driven swarms.tenant_domains. TODO comment makes the upgrade path explicit. |
| T-83-20 | DoS (token bloat) | assembled_input exceeds expected Orq token budget | mitigate | D-09 hard cap at 8000 chars enforced in assembleInput; truncated flag logged for post-deploy monitoring; R-01 watch list in CONTEXT. |
| T-83-21 | Repudiation | telemetry omission masks token-bloat regressions | mitigate | decision_details.input_size persists input_chars + truncated on every Stage 3 run — queryable for post-deploy verification. |
</threat_model>

<verification>
- All unit tests green (assembleInput 7/7, coordinator suite no regression).
- tsc --noEmit clean.
- Manual post-deploy spot-check: pick one fresh forward email after Plan 83-04 has populated conversation_context; run Stage 3 via /gsd-execute; confirm coordinator_runs.decision_details.input_size has input_chars > 1000 (i.e. wider than typical pre-deploy median).
</verification>

<success_criteria>
- Stage 3 reads body_full_text + conversation_context and ships them as a wrapped XML assembled_input to Orq.
- Telemetry (D-09) live on coordinator_runs.
- No registry behavior changed (Stage 3 separation preserved).
</success_criteria>

<output>
After completion, create `.planning/phases/83-body-ingestion-capture-full-thread-on-forwards-and-replies/83-06-SUMMARY.md`
</output>
