---
phase: 04-distribution
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [orq-agent/commands/orq-agent.md]
autonomous: true
gap_closure: true
requirements: [DIST-04]

must_haves:
  truths:
    - "/orq-agent --output ./custom-dir 'description' writes all generated files to ./custom-dir/[swarm-name]/ instead of ./Agents/[swarm-name]/"
    - "Subagent instructions in Waves 1, 2, and 3 reference {OUTPUT_DIR} variable instead of hardcoded ./Agents/"
    - "Step 7 completion summary shows the actual output directory, not hardcoded ./Agents/"
  artifacts:
    - path: "orq-agent/commands/orq-agent.md"
      provides: "Orchestrator with OUTPUT_DIR flowing through all pipeline stages"
      contains: "{OUTPUT_DIR}"
  key_links:
    - from: "orq-agent/commands/orq-agent.md Step 0"
      to: "orq-agent/commands/orq-agent.md Waves 1-3"
      via: "OUTPUT_DIR variable propagation"
      pattern: "\\{OUTPUT_DIR\\}"
---

<objective>
Fix --output flag propagation in orq-agent orchestrator

Purpose: The --output flag is parsed in Step 0 and used in Step 5 for directory creation, but Waves 1, 2, 3, and Step 7 still use hardcoded `./Agents/` paths. This means the flag is syntactically accepted but functionally broken for any non-default path. This gap closure replaces all 29 hardcoded `./Agents/` path references with `{OUTPUT_DIR}` so the --output flag flows through the entire pipeline.

Output: Updated orq-agent.md with consistent OUTPUT_DIR usage throughout all pipeline stages
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-distribution/04-02-SUMMARY.md
@.planning/phases/04-distribution/04-VERIFICATION.md
@orq-agent/commands/orq-agent.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded ./Agents/ paths with {OUTPUT_DIR} throughout orq-agent.md</name>
  <files>orq-agent/commands/orq-agent.md</files>
  <action>
Replace all hardcoded `./Agents/` path references in Waves 1, 2, 3, and Step 7 of orq-agent.md with `{OUTPUT_DIR}` to match the variable set in Step 0.

Specifically, replace `./Agents/[swarm-name]/` with `{OUTPUT_DIR}/[swarm-name]/` in these sections:

**Wave 1 (Research):**
- Line ~300: blueprint file path passed to researcher (`./Agents/[swarm-name]/blueprint.md`)
- Line ~303: research brief output path (`./Agents/[swarm-name]/research-brief.md`)
- Line ~317: research incomplete marker file (`./Agents/[swarm-name]/research-brief.incomplete`)

**Wave 2 (Spec Generation):**
- Line ~348: blueprint input path (`./Agents/[swarm-name]/blueprint.md`)
- Line ~349: research brief input path (`./Agents/[swarm-name]/research-brief.md`)
- Line ~353: spec output path (`./Agents/[swarm-name]/agents/[agent-key].md`)
- Line ~357: spec incomplete marker (`./Agents/[swarm-name]/agents/[agent-key].md.incomplete`)

**Wave 3 (Post-generation):**
- Line ~395: orchestration output path (`./Agents/[swarm-name]/ORCHESTRATION.md`)
- Lines ~400-403: orchestration/dataset input paths (blueprint, research brief, agent specs, datasets)
- Lines ~409-413: README generator input/output paths (blueprint, agent specs, orchestration, datasets, README)

**Step 7 (Completion Summary):**
- Line ~446: directory tree display (`./Agents/[swarm-name]/`)
- Line ~509: review agent specs path
- Line ~516: dataset location path
- Line ~533: JSON metadata output_directory field

**DO NOT modify:**
- Step 0 flag parsing (lines ~24-42) — these correctly define OUTPUT_DIR and show defaults as `./Agents/`
- Step 5 directory setup (line ~227) — already uses OUTPUT_DIR correctly
- The documentation/comment references explaining that `./Agents/` is the default value

**Pattern:** Every path that refers to where files are read from or written to during pipeline execution must use `{OUTPUT_DIR}` instead of the literal `./Agents/`. Documentation references explaining the default value can remain as `./Agents/`.
  </action>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/claude-code-prompt-agent && grep -n '\./Agents/' orq-agent/commands/orq-agent.md | grep -v 'OUTPUT_DIR' | grep -v 'defaults to' | grep -v 'default output' | grep -v 'Step 0' | grep -v '^\s*-.*--output' | grep -v 'GSD_MODE' | wc -l | tr -d ' '</automated>
    <manual>The automated check counts remaining ./Agents/ references that are NOT in Step 0 documentation, default-value explanations, or flag descriptions. Expected result: 0 (all pipeline paths replaced). A small number (1-3) is acceptable if they are genuinely documentation-only references.</manual>
  </verify>
  <done>All Wave 1, 2, 3 subagent instruction paths and Step 7 summary paths use {OUTPUT_DIR}/[swarm-name]/ instead of hardcoded ./Agents/[swarm-name]/. The --output flag now flows through the entire pipeline from parsing (Step 0) to directory creation (Step 5) to all subagent invocations (Waves 1-3) to completion output (Step 7).</done>
</task>

</tasks>

<verification>
1. `grep -c '\./Agents/' orq-agent/commands/orq-agent.md` returns a small number (only Step 0 documentation references)
2. `grep -c '{OUTPUT_DIR}' orq-agent/commands/orq-agent.md` returns 29+ (all pipeline path references)
3. No broken markdown formatting in orq-agent.md
</verification>

<success_criteria>
- The --output flag value propagates from Step 0 through Waves 1, 2, 3, and Step 7
- Zero hardcoded ./Agents/ paths remain in pipeline execution sections (Waves 1-3, Step 7)
- DIST-04 gap from VERIFICATION.md is closed
</success_criteria>

<output>
After completion, create `.planning/phases/04-distribution/04-03-SUMMARY.md`
</output>
