# Phase 82.8 — Discussion Log

**Session:** 2026-05-18
**Mode:** /gsd-discuss-phase (interactive update of existing CONTEXT.md)
**Purpose:** Audit trail of Q&A — NOT consumed by downstream agents (researcher, planner, executor read CONTEXT.md only).

---

## Pre-discussion context (already in CONTEXT.md before this session)

D-01, D-02, D-03 were drafted from operator review on 2026-05-18 (in-conversation discussion before /gsd-discuss-phase was invoked).

---

## Q1 — Continue or skip?

**Question:** Phase 82.8 heeft al een CONTEXT.md (zojuist geschreven). Wat wil je doen?
**Options:**
1. Update via discussie — Loop nog een paar resterende gray areas door (recommended)
2. Skip — direct naar plan-phase
3. View eerst

**Answer:** Update via discussie

---

## Gray area selection

**Question:** Welke gray areas wil je bespreken voor 82.8? (multi-select)
**Options:**
1. Stage 4 layout — collapsible / tabs / flat
2. Screenshot opslag + toegang — verifieer Phase 67 state
3. decision_details payload Stage 4 row — minimaal vs uitgebreid
4. Backfill historische rows — re-check huidige "geen backfill" beslissing

**Answer:** ALLE VIER

---

## Area 1: Stage 4 layout

### Q1.1 — Presentatie van drie types

**Options:**
1. Drie collapsible sections (recommended) — alle tellingen direct zichtbaar
2. Drie sub-tabs binnen Stage 4 — meer ruimte, extra klik
3. Flat lijst met type-chips filter

**Answer:** Item 1 (collapsible) — met aantekening "but with English badge labels ;-)"

**Captured in CONTEXT.md:** D-02 verfijnd — collapsible sections + English badge labels ("Handler error / Needs review / Auto-archived").

### Q1.2 — Default sortering

**Options:**
1. Nieuwste eerst (chronologisch desc) — uniform alle secties (recommended)
2. Per sectie verschillend (handler-fout op tijd, needs-review op urgency)

**Answer:** Nieuwste eerst — uniform

**Captured in CONTEXT.md:** D-02 — "nieuwste eerst (chronologisch desc) voor alle drie secties. Uniform, geen per-sectie sort-strategie."

---

## Area 2: Screenshot opslag + toegang

### Pre-question verification

Bash grep op `captureScreenshot` + screenshot-URL persistence onthulde:
- `web/lib/automations/debtor-email/label-email-in-icontroller.ts:88,158` — before/after/error capture
- `web/lib/inngest/functions/debtor-email-icontroller-tagger.ts:165-170` — schrijft URLs naar `debtor.email_labels.screenshot_before_url / .screenshot_after_url`

**Conclusion shared with user:** Screenshots ZIJN al end-to-end opgelost door Phase 67. Storage/RLS/TTL aanwezig.

### Q2.1 — Read-path

**Options:**
1. Direct join op `debtor.email_labels` (recommended) — één source of truth
2. Denormaliseer URLs naar Stage 4 `decision_details`
3. Beide (denormaliseer + late join fallback)

**Answer:** Direct join

**Captured in CONTEXT.md:** D-04 (nieuw) — locked direct-join strategie.

### Q2.2 — Access model

**Options:**
1. Behoud huidige Phase 67 strategie (recommended)
2. Switch naar signed URLs met TTL

**Answer:** Behoud huidige strategie

**Captured in CONTEXT.md:** D-03 + D-04 — "behoud Phase 67 access-strategie; plan-phase verifieert SSR-context permissions."

---

## Area 3: decision_details payload

### Q3.1 — Payload omvang

**Options:**
1. Minimaal — `{noise_category, source_stage, archived_at}` (recommended)
2. Uitgebreid — + outlook_message_id, icontroller_archive_id, classifier_rule_id, confidence_score

**Answer:** Minimaal

**Captured in CONTEXT.md:** D-05 (nieuw) — locked minimal payload; alle andere data via JOINs op email_pipeline.emails / debtor.email_labels / pipeline_events stage=1.

### Q3.2 — Idempotency strategie

**Options:**
1. Unique partial index op `(email_id) WHERE stage=4 AND decision='auto_archived_noise'` (recommended)
2. Application-level check before insert
3. Geen dedupe — vertrouw op Inngest step-cache

**Answer:** Unique partial index

**Captured in CONTEXT.md:** D-06 (nieuw) — locked DB-laag dedupe via partial unique index; worker vangt 23505 conflict op als success.

---

## Area 4: Backfill historische rows

### Q4.1 — Scope

**Options:**
1. Geen backfill — vangnet start vanaf deploy (huidige CONTEXT)
2. 30d backfill — éénmalige migratie
3. 7d backfill — compromis

**Answer:** 30d backfill

**Captured in CONTEXT.md:** D-07 (nieuw) — locked 30d backfill migratie van `debtor.email_labels` → `pipeline_events stage=4`. `decision_details.backfilled = true` marker. Idempotent via LEFT JOIN.

**Deferred section update:** "geen backfill" verwijderd; vervangen door "Backfill ouder dan 30d — deferred" en "Retroactief screenshots backfillen — deferred (acceptabel lege-state)".

---

## Summary of new decisions added this session

| Decision | Source | Status |
|---|---|---|
| D-02 verfijnd | Q1.1 + Q1.2 | LOCKED — collapsible sections, English labels, chronological desc |
| D-04 | Q2.1 + Q2.2 | LOCKED — direct join on debtor.email_labels |
| D-05 | Q3.1 | LOCKED — minimal decision_details payload |
| D-06 | Q3.2 | LOCKED — unique partial index for idempotency |
| D-07 | Q4.1 | LOCKED — 30d backfill, one-time migration |

---

## Deferred items captured this session

- Backfill ouder dan 30d (Q4.1 implicit deferral)
- Retroactief screenshots backfillen (D-04 implication — backfilled rows show empty-state strip)
- Stage 4 detail-pane variant (D-02 implication — same generic detail-pane as Stages 0-3)

All captured in CONTEXT.md `<deferred>` section.

---

## Next step

Run `/gsd-plan-phase 82.8` to break decisions into executable plans.

Suggested plan grouping (planner decides final structure):
- Plan 01: DB migrations (D-06 unique index + D-07 backfill)
- Plan 02: Stage 4 telemetry write in classifier-verdict-worker (D-01)
- Plan 03: Stage 4 page — three-section layout + auto-archived loader (D-02 + D-05)
- Plan 04: StageScreenshotStrip component + detail-pane mount (D-03 + D-04) — mount gated on 82.7.1 ship
- Plan 05: STATE.md update + verification (mark 82.2/82.3/82.4 complete, document 82.8 closure)
