---
phase: 82.8-stage-4-handled-overview-and-stage-1-screenshots
plan: 01
subsystem: debtor-email-pipeline
tags: [migration, schema, screenshots, stage-1, stage-4, additive]
requires: []
provides:
  - "debtor.email_labels.screenshot_before_path (text NULL)"
  - "debtor.email_labels.screenshot_after_path (text NULL)"
affects:
  - "Plan 82.8-03 capture-side path persistence"
  - "Plan 82.8-04 detail-pane read-side join"
  - "Plan 82.8-05 StageScreenshotStrip read-side"
  - "Plan 82.8-07 historical backfill (URL → path regex)"
tech-stack:
  added: []
  patterns:
    - "Additive migration; no existing column dropped or renamed"
    - "BEGIN/COMMIT block (atomic DDL)"
    - "ADD COLUMN IF NOT EXISTS (idempotent re-run)"
key-files:
  created:
    - "supabase/migrations/20260518a_email_labels_screenshot_paths.sql"
  modified: []
decisions:
  - "Two additive nullable text columns — no index (point-lookups via email_id already covered)"
  - "Retain existing screenshot_before_url / screenshot_after_url columns untouched (back-compat)"
metrics:
  duration: "~3 min"
  completed: 2026-05-18
---

# Phase 82.8 Plan 01: Screenshot Path Columns Summary

Additive migration adding `screenshot_before_path` and `screenshot_after_path` nullable text columns to `debtor.email_labels` so downstream detail-pane code (Plans 04/05) can sign URLs on demand instead of using the 1-hour expired URLs in the existing `_url` columns.

## What Was Built

**`supabase/migrations/20260518a_email_labels_screenshot_paths.sql`** — single atomic migration:

- Comment header explaining Phase 82.8 D-04 purpose and back-compat strategy
- Wrapped in `BEGIN; ... COMMIT;` for atomicity
- Two `ALTER TABLE debtor.email_labels ADD COLUMN IF NOT EXISTS` statements (idempotent)
- `COMMENT ON COLUMN` for both new columns referencing D-04 and the on-demand signing strategy

No `DROP COLUMN`, no `RENAME`, no touching of the existing `screenshot_before_url` / `screenshot_after_url` columns.

## Verification

- `test -f supabase/migrations/20260518a_email_labels_screenshot_paths.sql` — PASS
- `grep -q "screenshot_before_path text"` — PASS
- `grep -q "screenshot_after_path  text"` — PASS (double-space matches plan literal)
- `grep -q "BEGIN;"` — PASS
- `grep -q "COMMIT;"` — PASS
- `grep -c "ADD COLUMN"` → 2 (expected 2)
- `grep -c "DROP\|RENAME"` → 0 (expected 0)

## Deviations from Plan

None — plan executed exactly as written.

## Decisions Made

- **No index added.** The plan explicitly states these columns are only read in detail-pane point-lookups already keyed by `email_id`; an existing PK / FK on `email_id` covers the access pattern.
- **Column order in DDL** follows the plan's exact ordering (before then after) for consistent diff readability against future migrations.

## Commits

- `a8bb60c` — feat(82.8-01): add screenshot path columns to debtor.email_labels

## Self-Check: PASSED

- File `supabase/migrations/20260518a_email_labels_screenshot_paths.sql` — FOUND
- Commit `a8bb60c` — FOUND in `git log`
