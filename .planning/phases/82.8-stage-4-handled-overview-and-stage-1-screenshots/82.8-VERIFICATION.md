---
phase: 82.8-stage-4-handled-overview-and-stage-1-screenshots
verified: 2026-05-18T00:00:00Z
status: human_needed
score: 7/7 must-haves verified at code level (UAT confirmation pending)
verifier: gsd-verifier (code-level goal-backward analysis)
re_verification: false
overrides_applied: 0
---

# Phase 82.8: Stage 4 Handled Overview + Stage 1 Before/After Screenshots — Verification Report

**Phase Goal (per CONTEXT.md):** Three operator-facing gaps resolved against the 5-stage operator flow:
1. Stage 1 detail-pane shows before/after iController screenshots so operators can verify archive correctness and recover "wrongly disappeared" emails.
2. Stage 4 surfaces an "afgehandeld" overview (handled emails: no human needed, or handler awaiting review).
3. Stage 4 = three types: handler-error, needs-review, auto-archived.

Scope = telemetry + UI surface; preserves hard separation (Stage 1 noise registry vs Stage 3 intent registry). No new pipeline stages.

**Verified:** 2026-05-18
**Status:** human_needed — all 7 decision items implemented and wired at code level; final ACHIEVED grade pending operator UAT (visual / browser-driven).

---

## Goal Achievement — Per-Decision Verdict (D-01..D-07)

| Decision | Verdict | Evidence |
|---|---|---|
| D-01 — Stage 1 success → Stage 4 telemetry write | ACHIEVED | `web/lib/inngest/functions/classifier-verdict-worker.ts:243-297` |
| D-02 — Stage 4 overview: 3 collapsible sections, English labels | ACHIEVED | `web/app/(dashboard)/automations/[swarm]/stage-4/page.tsx:106-143`, `client-shell.tsx:3-24,46-50` |
| D-03 — Stage 1 before/after screenshots in detail-pane | ACHIEVED | `web/components/automations/bulk-review/audit/StageScreenshotStrip.tsx:1-53`, `_shell/detail-pane.tsx:45,375-397` |
| D-04 — Screenshot read-path: path columns + sign-on-demand | ACHIEVED | `supabase/migrations/20260518a_email_labels_screenshot_paths.sql:17-21`, `label-email-in-icontroller.ts:41-44,101,128,159,178,195`, `debtor-email-icontroller-tagger.ts:171-176` |
| D-05 — decision_details payload: minimal (noise_category, source_stage, archived_at) | ACHIEVED | `classifier-verdict-worker.ts:271-275` |
| D-06 — Idempotency via Phase 82.2 unique index, 23505 catch | ACHIEVED | `classifier-verdict-worker.ts:279-296` |
| D-07 — One-shot 30d backfill (Stage 4 events + screenshot paths) | ACHIEVED | `supabase/migrations/20260518b_backfill_stage4_auto_archived_noise.sql:1-62` |

---

## Per-Decision Detail

### D-01 — Stage 1 success → Stage 4 telemetry write — ACHIEVED

**Evidence:** `web/lib/inngest/functions/classifier-verdict-worker.ts:243-297`

- `step.run("emit-stage-4-handled", ...)` is added inside the `categorize_archive` branch, before `mark-completed` (line 337).
- `emitPipelineEvent()` call with `stage: 4`, `decision: "auto_archived_noise"`, `swarm_type: swarm_type ?? "debtor-email"`, `triggered_by: "pipeline"`.
- `archived_at` is generated inside `step.run()` (line 274 — `new Date().toISOString()`) per Phase 65 replay-safety rule.
- Independent `emails.id` lookup inside the step (lines 248-258) keeps the emit replay-atomic and resolves the gap that `emailRow` is only loaded conditionally upstream.
- Silent skip when no `emails` row exists (lines 259-264) — sound because the Phase 82.2 unique index requires `email_id IS NOT NULL`.

**Hard separation preserved:** No `swarm_intents` reference; Stage 4 row is purely informational.

### D-02 — Stage 4 three-section overview, English labels — ACHIEVED

**Evidence:** `web/app/(dashboard)/automations/[swarm]/stage-4/page.tsx` + `client-shell.tsx`

- `page.tsx:106-110` — three parallel data loads: `loadKanbanRows`, `loadSwarmNoiseCategories`, `loadAutoArchivedNoiseRows`.
- `page.tsx:116-121` — filters split `allRows` into `handlerErrorRows` (`kanban_reason === 'handler_error'`) and `needsReviewRows` (`kanban_reason === 'handler_needs_review'`).
- `page.tsx:139-143` — unified row stream concatenates handler-error → needs-review → auto-archived (default-open priority for keyboard nav).
- `client-shell.tsx:3-24` — header comment locks the three-section structure with English labels: "Handler error" (red, defaultOpen) / "Needs review" (amber, COLLAPSED) / "Auto-archived" (lime, COLLAPSED).
- `client-shell.tsx:46-50` — Radix `Collapsible` primitives imported (re-uses existing dependency, no new lib).
- `kanban-loader.ts:29` — `KanbanReason` union widened to include `"handler_needs_review"`, with comment noting it's empty today by design.
- `page.tsx:248-254` — stage-4 tab badge counts only `handlerErrorRows.length` (focuses operator attention on action lane).
- Sort order: `loadAutoArchivedNoiseRows` orders `created_at DESC` (line 56 of loader), matching D-02 "newest first".

**Hard separation preserved:** `client-shell.tsx:10-16` documents the contract: `categories` = Stage 1 noise registry; no `intents` (Stage 4 has no Replay path); auto-archived rows remain rooted in `swarm_noise_categories`.

### D-03 — Stage 1 before/after screenshots in detail-pane — ACHIEVED

**Evidence:**
- `web/components/automations/bulk-review/audit/StageScreenshotStrip.tsx:1-53` — new component reads `beforePath` / `afterPath`, renders `<ScreenshotThumb>` pair, falls back to "No screenshots available" empty state when both null. One-sided null renders an em-dash placeholder.
- `_shell/detail-pane.tsx:45` — imports `StageScreenshotStrip`.
- `_shell/detail-pane.tsx:375-397` — strip is appended INSIDE the Stage 1 audit-expander (`merged[1]`) via React `Fragment`, preserving any existing Stage 1 audit content (e.g. `Stage1EvidencePanel`).
- `stage-1/page.tsx:312, 882-950, 1054, 1255` — Stage 1 page-loader fetches `email_id, screenshot_before_path, screenshot_after_path` from `debtor.email_labels` and threads `screenshotPathsByEmailId` through `UnifiedDetailPane`.
- `stage-4/page.tsx:176-218` — same path-map loader runs on Stage 4 page (parallel SELECT alongside bodies/timeline) and is passed to `Stage4ClientShell`, so Auto-archived rows reuse the same Stage 1 audit-expander strip mount.
- Test pair exists: `web/components/automations/bulk-review/audit/__tests__/StageScreenshotStrip.test.tsx`.
- `ScreenshotThumb` dependency already exists (`web/components/automations/bulk-review/audit/ScreenshotThumb.tsx`) — re-uses Stage 2 EvidencePanel pattern; no new signed-url endpoint or URL-TTL handling required.

**Hard separation preserved:** Strip is mounted on Stage 1's audit slot only (`merged[1]`); Stage 4 reuses the same slot because Auto-archived rows are Stage 1 noise-filter outputs surfaced for visibility. No Stage 3 cross-wiring.

### D-04 — Screenshot read-path: path columns + sign-on-demand — ACHIEVED

**Evidence:**
- Migration `supabase/migrations/20260518a_email_labels_screenshot_paths.sql:17-21` — adds `screenshot_before_path text NULL` and `screenshot_after_path text NULL` via `ADD COLUMN IF NOT EXISTS` (idempotent), within `BEGIN/COMMIT`. Existing `_url` columns retained for back-compat.
- Capture-side `label-email-in-icontroller.ts:41-44` — return type carries `screenshot_before_path` + `screenshot_after_path`. Lines 75-78, 89-92, 135-138, 166-169, 183-186, 202-205 — all 5 return code-paths persist both URL and path pairs (consistent with D-04 plan-03 mandate).
- Persistence `debtor-email-icontroller-tagger.ts:171-176` — truthy-guarded writes of both path columns alongside the existing URL writes.
- Read-side: detail-pane loader (`stage-1/page.tsx:921-950`, `stage-4/page.tsx:198-218`) SELECTs `screenshot_before_path` + `screenshot_after_path` from `debtor.email_labels` and threads them as `screenshotPathsByEmailId` to the strip; the strip then re-signs via the existing `ScreenshotThumb(path)` API (no 1-hour TTL leak on read).

### D-05 — decision_details payload: minimal — ACHIEVED

**Evidence:** `classifier-verdict-worker.ts:271-275`

```
decision_details: {
  noise_category: category.category_key,
  source_stage: 1,
  archived_at: new Date().toISOString(),
}
```

Matches D-05 exactly. No denormalised screenshot URLs, no `outlook_message_id`, no `classifier_rule_id` — all retrievable via JOIN per the minimal-write principle.

### D-06 — Idempotency via Phase 82.2 unique index — ACHIEVED

**Evidence:** `classifier-verdict-worker.ts:279-296`

- `emitPipelineEvent(...)` call wrapped in try/catch.
- Detection on `err.code === '23505'` OR message-substring matches `"duplicate key value"` / `"23505"` / `"pipeline_events_one_per_stage_email"` — robust to PostgrestError wrapping.
- On match: silent return (idempotent no-op).
- No new migration was created (per D-06 revision — relies on the existing Phase 82.2 unique partial index `pipeline_events_one_per_stage_email`).

### D-07 — One-shot 30d backfill — ACHIEVED

**Evidence:** `supabase/migrations/20260518b_backfill_stage4_auto_archived_noise.sql:1-62`

- **Pass A (lines 9-42):** INSERTs Stage 4 `auto_archived_noise` rows for every tagged `debtor.email_labels` in the 30-day window. LEFT JOINs `pipeline_events` Stage 1 row for `noise_category`; `noise_category_unknown` flag set when Stage 1 trace missing (pre-Phase-82.2 emails). `automation_run_id` is correctly set to `NULL` because `email_labels` has no such column (RESEARCH §1.1 confirmed). `triggered_by='backfill'`, `backfilled=true` marker in decision_details. Idempotent via `ON CONFLICT ON CONSTRAINT pipeline_events_one_per_stage_email DO NOTHING`.
- **Pass B (lines 44-59):** UPDATEs `debtor.email_labels` extracting `screenshot_*_path` from existing `_url` strings via Postgres `substring(... FROM 'object/sign/automation-screenshots/(.+?)\?')`. Bounded to `screenshot_*_path IS NULL` (idempotent) and 30-day window.
- Atomic via `BEGIN/COMMIT`.

---

## Required Artifacts — Status

| Artifact | Status | Notes |
|---|---|---|
| `supabase/migrations/20260518a_email_labels_screenshot_paths.sql` | VERIFIED | additive DDL; idempotent |
| `supabase/migrations/20260518b_backfill_stage4_auto_archived_noise.sql` | VERIFIED | two-pass, idempotent |
| `web/lib/inngest/functions/classifier-verdict-worker.ts` (D-01 emit) | VERIFIED | emit-stage-4-handled step wired |
| `web/lib/automations/debtor-email/label-email-in-icontroller.ts` (D-04 capture) | VERIFIED | all 5 return paths persist path pair |
| `web/lib/inngest/functions/debtor-email-icontroller-tagger.ts` (D-04 persist) | VERIFIED | path columns written |
| `web/components/automations/bulk-review/audit/StageScreenshotStrip.tsx` | VERIFIED | + test file present |
| `web/app/(dashboard)/automations/[swarm]/stage-4/page.tsx` | VERIFIED | three-section data fan-out |
| `web/app/(dashboard)/automations/[swarm]/stage-4/client-shell.tsx` | VERIFIED | three Collapsibles, hard-separation contract documented |
| `web/app/(dashboard)/automations/[swarm]/stage-4/_lib/load-auto-archived-noise-rows.ts` | VERIFIED | new loader |
| `web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx` | VERIFIED | screenshot paths threaded |
| `web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx` | VERIFIED | strip mounted inside Stage 1 audit expander |

---

## Key Link Verification (wiring)

| From | To | Via | Status |
|---|---|---|---|
| `classifier-verdict-worker.ts categorize_archive success` | `pipeline_events` Stage 4 row | `emitPipelineEvent(stage:4, decision:'auto_archived_noise')` | WIRED |
| `Stage 4 page` | Auto-archived data | `loadAutoArchivedNoiseRows(admin, swarmType)` | WIRED |
| Auto-archived data | Detail-pane render | `bodyMap[email_id]` inline-filled (page.tsx:168-175) | WIRED |
| `Stage 1 page` + `Stage 4 page` | `StageScreenshotStrip` | `screenshotPathsByEmailId` prop → `detail-pane.tsx:382-388` | WIRED |
| `label-email-in-icontroller` capture | `debtor.email_labels.screenshot_*_path` | `tagger.ts:171-176` truthy guard writes | WIRED |
| Backfill migration | Stage 4 unique-index constraint | `ON CONFLICT ON CONSTRAINT pipeline_events_one_per_stage_email DO NOTHING` | WIRED |

No orphaned components — every new artifact has an import path from a page.

---

## Hard-Separation Compliance (RFC docs/agentic-pipeline/README.md)

- Stage 4 success row uses `decision = 'auto_archived_noise'`; carries `noise_category` (from `swarm_noise_categories`) in `decision_details` only — never touches `swarm_intents`.
- `client-shell.tsx:10-16` explicitly documents `intents = []` for Stage 4 and locks auto-archived row provenance to `swarm_noise_categories`.
- `load-auto-archived-noise-rows.ts:9-14` re-states: "this loader never touches swarm_intents."
- Stage 1 screenshot strip is mounted on the Stage 1 audit slot only (`merged[1]`); reused by Stage 4 Auto-archived rows because they ARE Stage 1 outputs — not blurring stages.

Compliant.

---

## Anti-Patterns Found

None. Notable positive patterns:

- Replay-safe id/timestamp generation inside `step.run()` (Phase 65 lesson applied — `classifier-verdict-worker.ts:243-297`).
- Idempotent migrations (`ADD COLUMN IF NOT EXISTS`, `ON CONFLICT DO NOTHING`).
- Additive schema change — existing `_url` columns retained, zero breaking changes.
- Direct-prop channel for stage-specific audit augmentation avoids polluting `build-stage-audit-map.ts` plumbing.
- Tests present for new isolated component (`StageScreenshotStrip.test.tsx`).

---

## Requirements Coverage

Phase 82.8 covers operator-stated requirements 1-3 from CONTEXT.md (2026-05-18 operator review):

1. Stage 1 before/after screenshots — D-03/D-04 (SATISFIED at code level).
2. Stage 4 "afgehandeld" overview — D-01/D-02/D-07 (SATISFIED at code level).
3. Stage 4 = three types — D-02 (SATISFIED; `handler_needs_review` section empty by design, awaiting first review-required handler).

---

## Human Verification Required (UAT)

Code-level verification is complete; the following require operator browser testing before phase can be graded `passed`:

### 1. Stage 4 — three-section layout renders correctly
**Test:** Open `/automations/debtor-email/stage-4` in a logged-in session.
**Expected:** Three Collapsible sections in order: Handler error (default OPEN, red accent), Needs review (COLLAPSED, amber, empty-state copy), Auto-archived (COLLAPSED, lime). Tab badge shows handler-error count only.
**Why human:** Visual + collapse-state defaults.

### 2. Auto-archived section shows backfilled 30-day history
**Test:** Expand the Auto-archived section after deploying both migrations.
**Expected:** Several hundred to low-thousand rows, newest first, with `backfilled: true` rows visible. Selecting a row shows sender/subject/body in detail-pane.
**Why human:** Live data + backfill volume sanity.

### 3. Stage 1 detail-pane shows before/after screenshots
**Test:** Open any recent Stage 1 row in detail-pane; expand the Stage 1 audit expander.
**Expected:** Two thumbnails (Before / After). Click to enlarge via `ScreenshotThumb` pattern.
**Why human:** Visual + signed-URL refresh works (no 403 on > 1h-old archives).

### 4. Auto-archived row detail-pane reuses Stage 1 screenshot strip
**Test:** Select an Auto-archived row, expand the Stage 1 audit slot in detail-pane.
**Expected:** Same Before/After strip renders (re-using Stage 1 audit slot). Empty-state copy "No screenshots available" for rows where `debtor.email_labels` has no paths.
**Why human:** Cross-section detail-pane reuse.

### 5. Stage 4 telemetry row is written on each fresh Stage 1 archive
**Test:** Trigger a Stage 1 noise classification for a new email. Inspect `pipeline_events` for `stage=4, decision='auto_archived_noise', email_id=<new>`.
**Expected:** Exactly one Stage 4 row written; replay does not duplicate (23505 caught silently).
**Why human:** Requires fresh inbound email or test fixture; involves Inngest replay timing.

### 6. iController capture writes both URL and path
**Test:** Run a fresh iController-archive for a recent email. Inspect `debtor.email_labels` columns.
**Expected:** All four columns populated: `screenshot_before_url`, `screenshot_after_url`, `screenshot_before_path`, `screenshot_after_path`.
**Why human:** Requires live Browserless run.

---

## Overall Phase Verdict

**code_verdict:** ACHIEVED — all 7 decisions (D-01..D-07) verified at file/line level, hard-separation contract preserved, idempotency wired at DB layer, replay-safety honoured.

**phase_status:** human_needed — six UAT items remain (visual + live-data confirmations). STATE.md already marks the phase as SHIPPED (`Phase 82.8 (2026-05-18) — Stage 4 three-section overview ... SHIPPED`), but a goal-backward verifier holds back the final `passed` grade until operator confirms the UAT checklist above.

---

## Recommendations

1. Run the 6 UAT items above with the operator to flip status from `human_needed` → `passed`.
2. After UAT confirmation, append a `verified` note to STATE.md (analogous to the `82.7.3` pattern: `docs(82.8): mark VERIFICATION status=verified after UAT confirmation`).
3. No code remediation required.
4. Note that `handler_needs_review` section is empty-by-design today; first review-required handler (likely V11.0) will populate it without further wiring changes — section is already mounted.
5. Watch for the trade-off noted in D-06: a handler that errors first, then retries successfully, will not write a second Stage 4 row (the first error row persists). Acceptable for v8.0; broader multi-row support is V11.0 work.

---

_Verified: 2026-05-18_
_Verifier: Claude (gsd-verifier, goal-backward code-level analysis)_
