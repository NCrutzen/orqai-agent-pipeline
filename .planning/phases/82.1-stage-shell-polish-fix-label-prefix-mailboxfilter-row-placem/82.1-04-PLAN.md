---
phase: 82.1-stage-shell-polish-fix-label-prefix-mailboxfilter-row-placem
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx
  - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/stage-1-override-pane.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/stage-1-widget.test.tsx
autonomous: true
requirements: []
decisions: [D-07, D-08, D-09, D-10, D-11, D-12]
must_haves:
  truths:
    - "/stage-1 detail pane renders the override picker INLINE in the Stage 1 cell of the 5-cell PipelineFlow — no duplicate header, no duplicate subject, no duplicate body expander, no duplicate Pipeline Overrides section."
    - "Confirming a category override via the Stage 1 cell dispatches confirmOverride (server action) → POST /api/automations/debtor-email/override succeeds → row is optimistically removed via useSelection().markPendingRemoval."
    - "OverrideConfirmDialog fires when ≥2 axes are dirty, or Stage 3 is dirty, or Stage 2 reRun is true (preserves Stage1OverridePane behaviour)."
    - "Keyboard shortcuts (Enter approve, Space reject, n skip, ⌘⏎ override-submit, Esc override-discard) work the same as Stage1OverridePane."
    - "stage-1-override-pane.tsx file no longer exists."
    - "_shell/components/stage-1-widget.tsx does NOT import swarm_intents / SwarmIntentRow (hard-separation grep gate)."
    - "Realtime channel ${swarmType}-review stays mounted via AutomationRealtimeProvider in page.tsx (unchanged)."
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx"
      provides: "Stage 1 override widget — category dropdown + OverrideConfirmDialog + confirmOverride wiring + optimistic-removal + keyboard listeners"
      min_lines: 150
      contains: "SwarmNoiseCategoryRow"
    - path: "web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx"
      provides: "Stage 1 cell renders new Stage1Widget; drops taggingFailuresSection prop; adds extrasBelowPipeline slot; adds iControllerBanner slot wiring (already present)"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx"
      provides: "Drops Stage1OverridePane mount; passes extrasBelowPipeline (tagging failures only) + iControllerBanner slots directly"
  key_links:
    - from: "_shell/components/stage-1-widget.tsx"
      to: "stage-1/actions.ts recordVerdict + /api/automations/debtor-email/override POST"
      via: "imported server action + fetch"
      pattern: "recordVerdict|/api/automations/debtor-email/override"
    - from: "Stage1Widget"
      to: "useSelection().markPendingRemoval"
      via: "_shell/selection-context"
      pattern: "markPendingRemoval"
    - from: "Stage1Widget"
      to: "OverrideConfirmDialog"
      via: "import from stage-1/components/override-confirm-dialog"
      pattern: "OverrideConfirmDialog"
---

<objective>
Fix 4 — Port the working Stage 1 override-picker logic from `stage-1/stage-1-override-pane.tsx` into a new `_shell/components/stage-1-widget.tsx`. Drop the `taggingFailuresSection` slot wiring for the override flow; introduce a narrower `extrasBelowPipeline` slot for ONLY the tagging-artifacts section. Delete the stale `stage-1-override-pane.tsx`.

Purpose: Today Stage1OverridePane is mounted inside UnifiedDetailPane's `taggingFailuresSection` slot, duplicating header/subject/body/pipeline-overrides chrome (the unified pane already renders all of these). Per D-07..D-12: the override picker should live INLINE in the Stage 1 cell widget (just the dropdown + confirm + remove flow); duplicate chrome must go.

Output: New `Stage1Widget` carries the override-picker logic. UnifiedDetailPane gets a narrower `extrasBelowPipeline` slot for tagging artifacts. `stage-1-override-pane.tsx` deleted. Hard-separation preserved at the import-level grep gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/82.1-stage-shell-polish-fix-label-prefix-mailboxfilter-row-placem/82.1-01-SUMMARY.md
</execution_context>

<context>
@.planning/phases/82.1-stage-shell-polish-fix-label-prefix-mailboxfilter-row-placem/82.1-CONTEXT.md
@web/app/(dashboard)/automations/[swarm]/stage-1/stage-1-override-pane.tsx
@web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-1-widget.tsx
@web/app/(dashboard)/automations/[swarm]/_shell/selection-context.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/actions.ts
@web/app/(dashboard)/automations/[swarm]/stage-1/components/override-confirm-dialog.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/components/eval-type-radio.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/components/icontroller-info-banner.tsx

<interfaces>
**Existing component to be wrapped (NOT replaced):**

`web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-1-widget.tsx` is the existing Phase 71-04 §S1 category Select component. It is a pure dropdown UI. It is RE-USED by the new `_shell/components/stage-1-widget.tsx` — see Task 1 below.

**New component prop contract (D-08) — locked:**
```ts
import type { SwarmNoiseCategoryRow } from "@/lib/swarms/types";

export interface Stage1WidgetProps {
  categories: SwarmNoiseCategoryRow[];     // hard-sep: noise registry ONLY
  value: string | null;                    // current prediction (null when fresh)
  onChange: (next: string) => void;        // marks the stage cell dirty in parent
  row: PredictedRow;                       // for server-action args (email_id, automation_run_id, result.message_id)
  swarmType: string;                       // threaded through recordVerdict + override POST
}
```

**Key sources to port from `stage-1-override-pane.tsx` (verbatim unless noted):**
- Category dropdown UI: lines 261-271 (`overrideOptions` filter) + the `<Select>` JSX is currently the legacy single-stage dropdown — the canonical inline-cell picker is the existing `Stage1Widget` (stage-1/components/stage-1-widget.tsx) which we re-use.
- `confirmOverride` flow: `submitOverride` (lines 612-766) — strip down to just stage_1 axis path (drop stage_2/3/4 payloads; the multi-axis flow stays in this widget for the Stage 1 cell only).
  - Actually preserve verbatim per D-12 §"Claude's Discretion" — keep the full multi-axis payload assembly. Stage 1 widget is invoked WHEN the user changes the Stage 1 cell; other cells' dirty state lives in the parent (UnifiedDetailPane) per existing DetailPaneInner `dirty` state. **For the port**: the widget only handles stage_1 dirty state + submit. Multi-axis support stays in UnifiedDetailPane's existing scaffolding — Plan 06 of Phase 82 already wired the dirty-state lift. Confirm during execute.
- `OverrideConfirmDialog` integration: lines 1213-1227 — mount inside the widget; trigger via `setConfirmOpen(true)` when dirtyCount ≥2 OR stage_3 OR stage_2 reRun (lines 802-820). For Stage 1-only flow, trigger only when needed.
- `useSelection().markPendingRemoval`: line 245 + call site 421 (and 723).
- Keyboard listeners: lines 833-905. Subscribe via `useEffect` in the widget.
- `confirmOverride` server-action call: there is no `confirmOverride` exported — the actual server action is `recordVerdict` from `./actions` (line 49) AND `fetch("/api/automations/debtor-email/override", ...)` for axis-payload submission (lines 697-707).

**UnifiedDetailPane slot changes (D-09):**
- DROP the `taggingFailuresSection?: ReactNode` prop from `UnifiedDetailPaneProps`.
- ADD a new `extrasBelowPipeline?: ReactNode` prop (renders below the 5-cell PipelineFlow). Tagging-artifacts section from Stage1OverridePane (lines 1050-1095) moves here, mounted only when Stage 1 client-shell passes it.
- KEEP the existing `iControllerBanner?: ReactNode` prop (already present at line 116). Stage 1 client-shell continues to pass an IControllerInfoBanner if needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create _shell/components/stage-1-widget.tsx with full override-picker logic</name>
  <files>
    web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx
    web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/stage-1-widget.test.tsx
  </files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/stage-1-override-pane.tsx (the SOURCE of all ported logic — read lines 168-906 in full; this is the canonical reference)
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-1-widget.tsx (the existing Phase 71-04 dropdown UI — to be re-used inside the new widget)
    - web/app/(dashboard)/automations/[swarm]/_shell/selection-context.tsx (verify useSelection() exports markPendingRemoval, pendingRemovalIds, selectedId, setSelected — already confirmed)
    - web/app/(dashboard)/automations/[swarm]/stage-1/actions.ts (recordVerdict signature)
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/override-confirm-dialog.tsx (OverrideConfirmDialog props)
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/eval-type-radio.tsx (EvalTypeRadio + EvalType type)
  </read_first>
  <behavior>
    Tests to add at `web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/stage-1-widget.test.tsx`:
    - Test 1 (RED first): "renders category dropdown with current value pre-selected" — pass `value="payment_admittance"`, render, assert the Select shows `payment_admittance` as its selected display.
    - Test 2: "dispatches confirmOverride on selection" — render with mocked `fetch` (`vi.spyOn(global, "fetch").mockResolvedValueOnce(new Response(null, { status: 200 }))`). User picks a different category. Stage1Widget should fire a POST to `/api/automations/debtor-email/override` with body containing `axis: "stage_1_category"`. Assert fetch called with that URL and JSON body via `expect(fetchSpy).toHaveBeenCalledWith("/api/automations/debtor-email/override", expect.objectContaining({ method: "POST" }))`.
    - Test 3: "marks row for optimistic removal after successful override submit" — wrap in a `<SelectionProvider initialSelectedId={row.id} rowIds={[row.id]}>`; after the override POST resolves, the widget should call `markPendingRemoval(row.id)`. Assert via spying on the SelectionProvider's context value (use the existing test harness pattern in `stage-1/__tests__/`).

    Hard-separation grep test (separate from the RTL file, runs in CI lint step):
    - `! grep -qE "swarm_intents|SwarmIntentRow" 'web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx'`
  </behavior>
  <action>
    Step A — Write failing tests first (RED):
    1. Create `web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/stage-1-widget.test.tsx`. Use the existing test harness conventions in this directory (search for `stage-1/__tests__/` siblings — `import { render, screen, fireEvent } from "@testing-library/react"`, `vi.spyOn`, `SelectionProvider` wrapper, etc.).
    2. Run `cd web && npx vitest run stage-1-widget.test --reporter=verbose` — expect ALL three tests to fail (component does not exist yet).

    Step B — Create the widget (GREEN):
    Create `web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx`.

    Imports — explicit (hard-separation gate: NO `SwarmIntentRow`, NO `intents`):
    ```ts
    "use client";
    import { useCallback, useEffect, useMemo, useRef, useState } from "react";
    import { useRouter } from "next/navigation";
    import { toast } from "sonner";
    import { Button } from "@/components/ui/button";
    import { Textarea } from "@/components/ui/textarea";
    import { Stage1Widget as Stage1CategorySelect } from "../../stage-1/components/stage-1-widget";
    import { EvalTypeRadio, type EvalType } from "../../stage-1/components/eval-type-radio";
    import { OverrideConfirmDialog, type OverrideConfirmTrigger } from "../../stage-1/components/override-confirm-dialog";
    import { recordVerdict } from "../../stage-1/actions";
    import { useSelection } from "../selection-context";
    import type { SwarmNoiseCategoryRow } from "@/lib/swarms/types";
    import type { PredictedRow } from "../../stage-1/page";
    ```

    Component contract (D-08): export `Stage1Widget(props: Stage1WidgetProps)` exactly per the interface block in `<interfaces>` above.

    Ported logic (port VERBATIM from `stage-1-override-pane.tsx` except where noted — D-12 default is verbatim):
    1. **Dirty state for stage_1 only**: `const [dirty, setDirty] = useState<{ categoryKey: string } | null>(null);`. Called via `onChange` prop from the parent UnifiedDetailPane when user picks from the dropdown.
    2. **Category dropdown**: render `<Stage1CategorySelect categories={categories} value={value} onChange={(key) => { onChange(key); setDirty({ categoryKey: key }); }} />` (re-uses the Phase 71-04 dropdown UI verbatim — no UI duplication).
    3. **Notes textarea + EvalTypeRadio**: copy lines 1115-1167 from stage-1-override-pane.tsx, scoped to local widget state.
    4. **OverrideConfirmDialog**: copy lines 1213-1227 from stage-1-override-pane.tsx. Trigger logic: for the Stage-1-cell-only flow, the only dialog trigger is multi_axis IF the parent reports other dirty axes (deferred — for Wave 2 Plan 04 the widget only owns its own dirty cell; multi-axis dialog wiring lives in UnifiedDetailPane and is already scaffolded there in dirty Record state). Keep the dialog mount inside this widget anyway so the stage_1-only single-axis confirm still works.
    5. **`onClickSubmit` + `submitOverride`**: copy and adapt from stage-1-override-pane.tsx lines 612-766. Reduce the axis-payload assembly to ONLY the stage_1 axis. POST `/api/automations/debtor-email/override` with payload `{ axis: "stage_1_category", email_id: <row.id>, original_event_id: <from timeline if available else nil UUID>, original_decision: <value ?? "">, decision: dirty.categoryKey, decision_details: { category_key: dirty.categoryKey }, eval_type: evalType, reason: notes.trim() || undefined }`. NOTE: the route handler accepts this shape per route docs — see `web/app/api/automations/debtor-email/override/route.ts`.
    6. **Optimistic removal**: on success → `markPendingRemoval(row.id)` from `useSelection()`. Toast success.
    7. **Keyboard listeners**: copy the relevant subset from lines 833-905. For the Stage-1-only widget keep: `bulk-review:override-submit` → `onClickSubmit`, `bulk-review:override-discard` → `discardOverride`, `bulk-review:eval-type-capability` / `bulk-review:eval-type-regression` → setEvalType. Approve/reject/skip/toggle-body handlers STAY on the parent UnifiedDetailPane (already wired there — see detail-pane.tsx lines 213-224); do not re-subscribe here.
    8. **Discard**: `setDirty(null); setNotes(""); setEvalType("regression"); setConfirmOpen(false);`
    9. **Notes validation**: copy the `notesRequired` / `notesValid` logic from stage-1-override-pane.tsx lines 968-976; gate the Submit button per the same rules.

    Hard-separation gate (D-11): the file MUST NOT import `swarm_intents` or `SwarmIntentRow`. Enforced via the grep acceptance criterion below.

    Step C — Run tests (GREEN):
    Run `cd web && npx vitest run stage-1-widget.test --reporter=verbose` — all three tests should pass.

    Step D (REFACTOR, optional):
    If any duplication with stage-1-override-pane.tsx remains beyond what's portable to the widget, leave it for follow-up. Verbatim is the explicit D-12 default.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx vitest run stage-1-widget.test --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `test -f 'web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx'`
    - `grep -q "export function Stage1Widget" 'web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx'`
    - `grep -q "SwarmNoiseCategoryRow" 'web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx'`
    - `! grep -qE "swarm_intents|SwarmIntentRow" 'web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx'` ← HARD-SEP GATE (D-11)
    - `grep -q "markPendingRemoval" 'web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx'`
    - `grep -q "OverrideConfirmDialog" 'web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx'`
    - `grep -q "/api/automations/debtor-email/override" 'web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx'`
    - `test -f 'web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/stage-1-widget.test.tsx'`
    - Vitest: all three new tests pass.
  </acceptance_criteria>
  <done>New Stage1Widget exists at `_shell/components/stage-1-widget.tsx`, exports the locked prop contract (D-08), passes the three RTL tests, contains no `swarm_intents` import (D-11 grep gate).</done>
</task>

<task type="auto">
  <name>Task 2: Wire new Stage1Widget into UnifiedDetailPane; swap slot prop</name>
  <files>
    web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx
    web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx
  </files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx (target: import path on line 51 for Stage1Widget; the n===1 widget render at lines 246-256; UnifiedDetailPaneProps lines 102-117; the taggingFailuresSection render at lines 430-432)
    - web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx (target: the `Stage1OverridePane` mount currently passed as `taggingFailuresSection`)
  </read_first>
  <action>
    Step A — `_shell/detail-pane.tsx`:
    1. Change the Stage1Widget import on line 51:
       `import { Stage1Widget } from "../stage-1/components/stage-1-widget";`
       → `import { Stage1Widget } from "./components/stage-1-widget";` (the new widget).
    2. Update `UnifiedDetailPaneProps` (lines 102-117):
       - DELETE: `taggingFailuresSection?: ReactNode;`
       - ADD: `extrasBelowPipeline?: ReactNode;`
       - KEEP: `iControllerBanner?: ReactNode;`
    3. Update the function signature destructure (line 130 + 178) — replace `taggingFailuresSection` with `extrasBelowPipeline` in both `UnifiedDetailPane` and `DetailPaneInner`.
    4. Update the render of `<Stage1Widget>` at lines 246-256: pass new props per D-08:
       ```tsx
       widget = (
         <Stage1Widget
           categories={_categories}
           value={ev?.decision ?? null}
           onChange={(next) => {
             onMarkDirty(1);
             // Cell-level dirty state lives inside Stage1Widget; this parent
             // signal only updates the PipelineFlow cell visual state.
             void next;
           }}
           row={row as unknown as import("../stage-1/page").PredictedRow}
           swarmType={swarmType}
         />
       );
       ```
       (The `row` cast bridges the unified `Row` shape to `PredictedRow` for server-action args. The widget reads `row.id` and `row.automation_run_id` + `row.result.message_id` for the POST body. If `row.automation_run_id` is missing on the unified Row shape, the parent `UnifiedDetailPaneProps.row` type must be widened to carry it through; check at execute-time and either widen `Row` in `_shell/_lib/types.ts` to optionally include `automation_run_id?: string | null` OR thread a separate `selectedPredictedRow` prop from the client shell. Prefer the type widening since it's a single optional field.)
    5. Update the slot render at lines 430-432: replace `taggingFailuresSection` with `extrasBelowPipeline`. Keep the existing `iControllerBanner` slot render at lines 365-367 unchanged.

    Step B — `stage-1/client-shell.tsx`:
    1. DELETE the import of `Stage1OverridePane`.
    2. DELETE the entire `overrideFlowSlot = <Stage1OverridePane ... />` JSX block (lines ~121-134).
    3. In the `<UnifiedDetailPane ... />` JSX, DELETE the `taggingFailuresSection={overrideFlowSlot}` prop.
    4. ADD an `extrasBelowPipeline={taggingFailuresExtra}` prop. Build `taggingFailuresExtra` locally as a `<TaggingArtifactsSection row={selectedPredictedRow} />` slot — copy lines 1050-1095 from stage-1-override-pane.tsx into a NEW small component file `web/app/(dashboard)/automations/[swarm]/stage-1/components/tagging-artifacts-section.tsx` (props: `{ row: PredictedRow | null }`; returns null when `!row.tagging`).
    5. Pass `iControllerBanner={icBanner}` if and only if Stage 4 fires the banner — for Stage 1 page, leave it `undefined` for now (the banner originally fired via `setShowICBanner(true)` inside Stage1OverridePane's submitOverride success path; the widget can fire its own banner via a `useState` + render the banner inside the widget if desired, OR Stage 1 client-shell can subscribe to a window event dispatched by the widget on success. Default: surface inside the widget itself — the IControllerInfoBanner can mount conditionally in Stage1Widget's JSX. **Out of scope for this plan unless smoke reveals the banner is missing.** Document as discretionary follow-up.)
    6. Select the PredictedRow that matches the current `selectedId` (already computed as `initialSelectedRow` for ?selected=; for client-side selection use `predictedRows.find((r) => r.id === selectedId)`). Thread this into the new TaggingArtifactsSection and into the widget via the row cast above.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "_shell/(detail-pane|components/stage-1-widget)|stage-1/(client-shell|stage-1-override-pane)" ; true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'from "./components/stage-1-widget"' 'web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx'`
    - `! grep -q "taggingFailuresSection" 'web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx'`
    - `grep -q "extrasBelowPipeline" 'web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx'`
    - `! grep -q "Stage1OverridePane" 'web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx'`
    - `grep -q "extrasBelowPipeline" 'web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx'`
    - tsc compiles without new errors in the touched files.
  </acceptance_criteria>
  <done>UnifiedDetailPane renders the new Stage1Widget inline in the Stage 1 cell. Stage1ClientShell no longer mounts Stage1OverridePane. TaggingArtifactsSection slot wired via extrasBelowPipeline. tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Delete stage-1-override-pane.tsx and verify no orphaned consumers</name>
  <files>web/app/(dashboard)/automations/[swarm]/stage-1/stage-1-override-pane.tsx</files>
  <read_first>
    - run: `grep -rE "stage-1-override-pane|Stage1OverridePane" web/app web/lib 2>/dev/null` BEFORE deleting — every match must be either the file itself OR an already-removed import in Task 2.
  </read_first>
  <action>
    1. Grep gate: `grep -rE "stage-1-override-pane|Stage1OverridePane" web/app web/lib 2>/dev/null`. Expected matches BEFORE deletion: the file itself + nothing else (Task 2 removed the client-shell import). If anything else matches, fix it before continuing.
    2. Run `git rm web/app/(dashboard)/automations/[swarm]/stage-1/stage-1-override-pane.tsx` (use `rm` if not staged; commit at end).
    3. Re-run grep — should return zero matches.
    4. Run `cd web && npx tsc --noEmit -p tsconfig.json` — must pass cleanly.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `! test -f 'web/app/(dashboard)/automations/[swarm]/stage-1/stage-1-override-pane.tsx'`
    - `! grep -rqE "stage-1-override-pane|Stage1OverridePane" web/app web/lib`
    - tsc passes.
  </acceptance_criteria>
  <done>stage-1-override-pane.tsx deleted, no orphaned references, tsc green.</done>
</task>

</tasks>

<verification>
Manual smoke on /stage-1:
1. Click any predicted row → detail pane opens.
2. Pipeline cells render with single "Stage N — Label" prefix (Plan 01 + this plan).
3. Stage 1 cell shows the category dropdown INLINE (not in a separate Pipeline Overrides section below).
4. Pick a different category → cell turns dirty → notes textarea + EvalType radio appear → Submit override.
5. Submit fires POST `/api/automations/debtor-email/override` (verify in Network tab) → toast success → row optimistically removed → next row selected.
6. ⌘⏎ shortcut submits override; Esc discards.
7. No duplicate header/subject/body/Pipeline Overrides chrome anywhere in the pane.
8. Tagging artifacts (if row.tagging is set) render BELOW the pipeline flow via extrasBelowPipeline slot.

RTL: `cd web && npx vitest run` — all Stage 1 tests pass, including the three new Stage1Widget tests.

Hard-separation grep gate:
```
! grep -qE "swarm_intents|SwarmIntentRow" 'web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx'
```

Cleanup gate:
```
! test -f 'web/app/(dashboard)/automations/[swarm]/stage-1/stage-1-override-pane.tsx'
```
</verification>

<success_criteria>
- New `_shell/components/stage-1-widget.tsx` exists with full override-picker logic (D-07, D-08).
- UnifiedDetailPane: `taggingFailuresSection` slot dropped; `extrasBelowPipeline` added; `iControllerBanner` kept (D-09).
- `stage-1-override-pane.tsx` deleted (D-10).
- Hard-separation grep gate: no `swarm_intents` / `SwarmIntentRow` in the new widget (D-11).
- Realtime channel `${swarmType}-review` still mounted in `stage-1/page.tsx` (D-12, no change).
- RTL: 3 new Stage1Widget tests pass; all existing Stage 1 tests still pass.
- tsc clean.
</success_criteria>

<output>
After completion, create `.planning/phases/82.1-stage-shell-polish-fix-label-prefix-mailboxfilter-row-placem/82.1-04-SUMMARY.md` per template.
</output>
