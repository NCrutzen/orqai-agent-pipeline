---
phase: 82.1-stage-shell-polish-fix-label-prefix-mailboxfilter-row-placem
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/noise-category-chip-strip.tsx
autonomous: true
requirements: []
decisions: [D-02, D-03]
must_haves:
  truths:
    - "/stage-1 renders MailboxFilter on the SAME horizontal row as NoiseCategoryChipStrip (chip strip flows left, MailboxFilter sits right via justifyContent: space-between)."
    - "/stage-1 no longer renders a standalone flex row above the row-list grid containing MailboxFilter."
    - "/stage-3 and /stage-4 mounts are untouched (they already share a row — see CONTEXT)."
    - "Stages 0/2 with no chip strip continue to render MailboxFilter on their existing row (untouched)."
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx"
      provides: "MailboxFilter mounted next to NoiseCategoryChipStrip"
      contains: "MailboxFilter"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx"
      provides: "MailboxFilter import + flex wrapper REMOVED"
  key_links:
    - from: "stage-1/page.tsx"
      to: "_shell/mailbox-filter.tsx"
      via: "direct import + render adjacent to <NoiseCategoryChipStrip>"
      pattern: "MailboxFilter"
---

<objective>
Fix 2 — Hoist MailboxFilter out of `stage-1/client-shell.tsx` onto the chip-strip row at the page level so it sits on the right edge of the NoiseCategoryChipStrip.

Purpose: Per Phase 82 D-11, MailboxFilter belongs on the right edge of the primary stage-specific chip strip. Today /stage-1's MailboxFilter renders inside `Stage1ClientShell` in its own flex row above the two-column grid; the chip strip lives one level up in `page.tsx`. Stages 3/4 already share rows (verified in CONTEXT additional_context).

Output: /stage-1 chip strip and MailboxFilter share a single flex row at the page level (justifyContent: space-between). Stage1ClientShell no longer mounts MailboxFilter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/82.1-stage-shell-polish-fix-label-prefix-mailboxfilter-row-placem/82.1-CONTEXT.md
@web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx

<interfaces>
From web/app/(dashboard)/automations/[swarm]/_shell/mailbox-filter.tsx (existing component):
```ts
export interface MailboxFilterProps {
  mailboxes: MailboxOption[];
  selected: number[];
}
```

From stage-1/page.tsx (already loaded):
```ts
const mailboxes = getSwarmMailboxes(swarmType, unifiedRows);
const selectedMailboxes = parseSelectedMailboxes(sp.mailbox);
```

These two variables MUST be passed into `<MailboxFilter>` at the page level (they are already computed before the JSX return).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount MailboxFilter on the chip-strip row at the page level</name>
  <files>
    web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx
    web/app/(dashboard)/automations/[swarm]/stage-1/noise-category-chip-strip.tsx
  </files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx (lines 930-985, the JSX block that renders `<NoiseCategoryChipStrip>` followed by `<Stage1ClientShell>`)
    - web/app/(dashboard)/automations/[swarm]/stage-1/noise-category-chip-strip.tsx (one-time read: confirm whether it returns a single element or fragment — determines whether wrapping in a flex container is safe)
  </read_first>
  <action>
    Step A — `stage-1/page.tsx`:
    1. Add an import at the top: `import { MailboxFilter } from "../_shell/mailbox-filter";`
    2. Locate the JSX where `<NoiseCategoryChipStrip ... />` is rendered (around line 935-941, inside `<main>` and BEFORE the `sp.sub === "pending"` ternary).
    3. Wrap `<NoiseCategoryChipStrip>` in a flex container that ALSO renders `<MailboxFilter>`. The result:

       ```tsx
       <div
         style={{
           display: "flex",
           alignItems: "center",
           justifyContent: "space-between",
           gap: "var(--space-3)",
           // Tight bottom margin so the chip-strip row sits flush above the row-list grid.
           marginBottom: "var(--space-3)",
         }}
       >
         <NoiseCategoryChipStrip
           categories={categories}
           counts={data.counts}
           activeTopic={sp.topic ?? "all"}
           candidateCount={data.candidates.length}
           activeSub={sp.sub ?? null}
         />
         <MailboxFilter mailboxes={mailboxes} selected={selectedMailboxes} />
       </div>
       ```

    4. Pass-through requirements: `mailboxes` and `selectedMailboxes` are already computed earlier in the function (lines ~914-915). Do not recompute.
    5. Do NOT remove the MailboxFilter prop from `<Stage1ClientShell>` props yet — leave the unused prop for graceful behaviour; the shell will drop its mount in Task 2 and stop reading these props. (Actually: in Task 2 we will also remove `mailboxes` and `selectedMailboxes` from the shell's prop type and call site. Confirm the props are wired in Task 2 below.)

    Step B — `stage-1/noise-category-chip-strip.tsx`:
    - If `NoiseCategoryChipStrip` renders a top-level element that already sets `width: 100%` or pushes via `marginRight: auto`, no edit needed. If it returns a `<nav>` / `<div>` with intrinsic width, no edit needed — `justifyContent: space-between` on the parent flex handles the push.
    - Verify the chip strip doesn't apply its own `margin-bottom` that would visually separate it from the row below. If it does, leave the chip strip's margin alone; the parent flex row inherits it.
    - **No edit required unless visual smoke reveals overflow.** If a min-width-0 fix is needed, add `style={{ minWidth: 0 }}` to the chip strip's outer wrapper — but only as a follow-up.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "stage-1/(page|client-shell|noise-category-chip-strip)" ; true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'import { MailboxFilter } from "../_shell/mailbox-filter"' 'web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx'`
    - `grep -q "MailboxFilter mailboxes={mailboxes} selected={selectedMailboxes}" 'web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx'`
    - `grep -q "justifyContent: \"space-between\"" 'web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx'`
  </acceptance_criteria>
  <done>Page-level chip-strip-plus-MailboxFilter flex row exists; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Remove MailboxFilter mount from Stage1ClientShell</name>
  <files>web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx (full file; we just read it — flex MailboxFilter wrapper lives at lines 147-156)
    - web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx (verify the page-level mount from Task 1 reads `mailboxes` + `selectedMailboxes` from local variables, NOT from any prop hand-off)
  </read_first>
  <action>
    In `stage-1/client-shell.tsx`:

    1. Delete the import on line 37: `import { MailboxFilter } from "../_shell/mailbox-filter";`
    2. Delete the entire flex wrapper currently at lines 147-156:

       ```tsx
       <div
         style={{
           display: "flex",
           alignItems: "center",
           justifyContent: "flex-end",
           gap: "var(--space-3)",
         }}
       >
         <MailboxFilter mailboxes={mailboxes} selected={selectedMailboxes} />
       </div>
       ```

    3. Keep client-side mailbox filter logic (the `useMemo` over `selectedMailboxes` at lines 94-100) — Task 1 still passes `selectedMailboxes` via the prop hand-off from the page. Remove the unused `mailboxes` prop:
       - In `Stage1ClientShellProps`, delete the line `mailboxes: MailboxOption[];`
       - In the function signature destructure, delete `mailboxes,`
       - In the import on line 41, drop `MailboxOption` if it is now unused: `import type { MailboxOption } from "../_shell/_lib/get-swarm-mailboxes";` — remove if no other reference remains. (Grep within this file for `MailboxOption`; if zero matches outside the import, delete the import line.)
       - In `stage-1/page.tsx`, REMOVE the `mailboxes={mailboxes}` line from the `<Stage1ClientShell ... />` JSX (the page-level MailboxFilter from Task 1 already consumes the local `mailboxes` variable).
    4. KEEP `selectedMailboxes` as a Stage1ClientShellProps field — it drives the in-shell `useMemo` row filter. Do not delete it.
    5. The outer flex column wrapper (`display: "flex", flexDirection: "column", gap: "var(--space-3)", padding: "var(--space-4)"`) stays; it now contains only the two-column grid.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "stage-1/(client-shell|page)" ; true</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "MailboxFilter" 'web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx'`
    - `! grep -qE 'mailboxes\s*:\s*MailboxOption\[\]' 'web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx'`
    - `grep -q "selectedMailboxes" 'web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx'` (row-filter useMemo stays)
    - `! grep -q "mailboxes={mailboxes}" 'web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx' | grep -v 'MailboxFilter'`  (no mailboxes prop passed to Stage1ClientShell)
  </acceptance_criteria>
  <done>Stage1ClientShell no longer mounts or imports MailboxFilter; tsc passes; client-side row filter on `selectedMailboxes` still runs.</done>
</task>

</tasks>

<verification>
Manual smoke:
- /stage-1: chip strip and MailboxFilter share one horizontal row (chips left, dropdown right).
- /stage-1 row list still filters when MailboxFilter changes (selectedMailboxes flows from URL → page → Stage1ClientShell).
- /stage-3 and /stage-4: unchanged (both already render MailboxFilter on the chip-strip row per CONTEXT).
- /stage-0, /stage-2: unchanged (D-02 says stages with no primary chip strip keep MailboxFilter on its own row).
</verification>

<success_criteria>
- MailboxFilter mounted at the page level next to NoiseCategoryChipStrip (D-02).
- MailboxFilter import and mount removed from Stage1ClientShell (D-02).
- No code-level changes to MailboxFilter itself (D-03).
- TypeScript passes.
</success_criteria>

<output>
After completion, create `.planning/phases/82.1-stage-shell-polish-fix-label-prefix-mailboxfilter-row-placem/82.1-02-SUMMARY.md` per template.
</output>
