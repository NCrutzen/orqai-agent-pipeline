---
phase: 56
plan: 03
type: execute
wave: 1
depends_on: [56-00]
files_modified:
  - .planning/briefs/artifacts/debtor-email-label-probe/candidates.json
  - .planning/briefs/artifacts/debtor-email-label-probe/SELECTORS.md
autonomous: false
requirements: [D-15, D-16, D-17, D-18, D-19]
must_haves:
  truths:
    - "Operator has run probe-label-ui.ts against production iController on Smeba mailbox"
    - "candidates.json captures all label/assign-related DOM elements (button, dropdown, modal markup)"
    - "Screenshots captured for: mailbox list, message detail, label-picker open state"
    - "SELECTORS.md documents the chosen selectors for: read current label state, open picker, search/select customer account, save"
    - "Probe artifact directory committed to git (screenshots may be referenced but kept out of git per .gitignore — selectors + JSON are committed)"
  artifacts:
    - path: ".planning/briefs/artifacts/debtor-email-label-probe/candidates.json"
      provides: "Raw DOM scan output for label/assign UI elements"
      min_lines: 10
    - path: ".planning/briefs/artifacts/debtor-email-label-probe/SELECTORS.md"
      provides: "Operator-curated selector contract for label-email-in-icontroller.ts to consume"
      min_lines: 25
  key_links:
    - from: "Wave 2 label-email-in-icontroller.ts (56-05)"
      to: "Probe-confirmed selectors"
      via: "direct CSS/XPath selectors copied from SELECTORS.md"
      pattern: "TODO\\(probe-artifact\\)"
---

<objective>
[BLOCKING] Run the probe script (`web/lib/automations/debtor-email/probe-label-ui.ts`, shipped in 56-00) against production iController. Capture the label/assign UI DOM. Curate selectors into SELECTORS.md so 56-05 (Browserless label module implementation) can copy them in instead of guessing.

This is the dispositive feasibility test: if iController has no per-message label primitive (RESEARCH Assumption A3), the entire phase is infeasible. The probe is the test.

Marked `autonomous: false` — operator action required: production iController credentials, Browserless run from a trusted host, manual review of DOM.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/56-icontroller-auto-labeling-van-accounts-aan-emails/56-CONTEXT.md
@.planning/phases/56-icontroller-auto-labeling-van-accounts-aan-emails/56-RESEARCH.md
@web/lib/automations/debtor-email/probe-label-ui.ts
@web/lib/automations/debtor-email/label-email-in-icontroller.ts
@web/lib/automations/debtor-email-cleanup/probe-email-popup.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Run probe-label-ui.ts on production iController + curate SELECTORS.md</name>
  <what-built>probe-label-ui.ts source exists (56-00); has never been executed. Selectors in label-email-in-icontroller.ts are TODO-marked.</what-built>
  <read_first>
    - web/lib/automations/debtor-email/probe-label-ui.ts (the script being run)
    - web/lib/automations/debtor-email-cleanup/probe-email-popup.ts (analog probe — same artifact format)
    - web/lib/automations/debtor-email/label-email-in-icontroller.ts (the consumer that needs the selectors)
    - .planning/phases/56-icontroller-auto-labeling-van-accounts-aan-emails/56-CONTEXT.md (D-15, D-16, D-17, D-18)
    - CLAUDE.md (Browserless patterns: playwright-core, connectOverCDP, NEVER networkidle)
  </read_first>
  <files>
    .planning/briefs/artifacts/debtor-email-label-probe/candidates.json,
    .planning/briefs/artifacts/debtor-email-label-probe/SELECTORS.md
  </files>
  <action>
**A) Run the probe** from the operator's local machine (Vercel + Browserless tokens already in `.env.local`):

```bash
cd "/Users/nickcrutzen/Developer/Agent Workforce"
ICONTROLLER_ENV=production pnpm tsx web/lib/automations/debtor-email/probe-label-ui.ts
```

Probe writes outputs to `.planning/briefs/artifacts/debtor-email-label-probe/`:
- `candidates.json` — raw DOM scan (label-keyword-matched elements)
- `00-mailbox-list-*.png`, `01-message-detail-*.png`, `02-label-picker-open-*.png` — screenshots (kept locally; ARE NOT committed if .gitignore excludes — see step D)

**B) Manual exploration** — operator opens the screenshots + interacts with iController in a separate browser tab. Capture:
- Where on the message-detail view is the **current label** displayed (so we can implement D-16 idempotency check)? Is it inline text? a tag chip? a dropdown showing the current value?
- What action opens the **label picker** (button click, dropdown trigger, right-click menu)?
- What is the **search input** for finding a customer account (autocomplete? type-ahead? full list)?
- How is **save/apply** triggered (button label, keyboard, auto-on-select)?
- Is there a **confirmation toast/dialog** to wait for after save?

**C) Iterate the probe** if needed — operator can edit `probe-label-ui.ts` to drill into specific elements (open the picker, capture sub-DOM). Re-run as needed. Each run overwrites the artifact dir.

**D) Curate `SELECTORS.md`** — the only file 56-05 will read. Strict format:

```markdown
# iController Label UI — Selector Contract

**Probed:** <date>  **Probed against:** production walkerfire.icontroller.eu
**Probed mailbox:** Smeba (mailbox_id=4)

## Message URL pattern (D-15)

Confirmed: `${URL_BASE}/messages/show/<icontroller_internal_id>` (or correct as observed).
The internal ID is found by: <how the caller resolves it — typically grep mailbox list rows for matching subject/sender>.

## Read current label state (D-16 idempotency)

**Selector:** `<exact CSS selector or XPath>`
**Read via:** `page.locator(...).textContent()` OR `page.evaluate(() => ...)` (specify which)
**Returns:** the displayed customer account name OR null when no label.
**Mapping to customer_account_id:** <how we go from displayed name to ID — JOIN against NXT? a data attribute on the element?>

## Open the label picker

**Trigger selector:** `<exact selector for the button/dropdown/etc>`
**Action:** `click()` / `hover()` / `keyboard.press("...")`.
**Wait for picker:** `page.waitForSelector(...)` — exact selector for the picker container's root.

## Search/select customer account

**Search input selector:** `<selector>`
**Type method:** `page.fill()` / `page.locator(...).pressSequentially()` / `evaluate` (specify; if shadow DOM use `evaluate` per CLAUDE.md).
**Result-row selector:** `<selector>` (with placeholder for matching against `customer_account_id` or `customer_name`).
**Click action:** `page.locator(...).first().click()` (or by-text variant).

## Save / Apply

**Selector:** `<selector>` OR "auto-saves on selection".
**Wait for confirmation:** `page.waitForSelector(...)` for the toast / for the picker to dismiss / for the message-detail to show the new label.

## Shadow DOM?

Yes / No. If yes, list which elements are inside shadow root and require `page.evaluate(...)` per CLAUDE.md.

## Edge cases observed

- Loading spinner during search? Wait selector / timeout?
- Empty search result handling? (e.g., "no matches" fallback — should we throw or skip_conflict?)
- Multi-account assignment? (per D-15/D-16 we assume single label per email — flag here if iController allows multi.)

## Failure modes worth screenshotting at runtime

List any UI states that should trigger `captureScreenshot` for debugging (e.g., error toasts, session-timeout banners).
```

**E) Decision:** if probe reveals iController has **no per-message label UI** (RESEARCH A3 false), STOP. Mark this task as failed. Update STATE.md noting Phase 56 is infeasible without product change. Else proceed.

**F)** **Idempotency check selector is mandatory** (D-16). If the probe finds no DOM that exposes current label state without opening the picker, document the workaround in SELECTORS.md (e.g., "open picker, read currently-selected option, close picker without saving") and flag for cross-check by 56-05 implementer.
  </action>
  <verify>
    <automated>test -f .planning/briefs/artifacts/debtor-email-label-probe/candidates.json && test -f .planning/briefs/artifacts/debtor-email-label-probe/SELECTORS.md && grep -q "Read current label state" .planning/briefs/artifacts/debtor-email-label-probe/SELECTORS.md && grep -q "Open the label picker" .planning/briefs/artifacts/debtor-email-label-probe/SELECTORS.md && grep -q "Save / Apply" .planning/briefs/artifacts/debtor-email-label-probe/SELECTORS.md</automated>
  </verify>
  <done>candidates.json captured; SELECTORS.md committed with all 4 mandatory selector sections; idempotency strategy documented</done>
  <acceptance_criteria>
    - File `.planning/briefs/artifacts/debtor-email-label-probe/candidates.json` exists with at least 1 element entry
    - File `.planning/briefs/artifacts/debtor-email-label-probe/SELECTORS.md` exists
    - SELECTORS.md contains a non-empty section for "Read current label state" (D-16)
    - SELECTORS.md contains a non-empty section for "Open the label picker"
    - SELECTORS.md contains a non-empty section for "Search/select customer account"
    - SELECTORS.md contains a non-empty section for "Save / Apply"
    - SELECTORS.md states explicit Yes/No on Shadow DOM presence
    - Each selector section provides a concrete `selector` string (not "TBD" or empty)
  </acceptance_criteria>
  <resume-signal>Type "label probe artifact ready" or describe blockers (including "iController has no label UI — phase infeasible")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator's machine → Browserless → iController prod | New session opens production iController; read-only intent but operator must enforce |
| Browserless → screenshot bucket | Screenshots may capture customer PII (names, emails on the message detail) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-03-01 | Tampering | Probe accidentally clicks Save/Apply | mitigate | probe-label-ui.ts is read-only-by-design (CONTEXT D-17, code comment); operator review of code before run; if accidental click happens, idempotency in 56-05 will eventually re-correct |
| T-56-03-02 | Information Disclosure | Screenshots contain customer PII | accept | Screenshots stay local (operator's box) or in `.planning/briefs/artifacts` which `.gitignore` excludes for PNG; SELECTORS.md does not embed images, only selector strings + behavior notes |
| T-56-03-03 | Spoofing | Probe runs against acceptance, selectors don't match production | mitigate | `ICONTROLLER_ENV=production` env required; Task 1 step A explicitly sets it; if production has different DOM, re-run is cheap |
| T-56-03-04 | Repudiation | Probe-derived selectors hand-guessed | mitigate | Task 1 step D mandates concrete selector strings (acceptance_criteria enforces non-empty sections); 56-05 cites SELECTORS.md as source of every selector in its task action |
</threat_model>

<verification>
- candidates.json + SELECTORS.md committed
- All 4 mandatory selector sections populated with concrete strings
- Idempotency strategy (D-16) documented
</verification>

<success_criteria>
56-05 (Wave 2 label-email-in-icontroller.ts implementation) can copy selectors from SELECTORS.md verbatim. No more guessing.
</success_criteria>

<output>
After completion, create `.planning/phases/56-icontroller-auto-labeling-van-accounts-aan-emails/56-03-SUMMARY.md` linking to the probe artifact directory.
</output>
