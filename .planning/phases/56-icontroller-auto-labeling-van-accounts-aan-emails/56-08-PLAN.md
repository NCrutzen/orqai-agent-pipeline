---
phase: 56
plan: 08
type: execute
wave: 5
depends_on: [56-05, 56-06, 56-07]
files_modified:
  - .planning/phases/56-icontroller-auto-labeling-van-accounts-aan-emails/56-08-FLIP-CHECKLIST.md
autonomous: false
requirements: [D-26, D-30]
must_haves:
  truths:
    - "Smeba has accumulated N>=50 human-verdicts in agent_runs (operator-driven approve/reject in dashboard)"
    - "Wilson CI-lo for Smeba >= 0.95 over the recent 50 verdicts"
    - "Operator has reviewed flip-cron shadow audit rows for at least 7 consecutive cron runs without anomaly"
    - "Operator has flipped LABELING_CRON_MUTATE=true in Vercel production env"
    - "Smeba labeling_settings.dry_run is now FALSE; subsequent inbound emails on Smeba are auto-labeled in iController"
    - "Other 4 mailboxes (Smeba-Fire, Sicli-Noord, Sicli-Sud, Berki) remain dry_run=true until their N=50 + CI-lo>=0.95 gate passes individually"
  artifacts:
    - path: ".planning/phases/56-icontroller-auto-labeling-van-accounts-aan-emails/56-08-FLIP-CHECKLIST.md"
      provides: "Per-mailbox rollout checklist with dates, N counts, CI-lo values, flip approvals/rejections"
      min_lines: 40
  key_links:
    - from: "Operator review of dashboard"
      to: "LABELING_CRON_MUTATE=true in Vercel"
      via: "manual env-var flip after N>=50 + CI-lo>=0.95 confirmed"
      pattern: "LABELING_CRON_MUTATE"
---

<objective>
Calendar-soft rollout gate. Days-to-weeks of waiting for Smeba to accumulate N>=50 verdicts in dry-run mode, operator review of shadow audit rows, then operator flips `LABELING_CRON_MUTATE=true` to enable mutation. Other 4 mailboxes follow as their N gates pass.

Mirrors Phase 60-07-FLIP-CHECKLIST.md pattern. Marked `autonomous: false` — entirely operator-driven.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-icontroller-auto-labeling-van-accounts-aan-emails/56-CONTEXT.md
@.planning/phases/56-icontroller-auto-labeling-van-accounts-aan-emails/56-04-SUMMARY.md
@.planning/phases/56-icontroller-auto-labeling-van-accounts-aan-emails/56-05-SUMMARY.md
@.planning/phases/56-icontroller-auto-labeling-van-accounts-aan-emails/56-06-SUMMARY.md
@.planning/phases/56-icontroller-auto-labeling-van-accounts-aan-emails/56-07-SUMMARY.md
@.planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-07-FLIP-CHECKLIST.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING — calendar-soft] Smeba canary review + flip LABELING_CRON_MUTATE</name>
  <what-built>56-04 + 56-05 + 56-06 + 56-07 are deployed. Smeba's `dry_run=true` (default) means: route writes email_labels with status='dry_run'; iController is NOT mutated. flip-cron runs daily in shadow mode; classifier_rule_evaluations accumulates `shadow_would_*` rows. Operator approves/rejects rows in the dashboard, accumulating human_verdicts in agent_runs.</what-built>
  <read_first>
    - .planning/phases/56-icontroller-auto-labeling-van-accounts-aan-emails/56-CONTEXT.md (D-24, D-25, D-26, D-30)
    - .planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-07-FLIP-CHECKLIST.md (analog from Phase 60)
    - web/lib/inngest/functions/labeling-flip-cron.ts (the cron the operator is monitoring)
  </read_first>
  <files>.planning/phases/56-icontroller-auto-labeling-van-accounts-aan-emails/56-08-FLIP-CHECKLIST.md</files>
  <action>
**1) Wait for Smeba to accumulate verdicts.** Operator visits `/automations/debtor-email-labeling` daily. For each Smeba row, click → drawer → review email body, LLM rationale (when method='llm_tiebreaker'), customer match → click Approve (correct) or Reject + paste corrected_account_id. Goal: N=50+ verdicts on Smeba. Expected timeline: days-to-weeks depending on inbound volume.

**2) Confirm Wilson CI-lo via SQL.** Operator runs:
```sql
select n, agree, ci_lo, action, evaluated_at
from public.classifier_rule_evaluations
 where swarm_type='debtor-email-labeling'
   and rule_key='mailbox_flip:smeba'
 order by evaluated_at desc limit 7;
```
Expect: at least 7 rows; latest row has `n>=50`, `ci_lo>=0.95`, `action='shadow_would_promote'`. The 7 rows confirm 7 consecutive cron runs without flip-flop or anomaly.

**3) Spot-check the rows being approved.** Operator picks 5 random Smeba `email_labels` rows where `human_verdict='approve'` and confirms in NXT directly that the customer_account_id is correct. (Anti-pattern guard: if operator was approving everything blindly, a 5-row spot-check catches it.)

**4) Cross-brand sanity (Pitfall 5).** Operator queries:
```sql
select count(*) from debtor.email_labels
 where source_mailbox='smeba'
   and customer_account_id is not null
   and nxt_database != 'smeba_db';
```
Expect: 0. (No Smeba emails resolved to a non-Smeba customer account.)

**5) Flip the env var.** Operator sets in Vercel production:
```
LABELING_CRON_MUTATE=true
```
Trigger a fresh Vercel deploy (the cron picks up env at function-cold-start; redeploy forces it).

**6) Manual cron invocation.** Operator triggers `labeling-flip-cron` once via Inngest dev server OR waits for the next 06:00 Amsterdam scheduled run. Then re-runs the SQL from step 2 — expect the latest row's `action='promoted'` AND `debtor.labeling_settings.dry_run=false where source_mailbox='smeba'`.

**7) First live label.** Operator watches the next inbound Smeba email. Dashboard row appears with `status='labeled'`, screenshot URLs populated. Operator opens iController in a separate tab and confirms the label is applied to that message.

**8) Document in 56-08-FLIP-CHECKLIST.md.** Use this template:

```markdown
# Phase 56 — Per-Mailbox Flip Checklist

## Smeba (mailbox_id=4) — canary

**Dry-run started:** <date 56-04 deployed>
**N=50 reached:** <date>
**Wilson CI-lo at flip:** <value>
**LABELING_CRON_MUTATE flipped:** <date + Vercel deploy commit>
**First live label confirmed:** <date + email_label.id + screenshot URLs>
**Spot-check (5 rows):** <ids> — all correct ✓
**Cross-brand sanity:** 0 leaks ✓

## Smeba-Fire (mailbox_id=?)

**Dry-run started:** <date>
**N=50 reached:** ⏳ pending
**Flipped:** ⬜
**Notes:** <e.g., low volume — expected 6 weeks>

## Sicli-Noord
... (same shape)

## Sicli-Sud
... (same shape)

## Berki
... (same shape)

## Demotion incidents
| Date | Mailbox | n | ci_lo | Action taken |
|------|---------|---|-------|--------------|
| (none yet) |
```

**9) Per-mailbox follow-on (one row per other mailbox).** As each remaining mailbox accumulates N=50 + CI-lo>=0.95, that mailbox's `labeling_settings.dry_run` flips automatically because `LABELING_CRON_MUTATE=true` is now global. Operator updates the checklist as each flip happens.

**10) FireControl is OUT OF SCOPE.** Per CONTEXT.md D-30: deferred until iController access for FireControl is confirmed.
  </action>
  <verify>
    <automated>test -f .planning/phases/56-icontroller-auto-labeling-van-accounts-aan-emails/56-08-FLIP-CHECKLIST.md && grep -q "Smeba (mailbox_id=4)" .planning/phases/56-icontroller-auto-labeling-van-accounts-aan-emails/56-08-FLIP-CHECKLIST.md && grep -q "LABELING_CRON_MUTATE flipped" .planning/phases/56-icontroller-auto-labeling-van-accounts-aan-emails/56-08-FLIP-CHECKLIST.md</automated>
  </verify>
  <done>56-08-FLIP-CHECKLIST.md committed; Smeba section has all 8 fields filled; first live label confirmed in iController</done>
  <acceptance_criteria>
    - File `56-08-FLIP-CHECKLIST.md` exists
    - Smeba section has non-empty values for: Dry-run started, N=50 reached date, Wilson CI-lo at flip (>= 0.95), LABELING_CRON_MUTATE flipped date, First live label confirmed
    - Spot-check field shows 5 row ids with confirmation
    - Cross-brand sanity field shows "0 leaks"
    - Other 4 mailboxes (Smeba-Fire, Sicli-Noord, Sicli-Sud, Berki) each have a section with at least "Dry-run started" populated
    - Demotion incidents table exists (may be empty)
  </acceptance_criteria>
  <resume-signal>Type "smeba flipped" with the email_label.id of the first live-labeled email, OR describe blockers (e.g., "spot-check found 2/5 wrong → pausing flip, opening Phase 57 follow-up")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Vercel env | LABELING_CRON_MUTATE=true is the irreversible flip from shadow to mutate |
| flip-cron mutate → iController auto-label | Each labeled email writes to a customer's record in NXT (via iController) — auditable but not undoable from this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-08-01 | Tampering | Operator flips MUTATE before N=50 | mitigate | Task acceptance criteria require Wilson CI-lo at flip be >= 0.95 (math gate). Independent: cron itself enforces N>=50 even if env is set. |
| T-56-08-02 | Tampering | Operator approves blindly to hit N=50 faster | mitigate | Step 3 spot-check (5 random rows) catches systematic blind-approve; if found wrong, operator pauses + opens Phase 57 follow-up |
| T-56-08-03 | Information Disclosure | Live labels expose customer relationships in iController audit log | accept | iController already logs operator-driven labeling; this just adds machine-driven entries with the same audit trail |
| T-56-08-04 | Tampering | Demotion silently flips back to dry-run mid-flow | mitigate | Step 8 demotion incidents table makes it visible; flip-cron writes console.warn (Slack/log pickup); Phase 56 acceptable, Phase 57 considers stricter alerting |
</threat_model>

<verification>
- 56-08-FLIP-CHECKLIST.md committed
- Smeba flipped, first live label confirmed
- Other 4 mailboxes tracked
</verification>

<success_criteria>
Phase 56 ships. Smeba auto-labels inbound debtor emails to NXT customer accounts. Other brands wait their turn. The data-driven Wilson gate is the only thing deciding when each brand goes live — no human "I think we're ready" decisions per-mailbox.
</success_criteria>

<output>
After completion, create `.planning/phases/56-icontroller-auto-labeling-van-accounts-aan-emails/56-08-SUMMARY.md` linking to the FLIP-CHECKLIST + listing each mailbox's flip date.
</output>
