---
phase: 40-kb-memory-lifecycle
plan: 05
type: execute
wave: 2
depends_on: [40-01, 40-02, 40-03, 40-04]
files_modified:
  - orq-agent/SKILL.md
  - orq-agent/commands/help.md
autonomous: true
requirements: [KBM-01, KBM-02, KBM-03, KBM-04, KBM-05]
must_haves:
  truths:
    - "SKILL.md Directory Structure lists orq-agent/commands/kb/resources/ with the 3 files"
    - "SKILL.md Subagents table lists memory-store-generator under Phase 40"
    - "SKILL.md adds Phase 40 (KB & Memory Lifecycle) H3 block with KBM-01..05 coverage"
    - "help.md /orq-agent:kb flag summary expanded with --mode kb|memory + --retrieval-threshold"
  artifacts:
    - path: orq-agent/SKILL.md
      provides: "Suite index wired for Phase 40"
      contains: "Phase 40, memory-store-generator, kb/resources, KBM-01"
    - path: orq-agent/commands/help.md
      provides: "Help surface updated with --mode kb|memory"
      contains: "--mode kb|memory, --retrieval-threshold"
  key_links:
    - from: orq-agent/SKILL.md
      to: orq-agent/agents/memory-store-generator.md
      via: "Subagents table row (Phase 40 section)"
      pattern: "memory-store-generator"
    - from: orq-agent/commands/help.md
      to: orq-agent/commands/kb.md
      via: "Flag summary for /orq-agent:kb"
      pattern: "--mode kb\\|memory"
---

<objective>
Wire Phase 40 surface into orq-agent/SKILL.md (index) and orq-agent/commands/help.md (discovery) following the V3.0 index-wiring recipe (Phases 36/37/38/39).

Purpose: Without index-wiring, a user running `/orq-agent:help` has no signal that `--mode memory` exists, and SKILL.md's Directory Structure + Subagents tables miss the new Phase 40 surface.
Output: SKILL.md + help.md updated. Protected pipelines (3/3 SHA-256) untouched.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-kb-memory-lifecycle/40-CONTEXT.md
@.planning/phases/40-kb-memory-lifecycle/40-01-PLAN.md
@.planning/phases/40-kb-memory-lifecycle/40-02-PLAN.md
@.planning/phases/40-kb-memory-lifecycle/40-03-PLAN.md
@.planning/phases/40-kb-memory-lifecycle/40-04-PLAN.md

@orq-agent/SKILL.md
@orq-agent/commands/help.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SKILL.md + help.md for Phase 40 surface</name>
  <files>orq-agent/SKILL.md, orq-agent/commands/help.md</files>
  <action>
Read orq-agent/SKILL.md and orq-agent/commands/help.md first. Apply the V3.0 index-wiring recipe (precedent: Phase 37 P04, Phase 38 P03, Phase 39 P04 — touch exactly these 2 files, never touch orq-agent.md/prompt.md/architect.md).

### SKILL.md edits

1. **Directory Structure block:** Under `commands/`, add the new `kb/resources/` subdir right after the existing `kb.md` entry (which is currently at line ~62 of SKILL.md). Insert:

       kb/
         resources/
           chunking-strategies.md  # Phase 40: sentence vs recursive decision rules (KBM-03)
           kb-vs-memory.md         # Phase 40: KB-vs-Memory decision rule + anti-patterns (KBM-04)
           retrieval-test-template.md  # Phase 40: retrieval test queries + pass criteria (KBM-01)

2. **Subagents tables:** Add a new H3 section immediately after `### Phase 9 (Guardrails)` (or wherever the last existing subagent section sits). Heading and row:

       ### Phase 40 (KB & Memory Lifecycle)

       | Agent | File | Purpose |
       |-------|------|---------|
       | Memory Store Generator | `agents/memory-store-generator.md` | Creates Orq.ai memory stores with descriptive keys (session_history / user_preferences / conversation_context), wires agents with memory instructions (`settings.memory_stores` + system prompt injection), and runs a read/write/recall round-trip test with cleanup before handoff (KBM-05) |

3. **Commands table:** The existing `/orq-agent:kb` row under "V2.0 Commands (Phase 5+)" stays. Update its `Purpose` column to reflect Phase 40 capabilities. Change from:

   > Manage KBs -- generate content, provision, upload files

   To:

   > Manage KBs + memory stores -- generate content, provision, upload, retrieval-quality test (KBM-01), embedding activation check (KBM-02), chunking picker (KBM-03), KB-vs-Memory gate (KBM-04); `--mode memory` dispatches to memory-store-generator (KBM-05)

4. **Insert Phase 40 H3 block** immediately after the existing `### Phase 39 (Dataset Generator Enhancements)` block. Heading and content:

       ### Phase 40 (KB & Memory Lifecycle)

       | Command | File | Tier Required | Purpose |
       |---------|------|---------------|---------|
       | `/orq-agent:kb` | `commands/kb.md` | deploy+ | Enhanced KB command — embedding model activation check (KBM-02), content-type-driven chunking picker (KBM-03), retrieval quality test with configurable threshold (KBM-01), KB-vs-Memory decision rule gate (KBM-04), and `--mode memory` dispatch to memory-store-generator (KBM-05) |

       **`/orq-agent:kb` Phase 40 requirement coverage:**

       - KBM-01 — Retrieval quality test with 5-10 LLM-synthesized sample queries after chunking; refuses deployment wire-up if pass rate < 70% (configurable via `--retrieval-threshold <N>`); see `commands/kb.md` Step 7.6 and `commands/kb/resources/retrieval-test-template.md`.
       - KBM-02 — Embedding model activation check via MCP `list_models --type embedding` with REST fallback; surfaces remediation (activate in AI Router → Models → Embeddings) before any `POST /v2/knowledge` call; see `commands/kb.md` Step 7.0.
       - KBM-03 — Content-type detection (prose vs structured) drives chunking strategy choice (sentence 512/50 vs recursive 1024/100); recorded in KB metadata + emitted by kb-generator in `manifest.json`; see `commands/kb/resources/chunking-strategies.md`.
       - KBM-04 — Lint-anchored KB-vs-Memory decision rule block ("static reference data" vs "dynamic user context") present verbatim in kb.md, memory-store-generator.md, and `commands/kb/resources/kb-vs-memory.md`; blocked patterns (memory for docs/FAQs, KB for conversation context) trigger STOP + redirect.
       - KBM-05 — New `agents/memory-store-generator.md` subagent creates memory stores, wires agents with `settings.memory_stores` + system prompt instruction, runs read/write/recall round-trip test with `test_write_<uuid>` value cleanup; invoked via `/orq-agent:kb --mode memory`.

       Resource docs under `orq-agent/commands/kb/resources/` (chunking-strategies, kb-vs-memory, retrieval-test-template) are consumed only by `commands/kb.md`, `agents/kb-generator.md`, and `agents/memory-store-generator.md` (all under the kb skill umbrella — single-consumer per Resources Policy below).

5. **Resources Policy "Migration status" paragraph:** Append Phase 40 entry after the Phase 39 sentence. Insert:

   > Phase 40 adds a fourth per-skill resources directory at `orq-agent/commands/kb/resources/` (3 files: chunking-strategies, kb-vs-memory, retrieval-test-template — consumed only by `commands/kb.md`, `agents/kb-generator.md`, and `agents/memory-store-generator.md` under the kb skill umbrella).

6. **Done When checklist:** Update the subagent count from `17` to `18` (new memory-store-generator adds one):
   - [ ] All 18 subagents under `orq-agent/agents/` match the subagents listed in the body below

### help.md edits

1. **Commands list block** (the code block shown by Step 2: Display Help): Update the `/orq-agent:kb` line to reflect the new flags. Change from:

       /orq-agent:kb                KB management (deploy+ tier)

   To:

       /orq-agent:kb                KB + memory management (deploy+ tier)
                                    (--mode kb|memory, --retrieval-threshold <N>)

   Use the Phase 39 `/orq-agent:datasets` continuation-line format for visual consistency (already established pattern in help.md).

2. **No other changes to help.md.** The banner, Constraints, When to use, etc. all stay intact.

### Constraints (DO NOT violate)

- Do NOT touch orq-agent/commands/orq-agent.md, orq-agent/commands/prompt.md, orq-agent/commands/architect.md (3 protected pipelines — SHA-256 guarded).
- Preserve all existing tables, rows, and text verbatim. Edits are ADDITIVE or targeted row-updates only.
- The V2.0 Commands table stays where it is; new Phase 40 H3 block goes AFTER Phase 39 block (chronological order, matching Phase 36/37/38/39 precedent).
- Subagent count in Done When checklist must update from 17 → 18.

### Verify immediately after write

    bash orq-agent/scripts/lint-skills.sh                    # full suite exit 0
    bash orq-agent/scripts/check-protected-pipelines.sh      # 3/3 SHA-256 match
    grep -q "Phase 40 (KB & Memory Lifecycle)" orq-agent/SKILL.md
    grep -q "memory-store-generator" orq-agent/SKILL.md
    grep -q "kb/resources" orq-agent/SKILL.md
    grep -q "KBM-01" orq-agent/SKILL.md
    grep -q "KBM-05" orq-agent/SKILL.md
    grep -q "All 18 subagents" orq-agent/SKILL.md
    grep -q -- "--mode kb|memory" orq-agent/commands/help.md
    grep -q -- "--retrieval-threshold" orq-agent/commands/help.md
  </action>
  <verify>
    <automated>bash orq-agent/scripts/lint-skills.sh &amp;&amp; bash orq-agent/scripts/check-protected-pipelines.sh &amp;&amp; grep -q "Phase 40 (KB & Memory Lifecycle)" orq-agent/SKILL.md &amp;&amp; grep -q "memory-store-generator" orq-agent/SKILL.md &amp;&amp; grep -q "kb/resources" orq-agent/SKILL.md &amp;&amp; grep -q "KBM-01" orq-agent/SKILL.md &amp;&amp; grep -q "KBM-05" orq-agent/SKILL.md &amp;&amp; grep -q "All 18 subagents" orq-agent/SKILL.md &amp;&amp; grep -q -- "--mode kb|memory" orq-agent/commands/help.md &amp;&amp; grep -q -- "--retrieval-threshold" orq-agent/commands/help.md</automated>
  </verify>
  <done>SKILL.md contains Phase 40 H3 block with KBM-01..05 coverage bullets, Phase 40 (KB & Memory Lifecycle) subagent section with memory-store-generator row, kb/resources/ directory listing, updated /orq-agent:kb Purpose cell, Resources Policy migration paragraph updated, Done When subagent count 17→18. help.md kb line expanded with --mode kb|memory and --retrieval-threshold flags. Full-suite lint green; protected pipelines 3/3.</done>
</task>

</tasks>

<verification>
- Full-suite lint exit 0
- Protected pipelines SHA-256 3/3 (no touches to 3 protected files)
- All 9 grep anchors pass
</verification>

<success_criteria>
- SKILL.md + help.md wired for Phase 40 following V3.0 recipe
- Subagent count updated 17 → 18
- Single-consumer resources registered in Resources Policy paragraph
</success_criteria>

<output>
After completion, create `.planning/phases/40-kb-memory-lifecycle/40-05-SUMMARY.md`.
</output>
