---
phase: 82.7.1
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-1-widget.tsx
autonomous: true
requirements: [E-04]
must_haves:
  truths:
    - "Submit override button fill is brand-secondary lime (matches Confirm primary in StageFeedbackPanel) — not amber"
    - "Discard changes is a ghost button (transparent fill, brand-secondary outline) with a trailing Esc keychip"
    - "Action row sits below a var(--space-4) top padding under the REG/CAP radio cards"
    - "⌘⏎ keychip is preserved on the Submit button per D-09"
    - "REG/CAP radio cards themselves are untouched (D-11)"
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-1-widget.tsx"
      provides: "Polished Submit (lime fill) + Discard (ghost) buttons with var(--space-4) action-row padding"
      contains: "var(--v7-lime)"
  key_links:
    - from: "stage-1-widget.tsx Submit button"
      to: "StageFeedbackPanel Confirm rhythm"
      via: "lime fill + pill radius + mono font"
      pattern: "background:\\s*[\"'`]var\\(--v7-lime\\)"
    - from: "stage-1-widget.tsx Discard button"
      to: "StageFeedbackPanel Save ghost rhythm"
      via: "transparent fill + brand-secondary outline"
      pattern: "border:.*var\\(--v7-brand-secondary\\)"
---

<objective>
Polish the Stage 1 override-form button rhythm: Submit override goes from
amber → brand-secondary lime fill (matches Confirm/Approve precedent from
82.7-01/02 — amber reads as a warning state, not a commit action). Discard
changes upgrades from a link to a real ghost button (transparent + brand-
secondary outline) with an `Esc` keychip. Action row gets `var(--space-4)`
top padding under the REG/CAP radio cards.

Purpose: Close E-04 (override form button row needs visual polish).
Output: Stage 1 override form action row matches StageFeedbackPanel rhythm.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/82.7.1-detail-pane-uat-followups/82.7.1-CONTEXT.md
@.planning/phases/82.7.1-detail-pane-uat-followups/82.7.1-PATTERNS.md
@.planning/phases/82.7-detail-pane-post-approve-polish/82.7-01-SUMMARY.md

<interfaces>
Template source — web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx:
- L188-197 — container shell (var(--space-4) padding, panel-2 bg, border, sm radius)
- L249-256 — button row (space-between, gap var(--space-2))
- L259-286 — Save button (ghost / outline brand-secondary) — Discard template
- L288-316 — Confirm button (lime fill, color var(--v7-bg), weight 600) — Submit template

Target file — web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-1-widget.tsx
(operator-facing Stage 1 override widget with REG/CAP radio cards + Submit/Discard form).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply lime-fill Submit + ghost Discard + var(--space-4) top padding to override form</name>
  <files>web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-1-widget.tsx</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-1-widget.tsx (entire file — locate the Submit + Discard form buttons and the REG/CAP radio cards)
    - web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx (L249-317 — exact button templates to mirror)
    - .planning/phases/82.7.1-detail-pane-uat-followups/82.7.1-PATTERNS.md (D-09/D-10/D-11 section, lines 480-608)
    - .planning/phases/82.7-detail-pane-post-approve-polish/82.7-01-SUMMARY.md (StageFeedbackPanel button rhythm origin)
  </read_first>
  <action>
**Locate the action row first.** In `web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-1-widget.tsx`, find the Submit and Discard controls inside the override form (the ones rendered under the REG/CAP radio cards — `eval-type-radio.tsx` is the radio card component, look for its consumer in stage-1-widget). Do NOT touch the radio cards themselves (D-11).

**D-09 — Submit override → lime fill, keep ⌘⏎ keychip:**

Swap the current Submit button's inline `style` block to mirror StageFeedbackPanel's Confirm (L288-316). The exact style block:

```typescript
style={{
  display: "inline-flex",
  alignItems: "center",
  gap: 6,
  padding: "6px 14px",
  background: "var(--v7-lime)",
  border: "1px solid var(--v7-lime)",
  borderRadius: "var(--v7-radius-pill)",
  color: "var(--v7-bg)",
  cursor: disabled ? "not-allowed" : "pointer",
  fontFamily: "var(--font-mono)",
  fontWeight: 600,
  fontSize: 12,
  opacity: disabled ? 0.6 : 1,
}}
```

(Use the actual disabled-state variable in the file — `disabled`, `inFlight`, `submitting`, whatever the existing code references. Mirror the file's existing pattern; don't invent new state.)

Preserve the existing `⌘⏎` keychip on the right edge — if the current button does not render one, ADD a trailing `<kbd>` with the same styling as Plan 01's per-stage Submit:

```typescript
<kbd
  style={{
    fontFamily: "var(--font-mono)",
    fontSize: 10,
    opacity: 0.7,
    marginLeft: 4,
  }}
>
  ⌘⏎
</kbd>
```

Label remains "Submit override" (D-02 locked).

**D-10 — Discard changes → ghost button + Esc keychip:**

Swap the current Discard control (whether currently a `<button>` link or styled element) to mirror StageFeedbackPanel's Save (L259-286). Inline `style`:

```typescript
style={{
  display: "inline-flex",
  alignItems: "center",
  gap: 6,
  padding: "6px 14px",
  background: "transparent",
  border: "1px solid var(--v7-brand-secondary)",
  borderRadius: "var(--v7-radius-pill)",
  color: "var(--v7-brand-secondary)",
  cursor: "pointer",
  fontFamily: "var(--font-mono)",
  fontSize: 12,
}}
```

Label: "Discard changes". Trailing affordance: `<kbd>Esc</kbd>` styled identically to the `⌘⏎` keychip above (mono, 10px, opacity 0.7, marginLeft 4).

Keep whatever `onClick` / handler the existing Discard control uses — this is pure visual polish, no behavior change.

**D-11 — var(--space-4) top padding above the action row:**

Wrap the action row (the container holding Submit + Discard) in an outer `<div>` with `style={{ paddingTop: "var(--space-4)" }}`, OR add `paddingTop: "var(--space-4)"` to the existing flex container's inline style if one already exists. Pick the lower-churn option based on the current structure.

If the existing action row already uses `marginTop: "var(--space-3)"` or similar, REPLACE it with `paddingTop: "var(--space-4)"` (D-11 explicit — one step larger because the row follows radio cards, not a textarea).

**Do NOT touch the REG/CAP radio cards** (`eval-type-radio.tsx` or wherever rendered). D-11: "REG / CAP cards themselves untouched."

**No keyboard-binding changes.** This plan is presentation-only. The existing `⌘⏎` and `Esc` key handlers (if any) stay; the keychips are visual affordances reflecting them.

  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | grep "stage-1-widget\\.tsx" | head -10; test $? -ne 0 || echo "TYPE_ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "background:\\s*[\"'\\\`]var\\(--v7-lime\\)" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/components/stage-1-widget.tsx` returns ≥1 match (Submit lime fill)
    - `grep -nE "border:.*var\\(--v7-brand-secondary\\)" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/components/stage-1-widget.tsx` returns ≥1 match (Discard ghost outline)
    - `grep -nE "paddingTop:.*var\\(--space-4\\)" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/components/stage-1-widget.tsx` returns ≥1 match (D-11 action-row padding)
    - `grep -E "⌘⏎|Esc" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/components/stage-1-widget.tsx | wc -l` returns ≥2 (both keychips present)
    - `grep -nE "background:\\s*[\"'\\\`]var\\(--v7-amber\\)" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/components/stage-1-widget.tsx` returns ZERO matches on the Submit button (amber fill removed) — narrow to the Submit context by visual review if grep is too broad
    - `cd web && npx tsc --noEmit` exits 0
    - REG/CAP radio cards untouched: `git diff web/app/\(dashboard\)/automations/\[swarm\]/stage-1/components/eval-type-radio.tsx` returns empty (file not modified)
  </acceptance_criteria>
  <done>
    Submit override is lime-filled with ⌘⏎ keychip; Discard changes is a ghost button with Esc keychip; action row has var(--space-4) top padding; REG/CAP radio cards untouched; tsc clean.
  </done>
</task>

</tasks>

<verification>
- `cd web && npm run build` exits 0
- Existing Stage 1 widget tests pass: `cd web && npx vitest run app/\(dashboard\)/automations/\[swarm\]/stage-1/__tests__/stage-1-widget.test.tsx` returns green
</verification>

<success_criteria>
- E-04 closed: override form button rhythm matches StageFeedbackPanel precedent (lime primary, ghost secondary, var(--space-4) action-row padding).
- D-09/D-10/D-11 satisfied; eval-type-radio component byte-identical.
</success_criteria>

<output>
After completion, create `.planning/phases/82.7.1-detail-pane-uat-followups/82.7.1-04-SUMMARY.md`
</output>
