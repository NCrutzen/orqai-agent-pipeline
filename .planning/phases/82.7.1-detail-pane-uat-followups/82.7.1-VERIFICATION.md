---
phase: 82.7.1-detail-pane-uat-followups
verified: 2026-05-18T00:00:00Z
status: human_needed
score: 15/15 must-haves verified (HEAD) — operator UAT pending
re_verification:
  previous_status: none
  previous_score: n/a
gaps: []
human_verification:
  - test: "Stage 0 dirty card — click per-stage Submit override button"
    expected: "Selected row leaves Stage 0 Bulk Review (override committed via overrideSubmit event → Stage 0 widget listener)"
    why_human: "Requires running Stage 0 Bulk Review surface with a real Stage-0 dirty row; event listener wiring on Stage 0 widget cannot be exercised by grep alone"
  - test: "Stage 0/1 Approve → 150ms opacity fade-out before unmount"
    expected: "Row visibly fades from opacity 1 → 0 over ~150ms, then disappears; no toast"
    why_human: "Visual timing (CSS transition smoothness) is operator-perceptible only"
  - test: "Brand swatch hover on PredictedRow → tooltip"
    expected: "After Radix default hover delay (~700ms), tooltip shows 'Smeba' / 'Smeba-Fire' / 'Berki' / 'Sicli-Noord' / 'Sicli-Sud' depending on row"
    why_human: "Tooltip render is a browser-only side effect (Radix portals to body)"
  - test: "Stage 1 override form — confirm lime Submit + ghost Discard rendering"
    expected: "Submit button has lime fill with ⌘⏎ keychip; Discard is transparent with brand-secondary outline and Esc keychip; var(--space-4) gap above the action row under REG/CAP cards"
    why_human: "Visual polish is operator-perceptible only; tests assert tokens but not the rendered colour fidelity"
  - test: "Stage 0 / Stage 2 row-list — em-dash placeholder chip on rows with null/empty stage_state"
    expected: "Chip column always shows a chip-shaped element; muted '—' chip appears on pre-cutover historical rows; row rhythm preserved"
    why_human: "Requires a tenant with historical rows where stage_state is null; renderer change is verified, data presence is not"
deployment_risk:
  - "Working tree at verification time has UNCOMMITTED reverts of Plans 01 and 03 across 5 files (detail-pane.tsx, pipeline-flow.tsx, stage-step.tsx, predicted-row.tsx, brand-color.ts). HEAD is correct (commits b2a71d7, 0f6f454, 499dde5 land all of D-01..D-03, D-06..D-08), but if the working tree is committed/pushed without first resetting these reverts, Plans 01 and 03 will regress on production. Recommend: `git checkout HEAD -- <those 5 paths>` before any further commit, OR explicitly inspect the diff to confirm the reverts are not intentional."
---

# Phase 82.7.1: Detail-pane UAT follow-ups — Verification Report

**Phase Goal:** Close 5 UX defects (E-01..E-05 / D-01..D-15) surfaced by Phase 82.7 operator UAT on the Stage 0/Stage 1 Bulk Review detail-pane surface.
**Verified:** 2026-05-18 (HEAD at commit `d343fdb`)
**Status:** human_needed
**Re-verification:** No — initial verification
**Type-check:** `npx tsc --noEmit` from `/web` → clean (zero diagnostics)

## Goal Achievement

### D-ID Scorecard (15/15 verified at HEAD)

| D-ID | Plan | Status | Evidence (HEAD) |
| ---- | ---- | ------ | --------------- |
| D-01 | 01 | PASS | Per-stage Submit button rendered in dirty branch: `stage-step.tsx:244-280` (testid `stage-${n}-submit-override` at L262; label "Submit override" at L278) |
| D-02 | 01 | PASS | Submit dispatches `KEYBOARD_EVENTS.overrideSubmit` (no direct POST): `detail-pane.tsx:329-331` (onSubmitDirty useCallback); StageStep button onClick → `onSubmitDirty?.()` at `stage-step.tsx:260`. Single-write path preserved (same event the footer fires at `detail-pane.tsx:507`). |
| D-03 | 01 | PASS | Footer activeStage===1 scoping intact: 2 occurrences of `activeStage === 1` in detail-pane.tsx (unchanged from Phase 82.6 D-02b). Existing per-stage cancel-override link (Phase 82.7-03) untouched and remains the Discard affordance for non-Stage-1 dirty cards. |
| D-04 | 02 | PASS | 150ms opacity fade: `row-list.tsx:135` (`transition: "opacity 150ms ease-out"`); `row-list.tsx:133` (`opacity: isPending ? 0 : 1`); deferred unmount via `_unmount` local Set with `setTimeout(180ms)` at `row-list.tsx:47-72`. |
| D-05 | 02 | PASS | Consistency by construction: filter operates on `_unmount` populated from ANY entry into `pendingRemovalIds` (`row-list.tsx:47-72`). `markPendingRemovalAndAdvance` (Approve, Phase 82.7-04) and override flows both write to `pendingRemovalIds` upstream → both inherit fade with zero special-casing. No `toast`/`sonner` reference added. |
| D-06 | 03 | PASS | Radix Tooltip wraps brand swatch: `predicted-row.tsx:102-120` (`TooltipProvider` → `Tooltip` → `TooltipTrigger asChild` swatch span → `TooltipContent` rendering brand name). |
| D-07 | 03 | PASS | `brandDisplayName()` exported from `web/lib/swarms/brand-color.ts:45` next to `brandColorToken()` (single source); `BRAND_DISPLAY_NAME` map covers all 5 Benelux brands at `brand-color.ts:35`. Fallback `"Unknown brand"`. |
| D-08 | 03 | PASS | No `brandColorToken` import in `_shell/row-list.tsx` (grep returned zero) → single source of truth preserved. `PredictedRow` IS the noise-view row component per CONTEXT — one edit covers both surfaces. |
| D-09 | 04 | PASS | Submit lime fill: `_shell/components/stage-1-widget.tsx:399-400` (`background: "var(--v7-lime)"` + matching border); `⌘⏎` keychip at L419. Zero `var(--v7-amber)` references in file. |
| D-10 | 04 | PASS | Discard ghost outline: `_shell/components/stage-1-widget.tsx:369-371` (`border: "1px solid var(--v7-brand-secondary)"` + matching color + transparent background); `Esc` keychip at L387. |
| D-11 | 04 | PASS | Action-row padding: `_shell/components/stage-1-widget.tsx:357` (`paddingTop: "var(--space-4)"`). REG/CAP cards (`eval-type-radio.tsx`) untouched — confirmed by `git diff` (Plan 04 SUMMARY). |
| D-12 | 05 | PASS | Em-dash placeholder rendered when label null/empty/whitespace: `row-list.tsx:193-225` (`isEmpty` trimmed-length branch); `data-empty="true"` at L205; em-dash literal `"—"` at L224. |
| D-13 | 05 | PASS | Stage 0/1/2 mapper assessment documented in `82.7.1-05-SUMMARY.md` (root-cause table: Stage 0 + Stage 2 brittle, Stage 1 safe). |
| D-14 | 05 | PASS | Renderer fix (a/c) shipped at row-list.tsx StageBadge; data-side root cause (b) deferral path documented in SUMMARY ("if Stage 1 still shows em-dash post-deploy → separate phase 82.7.2 candidate"). No mapper mutation. |
| D-15 | 05 | PASS | Chip shape preserved: `row-list.tsx:209-219` (same `padding: "2px 8px"`, `borderRadius: "var(--v7-radius-pill)"`, font tokens in both branches); empty branch differs only in border (var(--v7-muted)), background (transparent), color (muted), opacity (0.7), aria-label ("No category"). |

**Score:** 15/15 D-IDs verified at HEAD.

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx` | Per-stage Submit button in dirty branch | PASS | onSubmitDirty optional prop (L36); button rendered at L244-280; data-testid `stage-${n}-submit-override` |
| `web/app/(dashboard)/automations/[swarm]/stage-1/components/pipeline-flow.tsx` | onSubmitDirty prop threaded | PASS | Prop declared L65; destructured L79; threaded to StageStep at L123 |
| `web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx` | onSubmitDirty useCallback dispatching overrideSubmit event | PASS | useCallback at L329-331; threaded to PipelineFlow at L671; 2 occurrences of `KEYBOARD_EVENTS.overrideSubmit` (callback + existing footer) |
| `web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx` | Fade-then-unmount + em-dash placeholder | PASS | `_unmount` local Set (L47); setTimeout(180) deferred filter (L52-66); opacity/transition on li (L133-135); StageBadge `isEmpty` branch (L193-225) |
| `web/lib/swarms/brand-color.ts` | brandDisplayName() helper colocated with brandColorToken() | PASS | BRAND_DISPLAY_NAME map (L35); brandDisplayName export (L45); 5 Benelux brand codes only (no iccafe) |
| `web/app/(dashboard)/automations/[swarm]/stage-1/components/predicted-row.tsx` | Radix Tooltip on brand swatch | PASS | TooltipProvider/Trigger/Content wraps swatch span L102-120; role="img" + aria-label="Brand: {name}" at L107 |
| `web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx` | Polished Submit (lime) + Discard (ghost) buttons + space-4 padding | PASS | paddingTop var(--space-4) L357; Discard ghost L369-371 + Esc keychip L387; Submit lime L399-400 + ⌘⏎ keychip L419 |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| stage-step.tsx Submit button | pipeline-flow.tsx onSubmitDirty prop | `onSubmitDirty?.()` optional-chain call | WIRED | stage-step.tsx:260 onClick → onSubmitDirty?.() |
| detail-pane.tsx onSubmitDirty callback | Stage widget overrideSubmit listener | `window.dispatchEvent(new Event(KEYBOARD_EVENTS.overrideSubmit))` | WIRED | detail-pane.tsx:329-331 (same event Stage1Widget already listens for, confirmed by L507 footer existing dispatch — both fire identical event into shared single-write contract) |
| row-list.tsx `<li>` style | selection-context pendingRemovalIds | `opacity: pendingRemovalIds.has(r.id) ? 0 : 1` | WIRED | row-list.tsx:104 (isPending), 133 (opacity branch) |
| row-list.tsx useEffect | local `_unmount` Set | `setTimeout(180ms)` scheduling deferred filter | WIRED | row-list.tsx:52-66 |
| predicted-row.tsx swatch | brand-color.ts brandDisplayName | import + call in TooltipContent | WIRED | predicted-row.tsx:16 import; L107 + L118 usage |
| stage-1-widget.tsx Submit button | StageFeedbackPanel Confirm rhythm | lime fill + pill radius + mono font | WIRED | _shell/components/stage-1-widget.tsx:399-400 |
| stage-1-widget.tsx Discard button | StageFeedbackPanel Save ghost rhythm | transparent fill + brand-secondary outline | WIRED | _shell/components/stage-1-widget.tsx:369-371 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| stage-step.tsx Submit button | `onSubmitDirty?.()` | detail-pane.tsx callback dispatches window event consumed by Stage1Widget (existing footer-Submit pipeline; W6 single-write) | Yes — reuses production override path already proven in Stage 1 footer | FLOWING (logic shared with footer Submit). Live dispatch on Stages 0/2/3/4 widgets pending operator UAT. |
| row-list.tsx pendingRemovalIds | Set<string> | selection-context.tsx populated by markPendingRemovalAndAdvance (Approve, Phase 82.7-04) + Submit-override flow | Yes — both writers exist in production | FLOWING |
| predicted-row.tsx brand swatch | row.entity_brand | upstream PredictedRow data (Stage 1 page mapper) | Yes — entity_brand is a registry column already rendered (color dot has been live since Phase 69) | FLOWING |
| row-list.tsx StageBadge label | stage_badge.label from Row | Stage 0/1/2 page mappers (Stage 0 + Stage 2 pass r.stage_state directly with no fallback per Plan 05 D-13 investigation) | Sometimes null (D-13 root cause); renderer now handles gracefully | FLOWING with placeholder fallback |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| TypeScript type-check on web/ | `cd web && npx tsc --noEmit` | zero diagnostics | PASS |
| Plan 04 unit tests (per SUMMARY) | `npx vitest run app/(dashboard)/automations/[swarm]/stage-1/__tests__/stage-1-widget.test.tsx` | 4/4 passing (recorded in Plan 04 SUMMARY) | PASS (recorded) |
| Visual fade timing | n/a | requires browser | SKIP → human verification |
| Brand-swatch tooltip render | n/a | requires browser (Radix portal) | SKIP → human verification |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| E-01 | 01 | Stage 0 override has no submit path | SATISFIED | Per-stage Submit affordance on Stage 0/2/3/4 dirty cards (stage-step.tsx:244-280) wired through onSubmitDirty → overrideSubmit event |
| E-02 | 02 | Approve is silent — needs in-place visual feedback | SATISFIED | 150ms opacity fade on pendingRemoval rows (row-list.tsx:133-135) |
| E-03 | 03 | Brand-color swatch undocumented | SATISFIED | Radix Tooltip on brand swatch showing brand display name (predicted-row.tsx:102-120) |
| E-04 | 04 | Override form button row needs polish | SATISFIED | Lime Submit + ghost Discard + var(--space-4) padding (_shell/components/stage-1-widget.tsx:357,369-371,399-400) |
| E-05 | 05 | Some rows render without a category/safety chip | SATISFIED (renderer) | Em-dash placeholder in StageBadge (row-list.tsx:193-225). Data-side root cause (b) deferred per D-14 if observed post-deploy. |

### Anti-Patterns Found

None. Spot scan of modified files at HEAD:
- No `TODO`/`FIXME`/`placeholder` strings introduced in scope.
- No `toast`/`sonner` calls added (82.6 D-03 silent UX preserved).
- No direct `fetch`/POST in stage-step.tsx Submit button (W6 single-write preserved).
- No mutations to `eval-type-radio.tsx` (D-11 honored).
- No duplicate brand swatch added to `_shell/row-list.tsx` (D-08 honored).

### Human Verification Required

See `human_verification` in frontmatter (5 items): operator must exercise the Stage 0 override flow end-to-end, observe the 150ms fade, hover the brand swatch on a live row, eyeball the override-form polish, and confirm em-dash placeholders appear on Stage 0/Stage 2 surfaces where historical rows have null `stage_state`.

### Deviations & Notes

1. **Plan 04 target file deviation** — Plan 04 frontmatter declared `web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-1-widget.tsx` but the executor correctly retargeted to `web/app/(dashboard)/automations/[swarm]/_shell/components/stage-1-widget.tsx` because the former is a 67-line Select-only widget without REG/CAP cards or Submit/Discard buttons. All D-09/D-10/D-11 intent satisfied on the correct file. (Documented in 82.7.1-04-SUMMARY.md.)

2. **Plan 04 native `<button>` swap** — Executor replaced shadcn `<Button>` with native `<button type="button">` for both controls because the inline style overrides clash with shadcn variant defaults. Matches StageFeedbackPanel precedent (native button + inline style). Functionally equivalent. Removed unused `Button` import.

3. **Plan 02 fade preservation across Plan 05 edits** — Plan 05 SUMMARY explicitly verifies Plan 02 edits remain byte-identical (`_unmount` ×7, `transition: "opacity 150ms ease-out"` at L135, single `gridTemplateColumns` literal). Verified at HEAD — both feature stacks coexist in row-list.tsx without conflict.

4. **Orchestrator worktree recovery for Plan 04** — commit `08dd766` ("chore: merge executor worktree (worktree-agent-a4f78aea0057c6e14)") landed between Plan 01 SUMMARY and Plan 04 implementation. The Plan 04 commit `70f0309` is correctly on main. No artifacts lost.

5. **DEPLOYMENT RISK — uncommitted working-tree reverts.** At verification time, `git status` shows uncommitted MODIFIED files for 5 paths:
   - `web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx` (removes onSubmitDirty)
   - `web/app/(dashboard)/automations/[swarm]/stage-1/components/pipeline-flow.tsx` (removes onSubmitDirty prop thread)
   - `web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx` (removes per-stage Submit block, 55 lines)
   - `web/app/(dashboard)/automations/[swarm]/stage-1/components/predicted-row.tsx` (removes Tooltip wrapper + brandDisplayName usage)
   - `web/lib/swarms/brand-color.ts` (removes brandDisplayName helper)

   These reverts would undo Plans 01 and 03 entirely. HEAD is correct; verification is against HEAD. **Before any further commit/deploy:** confirm whether these working-tree changes are an accidental revert (then `git checkout HEAD -- <paths>`) or intentional (then they need their own context + plan because they erase D-01..D-03 and D-06..D-08). Plan 02 SUMMARY notes the working tree was already dirty when Plan 02 began — these may be the same long-lived uncommitted reverts.

### Gaps Summary

No gaps at HEAD. All 15 D-IDs are implemented and wired. Status is `human_needed` because 5 operator-perceptible behaviors (fade smoothness, tooltip hover, visual polish, em-dash render on live historical rows, Stage 0 end-to-end override) require a browser session against a tenant to confirm operator UX. Deployment risk (uncommitted working-tree reverts) is flagged separately and must be resolved before push.

---

*Verified: 2026-05-18 at HEAD `d343fdb`*
*Verifier: Claude (gsd-verifier)*
