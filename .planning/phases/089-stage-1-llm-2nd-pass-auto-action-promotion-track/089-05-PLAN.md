---
phase: 089
plan: 05
type: execute
wave: 1
depends_on: [089-01]
files_modified:
  - web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/actions.predictor.test.ts
autonomous: true
requirements: [SC-89-02]
tags: [stage-1, ui, rsc, classifier, llm]
must_haves:
  truths:
    - "Stage-1 RSC row-loader synthesizes ruleKey='llm:{cat}:{conf}' from decision_details for rows with predictor='llm_2nd_pass' (RESEARCH OQ4)"
    - "Synthesized ruleKey flows into the form payload passed to recordVerdict / approvePrediction so operator approve/reject lands on the same (swarm_type, rule_key) aggregate as the prediction"
    - "recordVerdict requires no schema change — already accepts rule_key: z.string().min(1) (RESEARCH §Pinpoint Edit Sites)"
    - "Existing regex-path behavior is preserved (rows with predictor='regex' continue to thread their existing rule_key)"
    - "Field paths on decision_details match those locked in 089-WAVE0-PROBE.md DECISION-02"
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx"
      provides: "Row-loader rule_key synthesis"
      contains: "llm:"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/actions.predictor.test.ts"
      provides: "vitest coverage for rule_key threading into recordVerdict"
  key_links:
    - from: "page.tsx row-loader (RSC)"
      to: "recordVerdict server action (actions.ts:160-194)"
      via: "form payload — rule_key passes as Zod-validated string"
    - from: "recordVerdict agent_runs insert (actions.ts:182)"
      to: "classifier_rule_telemetry view"
      via: "view's GROUP BY (swarm_type, rule_key, human_verdict)"
---

<objective>
Per CONTEXT D-01 + RESEARCH OQ4 + PATTERNS.md row-loader section, edit `web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx` so that when it derives the PredictedRow for an LLM-classified row, it ALSO synthesizes `ruleKey = "llm:" + cat + ":" + conf` from `decision_details` (exact field paths locked in 089-WAVE0-PROBE DECISION-02). Thread `ruleKey` into the row shape and into the form payload that posts to `recordVerdict` / `approvePrediction`.

Purpose: SC-89-02 second half — operator approvals on LLM-predicted bulk-review rows must land on the same `(swarm_type, rule_key)` aggregate as the prediction itself, so the telemetry view's `n` and `agree` counts track the LLM rule_key.
Output: Edits to one RSC file + one new/extended test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/089-stage-1-llm-2nd-pass-auto-action-promotion-track/089-CONTEXT.md
@.planning/phases/089-stage-1-llm-2nd-pass-auto-action-promotion-track/089-RESEARCH.md
@.planning/phases/089-stage-1-llm-2nd-pass-auto-action-promotion-track/089-PATTERNS.md
@.planning/phases/089-stage-1-llm-2nd-pass-auto-action-promotion-track/089-WAVE0-PROBE.md
@web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/actions.ts

<interfaces>
From web/app/(dashboard)/automations/[swarm]/stage-1/actions.ts:
- recordVerdict schema (L160-194): accepts `rule_key: z.string().min(1)` — already free-form, NO schema change
- recordVerdict writes agent_runs row with that rule_key + human_verdict
- approvePrediction (L855-858): reads rule_key from automation_runs.rule_key, throws if missing (covered by Plan 02 Edit Site 4 if DECISION-01=YES)

From web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx (per PATTERNS.md):
- L244-251: PredictedRow type (extend with optional ruleKey: string | null)
- L982-1004: existing predictor + llmConfidence derivation block (where rule_key synthesis goes)
- L706 + L1163: row-action wiring → recordVerdict / approvePrediction calls (where rule_key is threaded into form payload)

From 089-WAVE0-PROBE.md DECISION-02 (LOCKED):
- decision_details.<EXACT_CATEGORY_KEY_FIELD> — fill in from probe (likely `llm_category_key` or `llm.category_key`)
- decision_details.<EXACT_CONFIDENCE_FIELD> — fill in from probe (likely `llm_confidence` or `llm.confidence`)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend page.tsx row-loader to synthesize ruleKey for LLM rows and thread into action payloads</name>
  <files>web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx (FULL — must understand PredictedRow shape at L244-251, predictor derivation at L982-1004, and the two row-action wiring sites at L706 + L1163 per RESEARCH §Edit Sites)
    - web/app/(dashboard)/automations/[swarm]/stage-1/actions.ts (recordVerdict + approvePrediction signatures — confirm rule_key field name + Zod shape)
    - .planning/phases/089-stage-1-llm-2nd-pass-auto-action-promotion-track/089-PATTERNS.md (section "page.tsx row loader (EDIT)" — full snippet with exact synthesis pattern)
    - .planning/phases/089-stage-1-llm-2nd-pass-auto-action-promotion-track/089-WAVE0-PROBE.md (DECISION-02 — exact field paths)
  </read_first>
  <behavior>
    - Behavior 1: Row with predictor='llm_2nd_pass' + decision_details with llm category_key='auto_reply' + confidence='high' → PredictedRow has ruleKey='llm:auto_reply:high'
    - Behavior 2: Row with predictor='regex' → ruleKey is sourced from existing path (regex matchedRule from automation_runs.rule_key), NOT synthesized as llm:*
    - Behavior 3: Row with predictor=null or missing decision_details → ruleKey is null (does not throw)
    - Behavior 4: Form payload for the approve/reject action carries the synthesized ruleKey under the field name `rule_key` (matches recordVerdict's Zod schema)
  </behavior>
  <action>
Per RESEARCH OQ4 + PATTERNS.md section "page.tsx row loader (EDIT)":

**Step A — Extend PredictedRow type (~L244-251)**:
```typescript
type PredictedRow = {
  // existing fields...
  predictor: "regex" | "llm_2nd_pass" | null;
  llmConfidence: string | null;
  ruleKey: string | null;  // ★ Phase 89
};
```

**Step B — Synthesize ruleKey in the existing predictor derivation (~L982-1004)**:
Use the EXACT field paths from `089-WAVE0-PROBE.md DECISION-02`. Replace the placeholders below with the locked names (likely `llm_category_key` + `llm_confidence`):

```typescript
const rawPred = d.predictor;
const predictor: PredictedRow["predictor"] =
  rawPred === "regex" || rawPred === "llm_2nd_pass" ? rawPred : null;

// existing llmConfidence derivation — keep
const llmConfidence = /* existing */;

// ★ Phase 89: synthesize ruleKey for LLM rows so recordVerdict attributes
// operator approval to the same (swarm_type, rule_key) aggregate as the
// prediction. Field paths locked in 089-WAVE0-PROBE.md DECISION-02.
const llmCategoryKey =
  typeof d.<LOCKED_CATEGORY_KEY_FIELD> === "string"
    ? d.<LOCKED_CATEGORY_KEY_FIELD>
    : null;
const ruleKey: string | null =
  predictor === "llm_2nd_pass" && llmCategoryKey && llmConfidence
    ? `llm:${llmCategoryKey}:${llmConfidence}`
    : null;

return { ...r, predictor, llmConfidence, ruleKey };
```

**Step C — Thread ruleKey into form payload at ~L706 and ~L1163** (wherever the approve / reject / override form posts to `recordVerdict` or `approvePrediction`):

For `recordVerdict` (free-form rule_key field):
```typescript
formData.append("rule_key", row.ruleKey ?? <existing-fallback>);
```
The `<existing-fallback>` is whatever the row currently passes for regex rows (likely `row.rule_key` sourced from `automation_runs.rule_key`). Preserve that for regex rows; LLM rows substitute the synthesized value.

For `approvePrediction`: Per RESEARCH §Pinpoint Edit Sites, approvePrediction reads rule_key from `automation_runs.rule_key`. If 089-WAVE0-PROBE DECISION-01 = YES + Plan 02 Edit Site 4 was applied, `automation_runs.rule_key` is already populated for LLM rows → no UI change needed for approvePrediction. If DECISION-01 = NO, `approvePrediction` would throw `rule_key missing`; in that case, add a fallback by reading `row.ruleKey` into a hidden form field that approvePrediction picks up.

**DO NOT** modify recordVerdict's Zod schema or actions.ts — the existing `z.string().min(1)` accepts any non-empty string including `"llm:auto_reply:high"`.

Verify no breakage on regex-path rows: re-read `web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/actions.predictor.test.ts` (or sibling) to confirm regex rows still resolve rule_key from automation_runs.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx vitest run "app/\(dashboard\)/automations/\[swarm\]/stage-1/" 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ruleKey' "web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx"` returns at least 3 (type field + synthesis + threading)
    - `grep -cE 'llm:\$\{' "web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx"` returns at least 1 (synthesis template literal)
    - `grep -c 'predictor === "llm_2nd_pass"' "web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx"` returns at least 1 (synthesis guard)
    - **[W2 — approvePrediction branch coverage, conditional on 089-WAVE0-PROBE DECISION-01]**:
        - If DECISION-01 = YES (Plan 02 Edit Site 4 applied → automation_runs.rule_key populated by worker): `git diff HEAD -- "web/app/(dashboard)/automations/[swarm]/stage-1/actions.ts" | wc -l | grep -q '^0$'` — no actions.ts change needed; approvePrediction reads rule_key directly from automation_runs.
        - If DECISION-01 = NO (worker did NOT write automation_runs.rule_key on LLM rows): page.tsx must thread ruleKey via a hidden form field, and actions.ts approvePrediction must read it. Gates: `grep -cE 'name="ruleKey"|formData.append\("ruleKey"' "web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx"` returns at least 1 (hidden form field) AND `grep -c 'formData.get(.ruleKey.)' "web/app/(dashboard)/automations/[swarm]/stage-1/actions.ts"` returns at least 1 (server-side read). In this branch, the "no actions.ts edits" gate above is REPLACED by these two greps.
    - Vitest run in the stage-1 directory exits with 0 failures
  </acceptance_criteria>
  <done>Row-loader synthesizes ruleKey for LLM rows, threads it into action payloads, regex rows unchanged, all stage-1 tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Add/extend test asserting recordVerdict accepts llm:* rule_key and writes it to agent_runs</name>
  <files>web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/actions.predictor.test.ts</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/actions.ts (recordVerdict L160-194 — Zod schema + agent_runs insert)
    - web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/actions.predictor.test.ts (if exists — extend; if not, create following sibling test patterns)
    - .planning/phases/089-stage-1-llm-2nd-pass-auto-action-promotion-track/089-PATTERNS.md (section "actions.ts recordVerdict (EDIT — verify, likely no code change)")
  </read_first>
  <action>
Locate the existing test file at `web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/actions.predictor.test.ts` (or sibling). If it exists, ADD test cases. If not, find the nearest sibling test and create a new file mirroring its pattern.

Add at minimum:

1. **`recordVerdict accepts rule_key="llm:auto_reply:high"`** — invoke recordVerdict with a valid payload including `rule_key: "llm:auto_reply:high"`; mock supabase admin to capture the agent_runs.insert; assert the captured row has `rule_key: "llm:auto_reply:high"`.

2. **`recordVerdict rejects empty rule_key`** — invoke with `rule_key: ""`; assert Zod validation error (sanity check the existing z.string().min(1) guard).

3. **`recordVerdict preserves rule_key="payment_subject" for regex rows`** — Pitfall 2 regression equivalent at the action layer.

If the sibling test file already covers (2) or (3), skip duplicates; (1) is the SC-89-02-specific new assertion.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx vitest run "app/\(dashboard\)/automations/\[swarm\]/stage-1/__tests__/" 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists and contains the literal string `llm:auto_reply:high`
    - Vitest run exits 0
    - `grep -c 'recordVerdict' "web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/actions.predictor.test.ts"` returns at least 2
  </acceptance_criteria>
  <done>recordVerdict's acceptance of llm:* rule_keys is verified by automated test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser form → recordVerdict server action | Untrusted form input; rule_key field is Zod-validated as z.string().min(1) |
| RSC row-loader (server) → form (client) | Server synthesizes ruleKey from decision_details (trusted DB); client may tamper before submit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-089-05-01 | Tampering | Operator tampers with form to substitute rule_key='llm:auto_reply:high' on a row whose predictor was actually 'spam' — corrupts (rule_key, human_verdict) aggregate, inflates approve rate for auto_reply | mitigate | recordVerdict (actions.ts:160-194) does NOT cross-check the submitted rule_key against the row's decision_details. Phase 89 INHERITS this trust model (regex rule_keys have the same trust shape today). Mitigation: server logs reviewer_email + verdict + rule_key for audit. Wilson-CI promotion requires n≥30 — a single tampered row cannot promote anything; a sustained tampering campaign would require operator collusion. ASVS L1 acceptable. Future hardening (out of scope, future phase): RSC posts rule_key in a server-signed hidden field; recordVerdict verifies signature. |
| T-089-05-02 | Tampering | Form submits rule_key=null for an LLM row | mitigate | Zod `z.string().min(1)` rejects null/empty → 400. |
| T-089-05-03 | Information Disclosure | ruleKey synthesis exposes internal taxonomy in HTML | accept | Same exposure as today's regex matchedRule values in the audit panel. |
</threat_model>

<verification>
- `cd web && npx vitest run "app/\(dashboard\)/automations/\[swarm\]/stage-1/"` exits 0
- `git diff web/app/(dashboard)/automations/[swarm]/stage-1/actions.ts` is empty
- Manual UAT: open Stage-1 bulk review for debtor-email, find an LLM-predicted row, approve it, then SELECT from agent_runs to confirm `rule_key LIKE 'llm:%'` + `human_verdict='approved'` (covered in Plan 07 UAT)
</verification>

<success_criteria>
SC-89-02 second half — operator approvals on LLM rows land on the same (swarm_type, rule_key) aggregate as the prediction. Verified by Task 2 test + Plan 07 UAT.
</success_criteria>

<output>
After completion, create `.planning/phases/089-stage-1-llm-2nd-pass-auto-action-promotion-track/089-05-SUMMARY.md`
</output>
