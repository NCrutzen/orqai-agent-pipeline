---
phase: 089
plan: 06
type: execute
wave: 2
depends_on: [089-02, 089-03, 089-04, 089-05]
files_modified: []
autonomous: false
requirements: [SC-89-03]
tags: [stage-1, classifier, llm, supabase, db-push, blocking]
must_haves:
  truths:
    - "Migration `supabase/migrations/20260520_phase89_llm_rule_key_backfill.sql` is applied to the live Supabase project (mvqjhlxfvtqqubqgdvhz)"
    - "Idempotency verified: second push reports 0 new rows or no-op"
    - "Verification SELECT confirms rule_key LIKE 'llm:%' rows count matches the pre-push will_backfill_count from 089-WAVE0-PROBE Query C"
    - "human_verdict values on the affected rows are UNCHANGED (D-02 safety contract)"
  artifacts:
    - path: ".planning/phases/089-stage-1-llm-2nd-pass-auto-action-promotion-track/089-06-PUSH-LOG.md"
      provides: "Push output + verification SELECT outputs"
  key_links:
    - from: "applied migration"
      to: "Plan 07 shadow-eval harness"
      via: "harness reads classifier_rule_telemetry which now aggregates over backfilled rule_keys"
---

<objective>
**[BLOCKING]** Apply the historic `rule_key` backfill migration to the live Supabase database via `supabase db push`. Per `<schema_push_requirement>`: the phase CANNOT pass verification without this push — Plan 07's shadow-eval reads the live `classifier_rule_telemetry` view, which depends on backfilled `agent_runs.rule_key` values.

Purpose: SC-89-03 — make historic LLM verdicts visible to the Wilson-CI shadow run.
Output: Push log + verification SELECT outputs in `089-06-PUSH-LOG.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/089-stage-1-llm-2nd-pass-auto-action-promotion-track/089-CONTEXT.md
@.planning/phases/089-stage-1-llm-2nd-pass-auto-action-promotion-track/089-WAVE0-PROBE.md
@CLAUDE.md
@supabase/migrations/20260520_phase89_llm_rule_key_backfill.sql
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] supabase db push + idempotency + verification SELECT</name>
  <files>.planning/phases/089-stage-1-llm-2nd-pass-auto-action-promotion-track/089-06-PUSH-LOG.md</files>
  <read_first>
    - supabase/migrations/20260520_phase89_llm_rule_key_backfill.sql (must already exist from Plan 04)
    - .planning/phases/089-stage-1-llm-2nd-pass-auto-action-promotion-track/089-WAVE0-PROBE.md (Query C will_backfill_count — expected impact)
    - CLAUDE.md (Supabase project URL `https://mvqjhlxfvtqqubqgdvhz.supabase.co`)
  </read_first>
  <what-built>
Plan 04 created `supabase/migrations/20260520_phase89_llm_rule_key_backfill.sql`. This task pushes it to the live database and records the outcome.
  </what-built>
  <action>
1. Confirm `SUPABASE_ACCESS_TOKEN` is set (export from a fresh shell if not). If interactive confirmation cannot be suppressed, run via `supabase db push --include-all` after the user approves.

2. Run:
```bash
supabase db push
```
Capture stdout + stderr verbatim into `089-06-PUSH-LOG.md` under heading `## Push 1 (first apply)`.

3. Run again (idempotency check):
```bash
supabase db push
```
Capture under `## Push 2 (re-apply, expect no-op)`. Verify Supabase reports the migration as already-applied or 0 rows affected.

4. Run verification SELECT against the live database (use Supabase Management API per MEMORY.md or `psql` with the connection string):
```sql
SELECT rule_key, COUNT(*) AS n
FROM public.agent_runs
WHERE rule_key LIKE 'llm:%'
GROUP BY rule_key
ORDER BY n DESC;
```
Capture under `## Verification: rule_key distribution after backfill`. Cross-check the total count against `will_backfill_count` from 089-WAVE0-PROBE Query C — they should match (within ±5 for any LLM verdicts written between probe and push).

5. Run human_verdict safety check (D-02 contract):
```sql
SELECT COUNT(*) AS llm_rows_with_verdict,
       COUNT(*) FILTER (WHERE human_verdict IS NOT NULL) AS non_null_verdict
FROM public.agent_runs
WHERE rule_key LIKE 'llm:%';
```
Capture under `## Verification: human_verdict unchanged`. The `non_null_verdict` count must equal the number of rows operators actually reviewed (cross-check with Query C pre-push count of LLM rows with human_verdict IS NOT NULL — if this number changed, the migration violated D-02).
  </action>
  <how-to-verify>
- `supabase db push` first run succeeds (exit 0)
- `supabase db push` second run is a no-op or "already applied"
- Verification SELECT shows rule_keys like `llm:auto_reply:high`, `llm:payment_admittance:high` with non-zero counts
- human_verdict safety check shows the same non_null_verdict count as before the push
- `089-06-PUSH-LOG.md` contains all four sections with verbatim outputs
  </how-to-verify>
  <resume-signal>Type "approved" after pasting push outputs + verification SELECT results into 089-06-PUSH-LOG.md. If idempotency or human_verdict safety check fails, type "blocked: {reason}" and revert immediately.</resume-signal>
  <verify>
    <automated>test -f .planning/phases/089-stage-1-llm-2nd-pass-auto-action-promotion-track/089-06-PUSH-LOG.md &amp;&amp; grep -c 'Push 1\|Push 2\|rule_key distribution\|human_verdict' .planning/phases/089-stage-1-llm-2nd-pass-auto-action-promotion-track/089-06-PUSH-LOG.md | head -1</automated>
  </verify>
  <acceptance_criteria>
    - File `089-06-PUSH-LOG.md` exists
    - Contains sections `## Push 1`, `## Push 2`, `## Verification: rule_key distribution after backfill`, `## Verification: human_verdict unchanged`
    - rule_key distribution SELECT shows at least one rule_key starting with `llm:`
    - human_verdict safety check confirms `non_null_verdict` is unchanged vs pre-push
  </acceptance_criteria>
  <done>Migration applied, idempotency confirmed, human_verdict not corrupted, Plan 07 can run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supabase CLI → live mvqjhlxfvtqqubqgdvhz database | Service-role access; mutates one column on agent_runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-089-06-01 | Tampering | Migration corrupts human_verdict | mitigate | Task 1 step 5 explicitly verifies human_verdict counts unchanged. If fails → revert. |
| T-089-06-02 | DoS | Push locks agent_runs table | accept | Small UPDATE on tens-to-low-hundreds of rows. Push during low-traffic window if needed. |
| T-089-06-03 | Repudiation | Push run undocumented | mitigate | Push log captured in 089-06-PUSH-LOG.md committed to git. |
</threat_model>

<verification>
- 089-06-PUSH-LOG.md exists with all four required sections
- Verification SELECT confirms backfill applied
- human_verdict safety check passes
</verification>

<success_criteria>
SC-89-03 prerequisite: live database has llm:* rule_keys on historic agent_runs. Plan 07 shadow-eval can proceed.
</success_criteria>

<output>
After completion, create `.planning/phases/089-stage-1-llm-2nd-pass-auto-action-promotion-track/089-06-SUMMARY.md`
</output>
