---
phase: 32-iterate-command-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [orq-agent/commands/iterate.md]
autonomous: true
requirements: [LOOP-01, LOOP-02, LOOP-03]

must_haves:
  truths:
    - "iterate.md Step 5 invokes failure-diagnoser and prompt-editor in a loop -- not iterator.md"
    - "iterate.md enforces 5 stop conditions: max_iterations (>3), timeout (>10min), user_declined (all rejected in iteration-proposals.json), all_pass (results.overall_pass==true), min_improvement (<5% average bottleneck delta)"
    - "iterate.md checks max_iterations and timeout BEFORE each cycle starts"
    - "iterate.md checks user_declined AFTER failure-diagnoser but BEFORE prompt-editor"
    - "iterate.md checks all_pass and min_improvement AFTER prompt-editor completes"
    - "iterate.md forwards --agent flag to failure-diagnoser as agent_key_filter"
    - "iterate.md validates iteration-proposals.json between failure-diagnoser and prompt-editor (file exists, valid JSON, per_agent array, at least 1 approved)"
    - "iterate.md snapshots bottleneck scores from test-results.json BEFORE each cycle for min_improvement comparison"
    - "iterate.md cleans stale iteration-proposals.json before loop starts but does NOT clean iteration-log.md or audit-trail.md"
    - "iterate.md Step 6 displays before/after summary using initial scores vs final scores with delta and status columns"
    - "iterate.md contains zero references to iterator.md"
    - "iterate.md adds a second timeout check before prompt-editor invocation (not just at loop start)"
  artifacts:
    - path: "orq-agent/commands/iterate.md"
      provides: "Rewritten iterate command orchestrating 2 subagents in loop with 5 stop conditions"
      min_lines: 200
  key_links:
    - from: "orq-agent/commands/iterate.md"
      to: "orq-agent/agents/failure-diagnoser.md"
      via: "Step 5.2 invocation with swarm_dir, iteration_number, agent_key_filter"
      pattern: "failure-diagnoser"
    - from: "orq-agent/commands/iterate.md"
      to: "orq-agent/agents/prompt-editor.md"
      via: "Step 5.4 invocation with swarm_dir, iteration_number"
      pattern: "prompt-editor"
    - from: "orq-agent/commands/iterate.md"
      to: "iteration-proposals.json (disk)"
      via: "Step 5.3 validation gate checks file existence, JSON validity, approved agents"
      pattern: "iteration-proposals.json"
    - from: "orq-agent/commands/iterate.md"
      to: "test-results.json (disk)"
      via: "Step 5.5 post-cycle evaluation reads updated scores for all_pass and min_improvement"
      pattern: "test-results.json"
---

<objective>
Rewrite iterate.md to orchestrate failure-diagnoser and prompt-editor in a loop instead of invoking the monolithic iterator.md. Add stop condition evaluation, intermediate validation gates, and score tracking across cycles.

Purpose: Complete the V2.1 iterate pipeline restructure so users get the same iteration behavior through 2 focused subagents instead of the 544-line iterator monolith.
Output: Rewritten orq-agent/commands/iterate.md
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-iterate-command-rewrite/32-RESEARCH.md

@orq-agent/commands/iterate.md
@orq-agent/agents/failure-diagnoser.md
@orq-agent/agents/prompt-editor.md

<interfaces>
<!-- Handoff contracts between subagents (validation gate targets) -->

iteration-proposals.json required keys (written by failure-diagnoser Phase 5):
- `.iteration_number` — integer
- `.per_agent` — array of objects
- Each agent: `.agent_key`, `.approval` ("approved" | "rejected"), `.proposals[]` (section-level diffs with reasoning)

test-results.json required keys (read for stop conditions):
- `.results.overall_pass` — boolean
- `.results.per_agent[]` — array with agent_key, scores (per-evaluator medians), evaluators_used
- Bottleneck score = lowest evaluator median for each agent

iteration-log.md — written by prompt-editor Phase 6 (appended each cycle). iterate.md does NOT write to this file.
audit-trail.md — written by prompt-editor Phase 6 (appended each cycle). iterate.md does NOT write to this file.
</interfaces>

<decisions>
- [Phase 30]: Failure-diagnoser writes iteration-proposals.json but never modifies spec files (scope boundary with prompt-editor)
- [Phase 31]: Evaluator IDs passed through from test-results.json to experiment-runner to skip re-resolution during holdout re-test
- [Phase 31]: Before scores snapshotted in memory before test-results.json update, preserving original values in comparison display
- [P27]: REST-only for experiments (LOCKED) -- skip MCP entirely for experiment creation/execution
- [Phase 29]: mcp_available forwarded to dataset-preparer only -- experiment-runner REST-only, results-analyzer no API calls
</decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite iterate.md Step 5 with 2-subagent loop, stop conditions, and validation gates</name>
  <files>orq-agent/commands/iterate.md</files>
  <action>
Rewrite `orq-agent/commands/iterate.md`. Keep Steps 1, 1.5, 2, 3, and 7 exactly as-is. Modify Steps 4, 5, and 6 as described below. Remove ALL references to `iterator.md`.

**Step 4: Pre-check Test Results (MODIFY)**

Keep the existing Step 4 logic (check test-results.json exists, check overall_pass, display summary). Add at the END of Step 4 (after the "If false: proceed to Step 5" branch):

Snapshot initial bottleneck scores from test-results.json for later comparison:
- For each agent in `results.per_agent[]`, record the bottleneck score (lowest evaluator median across all evaluators for that agent)
- Store as `initial_scores` map: `{agent_key: bottleneck_score}`
- These are the "Before" values displayed in Step 6

Add stale artifact cleanup before the loop:
```bash
rm -f {swarm_dir}/iteration-proposals.json
```

Do NOT clean `iteration-log.md` or `audit-trail.md` -- these are append-only cumulative files.

**Step 5: Iteration Loop (REPLACE ENTIRELY)**

Replace the current "Invoke Iterator Subagent" step with the following loop structure:

Initialize loop state:
- `iteration = 0`
- `start_time = current timestamp`
- `previous_scores = initial_scores` (from Step 4 snapshot)
- `stop_reason = null`

### Step 5.1: Loop Start

Increment iteration counter: `iteration += 1`

**Stop check -- max_iterations:** If `iteration > 3`, set `stop_reason = "max_iterations"` and exit loop to Step 6.
**Stop check -- timeout:** If elapsed time since `start_time` > 10 minutes, set `stop_reason = "timeout"` and exit loop to Step 6.

Display:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 ORQ > ITERATE -- Iteration {iteration} of 3 (max)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Step 5.2: Invoke Failure Diagnoser

Read subagent instructions from `orq-agent/agents/failure-diagnoser.md`. Invoke with:
- **swarm_dir** (from Step 3)
- **iteration_number** (current iteration count)
- **agent_key_filter** (if `--agent` specified in Step 3)

### Step 5.3: Validate Iteration Proposals

Check `{swarm_dir}/iteration-proposals.json`:
1. File exists
2. Valid JSON via `jq . {swarm_dir}/iteration-proposals.json > /dev/null 2>&1`
3. Has `per_agent` array via `jq '.per_agent | length' {swarm_dir}/iteration-proposals.json`
4. Count agents with `approval: "approved"` via `jq '[.per_agent[] | select(.approval == "approved")] | length' {swarm_dir}/iteration-proposals.json`

**If any of checks 1-3 fail:** Display ABORT message and STOP (fatal error):
```
ABORT: Failure diagnosis failed.

Expected: {swarm_dir}/iteration-proposals.json
{Specific reason: "File not found" | "Invalid JSON" | "Missing per_agent array"}

The iteration pipeline stopped. Fix the upstream issue and re-run /orq-agent:iterate.
```

**If check 4 returns zero approved agents:** Set `stop_reason = "user_declined"` and exit loop to Step 6. This is NOT a fatal error -- the user chose to reject all proposals.

**Stop check -- timeout:** If elapsed time > 10 minutes, set `stop_reason = "timeout"` and exit loop to Step 6.

### Step 5.4: Invoke Prompt Editor

Snapshot current bottleneck scores from `test-results.json` as `before_cycle_scores` (these will be compared against updated scores after this cycle for min_improvement check).

Read subagent instructions from `orq-agent/agents/prompt-editor.md`. Invoke with:
- **swarm_dir** (from Step 3)
- **iteration_number** (current iteration count)

Prompt-editor will: apply approved changes, re-deploy via deployer, re-test on holdout via experiment-runner, compute before/after comparison, update test-results.json with new scores, and append to iteration-log.md and audit-trail.md.

### Step 5.5: Evaluate Post-Cycle Stop Conditions

Read updated `{swarm_dir}/test-results.json`.

**Stop check -- all_pass:** If `results.overall_pass == true`, set `stop_reason = "all_pass"` and exit loop to Step 6.

**Stop check -- min_improvement:**
For each agent that was approved in this cycle's iteration-proposals.json:
  - Get new bottleneck score from updated test-results.json
  - Get previous bottleneck score from `before_cycle_scores`
  - Compute delta: `((new - previous) / previous) * 100`
Compute average delta across all changed agents.
If average delta < 5%: set `stop_reason = "min_improvement"` and exit loop to Step 6.

Update `previous_scores` with current scores from updated test-results.json.

### Step 5.6: Continue Loop

Go to Step 5.1.

---

**Step 6: Display Iteration Results (MODIFY)**

Replace the current Step 6 content that expects iterator output with a loop-state-based summary.

After the loop exits (any stop_reason), display:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 ORQ > ITERATE -- Complete
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Iterations: {iteration}
Stopped: {stop_reason_display}

Agent              | Before | After  | Delta   | Status
-------------------|--------|--------|---------|--------
{agent-key}        | {initial_bottleneck} | {final_bottleneck} | {total_delta}%  | improved/unchanged/regressed
```

**Before column:** From `initial_scores` (Step 4 snapshot -- the scores from BEFORE any iteration).
**After column:** From final `test-results.json` after last completed cycle.
**Delta:** Total improvement across all iterations: `((after - before) / before) * 100`.
**Status:** `improved` if delta > 0, `unchanged` if delta == 0, `regressed` if delta < 0.

**Stop reason display (plain language):**
- `all_pass`: "All agents now passing evaluators."
- `max_iterations`: "Maximum iterations (3) reached."
- `min_improvement`: "Improvement below 5% threshold."
- `user_declined`: "User declined proposed changes."
- `timeout`: "10-minute timeout reached."

Show log file paths: `Files: iteration-log.md | audit-trail.md`

**Important implementation details:**
- The `allowed-tools` frontmatter must remain `Read, Bash`
- Keep the existing YAML frontmatter description line
- Each subagent is invoked by reading its `.md` file and following its instructions -- the standard Claude Code subagent invocation pattern
- iterate.md is an orchestrator ONLY -- it never diagnoses failures, edits prompts, re-deploys, or writes log files. All domain logic lives in the 2 subagents
- Let subagent terminal output flow through. The orchestrator adds only iteration-level headers
- The iterate command does NOT forward `mcp_available` to either subagent (failure-diagnoser makes no API calls; prompt-editor delegates to deployer/experiment-runner which handle their own API access)
  </action>
  <verify>
    <automated>grep -q "failure-diagnoser.md" orq-agent/commands/iterate.md && grep -q "prompt-editor.md" orq-agent/commands/iterate.md && ! grep -q "iterator\.md" orq-agent/commands/iterate.md && ! grep -q "Invoke Iterator" orq-agent/commands/iterate.md && echo "PASS: 2 subagents referenced, iterator.md invocation removed" || echo "FAIL"</automated>
  </verify>
  <done>iterate.md Step 5 replaced with loop over failure-diagnoser and prompt-editor. 5 stop conditions implemented at correct evaluation points. Validation gate for iteration-proposals.json added between subagents. Score snapshot for min_improvement tracking in place. Stale artifact cleanup added. All iterator.md references removed. Step 6 updated with loop-state summary. Steps 1-3 and 7 unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Validate rewritten iterate.md against all 3 LOOP requirements and verify no iterator.md references</name>
  <files>orq-agent/commands/iterate.md</files>
  <action>
Validation-only task. Read the rewritten iterate.md and verify:

1. **LOOP-01 coverage:** Confirm Step 5 invokes failure-diagnoser (Step 5.2) and prompt-editor (Step 5.4) in a loop. Confirm NO reference to `iterator.md` or "Invoke Iterator" remains anywhere in the file. Confirm the loop structure has a clear continue/exit flow.

2. **LOOP-02 coverage:** Confirm all 5 stop conditions are present with correct thresholds and evaluation timing:
   - `max_iterations` (iteration > 3) -- checked at Step 5.1 before cycle
   - `timeout` (elapsed > 10 minutes) -- checked at Step 5.1 before cycle AND at Step 5.3 before prompt-editor
   - `user_declined` (zero approved agents) -- checked at Step 5.3 after failure-diagnoser
   - `all_pass` (results.overall_pass == true) -- checked at Step 5.5 after prompt-editor
   - `min_improvement` (average delta < 5%) -- checked at Step 5.5 after prompt-editor

3. **LOOP-03 coverage:** Confirm:
   - `--agent` flag is parsed in Step 3 (unchanged from current file)
   - `--agent` flag forwarded to failure-diagnoser in Step 5.2 as `agent_key_filter`
   - `--agent` flag NOT passed to prompt-editor (it reads from iteration-proposals.json which is pre-filtered)
   - Step 6 references `iteration-log.md` and `audit-trail.md` file paths
   - iterate.md does NOT write to iteration-log.md or audit-trail.md (that is prompt-editor's job)

4. **Score snapshot integrity:** Confirm `initial_scores` snapshot taken in Step 4 (before any cycle), `before_cycle_scores` snapshot taken in Step 5.4 (before each cycle's prompt-editor), and Step 6 "Before" column uses `initial_scores` (not `before_cycle_scores`).

5. **Stale artifact cleanup:** Confirm `iteration-proposals.json` is cleaned before loop starts. Confirm `iteration-log.md` and `audit-trail.md` are NOT cleaned.

6. **Anti-pattern check:** Confirm iterate.md does NOT contain any diagnosis logic, prompt editing logic, re-deploy invocations, or log file writes. All domain logic should be delegated to subagents.

7. **Step continuity:** Confirm Steps 1, 1.5, 2, 3 are unchanged from the original. Confirm Step 7 guidance messages are present.

If any requirement is missing or incorrectly implemented, fix the file inline. If all checks pass, no changes needed.
  </action>
  <verify>
    <automated>! grep -q "iterator\.md" orq-agent/commands/iterate.md && grep -q "max_iterations" orq-agent/commands/iterate.md && grep -q "timeout" orq-agent/commands/iterate.md && grep -q "user_declined" orq-agent/commands/iterate.md && grep -q "all_pass" orq-agent/commands/iterate.md && grep -q "min_improvement" orq-agent/commands/iterate.md && grep -q "\-\-agent" orq-agent/commands/iterate.md && grep -q "iteration-log.md" orq-agent/commands/iterate.md && grep -q "audit-trail.md" orq-agent/commands/iterate.md && echo "PASS: all LOOP requirements verified" || echo "FAIL: requirement gap detected"</automated>
  </verify>
  <done>All 3 LOOP requirements verified. No iterator.md references remain. 5 stop conditions with correct thresholds and evaluation timing confirmed. --agent flag forwarded to failure-diagnoser only. Score snapshots at correct points. Stale cleanup correct. No domain logic leakage into orchestrator. Steps 1-3 and 7 preserved.</done>
</task>

</tasks>

<verification>
1. `orq-agent/commands/iterate.md` references failure-diagnoser.md and prompt-editor.md (not iterator.md)
2. Loop structure with clear iteration counter, continue, and exit-to-Step-6 flow
3. All 5 stop conditions present: max_iterations (>3), timeout (>10min), user_declined, all_pass, min_improvement (<5%)
4. Stop conditions evaluated at correct timing: pre-cycle (max_iterations, timeout), mid-cycle (user_declined, timeout), post-cycle (all_pass, min_improvement)
5. Validation gate for iteration-proposals.json between failure-diagnoser and prompt-editor
6. `--agent` flag forwarded to failure-diagnoser only
7. Score snapshots: initial_scores in Step 4, before_cycle_scores in Step 5.4, min_improvement uses before_cycle_scores
8. Step 6 Before/After uses initial_scores vs final test-results.json
9. Stale iteration-proposals.json cleaned; iteration-log.md and audit-trail.md preserved
10. Steps 1, 1.5, 2, 3, 7 structurally preserved
</verification>

<success_criteria>
- iterate.md is a thin orchestrator that loops over 2 subagents with stop condition evaluation
- An executor Claude can follow the rewrite without interpretation or clarifying questions
- No reference to iterator.md remains in iterate.md
- Users running `/orq-agent:iterate --agent my-agent` get single-agent filtering via failure-diagnoser
- Loop stops cleanly on any of the 5 conditions with the correct stop_reason displayed
- Before/After score comparison in Step 6 uses initial scores (pre-iteration) vs final scores (post-last-cycle)
</success_criteria>

<output>
After completion, create `.planning/phases/32-iterate-command-rewrite/32-01-SUMMARY.md`
</output>
