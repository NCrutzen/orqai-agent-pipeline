---
phase: 61-restore-lost-bulk-review-ux-60-05-regression-fix-horizontal-
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - web/app/(dashboard)/automations/debtor-email-review/detail-pane.tsx
  - web/app/(dashboard)/automations/debtor-email-review/row-strip.tsx
  - web/app/(dashboard)/automations/debtor-email-review/queue-tree.tsx
  - web/app/(dashboard)/automations/debtor-email-review/keyboard-shortcuts.tsx
autonomous: false  # ends with a human-verify checkpoint
requirements:
  - D-NO-OVERFLOW-1024-2560  # CONTEXT.md acceptance #1
  - D-LIGHTMODE-CONTRAST     # CONTEXT.md acceptance #8
  - D-LUCIDE-ICONS           # CONTEXT.md anti-pattern #4
  - D-AUTO-ADVANCE-TIMING    # CONTEXT.md acceptance #6

must_haves:
  truths:
    - "All Lucide icons render at 16px; no emoji icons remain in the route"
    - "No horizontal scrollbar at 1024, 1280, 1440, 1920, 2560 widths in light or dark mode"
    - "Auto-advance timing measured ≤220ms (200ms target + ~20ms tolerance) after Approve/Reject/Skip"
    - "Body lazy-fetch issues exactly one fetch on first open per row id; second open hits the in-memory cache (no network call)"
    - "All 9 keyboard shortcuts work end-to-end against a real predicted row in the dev server"
  artifacts:
    - path: "web/app/(dashboard)/automations/debtor-email-review/detail-pane.tsx"
      provides: "Polished detail pane with measured timings"
  key_links: []
---

<objective>
Visual polish + manual UAT against a running dev server. No new components.

Purpose: Plans 01 and 02 deliver the contract + structure. Plan 03 catches
visual regressions that automated tests can't (overflow, contrast, font
rendering, icon weight) and runs the manual UAT checklist that signs off the
phase.

Output:
- Polish edits to detail-pane / row-strip / queue-tree / keyboard-shortcuts
- A signed manual UAT checklist (commit message body or 61-03-SUMMARY.md)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-restore-lost-bulk-review-ux-60-05-regression-fix-horizontal-/61-CONTEXT.md
@.planning/phases/61-restore-lost-bulk-review-ux-60-05-regression-fix-horizontal-/61-01-SUMMARY.md
@.planning/phases/61-restore-lost-bulk-review-ux-60-05-regression-fix-horizontal-/61-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Visual polish sweep</name>
  <files>
    web/app/(dashboard)/automations/debtor-email-review/detail-pane.tsx,
    web/app/(dashboard)/automations/debtor-email-review/row-strip.tsx,
    web/app/(dashboard)/automations/debtor-email-review/queue-tree.tsx,
    web/app/(dashboard)/automations/debtor-email-review/keyboard-shortcuts.tsx
  </files>
  <action>
    Walk through the four files and fix any of the following if present:

    1. **Icon audit** — grep for emoji codepoints in the route folder; replace
       any with Lucide icons at `size={16}`. Acceptable Lucide set:
       `MailOpen, Check, X, SkipForward, Keyboard, ChevronRight, ChevronDown, Search, Inbox`.
       ```bash
       grep -P '[\x{1F300}-\x{1FAFF}]|[\x{2600}-\x{27BF}]' web/app/\(dashboard\)/automations/debtor-email-review/*.tsx
       ```
       Must return empty (or only matches inside JSX strings that are clearly content, not icons).

    2. **min-w-0 audit** — every flex/grid child in the route folder must have
       either `min-w-0` or an explicit width. Quick check:
       ```bash
       grep -nE 'flex-1|truncate' web/app/\(dashboard\)/automations/debtor-email-review/*.tsx
       ```
       For each match, verify the parent has `min-w-0`. Add where missing.

    3. **Auto-advance timing** — confirm the `setTimeout(..., 200)` in
       detail-pane.tsx has not regressed. Optionally upgrade to:
       ```ts
       const advance = () => { /* push selected= */ };
       if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) advance();
       else setTimeout(advance, 200);
       ```

    4. **kbd hint styling** in the action bar buttons — wrap the keyboard
       hints in proper `<kbd>` elements with consistent class:
       `className="ml-2 px-1.5 py-0.5 rounded-[4px] bg-black/30 text-[11px] font-mono opacity-70"`.

    5. **Cheatsheet styling** — the Sheet content should use the same `<kbd>`
       style and a clean two-column layout (key | description). Verify it
       opens on `?` and closes on Esc + clicking outside.

    6. **Tab order** — confirm row-strip elements are tabbable so screen
       readers can hit them. Add `tabIndex={0}` and `role="button"` +
       `onKeyDown` for Enter/Space → onSelect on the strip wrapper.

    7. **Contrast spot-check** — run a mental check on:
       - Status pill `var(--v7-muted)` on `var(--v7-panel-2)` (CONTEXT says it passes — confirm visually in Task 2)
       - Body text in body-frame: `bg-black/20` over `var(--v7-panel-2)` — verify in light mode that the text is still legible.

    Run `pnpm tsc --noEmit -p .` and `pnpm vitest run tests/queue/` after the
    sweep — both must stay green.
  </action>
  <verify>
    <automated>cd web && pnpm tsc --noEmit -p . && pnpm vitest run tests/queue/ && (grep -P '[\x{1F300}-\x{1FAFF}]|[\x{2600}-\x{27BF}]' web/app/\(dashboard\)/automations/debtor-email-review/*.tsx | grep -v '^#' | grep -c .) | { read n; [ "${n:-0}" = "0" ]; }</automated>
  </verify>
  <done>tsc + vitest green; zero emoji icon codepoints in route .tsx files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 61 end-to-end: extended verdict contract (Plan 01), 3-col layout
    + detail pane + keyboard shortcuts (Plan 02), visual polish (this plan
    Task 1).

    The user now needs to confirm the regression is closed by walking the
    UAT checklist below in a running dev server.
  </what-built>
  <how-to-verify>
    Start the dev server:
    ```
    cd web && pnpm dev
    ```
    Open http://localhost:3000/automations/debtor-email-review with at least
    one row in `automation_runs WHERE status='predicted' AND swarm_type='debtor-email'`
    (use any acceptance-environment row).

    **A. Layout sanity (no horizontal overflow)**
    1. At 1024×768 — confirm no horizontal scrollbar appears
    2. At 1280×800 — same
    3. At 1440×900 — same
    4. At 1920×1080 — same
    5. At 2560×1440 (or zoomed-out equivalent) — same; container caps at 1600px and centers
    6. Toggle dark/light mode (existing V7 toggle in sidebar) — confirm both render cleanly

    **B. Tree column**
    7. "Queue summary" header shows total count + "X promoted today" pill
    8. "Pending promotion" appears as a sibling top-level node below topic roots (not as a tab in the row pane)
    9. Clicking Pending promotion shows the candidate-rules pane in column 2
    10. Tree column width visually narrower than before (was 320px, now ~220-280px)

    **C. Row list (column 2)**
    11. Each row shows two lines: subject + meta. No Approve/Reject buttons
    12. Clicking a row updates the URL with `?selected=<id>` and highlights the row with a 3px brand-primary left bar
    13. Race-cohort banner still appears when `?rule=` matches a today-promoted rule

    **D. Detail pane (column 3)**
    14. Empty state shows when no row selected
    15. With a row selected: status pill, wrapped subject, 6-cell meta grid, body button, override dropdown, notes textarea, 3-button action bar all visible
    16. Click "Show full email" — body loads within 1-2s, second open is instant (no spinner = cache hit)
    17. Override dropdown shows exactly 5 options: Payment, Auto-reply, OOO (temporary), OOO (permanent), Unknown (skip)
    18. Type in notes — Approve persists notes; verify in DB:
        ```sql
        SELECT result->'review_note' FROM automation_runs WHERE id = '<id>';
        ```
    19. Approve advances to next row in ≤220ms (eyeball it)
    20. Reject same. Skip (n key) same.
    21. Skip → DB row has status='feedback' AND result.review_override='unknown' AND no Outlook side-effects (verify by checking that automation_runs.completed_at set but no Outlook category applied — easiest check: in iController acceptance the email is NOT removed from inbox)

    **E. Keyboard shortcuts** — with a row selected:
    22. ↓ moves selection down; ↑ moves up
    23. j and k mirror ↓ and ↑
    24. ⏎ approves
    25. Space rejects (and does not scroll the page)
    26. n skips
    27. e toggles body
    28. r focuses override dropdown (visible focus ring on the trigger)
    29. / focuses notes textarea (and does not navigate the URL)
    30. ? opens the cheatsheet sheet
    31. With focus inside the notes textarea, press ⏎/Space/n/e — none of the actions fire (input-focus guard works)

    **F. Persistence regression check**
    32. Approve a row with override_category='auto_reply' (different from predicted) and a 50-char note. Verify in DB:
        ```sql
        SELECT decision_path, result->'review_override', result->'review_note', corrected_category
        FROM automation_runs ar JOIN agent_runs agr ON agr.automation_run_id = ar.id
        WHERE ar.id = '<id>';
        ```
        - automation_runs.result.review_override = 'auto_reply'
        - automation_runs.result.review_note = '<the note>'
        - agent_runs.corrected_category = 'auto_reply'
    33. The classifier-verdict-worker Inngest function picked up the event and ran the auto_reply branch (check Inngest dashboard for `classifier/verdict.recorded`).

    Reply with **approved** if all 33 boxes tick, or list which steps failed.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All Plan 01 + Plan 02 success criteria still hold
- Manual UAT 33-box checklist all green
- `pnpm tsc --noEmit -p .` and `pnpm vitest run tests/queue/` clean
</verification>

<success_criteria>
- Zero horizontal overflow at 1024-2560
- Body fetch caches per row
- Auto-advance ≤220ms
- All 9 keyboard shortcuts pass UAT
- Light + dark mode both render cleanly
- Persistence round-trip verified in DB (review_override + review_note + agent_runs.corrected_category)
</success_criteria>

<output>
After UAT approval, create `.planning/phases/61-restore-lost-bulk-review-ux-60-05-regression-fix-horizontal-/61-03-SUMMARY.md` with:
- the 33-item UAT checklist + verdict per item
- screenshots at 1280 + 1920 (light + dark) — store in `.planning/phases/61-.../screenshots/`

Commit message template:
```
chore(61-03): UX polish + Phase 61 UAT sign-off

- Lucide icon audit (no emoji icons)
- min-w-0 hygiene sweep across route
- <kbd> styling on action-bar shortcut hints + cheatsheet
- Manual UAT (33 items) signed off against acceptance environment

Closes Phase 61 — bulk-review UX restored on top of 60-05 tree shell.
```
</output>

## PLAN COMPLETE
