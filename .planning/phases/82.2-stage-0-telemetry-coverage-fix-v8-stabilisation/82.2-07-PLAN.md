---
phase: 82.2-stage-0-telemetry-coverage-fix-v8-stabilisation
plan: 07
type: execute
wave: 3
depends_on: [82.2-06]
files_modified:
  - web/app/api/automations/debtor-email/ingest/route.ts
  - web/app/api/automations/sales-email/ingest/route.ts
autonomous: true
requirements: [SAFE-01, SAFE-02, CONS-01, TELE-COV-01]
must_haves:
  truths:
    - "debtor-email/ingest/route.ts: persists email row → creates Stage 0 placeholder automation_runs row → fires stage-0/email.received UNCONDITIONALLY → returns 200"
    - "debtor-email ingest route NO LONGER runs regex classification, auto-actions, predicted-category bulk-review INSERT, or Stage 1 pipeline_events emit (all moved to Stage 1 worker in Plan 06)"
    - "stage-0/email.received payload carries: automation_run_id, email_id, message_id, source_mailbox, subject, body_text, swarm_type, entity, mailbox_id, from, fromName, receivedAt — everything the worker chain needs"
    - "sales-email ingest already matches the thin pattern; only minor clean-up to align field set"
  artifacts:
    - path: "web/app/api/automations/debtor-email/ingest/route.ts"
      provides: "Thin ingest per D-A"
      max_lines_after_change: 350
    - path: "web/app/api/automations/sales-email/ingest/route.ts"
      provides: "Aligned thin ingest"
  key_links:
    - from: "debtor-email/ingest/route.ts"
      to: "stage-0/email.received Inngest event"
      via: "inngest.send (unconditional)"
      pattern: "stage-0/email.received"
---

<objective>
Thin down `web/app/api/automations/debtor-email/ingest/route.ts` per D-A. The route's new responsibilities are exactly: validate request → load mailbox settings (for `ingest_enabled` gate) → fetch the email body (Zapier NXT-S3 flow stays) → persist `email_pipeline.emails` row → INSERT Stage 0 placeholder `automation_runs` row → fire `stage-0/email.received` event → return 200. Everything else (regex, whitelist, auto-actions, predicted-category bulk-review, Stage 1 emit) is gone — already moved to the Stage 1 worker chain by Plan 06.

Then verify `sales-email/ingest/route.ts` matches the same thin pattern (per RESEARCH it mostly does at L231–247; small alignment work if any).

Purpose: D-A, D-B, D-C. This is the file that turns Stage 0 from gated-on-`unknown` into unconditional-first-step. After deploy, every inbound debtor email fires Stage 0 regardless of regex outcome — closes the 26–45% → ≥99% coverage gap structurally.

Output: Both ingest routes are thin and consistent. After deploy (hard cutover, D-C), live Stage 0 coverage rises immediately for new inbound emails.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82.2-stage-0-telemetry-coverage-fix-v8-stabilisation/82.2-CONTEXT.md
@.planning/phases/82.2-stage-0-telemetry-coverage-fix-v8-stabilisation/82.2-RESEARCH.md
@CLAUDE.md
@docs/agentic-pipeline/README.md
@docs/agentic-pipeline/stage-0-safety.md
@web/app/api/automations/debtor-email/ingest/route.ts
@web/app/api/automations/sales-email/ingest/route.ts
@web/lib/inngest/functions/stage-0-safety-worker.ts

<interfaces>
**Thin ingest contract (debtor-email post-refactor):**
```
POST /api/automations/debtor-email/ingest
  body: { messageId, source_mailbox, auth, ... } (Zapier-driven)
  flow:
    1. Validate auth (body.auth match — existing pattern)
    2. Load swarms_labeling_settings for source_mailbox (entity, mailbox_id, ingest_enabled, auto_label_enabled, icontroller_company, triage_shadow_mode)
       → if !ingest_enabled: return 200 { action: "skipped_disabled" }
    3. Fetch email via NXT-Zap client (existing Zapier SDK call — fetchEmail)
       → on 404: return 200 { action: "skipped_not_found" } (preserve today's automation_runs audit row)
       → on error: return 200 { action: "failed", error } (preserve today's audit row)
    4. Resolve / insert email_pipeline.emails row (today's L305–345 logic — preserve)
    5. INSERT Stage 0 placeholder automation_runs row: status='pending', automation='debtor-email-review', swarm_type='debtor-email', topic=null, mailbox_id, entity, result={ stage: 'stage_0_safety_pending', message_id, source_mailbox, entity, subject, from }, triggered_by='zapier:ingest'. Capture stage0RunId.
    6. inngest.send({ name: 'stage-0/email.received', data: { automation_run_id: stage0RunId, email_id: resolvedEmailId, message_id, source_mailbox, subject, body_text: msg.body, swarm_type: 'debtor-email', entity, mailbox_id, from, fromName, receivedAt } })
    7. return 200 { action: "stage_0_dispatched", messageId, source_mailbox }
```

**What is REMOVED from the ingest route (now in Stage 1 worker per Plan 06):**
- `classify(...)` call (L284)
- Stage 1 `emitPipelineEvent` (L347–362)
- `isWhitelistMatch` / `autoActionAllowed` gates (L364–365)
- The `!autoActionAllowed` bulk-review automation_runs insert with status='predicted' (L402–423)
- The whole auto-action branch (L467–592): categorize, archive, success audit, cleanup queue
- The MR_LABELS idempotency early-return (L272–281) — moved to Stage 1 worker pre-check
- The `fireStage0Event` helper (L621–) — replaced by inline `inngest.send` in step 6

**What STAYS in the ingest route:**
- Auth check
- Settings load (now ALSO used to drive the `ingest_enabled` short-circuit + threading entity/mailbox_id into the event)
- Email fetch (Zapier NXT-S3 flow)
- email_pipeline.emails persist
- Stage 0 placeholder automation_runs row creation (the worker's UPDATE-existing target — today's L378–400 logic, but now ALWAYS created, not gated on `r.category === "unknown"`)
- Fetch-error audit rows (L249–261 — preserved for operability)

**stage-0/email.received payload extensions:**
Today's Stage 0 worker reads `automation_run_id, email_id, message_id, source_mailbox, subject, body, swarm_type, entity, mailbox_id, safety_overridden`. Plan 06 also needs `from, fromName, receivedAt` to write the auto-action audit rows (which today are written by ingest with these fields). Verify Stage 0 worker passes ALL inbound payload fields through to `classifier/screen.requested` so the Stage 1 worker has them. If not, extend the worker's classifier-emit payload (this is a minor add to Plan 04 / a follow-up edit here).

**Hard cutover (D-C):** no feature flag, no per-mailbox rollout. In-flight emails finish on the old path — that's fine because the old path's last step (Stage 0 worker or auto-action completion) doesn't depend on the new ingest behavior. The 30-day backfill (Plan 09) cleans up historical gaps; this thin-ingest deploy closes the live gap.

**Sales-email ingest (verified by RESEARCH L231–247):** already fires stage-0/email.received unconditionally. Only check the field set matches what Stage 1 worker now needs (entity, mailbox_id, from, fromName, receivedAt). If missing, add.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage 0 worker payload pass-through audit</name>
  <files>web/lib/inngest/functions/stage-0-safety-worker.ts (read; possible small edit)</files>
  <action>
    Read the Plan-04-refactored worker. Verify the `classifier/screen.requested` emit (formerly L280–296, post-refactor inside the single step.run) passes ALL inbound payload fields through to Stage 1 — specifically: `from`, `fromName`, `receivedAt`, `mailbox_id` in addition to today's `automation_run_id, email_id, message_id, source_mailbox, subject, body_text, swarm_type, entity, safety_overridden`.

    If fields are missing, add them — both for `main-path safe` AND for `operator-override` downstreamEmit. Same for the budget-breach emit (verify Plan 06 Stage 1 worker doesn't need these on the budget-breach path — likely not, since budget-breach halts before dispatch).

    Update Plan 04 / Plan 05 tests if their fixtures lock the payload shape.
  </action>
  <verify>
    <automated>cd web && npx vitest run lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts</automated>
  </verify>
  <done>Stage 0 worker forwards all fields Stage 1 worker needs; tests still green.</done>
</task>

<task type="auto">
  <name>Task 2: Thin down debtor-email ingest route</name>
  <files>web/app/api/automations/debtor-email/ingest/route.ts</files>
  <action>
    Rewrite the POST handler per the thin contract above. Concrete steps:

    **MANDATORY DELETIONS (resolved per Plan 06 Step 7 — Stage 1 emit ownership):**
    - **DELETE the Stage 1 `emitPipelineEvent(...)` call at L347–362** of the current ingest route. The Stage 1 worker (`classifier-screen-worker.ts` Step 4 at ~L293–321) is the SOLE canonical Stage 1 emit site post-D-A. Leaving this in produces double-write Stage 1 rows and breaks TELE-COV-01's "exactly one row per stage" guarantee.
    - DELETE `fireStage0Event` helper (L621–end) and replace with the inline `inngest.send` in step 6 below.
    - DELETE the `classify(...)` call, whitelist/auto-action gates, predicted-category bulk-review INSERT, and the entire auto-action branch — all moved to Plan 06.

    **MANDATORY AUDIT (Phase 64 SAFE-01..03 placeholder semantics post-D-A):**
    Before finalising the rewrite, grep-verify the Stage 0 worker's UPDATE-by-id semantics still align with the now-universal placeholder creation:
    ```bash
    grep -n "automation_runs\|persist-verdict\|.eq(" web/lib/inngest/functions/stage-0-safety-worker.ts | head -40
    ```
    Confirm (already verified by planner pre-write): the worker at L217–252 does `UPDATE ... WHERE id=automation_run_id AND status='pending'` on the placeholder, with a fallback INSERT only when `automation_run_id` is absent (legacy path). Post-Plan-07, the placeholder is created universally (was previously only created in the `r.category === "unknown"` branch — Phase 64 SAFE-01..03 pattern was scoped to that single branch). Universal creation means the UPDATE path now ALWAYS fires for ingest-originated emails; the INSERT fallback only runs for non-ingest entry points. This is the intended behavior — checkbox in SUMMARY: "Worker UPDATE-by-id semantics confirmed compatible with universal placeholder; no INSERT-duplication risk."

    1. Remove imports no longer used: `classify`, `emitPipelineEvent`, `categorizeEmail`, `archiveEmail`, `CATEGORY_LABEL`, `MR_LABELS`, `whitelist`, `fireStage0Event` (delete the helper too).
    2. Keep imports: `inngest`, `createAdminClient`, settings loader, NXT-Zap email fetcher, env-var auth checker.
    3. Preserve the existing error-handling audit rows for `not_found` / `fetch_error` (today's L249–261).
    4. Preserve the email_pipeline.emails resolve/insert logic (today's L305–345). This is the only place that knows about Outlook source_id.
    5. Replace L283–592 with:
       ```typescript
       // Phase 82.2 D-A — thin ingest. Stage 0 fires unconditionally for
       // every inbound email; the Stage 1 worker (classifier-screen-worker)
       // owns regex classification + auto-actions + bulk-review queueing
       // after Stage 0 returns a `safe` verdict. See:
       //   docs/agentic-pipeline/README.md (5-stage funnel)
       //   docs/agentic-pipeline/stage-0-safety.md (Stage 0 first)
       const stage0Insert = await admin
         .from("automation_runs")
         .insert({
           automation: "debtor-email-review",
           status: "pending",
           swarm_type: "debtor-email",
           topic: null,
           entity: settings.entity,
           mailbox_id: mailboxId,
           result: {
             stage: "stage_0_safety_pending",
             message_id: messageId,
             source_mailbox: sourceMailbox,
             entity: settings.entity,
             subject: msg.subject,
             from: msg.from,
           },
           triggered_by: "zapier:ingest",
         })
         .select("id")
         .single();
       const stage0RunId = stage0Insert.data?.id;
       if (!stage0RunId) {
         // surface error audit row; return 500
       }

       await (inngest.send as unknown as SendFn)({
         name: "stage-0/email.received",
         data: {
           automation_run_id: stage0RunId,
           email_id: resolvedEmailId,
           message_id: messageId,
           source_mailbox: sourceMailbox,
           subject: msg.subject,
           body_text: msg.body,
           swarm_type: "debtor-email",
           entity: settings.entity,
           mailbox_id: mailboxId,
           from: msg.from,
           fromName: msg.fromName,
           receivedAt: msg.receivedAt || isoNow,
           safety_overridden: false,
         },
       });

       return NextResponse.json({
         action: "stage_0_dispatched",
         messageId,
         source_mailbox: sourceMailbox,
         entity: settings.entity ?? undefined,
       });
       ```
    6. The route MUST stay <350 lines after refactor (down from 675). If significantly longer, inline more.
    7. **Test against the existing route tests** (if any). `grep -rn "debtor-email/ingest" web/__tests__ web/app 2>/dev/null` to find them. Update any that assert on the old multi-branch action enum (`labeled`, `skipped_not_whitelisted`, etc.) — those actions are now Stage 1 worker concerns.

    **Replay-safety note:** API routes are NOT Inngest functions. The Stage 0 placeholder INSERT happens once per HTTP request — Zapier's at-most-once delivery + the route's idempotency-via-source_id-dedupe-on-email_pipeline.emails handles request retries. Document this with a comment referencing the existing carve-out at L286–292.
  </action>
  <verify>
    <automated>wc -l web/app/api/automations/debtor-email/ingest/route.ts | awk '{exit !($1<=350)}' && grep -q "stage-0/email.received" web/app/api/automations/debtor-email/ingest/route.ts && ! grep -q "categorizeEmail\|archiveEmail\|isWhitelistMatch" web/app/api/automations/debtor-email/ingest/route.ts</automated>
  </verify>
  <done>Route is ≤350 lines; fires stage-0/email.received; no longer imports/uses classify, categorizeEmail, archiveEmail, or whitelist machinery.</done>
</task>

<task type="auto">
  <name>Task 3: Align sales-email ingest field set</name>
  <files>web/app/api/automations/sales-email/ingest/route.ts</files>
  <action>
    Read sales-email/ingest/route.ts. Verify the existing `inngest.send({ name: "stage-0/email.received", data: { ... } })` at L233 passes the SAME field set debtor-email now passes (after Task 2). Specifically:
    - `automation_run_id` (sales-email may not create a placeholder — verify; if not, decide whether Plan 06 dispatch logic needs the placeholder for sales or whether sales is already fine without)
    - `entity`, `mailbox_id`, `from`, `fromName`, `receivedAt`, `safety_overridden: false`

    Make minimal changes — sales-email is already at 91% coverage; do NOT refactor it beyond aligning the payload field set. Document any divergence.
  </action>
  <verify>
    <automated>grep -A 30 "stage-0/email.received" web/app/api/automations/sales-email/ingest/route.ts | head -35</automated>
  </verify>
  <done>Sales-email stage-0/email.received payload field set matches debtor-email; any intentional divergence documented.</done>
</task>

<task type="auto">
  <name>Task 4: Type-check + lint the touched files</name>
  <files>(no file changes; verification only)</files>
  <action>
    Run `cd web && npx tsc --noEmit` to verify no TypeScript errors introduced. Run any project lint if available (`npm run lint` in web/).
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>No TypeScript errors; lint clean (or no lint configured).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zapier → Vercel ingest route | Existing body.auth secret check (preserved) |
| Vercel ingest route → Supabase | Service role INSERT into email_pipeline.emails + automation_runs |
| Vercel ingest route → Inngest | inngest.send stage-0/email.received |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82.2-07-01 | Elevation of Privilege | Stage 0 bypass (whitelisted email skips safety screen) | mitigate | THIS PHASE CLOSES IT — Stage 0 now fires unconditionally for every inbound debtor email |
| T-82.2-07-02 | Tampering | Hard cutover creates in-flight window where some emails follow old path | accept | Documented; backfill (Plan 09) covers historical; in-flight emails finish on old path within minutes |
| T-82.2-07-03 | Denial of Service | Thin route's automation_runs INSERT failure leaves Stage 0 un-fired | mitigate | Insert is awaited; failure returns 500 to Zapier (Zapier retry) |
| T-82.2-07-04 | Tampering | Replay of HTTP request creates duplicate placeholder automation_runs rows | mitigate | email_pipeline.emails source_id dedupe + Stage 0 worker's UPDATE-by-id semantics handle the duplicate (worst case: orphan pending row left behind, swept by automation-runs-sweeper) |
</threat_model>

<verification>
- Thin route ≤350 lines
- stage-0/email.received fired unconditionally (no `r.category === 'unknown'` gate)
- TypeScript clean
- Sales-email payload aligned
</verification>

<success_criteria>
- Whitelisted debtor emails now go through Stage 0
- Predicted-category non-unknown emails now go through Stage 0
- Stage 0 is the unconditional first step for every inbound email on every active swarm (RFC compliance)
</success_criteria>

<output>
After completion, create `.planning/phases/82.2-stage-0-telemetry-coverage-fix-v8-stabilisation/82.2-07-SUMMARY.md`
</output>
