---
phase: 82.2-stage-0-telemetry-coverage-fix-v8-stabilisation
plan: 12
type: execute
status: complete
requirements: [TELE-COV-01, TELE-COV-02, CONS-01]
---

# Plan 82.2-12 — 24h forward Stage-0 coverage verification

## Outcome — PASS

Verified at 2026-05-13 09:55 UTC (≈23.5h after Plan 08 deploy, ≈21h after Plan 11 backfill). All active mailboxes with non-zero Stage 1 traffic show **100% coverage** in the trailing 24h. Zero Stage 1 emails are missing their Stage 0 row.

## Coverage matrix

| Mailbox | Swarm | Stage 1 | Stage 0 | Coverage |
|---|---|---:|---:|---|
| debiteuren@smeba.nl | debtor-email | 18 | 18 | **100.0%** |
| debiteuren@smeba-fire.be | debtor-email | 14 | 14 | **100.0%** |
| debiteuren@berki.nl | debtor-email | 11 | 11 | **100.0%** |
| debiteuren@sicli-noord.be | debtor-email | 0 | 0 | n/a (no traffic) |
| debiteuren@sicli-sud.be | debtor-email | 0 | 0 | n/a (no traffic) |
| administratie@fire-control.nl | debtor-email | 71 | 71 | **100.0%** |
| verkoop@smeba.nl | sales-email | 27 | 27 | **100.0%** |
| **Total** | | **141** | **141** | **100.0%** |

Pre-Plan-07 baseline (Plan 02 diagnostic 2026-05-12): 53–63% coverage on debtor mailboxes, ~219-row 7-day gap. Post-deploy: 0-row gap in 24h. The D-A bug (gated `fireStage0Event()` in the pre-thin ingest) is structurally closed.

## Orphan check

```sql
SELECT e.mailbox, pe.swarm_type, COUNT(*) AS missing_stage0
FROM public.pipeline_events pe
JOIN email_pipeline.emails e ON e.id = pe.email_id
WHERE pe.stage = 1
  AND pe.created_at >= now() - interval '24 hours'
  AND NOT EXISTS (
    SELECT 1 FROM public.pipeline_events s0
    WHERE s0.email_id = pe.email_id AND s0.stage = 0 AND s0.swarm_type = pe.swarm_type
  )
GROUP BY 1,2;
```
Result: empty. Zero orphans.

## Smeba-fire observation

`debiteuren@smeba-fire.be` shows 14 stage1 + 14 stage0 rows — meaning the mailbox DOES have its own ingest path producing distinct pipeline_events rows. This contradicts yesterday's operator hypothesis that smeba-fire.be is forwarded to smeba.nl. The forwarding memory (`project_mailbox_forwarding.md`) was later updated by the operator (MEMORY.md line 14) to "Confirmed 2026-05-12 via duplicate cleanup queue rows with identical Outlook message_id" — i.e. forwarding exists at the Outlook level for SOME messages but smeba-fire still has its own ingest stream for others. Coverage looks healthy regardless.

## Plan 10 daily probe — separate issue

The `stage-0/coverage-probe` Inngest cron (`TZ=Europe/Amsterdam 0 9 * * 1-5`) did NOT fire at 07:00 UTC today and `pipeline_health` is still empty. Suspected cause: Inngest cron-registration sync lag after deploying a brand-new cron function. Not a Plan 12 verification blocker (coverage is computed live via the `stage0_coverage_24h` RPC) but worth surfacing as a Plan 10 follow-up. Manually invoking the probe from the Inngest dashboard once should kick it; subsequent weekday-09:00 ticks should fire normally.

## Phase 82.2 — closed

All five waves complete. Production state:

- Pre-existing 280 duplicate `pipeline_events` groups deduped (Plan 01).
- Partial UNIQUE index `pipeline_events_one_per_stage_email` live (Plan 01).
- `pipeline_health` table + `stage0_coverage_24h` / `stage0_backfill_candidates` RPCs live (Plan 03).
- Stage-0 safety worker refactored to single-emit with `emit_source` discriminator (Plan 04).
- Injection_suspected regression coverage in place (Plan 05).
- Stage-1 worker (classifier-screen-worker) owns debtor-email dispatch (Plan 06).
- Debtor-email ingest route thinned to dispatcher only; 675 → 346 lines (Plan 07).
- Production smoke approved on 5 emails including D-A whitelist gate (Plan 08).
- 269 historical gap rows backfilled with honest `unknown_legacy` markers (Plan 11).
- 24h forward coverage = 100% across all active mailboxes with traffic (this plan).
