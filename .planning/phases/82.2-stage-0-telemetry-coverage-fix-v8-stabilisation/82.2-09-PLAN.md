---
phase: 82.2-stage-0-telemetry-coverage-fix-v8-stabilisation
plan: 09
type: execute
wave: 4
depends_on: [82.2-03, 82.2-08]
files_modified:
  - web/lib/inngest/functions/stage-0-backfill.ts
  - web/lib/inngest/functions/__tests__/stage-0-backfill.test.ts
  - web/app/api/inngest/route.ts
autonomous: true
requirements: [TELE-COV-03]
must_haves:
  truths:
    - "An Inngest one-shot function backfills missing Stage 0 pipeline_events rows for the last 30 days"
    - "Source priority is automation_runs (reconstruct from result.verdict) → unknown_legacy marker"
    - "Function is replay-safe (Phase 65 rule); UUIDs/timestamps generated inside step.run"
    - "Idempotent — re-running on a window with no remaining gaps writes zero rows"
    - "Batches at 500 rows per step.run; one bulk INSERT per batch"
  artifacts:
    - path: "web/lib/inngest/functions/stage-0-backfill.ts"
      provides: "Inngest one-shot backfill triggered by pipeline.stage0.backfill"
      contains: "pipeline.stage0.backfill"
    - path: "web/lib/inngest/functions/__tests__/stage-0-backfill.test.ts"
      provides: "Unit tests for row construction + idempotency"
    - path: "web/app/api/inngest/route.ts"
      provides: "Registered stage0Backfill function"
      contains: "stage0Backfill"
  key_links:
    - from: "stage-0-backfill.ts"
      to: "public.stage0_backfill_candidates RPC"
      via: "admin.rpc('stage0_backfill_candidates', { window_days: 30 })"
      pattern: "stage0_backfill_candidates"
    - from: "stage-0-backfill.ts"
      to: "public.pipeline_events"
      via: "admin.from('pipeline_events').insert with triggered_by='backfill'"
      pattern: "triggered_by.*backfill"
---

<objective>
Create the Inngest one-shot backfill function per D-03/D-04/D-05. Reads the gap-finding RPC from Plan 03, reconstructs Stage 0 rows from `automation_runs.result` where possible, writes `decision='unknown_legacy'` as honest marker otherwise. Replay-safe, idempotent, 500-row batched.

Purpose: D-03, D-04, D-05. Closes the 30-day historical gap so the V9 Learning Inbox has complete per-email traces for the corpus window.

Output: One new Inngest function + tests + serve() registration. Plan 11 triggers it manually after this deploys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82.2-stage-0-telemetry-coverage-fix-v8-stabilisation/82.2-CONTEXT.md
@.planning/phases/82.2-stage-0-telemetry-coverage-fix-v8-stabilisation/82.2-RESEARCH.md
@CLAUDE.md
@web/lib/inngest/functions/classifier-backfill.ts
@web/lib/inngest/functions/labeling-flip-cron.ts
@web/app/api/inngest/route.ts
@web/lib/pipeline-events/emit.ts

<interfaces>
**Function shape (RESEARCH §Inngest function shapes, lines 344–459 — use that pseudocode verbatim as starting point):**

```typescript
// web/lib/inngest/functions/stage-0-backfill.ts
// Phase 82.2 D-03/D-04/D-05 — one-shot backfill.
// Triggered manually via:
//   inngest.send({ name: 'pipeline.stage0.backfill', data: { window_days?: 30 } })

import { inngest } from "@/lib/inngest/client";
import { createAdminClient } from "@/lib/supabase/admin";

const BATCH_SIZE = 500;

type SendFn = (p: { name: string; data: Record<string, unknown> }) => Promise<unknown>;

export const stage0Backfill = inngest.createFunction(
  { id: "stage-0/backfill", retries: 1 },
  { event: "pipeline.stage0.backfill" },
  async ({ event, step }) => { /* per RESEARCH §pseudocode */ }
);
```

**Row-construction logic (RESEARCH §Row construction logic per source):**
| automation_runs found? | result.verdict | Stage 0 row |
|---|---|---|
| Yes, stage='stage_0_safety' | 'safe' | decision='safe', triggered_by='backfill', created_at=ar.completed_at |
| Yes, stage='stage_0_safety' | 'injection_suspected' | decision='injection_suspected', same shape |
| Yes, stage='stage_0_safety_pending' | n/a | decision='unknown_legacy', decision_details.reason='pending_orphan' |
| No automation_runs row | n/a | decision='unknown_legacy', decision_details.reason='no_source_record', created_at = stage 1 row's created_at − 1 second |

**decision_details template:**
```json
{
  "source": "backfill",
  "backfill_run_id": "<runId>",
  "original_automation_run_id": "<id or null>",
  "regex_matched": "<from result or null>",
  "llm_reason": "<from result or null>",
  "matched_span": "<from result or null>",
  "reason": "<for unknown_legacy only>"
}
```

**Why bypass emitPipelineEvent helper (RESEARCH Risk #4):** the helper at `web/lib/pipeline-events/emit.ts` doesn't accept `created_at` overrides. Backfill writes the historical timestamp explicitly. INSERT direct via `admin.from('pipeline_events').insert(...)` is the correct path. Comment this in the file.

**Idempotency (RESEARCH §Schema facts + Plan 01 index):**
- The partial UNIQUE index from Plan 01 prevents duplicates DB-side.
- Defense-in-depth: pre-insert SELECT to skip rows already covered (handles race between gap query and insert; also handles re-runs gracefully without emitting unique-violation errors).

**Phase 65 replay-id rule (CLAUDE.md):**
- `runId = await step.run("resolve-run-id", () => crypto.randomUUID())` — REQUIRED, not as a top-level const.
- Any synthetic `created_at` fallback uses `await step.run("resolve-fallback-ts", ...)` OR derives deterministically from `automation_runs.completed_at` (DB-derived is OK).

**this-binding (CLAUDE.md commit dae6276):** never destructure `inngest.send`. Not relevant in this function since the function doesn't emit downstream events. Note for future maintainers.

**Registration:** add to `web/app/api/inngest/route.ts` in the `serve({ functions: [...] })` call.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write backfill unit tests (RED)</name>
  <files>web/lib/inngest/functions/__tests__/stage-0-backfill.test.ts</files>
  <behavior>
    - Test 1: Single candidate with `result.verdict='safe'` and `completed_at` set → produces one pipeline_events row with decision='safe', triggered_by='backfill', decision_details.source='backfill', decision_details.original_automation_run_id, created_at=ar.completed_at
    - Test 2: Single candidate with `result.verdict='injection_suspected'` → produces one row with decision='injection_suspected'
    - Test 3: Single candidate with `result.stage='stage_0_safety_pending'` (pending orphan) → produces one row with decision='unknown_legacy', decision_details.reason='pending_orphan'
    - Test 4: Single candidate with no automation_runs row at all (null result, null completed_at) → produces one row with decision='unknown_legacy', decision_details.reason='no_source_record'
    - Test 5: 1200 candidates → produces 3 batches (500 + 500 + 200); written count matches
    - Test 6 (idempotency): Pre-insert SELECT returns existing rows for half the batch → only the missing half is inserted; skipped count = half
    - Test 7 (replay-safety): runId is generated inside step.run (verify by capturing the step.run name)
    - Test 8 (window clamp): event.data.window_days=99 → RPC called with 30 (or whatever clamp logic decides per Plan 03 RPC)
  </behavior>
  <action>
    Match the existing `__tests__/` Vitest pattern. Mock supabase admin to capture both `rpc` calls and `insert` calls. Mock `crypto.randomUUID` to return a deterministic value for assertion. Mock `step.run` as a pass-through invoker.

    Tests fail until Task 2 (RED).
  </action>
  <verify>
    <automated>cd web && npx vitest run lib/inngest/functions/__tests__/stage-0-backfill.test.ts 2>&1 | tail -3</automated>
  </verify>
  <done>Tests file exists with 8 cases; current state has no implementation file so import-resolution fails (acceptable RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement stage-0-backfill.ts (GREEN)</name>
  <files>web/lib/inngest/functions/stage-0-backfill.ts</files>
  <action>
    Implement per RESEARCH §Inngest function shapes pseudocode (lines 344–459). Key elements:

    1. **Function id `stage-0/backfill`**, event `pipeline.stage0.backfill`, retries: 1.
    2. **Resolve runId inside step.run** (Phase 65).
    3. **Resolve windowDays** from event.data with default 30. The RPC clamps server-side; the function also clamps client-side as defense (`Math.min(30, Math.max(1, eventData.window_days ?? 30))`).
    4. **step.run("find-gaps")** calls `admin.rpc('stage0_backfill_candidates', { window_days: windowDays })`. Throw on error.
    5. **Loop batches**: `for (let i = 0; i < candidates.length; i += BATCH_SIZE)`. Inside each step.run(`backfill-batch-${batchIdx}`):
       - Pre-insert SELECT for existing rows in batch keys
       - Build `toInsert` array using `buildBackfillRow(candidate, runId)` helper
       - Bulk INSERT via `admin.from('pipeline_events').insert(toInsert)` — bypassing `emitPipelineEvent` helper (comment why)
       - Return `{ inserted, skipped }`
    6. **buildBackfillRow helper**: branch on `result.verdict` and `result.stage` per RESEARCH row-construction table.
    7. **Return shape**: `{ run_id, candidates_count, written, skipped }`.
    8. Header comment referencing CONTEXT D-03/D-04/D-05 + RESEARCH §Inngest function shapes.

    All Task 1 tests pass.
  </action>
  <verify>
    <automated>cd web && npx vitest run lib/inngest/functions/__tests__/stage-0-backfill.test.ts</automated>
  </verify>
  <done>All 8 backfill tests pass; function file exists; replay-safety rules satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Register stage0Backfill in inngest serve route</name>
  <files>web/app/api/inngest/route.ts</files>
  <action>
    Add the import + add `stage0Backfill` to the `functions: [...]` array in `serve(...)`. Verify the registration order matches existing convention (alphabetical or by-domain — match what's already there).

    Run `cd web && npx tsc --noEmit` to confirm clean type-check.
  </action>
  <verify>
    <automated>grep -q "stage0Backfill" web/app/api/inngest/route.ts && cd web && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>Function registered; type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Inngest | Manual event trigger (pipeline.stage0.backfill) |
| Inngest function → Supabase | Service-role bulk INSERT into pipeline_events |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82.2-09-01 | Tampering | Replay duplicates Stage 0 rows | mitigate | Partial UNIQUE index (Plan 01) + pre-insert SELECT |
| T-82.2-09-02 | Tampering | Backfill writes 'safe' for an email that was actually unsafe | mitigate | Reconstruction reads result.verdict directly; if missing, writes 'unknown_legacy' (honest marker) |
| T-82.2-09-03 | Repudiation | Backfill rows indistinguishable from live rows in audit | mitigate | triggered_by='backfill' discriminator + decision_details.source='backfill' make every row distinguishable |
| T-82.2-09-04 | Denial of Service | Large window crashes function | mitigate | RPC + client both clamp to 30 days; 500-row batches keep step.run under Vercel maxDuration |
| T-82.2-09-05 | Information Disclosure | backfill_run_id leaks | accept | Opaque UUID; no security claim |
</threat_model>

<verification>
- 8 unit tests pass
- Function registered in inngest serve
- TypeScript clean
</verification>

<success_criteria>
- Backfill function deployable and ready for Plan 11 manual trigger
- Idempotent on re-run
</success_criteria>

<output>
After completion, create `.planning/phases/82.2-stage-0-telemetry-coverage-fix-v8-stabilisation/82.2-09-SUMMARY.md`
</output>
