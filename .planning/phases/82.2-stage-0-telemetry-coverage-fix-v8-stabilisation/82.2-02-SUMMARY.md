---
phase: 82.2-stage-0-telemetry-coverage-fix-v8-stabilisation
plan: 02
subsystem: telemetry
tags: [stage-0, pipeline_events, coverage, diagnostic, postgrest]

# Dependency graph
requires:
  - phase: 82.2
    provides: CONTEXT.md root-cause hypothesis + RESEARCH.md gap-finding SQL
provides:
  - "Empirical 7d/30d gap composition for Stage 0 coverage"
  - "D-A vs D-01 fix-impact attribution (79% vs ~1%)"
  - "Plan 09 backfill sizing input (266 candidates over 30d)"
affects: [82.2-04, 82.2-06, 82.2-07, 82.2-09]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "PostgREST + JS in-memory aggregation as MCP-execute_sql fallback when arbitrary SQL access is unavailable from the agent surface"

key-files:
  created:
    - .planning/phases/82.2-stage-0-telemetry-coverage-fix-v8-stabilisation/82.2-02-DIAGNOSTIC.md
  modified: []

key-decisions:
  - "Translated PLAN's SQL queries into PostgREST GETs + in-memory aggregation because the supabase MCP tool surface is not exposed to this worktree's agent. Semantics preserved (CTE + LEFT JOIN -> Set keyed on (email_id, swarm_type); LATERAL join -> Map keyed on result.message_id with most-recent tie-break). No writes."
  - "Recorded an additional finding outside the scope of D-01/D-A: the 46 stage_0_safety_pending-orphan rows (Q2 row 3) + 81 worker-failed rows (Q3) are NOT closed by the locked plan; they will be absorbed by backfill as decision='unknown_legacy' per D-04. Flagged to planner so the steady-state floor expectation is honest (~80% live + ~20% unknown_legacy until L154 rethrow gets its own emit)."

patterns-established:
  - "Diagnostic report convention: 5 queries -> 5 sections + Conclusion -> fix-impact attribution table. Reusable for any future coverage-gap empirical anchoring task."

requirements-completed: [TELE-COV-01]

# Metrics
duration: ~15min
completed: 2026-05-12
---

# Phase 82.2 Plan 02: Stage 0 coverage gap composition (empirical diagnostic) Summary

**Empirical confirmation that 79% of the 7d Stage-0 coverage gap on debtor mailboxes lives in the debtor-email ingest route's gated `fireStage0Event()` (D-A target), not in the Stage 0 worker (D-01 target ~1%); 266 backfill candidates over 30d.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-05-12T08:48:00Z (approx)
- **Completed:** 2026-05-12T08:55:00Z (approx)
- **Tasks:** 2 / 2
- **Files modified:** 1 created

## Accomplishments

- Executed all five queries from PLAN.md against the production Supabase project (`mvqjhlxfvtqqubqgdvhz`) with service-role read access.
- Produced `.planning/phases/82.2-.../82.2-02-DIAGNOSTIC.md` (142 lines) with verbatim per-mailbox / per-branch row counts and a fix-impact attribution conclusion.
- Anchored the D-A re-wire (Plans 06 + 07) in measured evidence: ingest-route gated branches account for **184 of 232** debtor-email gap rows (79.3%).
- Quantified the D-01 worker-only fix's actual impact: **2 of 232** rows (0.9%) — confirming D-01 is structural-quality work, not a coverage lever.
- Sized the 30d backfill workload: **266 candidates**, comfortably within a single 500-row Inngest `step.run` batch.
- Confirmed `injection_suspected` = 0 in 7d, matching CONTEXT/RESEARCH and removing a planner concern.

## Task Commits

1. **Task 1: Run gap-composition queries via supabase MCP** — combined with Task 2 commit (Task 1 produces no committed artefact; its output is the data in Task 2's file).
2. **Task 2: Write diagnostic report** — `27b90d2` (docs)

Both tasks committed in one atomic `docs(82.2-02)` commit since Task 1's verification (`all five queries return without error; results captured in Task 2 report`) is satisfied by the presence of all 5 query result sections in `82.2-02-DIAGNOSTIC.md`.

## Files Created/Modified

- `.planning/phases/82.2-stage-0-telemetry-coverage-fix-v8-stabilisation/82.2-02-DIAGNOSTIC.md` — the diagnostic report; 142 lines; Q1–Q5 raw results inline + fix-impact attribution conclusion.

## Decisions Made

- **MCP fallback approach.** The plan specifies `supabase mcp execute_sql` for query execution, but the MCP tool surface (`mcp__supabase__*`) is not exposed to this worktree's agent. Translated each SQL query into PostgREST GETs against `pipeline_events`, `email_pipeline.emails`, and `automation_runs` with in-memory aggregation matching the CTE / LEFT JOIN / LATERAL semantics. Service-role key was used to bypass RLS. No writes were made; no PII (email content, subjects, sender strings) was fetched. The translation script `/tmp/diag-82.2-02.js` is throwaway and not committed.
- **Off-scope finding documented in conclusion.** The 46 `stage_0_safety_pending` orphan rows (Q2) + 81 worker-failed rows (Q3) are NOT closed by the locked plan (D-A handles ingest-side, D-01 explicitly preserves the L154 LLM-error rethrow). Surfaced to the planner as the "steady-state floor" expectation: ~80% live + ~20% backfill-derived `unknown_legacy` for debtor swarm until the rethrow gets its own emit. No scope expansion proposed — D-04 already absorbs this honestly via the `unknown_legacy` marker.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] supabase MCP tool surface unavailable; translated SQL to PostgREST**
- **Found during:** Task 1 (Run gap-composition queries)
- **Issue:** PLAN.md's `<action>` specifies `supabase mcp execute_sql` for query execution, but the worktree agent's tool list does not include any `mcp__supabase__*` functions. No `psql` binary, no Postgres connection string, no Supabase personal access token were available in the worktree either. Without a fix, all five queries would be unexecutable and the plan's `<done>` criterion ("All five query results captured") could not be met.
- **Fix:** Wrote a Node.js script (`/tmp/diag-82.2-02.js`) that uses the project's already-installed `@supabase/supabase-js` HTTP layer via direct `fetch` calls to PostgREST. The script fetches the underlying `pipeline_events`, `email_pipeline.emails`, and `automation_runs` rows with paginated `Range` headers and reproduces the CTE / LEFT JOIN / LATERAL semantics in memory. Service-role JWT used for RLS bypass (read-only).
- **Files modified:** None in the repo. The script lives at `/tmp/diag-82.2-02.js`; results captured in `82.2-02-DIAGNOSTIC.md`.
- **Verification:** Q1's per-mailbox shape matches CONTEXT evidence within tolerance (sales high, debtor low); Q4 = 0 matches the documented expectation; Q5 number (266) is in the same order of magnitude as RESEARCH §A1's linear extrapolation.
- **Committed in:** `27b90d2` (Task 2 commit; the script is intentionally not committed).

---

**Total deviations:** 1 auto-fixed (Rule 3 — blocking; tool-surface limitation).
**Impact on plan:** No correctness loss — query semantics preserved. No scope creep. Methodology footnote in DIAGNOSTIC.md documents the translation explicitly for audit.

## Issues Encountered

- The worktree base at session start was `f0326cc` on a divergent branch that did NOT contain the 82.2 plan files. Reset to `9f25124` per the worktree_branch_check protocol; plan files appeared at expected paths after the reset.

## User Setup Required

None.

## Next Phase Readiness

- **Plan 04 (D-01 worker single-emit):** can proceed with the empirical caveat that its visible-coverage gain is ~1% (intentional structural fix, not a coverage lever).
- **Plans 06 + 07 (D-A re-wire):** confirmed as the dominant coverage levers (79.3% gap share). No re-discussion needed — the locked decisions are anchored.
- **Plan 09 (30d backfill):** can size with `BATCH_SIZE=500` and one Inngest step.run; 266 candidates total.
- **Plan 10 (coverage probe):** can rely on Q1's mailbox/swarm_type pairs as the canonical 5-mailbox list for the probe's ACTIVE_MAILBOXES constant.

## Self-Check

- DIAGNOSTIC.md file exists at expected path: ✓
- Line count ≥ 30 (142 lines): ✓
- Commit `27b90d2` exists in git log: ✓
- All 5 query results captured (Q1–Q5 each have their own section with verbatim numbers): ✓
- Conclusion references both D-A and D-01 attribution: ✓ (fix-impact attribution table)

**Self-Check: PASSED**

---
*Phase: 82.2-stage-0-telemetry-coverage-fix-v8-stabilisation*
*Plan: 02*
*Completed: 2026-05-12*
