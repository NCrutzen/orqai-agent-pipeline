---
phase: 82.2-stage-0-telemetry-coverage-fix-v8-stabilisation
plan: 11
type: execute
status: complete
requirements: [TELE-COV-03]
---

# Plan 82.2-11 — Manual production backfill

## Trigger details

- **Invoked**: 2026-05-12 13:13 UTC (Inngest dashboard → `stage-0/backfill` → Invoke)
- **Payload**: `{"data": {"window_days": 30}}`
- **Run id**: `becd4724-7b6c-4cd8-b79b-c7905bcaf715`
- **Deploy on which it ran**: `cb01317` (Wave 4 push, Vercel deploy `ejndjg0qh`)

## Run 1 output

```json
{
  "run_id": "becd4724-7b6c-4cd8-b79b-c7905bcaf715",
  "candidates_count": 269,
  "written": 269,
  "skipped": 0
}
```

(Plan 02 diagnostic measured ~266 candidates one day prior; 3 additional gaps accumulated in the intervening 24h before Plan 07 deploy.)

## Verification (Plan 11 Task 2)

### Row signatures

All 269 rows have `triggered_by = 'backfill'` and `decision = 'unknown_legacy'` — the D-04 honest marker for rows where automation_runs reconstruction wasn't possible.

### Reason breakdown

| `decision_details.reason` | Rows | Interpretation |
|---|---:|---|
| `pending_orphan` | 32 | Had an `automation_runs` row at stage_0_safety / stage_0_safety_pending that never completed. Reconstructed `created_at` from `automation_runs.completed_at`. Real historical timestamps (2026-05-07 ×1, 2026-05-11 ×31). |
| `no_source_record` | 237 | Stage 1 traffic existed but NO `automation_runs` row at Stage 0 at all. These are the D-A bug victims — the gated `fireStage0Event()` call never fired pre-Plan-07. `created_at` falls back to `now()` per row-builder defensive fallback; original email's `received_at` is preserved in `email_pipeline.emails`. |

`no_source_record:pending_orphan` ratio 237:32 ≈ 88:12, consistent with Plan 02 diagnostic which measured D-A target branches (icontroller_delete, skipped_not_whitelisted, skipped_disabled) as 79.3% of the debtor gap.

### Idempotency (run 2)

```json
{
  "run_id": "118c1a5e-1ed8-4052-b95f-1fa392f3be6b",
  "candidates_count": 0,
  "written": 0,
  "skipped": 0
}
```

Re-invoked the same backfill ~2 minutes after run 1. **Zero candidates** in the 30-day window — meaning run 1 closed the entire pre-deploy gap, AND no new gaps have accumulated since Plan 07's thin-ingest deploy (10:30 UTC today). This is the load-bearing structural proof the phase set out to provide: with Plan 07 live, Stage 0 fires on every inbound email regardless of which branch downstream auto-action takes.

## Race protection

Deploy was live ~3h before backfill ran (Plan 08 smoke deployed at 10:30 UTC; backfill at 13:13 UTC) — well past the ≥30-minute wait. Plus the partial UNIQUE index would absorb any race-write at the DB level.

## Notes

- The 237 `no_source_record` rows' `created_at` clustering at the backfill run time is a deviation from Plan 11 Task 2.3's expectation of varied timestamps. The deviation is by design (D-04 + row-builder fallback) and the original email arrival times are still observable via `email_pipeline.emails.received_at` for any downstream consumer.
- Plan 12 (24h forward coverage check) is now unblocked.
