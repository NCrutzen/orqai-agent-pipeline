---
phase: 82.2-stage-0-telemetry-coverage-fix-v8-stabilisation
plan: 04
type: execute
wave: 2
depends_on: [82.2-01]
files_modified:
  - web/lib/inngest/functions/stage-0-safety-worker.ts
  - web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts
autonomous: true
requirements: [TELE-COV-01, TELE-COV-05]
must_haves:
  truths:
    - "Stage 0 safety worker emits exactly one pipeline_events row per invocation across all three sub-types (operator-override, budget-breach, main-path)"
    - "The override branch (L86–105) and budget-breach branch (L167–182) no longer skip emitPipelineEvent"
    - "decision_details.emit_source ∈ {operator-override, budget-breach, main-path} distinguishes the sub-types (D-02)"
    - "Safety verdict logic (regex, llm, budget) is unchanged"
  artifacts:
    - path: "web/lib/inngest/functions/stage-0-safety-worker.ts"
      provides: "Single-emit refactored worker"
      contains: "emit_source"
    - path: "web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts"
      provides: "Updated assertions for single-emit invariant + sub-type discriminator"
  key_links:
    - from: "stage-0-safety-worker.ts override branch"
      to: "emitPipelineEvent"
      via: "fall-through to single emit at function exit"
      pattern: "emit_source.*operator-override"
    - from: "stage-0-safety-worker.ts budget-breach branch"
      to: "emitPipelineEvent"
      via: "fall-through to single emit at function exit"
      pattern: "emit_source.*budget-breach"
---

<objective>
Refactor `web/lib/inngest/functions/stage-0-safety-worker.ts` so all three code paths fall through to exactly ONE `emitPipelineEvent` call at function exit. Closes the worker-side telemetry gaps in the `safety_overridden` short-circuit (L86–105) and budget-breach branch (L167–182) without changing what the worker decides.

Purpose: D-01 + D-02. The structural fix removes the class of bug (per-branch emit responsibility) — future branches added to the worker cannot forget to emit. RESEARCH §Worker emit-branch analysis confirmed the main-path emit at L255 already fires correctly on both `safe` and `injection_suspected`; only the override + budget-breach branches need emit coverage.

Output: Refactored worker file + updated test file. All existing test assertions still pass; new assertions cover the two newly-emitting branches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82.2-stage-0-telemetry-coverage-fix-v8-stabilisation/82.2-CONTEXT.md
@.planning/phases/82.2-stage-0-telemetry-coverage-fix-v8-stabilisation/82.2-RESEARCH.md
@CLAUDE.md
@docs/agentic-pipeline/stage-0-safety.md
@web/lib/inngest/functions/stage-0-safety-worker.ts
@web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts
@web/lib/pipeline-events/emit.ts
@web/lib/pipeline-events/types.ts

<interfaces>
**Existing branch structure (verified by RESEARCH §Worker emit-branch analysis):**

| Branch | Lines | Today's emit |
|--------|-------|--------------|
| `safety_overridden=true` | L86–105 | inngest.send(classifier/screen.requested) only; NO pipeline_events |
| Budget breach | L167–182 | inngest.send(pipeline/budget_breached) only; NO pipeline_events |
| Main path | L184–298 | emitPipelineEvent at L255 inside step.run("persist-verdict"); fires for both safe + injection_suspected |

**Refactor target shape (RESEARCH §Single-emit refactor):**
```typescript
type VerdictSource =
  | { kind: "operator-override" }
  | { kind: "budget-breach"; budget: BudgetState; reason: string }
  | { kind: "main-path"; verdict: "safe" | "injection_suspected"; regex_matched: boolean; llm_reason: string; matched_span: string|null; cost_cents: number; strip_changed: boolean; strip_delta_chars: number; strip_fallback_reason: string|null };

// Each branch sets verdictSource + optionally downstreamEmit, then falls through
// to a SINGLE step.run("persist-and-emit", ...) at the bottom that:
//   1. Writes automation_runs (unchanged per-branch semantics)
//   2. Calls emitPipelineEvent with decision_details.emit_source = verdictSource.kind
//   3. Sends downstreamEmit if non-null
```

**Decision mapping (D-02 preservation):**
- operator-override → `decision='safe'`, `decision_details.emit_source='operator-override'`, `decision_details.safety_overridden=true`
- budget-breach → `decision='over_budget'`, `decision_details.emit_source='budget-breach'`, `decision_details.reason=budgetVerdict.reason`, `decision_details.budget=budgetState`
- main-path → `decision=llmResult.verdict`, `decision_details.emit_source='main-path'`, plus existing fields (regex_matched, llm_reason, matched_span, safety_overridden=false, strip_*)

**Replay safety:** the single step.run boundary MUST contain both the automation_runs write AND the emitPipelineEvent call (mirroring today's L217–275 pattern). Inngest replay then atomically retries both.

**Test file pattern (verified):** vitest with mocked step.run as `vi.fn(async (_name, fn) => fn())`. Existing test at L249–254 asserts main-path emit; new tests assert override + budget-breach emits.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend test file with single-emit + sub-type assertions (RED)</name>
  <files>web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts</files>
  <behavior>
    - Test 1 (existing): main-path safe verdict emits pipeline_events with decision='safe', decision_details.emit_source='main-path' [extend existing assertion]
    - Test 2 (existing): main-path injection_suspected emits decision='injection_suspected', decision_details.emit_source='main-path' [extend existing assertion]
    - Test 3 (new): operator-override (safety_overridden=true) input emits exactly one pipeline_events row with decision='safe', decision_details.emit_source='operator-override', decision_details.safety_overridden=true AND sends classifier/screen.requested
    - Test 4 (new): budget-breach (mock budgetCheck → breached) emits exactly one pipeline_events row with decision='over_budget', decision_details.emit_source='budget-breach', AND sends pipeline/budget_breached AND does NOT send classifier/screen.requested
    - Test 5 (new) — invariant: in all four cases above, the captured pipeline_events insert array has exactly length 1 (single-emit invariant)
  </behavior>
  <action>
    Add a new `describe("single-emit refactor (Phase 82.2 D-01/D-02)", ...)` block. Use the existing mock-step pattern. Mock supabase admin to capture pipeline_events inserts in an array.

    These tests will FAIL against the current worker (override + budget-breach branches don't emit). That's the RED state — verifies the test catches the bug.
  </action>
  <verify>
    <automated>cd web && npx vitest run lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts -t "single-emit refactor" 2>&1 | grep -E "FAIL|✗" | head -5</automated>
  </verify>
  <done>New tests added; running them against the unchanged worker shows failures on Tests 3, 4, 5 (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor worker to single-emit pattern (GREEN)</name>
  <files>web/lib/inngest/functions/stage-0-safety-worker.ts</files>
  <action>
    Apply the RESEARCH §Single-emit refactor pseudocode (lines 124–201). Concrete steps:

    1. **Define `type VerdictSource`** at top of function body (shape per interfaces above).

    2. **Override branch (L86–105):** instead of returning `{ skipped: "safety_overridden" }`, set:
       - `verdictSource = { kind: "operator-override" }`
       - `downstreamEmit = { name: "classifier/screen.requested", data: { ...current L88–101 payload } }`
       - Fall through (do NOT return early).

    3. **Budget-breach branch (L167–182):** instead of returning `{ halted: true }`, set:
       - `verdictSource = { kind: "budget-breach", budget: budgetState, reason: budgetVerdict.reason ?? "budget breach" }`
       - `downstreamEmit = { name: "pipeline/budget_breached", data: { ...current L170–177 payload } }`
       - Fall through.

    4. **Main-path branch (L184–298):** set `verdictSource = { kind: "main-path", verdict: llmResult.verdict, regex_matched: regexResult.matched, ... }`. For `safe` verdict, set `downstreamEmit = { name: "classifier/screen.requested", data: { ...L280–296 payload } }`. For `injection_suspected`, leave `downstreamEmit = null`.

    5. **Single `step.run("persist-and-emit", ...)` at function exit** consolidates:
       - The automation_runs UPDATE / INSERT (today's L217–252) — gated on verdictSource.kind:
         - operator-override → SKIP automation_runs write (preserve current behavior: override branch today writes nothing to automation_runs). Document with a comment.
         - budget-breach → write `status='failed'`, `topic=null`, `error_message=verdictSource.reason`, `result={ stage: 'stage_0_safety', budget: verdictSource.budget }` (preserve today's budget-breach-handler contract; verify against `budget-breach-handler.ts` so it doesn't double-write).
         - main-path → existing UPDATE/INSERT logic from L218–252 unchanged.
       - **emitPipelineEvent — ALWAYS called.** decision + decision_details computed from verdictSource per the mapping table above. `triggered_by='pipeline'`. `email_id` from event data. `swarm_type` from event data. ON CONFLICT semantics: rely on the partial UNIQUE index from Plan 01 — wrap the call in try/catch for unique-violation; treat as idempotent success (replay-safe).
       - `downstreamEmit` send if non-null. **Same step.run for replay atomicity.**

    6. **Eliminate the two early returns.** Replace with a single `return verdictSource` (or equivalent shape satisfying existing return-value tests).

    7. **Keep:** `emitAutomationRunStale`, `step.run("strip-quoted-history")`, `step.run("regex-screen")`, `step.run("llm-verdict")`, `step.run("check-budget")` — only their fall-through behavior changes, not their internals.

    8. **Do NOT modify:** `llmInjectionVerdict`, `regexScreen`, `budgetCheck`, `stripQuotedHistory`, `emitPipelineEvent` helper. Verdict logic is out of scope (CONTEXT must_haves: "No regression in Stage 0 safety verdict logic").

    9. **Replay-safety check (CLAUDE.md Phase 65 / commit dd2583a):** `completedAt = new Date().toISOString()` MUST stay inside the step.run (it already is at L199, but verify after refactor that it didn't get hoisted).

    10. **this-binding check (CLAUDE.md commit dae6276):** `inngest.send` must NOT be destructured anywhere new. Use the existing pattern (direct method call) or `(inngest.send as unknown as SendFn)({...})` if needed.

    After refactor, run the test file. Tests 1–5 should all pass (GREEN).
  </action>
  <verify>
    <automated>cd web && npx vitest run lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts</automated>
  </verify>
  <done>Worker refactored; all tests in stage-0-safety-worker.test.ts pass; the two early returns at L104 and L181 are gone; emitPipelineEvent is called from exactly one site.</done>
</task>

<task type="auto">
  <name>Task 3: Grep-audit for forgotten emits + downstream consumer check</name>
  <files>(read-only audit)</files>
  <action>
    1. `grep -n "emitPipelineEvent" web/lib/inngest/functions/stage-0-safety-worker.ts` → expect exactly ONE match (the single emit at function exit). If >1, refactor is incomplete.

    2. `grep -n "return.*skipped\|return.*halted" web/lib/inngest/functions/stage-0-safety-worker.ts` → expect zero matches (early returns eliminated).

    3. Grep downstream consumers of `decision_details` for `pipeline_events.stage=0` to verify the new `emit_source` field is non-breaking (RESEARCH A6 assumption):
       ```bash
       grep -rn "decision_details" web/lib/ web/app/api/ --include="*.ts" | grep -i "stage.*0\|safety" | head -20
       ```
       For each consumer, verify it reads `decision_details` as free-form jsonb (not a typed shape requiring exhaustive keys). Document findings.

    4. Run the full Inngest test suite to confirm no regression: `cd web && npx vitest run lib/inngest/functions/__tests__/`
  </action>
  <verify>
    <automated>cd web && npx vitest run lib/inngest/functions/__tests__/ 2>&1 | tail -3</automated>
  </verify>
  <done>Single emit confirmed; no early returns; downstream consumers unaffected by decision_details.emit_source addition; full Inngest test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inngest worker → Supabase | Service-role writes (pipeline_events, automation_runs) |
| Inngest worker → Inngest | downstreamEmit (classifier/screen.requested OR pipeline/budget_breached) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82.2-04-01 | Tampering | Replay double-write of pipeline_events | mitigate | Partial UNIQUE index (Plan 01) + try/catch on unique violation in the single step.run |
| T-82.2-04-02 | Tampering | Refactor accidentally drops automation_runs write on budget-breach | mitigate | Test 4 asserts automation_runs.status='failed' written; manual grep against budget-breach-handler.ts to avoid double-write |
| T-82.2-04-03 | Repudiation | downstreamEmit fires but pipeline_events insert fails (or vice versa) | mitigate | Both inside same step.run — Inngest replays atomically; UNIQUE index makes pipeline_events idempotent; downstreamEmit is fire-and-forget per Inngest semantics |
| T-82.2-04-04 | Information Disclosure | new decision_details.emit_source leaks to a consumer that fails closed on unknown keys | mitigate | Task 3 grep-audit verifies all consumers read decision_details as free-form jsonb (RESEARCH A6) |
</threat_model>

<verification>
- All tests in stage-0-safety-worker.test.ts pass
- Full lib/inngest/functions/__tests__/ suite passes
- Exactly one emitPipelineEvent call site in stage-0-safety-worker.ts
- Zero `return { skipped: ... }` or `return { halted: ... }` patterns
</verification>

<success_criteria>
- Override + budget-breach branches now write pipeline_events
- decision_details.emit_source distinguishes the three sub-types
- Verdict logic untouched (no regression in safe / injection_suspected decisions)
- Plan 06–07 (D-A re-wire) can build on a stable worker
</success_criteria>

<output>
After completion, create `.planning/phases/82.2-stage-0-telemetry-coverage-fix-v8-stabilisation/82.2-04-SUMMARY.md`
</output>
