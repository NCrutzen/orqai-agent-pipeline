---
phase: 82.2-stage-0-telemetry-coverage-fix-v8-stabilisation
plan: 05
type: tdd
wave: 3
depends_on: [82.2-01, 82.2-04]
files_modified:
  - web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts
autonomous: true
requirements: [TELE-COV-04]
must_haves:
  truths:
    - "A synthetic prompt-injection payload through the worker produces a pipeline_events row with decision='injection_suspected'"
    - "The injection_suspected branch is regression-covered against future refactors"
  artifacts:
    - path: "web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts"
      provides: "D-06 synthetic injection test in a dedicated describe block"
  key_links:
    - from: "synthetic test"
      to: "stage-0-safety-worker.ts main-path branch"
      via: "mocked llmInjectionVerdict returning injection_suspected"
      pattern: "injection_suspected"
---

<objective>
Add D-06's synthetic prompt-injection test as regression coverage for the `injection_suspected` branch. Worker-level (not end-to-end ingest) per RESEARCH §Suspected-branch trigger recommendation — matches the existing `__tests__/` mock-step pattern.

Purpose: CONTEXT.md D-06. RESEARCH established that the suspected branch DOES emit today (test L249–254 already asserts this); zero rows in 7d = zero injection attempts, not a missing emit. This plan adds a NEW, more comprehensive test that verifies the FULL pipeline_events row shape on the post-refactor (Plan 04) single-emit path, including the new `decision_details.emit_source='main-path'` field.

Output: One new `describe` block in the existing test file. RED → GREEN → no refactor needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82.2-stage-0-telemetry-coverage-fix-v8-stabilisation/82.2-CONTEXT.md
@.planning/phases/82.2-stage-0-telemetry-coverage-fix-v8-stabilisation/82.2-RESEARCH.md
@web/lib/inngest/functions/stage-0-safety-worker.ts
@web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts

<interfaces>
Mock payload template (RESEARCH §Test payload, lines 218–227):
```typescript
mockRegex.mockReturnValue({ matched: "ignore_previous" });
mockLlm.mockResolvedValue({
  verdict: "injection_suspected",
  reason: "imperative override detected",
  matched_span: "ignore previous",
  usage: { prompt_tokens: 200, completion_tokens: 30, total_tokens: 230, cost_cents: 4 },
});
```

Expected pipeline_events insert (after Plan 04 refactor):
- `swarm_type` = event data
- `stage` = 0
- `email_id` = event data
- `decision` = 'injection_suspected'
- `decision_details.emit_source` = 'main-path' (NEW post-Plan 04)
- `decision_details.regex_matched` = true
- `decision_details.llm_reason` = "imperative override detected"
- `decision_details.matched_span` = "ignore previous"
- `decision_details.safety_overridden` = false
- `cost_cents` = 4
- `triggered_by` = 'pipeline'

Assert ALSO: NO `classifier/screen.requested` event sent (injection_suspected halts the pipeline).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write synthetic injection test (RED on un-refactored worker; GREEN after Plan 04)</name>
  <files>web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts</files>
  <behavior>
    - Test 1: When llmInjectionVerdict returns verdict='injection_suspected', the captured pipeline_events insert has decision='injection_suspected'
    - Test 2: The captured pipeline_events insert has decision_details.emit_source='main-path', decision_details.regex_matched=true, decision_details.llm_reason='imperative override detected', decision_details.matched_span='ignore previous'
    - Test 3: cost_cents=4 (from mocked usage.cost_cents)
    - Test 4: No inngest.send call to 'classifier/screen.requested' was made (injection halts forwarding)
    - Test 5: automation_runs row was written with status='predicted', topic='safety_review' (existing post-Phase-64 behavior)
  </behavior>
  <action>
    Add `describe("D-06 synthetic injection (Phase 82.2)", ...)` to the test file.

    Doc-comment block at the top of the describe MUST state: "Worker-level test per RESEARCH §Suspected-branch trigger recommendation. End-to-end integration test through the ingest path is deferred — Phase 64 fixtures already cover the wire-level path; this regression-covers the single-emit refactor's pipeline_events shape."

    Implementation follows the existing test pattern in the file:
    1. Re-use the existing supabase mock that captures inserts
    2. Mock regexScreen + llmInjectionVerdict with the values above
    3. Invoke the worker handler with a minimal valid event (use the same fixture pattern as the existing injection_suspected test at L206–265)
    4. Assert on captured pipeline_events insert array (length 1; row shape per interfaces)
    5. Assert on inngest.send call count + names (one call to nothing-or-only-non-classifier OR zero classifier/screen.requested calls)
    6. Assert on automation_runs upsert
  </action>
  <verify>
    <automated>cd web && npx vitest run lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts -t "D-06 synthetic injection"</automated>
  </verify>
  <done>D-06 test describe block exists; all assertions pass after Plan 04 worker refactor is in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → mocked LLM | Synthetic injection payload; no real Orq.ai call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82.2-05-01 | Tampering | Future refactor silently drops injection_suspected emit | mitigate | This test asserts decision='injection_suspected' presence; CI catches regression |
| T-82.2-05-02 | Repudiation | Test passes against mock but real Orq.ai behavior diverges | accept | Mock-level coverage is sufficient per D-06 scope; Phase 64 fixtures + Orq.ai experiments cover real-LLM verification |
</threat_model>

<verification>
- D-06 test block exists in stage-0-safety-worker.test.ts
- All assertions pass post-Plan-04 refactor
</verification>

<success_criteria>
- Future refactor that drops the injection_suspected emit fails this test in CI
- decision_details.emit_source='main-path' field is locked in by test expectation
</success_criteria>

<output>
After completion, create `.planning/phases/82.2-stage-0-telemetry-coverage-fix-v8-stabilisation/82.2-05-SUMMARY.md`
</output>
