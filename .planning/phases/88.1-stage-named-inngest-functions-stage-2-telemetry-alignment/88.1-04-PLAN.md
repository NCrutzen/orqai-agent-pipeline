---
phase: 88.1-stage-named-inngest-functions-stage-2-telemetry-alignment
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - web/lib/inngest/functions/debtor-email-icontroller-tagger.ts
  - web/lib/inngest/functions/stage-2-icontroller-label-applier.ts
  - web/lib/inngest/functions/__tests__/debtor-email-icontroller-tagger.test.ts
  - web/lib/inngest/functions/__tests__/stage-2-icontroller-label-applier.test.ts
  - web/lib/inngest/events.ts
autonomous: true
requirements: [D-01, D-05, D-06, R-01, R-05]
tags: [inngest, rename, stage-2]

must_haves:
  truths:
    - "Source file `debtor-email-icontroller-tagger.ts` renamed via `git mv` to `stage-2-icontroller-label-applier.ts`."
    - "Function id changed from `automations/debtor-email-icontroller-tagger` to `stage-2-icontroller-label-applier`."
    - "Event listener changed from `debtor-email/icontroller-tag.requested` to `debtor-email/stage-2.icontroller-label.requested`."
    - "Exported symbol renamed `debtorEmailIcontrollerTagger` → `stage2IcontrollerLabelApplier`."
  artifacts:
    - path: "web/lib/inngest/functions/stage-2-icontroller-label-applier.ts"
      provides: "Renamed Stage 2 icontroller-label-applier function"
      contains: 'id: "stage-2-icontroller-label-applier"'
    - path: "web/lib/inngest/events.ts"
      provides: "Event taxonomy with new stage-2 icontroller-label event"
      contains: '"debtor-email/stage-2.icontroller-label.requested"'
  key_links:
    - from: "stage-2-icontroller-label-applier.ts"
      to: "events.ts new event entry"
      via: "{ event: 'debtor-email/stage-2.icontroller-label.requested' }"
      pattern: "event:.*stage-2.icontroller-label.requested"
---

<objective>
Rename `debtor-email-icontroller-tagger` → `stage-2-icontroller-label-applier` end-to-end. Pure rename, locks this function as Stage 2 per the roadmap entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-CONTEXT.md
@.planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-AUDIT.md
@CLAUDE.md
@web/lib/inngest/functions/debtor-email-icontroller-tagger.ts
@web/lib/inngest/events.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: `git mv` source + test, then patch id/event/export symbol</name>
  <files>web/lib/inngest/functions/stage-2-icontroller-label-applier.ts, web/lib/inngest/functions/__tests__/stage-2-icontroller-label-applier.test.ts</files>
  <action>
Step 1 — file renames via `git mv`:

```
git mv web/lib/inngest/functions/debtor-email-icontroller-tagger.ts web/lib/inngest/functions/stage-2-icontroller-label-applier.ts
git mv web/lib/inngest/functions/__tests__/debtor-email-icontroller-tagger.test.ts web/lib/inngest/functions/__tests__/stage-2-icontroller-label-applier.test.ts
```

Step 2 — patch the renamed source:
- Line 81 area: `id: "automations/debtor-email-icontroller-tagger"` → `id: "stage-2-icontroller-label-applier"`.
- Line 89 area: `{ event: "debtor-email/icontroller-tag.requested" }` → `{ event: "debtor-email/stage-2.icontroller-label.requested" }`.
- Exported binding `debtorEmailIcontrollerTagger` → `stage2IcontrollerLabelApplier`. Update any internal references in this file.
- Preserve concurrency / retries / step.run names verbatim.
- Inline-call `inngest.send` everywhere (no destructure — Phase 65 lesson).

Step 3 — patch the renamed test:
- Update imports + bindings to the new export name.
- Update any `describe`/`it` literal strings that hold the legacy id or event name.

Run isolation: `cd web && npx vitest run lib/inngest/functions/__tests__/stage-2-icontroller-label-applier.test.ts`.
  </action>
  <verify>
    <automated>cd web && npx vitest run lib/inngest/functions/__tests__/stage-2-icontroller-label-applier.test.ts</automated>
  </verify>
  <done>Files renamed via git mv; id + event + export updated; isolated test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Patch events.ts entry for icontroller-tag → stage-2.icontroller-label</name>
  <files>web/lib/inngest/events.ts</files>
  <action>
In `web/lib/inngest/events.ts`, locate `"debtor-email/icontroller-tag.requested"` (currently around line 465). Replace KEY with `"debtor-email/stage-2.icontroller-label.requested"`. Preserve the entire `data` shape verbatim (email_label_id, email_id, automation_run_id, customer_account_id, customer_name, source_mailbox, icontroller_mailbox_id, icontroller_company, icontroller_message_url, entity, sender_email, subject, received_at). Update the leading JSDoc comment to reference Phase 88.1 + the new key name; preserve the architectural notes (D-01, D-02, R-02 from Phase 67).
  </action>
  <verify>
    <automated>cd web && grep -n "debtor-email/stage-2.icontroller-label.requested" lib/inngest/events.ts && ! grep -n "debtor-email/icontroller-tag.requested" lib/inngest/events.ts</automated>
  </verify>
  <done>events.ts has the new key; old key is gone; data shape unchanged.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88.1-07 | T (Tampering) | git rename detection | mitigate | git mv per R-05 |
| T-88.1-08 | T (Tampering) | inngest.send `this`-binding loss | mitigate | inline-only call sites |
</threat_model>

<verification>
- Isolated test passes.
- Grep confirms new id + new event in renamed source.
- Grep confirms zero `automations/debtor-email-icontroller-tagger` and zero `debtor-email/icontroller-tag.requested` in the renamed source/test pair.
</verification>

<success_criteria>
- Two more of the seven legacy strings (`automations/debtor-email-icontroller-tagger` + `debtor-email/icontroller-tag.requested`) are purged from this pair; remaining call-sites purged in Plan 06.
</success_criteria>

<output>
After completion, create `.planning/phases/88.1-.../88.1-04-SUMMARY.md`.
</output>
