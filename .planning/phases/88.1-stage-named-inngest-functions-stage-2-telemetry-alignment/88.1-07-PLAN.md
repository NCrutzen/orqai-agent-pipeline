---
phase: 88.1-stage-named-inngest-functions-stage-2-telemetry-alignment
plan: 07
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06]
files_modified:
  - .planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-DEPLOY-LOG.md
  - docs/runbooks/inngest-function-rename-phase-88.1.md
autonomous: false
requirements: [D-02, D-04, D-06, R-02, R-04]
tags: [deploy, smoke, runbook, operator]

must_haves:
  truths:
    - "Pre-merge: Inngest dashboard shows zero queued events for the four legacy function ids (R-02 mitigation)."
    - "Post-deploy: the four new function ids appear as registered functions on the Inngest dashboard."
    - "One live test event per new event name is fired and received by the correct new function id."
    - "1h post-deploy telemetry check confirms pipeline_events stage=2 rows are still being written for swarm_type='debtor-email' (or the D-04 patch task is queued if Wave 0 surfaced a GAP)."
    - "Operator runbook captures old-id → new-id mapping + cutover timestamp (R-04 mitigation)."
  artifacts:
    - path: ".planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-DEPLOY-LOG.md"
      provides: "Operator-completed deploy log with timestamps + smoke evidence"
      contains: "cutover_timestamp"
    - path: "docs/runbooks/inngest-function-rename-phase-88.1.md"
      provides: "Discoverable old-id → new-id mapping for future debugging"
      contains: "## Old function id → new function id"
  key_links:
    - from: "operator runbook"
      to: "Inngest dashboard historical-run lookup"
      via: "old-id is documented as the search key for pre-cutover runs"
      pattern: "querying historical runs requires the OLD id"
---

<objective>
Execute the Wave 5 cutover per D-02: operator-driven merge in a 06:00 Amsterdam Mon-Fri window, Vercel deploys atomically, live smoke confirms each new event name reaches the correct new function, and the operator runbook captures the discontinuity per R-04.

Purpose: ship the rename safely, record the discontinuity for future "where did the runs go?" debugging.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-CONTEXT.md
@.planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-AUDIT.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Pre-merge quiesce check + window selection</name>
  <what-built>Plans 02-06 landed a single PR; PR description references this plan, the audit, and the old-id → new-id mapping.</what-built>
  <how-to-verify>
1. Operator picks a 06:00 Amsterdam Mon-Fri window. Earliest valid: next Mon 06:00 Europe/Amsterdam after PR open.
2. ~5 minutes before merge, operator opens the Inngest dashboard and checks the four legacy function ids for queued events:
   - `classifier/label-resolver`
   - `automations/debtor-email-icontroller-tagger`
   - `automations/debtor-email-icontroller-shard-worker`
   - `automations/debtor-email-icontroller-dispatch`
3. If ANY of the four shows queued or in-flight runs, ABORT and reschedule. Per R-02, the cutover assumes verified-empty queues.
4. Once all four read clear, operator records the cutover timestamp (UTC ISO) into 88.1-DEPLOY-LOG.md → `## Pre-merge quiesce check`.
  </how-to-verify>
  <resume-signal>Type "quiesce clear" with cutover_timestamp, or "aborted" with rescheduled window.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Merge + Vercel deploy</name>
  <what-built>PR with all Wave-1 plans (03-06) is ready and CI is green.</what-built>
  <how-to-verify>
1. Operator merges the PR.
2. Vercel auto-deploys. Wait for "Ready" state.
3. Operator confirms in the Inngest dashboard that all four NEW function ids appear as registered:
   - `stage-2-customer-resolver`
   - `stage-2-icontroller-label-applier`
   - `stage-1-icontroller-noise-cleanup`
   - `stage-1-icontroller-noise-cleanup-dispatcher`
4. The four legacy ids are still listed in the dashboard but show no NEW runs after the cutover timestamp (only their historical orphan tail per D-06).
5. Record deploy URL + Vercel deployment ID + Inngest "ready" timestamp into 88.1-DEPLOY-LOG.md → `## Deploy`.
  </how-to-verify>
  <resume-signal>Type "deployed" with Vercel deployment ID + 4 new ids confirmed registered.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Live smoke — fire one test event per new event name</name>
  <files>.planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-DEPLOY-LOG.md</files>
  <action>
For each of the three renamed events, fire one test `inngest.send` via a one-shot tsx invocation against production Inngest. Use a known-safe payload that the function will accept-then-reject early (e.g. a non-existent automation_run_id) so no real downstream side effects fire.

Smoke targets:

1. `debtor-email/stage-2.customer-resolve.requested` → expect receipt by function id `stage-2-customer-resolver`.
2. `debtor-email/stage-2.icontroller-label.requested` → expect receipt by `stage-2-icontroller-label-applier`.
3. `debtor-email/stage-1.icontroller-cleanup.requested` → expect receipt by `stage-1-icontroller-noise-cleanup`.

(The renamed dispatcher is cron-triggered. Smoke = wait for the first scheduled run after deploy, which will land within 5 minutes of the next `*/5 6-19 * * 1-5` tick. Operator confirms in dashboard, no manual send needed.)

For each send, capture the Inngest event ID and the resulting function-run URL. Append to 88.1-DEPLOY-LOG.md → `## Live smoke` with one row per smoke:

```markdown
| Event name | Inngest event_id | Function run URL | Status | Notes |
|---|---|---|---|---|
| debtor-email/stage-2.customer-resolve.requested | evt_xxx | https://app.inngest.com/... | received | rejected on missing automation_run_id (expected) |
```

Use INLINE `inngest.send` (never destructure — Phase 65 lesson, CLAUDE.md).

If any smoke shows the event was received under the OLD function id, or the event is not received at all within 60s, ABORT and revert the PR.
  </action>
  <verify>
    <automated>grep -c "received" .planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-DEPLOY-LOG.md | awk '$1 >= 3 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>All three event smokes recorded with `received` status under the matching new function id. Dispatcher cron tick confirmed in dashboard.</done>
</task>

<task type="auto">
  <name>Task 4: 1h post-deploy telemetry check</name>
  <files>.planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-DEPLOY-LOG.md</files>
  <action>
1 hour after the deploy "Ready" timestamp, run via Supabase MCP `execute_sql`:

```sql
SELECT stage,
       swarm_type,
       COUNT(*) AS n,
       MIN(created_at) AS first_seen,
       MAX(created_at) AS last_seen
FROM pipeline_events
WHERE created_at > '<cutover_timestamp>'
  AND swarm_type = 'debtor-email'
  AND stage = 2
GROUP BY stage, swarm_type;
```

Expected: ≥1 row with `stage=2`, `swarm_type='debtor-email'`, and `last_seen` within the last 10 minutes (proving the Stage 2 customer-resolver + icontroller-label-applier are emitting under their new ids).

If the Wave 0 audit (88.1-01) recorded a D-04 GAP, this is also the moment to confirm whether the Wave-2 patch (if any was scoped into Plan 06) is producing rows.

Append result to 88.1-DEPLOY-LOG.md → `## 1h telemetry check` with the raw row count + first_seen + last_seen.
  </action>
  <verify>
    <automated>grep -c "## 1h telemetry check" .planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-DEPLOY-LOG.md</automated>
  </verify>
  <done>1h telemetry section written with concrete row counts. If zero rows, R-03 (telemetry missing) is flagged as follow-up.</done>
</task>

<task type="auto">
  <name>Task 5: Write operator runbook for the rename discontinuity</name>
  <files>docs/runbooks/inngest-function-rename-phase-88.1.md</files>
  <action>
Create `docs/runbooks/inngest-function-rename-phase-88.1.md` with these sections:

```markdown
# Inngest function rename — Phase 88.1 (Stage-named)

**Cutover timestamp:** <UTC ISO from Task 1>
**Vercel deploy:** <deployment ID from Task 2>
**PR:** <merged PR URL>

## What changed

Four Inngest function ids + three event names renamed to align with the canonical 5-stage funnel vocabulary. Pure rename, no behavior change. See `.planning/phases/88.1-.../88.1-CONTEXT.md` for the full rationale.

## Old function id → new function id

| Old function id | New function id | File |
|---|---|---|
| classifier/label-resolver | stage-2-customer-resolver | web/lib/inngest/functions/stage-2-customer-resolver.ts |
| automations/debtor-email-icontroller-tagger | stage-2-icontroller-label-applier | web/lib/inngest/functions/stage-2-icontroller-label-applier.ts |
| automations/debtor-email-icontroller-shard-worker | stage-1-icontroller-noise-cleanup | web/lib/inngest/functions/stage-1-icontroller-noise-cleanup.ts |
| automations/debtor-email-icontroller-dispatch | stage-1-icontroller-noise-cleanup-dispatcher | web/lib/inngest/functions/stage-1-icontroller-noise-cleanup-dispatcher.ts |

## Old event name → new event name

| Old event name | New event name |
|---|---|
| debtor-email/label-resolve.requested | debtor-email/stage-2.customer-resolve.requested |
| debtor-email/icontroller-tag.requested | debtor-email/stage-2.icontroller-label.requested |
| icontroller/cleanup.shard.requested | debtor-email/stage-1.icontroller-cleanup.requested |

## Looking up historical runs

Inngest treats a function-id change as a NEW function. Runs that fired BEFORE <cutover_timestamp> are queryable in the Inngest dashboard ONLY under the OLD function id. Runs AFTER the cutover are under the new id.

To answer "what did stage-2-customer-resolver do before 2026-05-XX?": search the dashboard for `classifier/label-resolver`. Same row-by-row data, different id namespace.

## Smoke evidence

See `.planning/phases/88.1-.../88.1-DEPLOY-LOG.md` for the three event smokes + 1h telemetry result captured at deploy time.
```

Commit to the workspace tree so it lands at the next push.
  </action>
  <verify>
    <automated>test -f docs/runbooks/inngest-function-rename-phase-88.1.md && grep -c "Old function id" docs/runbooks/inngest-function-rename-phase-88.1.md</automated>
  </verify>
  <done>Runbook exists, contains old→new mapping for ids and events, references the deploy log for evidence.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88.1-15 | R (Repudiation) | "where did the runs go?" debugging 6 months later | mitigate | runbook + deploy log document the discontinuity per R-04 |
| T-88.1-16 | D (Denial of service) | events queued at cutover-second under old name silently fail | accept | per CONTEXT.md D-02: 06:00 Amsterdam quiet window keeps blast radius near-zero; Inngest retries cover the rest |
| T-88.1-17 | T (Tampering) | smoke fires real downstream side effects | mitigate | smoke payloads use known-invalid automation_run_id so functions reject early; runbook records this convention |
</threat_model>

<verification>
- 88.1-DEPLOY-LOG.md exists with: pre-merge quiesce row, deploy row, three smoke rows, 1h telemetry row.
- docs/runbooks/inngest-function-rename-phase-88.1.md exists with old→new mappings.
- All four new function ids are visible + receiving in the Inngest dashboard.
- pipeline_events shows stage=2 rows post-cutover.
</verification>

<success_criteria>
- Phase 88.1 acceptance criteria from CONTEXT.md success criterion 2 + 3 met live.
- Future on-call operator finds the discontinuity documented and discoverable.
</success_criteria>

<output>
After completion, create `.planning/phases/88.1-.../88.1-07-SUMMARY.md`.
</output>
