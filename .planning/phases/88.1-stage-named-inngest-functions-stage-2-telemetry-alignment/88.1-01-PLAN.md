---
phase: 88.1-stage-named-inngest-functions-stage-2-telemetry-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-AUDIT.md
autonomous: true
requirements: [D-01, D-03, D-04, D-08]
tags: [inngest, rename, audit, telemetry]

must_haves:
  truths:
    - "An exhaustive list exists of every file:line referencing any of the 4 legacy function ids or 4 legacy event names."
    - "Telemetry status of the two Stage 2 functions w.r.t. pipeline_events.stage=2 emission is confirmed (already-emitting OR gap-with-patch-task)."
    - "Cleanup-dispatcher's actual trigger shape is documented (it's a cron, not an event consumer — no dispatcher event-name rename in scope)."
    - "Event-payload data shapes are confirmed unchanged across the four event renames."
  artifacts:
    - path: ".planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-AUDIT.md"
      provides: "Wave 1 PR checklist + telemetry verdict + dispatcher trigger note + payload-shape confirmation"
      contains: "## Legacy id occurrences"
  key_links:
    - from: "Wave 1 plans (02/03/04/05)"
      to: "88.1-AUDIT.md occurrence list"
      via: "every grep hit becomes a Wave-1 rename target"
      pattern: "file:line entries grouped by legacy id/event name"
---

<objective>
Produce the Wave 0 audit artefact for Phase 88.1: an exhaustive, file:line-level inventory of every legacy function-id and event-name occurrence, plus a verified telemetry status for the two Stage 2 functions and a confirmation that the four event-payload shapes do not change across the rename. No code changes in this wave — only the audit artefact.

Purpose: every Wave 1 rename plan operates from a frozen target list. Without this artefact, R-01 ("producer site missed during rename") is unmitigated.

Output: `.planning/phases/88.1-.../88.1-AUDIT.md` committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-CONTEXT.md
@CLAUDE.md
@web/lib/inngest/events.ts
@web/app/api/inngest/route.ts
@web/lib/inngest/functions/classifier-label-resolver.ts
@web/lib/inngest/functions/debtor-email-icontroller-tagger.ts
@web/lib/inngest/functions/debtor-email-icontroller-cleanup-worker.ts
@web/lib/inngest/functions/debtor-email-icontroller-cleanup-dispatcher.ts

<interfaces>
The 4 legacy function ids (search targets):
- `classifier/label-resolver`
- `automations/debtor-email-icontroller-tagger`
- `automations/debtor-email-icontroller-shard-worker`
- `automations/debtor-email-icontroller-dispatch`

The 4 legacy event names (search targets):
- `debtor-email/label-resolve.requested`
- `debtor-email/icontroller-tag.requested`
- `icontroller/cleanup.shard.requested`
- (dispatcher — verify: it is a cron in cleanup-dispatcher.ts line 47 `{ cron: "TZ=Europe/Amsterdam */5 6-19 * * 1-5" }`, NOT an event consumer; so there is NO 4th legacy event name to retire)

The 4 new function ids:
- `stage-2-customer-resolver`
- `stage-2-icontroller-label-applier`
- `stage-1-icontroller-noise-cleanup`
- `stage-1-icontroller-noise-cleanup-dispatcher`

The 3 new event names (matching renamed listeners):
- `debtor-email/stage-2.customer-resolve.requested`
- `debtor-email/stage-2.icontroller-label.requested`
- `debtor-email/stage-1.icontroller-cleanup.requested`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exhaustive grep inventory of all 7 legacy strings</name>
  <files>.planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-AUDIT.md</files>
  <action>
Run ripgrep from repo root for each of the 7 legacy strings (4 function ids + 3 event names — the cron-only dispatcher has no event name) and one grouping pass for related comments. Use literal-string mode (`rg -F`) to avoid regex pitfalls. Cover `*.ts`, `*.tsx`, `*.md` under `web/` AND `.planning/`. Exclude `node_modules`, `.next`, `dist`, `coverage`.

Commands to run (capture stdout per query):

```
rg -nF "classifier/label-resolver" web/ .planning/ --type-add 'src:*.{ts,tsx,md}' -tsrc
rg -nF "automations/debtor-email-icontroller-tagger" web/ .planning/ -tsrc
rg -nF "automations/debtor-email-icontroller-shard-worker" web/ .planning/ -tsrc
rg -nF "automations/debtor-email-icontroller-dispatch" web/ .planning/ -tsrc
rg -nF "debtor-email/label-resolve.requested" web/ .planning/ -tsrc
rg -nF "debtor-email/icontroller-tag.requested" web/ .planning/ -tsrc
rg -nF "icontroller/cleanup.shard.requested" web/ .planning/ -tsrc
```

Then a cross-reference pass on the legacy variable/import names that won't change semantics but WILL be touched by file renames:

```
rg -nF "classifier-label-resolver" web/ -tsrc
rg -nF "debtor-email-icontroller-tagger" web/ -tsrc
rg -nF "debtor-email-icontroller-cleanup-worker" web/ -tsrc
rg -nF "debtor-email-icontroller-cleanup-dispatcher" web/ -tsrc
rg -nF "classifierLabelResolver" web/ -tsrc
rg -nF "debtorEmailIcontrollerTagger" web/ -tsrc
rg -nF "cleanupIControllerShardWorker" web/ -tsrc
rg -nF "cleanupIControllerDispatch" web/ -tsrc
```

Compose `88.1-AUDIT.md` with the structure below. Group hits by legacy string; under each, list `file:line — short_context_snippet`. Mark each occurrence with one of: `RENAME` (must be updated in Wave 1), `HISTORICAL` (markdown/CONTEXT/runbook reference allowed to keep old name per CONTEXT.md success criterion 1), `DOC` (operator runbook section to update in Wave 2).

Audit document structure:

```markdown
# Phase 88.1 — Wave 0 Audit Artefact

**Generated:** {iso-date}
**Source:** ripgrep over web/ + .planning/

## 1. Legacy function-id occurrences
### `classifier/label-resolver`
- web/lib/inngest/functions/classifier-label-resolver.ts:37 — RENAME — `{ id: "classifier/label-resolver", retries: 0 }`
- ...

### `automations/debtor-email-icontroller-tagger`
- ...

### `automations/debtor-email-icontroller-shard-worker`
- ...

### `automations/debtor-email-icontroller-dispatch`
- ...

## 2. Legacy event-name occurrences
### `debtor-email/label-resolve.requested`
- ...

### `debtor-email/icontroller-tag.requested`
- ...

### `icontroller/cleanup.shard.requested`
- ...

## 3. File-rename targets (git mv per R-05)
| Old path | New path |
|---|---|
| web/lib/inngest/functions/classifier-label-resolver.ts | web/lib/inngest/functions/stage-2-customer-resolver.ts |
| web/lib/inngest/functions/__tests__/classifier-label-resolver.test.ts | web/lib/inngest/functions/__tests__/stage-2-customer-resolver.test.ts |
| web/lib/inngest/functions/debtor-email-icontroller-tagger.ts | web/lib/inngest/functions/stage-2-icontroller-label-applier.ts |
| web/lib/inngest/functions/__tests__/debtor-email-icontroller-tagger.test.ts | web/lib/inngest/functions/__tests__/stage-2-icontroller-label-applier.test.ts |
| web/lib/inngest/functions/debtor-email-icontroller-cleanup-worker.ts | web/lib/inngest/functions/stage-1-icontroller-noise-cleanup.ts |
| web/lib/inngest/functions/debtor-email-icontroller-cleanup-dispatcher.ts | web/lib/inngest/functions/stage-1-icontroller-noise-cleanup-dispatcher.ts |

## 4. Export-symbol renames (route.ts imports)
| Old export | New export |
|---|---|
| classifierLabelResolver | stage2CustomerResolver |
| debtorEmailIcontrollerTagger | stage2IcontrollerLabelApplier |
| cleanupIControllerShardWorker | stage1IcontrollerNoiseCleanup |
| cleanupIControllerDispatch | stage1IcontrollerNoiseCleanupDispatcher |

## 5. Cleanup-dispatcher trigger note
Confirmed via cleanup-dispatcher.ts line 47: `{ cron: "TZ=Europe/Amsterdam */5 6-19 * * 1-5" }`. No event consumer → no 4th legacy event name to retire. The dispatcher's downstream `inngest.send` to the shard worker IS in scope (it sends `icontroller/cleanup.shard.requested` → new name).

## 6. Event-payload shape parity check
For each of the 3 event renames, the `Events` type entry in `web/lib/inngest/events.ts` keeps the SAME data fields — only the key (event name) changes. Confirmed by reading lines 209-224, 245-253, 465-481.
```

Write the file via the Write tool. Do not add code commits in this wave.
  </action>
  <verify>
    <automated>test -f .planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-AUDIT.md && grep -c "RENAME" .planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-AUDIT.md | awk '$1 >= 10 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>88.1-AUDIT.md exists, contains all 7 legacy-string sections each with ≥1 entry, contains the file-rename table (6 rows), the export-rename table (4 rows), the cleanup-dispatcher cron note, and the payload-parity confirmation.</done>
</task>

<task type="auto">
  <name>Task 2: Telemetry audit — verify pipeline_events.stage=2 emission for Stage 2 functions</name>
  <files>.planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-AUDIT.md</files>
  <action>
Per D-04, verify that `classifier-label-resolver` (Stage 2 customer-resolver) and `debtor-email-icontroller-tagger` (Stage 2 icontroller-label-applier) already emit `pipeline_events` rows with `stage=2` and `swarm_type='debtor-email'` in production.

Two-pronged check:

(a) Static check — grep both source files for `pipeline_events` writes and confirm `stage: 2` (or `stage: '2'`) literal in the insert payload:

```
rg -nF "pipeline_events" web/lib/inngest/functions/classifier-label-resolver.ts
rg -nF "pipeline_events" web/lib/inngest/functions/debtor-email-icontroller-tagger.ts
rg -nC2 "stage:" web/lib/inngest/functions/classifier-label-resolver.ts
rg -nC2 "stage:" web/lib/inngest/functions/debtor-email-icontroller-tagger.ts
```

(b) Live check — via Supabase MCP `execute_sql`:

```sql
SELECT stage, swarm_type, COUNT(*) AS n,
       MAX(created_at) AS last_seen
FROM pipeline_events
WHERE created_at > now() - interval '7 days'
  AND swarm_type = 'debtor-email'
  AND stage = 2
GROUP BY stage, swarm_type
ORDER BY stage;
```

And to confirm WHICH function id is the writer (so we can prove the right two functions are emitting):

```sql
SELECT stage,
       decision_details->>'source_function' AS src,
       decision_details->'agent' AS agent,
       COUNT(*) AS n
FROM pipeline_events
WHERE created_at > now() - interval '7 days'
  AND swarm_type = 'debtor-email'
  AND stage = 2
GROUP BY 1, 2, 3
ORDER BY n DESC
LIMIT 20;
```

Append a `## 7. Telemetry audit verdict` section to `88.1-AUDIT.md`:

- **PASS** (no Wave 2 patch needed): if BOTH static-grep AND live-query show stage=2 rows from both functions in the last 7 days.
- **GAP** (Wave 2 patch task required): if either function fails to emit stage=2. Record exact symptom: which function, what stage value (if any) it's emitting today, what swarm_type label it uses.

If GAP, add a follow-up note: "Wave 2 plan 88.1-05 to include a `stage=2` emit patch task for {function}."

Do NOT preemptively patch — D-04 is verify-only in Wave 0.
  </action>
  <verify>
    <automated>grep -c "## 7. Telemetry audit verdict" .planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-AUDIT.md</automated>
  </verify>
  <done>Section 7 written to 88.1-AUDIT.md with verdict (PASS or GAP-with-symptom). If GAP, the symptom is concrete enough for Wave 2 to write a patch task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave 0 → Wave 1 | the audit artefact is the only input that bounds Wave 1's blast radius |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88.1-01 | I (Information disclosure) | Supabase MCP execute_sql against pipeline_events | accept | read-only aggregate query, no PII columns selected; service-role read against own project |
| T-88.1-02 | T (Tampering) | audit artefact accuracy | mitigate | every claim in 88.1-AUDIT.md cites a file:line or a SQL result row; Wave 1 grep guard re-runs the same searches as a sanity gate |
</threat_model>

<verification>
- 88.1-AUDIT.md exists and is committed (no code changes).
- File-rename + export-rename tables are complete (6 + 4 rows).
- Telemetry verdict is unambiguous (PASS or concrete GAP).
- Cleanup-dispatcher cron-vs-event clarification recorded so Wave 1 planner doesn't hunt for a 4th event name.
</verification>

<success_criteria>
- A single artefact (`88.1-AUDIT.md`) drives all subsequent Wave 1 file targets.
- Zero code committed in this plan.
- The audit either UNBLOCKS Wave 1 as a pure rename (D-04 PASS) or surfaces the exact stage=2 patch needed (D-04 GAP → Wave 2).
</success_criteria>

<output>
After completion, create `.planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-01-SUMMARY.md`.
</output>
