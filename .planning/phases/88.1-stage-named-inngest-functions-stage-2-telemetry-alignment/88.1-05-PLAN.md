---
phase: 88.1-stage-named-inngest-functions-stage-2-telemetry-alignment
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - web/lib/inngest/functions/debtor-email-icontroller-cleanup-worker.ts
  - web/lib/inngest/functions/stage-1-icontroller-noise-cleanup.ts
  - web/lib/inngest/functions/debtor-email-icontroller-cleanup-dispatcher.ts
  - web/lib/inngest/functions/stage-1-icontroller-noise-cleanup-dispatcher.ts
  - web/lib/inngest/events.ts
autonomous: true
requirements: [D-01, D-03, D-05, D-06, R-01, R-05]
tags: [inngest, rename, stage-1]

must_haves:
  truths:
    - "Cleanup-worker renamed via `git mv` to `stage-1-icontroller-noise-cleanup.ts`; id changes from `automations/debtor-email-icontroller-shard-worker` to `stage-1-icontroller-noise-cleanup`; event listener `icontroller/cleanup.shard.requested` → `debtor-email/stage-1.icontroller-cleanup.requested`."
    - "Cleanup-dispatcher renamed via `git mv` to `stage-1-icontroller-noise-cleanup-dispatcher.ts`; id changes from `automations/debtor-email-icontroller-dispatch` to `stage-1-icontroller-noise-cleanup-dispatcher`."
    - "Dispatcher's `inngest.send` to the shard worker fires the NEW event name (`debtor-email/stage-1.icontroller-cleanup.requested`)."
    - "Dispatcher's `cron:` schedule preserved verbatim."
    - "Exported symbols renamed: `cleanupIControllerShardWorker` → `stage1IcontrollerNoiseCleanup`; `cleanupIControllerDispatch` → `stage1IcontrollerNoiseCleanupDispatcher`."
  artifacts:
    - path: "web/lib/inngest/functions/stage-1-icontroller-noise-cleanup.ts"
      provides: "Renamed Stage 1 noise-cleanup shard worker"
      contains: 'id: "stage-1-icontroller-noise-cleanup"'
    - path: "web/lib/inngest/functions/stage-1-icontroller-noise-cleanup-dispatcher.ts"
      provides: "Renamed Stage 1 noise-cleanup dispatcher"
      contains: 'id: "stage-1-icontroller-noise-cleanup-dispatcher"'
    - path: "web/lib/inngest/events.ts"
      provides: "Event taxonomy with renamed stage-1.icontroller-cleanup event"
      contains: '"debtor-email/stage-1.icontroller-cleanup.requested"'
  key_links:
    - from: "stage-1-icontroller-noise-cleanup-dispatcher.ts"
      to: "stage-1-icontroller-noise-cleanup.ts"
      via: "inngest.send({ name: 'debtor-email/stage-1.icontroller-cleanup.requested' })"
      pattern: "inngest\\.send.*stage-1.icontroller-cleanup.requested"
---

<objective>
Rename the Stage 1 noise-cleanup pair (worker + dispatcher) end-to-end. Per D-03, the dispatcher rename ships in the same PR for vocabulary consistency. Pure rename — preserve the dispatcher's `cron:` and the worker's concurrency/step config verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-CONTEXT.md
@.planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-AUDIT.md
@CLAUDE.md
@web/lib/inngest/functions/debtor-email-icontroller-cleanup-worker.ts
@web/lib/inngest/functions/debtor-email-icontroller-cleanup-dispatcher.ts
@web/lib/inngest/events.ts

<interfaces>
Worker source file currently registers `id: "automations/debtor-email-icontroller-shard-worker"` (line 50) on event `icontroller/cleanup.shard.requested` (line 66).

Dispatcher source file currently registers `id: "automations/debtor-email-icontroller-dispatch"` (line 43) on `cron: "TZ=Europe/Amsterdam */5 6-19 * * 1-5"` (line 47). It sends `icontroller/cleanup.shard.requested` to the worker.

No 4th event-name rename — the dispatcher is cron-triggered (see 88.1-AUDIT.md § 5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename cleanup-worker + patch id/event/export</name>
  <files>web/lib/inngest/functions/stage-1-icontroller-noise-cleanup.ts</files>
  <action>
Step 1 — rename:

```
git mv web/lib/inngest/functions/debtor-email-icontroller-cleanup-worker.ts web/lib/inngest/functions/stage-1-icontroller-noise-cleanup.ts
```

(There is no `__tests__/debtor-email-icontroller-cleanup-worker.test.ts` per the audit; if 88.1-AUDIT.md surfaces one, add a `git mv` here.)

Step 2 — patch the renamed source:
- `id: "automations/debtor-email-icontroller-shard-worker"` → `id: "stage-1-icontroller-noise-cleanup"`.
- `{ event: "icontroller/cleanup.shard.requested" }` → `{ event: "debtor-email/stage-1.icontroller-cleanup.requested" }`.
- Exported binding `cleanupIControllerShardWorker` → `stage1IcontrollerNoiseCleanup`.
- Preserve concurrency, retries, step.run names verbatim.
- Any inline comments referring to the old id/event get updated; preserve any audit-trail comments referencing prior phases.
  </action>
  <verify>
    <automated>cd web && grep -n 'id: "stage-1-icontroller-noise-cleanup"' lib/inngest/functions/stage-1-icontroller-noise-cleanup.ts && grep -n "debtor-email/stage-1.icontroller-cleanup.requested" lib/inngest/functions/stage-1-icontroller-noise-cleanup.ts</automated>
  </verify>
  <done>Worker renamed via git mv; id + event + export symbol updated; legacy strings absent from this file.</done>
</task>

<task type="auto">
  <name>Task 2: Rename cleanup-dispatcher + patch id/cron/downstream-send/export</name>
  <files>web/lib/inngest/functions/stage-1-icontroller-noise-cleanup-dispatcher.ts</files>
  <action>
Step 1 — rename:

```
git mv web/lib/inngest/functions/debtor-email-icontroller-cleanup-dispatcher.ts web/lib/inngest/functions/stage-1-icontroller-noise-cleanup-dispatcher.ts
```

Step 2 — patch:
- `id: "automations/debtor-email-icontroller-dispatch"` → `id: "stage-1-icontroller-noise-cleanup-dispatcher"`.
- `cron: "TZ=Europe/Amsterdam */5 6-19 * * 1-5"` — preserve VERBATIM. No changes to schedule per CONTEXT.md scope rule.
- Locate every `inngest.send({ name: "icontroller/cleanup.shard.requested", ... })` inside this file and rename to `inngest.send({ name: "debtor-email/stage-1.icontroller-cleanup.requested", ... })`. INLINE the call — never destructure `inngest.send` (Phase 65 lesson, see CLAUDE.md).
- Exported binding `cleanupIControllerDispatch` → `stage1IcontrollerNoiseCleanupDispatcher`.
- Preserve every step.run name verbatim.

Step 3 — typecheck this file in isolation:

```
cd web && npx tsc --noEmit lib/inngest/functions/stage-1-icontroller-noise-cleanup-dispatcher.ts
```

(Full `tsc --noEmit` will still fail until route.ts is updated in Plan 06 — that's expected.)
  </action>
  <verify>
    <automated>cd web && grep -n 'id: "stage-1-icontroller-noise-cleanup-dispatcher"' lib/inngest/functions/stage-1-icontroller-noise-cleanup-dispatcher.ts && grep -nF "debtor-email/stage-1.icontroller-cleanup.requested" lib/inngest/functions/stage-1-icontroller-noise-cleanup-dispatcher.ts && ! grep -nF "icontroller/cleanup.shard.requested" lib/inngest/functions/stage-1-icontroller-noise-cleanup-dispatcher.ts</automated>
  </verify>
  <done>Dispatcher renamed via git mv; id + downstream send name + export updated; cron unchanged; legacy strings absent from this file.</done>
</task>

<task type="auto">
  <name>Task 3: Rename events.ts entry for the shard event</name>
  <files>web/lib/inngest/events.ts</files>
  <action>
In `web/lib/inngest/events.ts`, locate the `"icontroller/cleanup.shard.requested"` key (currently around line 209). Replace KEY with `"debtor-email/stage-1.icontroller-cleanup.requested"`. Preserve the `data` shape verbatim (workerIndex + rows array). Update the leading comment to reference Phase 88.1 + the new key name.
  </action>
  <verify>
    <automated>cd web && grep -nF "debtor-email/stage-1.icontroller-cleanup.requested" lib/inngest/events.ts && ! grep -nF "icontroller/cleanup.shard.requested" lib/inngest/events.ts</automated>
  </verify>
  <done>events.ts has the new key; old key gone; data shape preserved.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88.1-09 | T (Tampering) | cron string accidentally mutated during rename | mitigate | task action explicitly forbids cron change; verify step grep-confirms |
| T-88.1-10 | T (Tampering) | dispatcher's downstream send drops the new event into the void | mitigate | Plan 06 grep guard catches any unrenamed call site; Plan 07 live smoke confirms receipt |
| T-88.1-11 | T (Tampering) | inngest.send destructuring | mitigate | inline-call requirement repeated in each task |
</threat_model>

<verification>
- Grep confirms new ids + new event in renamed sources.
- Grep confirms zero legacy `icontroller/cleanup.shard.requested` AND zero `automations/debtor-email-icontroller-shard-worker` AND zero `automations/debtor-email-icontroller-dispatch` in the renamed pair.
- Cron string in dispatcher unchanged.
</verification>

<success_criteria>
- Four more legacy strings (`automations/debtor-email-icontroller-shard-worker` + `automations/debtor-email-icontroller-dispatch` + `icontroller/cleanup.shard.requested` + the dispatcher's downstream send) purged from this pair. Plan 06 closes the remaining call-sites in events.ts cross-references, route.ts, UI surfaces, and scripts.
</success_criteria>

<output>
After completion, create `.planning/phases/88.1-.../88.1-05-SUMMARY.md`.
</output>
