---
phase: 88.1-stage-named-inngest-functions-stage-2-telemetry-alignment
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - web/lib/inngest/functions/classifier-label-resolver.ts
  - web/lib/inngest/functions/stage-2-customer-resolver.ts
  - web/lib/inngest/functions/__tests__/classifier-label-resolver.test.ts
  - web/lib/inngest/functions/__tests__/stage-2-customer-resolver.test.ts
  - web/lib/inngest/events.ts
autonomous: true
requirements: [D-01, D-05, D-06, R-01, R-05]
tags: [inngest, rename, stage-2]

must_haves:
  truths:
    - "Source file `classifier-label-resolver.ts` renamed via `git mv` to `stage-2-customer-resolver.ts`; blame preserved."
    - "Function id changed from `classifier/label-resolver` to `stage-2-customer-resolver`."
    - "Event listener changed from `debtor-email/label-resolve.requested` to `debtor-email/stage-2.customer-resolve.requested`."
    - "Test file mirrored via `git mv`; export symbol renamed `classifierLabelResolver` → `stage2CustomerResolver`."
    - "Events.ts has new `debtor-email/stage-2.customer-resolve.requested` entry; old entry deleted; data shape unchanged."
  artifacts:
    - path: "web/lib/inngest/functions/stage-2-customer-resolver.ts"
      provides: "Renamed Stage 2 customer-resolver function definition"
      contains: 'id: "stage-2-customer-resolver"'
    - path: "web/lib/inngest/events.ts"
      provides: "Event taxonomy with new stage-2 customer-resolve event"
      contains: '"debtor-email/stage-2.customer-resolve.requested"'
  key_links:
    - from: "stage-2-customer-resolver.ts"
      to: "events.ts new event entry"
      via: "{ event: 'debtor-email/stage-2.customer-resolve.requested' }"
      pattern: "event:.*stage-2.customer-resolve.requested"
---

<objective>
Rename `classifier-label-resolver` → `stage-2-customer-resolver` end-to-end: source file, test file, function id, event listener name, exported symbol, and the matching `Events` type entry. Pure rename, no behavior change.

Purpose: deliver one of the three roadmap-listed renames; bring the function name in line with its actual stage role (Stage 2 entity resolution).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-CONTEXT.md
@.planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-AUDIT.md
@CLAUDE.md
@web/lib/inngest/functions/classifier-label-resolver.ts
@web/lib/inngest/events.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: `git mv` source + test, then patch id/event/export symbol</name>
  <files>web/lib/inngest/functions/stage-2-customer-resolver.ts, web/lib/inngest/functions/__tests__/stage-2-customer-resolver.test.ts</files>
  <action>
Step 1 — file renames via `git mv` (NOT delete+create; rename detection preserves blame per R-05):

```
git mv web/lib/inngest/functions/classifier-label-resolver.ts web/lib/inngest/functions/stage-2-customer-resolver.ts
git mv web/lib/inngest/functions/__tests__/classifier-label-resolver.test.ts web/lib/inngest/functions/__tests__/stage-2-customer-resolver.test.ts
```

Step 2 — patch the renamed source file:

In `web/lib/inngest/functions/stage-2-customer-resolver.ts`:
- Line 37 area: `{ id: "classifier/label-resolver", retries: 0 }` → `{ id: "stage-2-customer-resolver", retries: 0 }`.
- Line 38 area: `{ event: "debtor-email/label-resolve.requested" }` → `{ event: "debtor-email/stage-2.customer-resolve.requested" }`.
- Rename the exported binding `classifierLabelResolver` → `stage2CustomerResolver` (camelCase mirrors the kebab function id). Update any internal references inside the same file.
- Preserve `cron:` and `concurrency:` configs verbatim (none on this function today, but check the actual current source — do not invent any).
- Preserve every step.run name, every Supabase call, every `inngest.send` call site INLINE (never destructure `inngest.send` per CLAUDE.md / Phase 65 lesson).

Step 3 — patch the renamed test file:

In `web/lib/inngest/functions/__tests__/stage-2-customer-resolver.test.ts`:
- Update import: `from "../classifier-label-resolver"` → `from "../stage-2-customer-resolver"`.
- Update imported binding: `classifierLabelResolver` → `stage2CustomerResolver`.
- Update any `describe`/`it` strings that literally contain the legacy id or event name (cosmetic but flagged by Plan 02 grep guard).
- Run the test in isolation to confirm GREEN: `cd web && npx vitest run lib/inngest/functions/__tests__/stage-2-customer-resolver.test.ts`.
  </action>
  <verify>
    <automated>cd web && npx vitest run lib/inngest/functions/__tests__/stage-2-customer-resolver.test.ts</automated>
  </verify>
  <done>Source + test renamed via git mv (blame intact); id + event + export symbol updated; test passes in isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Patch events.ts entry for label-resolve → stage-2.customer-resolve</name>
  <files>web/lib/inngest/events.ts</files>
  <action>
In `web/lib/inngest/events.ts`, locate the `"debtor-email/label-resolve.requested"` key (currently around line 245). Replace the KEY with `"debtor-email/stage-2.customer-resolve.requested"`. Keep the `data` shape unchanged (the four fields: automation_run_id, swarm_type, category_key, message_id, source_mailbox). Keep the JSDoc comment — update the comment text to reflect the new event name + Phase 88.1.

Do NOT add a transitional dual-emit entry — D-02 is atomic deploy.

Run `cd web && npx tsc --noEmit` to confirm the rest of the codebase still typechecks (will fail until Plans 04+05+06 land too — that's expected; record the failures as "expected callers in remaining plans" but DO confirm the failure list matches the audit's call-site inventory for this event only).
  </action>
  <verify>
    <automated>cd web && grep -n "debtor-email/stage-2.customer-resolve.requested" lib/inngest/events.ts && ! grep -n "debtor-email/label-resolve.requested" lib/inngest/events.ts</automated>
  </verify>
  <done>events.ts has the new key, the old key is gone, data shape unchanged.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88.1-05 | T (Tampering) | git rename detection on file moves | mitigate | use `git mv` not delete+create per R-05 |
| T-88.1-06 | T (Tampering) | inngest.send call-site destructuring breaks `this` binding | mitigate | task action explicitly forbids destructuring (Phase 65 lesson in CLAUDE.md) |
</threat_model>

<verification>
- Test in isolation passes.
- Grep confirms new id + new event name present in the renamed source file.
- Grep confirms zero `classifier/label-resolver` occurrences in `web/lib/inngest/functions/stage-2-customer-resolver.ts`.
</verification>

<success_criteria>
- File pair renamed with git rename detection intact.
- One of the seven legacy strings (`classifier/label-resolver` + `debtor-email/label-resolve.requested`) is now fully purged from the renamed source/test pair; remaining call-site purges happen in Plan 06.
</success_criteria>

<output>
After completion, create `.planning/phases/88.1-.../88.1-03-SUMMARY.md`.
</output>
