---
phase: 88.1-stage-named-inngest-functions-stage-2-telemetry-alignment
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - web/app/api/inngest/route.ts
  - web/lib/automations/debtor-email/label-email-in-icontroller.ts
  - web/lib/inngest/functions/classifier-screen-worker.ts
  - web/lib/inngest/functions/classifier-verdict-worker.ts
  - web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts
  - web/lib/inngest/functions/__tests__/classifier-verdict-worker.test.ts
  - web/app/(dashboard)/automations/[swarm]/_shell/_lib/build-stage-audit-map.ts
  - web/app/(dashboard)/automations/[swarm]/stage-4/page.tsx
  - web/scripts/replay-stage1-unknown-failures.ts
autonomous: true
requirements: [D-01, D-08, R-01]
tags: [inngest, rename, wiring, grep-guard]

must_haves:
  truths:
    - "route.ts imports + functions array reference only the 4 new function-id files + new export symbols."
    - "Every remaining call-site under web/ that previously sent one of the 3 legacy event names now sends the new name."
    - "Every UI / script reference to a legacy function id is updated."
    - "The Plan 02 grep guard test (`no-legacy-inngest-names`) now passes GREEN."
    - "Full Vitest suite + typecheck + build + lint all exit 0."
  artifacts:
    - path: "web/app/api/inngest/route.ts"
      provides: "Inngest serve registration with new function ids"
      contains: "stage2CustomerResolver"
  key_links:
    - from: "route.ts"
      to: "renamed function files"
      via: "import { stage2CustomerResolver } from '@/lib/inngest/functions/stage-2-customer-resolver'"
      pattern: "stage-2-customer-resolver|stage-2-icontroller-label-applier|stage-1-icontroller-noise-cleanup"
---

<objective>
Close the rename loop: rewire `route.ts`, every producer call-site, every UI/script reference, and run the Plan 02 grep guard to GREEN. After this plan, zero of the 7 legacy strings exist anywhere under `web/`.

Purpose: deliver a single mergeable PR per D-08 Wave 1 boundary, atomic per D-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-CONTEXT.md
@.planning/phases/88.1-stage-named-inngest-functions-stage-2-telemetry-alignment/88.1-AUDIT.md
@CLAUDE.md
@web/app/api/inngest/route.ts
@web/lib/automations/debtor-email/label-email-in-icontroller.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire route.ts imports + functions array</name>
  <files>web/app/api/inngest/route.ts</files>
  <action>
In `web/app/api/inngest/route.ts`:

Update imports:
- `import { cleanupIControllerDispatch } from "@/lib/inngest/functions/debtor-email-icontroller-cleanup-dispatcher";` → `import { stage1IcontrollerNoiseCleanupDispatcher } from "@/lib/inngest/functions/stage-1-icontroller-noise-cleanup-dispatcher";`
- `import { cleanupIControllerShardWorker } from "@/lib/inngest/functions/debtor-email-icontroller-cleanup-worker";` → `import { stage1IcontrollerNoiseCleanup } from "@/lib/inngest/functions/stage-1-icontroller-noise-cleanup";`
- `import { debtorEmailIcontrollerTagger } from "@/lib/inngest/functions/debtor-email-icontroller-tagger";` → `import { stage2IcontrollerLabelApplier } from "@/lib/inngest/functions/stage-2-icontroller-label-applier";`
- `import { classifierLabelResolver } from "@/lib/inngest/functions/classifier-label-resolver";` → `import { stage2CustomerResolver } from "@/lib/inngest/functions/stage-2-customer-resolver";`

Update the `functions: [...]` array entries to the four new symbol names. Preserve every OTHER function in the array verbatim.
  </action>
  <verify>
    <automated>cd web && grep -nF "stage2CustomerResolver" app/api/inngest/route.ts && grep -nF "stage2IcontrollerLabelApplier" app/api/inngest/route.ts && grep -nF "stage1IcontrollerNoiseCleanup" app/api/inngest/route.ts && grep -nF "stage1IcontrollerNoiseCleanupDispatcher" app/api/inngest/route.ts && ! grep -nE "classifierLabelResolver|debtorEmailIcontrollerTagger|cleanupIControllerShardWorker|cleanupIControllerDispatch" app/api/inngest/route.ts</automated>
  </verify>
  <done>route.ts uses only new symbols + new file paths; the four legacy import bindings are gone.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire producer call-sites + UI + scripts to new event names + new function-id references</name>
  <files>web/lib/automations/debtor-email/label-email-in-icontroller.ts, web/lib/inngest/functions/classifier-screen-worker.ts, web/lib/inngest/functions/classifier-verdict-worker.ts, web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts, web/lib/inngest/functions/__tests__/classifier-verdict-worker.test.ts, web/app/(dashboard)/automations/[swarm]/_shell/_lib/build-stage-audit-map.ts, web/app/(dashboard)/automations/[swarm]/stage-4/page.tsx, web/scripts/replay-stage1-unknown-failures.ts</files>
  <action>
Work the audit-driven list (88.1-AUDIT.md § 1 + § 2). For every file:line entry tagged `RENAME`, apply the matching substitution:

| Legacy → New |
|---|
| `classifier/label-resolver` → `stage-2-customer-resolver` |
| `automations/debtor-email-icontroller-tagger` → `stage-2-icontroller-label-applier` |
| `automations/debtor-email-icontroller-shard-worker` → `stage-1-icontroller-noise-cleanup` |
| `automations/debtor-email-icontroller-dispatch` → `stage-1-icontroller-noise-cleanup-dispatcher` |
| `debtor-email/label-resolve.requested` → `debtor-email/stage-2.customer-resolve.requested` |
| `debtor-email/icontroller-tag.requested` → `debtor-email/stage-2.icontroller-label.requested` |
| `icontroller/cleanup.shard.requested` → `debtor-email/stage-1.icontroller-cleanup.requested` |

For producer call-sites (`inngest.send({ name: ... })`):
- `web/lib/automations/debtor-email/label-email-in-icontroller.ts` — likely the producer of `debtor-email/icontroller-tag.requested`. Inline-call the renamed event.
- `web/lib/inngest/functions/classifier-verdict-worker.ts` — likely producer of `debtor-email/label-resolve.requested` via `swarm_noise_categories.swarm_dispatch` registry lookup. **WAIT** — that dispatch is registry-driven (the dispatch name lives in `public.swarm_noise_categories.swarm_dispatch` rows, see comment in events.ts:524). Two actions needed:
  1. Code: update any HARD-CODED occurrences of the legacy event in classifier-verdict-worker.ts (likely in comments / fallback paths).
  2. Data: a one-shot Supabase MCP `execute_sql` to UPDATE `public.swarm_noise_categories` SET `swarm_dispatch = 'debtor-email/stage-2.customer-resolve.requested'` WHERE `swarm_dispatch = 'debtor-email/label-resolve.requested'`. Same UPDATE pattern if any rows reference `debtor-email/icontroller-tag.requested`. Run a SELECT first to confirm the affected row count; record before/after counts in the plan's SUMMARY.
- `web/lib/inngest/functions/classifier-screen-worker.ts` — likely contains comments referencing cleanup-worker. Update comments only if they hold one of the 7 legacy strings.
- Test files `classifier-screen-worker.test.ts` + `classifier-verdict-worker.test.ts` — update any `expect(inngest.send).toHaveBeenCalledWith({ name: "<legacy>" })` mock-call assertions to the new names.

For UI/script consumers:
- `web/app/(dashboard)/automations/[swarm]/_shell/_lib/build-stage-audit-map.ts` — likely contains a map keyed by function id (for the per-stage audit panel). Replace legacy keys with new keys.
- `web/app/(dashboard)/automations/[swarm]/stage-4/page.tsx` — same kind of reference; replace.
- `web/scripts/replay-stage1-unknown-failures.ts` — script likely calls `inngest.send({ name: "debtor-email/label-resolve.requested" })`. Inline-replace.

Throughout: NEVER destructure `inngest.send`. Inline calls only. Preserve every other field in each `inngest.send` payload verbatim.

Run after all edits:

```
cd web && npm run lint
cd web && npx tsc --noEmit
cd web && npm run test
cd web && npm run build
```

All must exit 0. The grep guard test (`no-legacy-inngest-names`) is part of `npm run test`; it MUST be GREEN before this plan is done.
  </action>
  <verify>
    <automated>cd web && npx vitest run scripts/__tests__/no-legacy-inngest-names.test.ts && npx tsc --noEmit && npm run lint && npm run build && npm run test</automated>
  </verify>
  <done>Plan 02 grep guard exits 0. tsc + lint + build all green. Supabase `swarm_noise_categories.swarm_dispatch` updated (with before/after counts captured in SUMMARY).</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88.1-12 | T (Tampering) | registry-driven dispatch name in `swarm_noise_categories` not updated → silently fails after deploy | mitigate | Task 2 explicit Supabase UPDATE + before/after row count recorded in SUMMARY; Plan 07 smoke confirms live |
| T-88.1-13 | T (Tampering) | producer call-site missed | mitigate | Plan 02 grep guard is the single GREEN gate |
| T-88.1-14 | T (Tampering) | inngest.send destructuring | mitigate | inline-call repeated in action |
</threat_model>

<verification>
- `npx vitest run scripts/__tests__/no-legacy-inngest-names.test.ts` exits 0 (grep guard GREEN).
- `npx tsc --noEmit` exits 0.
- `npm run lint && npm run build && npm run test` all exit 0.
- Supabase: `SELECT COUNT(*) FROM public.swarm_noise_categories WHERE swarm_dispatch IN ('debtor-email/label-resolve.requested', 'debtor-email/icontroller-tag.requested', 'icontroller/cleanup.shard.requested')` returns 0.
</verification>

<success_criteria>
- The PR is mergeable as a single atomic unit per D-02.
- All 7 legacy strings extinct from `web/`.
- Registry row data (swarm_noise_categories.swarm_dispatch) matches new event names.
</success_criteria>

<output>
After completion, create `.planning/phases/88.1-.../88.1-06-SUMMARY.md`.
</output>
