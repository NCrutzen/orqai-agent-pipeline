---
phase: 34-foundation-auth
plan: 03
type: execute
wave: 3
depends_on:
  - 34-01
  - 34-02
files_modified: []
autonomous: false
requirements:
  - FOUND-01
  - FOUND-02
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - PROJ-04
must_haves:
  truths:
    - "User can sign in with their Moyne Roberts M365 account and reach the app dashboard"
    - "A personal Microsoft account is rejected at login (tenant restriction verified)"
    - "User can create a named project and invite colleagues to it"
    - "User only sees projects they belong to -- no cross-project data leakage"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 34 deliverable: auth flow (SSO + email/password), tenant restriction, project CRUD, and colleague invitation.

Purpose: Confirm all four phase success criteria are met before moving to Phase 35.
Output: Human-verified confirmation that auth and project management work end-to-end.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/34-foundation-auth/34-CONTEXT.md
@.planning/phases/34-foundation-auth/34-01-SUMMARY.md
@.planning/phases/34-foundation-auth/34-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Auth flow and tenant restriction</name>
  <files>none</files>
  <action>
    Human verification of the complete auth system. No code changes -- this is a manual verification checkpoint.
  </action>
  <what-built>
    Complete auth system with M365 SSO (tenant-restricted to Moyne Roberts) and email/password for external invited users. Protected dashboard with collapsible sidebar.
  </what-built>
  <how-to-verify>
    Prerequisites: Ensure Supabase project is configured with Azure OAuth provider (tenant-specific URL) and schema.sql has been executed.

    1. Open http://localhost:3000 -- should redirect to /login
    2. Verify login page shows "Sign in with Microsoft" button AND email/password fields
    3. Click "Sign in with Microsoft" -- should redirect to Azure AD login
    4. Sign in with your Moyne Roberts work account -- should redirect back to dashboard
    5. Verify sidebar shows: Dashboard, Projects, Runs, Settings, Profile
    6. Verify sidebar collapses to icons-only mode
    7. Sign out and try signing in with a personal Microsoft account (@outlook.com or @hotmail.com) -- should be REJECTED (tenant restriction)
    8. If rejected: tenant restriction is working correctly
    9. If personal account succeeds: Azure Tenant URL in Supabase is misconfigured (set to `common` instead of tenant-specific URL)
  </how-to-verify>
  <verify>Human confirms auth flow works end-to-end</verify>
  <done>SSO login works with Moyne Roberts account. Personal accounts are rejected. Dashboard renders with sidebar.</done>
  <resume-signal>Type "approved" if auth works correctly, or describe any issues found</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Project CRUD and invitation flow</name>
  <files>none</files>
  <action>
    Human verification of project management features. No code changes -- this is a manual verification checkpoint.
  </action>
  <what-built>
    Project list home page with search/filter, create project modal, colleague invitation with AD autocomplete + manual email, and project-scoped data isolation via RLS.
  </what-built>
  <how-to-verify>
    1. Dashboard home page shows activity stats section (with placeholder values) and empty state message "Create your first project to get started"
    2. Click "Create Project" -- modal opens with name and description fields
    3. Create a project with name "Test Project" -- modal closes, project appears in list
    4. Click the project card -- navigates to project detail page showing name, description, members (you)
    5. Click "Invite Member" -- modal shows directory search and email input modes
    6. Try AD directory search (type a colleague's name) -- autocomplete results appear
    7. Invite an external email address -- invite API is called
    8. Go back to home page -- search/filter works on project list
    9. (If possible) Sign in as a different user and verify they do NOT see your project (RLS isolation)
  </how-to-verify>
  <verify>Human confirms project CRUD and invitation flow works end-to-end</verify>
  <done>Projects can be created, viewed, and searched. Invitations work for both AD and email users. RLS isolation confirmed.</done>
  <resume-signal>Type "approved" if projects and invitations work correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All four phase success criteria from ROADMAP.md verified:
1. User can sign in with their Moyne Roberts M365 account and reach the app dashboard
2. A personal Microsoft account is rejected at login (tenant restriction verified)
3. User can create a named project and invite colleagues to it
4. User only sees projects they belong to -- no cross-project data leakage
</verification>

<success_criteria>
- Human confirms SSO login works with Moyne Roberts account
- Human confirms personal Microsoft accounts are rejected
- Human confirms project creation and invitation flow works
- Human confirms project isolation (users only see their own projects)
</success_criteria>

<output>
After completion, create `.planning/phases/34-foundation-auth/34-03-SUMMARY.md`
</output>
