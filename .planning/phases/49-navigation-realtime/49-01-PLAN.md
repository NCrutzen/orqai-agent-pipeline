---
phase: 49-navigation-realtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/lib/v7/types.ts
  - web/lib/v7/swarm-data.ts
  - web/lib/v7/use-realtime-table.ts
  - web/components/v7/swarm-realtime-provider.tsx
  - web/components/v7/realtime-status-indicator.tsx
  - web/components/v7/sidebar-mini-stat.tsx
  - web/components/v7/swarm-list-item.tsx
  - web/components/v7/swarm-sidebar.tsx
  - web/components/v7/sidebar-chooser.tsx
  - web/app/(dashboard)/layout.tsx
autonomous: true
requirements: [NAV-01, NAV-03, RT-01]

must_haves:
  truths:
    - "When a user visits a /swarm/* route, the new V7 swarm sidebar renders; on other dashboard routes, the legacy shadcn sidebar still renders"
    - "The V7 sidebar displays one row per swarm the user has project_members access to, sorted by updated_at desc"
    - "Each sidebar row shows mini-stat pills with active jobs count (swarm_jobs.stage in ready/progress/review) and agent count (swarm_agents)"
    - "Mini-stats update in real time when a new swarm_jobs or swarm_agents row is inserted or updated, without page refresh"
    - "SwarmRealtimeProvider opens exactly one Supabase Realtime channel per swarm view (verifiable via supabase.getChannels())"
    - "useRealtimeTable('jobs' | 'agents' | 'events' | 'briefings') returns the live rows and channel status from the provider"
    - "Provider cleanup removes the channel on unmount; navigating between swarms leaves no stale channels"
  artifacts:
    - path: "web/lib/v7/types.ts"
      provides: "Row types for the 4 V7 tables and the Realtime bundle"
      exports: ["AgentEvent", "SwarmJob", "SwarmAgent", "SwarmBriefing", "RealtimeBundle", "ChannelStatus"]
      min_lines: 30
    - path: "web/lib/v7/swarm-data.ts"
      provides: "Server-side swarm list fetcher with initial counts"
      exports: ["fetchSwarmsWithCounts", "SwarmWithCounts"]
      min_lines: 25
    - path: "web/lib/v7/use-realtime-table.ts"
      provides: "Typed hook reading Realtime rows from SwarmRealtimeContext"
      exports: ["useRealtimeTable"]
      min_lines: 15
    - path: "web/components/v7/swarm-realtime-provider.tsx"
      provides: "Single-channel postgres_changes provider for a swarm view"
      exports: ["SwarmRealtimeProvider", "SwarmRealtimeContext"]
      contains: "supabase.channel\\(`swarm:\\$\\{swarmId\\}`\\)"
      min_lines: 80
    - path: "web/components/v7/swarm-sidebar.tsx"
      provides: "V7 sidebar with swarm list, mini-stats, dashboard-wide Realtime"
      exports: ["SwarmSidebar"]
      contains: "dashboard:swarms"
      min_lines: 100
    - path: "web/components/v7/sidebar-chooser.tsx"
      provides: "Client wrapper that picks V7 vs legacy sidebar by pathname"
      exports: ["SidebarChooser"]
      min_lines: 15
    - path: "web/app/(dashboard)/layout.tsx"
      provides: "Branches between V7 and legacy sidebar; passes SSR swarm list"
      contains: "SidebarChooser"
  key_links:
    - from: "web/components/v7/swarm-realtime-provider.tsx"
      to: "@supabase/supabase-js channel API"
      via: "channel(...).on('postgres_changes', ...).subscribe"
      pattern: "postgres_changes"
    - from: "web/components/v7/swarm-realtime-provider.tsx"
      to: "supabase.removeChannel"
      via: "useEffect cleanup"
      pattern: "removeChannel\\(channel\\)"
    - from: "web/components/v7/swarm-sidebar.tsx"
      to: "swarm_jobs + swarm_agents via dashboard-wide channel"
      via: "channel('dashboard:swarms').on('postgres_changes', ...)"
      pattern: "dashboard:swarms"
    - from: "web/app/(dashboard)/layout.tsx"
      to: "SidebarChooser (client)"
      via: "Server → Client props boundary"
      pattern: "<SidebarChooser"
---

<objective>
Build the realtime infrastructure and the V7 swarm sidebar.

Purpose: Delivers the single-channel Realtime distribution pattern (RT-01) and the live swarm navigation (NAV-01, NAV-03) that every later V7 phase consumes. The sidebar is the user's entry point into the swarm operating view.

Output: Typed row definitions, a server-side swarm fetcher, the SwarmRealtimeProvider with its useRealtimeTable hook, the V7 sidebar with mini-stat pills, and the (dashboard) layout branch that renders the right sidebar per route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-navigation-realtime/49-CONTEXT.md
@.planning/phases/49-navigation-realtime/49-UI-SPEC.md
@.planning/phases/49-navigation-realtime/49-RESEARCH.md
@.planning/phases/48-foundation/48-02-SUMMARY.md
@.planning/phases/48-foundation/48-03-SUMMARY.md
@supabase/migrations/20260415_v7_foundation.sql
@web/lib/supabase/broadcast-client.ts
@web/lib/supabase/client.ts
@web/lib/supabase/server.ts
@web/components/ui/glass-card.tsx
@web/components/app-sidebar.tsx
@web/app/(dashboard)/layout.tsx
@web/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define V7 row types and Realtime bundle shape</name>
  <files>web/lib/v7/types.ts</files>
  <read_first>supabase/migrations/20260415_v7_foundation.sql, .planning/phases/49-navigation-realtime/49-RESEARCH.md</read_first>
  <action>Create `web/lib/v7/types.ts` exporting:

1. `AgentEvent` — mirrors agent_events row: id (string), swarm_id (string), agent_name (string), event_type ('thinking' | 'tool_call' | 'tool_result' | 'waiting' | 'done' | 'error' | 'delegation'), span_id (string | null), parent_span_id (string | null), content (unknown), started_at (string | null), ended_at (string | null), created_at (string).
2. `SwarmJob` — mirrors swarm_jobs row: id, swarm_id, title, description (string | null), stage ('backlog' | 'ready' | 'progress' | 'review' | 'done'), priority ('low' | 'normal' | 'high' | 'urgent'), assigned_agent (string | null), tags (unknown), position (number), created_at, updated_at.
3. `SwarmAgent` — mirrors swarm_agents row: id, swarm_id, agent_name, role (string | null), status ('idle' | 'active' | 'waiting' | 'error' | 'offline'), parent_agent (string | null), metrics (unknown), skills (unknown), orqai_deployment_id (string | null), created_at, updated_at.
4. `SwarmBriefing` — mirrors swarm_briefings row: id, swarm_id, narrative (string), metrics_snapshot (unknown), generated_at, expires_at.
5. `ChannelStatus` — `'SUBSCRIBED' | 'TIMED_OUT' | 'CLOSED' | 'CHANNEL_ERROR' | 'CONNECTING'`.
6. `RealtimeBundle` — `{ events: AgentEvent[]; jobs: SwarmJob[]; agents: SwarmAgent[]; briefings: SwarmBriefing[]; status: ChannelStatus }`.
7. `EMPTY_BUNDLE` — exported const matching RealtimeBundle with empty arrays and status 'CONNECTING'.

All timestamps are ISO strings from Supabase. Use `unknown` (not `any`) for JSONB fields; downstream components cast as needed.</action>
  <verify>tsc --noEmit on web/ passes; grep exports all 7 names</verify>
  <acceptance_criteria>
    - File exports AgentEvent, SwarmJob, SwarmAgent, SwarmBriefing, ChannelStatus, RealtimeBundle, EMPTY_BUNDLE
    - SwarmJob.stage union contains 'backlog' | 'ready' | 'progress' | 'review' | 'done'
    - AgentEvent.event_type union contains all 7 event types from the migration CHECK constraint
    - No use of `any`; JSONB fields typed as `unknown`
  </acceptance_criteria>
  <done>Types compile and can be imported by provider, hook, and sidebar</done>
</task>

<task type="auto">
  <name>Task 2: Server-side swarm list fetcher with initial counts</name>
  <files>web/lib/v7/swarm-data.ts</files>
  <read_first>web/lib/supabase/server.ts, web/app/(dashboard)/page.tsx, .planning/phases/49-navigation-realtime/49-RESEARCH.md</read_first>
  <action>Create `web/lib/v7/swarm-data.ts` exporting:

1. `SwarmWithCounts` type: `{ id: string; name: string; description: string | null; activeJobs: number; agentCount: number }`.
2. `fetchSwarmsWithCounts(): Promise<SwarmWithCounts[]>` — server-only (uses `createClient` from `@/lib/supabase/server`). Implementation:
   - Get `projects` via RLS (user's project_members gate already applied by Supabase): `select('id, name, description').order('updated_at', { ascending: false })`.
   - If empty, return [].
   - Collect swarm IDs; fetch `swarm_jobs` `select('swarm_id, stage').in('swarm_id', ids)` and `swarm_agents` `select('swarm_id').in('swarm_id', ids)` in parallel via Promise.all.
   - Return swarms with activeJobs (jobs where stage in ['ready','progress','review'] and swarm_id matches) and agentCount (agents where swarm_id matches).
3. Export `ACTIVE_JOB_STAGES = ['ready', 'progress', 'review'] as const` so the sidebar client can reuse the same definition for live recomputation.

File must start with no `"use client"` directive — this is a server-only helper.</action>
  <verify>tsc --noEmit; running a local SSR render hits Supabase once and returns a non-empty array when the test user has a project</verify>
  <acceptance_criteria>
    - Exports fetchSwarmsWithCounts, SwarmWithCounts, ACTIVE_JOB_STAGES
    - Uses Promise.all for jobs + agents fetches
    - ACTIVE_JOB_STAGES is ['ready', 'progress', 'review']
    - No "use client" directive
  </acceptance_criteria>
  <done>Server component can call fetchSwarmsWithCounts() and receive typed array</done>
</task>

<task type="auto">
  <name>Task 3: SwarmRealtimeProvider with single-channel multi-event subscription</name>
  <files>web/components/v7/swarm-realtime-provider.tsx, web/lib/v7/use-realtime-table.ts</files>
  <read_first>web/lib/supabase/broadcast-client.ts, web/lib/supabase/client.ts, web/lib/v7/types.ts, .planning/phases/49-navigation-realtime/49-RESEARCH.md</read_first>
  <action>Create `web/components/v7/swarm-realtime-provider.tsx` as a client component ('use client' directive at top):

1. Import React Context createContext, useContext, useEffect, useMemo, useState; import createClient from `@/lib/supabase/client`; import types from `@/lib/v7/types`.
2. Export `SwarmRealtimeContext = createContext<RealtimeBundle | null>(null)`.
3. Export `SwarmRealtimeProvider({ swarmId, children })` component:
   - State: `const [bundle, setBundle] = useState<RealtimeBundle>(EMPTY_BUNDLE)`.
   - useEffect deps `[swarmId]`:
     - Create supabase client.
     - Declare `let cancelled = false`.
     - Promise.all fetching initial snapshots for all 4 tables filtered by swarm_id=${swarmId}. On resolve, if !cancelled, setBundle merging the 4 arrays.
     - Build channel: `supabase.channel(\`swarm:${swarmId}\`)`.
     - For each of the 4 tables (agent_events, swarm_jobs, swarm_agents, swarm_briefings), call `.on('postgres_changes', { event: '*', schema: 'public', table: <table>, filter: \`swarm_id=eq.${swarmId}\` }, (payload) => applyMutation(<logical-name>, payload))`. Use logical names: 'events', 'jobs', 'agents', 'briefings'.
     - `.subscribe((status) => setBundle(prev => ({ ...prev, status: status as ChannelStatus })))`.
     - Cleanup returns `() => { cancelled = true; supabase.removeChannel(channel); }`.
   - `applyMutation` (closure helper): switch on `payload.eventType` INSERT → push to array, UPDATE → map by id, DELETE → filter out by old.id. Uses setBundle with functional update.
   - Wrap value in `useMemo(() => bundle, [bundle])` to provide a stable reference when state didn't change (micro-optimization to avoid re-render if the ref is same).
4. Create `web/lib/v7/use-realtime-table.ts` as a non-'use client' module (hook itself can be called from client components only). Export `useRealtimeTable<K extends 'events' | 'jobs' | 'agents' | 'briefings'>(table: K)` returning `{ rows: RealtimeBundle[K extends 'events' ? 'events' : ...], status: ChannelStatus }`. Implement via useContext(SwarmRealtimeContext); throw if null with message 'useRealtimeTable must be used inside SwarmRealtimeProvider'.

Simplify the generic: map table name to array property. TableMap = { events: AgentEvent; jobs: SwarmJob; agents: SwarmAgent; briefings: SwarmBriefing }. Return type: `{ rows: TableMap[K][]; status: ChannelStatus }`.

Do NOT add console.log statements. Do NOT mock anything.</action>
  <verify>tsc --noEmit; grep confirms postgres_changes and removeChannel and `swarm:${swarmId}`</verify>
  <acceptance_criteria>
    - swarm-realtime-provider.tsx starts with "use client"
    - Provider creates exactly one channel via supabase.channel(`swarm:${swarmId}`)
    - Four chained .on('postgres_changes', ...) calls, one per V7 table
    - Each filter uses `swarm_id=eq.${swarmId}`
    - useEffect cleanup calls supabase.removeChannel(channel)
    - applyMutation handles INSERT, UPDATE, DELETE branches
    - useRealtimeTable throws if context is null
  </acceptance_criteria>
  <done>Provider mounts with a swarmId and children can read rows via useRealtimeTable</done>
</task>

<task type="auto">
  <name>Task 4: Sidebar mini-stat pill and realtime status indicator primitives</name>
  <files>web/components/v7/sidebar-mini-stat.tsx, web/components/v7/realtime-status-indicator.tsx</files>
  <read_first>web/app/globals.css, web/components/ui/glass-card.tsx, .planning/phases/49-navigation-realtime/49-UI-SPEC.md</read_first>
  <action>Create two small client components:

1. `web/components/v7/sidebar-mini-stat.tsx` (no 'use client' needed — pure presentational):
   - Export `SidebarMiniStat({ count, label, tone }: { count: number; label: string; tone: 'blue' | 'teal' })`.
   - Renders a pill span: `inline-flex items-center gap-1 px-2 py-0.5 rounded-[var(--v7-radius-pill)] text-[12px] leading-[1.3]`.
   - Background/text color via tone: `tone === 'blue'` → `bg-[var(--v7-blue-soft)] text-[var(--v7-blue)]`, `tone === 'teal'` → `bg-[var(--v7-teal-soft)] text-[var(--v7-teal)]`.
   - Content: `{count} {label}` — e.g., "3 active".

2. `web/components/v7/realtime-status-indicator.tsx`:
   - 'use client' directive (reads status prop, but keep client-only for future use).
   - Export `RealtimeStatusIndicator({ status }: { status: ChannelStatus })` — returns null when `status === 'SUBSCRIBED'`.
   - When CHANNEL_ERROR or TIMED_OUT → amber dot + "Reconnecting…" (`text-[var(--v7-muted)]`, 12px, leading-1.3).
   - When CLOSED → red dot + "Disconnected. Refresh to retry."
   - When CONNECTING → hidden (treated like SUBSCRIBED for first paint).
   - Dot: `inline-block w-2 h-2 rounded-full` with `bg-[var(--v7-amber)]` or `bg-[var(--v7-red)]`.
   - Import `ChannelStatus` from `@/lib/v7/types`.</action>
  <verify>tsc --noEmit; grep confirms both components exist and use V7 tokens via arbitrary values</verify>
  <acceptance_criteria>
    - SidebarMiniStat renders a single span with rounded-full-pill and V7 tone classes
    - RealtimeStatusIndicator returns null when status is 'SUBSCRIBED'
    - Both files use --v7-* CSS variables via Tailwind arbitrary values
  </acceptance_criteria>
  <done>Both primitives importable and renderable</done>
</task>

<task type="auto">
  <name>Task 5: SwarmListItem and SwarmSidebar with dashboard-wide Realtime</name>
  <files>web/components/v7/swarm-list-item.tsx, web/components/v7/swarm-sidebar.tsx</files>
  <read_first>web/lib/v7/swarm-data.ts, web/lib/v7/types.ts, web/components/v7/sidebar-mini-stat.tsx, web/lib/supabase/client.ts, .planning/phases/49-navigation-realtime/49-UI-SPEC.md, .planning/phases/49-navigation-realtime/49-RESEARCH.md</read_first>
  <action>Create `web/components/v7/swarm-list-item.tsx` as a client component:

1. 'use client' at top.
2. Export `SwarmListItem({ swarm, activeJobs, agentCount, isActive })`:
   - Props: swarm (`SwarmWithCounts`), activeJobs (number), agentCount (number), isActive (boolean).
   - Renders an `<a href={\`/swarm/${swarm.id}\`}>` element (Next.js Link is fine too — use `next/link`).
   - Layout: flex column gap-1, rounded-[var(--v7-radius-inner)], transition-all duration-[180ms] ease-out.
   - Default state: padding p-3, bg-transparent, hover `bg-[rgba(255,255,255,0.04)]` translate-x-0.5.
   - Active state: left-border-l-[3px] border-[var(--v7-teal)] bg-[var(--v7-teal-soft)] pl-[9px] (to keep text aligned after the 3px border).
   - Swarm name: 16px text, `text-[var(--v7-text)]`, truncate.
   - Mini-stats row: flex gap-2, renders `<SidebarMiniStat count={activeJobs} label="active" tone="blue" />` and `<SidebarMiniStat count={agentCount} label="agents" tone="teal" />`.

Create `web/components/v7/swarm-sidebar.tsx` as a client component:

1. 'use client' at top.
2. Imports: useEffect, useMemo, useState from React; createClient from `@/lib/supabase/client`; usePathname from `next/navigation`; types from `@/lib/v7/types`; `SwarmWithCounts` from `@/lib/v7/swarm-data`; `ACTIVE_JOB_STAGES` from `@/lib/v7/swarm-data`; `SwarmListItem`; lucide Sparkles icon (for brand mark placeholder).
3. Export `SwarmSidebar({ swarms, initialJobs, initialAgents })`:
   - Props: swarms (SwarmWithCounts[]), initialJobs (`Array<{ swarm_id: string; stage: string }>`), initialAgents (`Array<{ swarm_id: string }>`).
   - State: `jobs` initialized to initialJobs, `agents` initialized to initialAgents.
   - pathname = usePathname(); activeId = pathname.startsWith('/swarm/') ? pathname.split('/')[2] : null.
   - useEffect [] (mount once):
     - createClient().channel('dashboard:swarms')
       - .on('postgres_changes', { event: '*', schema: 'public', table: 'swarm_jobs' }, (p) => { setJobs(prev => applyJobMutation(prev, p)); })
       - .on('postgres_changes', { event: '*', schema: 'public', table: 'swarm_agents' }, (p) => { setAgents(prev => applyAgentMutation(prev, p)); })
       - .subscribe();
     - cleanup: supabase.removeChannel(channel).
   - Derive `statsBySwarm` via useMemo from swarms + jobs + agents:
     - Map<id, { active: number; agents: number }> where active = count of jobs matching swarm_id and stage in ACTIVE_JOB_STAGES; agents = count of agents matching swarm_id.
   - Render:
     - `<aside>` with classes: `w-[286px] h-screen overflow-hidden flex flex-col gap-5 p-6 border-r border-[var(--v7-line)] bg-[var(--v7-bg)]`. Use `[backdrop-filter:blur(var(--v7-glass-blur))]` and overlay gradient via `style={{ backgroundImage: 'linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01))' }}`.
     - Brand block: div flex-row gap-3 items-center. 48x48 rounded-[var(--v7-radius-inner)] with `bg-[linear-gradient(135deg,var(--v7-teal),var(--v7-blue))]` containing a lucide `Sparkles` icon (size 24, text-white). Next to it: column with "Agent OS" in `font-[var(--font-cabinet)]` 32px weight-700 and "Control room for swarms" 12px `text-[var(--v7-faint)]`.
     - Section label "Swarms": uppercase tracking-[0.1em] text-[12px] `text-[var(--v7-faint)]`.
     - Swarm list: flex-col gap-2. If swarms.length === 0 render empty state (flex-col gap-1: heading "No swarms configured" 16/400 `text-[var(--v7-text)]`; body "Create your first agent swarm in the projects page to see it appear here." 12/400 `text-[var(--v7-muted)]`). Otherwise map swarms → SwarmListItem with activeJobs and agentCount from statsBySwarm, isActive = swarm.id === activeId.
     - Footer (mt-auto): flex-col gap-1, text-[12px] `text-[var(--v7-faint)]`. Two lines: "{swarms.length} active swarms" and "{totalJobsToday} jobs today" (totalJobsToday = total length of jobs array — simple for Phase 49).

4. Helper functions inside the file (not exported): applyJobMutation and applyAgentMutation — switch on eventType, apply to prev array. Keep them concise: INSERT → [...prev, new], UPDATE → prev.map(r => r.id === new.id ? new : r), DELETE → prev.filter(r => r.id !== old.id).

Use Tailwind arbitrary values for all V7 tokens; do not add new tokens.</action>
  <verify>tsc --noEmit; `grep -n "dashboard:swarms" web/components/v7/swarm-sidebar.tsx`; `grep -n "SidebarMiniStat" web/components/v7/swarm-sidebar.tsx`</verify>
  <acceptance_criteria>
    - swarm-sidebar.tsx starts with "use client"
    - Opens one channel named exactly 'dashboard:swarms'
    - Two .on('postgres_changes', ...) chained for swarm_jobs and swarm_agents (no swarm_id filter — dashboard-wide)
    - Cleanup calls supabase.removeChannel(channel)
    - Uses usePathname to compute activeId
    - Empty-state copy matches UI-SPEC: "No swarms configured" + body string
    - Uses SidebarMiniStat for mini-stats with tones blue (active) and teal (agents)
    - Uses ACTIVE_JOB_STAGES from swarm-data.ts (not a local duplicate)
  </acceptance_criteria>
  <done>Component renders a list with live-updating pills</done>
</task>

<task type="auto">
  <name>Task 6: SidebarChooser client wrapper + dashboard layout branching</name>
  <files>web/components/v7/sidebar-chooser.tsx, web/app/(dashboard)/layout.tsx</files>
  <read_first>web/app/(dashboard)/layout.tsx, web/components/app-sidebar.tsx, web/components/v7/swarm-sidebar.tsx, web/lib/v7/swarm-data.ts</read_first>
  <action>Create `web/components/v7/sidebar-chooser.tsx`:

1. 'use client' directive.
2. Imports: usePathname from next/navigation; AppSidebar from `@/components/app-sidebar`; SwarmSidebar from `@/components/v7/swarm-sidebar`; types.
3. Export `SidebarChooser({ user, swarms, initialJobs, initialAgents })` — all props passed through from the server layout.
4. Inside: `const pathname = usePathname();` Return `pathname.startsWith('/swarm')` ? `<SwarmSidebar swarms={swarms} initialJobs={initialJobs} initialAgents={initialAgents} />` : `<AppSidebar user={user} />`.

Modify `web/app/(dashboard)/layout.tsx`:

1. Keep the existing auth + project_members gate (do not regress Phase 48-03 behavior).
2. After the access check, call `fetchSwarmsWithCounts()` from `@/lib/v7/swarm-data`. Destructure swarms from it.
3. Also fetch shallow initial rows for the sidebar's dashboard-wide channel:
   - `swarm_jobs` select('id, swarm_id, stage') where swarm_id IN swarm IDs.
   - `swarm_agents` select('id, swarm_id') where swarm_id IN swarm IDs.
   - Guard: if swarms is empty, skip both queries and pass [].
4. Render `<SidebarChooser user={user} swarms={swarms} initialJobs={jobs} initialAgents={agents} />` instead of the current AppSidebar direct render.
5. Keep `<SidebarProvider>` and `<main>` structure — SidebarProvider is shadcn's context for the legacy AppSidebar's icon-collapse behavior. The V7 sidebar does not use it but it does not interfere.

Do NOT remove the existing DEBUG log or other Phase 48-03 lines beyond what is directly replaced.</action>
  <verify>tsc --noEmit; open a /swarm/abc URL in dev → V7 sidebar renders. Open / → legacy sidebar still renders</verify>
  <acceptance_criteria>
    - SidebarChooser uses usePathname.startsWith('/swarm')
    - (dashboard)/layout.tsx calls fetchSwarmsWithCounts() and passes result to SidebarChooser
    - (dashboard)/layout.tsx keeps the project_members COUNT(*) gate unchanged
    - Legacy AppSidebar still renders for non-/swarm routes
    - No direct `<AppSidebar>` import remains in (dashboard)/layout.tsx (moved into SidebarChooser)
  </acceptance_criteria>
  <done>Sidebar choice works; both paths render without runtime errors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd web && tsc --noEmit` passes for all files created or modified by this plan (pre-existing errors in debtor-email-analyzer and sales-email-analyzer are allowed)
- [ ] `grep -n "postgres_changes" web/components/v7/swarm-realtime-provider.tsx` shows 4 matches
- [ ] `grep -n "removeChannel" web/components/v7/swarm-realtime-provider.tsx` shows 1 match in cleanup
- [ ] `grep -n "dashboard:swarms" web/components/v7/swarm-sidebar.tsx` shows 1 match
- [ ] `grep -n "fetchSwarmsWithCounts" web/app/(dashboard)/layout.tsx` shows 1 match
- [ ] File boundaries: no 'use client' in swarm-data.ts (must be server-only); 'use client' in provider, sidebar, list-item, chooser, status-indicator
</verification>

<success_criteria>
- All 6 tasks completed
- All verification checks pass
- Provider + hook pattern established for later phases (51, 52, 53) to consume
- Sidebar renders and updates live on Realtime events
</success_criteria>

<output>
After completion, create `.planning/phases/49-navigation-realtime/49-01-SUMMARY.md` documenting files created, decisions made, and any deviations.
</output>
