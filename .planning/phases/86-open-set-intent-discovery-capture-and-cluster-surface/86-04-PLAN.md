---
phase: 86-open-set-intent-discovery-capture-and-cluster-surface
plan: 04
type: execute
wave: 4
depends_on: [86-01, 86-02, 86-03]
files_modified:
  - .planning/phases/86-open-set-intent-discovery-capture-and-cluster-surface/86-OPERATOR-RUNBOOK.md
  - .planning/phases/86-open-set-intent-discovery-capture-and-cluster-surface/86-VERIFICATION-LOG.md
autonomous: false
requirements: [V-Success-1, V-Success-2, V-Success-3, V-Success-4]
must_haves:
  truths:
    - "Success #1 is RELAXED to 14 days with a day-7 sub-criterion (drift #4: plan-time refinement; CONTEXT.md remains unchanged)"
    - "Operator runbook documents the day-7, day-14, day-21+ verification cadence with the exact SQL + dashboard checks"
    - "Spot-check protocol: 10 random samples per cluster, operator verdicts ≥80% same-intent for Success #3"
    - "Verification log records every check + verdict so the closure (Phase 87) has audit data"
  artifacts:
    - path: ".planning/phases/86-open-set-intent-discovery-capture-and-cluster-surface/86-OPERATOR-RUNBOOK.md"
      provides: "Step-by-step runbook for day-7, day-14, day-21+ checks with SQL + UI steps + verdict templates"
    - path: ".planning/phases/86-open-set-intent-discovery-capture-and-cluster-surface/86-VERIFICATION-LOG.md"
      provides: "Append-only log of operator verdicts; closure phase reads this"
  key_links:
    - from: "operator runbook"
      to: "intent_proposal_clusters + intent_proposal_views"
      via: "SQL queries documented inline + UI navigation steps"
      pattern: "FROM public.intent_proposal_clusters"
---

<objective>
Hand the discovery surface to the operator. Phase 86 only succeeds if the human side of the loop runs — Wave 1+2 build the engine, Wave 3 builds the surface, and Wave 4 lights up the actual feedback loop with a runbook and verdict log.

Per drift #4: Success #1 ("≥5 distinct clusters with ≥3 samples each within 7 days") is RELAXED to 14 days with a new day-7 sub-criterion (≥1 cluster surfaces, proving end-to-end write-through). The relaxation is a plan-time refinement based on the RESEARCH §Q4 forecast (~5 proposals/week → 1-2 clusters in week 1, 4-6 by week 3-4). CONTEXT.md stays unchanged; this plan documents the operational refinement.

Output: a runbook the operator follows for 4 weeks + an append-only verification log that Phase 87 (V8.1 closure) will consume.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-open-set-intent-discovery-capture-and-cluster-surface/86-CONTEXT.md
@.planning/phases/86-open-set-intent-discovery-capture-and-cluster-surface/86-RESEARCH.md
@.planning/phases/86-open-set-intent-discovery-capture-and-cluster-surface/86-01-SUMMARY.md
@.planning/phases/86-open-set-intent-discovery-capture-and-cluster-surface/86-02-SUMMARY.md
@.planning/phases/86-open-set-intent-discovery-capture-and-cluster-surface/86-03-SUMMARY.md

<drift_4_resolution>
**Success #1 — relaxed verification window:**

CONTEXT.md Success #1 reads: *"Within 7 days of Phase 85 going live, the proposals tab shows ≥5 distinct clusters with ≥3 samples each."*

RESEARCH §Q4 forecast (HIGH-confidence on smoke data):
- ~5 V3 proposals/week at current Stage 3 volume.
- Week 1: 1-2 clusters with ≥3 samples, 3-5 singletons.
- Week 3-4: 4-6 clusters with ≥3 samples (Success #1 target reached).

**Plan-time refinement (this plan, NOT CONTEXT.md):**
- Day 7: **≥1 cluster surfaces** (sub-criterion — proves write-through).
- Day 14: **≥2 distinct clusters with ≥3 samples each** (revised primary check).
- Day 21-28: **≥5 distinct clusters with ≥3 samples each** (original target, moved out).

If the day-7 check fails (zero clusters, zero raw proposals), the failure mode is most likely upstream:
1. Phase 85 V3 schema not firing → check `agent_runs.tool_outputs.intent_first_pass` populated.
2. Phase 86 cron not running → check Inngest dashboard for `intent-proposals-refresh` executions.
3. The SQL view filter is wrong → check `intent_proposals_v1` SELECT directly.

CONTEXT.md does NOT need editing; this refinement lives in the operator runbook and the SUMMARY for this plan.
</drift_4_resolution>

<success_criteria_recap>
| ID | From CONTEXT | Refined here? | Check cadence |
|---|---|---|---|
| Success #1 | ≥5 clusters / ≥3 samples within 7d | YES (drift #4) — day-7 ≥1 cluster sub-criterion, ≥5 by day-21-28 | day-7, day-14, day-21, day-28 |
| Success #2 | ≥1 cluster matches a known missing intent (e.g. WKA, PO notifications) | NO — same | day-14, day-28 |
| Success #3 | Top-3 clusters: ≥80% same-intent on 10 random samples each | NO — same | day-14, day-28 (after clusters of size ≥10 exist) |
| Success #4 | Operator opens tab ≥2×/week for 4 consecutive weeks | NO — same; measured via `intent_proposal_views` count | every Friday end-of-day |
</success_criteria_recap>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write operator runbook + verification log scaffold</name>
  <files>.planning/phases/86-open-set-intent-discovery-capture-and-cluster-surface/86-OPERATOR-RUNBOOK.md, .planning/phases/86-open-set-intent-discovery-capture-and-cluster-surface/86-VERIFICATION-LOG.md</files>
  <action>
    Write `86-OPERATOR-RUNBOOK.md` with these sections:

    1. **Pre-flight (day 0, immediately after Plan 03 ships)**
       - Confirm `/automations/debtor-email/intent-proposals` returns 200.
       - Confirm DiscoveryTabStrip is visible below StageTabStrip.
       - Confirm empty-state copy is verbatim D-06.
       - Confirm one row in `intent_proposal_views` per page load.
       - SQL: `SELECT count(*) FROM public.intent_proposals_v1;` — log baseline (likely 0 if Phase 85 just deployed).

    2. **Day 7 check (sub-criterion for Success #1)**
       - SQL: `SELECT count(*) AS raw_proposals FROM public.intent_proposals_v1 WHERE created_at &gt; now() - interval '7 days';` — expect ≥3.
       - SQL: `SELECT count(*) AS clusters FROM public.intent_proposal_clusters WHERE swarm_type = 'debtor-email' AND refreshed_at &gt; now() - interval '24 hours';` — expect ≥1.
       - UI: open the tab, confirm ≥1 cluster visible.
       - Verdict: PASS / FAIL. If FAIL, run the 3 upstream checks (Phase 85 emission, cron execution, view filter).
       - Log to `86-VERIFICATION-LOG.md`.

    3. **Day 14 check (Success #1 primary, refined)**
       - Same SQLs; expect ≥2 clusters of size ≥3.
       - If only 1 cluster, threshold may be too tight — note in log; do NOT change threshold yet.
       - Log.

    4. **Day 14-28 check (Success #2)**
       - For each cluster of size ≥3: does the centroid_label match a corpus-suspected missing intent? (WKA family, PO notifications, payment schedule — from Phase 85 CORPUS.md).
       - At least 1 match = PASS Success #2. Log the matches.

    5. **Day 21-28 check (Success #3)**
       - Select the top 3 clusters by `member_count`.
       - For each: pull 10 random samples (sample_email_ids from the snapshot, JOIN through `pipeline_events` back to `email_pipeline.emails` for subject + body excerpt).
       - Operator reads each sample, assigns verdict "same intent as centroid" / "different intent".
       - ≥8/10 same-intent per cluster = PASS Success #3.
       - Log per-cluster verdict table.

       **Helper SQL — top 3 clusters (operator runs in Supabase SQL editor):**
       ```sql
       SELECT cluster_id, centroid_label, member_count, sample_email_ids
       FROM public.intent_proposal_clusters
       WHERE swarm_type = 'debtor-email'    -- adjust per swarm if needed
         AND refresh_window_end >= now() - interval '7 days'
       ORDER BY member_count DESC
       LIMIT 3;
       ```

       **Helper SQL — 10 random samples for a given cluster (operator pastes the cluster_id from above):**
       ```sql
       -- For each of the 3 cluster_ids, run this with $CLUSTER_ID substituted.
       WITH cluster_samples AS (
         SELECT unnest(sample_email_ids) AS pipeline_event_id
         FROM public.intent_proposal_clusters
         WHERE cluster_id = $CLUSTER_ID
       )
       SELECT
         pe.id                                         AS pipeline_event_id,
         pe.decision_details->>'intent_proposal'       AS proposal_label,
         pe.decision_details->>'proposal_reason'       AS proposal_reason,
         pe.decision_details->'ranked'->0->>'intent'   AS ranked_top_intent,
         e.subject                                     AS subject,
         e.sender_email                                AS sender_email,
         left(coalesce(e.body_full_text, e.body_text, ''), 500) AS body_excerpt
       FROM cluster_samples cs
       JOIN public.pipeline_events pe ON pe.id = cs.pipeline_event_id
       LEFT JOIN email_pipeline.emails e ON e.id = pe.email_id
       ORDER BY random()
       LIMIT 10;
       ```

       Note: `sample_email_ids` on the snapshot table stores `pipeline_event_id` (the canonical Phase 86 row id per Plan 01 storage_truth lock — not a coordinator_run_id). If the executor of Plan 02 named the column differently, update this SQL accordingly.

    6. **Weekly check (Success #4) — every Friday**
       - SQL: `SELECT count(*) AS opens, count(DISTINCT operator_id) AS operators FROM public.intent_proposal_views WHERE viewed_at &gt; now() - interval '7 days';` — expect ≥2 opens.
       - 4 consecutive weeks of ≥2 opens = PASS Success #4.
       - Log weekly count.

    7. **Failure modes**
       - Zero raw proposals after 7 days → escalate to Phase 85 investigation.
       - Many singletons, zero clusters of size ≥3 → may indicate operator should let it cook longer; do not lower threshold without 4-week data.
       - Operator opens &lt;2×/week → discovery loop broken; reopen Phase 86 design before V9.0 builds on top (per R-03).

    Then write `86-VERIFICATION-LOG.md` with empty templates for each check (day 7, 14, 21, 28; weekly 1-4):
    ```markdown
    ## Day 7 check
    **Date:** _____
    **Raw proposals:** _____
    **Clusters:** _____
    **Verdict:** PASS / FAIL
    **Notes:**

    ## Day 14 check
    ...
    ```

    Both files committed; no code changes. Operator runs Task 2 (checkpoint).
  </action>
  <verify>
    <automated>
    test -f .planning/phases/86-open-set-intent-discovery-capture-and-cluster-surface/86-OPERATOR-RUNBOOK.md && test -f .planning/phases/86-open-set-intent-discovery-capture-and-cluster-surface/86-VERIFICATION-LOG.md
    # Expect: both exist.
    grep -c "Success #" .planning/phases/86-open-set-intent-discovery-capture-and-cluster-surface/86-OPERATOR-RUNBOOK.md
    # Expect: ≥4 occurrences (one per success criterion).
    grep -n "day 7\|day 14\|day 21\|day 28" .planning/phases/86-open-set-intent-discovery-capture-and-cluster-surface/86-OPERATOR-RUNBOOK.md | head
    # Expect: each day appears at least once.
    </automated>
  </verify>
  <done>
    Runbook + log scaffold exist, every Success criterion has a documented check cadence, drift #4 refinement is captured (day-7 sub-criterion + day-14 primary + day-21-28 for the original ≥5 target).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator dry-run of day-0 pre-flight</name>
  <what-built>
    The full Phase 86 surface is live: data layer (Plan 01), refresh cron (Plan 02), UI tab (Plan 03), and the operator runbook (Task 1 above). This checkpoint confirms the operator can actually walk the runbook before week 1 starts.
  </what-built>
  <how-to-verify>
    1. Open `/automations/debtor-email/intent-proposals` in the production dashboard.
    2. Confirm the page renders with:
       - PageHeader at the top.
       - StageTabStrip (Stage 0..4) visible.
       - DiscoveryTabStrip visible BELOW the stage strip with one "Intent proposals" tab, highlighted active.
       - Cluster list area below (likely empty-state on day 0).
    3. If empty: confirm copy is verbatim "No novel intent proposals yet" + "The classifier flags emails that don't fit existing intents. Wait for the first week of live traffic."
    4. Click the "Refresh" button. Confirm:
       - Button disables for 5 seconds.
       - After 5s, button re-enables.
       - (Server-side) one event was sent to Inngest (check Inngest dashboard for `intent-proposals.refresh` event).
    5. Reload the page. Run via supabase MCP: `SELECT count(*) FROM public.intent_proposal_views WHERE viewed_at &gt; now() - interval '5 minutes';` — expect ≥1 (the row from your reload).
    6. Open the runbook (`86-OPERATOR-RUNBOOK.md`) and confirm the day-0 pre-flight steps match what you just did.
    7. Schedule the day-7, day-14, day-21, day-28 checks in your calendar.

    Resume the plan when:
    (a) all 6 visual/functional confirmations above pass, OR
    (b) report which step failed + what was observed.
  </how-to-verify>
  <resume-signal>Type "approved" once the day-0 pre-flight passes and the calendar reminders are set, OR describe the failure mode and which upstream check (Phase 85 emission, cron, view filter) is suspected.</resume-signal>
</task>

</tasks>

<verification>
- `86-OPERATOR-RUNBOOK.md` exists with all 4 Success criteria mapped to a check cadence.
- `86-VERIFICATION-LOG.md` exists with empty templates for day-7/14/21/28 + 4 weekly checks.
- Operator has walked the day-0 pre-flight and observed:
  - Tab renders + DiscoveryTabStrip visible.
  - Empty-state copy verbatim.
  - Refresh button works.
  - Telemetry row inserted per page load.
- Drift #4 refinement is documented in this plan's SUMMARY + the runbook; CONTEXT.md stays unchanged.
</verification>

<deviation_rules>
- DO NOT edit CONTEXT.md to change Success #1 — the refinement lives here (drift #4 is a plan-time operational adjustment, not a re-decision).
- DO NOT promote any proposal to `swarm_intents` based on operator verdict — V9.0 owns the promotion path. This plan only LOGS verdicts.
- DO NOT lower the Levenshtein 0.85 threshold based on week-1 data — operator should let it cook 4 weeks before recommending a recalibration.
- If operator opens the tab &lt;2×/week by week 4, this triggers R-03 mitigation (reopen Phase 86 design) — do NOT mark Phase 86 as shipped; escalate.
</deviation_rules>

<out_of_scope>
- Implementing promote-to-swarm_intents — V9.0.
- Editing cluster labels — V9.0.
- Automated alerts on threshold drift — future phase.
</out_of_scope>

<success_criteria>
- All 4 CONTEXT Success criteria have a documented check cadence in the runbook.
- Day-0 pre-flight passed (Task 2 checkpoint).
- Verification log scaffold ready for week-1 entries.
- Drift #4 refinement (day-7 sub-criterion + 14-day primary + 21-28 day for original ≥5 target) is captured in the runbook AND this plan's SUMMARY.
</success_criteria>

<output>
After completion, create `.planning/phases/86-open-set-intent-discovery-capture-and-cluster-surface/86-04-SUMMARY.md` documenting:
- Drift #4 refinement (with rationale: RESEARCH §Q4 forecast).
- Day-0 pre-flight verdict (operator-confirmed at Task 2).
- Calendar dates for day-7, day-14, day-21, day-28 checks.
- Owner for the verification log entries (likely the operator running the runbook).
</output>
