---
phase: 88.2-tier-2-ci-backlog-cleanup
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - web/eslint.config.mjs
autonomous: true
requirements: [R-1, R-3]
notes: |
  Foundation wave. Single-file config diff retires ~108 errors (via globalIgnores
  of lib/automations/**) + ~163 _-prefix warnings (via argsIgnorePattern). After
  this lands the residual lint count drops from 194 → ~86 errors. No source
  edits; isolated config blast radius.

must_haves:
  truths:
    - "`(cd web && npm run lint)` reports ≤ 86 errors (down from 194)"
    - "lib/automations/** is ignored by ESLint"
    - "_-prefixed args and vars no longer produce no-unused-vars warnings"
  artifacts:
    - path: web/eslint.config.mjs
      provides: globalIgnores + argsIgnorePattern config
      contains: 'lib/automations/**'
  key_links:
    - from: web/eslint.config.mjs
      to: ESLint flat-config resolution
      via: globalIgnores import from "eslint/config"
      pattern: 'globalIgnores\(\['
---

<objective>
Tune the ESLint flat-config to retire ~108 errors (ignore `lib/automations/**`) +
~163 `_`-prefix warnings (`argsIgnorePattern`/`varsIgnorePattern`) in a single
config diff. This is the foundation for the rest of the phase: every subsequent
wave's lint baseline reflects these changes.

Purpose: Closes R-1's largest, cheapest leverage point (one config-block edit
worth ~56% of the lint debt). Per D-15 (use `globalIgnores()` not `.eslintignore`)
and SPEC §"Out of scope" (lib/automations/** is trusted-boundary, deliberately
not type-cleaned).

Output: Updated `web/eslint.config.mjs`; verified residual lint error count.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-SPEC.md
@.planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-CONTEXT.md
@.planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-RESEARCH.md
@web/eslint.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply ESLint config diff per RESEARCH §"ESLint Config Diff (precise)"</name>
  <files>web/eslint.config.mjs</files>
  <read_first>
    - web/eslint.config.mjs (full file — current 13 lines)
    - .planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-RESEARCH.md §"ESLint Config Diff (precise)" (lines ~239-309) — canonical target diff
    - .planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-CONTEXT.md §"Decisions" D-15 (globalIgnores mechanism) and D-12 reference (argsIgnorePattern as warning, not error)
  </read_first>
  <action>
    Edit `web/eslint.config.mjs` to match the target diff in RESEARCH §"ESLint Config Diff (precise)" verbatim:

    (1) Inside the existing `globalIgnores([...])` array, append `"lib/automations/**"` AFTER `"next-env.d.ts"`. Add the multi-line justification comment block immediately above it (cites SPEC §"Out of scope" + CONTEXT.md "Deferred Ideas") per D-15.

    (2) After the `globalIgnores` block (still inside `defineConfig([...])`), add a new config block setting `@typescript-eslint/no-unused-vars` to severity `"warn"` with `argsIgnorePattern: "^_"`, `varsIgnorePattern: "^_"`, `caughtErrorsIgnorePattern: "^_"`, `destructuredArrayIgnorePattern: "^_"`. Add a one-line comment above the block citing D-12 / SPEC Req 1.

    Do NOT touch other config blocks. Do NOT add `.eslintignore`. Do NOT change extends order (must remain `...nextVitals, ...nextTs` before the ignores).
  </action>
  <verify>
    <automated>cd web && npm run lint 2>&1 | tee /tmp/lint-after.txt; ERR=$(grep -cE "  error " /tmp/lint-after.txt); WARN=$(grep -cE "  warning " /tmp/lint-after.txt); echo "errors=$ERR warnings=$WARN"; test "$ERR" -le 90 && test "$WARN" -le 25</automated>
  </verify>
  <acceptance_criteria>
    - `web/eslint.config.mjs` contains the literal string `"lib/automations/**"` inside the `globalIgnores([...])` array
    - `web/eslint.config.mjs` contains the literal string `argsIgnorePattern: "^_"` AND `varsIgnorePattern: "^_"`
    - `cd web && npx eslint lib/automations/heeren-oefeningen/explore/explore.ts 2>&1 | grep -ic "ignored"` returns at least 1 (or eslint exits 0 with no output for that file)
    - `cd web && npm run lint 2>&1 | grep -cE "  error "` returns a number ≤ 90 (down from 194; target ~86)
    - `cd web && npm run lint 2>&1 | grep -cE "  warning "` returns a number ≤ 25 (down from 179)
    - No `.eslintignore` file exists or is created (`test ! -f web/.eslintignore && echo ok` → ok)
  </acceptance_criteria>
  <done>
    Residual lint error count is ≤ 90 (target ~86) and warning count is ≤ 25.
    `web/eslint.config.mjs` matches the RESEARCH §"ESLint Config Diff (precise)" target.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify build + typecheck still green after config diff (R-3 sanity)</name>
  <files></files>
  <read_first>
    - web/eslint.config.mjs (the file just edited)
    - .planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-RESEARCH.md §"Validation Architecture" → "Phase Requirements → Test Map" (R-3 row)
  </read_first>
  <action>
    Run `npm run typecheck` and `npm run build` from `web/` to confirm the ESLint config edit didn't regress either gate. Build is the residual exposure per RESEARCH §"Risks & Landmines" — Pitfall 6 notes the cache key includes every `.ts`/`.tsx`, but a config-only change should not invalidate runtime build output.

    If typecheck fails: the failure is unrelated to this plan (it should not be touching .ts files). Stop and surface the error. Do NOT proceed to Wave 1 until this is resolved.

    If build fails: same — surface error, do not silently advance.
  </action>
  <verify>
    <automated>cd web && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `(cd web && npm run typecheck)` exits 0
    - `(cd web && npm run build)` exits 0
    - No new files were created or modified except `web/eslint.config.mjs` from Task 1 (`git status --porcelain web/` shows only that file modified)
  </acceptance_criteria>
  <done>
    Build + typecheck baseline confirmed green on top of the ESLint config diff. Foundation wave complete; Wave 1 unblocked.
  </done>
</task>

</tasks>

<verification>
- `(cd web && npm run lint 2>&1 | grep -cE "  error ")` ≤ 90
- `(cd web && npm run typecheck)` exits 0
- `(cd web && npm run build)` exits 0
- `web/eslint.config.mjs` contains `"lib/automations/**"` and `argsIgnorePattern: "^_"`
</verification>

<success_criteria>
- Residual lint error count drops from 194 to ≤ 90 (target ~86) with no source-file edits.
- Typecheck and build remain green.
- ESLint config follows D-15 (flat-config `globalIgnores`, not `.eslintignore`).
</success_criteria>

<output>
Create `.planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-01-SUMMARY.md` recording:
- Before/after lint error + warning counts
- Confirmation that typecheck + build remained green
- Any deviation from the RESEARCH target diff (expected: none)
</output>
