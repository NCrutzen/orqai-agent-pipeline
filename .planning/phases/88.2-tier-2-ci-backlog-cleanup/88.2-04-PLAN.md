---
phase: 88.2-tier-2-ci-backlog-cleanup
plan: 04
type: execute
wave: 3
depends_on: [88.2-02]
files_modified:
  - web/tests/queue/rule-filter.test.tsx
  - web/tests/classifier/corpus-mapping.test.ts
  - web/tests/labeling/orq-agents-client.test.ts
  - web/tests/labeling/classifier-invoice-copy-handler.test.ts
  - web/lib/inngest/functions/__tests__/classifier-verdict-worker.test.ts
  - web/lib/v7/graph/__tests__/layout.test.ts
  - web/tests/swarm-registry/verdict-worker-dispatch.test.ts
  - .planning/todos/pending/2026-05-21-test-fixture-*.md
autonomous: true
requirements: [R-2]
notes: |
  Residual ~9 test failures after Plan 02 — all fixture/assertion drift.
  Per D-07 (15-min time-box per test) and D-09 (≤ 10 skips total), the executor
  fixes drift if small/obvious, otherwise `.skip()` + todo. Per D-08 todos are
  one-page minimal.

  depends_on 88.2-02 only — needs the test-infra primitives in place so the
  residual really is fixture drift, not unaddressed root-cause overlap. Does
  NOT depend on Plan 03 (lint burn-down) — these tests can be triaged in
  parallel with lint work, but the orchestrator waves them after the test-infra
  primitives so the diagnostic signal is clean.

must_haves:
  truths:
    - "`(cd web && npm test)` exits 0"
    - "Every `.skip()` introduced in this phase has a one-page todo at .planning/todos/pending/2026-05-21-test-fixture-{slug}.md referenced inline in the skip reason"
    - "≤ 10 `.skip()`s added total across the phase (D-09 cap)"
    - "For each of the 9 candidates, the executor either repaired the test (within 15 min per D-07) or skip+todo'd it with rationale"
  artifacts:
    - path: .planning/todos/pending/
      provides: One todo file per .skip() added by this phase
      contains: 'test-fixture-'
  key_links:
    - from: '.skip("...")'
      to: '.planning/todos/pending/2026-05-21-test-fixture-{slug}.md'
      via: inline path reference in the skip reason string
      pattern: '\.skip\(.*todos/pending'
---

<objective>
Resolve the residual ~9 fixture/assertion-drift test failures after Plan 02's
test-infra primitives landed. Apply D-07's 15-min-per-test time-box: fix if
small and obvious, otherwise `.skip()` + minimal one-page todo (D-08). Stay
under the D-09 hard cap of ≤ 10 `.skip()`s for the whole phase.

Purpose: R-2 final closure — `npm test` exits 0 with 0 failures. Skipped tests
must have tracked todos so the gap is visible, not lost.

Output: Either repaired tests or `.skip()` + new todo files under
`.planning/todos/pending/`. `npm test` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-SPEC.md
@.planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-CONTEXT.md
@.planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-RESEARCH.md

<interfaces>
<!-- Residual failures after Plan 02 (from RESEARCH §"Test Failure Inventory" → Wave 3 rows): -->

(F1) web/tests/queue/rule-filter.test.tsx — 1 test
     Error: "expected [] to include `decision_details->>mailbox_id=[\"12\"]`"
     Likely cause: query-shape drift (filter syntax changed)
     Sanity-check budget: 15 min. Try regenerating the test's expected query string from current code; if mismatch is small, fix the literal.

(F2) web/tests/classifier/corpus-mapping.test.ts — 1 test
     Error: "expected [ 'auto_reply', …(13) ] to deeply equal [ 'auto_reply', 'ooo_permanent', …(3) ]"
     Likely cause: registry vocab drift (intent vocabulary expanded; test snapshot stale).
     Sanity-check: if the expected array is just outdated and the new array is the correct vocab, regenerate.

(F3) web/tests/labeling/orq-agents-client.test.ts — 3 tests
     Error: "Orq invoke test-agent: empty output text"
     Likely cause: Orq response-envelope shape changed; test mock returns the old shape.
     Sanity-check: read the production Orq response handler; if the test mock is one field off, fix. Otherwise skip+todo.

(F4) web/tests/labeling/classifier-invoice-copy-handler.test.ts — 6 tests
     Error: "invalid message_id format" — input fixture violates new message_id regex validator
     Likely cause: message_id validation added since fixture was written.
     Sanity-check: regenerate the fixture's message_id field with a valid value. Per CONTEXT.md additional_context, this one specifically gets a 5-min sanity check before skipping (OQ-4).

(F5) web/lib/inngest/functions/__tests__/classifier-verdict-worker.test.ts — 3 tests
     Error: "Cannot read properties of undefined (reading 'stage_decisions')"
     Likely cause: view-row fixture missing the `stage_decisions` field; code-side schema evolved.
     Sanity-check: if adding the field to the fixture (e.g. `stage_decisions: []`) makes the test pass, do it.

(F6) web/lib/v7/graph/__tests__/layout.test.ts — 1 test
     Error: "expected 22 to be close to 18"
     Likely cause: graph layout algorithm produces slightly different numeric output now.
     Sanity-check: if the new value (22) is correct per the algorithm, update the expectation. Otherwise skip+todo.

(F7) web/tests/swarm-registry/verdict-worker-dispatch.test.ts — 1 residual test
     The 1 assertion-drift left over from Plan 02 (after the 3 .schema-chain failures were fixed).
     Sanity-check: read the failing assertion; if drift is small, fix.

Total: 9 candidate failures + 1 carryover from Plan 02 = max 10 to triage.
Skip-cap (D-09) is 10 — leaves no headroom for surprises; surface immediately if more than 8 actually need skipping.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage and repair the 9 fixture-drift failures (D-07 time-box; D-08 minimal todos; D-09 cap)</name>
  <files>web/tests/queue/rule-filter.test.tsx, web/tests/classifier/corpus-mapping.test.ts, web/tests/labeling/orq-agents-client.test.ts, web/tests/labeling/classifier-invoice-copy-handler.test.ts, web/lib/inngest/functions/__tests__/classifier-verdict-worker.test.ts, web/lib/v7/graph/__tests__/layout.test.ts, web/tests/swarm-registry/verdict-worker-dispatch.test.ts, .planning/todos/pending/2026-05-21-test-fixture-*.md</files>
  <read_first>
    - .planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-RESEARCH.md §"Test Failure Inventory" → "Failing Files by Root Cause" (Wave 3 rows)
    - .planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-CONTEXT.md §"Decisions" D-07 (15-min time-box per test), D-08 (one-page todos: title=test name, body=failing line + one-line root cause + which fixture needs work), D-09 (hard cap ≤ 10 skips total — cookies-mock + Supabase mock should leave this number low)
    - .planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-SPEC.md §"Acceptance Criteria" — every `.skip()` must reference a `.planning/todos/pending/` entry
    - Each failing test file at its failing test (one Read per file, focused on the `it(...)` or `test(...)` block)
    - For F4 (classifier-invoice-copy-handler.test.ts): also read the production validator that throws "invalid message_id format" — usually `web/lib/automations/debtor-email/` somewhere; grep for the error string. The fix may be a one-line valid-format string in the fixture.
    - For F5 (classifier-verdict-worker.test.ts): also read the SQL view or the function that produces `stage_decisions` to confirm the right field name + value shape.
    - At least one existing pending-todo file under `.planning/todos/pending/` to match house style (e.g. `.planning/todos/pending/2026-05-20-tier-2-ci-pr-checks-workflow.md`)
  </read_first>
  <action>
    For each of F1..F7 (and the F7 verdict-worker-dispatch carryover from Plan 02), execute this triage loop:

      1. Run the test in isolation: `cd web && npx vitest run <path> 2>&1 | tail -30`. Read the failure.
      2. Start a 15-minute timer (D-07).
      3. Try the obvious fix:
         - F1: regenerate the expected query string from current code.
         - F2: regenerate the expected intent-key array.
         - F3: read the production Orq response shape; align the mock's response.
         - F4 (special — OQ-4): 5-min sanity check first; replace the `message_id` fixture with a value matching the validator regex (likely `<...@...>` RFC 5322 style).
         - F5: add the missing `stage_decisions: []` (or correct shape) to the view-row fixture.
         - F6: update the numeric expectation to 22 (or document why 18 was correct and regress the layout change in a follow-up).
         - F7: read the assertion; adjust the literal.
      4. Re-run. If green within 15 min → keep the fix; commit per D-13 with message `fix(test): repair <test-name> fixture drift`.
      5. If not green within 15 min → revert your changes to that file, then mark the failing test(s) with `.skip("see .planning/todos/pending/2026-05-21-test-fixture-{slug}.md")`. Slug = short kebab-case test name (e.g. `orq-agents-client-empty-output`).
      6. Write the todo at `.planning/todos/pending/2026-05-21-test-fixture-{slug}.md` per D-08:
         - Title (h1): the failing test description (the `it(...)` text).
         - Body (one page max):
           - **Failing line:** `web/...:LINENO`
           - **Error:** the verbatim error message (one line)
           - **Likely root cause:** one sentence (e.g. "Orq response envelope shape changed; fixture predates the `output.text` → `output_text` rename")
           - **Fixture/file likely needing work:** path
           - **Skip reason:** "Time-boxed per Phase 88.2 D-07; see CONTEXT.md."
           - Frontmatter: `---\nphase-link: 88.2\nopened: 2026-05-21\nstatus: pending\n---`

    **D-09 enforcement:** Count `.skip()` calls added across the entire phase before declaring done. If `> 10`, STOP and surface for the operator — per CONTEXT.md additional_context this means fixtures need their own phase, not a sub-task here.

    **Commit cadence (D-13):** one commit per directory bucket. Likely buckets here:
      - `fix(test): repair tests/queue/ and tests/classifier/ fixture drift` (F1, F2)
      - `fix(test): repair tests/labeling/ fixtures or .skip() with todos` (F3, F4)
      - `fix(test): repair lib/inngest/ classifier-verdict-worker fixture` (F5)
      - `fix(test): repair lib/v7/graph/ layout assertion` (F6)
      - `fix(test): repair tests/swarm-registry/ verdict-worker-dispatch carryover` (F7)
      - `docs(todos): add fixture-drift todos for skipped tests` (todo files)
  </action>
  <verify>
    <automated>cd web && npm test 2>&1 | tee /tmp/test-after.txt | tail -10; FAIL=$(grep -E "Tests +[0-9]+ failed" /tmp/test-after.txt | grep -oE "[0-9]+ failed" | grep -oE "^[0-9]+"); SKIPS_ADDED=$(cd .. && git diff main -- web/ | grep -cE "^\+.*\.skip\("); echo "fails=$FAIL skips_added_by_phase=$SKIPS_ADDED"; test "${FAIL:-1}" -eq 0 && test "$SKIPS_ADDED" -le 10</automated>
  </verify>
  <acceptance_criteria>
    - `(cd web && npm test)` exits 0
    - `cd web && npm test 2>&1 | grep -E "Tests +[0-9]+ failed"` returns nothing OR shows `0 failed`
    - Total `.skip()` calls added across the whole phase ≤ 10: `git diff main -- web/ | grep -cE "^\+.*\.skip\("` ≤ 10
    - Every newly-added `.skip()` references a `.planning/todos/pending/2026-05-21-test-fixture-*.md` path: `git diff main -- web/ | grep -E "^\+.*\.skip\(" | grep -v "todos/pending" | wc -l` returns `0`
    - Every referenced todo file exists: for each skip with a path string, `test -f <that-path>` succeeds
    - Each todo file has the D-08 structure: an `## Failing line` line, an `## Error` line, and a `## Likely root cause` line (verify with `grep -l "Likely root cause" .planning/todos/pending/2026-05-21-test-fixture-*.md`)
    - No test was rewritten as a new test (SPEC §"Out of scope"): test descriptions and assertion intent preserved or the test is `.skip()`'d
  </acceptance_criteria>
  <done>
    `npm test` exits 0. The 9 fixture-drift candidates are either repaired or `.skip()`'d with todos. Skip count ≤ 10. R-2 acceptance criteria met.
  </done>
</task>

</tasks>

<verification>
- `(cd web && npm test)` exits 0
- Total skips added by phase ≤ 10
- Each skip has a matching `.planning/todos/pending/` todo
- `(cd web && npm run lint)` still exits 0 (Plan 03 baseline holds)
- `(cd web && npm run typecheck)` still exits 0
</verification>

<success_criteria>
- R-2 fully met: zero failing tests, skip-with-todo discipline observed.
- D-07 time-box and D-09 skip-cap honoured.
- D-08 todos are one-page-max and reference the failing line.
</success_criteria>

<output>
Create `.planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-04-SUMMARY.md` recording:
- For each of F1..F7: outcome (FIXED / SKIPPED) + commit SHA + (if skipped) todo path
- Final skip count for the phase
- Any deviation from D-07 budget (e.g. "F4 took 20 min — over budget but completed")
- Any tests that surfaced as more than the 9 candidates (unexpected drift)
</output>
