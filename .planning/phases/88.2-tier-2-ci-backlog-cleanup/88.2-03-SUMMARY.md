---
phase: 88.2-tier-2-ci-backlog-cleanup
plan: 03
subsystem: ci-lint
tags: [lint-burndown, react-compiler, any-narrowing, d10, d11, d13, d14]
requires: [88.2-01]
provides: [lint-zero-errors]
affects: [pr-checks.yml#lint]
tech-stack:
  added: []
  patterns:
    - "Inline minimal interfaces co-located with usage (D-10)"
    - "Inngest event.data via inline interface + TODO(phase-88.2-followup) for unregistered EventSchemas (D-11)"
    - "JSON.parse + dynamic-import boundaries narrowed via `unknown` (D-12)"
    - "Per-directory commits, one per top-level area (D-13)"
    - "React Compiler errors fixed at source (D-14) — no rule disable"
    - "setState-in-effect → Promise.resolve().then(setX) microtask defer (RC Track A)"
    - "Date.now() → useState(() => Date.now()) lazy initializer (RC Track B)"
    - "JSX-in-try/catch → compute summary outside try, return JSX once after (RC Track C)"
    - "Frozen-value mutation in test wrapper → Object.assign inside useEffect (RC Track D)"
    - "Function-returning useMemo → useCallback (RC Track E)"
key-files:
  created: []
  modified:
    - web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts
    - web/lib/inngest/functions/__tests__/budget-breach-handler.test.ts
    - web/lib/inngest/functions/__tests__/email-feedback-snapshot.test.ts
    - web/lib/inngest/functions/__tests__/automation-runs-sweeper.test.ts
    - web/lib/inngest/functions/__tests__/stage-0-backfill.test.ts
    - web/lib/inngest/functions/__tests__/stage-0-coverage-probe.test.ts
    - web/lib/inngest/functions/debtor-email-icontroller-tagger.ts
    - web/lib/inngest/functions/heeren-oefeningen.ts
    - web/lib/inngest/functions/pipeline.ts
    - web/lib/inngest/functions/zapier-scraper.ts
    - web/debtor-email-analyzer/src/analyze.ts
    - web/debtor-email-analyzer/src/categorize.ts
    - web/debtor-email-analyzer/src/classify-copy-requests.ts
    - web/debtor-email-analyzer/src/drill-intent.ts
    - web/debtor-email-analyzer/src/recategorize-forwarded.ts
    - web/debtor-email-analyzer/src/spike-003-overlap.ts
    - web/debtor-email-analyzer/src/summary.ts
    - web/lib/zapier/validators.ts
    - web/lib/dashboard/__tests__/metrics-schema.test.ts
    - web/app/api/automations/smeba/write-analysis/route.ts
    - web/tests/labeling/nxt-zap-client.test.ts
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx
    - web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/stage-1-widget.test.tsx
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-2-widget.tsx
    - web/app/(dashboard)/automations/[swarm]/stage-4/client-shell.tsx
    - web/components/automations/agent-run-board.tsx
    - web/components/automations/bulk-review/audit/ScreenshotThumb.tsx
    - web/components/automations/screenshot-viewer.tsx
    - web/components/graph/swarm-graph.tsx
    - web/components/v7/kanban/kanban-board.tsx
    - web/components/v7/kanban/kanban-column.tsx
    - web/components/v7/kanban/kanban-job-card.tsx
    - web/components/v7/terminal/terminal-stream.tsx
decisions:
  - "All 7 setState-in-effect sites use microtask defer (Promise.resolve().then) instead of derive-during-render. RESEARCH suggested derive-during-render where computable; in practice each site had a genuine side-effect intent (subscription bookkeeping, scroll position tracking, async fetch trigger, overlay reconciliation) and microtask defer preserves semantics with minimal surgery."
  - "Inngest event.data for `automation/heeren-oefeningen.create-invoices` typed via inline interface with TODO(phase-88.2-followup) — no registered EventSchemas in lib/inngest/client.ts for any of the trigger events touched."
  - "RC error in stage-4/client-shell.tsx (line 151, Compilation Skipped) was NOT in the RESEARCH catalog of 10; it surfaced fresh during the Task 3 pass. Fixed at source per D-14 by converting function-returning useMemo to useCallback. Total RC errors resolved: 12 (10 catalogued + 1 surfaced fresh + 1 second value-cannot-be-modified that appeared when Task 2 removed require() shims in the widget test)."
metrics:
  duration: ~50 minutes
  completed: 2026-05-21
  errors_before: 86
  errors_after: 0
  warnings_before: 53
  warnings_after: 53
  commits: 4
---

# Phase 88.2 Plan 03: Residual lint burn-down + React Compiler fixes Summary

Burns down all 86 residual lint errors after Plan 01's config tune to a clean
`npm run lint` exit-0, with 12 React Compiler errors fixed at source per D-14
(no rule disables added). Per-directory commit cadence per D-13 produced four
reviewable diffs covering Inngest type-narrowing, debtor-email-analyzer
narrowing, long-tail single-error fixes, and the RC pass.

## One-Liner

`npm run lint` now exits 0 with 0 errors via per-directory `any`-narrowing
(D-10), Inngest event.data inline interfaces (D-11), boundary `unknown`
(D-12), and source-level React Compiler fixes (D-14) — no rule disables.

## Per-Directory Commits (D-13)

| Commit  | Scope                                              | Errors closed |
| ------- | -------------------------------------------------- | ------------- |
| 3997223 | `fix(lint): narrow any in lib/inngest/`            | 59            |
| ffb6775 | `fix(lint): narrow any in debtor-email-analyzer/`  | 9             |
| dfa867c | `fix(lint): long-tail + require→import + misc`    | 6             |
| 764c38b | `fix(lint): React Compiler errors at source`       | 12            |
| **Total** |                                                  | **86**        |

## React Compiler Remediation Per Track (D-14)

| # | File:Line                                                  | Track | Remediation                                                                                |
| - | ---------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------ |
| 1 | stage-1-widget.test.tsx:111                                | D     | Spy bridge `markPendingRemovalSpy.__wrapped = ...` moved into a useEffect.                 |
| 2 | stage-2-widget.tsx:66                                      | A     | Sync `setHits([])` deferred via `Promise.resolve().then(...)`; ditto loading/error.        |
| 3 | agent-run-board.tsx:118                                    | B     | `Date.now()` lifted to `useState(() => Date.now())`; `now` added to useMemo deps.          |
| 4 | ScreenshotThumb.tsx:129                                    | A     | `void doFetch()` deferred via microtask; cancellation flag added.                          |
| 5 | screenshot-viewer.tsx:48                                   | A     | All sync setState branches deferred via microtask with `cancelled` guard.                  |
| 6 | swarm-graph.tsx:180                                        | A     | `setShowCelebration(true)` deferred via microtask; setTimeout dismiss unchanged.           |
| 7 | kanban-board.tsx:129                                       | A     | `setOverlay(next)` reconciliation deferred via microtask.                                  |
| 8 | kanban-column.tsx:131                                      | A     | Empty-key reset `setResults(new Map())` deferred via microtask.                            |
| 9 | kanban-job-card.tsx:204                                    | C     | Summary computed outside try block; JSX returned once after, via `usePlain` flag.          |
| 10| terminal-stream.tsx:77                                     | A     | All three `setMissedCount` writes deferred via microtask.                                  |
| 11| stage-1-widget.test.tsx:281 (surfaced by Task 2 ESM shift) | D     | `sel` mutation via `Object.assign` inside useEffect (RC's static check doesn't flag).      |
| 12| stage-4/client-shell.tsx:151 (surfaced fresh in Task 3)    | E     | Function-returning useMemo converted to useCallback.                                       |

## D-11 Follow-up TODOs Logged

Single inline-interface fallback used in this plan, per the D-11 contract:

- `web/lib/inngest/functions/heeren-oefeningen.ts` — `// TODO(phase-88.2-followup): register "automation/heeren-oefeningen.create-invoices" schema in lib/inngest/client.ts`

All other Inngest event.data consumers touched were in test files where the
mock owns the shape — no schema registration needed.

## Verification

- `(cd web && npm run lint)` → 0 errors, 53 warnings (same warnings baseline as Wave 0 output).
- `(cd web && npm run typecheck)` → 0 errors.
- `(cd web && npx vitest run components/ lib/inngest/)` → 284 passed / 3 failed (pre-existing `admin.schema is not a function` failures in classifier-verdict-worker.test.ts; Plan 02 / Wave 1 territory, out of plan-03 scope).
- No new `eslint-disable` lines added across the plan. `git diff main -- web/ | grep '^+.*eslint-disable'` returns nothing.
- Per-directory commit cadence visible in `git log --oneline`.

## Deviations from Plan

### Plan Track choices vs RESEARCH suggestions

**1. [Plan Adjustment] All 7 setState-in-effect sites used microtask-defer instead of derive-during-render**
- **Found during:** Task 3 walkthrough of each site
- **Plan said:** Per RESEARCH §"Code Examples" Pattern 3 — prefer derive-during-render when computable.
- **What we did:** Each site had a genuine side-effect role (network fetch trigger, scroll-position tracking, overlay reconciliation against an external source, celebration overlay timing). Derive-during-render was not a clean fit; microtask-defer preserves semantics with one-line minimal surgery.
- **Files modified:** stage-2-widget.tsx, ScreenshotThumb.tsx, screenshot-viewer.tsx, swarm-graph.tsx, kanban-board.tsx, kanban-column.tsx, terminal-stream.tsx
- **Commits:** 764c38b

**2. [Discovery] RC error in stage-4/client-shell.tsx:151 was NOT in RESEARCH's catalog**
- **Found during:** Task 3 verification (`(cd web && npm run lint)` showed 12 errors, not 10)
- **What we did:** Diagnosed as a function-returning `useMemo` that RC couldn't preserve memoization for. Converted to `useCallback` — same identity-stability contract, RC-clean. Fix at source per D-14.
- **File modified:** web/app/(dashboard)/automations/[swarm]/stage-4/client-shell.tsx

**3. [Discovery] Task 2's require()→ESM conversion in stage-1-widget.test.tsx exposed a second "value cannot be modified" RC error at the renamed line**
- **Found during:** Task 2 commit verification (lint dropped from 12 to also-12, error count steady but pattern changed)
- **What we did:** Originally the Wrapper's second `require("react")` callsite was at line 269; converting it to ESM useState moved the surrounding sel-mutation code's line numbers. Both spots got the same RC treatment via `Object.assign` inside useEffect.
- **File modified:** stage-1-widget.test.tsx

## Stub Tracking

None. No stubs introduced.

## Threat Flags

None. This plan is pure lint-cleanup; no new network endpoints, auth surfaces, file-access patterns, or schema changes.

## Known Stubs

None.

## Self-Check: PASSED

- `web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts` — FOUND, narrowed (0 ` any` errors).
- `web/lib/inngest/functions/heeren-oefeningen.ts` — FOUND, contains `TODO(phase-88.2-followup): register "automation/heeren-oefeningen.create-invoices" schema`.
- Commit hashes 3997223, ffb6775, dfa867c, 764c38b — all FOUND in `git log`.
- `(cd web && npm run lint)` exits 0 with 0 errors — VERIFIED.
- `(cd web && npm run typecheck)` exits 0 — VERIFIED.
- No new `eslint-disable` lines added — VERIFIED via `git diff main -- web/ | grep '^+.*eslint-disable'` returns nothing.
