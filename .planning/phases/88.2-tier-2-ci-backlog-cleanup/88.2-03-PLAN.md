---
phase: 88.2-tier-2-ci-backlog-cleanup
plan: 03
type: execute
wave: 2
depends_on: [88.2-01]
files_modified:
  - web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts
  - web/lib/inngest/functions/__tests__/budget-breach-handler.test.ts
  - web/lib/inngest/functions/__tests__/email-feedback-snapshot.test.ts
  - web/lib/inngest/**/*.ts
  - web/debtor-email-analyzer/src/**/*.ts
  - web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/stage-1-widget.test.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-2-widget.tsx
  - web/components/automations/agent-run-board.tsx
  - web/components/automations/bulk-review/audit/ScreenshotThumb.tsx
  - web/components/automations/screenshot-viewer.tsx
  - web/components/graph/swarm-graph.tsx
  - web/components/v7/kanban/kanban-board.tsx
  - web/components/v7/kanban/kanban-column.tsx
  - web/components/v7/kanban/kanban-job-card.tsx
  - web/components/v7/terminal/terminal-stream.tsx
  - web/tests/labeling/**/*.ts
  - web/lib/zapier/**/*.ts
  - web/lib/dashboard/**/*.ts
  - web/app/(dashboard)/**/*.tsx
  - web/app/api/**/*.ts
autonomous: true
requirements: [R-1]
notes: |
  Lint burn-down for the residual ~86 errors after Plan 01's config tune.
  Per D-13 (per-directory commits), this plan groups by directory bucket with
  one commit per bucket. The single-file hotspot is the Inngest test trio
  (~51 of ~86 errors). The 10 React Compiler errors are fixed at source per
  D-14 — no rule disables. 7 of 10 RC errors share the "setState-in-effect"
  pattern (one canonical fix applied 7×).

  depends_on 88.2-01 only — does NOT depend on Plan 02, because lint burn-down
  is independent of test-infra. (Tests that get touched by this plan are
  already running; their lint errors are independent of whether the cookies/
  Supabase mocks exist.)

  This plan should NOT regress tests fixed by Plan 02. Run `npm test` after
  each per-directory commit to confirm.

must_haves:
  truths:
    - "`(cd web && npm run lint)` exits 0"
    - "No React Compiler error appears in lint output"
    - "No `no-explicit-any` error remains in lib/inngest/, debtor-email-analyzer/src/, or other in-scope buckets"
    - "Inngest event.data typing follows D-11 (registered event schemas where present, inline interface + TODO comment otherwise)"
    - "No new `eslint-disable` lines added without a one-line reason comment"
  artifacts:
    - path: web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts
      provides: any-narrowed test file (was 41 errors)
      contains_no: ': any'
  key_links:
    - from: any-narrowing in lib/inngest/
      to: D-11 inngest event schema
      via: client.createFunction signature
      pattern: 'inngest.createFunction'
---

<objective>
Burn down the residual ~86 lint errors after Plan 01's config tune, per D-13's
per-directory commit cadence. Three tracks:

  (A) `any`-narrowing — 58 errors in `lib/inngest/`, 9 in `debtor-email-analyzer/src/`,
      and the long-tail singletons in `lib/zapier/`, `lib/dashboard/`, `app/api/`,
      `tests/`. Strategy per D-10 (inline minimal interfaces) and D-11 (registered
      event schemas for Inngest `event.data`).
  (B) `require()` → ESM `import` — 2 errors in `app/(dashboard)/` (the 35 in
      `lib/automations/**` are ignored by Plan 01).
  (C) React Compiler errors — 10 total, fixed at source per D-14. 7 of 10 share
      one canonical "setState-in-effect → derive during render" fix.

Purpose: R-1 acceptance — `npm run lint` exits 0 with no errors and no rule
disables added.

Output: A green lint run. Per-directory commits with reviewable diffs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-SPEC.md
@.planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-CONTEXT.md
@.planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-RESEARCH.md

<interfaces>
<!-- Per-file lint-error hotspots after Plan 01 (from RESEARCH §"Top Per-File Hotspots"): -->

41 errors: web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts (no-explicit-any)
 7 errors: web/lib/inngest/functions/__tests__/budget-breach-handler.test.ts (no-explicit-any)
 3 errors: web/lib/inngest/functions/__tests__/email-feedback-snapshot.test.ts (no-explicit-any)
 9 errors: web/debtor-email-analyzer/src/** (no-explicit-any, multiple files)
 2 errors: web/app/(dashboard)/** (no-require-imports)
 1 each:   web/lib/zapier/, web/lib/dashboard/, web/app/api/ (no-explicit-any)
 1 error:  web/app/(dashboard)/automations/... (react/no-unescaped-entities)
 1 error:  web/tests/labeling/... (prefer-const)
 1 error:  web/lib/inngest/... (prefer-as-const)

<!-- 10 React Compiler errors with verbatim file:line (from RESEARCH §"React Compiler Errors"): -->

(1) app/(dashboard)/automations/[swarm]/stage-1/__tests__/stage-1-widget.test.tsx:110 — "This value cannot be modified"
(2) app/(dashboard)/automations/[swarm]/stage-1/components/stage-2-widget.tsx:66 — "setState in effect" → derive during render
(3) components/automations/agent-run-board.tsx:118 — "Date.now() impure in render" → move to useState init or useEffect
(4) components/automations/bulk-review/audit/ScreenshotThumb.tsx:129 — "setState in effect"
(5) components/automations/screenshot-viewer.tsx:48 — "setState in effect"
(6) components/graph/swarm-graph.tsx:180 — "setState in effect"
(7) components/v7/kanban/kanban-board.tsx:129 — "setState in effect"
(8) components/v7/kanban/kanban-column.tsx:131 — "setState in effect"
(9) components/v7/kanban/kanban-job-card.tsx:204 — "JSX in try/catch" → extract child component or use error boundary
(10) components/v7/terminal/terminal-stream.tsx:77 — "setState in effect"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Burn down lib/inngest/ any-narrowing (51 errors in 3 test files + 7 in production files)</name>
  <files>web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts, web/lib/inngest/functions/__tests__/budget-breach-handler.test.ts, web/lib/inngest/functions/__tests__/email-feedback-snapshot.test.ts, web/lib/inngest/**/*.ts</files>
  <read_first>
    - .planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-RESEARCH.md §"Lint Error Inventory" → "Top Per-File Hotspots" + "Code Examples" Pattern 1 (inline minimal interface) and Pattern 2 (Inngest event schema)
    - .planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-CONTEXT.md §"Decisions" D-10 (inline minimal interfaces), D-11 (registered event schema first), D-12 (unknown + boundary assertion only for true boundaries)
    - web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts (full file — 41 `any` sites)
    - web/lib/inngest/functions/__tests__/budget-breach-handler.test.ts (full file — 7 `any` sites)
    - web/lib/inngest/functions/__tests__/email-feedback-snapshot.test.ts (full file — 3 `any` sites)
    - web/lib/inngest/client.ts (or wherever `EventSchemas` are registered — per D-11, check whether `stage-0-safety.classify` etc. have registered schemas before inlining)
    - At least one example Inngest function file like `web/lib/inngest/functions/stage-0-safety-worker.ts` to see the production `event.data` consumer shape — narrowing the test mock must match the real signature
    - CLAUDE.md §"Inngest" — replay-safe id generation rule (do NOT refactor replay-safety as a side effect — only narrow types)
  </read_first>
  <action>
    Per D-13, this is ONE commit covering the entire `lib/inngest/` bucket: `fix(lint): narrow any in lib/inngest/`.

    **Step A — Audit event schemas:** Run `grep -l "EventSchemas\|createFunction" web/lib/inngest/` to find the registration. If a schema exists for a given event type, type the test mock's `event.data` from the schema (D-11). If absent, add an inline `interface` next to the test setup and a `// TODO(phase-88.2-followup): register <event-name> schema in lib/inngest/client.ts` comment.

    **Step B — Narrow per D-10:** For each `: any` (and `as any`), write the smallest interface that satisfies the call sites in that test. Co-locate the interface above the test setup; do not export. For test-builder helpers, parameterize the type rather than `any`.

    **Step C — `unknown` only at genuine boundaries (D-12):** JSON.parse of external payloads → `unknown` + assertion is acceptable. Internal mock construction → use a proper interface.

    **Step D — Handle the `prefer-as-const` singleton in `lib/inngest/`:** Find via `cd web && npx eslint lib/inngest/ 2>&1 | grep "prefer-as-const"` and apply the trivial fix (`x: "foo"` → `x: "foo" as const`).

    **Step E — Do NOT touch replay-safety patterns:** If you see `step.run("resolve-run-id", async () => crypto.randomUUID())` in the surrounding code, leave it. Per CLAUDE.md §Inngest, that is load-bearing. This plan narrows types only.

    Run `(cd web && npm run lint lib/inngest/)` after edits — must exit 0 (no errors) for the bucket. Then run `(cd web && npm test lib/inngest/)` to confirm no test regression.
  </action>
  <verify>
    <automated>cd web && npx eslint lib/inngest/ 2>&1 | grep -cE "  error " | grep -q "^0$" && npx vitest run lib/inngest/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx eslint lib/inngest/ 2>&1 | grep -cE "  error "` returns `0` (was: ~58 errors)
    - `cd web && grep -rn ": any" web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts | grep -v "^//" | wc -l` ≤ 0
    - `cd web && npx vitest run lib/inngest/` exits 0 (no test regression from the narrowing)
    - No new `eslint-disable` line added in `lib/inngest/` (`git diff lib/inngest/ | grep "+.*eslint-disable" | wc -l` → 0)
    - Any inline interface added for `event.data` without a registered schema has a matching `TODO(phase-88.2-followup): register .* schema` comment (`grep -rn "phase-88.2-followup" web/lib/inngest/ | wc -l` ≥ 1 if any inline shortcut was used; can be 0 if all events had registered schemas)
  </acceptance_criteria>
  <done>
    `lib/inngest/` lint errors at 0. Inngest test suite still passes. Replay-safety patterns untouched. Inline interfaces co-located with usage per D-10.
  </done>
</task>

<task type="auto">
  <name>Task 2: Burn down debtor-email-analyzer/src/ + long-tail (any + require → import + misc)</name>
  <files>web/debtor-email-analyzer/src/**/*.ts, web/lib/zapier/**/*.ts, web/lib/dashboard/**/*.ts, web/app/api/**/*.ts, web/tests/labeling/**/*.ts, web/app/(dashboard)/**/*.tsx</files>
  <read_first>
    - .planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-RESEARCH.md §"Lint Error Inventory" → "Errors by Directory Bucket × Rule" and "Code Examples" Pattern 1 (any narrowing) + Pattern 4 (require → import)
    - .planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-CONTEXT.md §"Decisions" D-10, D-12, D-13
    - Run `cd web && npx eslint . 2>&1 | grep "  error" | grep -v "lib/inngest" | grep -v "React Compiler\|cascading\|Cannot call impure\|JSX within try\|cannot be modified"` to enumerate the remaining error sites with file:line:rule. The 2 `no-require-imports` errors in `app/(dashboard)/` need to be among the matches.
    - Each individual file whose error line you are about to edit (one targeted Read per file is enough — these are single-error files mostly)
  </read_first>
  <action>
    Per D-13, this is ONE commit per top-level area. Group commits as:
      (1) `fix(lint): narrow any in debtor-email-analyzer/src/` (9 errors)
      (2) `fix(lint): require → import + misc in app/(dashboard)/` (2 require-imports + 1 unescaped-entities)
      (3) `fix(lint): long-tail single-error files (zapier, dashboard, app/api, tests/labeling)` (3+1 = 4 errors)

    **For each `any` site (D-10):** inline minimal interface co-located with usage. No domain modelling.

    **For each `require()`:** convert to `import x from "y"` (Pattern 4). If genuinely dynamic, use `await import("...")`. Verify the import works in the build context (the 2 cases are in `app/(dashboard)/` so they're inside Next's bundler scope — ESM import is fine).

    **For `react/no-unescaped-entities`:** the 1 case in `app/(dashboard)/automations/...` is a literal `'` or `"` in JSX. Replace with `&apos;` / `&quot;` / `&rsquo;` as appropriate. Five-second fix.

    **For `prefer-const`:** the 1 case in `tests/labeling/` — change `let` to `const`. Trivial.

    Do NOT touch `lib/automations/**` (ignored by Plan 01). Do NOT touch `lib/inngest/` (handled by Task 1). Do NOT touch the 10 RC errors (handled by Task 3).

    Run `(cd web && npm run lint)` after each commit. Total residual error count should drop after each.
  </action>
  <verify>
    <automated>cd web && npx eslint . 2>&1 | grep -cE "  error " | tr -d '\n'; echo " errors remaining"; npx eslint . 2>&1 | grep -E "  error " | grep -v -iE "cascading|impure|JSX within try|cannot be modified|setState" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx eslint debtor-email-analyzer/src/ 2>&1 | grep -cE "  error "` returns `0` (was: 9)
    - `cd web && npx eslint . 2>&1 | grep "no-require-imports" | grep -v "lib/automations" | wc -l` returns `0` (was: 2 outside lib/automations/)
    - `cd web && npx eslint . 2>&1 | grep "no-unescaped-entities" | wc -l` returns `0`
    - `cd web && npx eslint . 2>&1 | grep "prefer-const" | wc -l` returns `0`
    - All non-React-Compiler errors are at 0 (only the 10 RC errors remain; Task 3 handles those): `cd web && npx eslint . 2>&1 | grep -E "  error " | grep -v -iE "cascading|impure|JSX within try|cannot be modified|setState synchronously" | wc -l` returns `0`
    - `(cd web && npm test)` exits 0 (no regression vs. Plan 02 baseline)
  </acceptance_criteria>
  <done>
    Only the 10 React Compiler errors remain. All `any`, `require`, `unescaped-entities`, `prefer-const` errors are 0. Test suite still green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix the 10 React Compiler errors at source (D-14; no rule disable)</name>
  <files>web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/stage-1-widget.test.tsx, web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-2-widget.tsx, web/components/automations/agent-run-board.tsx, web/components/automations/bulk-review/audit/ScreenshotThumb.tsx, web/components/automations/screenshot-viewer.tsx, web/components/graph/swarm-graph.tsx, web/components/v7/kanban/kanban-board.tsx, web/components/v7/kanban/kanban-column.tsx, web/components/v7/kanban/kanban-job-card.tsx, web/components/v7/terminal/terminal-stream.tsx</files>
  <read_first>
    - .planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-RESEARCH.md §"React Compiler Errors (verbatim, file:line)" — has every file:line:error in a table
    - .planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-RESEARCH.md §"Code Examples" Pattern 3 (setState-in-effect → derive during render) — canonical fix for 7 of the 10
    - .planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-RESEARCH.md §"Common Pitfalls" Pitfall 4 (don't make setState-in-effect worse) — derive-during-render is the right answer when the value is computable from props/state
    - .planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-CONTEXT.md §"Decisions" D-14 (fix at source — no rule disable)
    - Each of the 10 files at its target line (one Read per file, focused on the surrounding 30 lines so you can see what `useState`/`useEffect` is doing)
  </read_first>
  <action>
    Per D-13, this is ONE commit: `fix(lint): React Compiler errors at source`.

    **Track A — 7× "setState synchronously within an effect" (errors 2, 4, 5, 6, 7, 8, 10):**
    For each, apply Pattern 3 from RESEARCH §"Code Examples":
      - If the effect's job is to compute a value from props/state → delete the `useState`+`useEffect` pair and compute during render: `const x = derive(...)`.
      - If the effect is genuinely side-effectful (subscription, manual DOM, network) → keep the effect but use the functional updater `setX(prev => ...)` and gate with `if (prev !== next) return`. Per Pitfall 4, prefer derive-during-render whenever the value is computable.
      - For each file, read the surrounding context to determine which path applies. The 7 sites are spread across kanban, screenshot, graph, terminal-stream — most are derived-state candidates.

    **Track B — error #3 (`Date.now()` impure in render, `agent-run-board.tsx:118`):**
    Move `Date.now()` to a `useState` initialiser: `const [now] = useState(() => Date.now())`. If the value needs to update over time, use `useEffect` + interval to refresh state. Match the existing component's behaviour intent.

    **Track C — error #9 (`JSX within try/catch`, `kanban-job-card.tsx:204`):**
    Extract the JSX-producing call into a separate component, then either use a React error boundary at the parent OR remove the try/catch (if the boundary handles it). Per RESEARCH §"React Compiler Errors" remediation column.

    **Track D — error #1 (`This value cannot be modified`, `stage-1-widget.test.tsx:110`):**
    Test file — re-assignment of a value RC considers frozen. Refactor to a fresh local binding or use `structuredClone` / spread to mutate a copy. Read the surrounding test logic to choose the least-invasive fix.

    **CRITICAL — no `eslint-disable`:** D-14 forbids rule disables. If a fix genuinely cannot land at source within reasonable scope (unlikely for these 10 well-categorized errors), STOP and surface for the operator. Do NOT add a disable line.

    Verify after edits: `(cd web && npm run lint)` exits 0; `(cd web && npm run typecheck)` exits 0; `(cd web && npm test)` exits 0 (kanban / screenshot / graph components have tests that may depend on the previous behaviour — derive-during-render should be observationally equivalent, but verify).
  </action>
  <verify>
    <automated>cd web && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `(cd web && npm run lint)` exits 0
    - `cd web && npm run lint 2>&1 | grep -ciE "cascading|impure|JSX within try|cannot be modified|setState synchronously"` returns `0`
    - `git diff web/components/ web/app/ | grep -E "^\+.*eslint-disable" | wc -l` returns `0` (no new disable lines)
    - `(cd web && npm run typecheck)` exits 0
    - `(cd web && npm test)` exits 0 (no regression in component tests)
    - For each of the 7 "setState-in-effect" sites, the surrounding diff shows EITHER (a) the `useEffect` + `useState` pair removed and replaced with derive-during-render, OR (b) a functional-updater `setX(prev => ...)` with a guard
  </acceptance_criteria>
  <done>
    All 10 React Compiler errors resolved at source per D-14. `npm run lint` exits 0. Typecheck and tests still green. No `eslint-disable` lines added.
  </done>
</task>

</tasks>

<verification>
- `(cd web && npm run lint)` exits 0
- `(cd web && npm run lint 2>&1 | grep -E "  error ")` returns nothing
- `(cd web && npm run typecheck)` exits 0
- `(cd web && npm test)` exits 0 (no test regression vs. Plan 02 baseline)
- `git diff main -- web/ | grep -cE "^\+.*eslint-disable" returns 0 new disable lines (or only lines with a one-line reason comment per SPEC constraint)
- Per-directory commits visible in `git log --oneline` — at minimum: `fix(lint): narrow any in lib/inngest/`, `fix(lint): debtor-email-analyzer/src/`, `fix(lint): require → import + misc in app/(dashboard)/`, `fix(lint): React Compiler errors at source`
</verification>

<success_criteria>
- R-1 acceptance met: `npm run lint` exits 0 with 0 errors.
- No rule disables added (D-14 constraint).
- Per-directory commit cadence visible in history (D-13).
- Test + typecheck baselines from Plan 02 still hold.
</success_criteria>

<output>
Create `.planning/phases/88.2-tier-2-ci-backlog-cleanup/88.2-03-SUMMARY.md` recording:
- Final error count (target: 0)
- Per-directory commit hashes
- For each RC error: which remediation path was chosen (Track A subcategory, etc.)
- Any inline event-schema TODOs added for Inngest (D-11 fallback) with the event names
</output>
