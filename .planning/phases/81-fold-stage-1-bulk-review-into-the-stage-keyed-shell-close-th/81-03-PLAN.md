---
phase: 81
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/noise-category-chip-strip.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/candidate-rule-list.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/pending-promotion-detail-pane.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/row-list.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/actions.ts
  - web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/page-shell.test.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/noise-category-chip-strip.test.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/load-page-data.test.ts
autonomous: true
requirements: []
decisions_addressed: [D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-16, D-17, D-18]

must_haves:
  truths:
    - "Filters popover (D-07 entity + mailbox) is deferred to a follow-up phase; URL params ?entity= and ?mailbox= remain functional via direct URL editing — guarded by a regression test in load-page-data.test.ts; deferral recorded in deferred-items.md."
    - "/stage-1 renders <PageHeader> + <StageTabStrip currentStage={1}> on top — no 'Bulk Review' h1 or intro paragraph anywhere on the rendered page (D-16, D-17, D-18)."
    - "Below the tab strip, a horizontal noise-category chip strip renders one chip per swarm_noise_categories row + an 'All' chip (+ 'unknown' is just a regular chip per the seed)."
    - "Each chip displays a count badge from classifier_queue_counts (already loaded by loadPageData)."
    - "Clicking a chip writes ?topic=<noise_key> to the URL via router.push and clears ?sub if present."
    - "The chip strip ends with a 1px divider + 'Pending promotion · N' tail pill that links to ?sub=pending."
    - "Visiting /stage-1?sub=pending swaps the row list for a candidate-rule list and the detail pane for a rule-evidence pane."
    - "The default (no sub) view still renders RowList + DetailPane in a 2-col grid: minmax(380px,460px) | 1fr (D-05) — NOT the legacy 3-col QueueTree grid."
    - "The realtime channel name remains ${swarmType}-review (D-19)."
    - "loadPageData branches on params.sub === 'pending'; the legacy params.tab === 'pending' branch is removed (D-10)."
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx"
      provides: "Shell-wrapped Stage 1 RSC with chip-strip + 2-col grid + sub=pending branch"
      contains: "currentStage={1}"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/noise-category-chip-strip.tsx"
      provides: "Horizontal noise-category chip strip with tail Pending pill"
      contains: "swarm_noise_categories"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/candidate-rule-list.tsx"
      provides: "Extracted PendingPromotionPanel rendered when ?sub=pending"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/pending-promotion-detail-pane.tsx"
      provides: "Right-pane rule evidence (rule_key, n, ci_lo, sample emails, promote/reject)"
  key_links:
    - from: "stage-1/page.tsx"
      to: "stage-1/noise-category-chip-strip.tsx"
      via: "import + render below StageTabStrip"
      pattern: "<NoiseCategoryChipStrip"
    - from: "stage-1/page.tsx"
      to: "stage-1/candidate-rule-list.tsx + stage-1/pending-promotion-detail-pane.tsx"
      via: "ternary on sp.sub === 'pending'"
      pattern: "sp\\.sub === \"pending\""
    - from: "stage-1/page.tsx"
      to: "AutomationRealtimeProvider"
      via: "channel ${swarmType}-review (D-19)"
      pattern: "\\$\\{swarmType\\}-review"
---

<objective>
Reframe `stage-1/page.tsx` from a legacy 3-col Bulk Review surface into the Sketch-005
shell-wrapped 2-col surface (D-05). Drop the `<h1>...— Bulk Review</h1>` and the intro
paragraph (D-16, D-17, D-18); wrap in `<PageHeader>` + `<StageTabStrip currentStage={1}>`
the same way `/stage-3/page.tsx` does. Replace the `<QueueTree>` left column with a
horizontal `<NoiseCategoryChipStrip>` rendered below the tab strip and above the row
list (D-05, D-06). Wire the `?sub=pending` URL state end-to-end (D-09, D-10, D-11) by
adding `sub` to `PageSearchParams`, branching `loadPageData` on `params.sub === "pending"`,
and rendering `<CandidateRuleList>` + `<PendingPromotionDetailPane>` instead of
`<RowList>` + `<DetailPane>` when active.

This plan does NOT delete `QueueTree` (Plan 04 owns that), does NOT purge the leftover
"Bulk Review" string occurrences in moved sibling files (Plan 04), does NOT extend the
middleware redirect tests (Plan 04). It DOES delete the h1 + intro paragraph in
`page.tsx` and the `selection.tab === "pending"` render branch in `row-list.tsx`.

Purpose: Deliver the actual Sketch-005 lock — the chrome and behaviour of /stage-1 now
matches /stage-0 / /stage-3 / /stage-4. Pending Promotion finally renders.

Output: New `/stage-1` chrome, two new client components (chip strip + rule-list +
rule-detail), a loader branch on `?sub=pending`, RTL tests for page shell + chip strip,
and extended loader tests for the new branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-CONTEXT.md
@.planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-RESEARCH.md
@.planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-PATTERNS.md
@.claude/skills/sketch-findings-agent-workforce/references/stage-keyed-shell-phase-76.md
@.claude/skills/sketch-findings-agent-workforce/references/bulk-review-phase-71.md
@web/app/(dashboard)/automations/[swarm]/stage-3/page.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/recipient-chip-strip.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/row-list.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/detail-pane.tsx
@web/app/(dashboard)/automations/[swarm]/stage-2/_lib/load-stage-2-weekly-count.ts
@web/lib/swarms/registry.ts
@web/lib/swarms/types.ts

<interfaces>
<!-- After Plan 01, stage-1/page.tsx is the inlined 845-line page. This plan REWRITES its
     top-level render shape. Preserve all existing data-loading code (loadPageData and
     friends) — only the JSX envelope and one loader-branch change. -->

<!-- Existing PageSearchParams (review/page.tsx) currently has:
       { tab?: string; topic?: string; entity?: string; mailbox?: string; selected?: string; rule?: string }
     Add `sub?: string` and remove the `tab === "pending"` branch from loadPageData.
     Note: keep `tab?: string` in the type — middleware rewrites legacy ?tab=pending → ?sub=pending,
     so the param can still arrive in extreme edge cases (defensive); just don't BRANCH on it. -->

<!-- StageTabStrip API: see Plan 02 interfaces. counts={{ 1: data.rows.length, 2: stage2Count }} -->

<!-- New chip-strip prop shape (mirror recipient-chip-strip.tsx): -->
interface NoiseCategoryChipStripProps {
  categories: SwarmNoiseCategoryRow[];   // from loadSwarmNoiseCategories
  counts: QueueCountRow[];                // from existing classifier_queue_counts RPC call
  activeTopic: string;                    // sp.topic ?? "all"
  candidateCount: number;                 // data.candidates.length — tail pill badge
  activeSub: string | null;               // sp.sub ?? null
}

<!-- New candidate-rule-list prop shape (extracted from row-list.tsx::PendingPromotionPanel): -->
interface CandidateRuleListProps {
  rules: ClassifierRuleCandidate[];       // existing data.candidates type
  selectedRuleKey: string | null;
  swarmType: string;
}

<!-- New pending-promotion-detail-pane prop shape: -->
interface RuleSample {
  email_id: string;
  subject: string;
  sender: string;
  created_at: string;
}
interface PendingPromotionDetailPaneProps {
  rules: ClassifierRuleCandidate[];
  selectedRuleKey: string | null;
  samples: RuleSample[];   // fetched via loadRuleSamples in page.tsx when sp.sub === "pending" && sp.rule != null
  swarmType: string;
}

<!-- Stage 2 count helper (from Plan 02): -->
import { loadStage2WeeklyCount } from "../stage-2/_lib/load-stage-2-weekly-count";
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build noise-category-chip-strip.tsx (mirror recipient-chip-strip; tail Pending pill)</name>
  <files>
    web/app/(dashboard)/automations/[swarm]/stage-1/noise-category-chip-strip.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/noise-category-chip-strip.test.tsx
  </files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/recipient-chip-strip.tsx (FULL — visual + URL-write template; duplicate styling per CONTEXT §specifics, do NOT extract a generic abstraction)
    - .planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-PATTERNS.md (§"noise-category-chip-strip.tsx" — full code excerpt)
    - .planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-RESEARCH.md (§Code Examples — sketch)
    - web/lib/swarms/types.ts (SwarmNoiseCategoryRow shape — `category_key`, `display_label`, plus existing fields)
  </read_first>
  <behavior>
    - Renders `role="tablist"` outer with `aria-label="Filter Stage 1 by noise category"`.
    - Renders an "All" chip first; active when `activeTopic === "all" && !activeSub`.
    - Iterates `categories`; renders one chip per row (key=`category_key`, label=`display_label`, count from `countByTopic`). The `unknown` row is rendered like any other (NO special-case branching — RESEARCH Anti-Pattern 6).
    - Renders a 1px × 24px vertical divider (`background: var(--v7-line)`, 8px gap each side) AFTER the chip loop.
    - Renders a tail `<Link href="${pathname}?sub=pending" role="tab" aria-selected={activeSub === "pending"}>` showing `Pending promotion · {candidateCount}`.
    - When `categories.length === 0`, the strip still renders ("All" chip + divider + tail pill) — empty-strip is signal (CONTEXT §specifics).
    - Chip click writes URL via `router.push` with: chosen `topic` set (or `topic` deleted when "all"), AND `sub` deleted (so chip selection clears the Pending sub-view — Assumption A5).
    - Pending pill click writes `?sub=pending` (preserves other params; clears `topic`).
    - All styling uses existing `var(--v7-*)` tokens — no new tokens, no new CSS files.
  </behavior>
  <action>
    1. Create `noise-category-chip-strip.tsx` based on `recipient-chip-strip.tsx`:
       - Top: `"use client";`
       - Imports: `useCallback`, `Link`, `useRouter`, `usePathname`, `useSearchParams`, `SwarmNoiseCategoryRow` from `@/lib/swarms/types`, and a `QueueCountRow` type (import from `./page` after Task 3 adds the export, OR define inline as `{ topic: string | null; entity: string | null; mailbox_id: string | null; count: number }` matching the existing classifier_queue_counts RPC shape — verify in current `review/page.tsx` after Plan 01 move).
       - Define `Props` exactly as the interface in `<context>` above.
       - Define a local `Chip` helper: same shape as the `Chip` in `recipient-chip-strip.tsx` (button, role="tab", aria-selected, var(--v7-radius-pill), brand-secondary tokens, 11px tabular-nums count badge) MINUS the brand dot.
       - `navigate(nextTopic)`: clone `URLSearchParams`, `if (nextTopic === "all") qs.delete("topic"); else qs.set("topic", nextTopic);`, `qs.delete("sub");`, then `router.push(...)`.
       - Build `countByTopic = new Map<string, number>()` from `counts`, only counting entries where `c.topic !== null`.
       - `totalCount = counts.reduce((s, c) => s + c.count, 0)` for the "All" chip badge.
       - Render outer `<div role="tablist" aria-label="Filter Stage 1 by noise category" className="flex items-center gap-2 overflow-x-auto py-3">`, then "All" chip, then `categories.map(...)`, then divider span, then tail `<Link>` pill.
    2. Create `__tests__/noise-category-chip-strip.test.tsx`:
       - Mock `next/navigation`: `useRouter: () => ({ push })`, `usePathname: () => "/automations/debtor-email/stage-1"`, `useSearchParams: () => new URLSearchParams("")`.
       - Cases:
         (a) Renders one chip per `categories` entry + "All" chip. Asserts `getAllByRole("tab")` has length `categories.length + 2` (categories + "All" + Pending pill).
         (b) Click on "payment" chip calls `push` with `?topic=payment` (no `sub` in result).
         (c) Click on "All" chip calls `push` with no `topic` param.
         (d) When `activeSub === "pending"`, the Pending pill has `aria-selected="true"`.
         (e) When `categories === []`, the "All" chip and the Pending pill still render (empty strip = signal).
         (f) Component contains NO references to `swarm_intents` (grep on the rendered DOM is impossible; this is a static-source-grep gate handled at file level: `! grep -q "swarm_intents" noise-category-chip-strip.tsx`).
  </action>
  <verify>
    <automated>
      bash -lc 'cd web && npx vitest run app/\(dashboard\)/automations/\[swarm\]/stage-1/__tests__/noise-category-chip-strip.test.tsx --no-coverage 2>&1 | tail -3 | grep -q "passed"'
    </automated>
  </verify>
  <acceptance_criteria>
    - File `noise-category-chip-strip.tsx` exists; first line is `"use client";`.
    - File contains the literal substrings: `role="tablist"`, `aria-label="Filter Stage 1 by noise category"`, `var(--v7-radius-pill)`, `var(--v7-brand-secondary-soft)`, `var(--v7-line)`, `qs.delete("sub")`, `Pending promotion · `.
    - File does NOT contain `swarm_intents` anywhere.
    - File does NOT contain `useReducer` or any new state library imports.
    - All RTL test cases (a-f) pass.
  </acceptance_criteria>
  <done>
    Chip strip is a self-contained client component, registry-driven, URL-state via router.push, no new abstractions extracted. Tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract candidate-rule-list.tsx + pending-promotion-detail-pane.tsx out of row-list.tsx</name>
  <files>
    web/app/(dashboard)/automations/[swarm]/stage-1/candidate-rule-list.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/pending-promotion-detail-pane.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/row-list.tsx
  </files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/row-list.tsx (FULL — locate the `PendingPromotionPanel` component around line 268 per RESEARCH §Pitfall 5; identify its props + the `selection.tab === "pending"` render gate)
    - web/app/(dashboard)/automations/[swarm]/stage-1/detail-pane.tsx (right-pane visual envelope to mirror)
    - .planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-PATTERNS.md (§"candidate-rule-list.tsx" + §"pending-promotion-detail-pane.tsx")
    - .planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-RESEARCH.md (Open Q4 — grep `web/app/(dashboard)/swarm/[swarmId]/(components)` for existing rule-promotion UI BEFORE plumbing fresh server actions)
    - web/app/(dashboard)/automations/[swarm]/stage-1/actions.ts (existing server actions — see whether `promoteRule` / `rejectRule` already exist; if not, ADD them here)
  </read_first>
  <action>
    1. **Wave 0 grep** (RESEARCH Open Q4): run
       `rg -n "promoteRule\|rejectRule\|ci_lo\|Wilson" web/app/\(dashboard\)/swarm/`.
       - If existing rule-promotion components/server-actions are found, REUSE them
         (import paths from the discovered modules). Note the location in the SUMMARY.
       - If nothing relevant is found, plumb fresh server actions `promoteRule` and
         `rejectRule` in `stage-1/actions.ts`. Each takes `{ rule_key: string }`,
         performs the appropriate `classifier_rules` table mutation (use existing
         service-role admin client; mirror the shape of any existing action in
         `actions.ts` — e.g. `approveRow`, `rejectRow`).
    2. Open `row-list.tsx` and locate `PendingPromotionPanel` (around line 268). Cut its
       full body. Create `candidate-rule-list.tsx` with `"use client";` at top, paste
       the body, and rename the exported function to `CandidateRuleList`. Update its
       props interface to `CandidateRuleListProps` (see `<interfaces>`).
    3. In `row-list.tsx`, REMOVE the `if (selection.tab === "pending") return <PendingPromotionPanel … />` (or equivalent inline render) branch. RowList now ONLY renders the predicted-row list — no sub-view forks. Also remove any now-unused imports.
    4. Create `pending-promotion-detail-pane.tsx` with `"use client";`. It mirrors the
       outer-shell of `detail-pane.tsx` (same right-pane container styling — read
       `detail-pane.tsx`'s top-level `<aside>` / wrapper to copy padding + tokens).
       **Wave 0 verification of candidate row shape (Step 1):** Read
       `web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx` and confirm the
       `ClassifierCandidate` interface around line 72:
       ```
       export interface ClassifierCandidate {
         rule_key: string;
         status: string;
         n: number;
         ci_lo: number | null;
       }
       ```
       The candidates query at the existing call site (`.from("classifier_rules")
       .select("rule_key, status, n, ci_lo").eq("swarm_type", swarmType)
       .eq("status", "candidate")`) does NOT carry sample emails today. Therefore
       D-09 ("sample matched emails") requires plumbing a thin sample-fetch in
       this plan — DO NOT defer.

       **Plumb a thin sample fetch:** Add a server action or loader helper
       `loadRuleSamples(admin, swarm_type, rule_key, limit = 5): Promise<RuleSample[]>`
       in `stage-1/actions.ts` (or a new `_lib/load-rule-samples.ts`). Query the
       email-pipeline source the existing classifier rule was matched against
       (mirror the predicted-row source — `pipeline_events` filtered by
       `decision_details->>rule_key = rule_key` and `swarm_type`, with
       `LIMIT 5` ordering by `created_at DESC`). Return shape:
       `{ email_id: string; subject: string; sender: string; created_at: string }[]`.

       Body shows for the `selectedRule`:
       - `<h3>{rule.rule_key}</h3>`
       - `<dl>` with: status, n, ci_lo (Wilson lower bound), ci_hi if available
       - A short list of sample matched emails fetched via `loadRuleSamples`
         (server-side fetch in the page render path when `sp.sub === "pending" &&
         sp.rule != null`; pass results into the detail pane as a `samples` prop).
         Render up to 5 rows showing subject + sender + relative time. Empty array
         renders `"No samples found"` inline.
       - Two buttons: `<form action={promoteRule}>` and `<form action={rejectRule}>`
         each with a hidden input `name="rule_key" value={rule.rule_key}`.
       - When `selectedRuleKey === null`, render an empty-state: `<p style={{ color: "var(--v7-text-muted)" }}>Select a rule to see evidence.</p>`.
    5. Verify nothing else imports `PendingPromotionPanel` after extraction:
       `! rg -n "PendingPromotionPanel" web/app web/tests`.
  </action>
  <verify>
    <automated>
      bash -lc 'cd web && npx tsc --noEmit 2>&1 | grep -E "stage-1/(row-list|candidate-rule-list|pending-promotion-detail-pane|actions)" || echo "no TS errors in extracted files"'
    </automated>
  </verify>
  <acceptance_criteria>
    - `candidate-rule-list.tsx` exists, exports `CandidateRuleList` as a named export, starts with `"use client";`.
    - `pending-promotion-detail-pane.tsx` exists, exports `PendingPromotionDetailPane`, starts with `"use client";`.
    - `row-list.tsx` no longer contains a `selection.tab === "pending"` render branch (grep gate: `! grep -q "selection\\.tab === \"pending\"" row-list.tsx`).
    - `! rg -n "PendingPromotionPanel" web/app web/tests` (the old name is gone).
    - `actions.ts` exports `promoteRule` and `rejectRule` (either freshly plumbed or re-exported from a discovered location — record which in SUMMARY).
    - `npx tsc --noEmit` produces zero errors in the four touched files.
  </acceptance_criteria>
  <done>
    Pending Promotion is now a top-level page-managed branch, not a row-list-internal fork. RowList is single-responsibility (predicted rows only).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite stage-1/page.tsx as shell-wrapped 2-col surface with sub=pending branch</name>
  <files>
    web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/page-shell.test.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/load-page-data.test.ts
  </files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-3/page.tsx (FULL — canonical shell-wrap shape; D-04/D-05 referenced)
    - web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx (FULL — current 845-line page after Plan 01 move; this is what gets rewritten)
    - web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/load-page-data.test.ts (existing mock shape — extend, don't replace)
    - .planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-PATTERNS.md (§"stage-1/page.tsx" — exact `<main>` body shape)
    - .planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-RESEARCH.md (§"Pattern 1" + §"Pattern 3" + §Pitfall 2)
  </read_first>
  <behavior>
    - Page accepts `params` and `searchParams` (both `Promise<...>`). `PageSearchParams` includes `sub?: string` (NEW), `topic?: string`, `entity?: string`, `mailbox?: string`, `selected?: string`, `rule?: string`, and `tab?: string` (kept in the type for defensive parsing; not branched on).
    - 404 gate uses `if (!swarm || !swarm.enabled) notFound();` (per existing review/page.tsx — preserves the `enabled` guard for the daily-driver surface).
    - Parallel fetch via `Promise.all`: `[loadPageData(sp, admin, swarmType), loadSwarmNoiseCategories(admin, swarmType), swarmType === "debtor-email" ? loadStage2WeeklyCount(admin) : Promise.resolve(0)]`.
    - Renders `<PageHeader swarm={swarm} />` (NO h1, NO intro paragraph — D-16, D-17).
    - Renders `<StageTabStrip swarm={swarm} currentStage={1} counts={{ 1: data.rows.length, 2: stage2Count }} />`.
    - Wraps body in `<AutomationRealtimeProvider automations={[\`${swarmType}-review\`]}>` (D-19 — channel name preserved).
    - Inside `<SelectionProvider>`, renders `<main>` containing:
      - `<NoiseCategoryChipStrip categories={noiseCategories} counts={data.counts} activeTopic={sp.topic ?? "all"} candidateCount={data.candidates.length} activeSub={sp.sub ?? null} />`
      - `<div className="grid grid-cols-[minmax(380px,460px)_1fr] gap-4 min-w-0">` (D-05 — 2-col, NOT 3-col)
        - When `sp.sub === "pending"`: `<CandidateRuleList ... />` + `<PendingPromotionDetailPane ... />`
        - Else: `<RowList ... />` + `<DetailPane ... />` (existing props preserved)
      - `<KeyboardShortcuts ... />` and `<Cheatsheet />` if currently present at end of `<main>`.
    - `loadPageData` branches on `params.sub === "pending"`:
      - **pending branch:** still load `classifier_rules` candidates; SKIP the predicted-row + body/timeline/coordinator/tagging waterfall. Return `PageData` with `rows: []` and full `candidates`.
      - **default branch:** unchanged from review/page.tsx logic.
    - The legacy `params.tab === "pending"` branch is REMOVED from `loadPageData` (D-10).
  </behavior>
  <action>
    1. Open `stage-1/page.tsx` (post-Plan-01 inlined version — 845 LOC). Identify the
       three regions to surgically edit:
       (a) `PageSearchParams` interface — add `sub?: string` field.
       (b) `loadPageData` function — remove the `params.tab === "pending"` branch
           (replace with `params.sub === "pending"` branch returning `{ rows: [], candidates, counts, ... }`).
       (c) The default-export RSC `Page` function — replace its return JSX entirely
           with the shell-wrapped 2-col shape (PATTERNS §"stage-1/page.tsx" body shape).
    2. Add imports at top of file:
       ```typescript
       import { PageHeader } from "../_shell/page-header";
       import { StageTabStrip } from "../_shell/stage-tab-strip";
       import { loadStage2WeeklyCount } from "../stage-2/_lib/load-stage-2-weekly-count";
       import { NoiseCategoryChipStrip } from "./noise-category-chip-strip";
       import { CandidateRuleList } from "./candidate-rule-list";
       import { PendingPromotionDetailPane } from "./pending-promotion-detail-pane";
       ```
       Keep all existing imports (loadPageData internals, `RowList`, `DetailPane`,
       `SelectionProvider`, `AutomationRealtimeProvider`, etc.).
    3. DELETE from the JSX:
       - The `<h1>... — Bulk Review</h1>` (D-16 + D-18).
       - The intro `<p>Review predicted classifications…</p>` (D-17).
       - The `<QueueTree ... />` element (D-02 — but DO NOT delete the `queue-tree.tsx` FILE; Plan 04 owns the file deletion. Just remove the JSX usage here so this plan doesn't break compile. The import line for QueueTree should also be removed.)
       - The 3-col grid styling (`grid-cols-[clamp(220px,18vw,280px)_minmax(380px,460px)_1fr]` or equivalent).
    4. ADD the new `<main>` body per `<behavior>`. The 2-col grid is exactly:
       `<div className="grid grid-cols-[minmax(380px,460px)_1fr] gap-4 min-w-0">`.
    5. In `loadPageData`, branch on `params.sub === "pending"` and short-circuit the
       predicted-row pipeline. The candidate-rule fetch (`classifier_rules` candidates)
       is still performed in BOTH branches — the pending branch just skips the rest.
       Existing legacy `params.tab === "pending"` code path: DELETE.
    6. **page-shell.test.tsx (NEW):** RTL test asserting:
       (a) Renders `<PageHeader>` (mockable element with `data-testid="page-header"`) and `<StageTabStrip>` with `currentStage={1}` (mock the StageTabStrip to render `data-current-stage`).
       (b) Does NOT render any element containing the text "Bulk Review" (`expect(container.textContent).not.toContain("Bulk Review")`).
       (c) Renders the `<NoiseCategoryChipStrip>` (mock it to a recognisable test marker).
       (d) Renders the 2-col grid (`querySelector('.grid-cols-\\[minmax\\(380px\\,460px\\)_1fr\\]')` or look for the className substring).
       (e) When `searchParams.sub === "pending"`, renders `CandidateRuleList` (mock marker) instead of `RowList`.
       (f) Cross-swarm check: with mocked sales-email swarm, the chip-strip prop `categories` reflects the mocked sales-email registry rows (test-`-t "cross-swarm"` for goal-backward check #10).
    7. **load-page-data.test.ts (EXTEND):** Add cases:
       - `params.sub === "pending"` → returned `PageData.rows` is `[]`, `PageData.candidates` is non-empty.
       - `params.sub === "pending"` → the mock `.from("pipeline_events_email_summary")` (or whichever predicted-row source) is NOT called (assert via the recorded `_eqCalls` / `.from` invocations).
       - `params.sub === undefined` (default) → existing behaviour preserved (regression assertion).
       - `params.tab === "pending"` (legacy) → does NOT trigger the candidate-only branch (the legacy path is removed; loader treats it as default).
       - **Filters URL-direct-edit regression (D-07 + CONTEXT §discretion):** call
         `loadPageData({ entity: "X", mailbox: "12" }, admin, swarmType)` and assert
         the loader applies BOTH filters (recorded `.eq("entity", "X")` and
         `.eq("mailbox_id", "12")` invocations on the predicted-row builder). This
         guards the deferred Filters-popover decision: the popover UI is dropped,
         but URL params must keep working via direct URL editing.
    8. **Record deferral of Filters popover** (CONTEXT §discretion). Create or
       append to `.planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/deferred-items.md`
       with this entry:
       ```markdown
       ## Filters popover (entity + mailbox) — deferred to follow-up phase

       Phase 81 D-07 specified a compact secondary "Filters" button popover for
       entity + mailbox drill-down. Per CONTEXT §discretion (planner's call based
       on plan budget), the popover UI is deferred. URL params `?entity=` and
       `?mailbox=` remain functional via direct URL editing — verified by a
       regression test in `stage-1/__tests__/load-page-data.test.ts` (Plan 03
       Task 3, Step 7).

       Carry-forward: a future phase should ship the popover as a single ~80-LOC
       client component reading current URL params, presenting entity dropdown +
       mailbox multi-select, and `router.push`-ing on apply.
       ```
  </action>
  <verify>
    <automated>
      bash -lc 'cd web && npx vitest run app/\(dashboard\)/automations/\[swarm\]/stage-1/__tests__/page-shell.test.tsx app/\(dashboard\)/automations/\[swarm\]/stage-1/__tests__/load-page-data.test.ts --no-coverage 2>&1 | tail -5 | grep -q "passed"'
    </automated>
  </verify>
  <acceptance_criteria>
    - `stage-1/page.tsx` contains: `<PageHeader`, `<StageTabStrip`, `currentStage={1}`, `counts={{ 1:`, `${swarmType}-review`, `<NoiseCategoryChipStrip`, `grid-cols-[minmax(380px,460px)_1fr]`, `sp.sub === "pending"`, `<CandidateRuleList`, `<PendingPromotionDetailPane`, `loadStage2WeeklyCount`.
    - `stage-1/page.tsx` does NOT contain: `Bulk Review`, `Review predicted classifications`, `<QueueTree`, `params.tab === "pending"`, `clamp(220px,18vw,280px)`.
    - `PageSearchParams` interface contains `sub?: string`.
    - `loadPageData` contains a branch on `params.sub === "pending"` that returns `{ rows: [], candidates, ... }`.
    - All page-shell.test.tsx and extended load-page-data.test.ts cases pass.
    - `cd web && npx tsc --noEmit` produces zero errors in `stage-1/page.tsx`.
    - Loader filters correctly when called with `?entity=X&mailbox=12` (verified by the new RTL/loader regression test in load-page-data.test.ts — guards the deferred Filters-popover decision).
    - A `deferred-items.md` entry exists at `.planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/deferred-items.md` recording: "Filters popover (entity + mailbox) deferred to follow-up phase; URL params remain functional via direct URL editing".
    - Manual smoke (recorded in SUMMARY): `/automations/debtor-email/stage-1` renders with the new chrome; clicking a chip writes `?topic=...` to the URL; clicking the Pending pill writes `?sub=pending` and the row list swaps to the candidate-rule list.
  </acceptance_criteria>
  <done>
    `/stage-1` is now the canonical Sketch-005 shell-wrapped surface. Pending Promotion sub-view actually renders. The Stage 2 tab badge shows the live count from Plan 02's loader. Plan 04 will purge leftover "Bulk Review" strings in sibling files and delete the now-unused `queue-tree.tsx`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → RSC route | Untrusted `searchParams.sub`, `searchParams.topic`, `searchParams.rule` |
| Browser/Client → Server Action | `promoteRule` / `rejectRule` invoked from PendingPromotionDetailPane |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-03-01 | Tampering | `searchParams.sub` rendered into branch logic | mitigate | Consumed only as `=== "pending"` equality check; never interpolated into JSX or SQL. |
| T-81-03-02 | Tampering | `searchParams.topic` filter into Supabase query | accept | Already mitigated pre-phase: passed to `.eq("decision_details->>topic", topic)` (parameterised by Supabase client). |
| T-81-03-03 | Tampering | `promoteRule` / `rejectRule` server actions | mitigate | Use existing service-role admin client; rule_key validated against `classifier_rules` registry before mutation; mirror existing `approveRow` / `rejectRow` patterns in `actions.ts`. If REUSING discovered components from `/swarm/[swarmId]/(components)`, inherit their auth posture. |
| T-81-03-04 | Information Disclosure | Cross-swarm chip strip rendering wrong registry | mitigate | `loadSwarmNoiseCategories` is parameterised by `swarmType`; chip strip receives the per-swarm result; goal-backward check #10 (cross-swarm test in page-shell.test.tsx) covers this. |
</threat_model>

<verification>
- Goal-backward check #1 (CONTEXT.md): `/stage-1` renders shell + StageTabStrip(currentStage=1), no "Bulk Review" h1 → page-shell.test.tsx (a)+(b).
- Check #2: chip-strip renders one chip per noise registry row + "All" + count badges → noise-category-chip-strip.test.tsx (a).
- Check #3: chip click writes `?topic=<key>` → noise-category-chip-strip.test.tsx (b)+(c).
- Check #4: `?sub=pending` renders candidate-rule list + detail pane → page-shell.test.tsx (e) + load-page-data.test.ts new cases.
- Check #10: cross-swarm registry-driven render → page-shell.test.tsx (f).
- D-19 (realtime channel): `${swarmType}-review` literal preserved in stage-1/page.tsx (acceptance criteria grep).
</verification>

<success_criteria>
- `/automations/debtor-email/stage-1` renders the new chrome (PageHeader + StageTabStrip + chip strip + 2-col grid).
- `/automations/debtor-email/stage-1?topic=payment` filters to payment-noise rows (existing loader behaviour, now reachable via chip click).
- `/automations/debtor-email/stage-1?sub=pending` renders CandidateRuleList + PendingPromotionDetailPane.
- "Bulk Review" string absent from `stage-1/page.tsx` (sibling files cleaned in Plan 04).
- All new + extended tests pass; `tsc --noEmit` clean for the touched files.
</success_criteria>

<output>
Create `.planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-03-SUMMARY.md` with:
- New components inventory (LOC, exports, where mounted)
- Outcome of Open Q4 grep (reused existing rule-promotion components yes/no; if no, what server actions were plumbed)
- Manual smoke results (chip click → URL change; Pending pill → sub-view swap)
- Confirmation that QueueTree FILE still exists (deferred to Plan 04) but the import + JSX usage are gone from page.tsx
</output>
