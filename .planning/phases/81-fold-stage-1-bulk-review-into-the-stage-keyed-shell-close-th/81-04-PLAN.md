---
phase: 81
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - web/app/(dashboard)/automations/[swarm]/stage-1/queue-tree.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/row-list.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/detail-pane.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/recipient-chip-strip.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/keyboard-shortcuts.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/categories.ts
  - web/app/(dashboard)/automations/[swarm]/stage-1/actions.ts
  - web/app/(dashboard)/automations/[swarm]/stage-1/race-cohort-banner.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/row-strip.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/components/
  - web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/safety-review-loader.test.ts
  - web/__tests__/middleware-review-redirect.test.ts
autonomous: true
requirements: []
decisions_addressed: [D-02, D-18]

must_haves:
  truths:
    - "QueueTree component file is deleted; no non-test file references QueueTree."
    - "User-visible 'Bulk Review' strings are purged across stage-1/ files (operator UI copy, button labels, headings, aria-labels)."
    - "JSDoc / file-header comments referencing the historical 'Bulk Review' name are intentionally PRESERVED (audit trail per D-18)."
    - "Window event names like 'bulk-review:*' are PRESERVED (D-19 spirit — backend identifiers, not user-visible)."
    - "Middleware redirect tests cover ?sub=pending round-trip (regression) and the existing ?tab=pending → ?sub=pending rewrite still passes."
    - "Stage 3 + Stage 4 surfaces render unchanged (regression smoke green)."
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/queue-tree.tsx"
      provides: "DELETED — file no longer exists"
  key_links:
    - from: "stage-1/__tests__/safety-review-loader.test.ts"
      to: "mock fixture with .schema(name) accessor"
      via: "method on the chainable MockBuilder"
      pattern: "schema\\(name"
---

<objective>
Final-pass cleanup. Plan 03 already removed QueueTree's import + JSX usage from
`stage-1/page.tsx`; this plan deletes the orphaned `queue-tree.tsx` file (D-02). It also
purges leftover user-visible "Bulk Review" strings in the moved sibling files (D-18 —
JSDoc / window-event-names stay; only operator-UI copy is targeted), extends the
middleware redirect tests to cover the `?sub=pending` round-trip, runs a regression
smoke against `/stage-3` + `/stage-4`, and time-boxes a fix for the inherited
`safety-review-loader.test.ts` mock-fixture failures (RESEARCH §Pitfall 4 — 30-min cap
per CONTEXT discretion).

Purpose: Close the loop on Phase 81's hygiene asks. After this plan, `git grep
"QueueTree"` returns zero non-test hits, "Bulk Review" is absent from the operator
surface, and the legacy URL contract is provably preserved.

Output: Smaller codebase, cleaner operator copy, broader redirect-test coverage, and
either a fixed safety-review-loader fixture OR a documented carry-forward of the 22
inherited failures with explicit Phase-82 ownership.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-CONTEXT.md
@.planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-RESEARCH.md
@.planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-PATTERNS.md
@.planning/phases/76-stage-3-kanban-human-lane-wiring/deferred-items.md
@web/__tests__/middleware-review-redirect.test.ts
@web/middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete queue-tree.tsx + purge "Bulk Review" from user-visible copy across stage-1/</name>
  <files>
    web/app/(dashboard)/automations/[swarm]/stage-1/queue-tree.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/row-list.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/detail-pane.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/recipient-chip-strip.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/keyboard-shortcuts.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/categories.ts,
    web/app/(dashboard)/automations/[swarm]/stage-1/actions.ts,
    web/app/(dashboard)/automations/[swarm]/stage-1/race-cohort-banner.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/row-strip.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/components/
  </files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/queue-tree.tsx (FULL — to confirm no other file imports it before deletion)
    - .planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-CONTEXT.md (D-18 + D-19: JSDoc and window event names stay; only operator UI copy is purged)
  </read_first>
  <action>
    1. Confirm zero non-test imports of `QueueTree` remain (Plan 03 should already have removed them):
       ```bash
       cd web && rg -n "QueueTree\|queue-tree" -- '*.ts' '*.tsx' | grep -v "__tests__" | grep -v "queue-tree.tsx:"
       ```
       Expected output: empty (no hits) — meaning the only remaining references are inside `queue-tree.tsx` itself or in test files. If there are non-test hits outside queue-tree.tsx, STOP and surface back to Plan 03 — do not delete the file until orphaned.
    2. Delete the file:
       ```bash
       cd web && git rm app/\(dashboard\)/automations/\[swarm\]/stage-1/queue-tree.tsx
       ```
    3. Verify the deletion is clean:
       ```bash
       cd web && rg -n "QueueTree" app/ tests/ | grep -v "__tests__" || echo "clean"
       ```
       Expected: `clean` — only test files (if any) reference QueueTree, and that's allowed per D-02 + the goal-backward check #8.
    4. Purge "Bulk Review" from user-visible copy across the moved files. The classes of strings to target:
       - JSX text content: `>Bulk Review<`, `>{... Bulk Review ...}<`
       - Button / heading labels: `<button>Bulk Review</button>`, `<h2>Bulk Review</h2>`
       - `aria-label="Bulk Review ..."`, `title="Bulk Review ..."`
       - Toast / notification strings rendered to operators
       Strings to LEAVE INTACT (D-18 + D-19 spirit):
       - JSDoc / `/* ... */` and `//` comments referring to "Bulk Review" historically
       - `bulk-review:*` window event names (e.g. `window.dispatchEvent(new CustomEvent("bulk-review:select"))`) — backend identifier; renaming would break consumers and is out of scope
       - String constants used as event names or DOM identifiers (e.g. `const BULK_REVIEW_EVENT = "bulk-review:..."`)
       - Test fixture strings that exist purely to assert old behaviour (rare; check carefully)
    5. Run a comprehensive grep to identify candidates:
       ```bash
       cd web && rg -n "Bulk Review" app/\(dashboard\)/automations/\[swarm\]/stage-1/
       ```
       For each hit, decide: is this rendered to an operator? If yes → replace with stage-keyed phrasing (e.g. "Stage 1", "Noise filter", or just remove if redundant). If it's a comment / event name → leave alone.
    6. Replacement guidance (use these specific replacements where applicable):
       - `<h1>... Bulk Review</h1>` → already removed by Plan 03; verify no copies survive in sibling files.
       - `aria-label="Bulk Review queue"` → `aria-label="Stage 1 noise filter queue"`
       - Button `Open Bulk Review` → `Open Stage 1`
       - Toast `Approved in Bulk Review` → `Approved` (drop the surface name)
    7. After edits, re-run the grep to verify only intentional (commented / event-name) hits remain:
       ```bash
       cd web && rg -n "Bulk Review" app/\(dashboard\)/automations/\[swarm\]/stage-1/
       ```
       Each remaining hit must be inside a `/* */`, `//`, JSDoc block, OR be the literal `bulk-review:` event-name prefix (string constant). Record the surviving hits in the SUMMARY.
  </action>
  <verify>
    <automated>
      bash -lc 'cd web && [ ! -f "app/(dashboard)/automations/[swarm]/stage-1/queue-tree.tsx" ] && rg -n "QueueTree" app/ tests/ | grep -v "__tests__" | grep -v -E "^\s*//|^\s*\*" && exit 1 || echo "QueueTree purged from non-test code"'
    </automated>
  </verify>
  <acceptance_criteria>
    - File `web/app/(dashboard)/automations/[swarm]/stage-1/queue-tree.tsx` does not exist.
    - `rg -n "QueueTree" web/app/ web/tests/ | grep -v __tests__ | grep -v -E "^\s*//|^\s*\*"` returns zero non-comment hits in non-test files (goal-backward check #8).
    - For every remaining `Bulk Review` hit under `stage-1/`, the line is a comment OR a `bulk-review:` event-name string constant. Listed exhaustively in SUMMARY.
    - No newly broken tests introduced by the copy-purge (run `cd web && npx vitest run app/\(dashboard\)/automations/\[swarm\]/stage-1/ --no-coverage`).
  </acceptance_criteria>
  <done>
    QueueTree gone, "Bulk Review" no longer surfaces to operators, JSDoc and event-name audit trail intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend middleware-review-redirect tests for ?sub=pending round-trip + run regression smoke on /stage-3 and /stage-4</name>
  <files>
    web/__tests__/middleware-review-redirect.test.ts
  </files>
  <read_first>
    - web/__tests__/middleware-review-redirect.test.ts (FULL — existing 11 cases; understand the test harness before extending)
    - web/middleware.ts (FULL — `resolveReviewRedirect`; verify the function already handles `?sub=pending` correctly — should NOT need editing per RESEARCH; if a new failing test exposes a gap, surface back as a blocker)
  </read_first>
  <action>
    1. Read the existing test harness and identify the canonical "redirect with query params preserved" test pattern.
    2. Add new cases (one `it(...)` per case) covering:
       - **`/review?sub=pending` → 308 → `/stage-1?sub=pending`** (sub param preserved through the redirect — direct external bookmark to the sub-view).
       - **`/review?tab=pending` → 308 → `/stage-1?sub=pending`** (legacy `?tab=pending` rewritten to `?sub=pending` per existing middleware logic — confirm still works).
       - **`/review?topic=payment&sub=pending` → 308 → `/stage-1?topic=payment&sub=pending`** (multi-param round-trip with the new sub state).
       - **`/review?selected=abc123` → 308 → `/stage-1?selected=abc123`** (regression — pre-existing param preservation still works; safety net since the implementation was untouched in Phase 81).
    3. Run the extended test file:
       ```bash
       cd web && npx vitest run __tests__/middleware-review-redirect.test.ts --no-coverage
       ```
       All cases (existing 11 + new 4) must pass.
    4. Run regression smoke on Stage 3 + Stage 4 unit tests (goal-backward check #9):
       ```bash
       cd web && npx vitest run app/\(dashboard\)/automations/\[swarm\]/stage-3 app/\(dashboard\)/automations/\[swarm\]/stage-4 app/\(dashboard\)/automations/\[swarm\]/_shell/__tests__/derive-stage-tabs.test.ts --no-coverage
       ```
       All cases must pass (no regressions from the new Stage 2 tab in the strip — `derive-stage-tabs.ts:40` was untouched and Stage 2's badge count comes from `counts={{ 2: ... }}` only when the consumer page passes it).
    5. If any of the redirect tests reveals that `resolveReviewRedirect` doesn't actually preserve `?sub=pending` cleanly (e.g. it strips the param, or it's mis-mapped), surface as a BLOCKER — do NOT modify `middleware.ts` in this plan. The contract per D-03 + RESEARCH is "no middleware changes". A blocker means returning to the planner for a follow-up.
  </action>
  <verify>
    <automated>
      bash -lc 'cd web && npx vitest run __tests__/middleware-review-redirect.test.ts app/\(dashboard\)/automations/\[swarm\]/stage-3 app/\(dashboard\)/automations/\[swarm\]/stage-4 app/\(dashboard\)/automations/\[swarm\]/_shell/__tests__/derive-stage-tabs.test.ts --no-coverage 2>&1 | tail -3 | grep -q "passed"'
    </automated>
  </verify>
  <acceptance_criteria>
    - `middleware-review-redirect.test.ts` has at least 15 passing cases (11 original + 4 new).
    - The 4 new cases cover: `?sub=pending` direct, `?tab=pending` legacy rewrite, multi-param `?topic=&sub=`, and `?selected=` regression.
    - All Stage 3 + Stage 4 + derive-stage-tabs unit tests pass unchanged.
    - `web/middleware.ts` is NOT modified by this task (verified via `git diff --stat web/middleware.ts` showing no changes).
  </acceptance_criteria>
  <done>
    Redirect contract is provably preserved across legacy + new URL shapes. Stage 3/4 unaffected.
  </done>
</task>

<task type="auto">
  <name>Task 3: Time-boxed (30 min) — add .schema(name) to safety-review-loader test mock fixture</name>
  <files>
    web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/safety-review-loader.test.ts
  </files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/safety-review-loader.test.ts (FULL — see the chainable MockBuilder; identify why `.schema()` is missing)
    - .planning/phases/76-stage-3-kanban-human-lane-wiring/deferred-items.md (FULL — context: 22 inherited failures from commit 5ad38e4 introducing `admin.schema("email_pipeline").from(...)` and `admin.schema("debtor").from(...)`)
    - .planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-RESEARCH.md (§Pitfall 4 — explicit 30-min time-box per CONTEXT §discretion)
  </read_first>
  <action>
    1. Open the test file and locate the mock fixture (search for `MockBuilder` / `mockAdmin` / similar). Identify exactly where `.schema(name)` would need to be inserted.
    2. Add a `.schema(name: string)` method to the chainable mock:
       - Simplest viable option: `schema(_name: string) { return this; }` — returns the same builder, ignoring the schema name. This unblocks the test paths that call `admin.schema("debtor").from("email_labels")` because they now chain through to `.from(...)`.
       - If the mock needs schema-keyed sub-builders (e.g. one mock for `debtor`, another for `email_pipeline`), implement a simple lookup: `schema(name) { return this._schemas?.[name] ?? this; }`.
       - Run the failing tests to see which form is needed; start with the trivial `return this` and only escalate if tests demand it.
    3. Run the test file:
       ```bash
       cd web && npx vitest run app/\(dashboard\)/automations/\[swarm\]/stage-1/__tests__/safety-review-loader.test.ts --no-coverage
       ```
    4. **Time-box: 30 minutes from starting Step 1.** If at the 30-min mark the failures are not down to zero (or a small number with a clear cause), STOP. Revert the `schema()` addition and document in the SUMMARY:
       - The 22 (or current count) failures are carried forward to Phase 82.
       - The blocker beyond the trivial `.schema()` accessor (e.g. mock needs schema-keyed sub-builders, or production code shape changed in a way the test mock can't track).
       - Mark `.planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/deferred-items.md` with the carry-forward note.
    5. If failures DO reach zero (or close enough), record the count delta in the SUMMARY (e.g. "fixed 22 inherited failures with .schema() addition") and commit.
  </action>
  <verify>
    <automated>
      bash -lc 'cd web && npx vitest run app/\(dashboard\)/automations/\[swarm\]/stage-1/__tests__/safety-review-loader.test.ts --no-coverage 2>&1 | tail -5 | grep -E "(Test Files|Tests)" || echo "test run completed"'
    </automated>
  </verify>
  <acceptance_criteria>
    - EITHER: `safety-review-loader.test.ts` passes (or reduces from 22 failures to a small delta, recorded in SUMMARY) AND the `.schema(name)` accessor is committed.
    - OR: the time-box was hit, the change was reverted, and a `deferred-items.md` carry-forward note is committed describing the deeper blocker.
    - The SUMMARY explicitly records which path was taken (fixed / time-boxed / partial).
    - No OTHER tests break as a side-effect (run `cd web && npx vitest run --no-coverage` once more if `.schema()` was added).
  </acceptance_criteria>
  <done>
    Either the inherited test failures are closed out, or they're explicitly carried forward with a documented cause. No silent regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Edge middleware | Untrusted `/review[?...]` URLs reach `resolveReviewRedirect` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-04-01 | Tampering | Open redirect via legacy `/review?next=...` style | accept | `resolveReviewRedirect` constructs target from regex-captured `swarm` + closed-enum mapping; already mitigated in Phase 76 (T-76-08-01). New `?sub=pending` test cases extend the regression net. |
| T-81-04-02 | Tampering | Test-only changes (mock fixture `.schema()`) | accept | Test infrastructure only; no production runtime path. |
</threat_model>

<verification>
- Goal-backward check #5: legacy redirect with query params preserved → Task 2 extended tests.
- Goal-backward check #8: no QueueTree references in non-test files → Task 1 acceptance criteria.
- Goal-backward check #9: Stage 3/4 unchanged → Task 2 regression smoke.
- D-18: "Bulk Review" purged from user-visible copy, JSDoc + event-name strings preserved → Task 1 grep + manual review of survivors.
</verification>

<success_criteria>
- `queue-tree.tsx` deleted; no non-test imports of `QueueTree` anywhere.
- "Bulk Review" survives only in comments / event-name strings (each survivor explicitly listed in SUMMARY).
- Middleware redirect tests cover `?sub=pending` round-trip with at least 4 new cases passing.
- Stage 3 + Stage 4 + derive-stage-tabs tests green.
- `safety-review-loader.test.ts` either fully fixed or explicitly carried forward with cause.
- Phase 81 ready for `/gsd-verify-work 81`.
</success_criteria>

<output>
Create `.planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-04-SUMMARY.md` with:
- Confirmation of `queue-tree.tsx` deletion (file count delta, git rename history)
- Exhaustive list of remaining "Bulk Review" hits under `stage-1/` with classification (comment / event-name)
- Number of NEW middleware redirect test cases added + pass count
- Stage 3/4 regression result (pass/fail count)
- Outcome of Task 3 (time-boxed): fixed / partial / carried-forward, with cause if not fully fixed
</output>
