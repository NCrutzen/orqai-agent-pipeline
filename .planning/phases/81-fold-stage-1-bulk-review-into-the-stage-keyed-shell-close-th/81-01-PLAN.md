---
phase: 81
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/actions.ts
  - web/app/(dashboard)/automations/[swarm]/stage-1/categories.ts
  - web/app/(dashboard)/automations/[swarm]/stage-1/detail-pane.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/keyboard-shortcuts.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/queue-tree.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/race-cohort-banner.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/recipient-chip-strip.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/row-list.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/row-strip.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/selection-context.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/components/
  - web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/
  - web/tests/queue/fetch-review-email-body.test.ts
  - web/tests/queue/detail-pane.test.tsx
  - web/tests/queue/race-cohort.test.tsx
  - web/tests/queue/keyboard-shortcuts.test.tsx
  - web/tests/queue/actions.test.ts
autonomous: true
requirements: []
decisions_addressed: [D-01, D-02, D-03, D-04, D-19]

must_haves:
  truths:
    - "The directory web/app/(dashboard)/automations/[swarm]/review/ no longer exists."
    - "Every file that previously imported from .../[swarm]/review/... now imports from .../[swarm]/stage-1/... (or has been updated to the new path)."
    - "The /stage-1 route still resolves and renders the legacy Bulk Review surface end-to-end (no behaviour change in this wave — Plan 02 reframes the page)."
    - "The middleware /review → /stage-1 308 redirect with query params preserved still passes existing tests."
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx"
      provides: "Inlined RSC route page (no longer a re-export shim of ../review/page)"
      contains: "loadPageData"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/queue-tree.tsx"
      provides: "QueueTree (kept this wave; deleted in Plan 04)"
  key_links:
    - from: "web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx"
      to: "./row-list, ./detail-pane, ./queue-tree, ./recipient-chip-strip, ./selection-context, ./keyboard-shortcuts, ./actions, ./categories, ./race-cohort-banner"
      via: "relative imports (no ../review/ paths anywhere)"
      pattern: "^import .* from \"\\./(row-list|detail-pane|queue-tree|recipient-chip-strip|selection-context|keyboard-shortcuts|actions|categories|race-cohort-banner)"
    - from: "web/tests/queue/*.test.{ts,tsx}"
      to: "@/app/(dashboard)/automations/[swarm]/stage-1/..."
      via: "rewritten alias imports"
      pattern: "automations/\\[swarm\\]/stage-1"
---

<objective>
Mechanically move the entire `review/` directory tree into `stage-1/` (D-01), inline the
existing `loadPageData` loader into the new `stage-1/page.tsx` (replacing the 22-line
re-export shim), rewrite all 7 external importers and 2 self-aliased test imports to
the new path, and delete the (now empty) `review/` directory. ZERO behaviour change in
this wave — the goal is a clean, compiling, green-tests checkpoint that future waves can
build on. Plan 02 is what reframes the page as a shell-wrapped surface; Plan 04 is what
deletes `QueueTree`. This plan does NEITHER of those.

Purpose: Establish the new directory shape (D-01) and remove the re-export shim (D-04)
without coupling the move to behavioural changes. If something breaks, it's a path
issue, not a logic issue.

Output: `/stage-1/` directory holds every file that lived under `/review/`, the legacy
re-export shim is gone, all external imports compile, all moved tests pass, the legacy
URL `/review` still 308-redirects via middleware (D-03 — no middleware changes), and
the `${swarmType}-review` realtime channel name is preserved (D-19).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-CONTEXT.md
@.planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-RESEARCH.md
@.planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-PATTERNS.md
@web/app/(dashboard)/automations/[swarm]/review/page.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx
@web/middleware.ts
@web/__tests__/middleware-review-redirect.test.ts

<interfaces>
<!-- Current external importers of `review/` (verified — RESEARCH §Pitfall 1). -->
<!-- All 7 must be rewritten in this plan; the Wave 2/3 plans must NOT have to fix these. -->

External importers (alias paths to rewrite):
- web/tests/queue/fetch-review-email-body.test.ts:66
    `from "@/app/(dashboard)/automations/[swarm]/review/actions"`
    → `from "@/app/(dashboard)/automations/[swarm]/stage-1/actions"`
- web/tests/queue/detail-pane.test.tsx:47-49
    `.../review/detail-pane`, `.../review/selection-context`, `.../review/page`
    → replace `review` with `stage-1`
- web/tests/queue/race-cohort.test.tsx:7
    `.../review/race-cohort-banner` → `.../stage-1/race-cohort-banner`
- web/tests/queue/keyboard-shortcuts.test.tsx:12-13
    `.../review/keyboard-shortcuts`, `.../review/selection-context` → `.../stage-1/...`
- web/tests/queue/actions.test.ts:99
    `.../review/actions` → `.../stage-1/actions`

Self-aliased imports inside moved tests (must be rewritten in same step):
- review/__tests__/load-page-data.test.ts:234 — uses `@/app/(dashboard)/automations/[swarm]/review/...`
    → rewrite to `@/app/(dashboard)/automations/[swarm]/stage-1/...` (or relative `../`)
- review/__tests__/safety-review-loader.test.ts:174 — same pattern

Files to MOVE verbatim (no edits this plan):
  actions.ts, categories.ts, detail-pane.tsx, keyboard-shortcuts.tsx,
  queue-tree.tsx, race-cohort-banner.tsx, recipient-chip-strip.tsx,
  row-list.tsx, row-strip.tsx, selection-context.tsx,
  components/, __tests__/

File that needs INLINING (not a verbatim move):
  review/page.tsx → stage-1/page.tsx
  The current stage-1/page.tsx is a 22-line re-export shim; replace it with the FULL
  contents of review/page.tsx (845 lines), only adjusting relative imports to local
  ones (e.g. `./row-list` instead of `./row-list` — already relative — but verify ALL
  imports). The `export const dynamic = "force-dynamic"` already exists in the shim;
  the inlined version should retain it (review/page.tsx also declares it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move review/ → stage-1/ verbatim and inline page.tsx</name>
  <files>
    web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/actions.ts,
    web/app/(dashboard)/automations/[swarm]/stage-1/categories.ts,
    web/app/(dashboard)/automations/[swarm]/stage-1/detail-pane.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/keyboard-shortcuts.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/queue-tree.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/race-cohort-banner.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/recipient-chip-strip.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/row-list.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/row-strip.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/selection-context.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/components/,
    web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/
  </files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/review/page.tsx (FULL — 845 lines; this becomes the body of the new stage-1/page.tsx)
    - web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx (current 22-line shim; will be REPLACED, not appended)
    - .planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-PATTERNS.md (§"Mechanical moves")
    - .planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-RESEARCH.md (§"Pitfall 1: External imports targeting review/")
  </read_first>
  <action>
    1. From `web/`, run the moves with `git mv` so history is preserved:
       ```bash
       cd web/app/\(dashboard\)/automations/\[swarm\]
       # First: delete the existing 22-line shim at stage-1/page.tsx (will be replaced).
       rm stage-1/page.tsx
       # Move every file/dir from review/ into stage-1/ verbatim.
       for f in actions.ts categories.ts detail-pane.tsx keyboard-shortcuts.tsx \
                queue-tree.tsx race-cohort-banner.tsx recipient-chip-strip.tsx \
                row-list.tsx row-strip.tsx selection-context.tsx components __tests__; do
         git mv "review/$f" "stage-1/$f"
       done
       # Move the page.tsx as well — it becomes the new full-fat page.
       git mv review/page.tsx stage-1/page.tsx
       # The review/ directory should now be empty:
       rmdir review
       ```
    2. Open the freshly moved `stage-1/page.tsx` and verify the relative imports — all
       imports inside `review/page.tsx` already used `./row-list`, `./detail-pane`, etc.
       (relative), so they keep working unchanged. Confirm with grep.
    3. Verify `export const dynamic = "force-dynamic"` is still present at the top of
       `stage-1/page.tsx` (it was already in `review/page.tsx`).
    4. Verify the `<AutomationRealtimeProvider automations={[\`${swarmType}-review\`]}>`
       channel name is unchanged (D-19).
    5. DO NOT touch QueueTree, the h1 "Bulk Review" string, the intro paragraph, the
       3-col grid layout, or `?tab=pending` branching — those are Plan 02 / Plan 04's
       jobs. This task is a pure move + inline.
  </action>
  <verify>
    <automated>
      bash -lc 'cd web && [ ! -d "app/(dashboard)/automations/[swarm]/review" ] &&
        [ -f "app/(dashboard)/automations/[swarm]/stage-1/page.tsx" ] &&
        [ -f "app/(dashboard)/automations/[swarm]/stage-1/queue-tree.tsx" ] &&
        [ -d "app/(dashboard)/automations/[swarm]/stage-1/__tests__" ] &&
        ! grep -q "^export { default } from \"\\.\\./review/page\"" "app/(dashboard)/automations/[swarm]/stage-1/page.tsx" &&
        grep -q "^export const dynamic = \"force-dynamic\"" "app/(dashboard)/automations/[swarm]/stage-1/page.tsx" &&
        grep -q "${swarmType}-review" "app/(dashboard)/automations/[swarm]/stage-1/page.tsx"'
    </automated>
  </verify>
  <acceptance_criteria>
    - `web/app/(dashboard)/automations/[swarm]/review/` directory does not exist (`test ! -d` passes).
    - `web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx` exists and is NOT the 22-line shim (no `export { default } from "../review/page"` line).
    - `stage-1/page.tsx` contains `export const dynamic = "force-dynamic"` and `${swarmType}-review` (channel name preserved per D-19).
    - All 11 listed sibling files (`actions.ts`, `categories.ts`, `detail-pane.tsx`, `keyboard-shortcuts.tsx`, `queue-tree.tsx`, `race-cohort-banner.tsx`, `recipient-chip-strip.tsx`, `row-list.tsx`, `row-strip.tsx`, `selection-context.tsx`, plus `components/` dir and `__tests__/` dir) exist under `stage-1/`.
    - `git status` shows the moves as renames, not delete+add (history preserved).
  </acceptance_criteria>
  <done>
    review/ is gone, stage-1/ is the full directory, page.tsx is no longer a shim, no
    behaviour change yet, no compile yet (Task 2 fixes external importers).
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite all 7 external importers + 2 self-aliased test imports from review/ → stage-1/</name>
  <files>
    web/tests/queue/fetch-review-email-body.test.ts,
    web/tests/queue/detail-pane.test.tsx,
    web/tests/queue/race-cohort.test.tsx,
    web/tests/queue/keyboard-shortcuts.test.tsx,
    web/tests/queue/actions.test.ts,
    web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/load-page-data.test.ts,
    web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/safety-review-loader.test.ts
  </files>
  <read_first>
    - .planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-RESEARCH.md (§"Pitfall 1" — full inventory of importers, line numbers)
    - web/tests/queue/fetch-review-email-body.test.ts (line 66)
    - web/tests/queue/detail-pane.test.tsx (lines 47-49)
    - web/tests/queue/race-cohort.test.tsx (line 7)
    - web/tests/queue/keyboard-shortcuts.test.tsx (lines 12-13)
    - web/tests/queue/actions.test.ts (line 99)
    - web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/load-page-data.test.ts (line 234 — self-alias)
    - web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/safety-review-loader.test.ts (line 174 — self-alias)
  </read_first>
  <action>
    1. Find every remaining occurrence of the old path:
       ```bash
       cd web && rg -n "automations/\[swarm\]/review" -- '*.ts' '*.tsx'
       ```
       Expected hits BEFORE this task: 7 importers + 2 self-aliased tests + maybe
       1-2 comments in `middleware.ts` / `web/__tests__/middleware-review-redirect.test.ts`.
       Comment-only hits are LEFT as-is (per RESEARCH §Pitfall 1: middleware.ts:5 and
       middleware-review-redirect.test.ts are comment references and don't affect
       routing — keep them; they document the legacy URL contract).
    2. Rewrite each code import (NOT comments) by replacing the substring
       `automations/[swarm]/review` with `automations/[swarm]/stage-1`. Specifically:
       - `web/tests/queue/fetch-review-email-body.test.ts:66` —
         `"@/app/(dashboard)/automations/[swarm]/review/actions"` → `"@/app/(dashboard)/automations/[swarm]/stage-1/actions"`
       - `web/tests/queue/detail-pane.test.tsx:47-49` — three imports: `detail-pane`, `selection-context`, `page` — replace `review` with `stage-1` in each.
       - `web/tests/queue/race-cohort.test.tsx:7` — `race-cohort-banner` import.
       - `web/tests/queue/keyboard-shortcuts.test.tsx:12-13` — `keyboard-shortcuts` + `selection-context`.
       - `web/tests/queue/actions.test.ts:99` — `actions` import.
       - `stage-1/__tests__/load-page-data.test.ts:234` — self-alias `@/app/(dashboard)/automations/[swarm]/review/...` → `@/app/(dashboard)/automations/[swarm]/stage-1/...`
       - `stage-1/__tests__/safety-review-loader.test.ts:174` — same self-alias rewrite.
    3. DO NOT add re-export shims in the deleted directory (D-04 explicit: no backwards-compat re-export).
    4. Run the loader/test grep guard again to confirm only intentional comment hits remain:
       ```bash
       rg -n "automations/\[swarm\]/review" -- '*.ts' '*.tsx'
       ```
       The only acceptable hits are inside `web/middleware.ts` (comment) and
       `web/__tests__/middleware-review-redirect.test.ts` (comment / string literal
       describing the legacy URL — those are CORRECT and should stay).
  </action>
  <verify>
    <automated>
      bash -lc 'cd web && rg -n "from [\"'\''].*automations/\[swarm\]/review" -- "*.ts" "*.tsx" | grep -v "^web/middleware.ts:" | grep -v "^web/__tests__/middleware-review-redirect.test.ts:" || echo "CLEAN — no code imports of review/ remain"'
    </automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "from [\"'\''].*automations/\[swarm\]/review" -- '*.ts' '*.tsx'` returns ZERO matches outside `web/middleware.ts` and `web/__tests__/middleware-review-redirect.test.ts`.
    - The 7 explicit external importer files (listed in `<files>`) each contain `automations/[swarm]/stage-1/` in the rewritten imports.
    - `cd web && npx vitest run tests/queue/ -- --no-coverage` passes (or fails with the SAME failures as on `main`, no NEW failures from path issues).
  </acceptance_criteria>
  <done>
    All code imports compile against the new `stage-1/` path. Comment-level mentions of
    "/review" survive intentionally in middleware (legacy redirect contract).
  </done>
</task>

<task type="auto">
  <name>Task 3: Smoke — middleware redirect + loader test compile against new paths</name>
  <files>(no edits; verification-only task)</files>
  <read_first>
    - web/__tests__/middleware-review-redirect.test.ts
    - web/middleware.ts (resolveReviewRedirect — already correct per commit 5a00de2; do NOT modify)
  </read_first>
  <action>
    1. Run the middleware redirect tests — they should pass unchanged (D-03; middleware
       was untouched):
       ```bash
       cd web && npx vitest run __tests__/middleware-review-redirect.test.ts --no-coverage
       ```
       Expected: all 11 cases green. If any fail, investigate — Plan 04 owns extending
       these tests for new cases, but THIS plan must not regress them.
    2. Run the moved loader tests to confirm they at least compile against the new path
       (some may still have the inherited 22 `.schema()` failures from Phase 71-08 —
       those are pre-existing and explicitly allowed to carry forward per CONTEXT
       §Claude's Discretion + RESEARCH §Pitfall 4):
       ```bash
       cd web && npx vitest run app/\(dashboard\)/automations/\[swarm\]/stage-1/__tests__/ --no-coverage
       ```
       Expected: identical pass/fail set to what `review/__tests__/` had on `main`
       (22 known-failing in `safety-review-loader.test.ts`, rest green). If a NEW
       failure appears, it's a path-rewrite bug from Task 2 — fix before declaring
       this plan done.
    3. Verify a TypeScript build over the entire `[swarm]/` route subtree (broader
       than just review/stage-1 paths — catches any compile error inlining the
       845-line page introduced anywhere under the route group). The repo as a whole
       does NOT currently compile cleanly (4-5 unrelated errors in
       `scripts/__tests__/...`); a full `tsc --noEmit` exit-0 gate is therefore not
       viable, so we scope the gate to the route subtree this plan touches:
       ```bash
       cd web && npx tsc --noEmit 2>&1 | grep -E "app/\(dashboard\)/automations/\[swarm\]/" | tee /tmp/81-01-tsc.log; test ! -s /tmp/81-01-tsc.log
       ```
       Expected: zero lines in `/tmp/81-01-tsc.log` (file empty → `test ! -s` exits 0).
       If lines appear, they are compile errors introduced by the inlining /
       path-rewrite — fix before declaring this plan done.
  </action>
  <verify>
    <automated>
      bash -lc 'cd web && npx vitest run __tests__/middleware-review-redirect.test.ts --no-coverage 2>&1 | tail -5 | grep -q "11 passed" && npx tsc --noEmit 2>&1 | grep -E "app/\(dashboard\)/automations/\[swarm\]/" | tee /tmp/81-01-tsc.log; test ! -s /tmp/81-01-tsc.log'
    </automated>
  </verify>
  <acceptance_criteria>
    - All 11 cases in `middleware-review-redirect.test.ts` pass.
    - Moved tests under `stage-1/__tests__/` show the same pass/fail count as the
      pre-move baseline (no NEW failures introduced by the rename).
    - `npx tsc --noEmit` produces zero errors anywhere under `web/app/(dashboard)/automations/[swarm]/` (broader scope catches inlining-induced errors outside review/stage-1 paths).
  </acceptance_criteria>
  <done>
    Wave 1 checkpoint reached: directory renamed, all imports rewritten, middleware redirect intact, loader tests compile. Plan 02 (shell-wrap) and Plan 03 (Stage 2 placeholder) can now proceed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Edge middleware | Untrusted URL `/review[?...]` lands on `resolveReviewRedirect` |
| Browser → RSC route | Untrusted query params `?topic=`, `?selected=`, `?sub=`, `?tab=` reach `loadPageData` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-01-01 | Tampering | `resolveReviewRedirect` (legacy `/review` redirect) | accept | No change in this plan; already mitigated in Phase 76 (closed-enum `tab`, regex-captured `swarm`, T-76-08-01 test). Carrying forward as-is. |
| T-81-01-02 | Information Disclosure | `loadPageData` after directory rename | accept | No code change; only path rewrites. Existing Supabase-parameterised queries unchanged. |
| T-81-01-03 | Tampering | External-importer rewrites (test files) | accept | Test files only — not in production runtime path. |
</threat_model>

<verification>
- Goal-backward check #5 (CONTEXT.md): legacy `/review` 308-redirects with query params preserved. → covered by Task 3 middleware test re-run (no middleware edits).
- Goal-backward check #7: `web/app/(dashboard)/automations/[swarm]/review/` directory does not exist; no imports target `../review/...`. → covered by Task 1 + Task 2 acceptance criteria.
- Cross-cutting: D-19 realtime channel name `${swarmType}-review` preserved verbatim in moved page.tsx.
</verification>

<success_criteria>
- `review/` directory absent.
- `stage-1/page.tsx` is the inlined 845-line page (NOT a 22-line re-export shim).
- All 7 external importers + 2 self-aliased tests rewritten to `stage-1/`.
- Middleware redirect tests still pass (11/11).
- Moved loader tests show same pass/fail set as pre-move baseline.
- `git log --follow` on any moved file shows continuous history through the rename.
</success_criteria>

<output>
Create `.planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-01-SUMMARY.md` with:
- Files moved (count, with rename history confirmation)
- External importers rewritten (the 7 files + 2 self-alias rewrites)
- Comment-level `/review` references intentionally retained (middleware.ts, middleware-review-redirect.test.ts)
- Pre-existing `safety-review-loader.test.ts` `.schema()` failures explicitly carried forward (count) — Plan 04 may revisit
</output>
