---
milestone: v8.1
name: Validation + Visibility
audited: 2026-05-30
operator_uat_closed: 2026-06-01
status: passed  # was tech_debt — all 4 operator-UAT items dispositioned 2026-06-01 (see "Operator UAT closure" below); residual items are forward-looking deferrals tracked to v8.2/backlog
scores:
  phases: 8/8 code-complete
  requirements: SC-based (no REQUIREMENTS.md traceability table for v8.1)
  integration: live — funnel processing real traffic E2E (verified 2026-05-30)
  flows: Stage 0→1→2→3 confirmed live; Stage 1 noise + Stage 3 retro exercised today
gaps: []  # no critical blockers
tech_debt:
  - phase: 84-stage-1-noise-rules
    items:
      - "own_outbound_invoice_loopback rule DISABLED 2026-05-30 — keys on forwarder domain, MISCLASSIFIED ~40% actionable internal forwards as noise (Phase 87 finding). NOTE: not actually archived — shadow mode suppressed the Outlook action (verified via Graph; mail still in inboxes). Fix still needed before auto-archive graduates. Needs true-system-loopback matcher before re-enable."
      - "VERIFICATION status: human_needed — 7-day shadow window (Day-0 + D-05 gate per PROMOTION-RUNBOOK). Window has elapsed; operator confirmation outstanding."
  - phase: 85-stage-3-prompt-v3
    items:
      - "passed_with_open_operator_items — 3-day token observation + V2-retirement TODO (wall-clock window elapsed)."
  - phase: 86-open-set-discovery
    items:
      - "VERIFICATION status: human_needed — operator UAT (Day-0 checkpoint + first 04:00 cron tick). Cron confirmed live (101 clusters, refreshed daily); operator sign-off outstanding."
  - phase: 87-retro-baseline
    items:
      - "No formal VERIFICATION.md — 87-BASELINE-REPORT.md (hand-graded, SC-3/SC-4 PASS) + 87-05-SUMMARY.md serve as closure evidence."
      - "27-email loopback recovery = operator follow-up (87-LOOPBACK-RECOVERY.md)."
      - "SC-1 REFUTED usefully — catch-all driven by upstream noise, not Stage 3; next lever is Stage 1 (informs V8.2)."
  - phase: 88-review-surface-cleanup
    items:
      - "VERIFICATION status: human_needed — frontend operator UAT outstanding."
---

# v8.1 "Validation + Visibility" — Milestone Audit

**Audited:** 2026-05-30 · **Status:** `tech_debt` (no critical blockers; deferred operator-window items + one follow-up)

## Definition of done (from ROADMAP)

> Observe → understand → THEN automate. Fix the input + calibration + visibility layers so that Stage 3 intent distribution reflects reality, not artifacts of upstream parsing bugs or impoverished prompt context.

**Verdict: achieved.** The milestone delivered the input fix (Phase 83), calibration (Phase 84), Stage 3 prompt v3 + open-set schema (Phase 85), discovery surface (Phase 86), and the retro baseline (Phase 87) that measures Stage 3 reality. The baseline did exactly its job — it revealed that the catch-all bucket is driven by upstream noise reaching Stage 3, refuting the assumption that better context alone would shrink it. That is a successful "observe → understand" outcome that directly informs V8.2.

## Phase scorecard (in-scope: 83–88, 88.1, 88.2)

| Phase | Deliverable | Verification | Code |
|-------|-------------|--------------|------|
| 83 Body ingestion | full-thread capture + 30d backfill | shipped (V3/V4 partial pending live traffic) | ✅ |
| 84 Stage 1 noise rules | 7 noise categories + tenant_domains | human_needed (shadow window) — loopback rule since disabled | ✅ |
| 85 Stage 3 prompt v3 | V3 schema + tolerant parser + Orq prompt | passed_with_open_operator_items | ✅ |
| 86 Open-set discovery | proposals view + clusters cron + UI | human_needed (operator UAT) — cron live (101 clusters) | ✅ |
| 87 Retro baseline | retro fn + baseline report | BASELINE-REPORT (SC-3/SC-4 PASS) + 05-SUMMARY | ✅ |
| 88 Review-surface cleanup | 4-axis override + chip semantics | human_needed (frontend UAT) | ✅ |
| 88.1 Inngest stage-naming | 4 fn + 3 event renames | shipped (real-traffic verified) | ✅ |
| 88.2 Tier-2 CI cleanup | 194→0 lint, 47→0 tests | passed | ✅ |

## Cross-phase integration (inline assessment — checker agent not installed)

The 5-stage funnel is **demonstrably live end-to-end** as of 2026-05-30:
- Stage 0→1→2→3 events observed in `pipeline_events` (organic inbound classified at 19:41 UTC during this session).
- Stage 1 noise rules firing (67 loopback categorizations pre-disable; other noise categories intact).
- Stage 3 retro reconstructed inputs from persisted Stage 2/3 data and re-classified 105 emails with zero side-channel leakage — proves the persisted-state contract across phases 83/85/86/87 holds.
- Phase 86 cluster cron healthy (101 clusters, refreshed within 24h).
- Bulk Review surfaces (Phase 88) present; Inngest functions registered under stage-named ids (Phase 88.1).

No broken cross-phase wiring detected.

## Tech debt / deferred (see frontmatter)

Two items warrant tracking before they're forgotten:
1. **Loopback rule follow-up** — disabled today; needs a true-system-loopback matcher to safely re-enable (pairs with Phase 999.6). 27 actionable emails were misclassified as noise but NOT archived (shadow mode contained it — 25/27 still in inbox, 2 human-deleted); recovery is optional/low-priority.
2. **Operator-window confirmations** — 84/85/86/88 carry `human_needed`/open-operator items. These are wall-clock shadow/UAT windows that have now elapsed; they need operator sign-off but were explicitly marked KEEP-OPEN-PENDING (non-blocking for code closure) in the ROADMAP.

## Nyquist

VALIDATION.md scan: not separately audited here (v8.1 phases used SC-based verification + UAT rather than the Nyquist VALIDATION track). Not a blocker.

## Operator UAT closure (2026-06-01)

All four `human_needed`/open-operator items dispositioned in a single operator session (evidence-based, live data):

| Phase | Disposition | Evidence |
|-------|-------------|----------|
| **85** Stage 3 prompt v3 | ✅ **PASSED** — operator signed off | `85-OPERATOR-SIGNOFF.md`: Task 1 regression GO (1/12, the 1 a correction) · Task 2 cost PASS (~€2–7/mo, 0 errors) · Task 3 V2-retirement scheduled 2026-06-03. "Phase 85 closed 2026-06-01." |
| **88** Review-surface cleanup | ✅ **RESOLVED** — UAT performed + fixed | `88-UAT-FINDINGS.md`: live operator UAT 2026-05-20 found F-01/F-02/F-03, all fixed + migration `20260521_phase88_classifier_queue_verdict_pending` applied to prod + smoke-validated. |
| **86** Open-set discovery | ✅ **PASSED** — accepted on live cron evidence | `intent_proposal_clusters`=164 (63 refreshed/48h), `intent_proposals_v1`=24. Cron demonstrably healthy. 4-week observation-log + page-UAT deferred (`intent_proposal_views`=0, non-blocking — operator decision). |
| **84** Stage 1 noise rules | ✅ **PASSED (code) — D-05 gate evaluated, 0 promotions** | D-05 gate run against live data 2026-06-01: `classifier_rule_telemetry` N=0 for all 16 rules (no operator verdicts accumulated during shadow → Wilson path ineligible); the only corpus-path candidate `own_outbound_invoice_loopback` is DISABLED. **0/16 eligible** → all rules stay `candidate`, deferred per PROMOTION-RUNBOOK §7 valid-closure. |

**Cross-cutting finding (feeds v8.2):** Phase 84's promotion telemetry (`classifier_rule_telemetry` → `classifier_rules.n`) is **empty (N=0)** because the bulk-review verdict queue for Stage-1 noise predictions was never worked. The **same graduated-automation Wilson-CI gate is what v8.2's "auto-flip only after dry-run ≥99%" depends on** — so v8.2 must ensure operator verdicts actually accumulate (or the gate never fires). Tracked alongside the v8.2 dry-run gate.

**Residual forward-looking deferrals (tracked, non-blocking):** loopback true-system matcher (pairs with backlog 999.6); the 15 held/extended Phase 84 noise rules (revisit in v8.2 when verdict volume accumulates).

## Recommendation

**Operator items closed.** The milestone met its definition of done and all four operator-UAT confirmations are now dispositioned (2026-06-01). Residual items are explicitly forward-looking deferrals tracked to v8.2/backlog — not blockers. **Ready to `/gsd-complete-milestone v8.1`.**
