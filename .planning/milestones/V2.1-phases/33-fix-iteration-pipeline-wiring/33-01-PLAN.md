---
phase: 33-fix-iteration-pipeline-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/agents/prompt-editor.md
  - orq-agent/commands/iterate.md
autonomous: true
gap_closure: true
requirements:
  - ITPIPE-05
  - LOOP-01

must_haves:
  truths:
    - "prompt-editor Phase 1.2 reads holdout_dataset_id from dataset-prep.json using agents.{agent_key}.holdout_dataset_id flat-map path"
    - "iterate.md Step 5.4 forwards mcp_available to prompt-editor context for deployer delegation"
    - "JSON handoff contract between dataset-prep.json producer and prompt-editor consumer is aligned"
  artifacts:
    - path: "orq-agent/agents/prompt-editor.md"
      provides: "Fixed holdout dataset ID schema path in Phase 1.2"
      contains: "agents.{agent_key}.holdout_dataset_id"
    - path: "orq-agent/commands/iterate.md"
      provides: "mcp_available forwarded in Step 5.4 prompt-editor invocation"
      contains: "mcp_available"
  key_links:
    - from: "orq-agent/agents/prompt-editor.md Phase 1.2"
      to: "dataset-prep.json"
      via: "agents.{agent_key}.holdout_dataset_id flat-map lookup"
      pattern: "agents\\.\\{agent_key\\}\\.holdout_dataset_id"
    - from: "orq-agent/commands/iterate.md Step 5.4"
      to: "orq-agent/agents/prompt-editor.md Phase 3"
      via: "mcp_available context forwarding"
      pattern: "mcp_available.*from Step 2"
---

<objective>
Fix two integration breaks identified by V2.1 milestone audit: (1) prompt-editor Phase 1.2 uses wrong schema path for holdout dataset IDs, making the holdout re-test path unreachable, and (2) iterate.md Step 5.4 does not forward mcp_available to prompt-editor, causing unnecessary MCP attempts in MCP-unavailable environments.

Purpose: Close the last 2 of 24 V2.1 requirements (ITPIPE-05, LOOP-01) so the full iteration pipeline works end-to-end.
Output: Two corrected markdown agent instruction files with aligned JSON handoff contracts.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/33-fix-iteration-pipeline-wiring/33-RESEARCH.md
@.planning/V2.1-MILESTONE-AUDIT.md

@orq-agent/agents/prompt-editor.md
@orq-agent/commands/iterate.md
@orq-agent/agents/dataset-preparer.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix holdout dataset ID schema path in prompt-editor Phase 1.2</name>
  <files>orq-agent/agents/prompt-editor.md</files>
  <action>
In `orq-agent/agents/prompt-editor.md`, locate Step 1.2 (line ~32). Replace the broken text:

```
Read `{swarm_dir}/dataset-prep.json`. For each approved agent, extract `holdout_dataset_id` from the matching entry in `per_agent_datasets[]` (where `agent_key` matches).
```

With the corrected text:

```
Read `{swarm_dir}/dataset-prep.json`. For each approved agent, extract `holdout_dataset_id` from `agents.{agent_key}.holdout_dataset_id`.
```

**Why:** dataset-prep.json uses a flat map under `agents.{agent_key}`, not a `per_agent_datasets[]` array. The array structure exists only in test-results.json. The current path always fails, triggering the STOP guard and making holdout re-test unreachable. Do NOT switch the source file to test-results.json -- dataset-prep.json is the canonical dataset source and prompt-editor updates test-results.json later (circular dependency risk).

**Verify alignment:** After the fix, confirm the access pattern matches dataset-preparer.md Phase 8 output schema: `agents.{agent_key}.holdout_dataset_id`.
  </action>
  <verify>
    <automated>grep -c "agents\.{agent_key}\.holdout_dataset_id" orq-agent/agents/prompt-editor.md | grep -q "1" && grep -c "per_agent_datasets" orq-agent/agents/prompt-editor.md | grep -q "0" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>prompt-editor Phase 1.2 reads holdout_dataset_id using `agents.{agent_key}.holdout_dataset_id` path. Zero references to `per_agent_datasets[]` remain in prompt-editor.md.</done>
</task>

<task type="auto">
  <name>Task 2: Forward mcp_available in iterate.md Step 5.4</name>
  <files>orq-agent/commands/iterate.md</files>
  <action>
In `orq-agent/commands/iterate.md`, locate Step 5.4 (lines ~307-310). The current invocation context list is:

```
Read subagent instructions from `orq-agent/agents/prompt-editor.md`. Invoke with:
- **swarm_dir** (from Step 3)
- **iteration_number** (current iteration count)
```

Add `mcp_available` as a third forwarded context variable:

```
Read subagent instructions from `orq-agent/agents/prompt-editor.md`. Invoke with:
- **swarm_dir** (from Step 3)
- **iteration_number** (current iteration count)
- **mcp_available** (from Step 2 -- forwarded to deployer via prompt-editor Phase 3)
```

**Why:** prompt-editor Phase 3 delegates to deployer.md, which needs `mcp_available` to choose MCP-first vs REST-only. Without it, deployer attempts MCP first in MCP-unavailable environments before falling back to REST (unnecessary latency). This follows the same pattern as test.md Step 5.1 which forwards mcp_available to dataset-preparer for the same reason.

Do NOT forward mcp_available to failure-diagnoser (Step 5.2) -- it makes no API calls.
  </action>
  <verify>
    <automated>grep -c "mcp_available" orq-agent/commands/iterate.md | grep -q "[1-9]" && grep -A5 "Step 5.4" orq-agent/commands/iterate.md | grep -q "mcp_available" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>iterate.md Step 5.4 includes mcp_available in the forwarded context list with a comment explaining the propagation chain (iterate -> prompt-editor -> deployer).</done>
</task>

</tasks>

<verification>
1. prompt-editor.md Phase 1.2 uses `agents.{agent_key}.holdout_dataset_id` (flat-map path matching dataset-prep.json schema)
2. prompt-editor.md Phase 1.2 has zero references to `per_agent_datasets[]`
3. iterate.md Step 5.4 includes `mcp_available` in forwarded context
4. iterate.md Step 5.2 (failure-diagnoser) does NOT include `mcp_available` (it makes no API calls)
5. Contract alignment: dataset-preparer Phase 8 writes `agents.{key}.holdout_dataset_id`, prompt-editor Phase 1.2 reads `agents.{key}.holdout_dataset_id` -- paths match
</verification>

<success_criteria>
- Holdout re-test path is reachable: prompt-editor can resolve holdout_dataset_id from dataset-prep.json without triggering STOP guard
- mcp_available propagates through full iterate chain: iterate.md -> prompt-editor -> deployer
- Both ITPIPE-05 and LOOP-01 requirements are fully satisfied (closing V2.1 milestone at 24/24)
</success_criteria>

<output>
After completion, create `.planning/phases/33-fix-iteration-pipeline-wiring/33-01-SUMMARY.md`
</output>
