---
phase: 30-failure-diagnoser
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [orq-agent/agents/failure-diagnoser.md]
autonomous: true
gap_closure: true
requirements: [ITPIPE-01, ITPIPE-02, ITPIPE-03]

must_haves:
  truths:
    - "failure-diagnoser.md Phase 2 Step 2.2 step 1 specifies an explicit path convention for resolving agent_key to a spec file path: {swarm_dir}/agents/{agent_key}.md"
    - "failure-diagnoser.md includes a Glob fallback instruction for locating the spec file if the conventional path does not exist"
  artifacts:
    - path: "orq-agent/agents/failure-diagnoser.md"
      provides: "Updated subagent with explicit spec file path resolution"
      contains: "swarm_dir}/agents/{agent_key}.md"
  key_links:
    - from: "orq-agent/agents/failure-diagnoser.md"
      to: "agent spec .md files (disk)"
      via: "Phase 2 Step 2.2 resolves agent_key to {swarm_dir}/agents/{agent_key}.md with Glob fallback"
      pattern: "agents/.*\\.md"
---

<objective>
Fix the spec file path resolution gap in failure-diagnoser.md Phase 2 Step 2.2. The current instruction says "Read the agent spec .md file" but never specifies how to resolve agent_key to a file path. Add explicit path convention and Glob fallback.

Purpose: Prevent executor LLMs from guessing or hallucinating spec file paths during diagnosis.
Output: Updated orq-agent/agents/failure-diagnoser.md with explicit path resolution.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/30-failure-diagnoser/30-VERIFICATION.md
@orq-agent/agents/failure-diagnoser.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit spec file path resolution to failure-diagnoser.md Phase 2 Step 2.2</name>
  <files>orq-agent/agents/failure-diagnoser.md</files>
  <action>
In `orq-agent/agents/failure-diagnoser.md`, locate Phase 2 Step 2.2, the numbered diagnosis steps. Step 1 currently reads:

```
1. **Read the agent spec `.md` file** to get the current instructions content
```

Replace it with:

```
1. **Read the agent spec `.md` file** at `{swarm_dir}/agents/{agent_key}.md`. If the file does not exist at that path, use Glob to find `{agent_key}.md` within the swarm directory. Read the file to get the current instructions content.
```

This adds:
- Explicit path convention (`{swarm_dir}/agents/{agent_key}.md`) matching the project's standard agent directory layout
- Glob fallback for cases where the spec file is in a non-standard location
- No other changes to the file
  </action>
  <verify>
    <automated>grep -q "swarm_dir}/agents/{agent_key}.md" orq-agent/agents/failure-diagnoser.md && grep -q "Glob" orq-agent/agents/failure-diagnoser.md && echo "PASS: path resolution added with Glob fallback" || echo "FAIL"</automated>
  </verify>
  <done>Phase 2 Step 2.2 step 1 explicitly states the path convention ({swarm_dir}/agents/{agent_key}.md) with a Glob fallback. An executor LLM no longer needs to guess where spec files live.</done>
</task>

</tasks>

<verification>
1. Phase 2 Step 2.2 step 1 contains explicit path `{swarm_dir}/agents/{agent_key}.md`
2. Glob fallback instruction present for non-standard layouts
3. No other lines in the file changed (surgical edit only)
4. All 15 verification truths from 30-VERIFICATION.md still hold (14 verified + 1 partial now fixed)
</verification>

<success_criteria>
- The spec file path resolution gap identified in 30-VERIFICATION.md is closed
- Truth 3 ("failure-diagnoser.md reads each failing agent's spec .md file") upgrades from PARTIAL to VERIFIED
- The key link from failure-diagnoser.md to agent spec files is fully wired
</success_criteria>

<output>
After completion, create `.planning/phases/30-failure-diagnoser/30-02-SUMMARY.md`
</output>
