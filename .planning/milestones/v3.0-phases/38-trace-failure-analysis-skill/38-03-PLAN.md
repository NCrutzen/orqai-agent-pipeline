---
phase: 38-trace-failure-analysis-skill
plan: 03
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - orq-agent/SKILL.md
  - orq-agent/commands/help.md
  - orq-agent/commands/traces.md
autonomous: true
requirements:
  - TFAIL-01
  - TFAIL-02
  - TFAIL-03
  - TFAIL-04
  - TFAIL-05
  - TFAIL-06
must_haves:
  truths:
    - "orq-agent/SKILL.md indexes the new skill with a Phase 38 H3 section, a command row, a requirement-coverage bullet block, and the resources subdirectory under Directory Structure"
    - "orq-agent/commands/help.md lists /orq-agent:trace-failure-analysis in the Commands block in pipeline-order (between /orq-agent:observability and /orq-agent:harden)"
    - "orq-agent/commands/traces.md Companion Skills adds a forward handoff to /orq-agent:trace-failure-analysis (replacing/supplementing the TODO(TFAIL) placeholder)"
    - "3 protected entry points (orq-agent.md, prompt.md, architect.md) remain byte-identical — NOT touched"
  artifacts:
    - path: "orq-agent/SKILL.md"
      provides: "New '### Phase 38 (Trace Failure Analysis)' H3 + coverage bullet block + resources/ dir listing under Directory Structure"
      contains: "trace-failure-analysis"
    - path: "orq-agent/commands/help.md"
      provides: "New Commands: line for /orq-agent:trace-failure-analysis"
      contains: "/orq-agent:trace-failure-analysis"
    - path: "orq-agent/commands/traces.md"
      provides: "Companion Skills updated — forward handoff to /orq-agent:trace-failure-analysis"
      contains: "trace-failure-analysis"
  key_links:
    - from: "orq-agent/SKILL.md"
      to: "orq-agent/commands/trace-failure-analysis.md"
      via: "Command table row + coverage block"
      pattern: "trace-failure-analysis"
    - from: "orq-agent/commands/help.md"
      to: "/orq-agent:trace-failure-analysis"
      via: "Pipeline-ordered Commands block entry"
      pattern: "/orq-agent:trace-failure-analysis"
    - from: "orq-agent/commands/traces.md Companion Skills"
      to: "/orq-agent:trace-failure-analysis"
      via: "Forward handoff bullet"
      pattern: "trace-failure-analysis"
---

<objective>
Wire the Phase 38 skill into the suite's discovery surfaces so users can find and route to it via `/orq-agent:help` and so `/orq-agent:traces` points forward to it as the natural next step.

Purpose: Close the index-wiring gap. Without this plan, the skill exists on disk but is invisible to users and has no upstream entry point from `/orq-agent:traces`.

Output: 3 existing files edited — additive only, protected entry points untouched.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-trace-failure-analysis-skill/38-CONTEXT.md

# Edit targets
@orq-agent/SKILL.md
@orq-agent/commands/help.md
@orq-agent/commands/traces.md

# Protected pipeline reference — confirm NOT editing these
@orq-agent/commands/orq-agent.md
@orq-agent/commands/prompt.md
@orq-agent/commands/architect.md

# Phase 37 precedent: how observability.md was wired into the same 2 index files
# (look for "### Phase 37 (Observability)" in SKILL.md and "/orq-agent:observability" in help.md)

<interfaces>
<!-- Protected pipeline hash check (see orq-agent/scripts/check-protected-pipelines.sh) hashes -->
<!-- the <pipeline> blocks ONLY on orq-agent.md / prompt.md / architect.md. Edits OUTSIDE those -->
<!-- blocks are safe even on those 3 files — but this plan does not touch them at all. -->

<!-- help.md pipeline-order principle (from SKILL.md Key Decisions + Phase 36 decision log): -->
<!-- diagnose-before-fix ordering — insert `/orq-agent:trace-failure-analysis` between -->
<!-- `/orq-agent:observability` and `/orq-agent:harden`. -->

<!-- SKILL.md pattern for Phase N index wiring (Phase 37 precedent): -->
<!-- 1. Directory Structure block: add new command file + resources/ subdir under commands/ -->
<!-- 2. Commands block: new `### Phase N (<Subsystem>)` H3 table AFTER prior phase block -->
<!-- 3. Requirement coverage bullet block directly below the table -->
<!-- 4. Resources Policy section: note the new per-skill resources/ dir if applicable -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Index-wire the new skill into SKILL.md, help.md, and traces.md (additive edits only)</name>
  <files>orq-agent/SKILL.md, orq-agent/commands/help.md, orq-agent/commands/traces.md</files>
  <read_first>
    - orq-agent/SKILL.md (full read — understand existing structure: Directory Structure, Commands tables per phase, Resources Policy)
    - orq-agent/commands/help.md (full read — find Commands block; observe pipeline-order: observability sits after quickstart, harden sits under V2.0 Commands)
    - orq-agent/commands/traces.md Companion Skills section (currently holds `TODO(TFAIL)` forward-link placeholder — this plan replaces it with a live link)
    - .planning/phases/38-trace-failure-analysis-skill/38-CONTEXT.md Integration Points block
  </read_first>
  <action>
Make 3 surgical edits. Each edit is additive; no deletions outside of the `TODO(TFAIL)` placeholder in traces.md.

### Edit 1 — `orq-agent/SKILL.md`

**1a. Directory Structure block** (the fenced code block starting `orq-agent/`):

Add these lines under `commands/` immediately after the `observability/` block (right after the `generic-otel.md` line):

```
    trace-failure-analysis.md    # Phase 38: Trace failure analysis skill (TFAIL-01..06)
    trace-failure-analysis/
      resources/
        grounded-theory-methodology.md  # Phase 38: Open + axial coding + first-upstream rule
        failure-mode-classification.md  # Phase 38: 4-category decision rules
        handoff-matrix.md                # Phase 38: Classification → next skill mapping
```

**1b. New Commands table** — immediately AFTER the existing `### Phase 37 (Observability)` H3 + its coverage bullet block, insert:

```markdown
### Phase 38 (Trace Failure Analysis)

| Command | File | Tier Required | Purpose |
|---------|------|---------------|---------|
| `/orq-agent:trace-failure-analysis` | `commands/trace-failure-analysis.md` | deploy+ | Turn ~100 production traces into a 4-8 mode failure taxonomy via grounded-theory coding — mixed 50/30/20 sampling (TFAIL-01), open + axial coding (TFAIL-02), first-upstream-failure labeling (TFAIL-03), transition matrix for multi-step pipelines (TFAIL-04), 4-category classification specification/generalization-code-checkable/generalization-subjective/trivial-bug (TFAIL-05), error-analysis report with handoff recommendations (TFAIL-06) |

**`/orq-agent:trace-failure-analysis` requirement coverage:**

- TFAIL-01 — Mixed sampling: 50% random + 30% failure-driven + 20% outliers targeting ~100 traces; sampling plan recorded in the final report.
- TFAIL-02 — Open coding (freeform per-trace annotations) then axial coding clustering annotations into 4-8 non-overlapping failure modes; saturation heuristic stops open coding when two consecutive batches yield no new themes.
- TFAIL-03 — First-upstream-failure rule: label ONLY the first span in topological order whose output fails its criterion; cascade children carry `cascade-of: <parent_mode>`.
- TFAIL-04 — Transition failure matrix (rows = last success, columns = first failure) for multi-step pipelines; skipped with explicit note for single-step pipelines.
- TFAIL-05 — Mutually-exclusive classification of every mode into specification / generalization-code-checkable / generalization-subjective / trivial-bug.
- TFAIL-06 — `error-analysis-YYYYMMDD-HHMM.md` report with taxonomy, rates, 3 example trace IDs per mode, transition matrix, and per-mode handoff (to `/orq-agent:prompt`, `/orq-agent:harden`, or developer fix).

Resource docs under `orq-agent/commands/trace-failure-analysis/resources/` (grounded-theory methodology, failure-mode classification, handoff matrix) are consumed only by `commands/trace-failure-analysis.md` (single-consumer; see Resources Policy below).
```

**1c. Resources Policy "Migration status" paragraph** — update the sentence `Phase 37 established the first live per-skill resources directory at ...` to append: `Phase 38 adds a second at orq-agent/commands/trace-failure-analysis/resources/ (3 files: grounded-theory-methodology, failure-mode-classification, handoff-matrix — consumed only by trace-failure-analysis.md).`

### Edit 2 — `orq-agent/commands/help.md`

Inside the fenced code block that starts with `━━━` and lists `Commands:`, insert ONE new line in pipeline-order BETWEEN `/orq-agent:observability` and `/orq-agent:automations` (diagnose-before-fix ordering — instrument traces → analyze failures → automate triggers):

```
  /orq-agent:trace-failure-analysis  Grounded-theory failure taxonomy from ~100 traces (deploy+ tier)
```

Maintain 2-space gutter + right-pad alignment with the neighboring lines (match the existing column alignment visually — the description column starts at the same horizontal position as the other rows).

Do NOT modify any other line in help.md. Do NOT change the version placeholder `vX.Y.Z`.

### Edit 3 — `orq-agent/commands/traces.md`

Locate the Companion Skills section. Find this line:

```
- → Phase 38 trace-failure analysis skill (forward link `TODO(TFAIL)`) — once error rows surface here, that skill will consume the IDs for systematic failure triage.
```

Replace with:

```
- → `/orq-agent:trace-failure-analysis` — once error rows surface here, that skill consumes the trace IDs for grounded-theory taxonomy building (4-8 modes, first-upstream-failure labeling, handoff report).
```

Do NOT modify any other line in traces.md. This is the only change; the `TODO(TFAIL)` placeholder elsewhere in the repo (observability.md) is resolved in a later phase — not this one.

### Protected files — DO NOT TOUCH

- `orq-agent/commands/orq-agent.md`
- `orq-agent/commands/prompt.md`
- `orq-agent/commands/architect.md`

Running `bash orq-agent/scripts/check-protected-pipelines.sh` after edits MUST exit 0 (3/3 SHA-256 matches).
  </action>
  <acceptance_criteria>
```bash
# SKILL.md edits landed
grep -q "trace-failure-analysis.md" orq-agent/SKILL.md
grep -q "### Phase 38 (Trace Failure Analysis)" orq-agent/SKILL.md
grep -q "/orq-agent:trace-failure-analysis" orq-agent/SKILL.md
grep -q "TFAIL-01" orq-agent/SKILL.md
grep -q "TFAIL-06" orq-agent/SKILL.md
grep -q "grounded-theory-methodology.md" orq-agent/SKILL.md
grep -q "failure-mode-classification.md" orq-agent/SKILL.md
grep -q "handoff-matrix.md" orq-agent/SKILL.md

# help.md edit landed in pipeline-order
grep -q "/orq-agent:trace-failure-analysis" orq-agent/commands/help.md
# Pipeline-order check: trace-failure-analysis line appears AFTER observability AND BEFORE automations line
awk '/\/orq-agent:observability/{obs=NR} /\/orq-agent:trace-failure-analysis/{tfa=NR} /\/orq-agent:automations/{aut=NR} END{exit !(obs<tfa && tfa<aut)}' orq-agent/commands/help.md

# traces.md Companion Skills forward handoff live
grep -q "/orq-agent:trace-failure-analysis" orq-agent/commands/traces.md
# Old TODO(TFAIL) placeholder removed from traces.md
! grep -q "TODO(TFAIL)" orq-agent/commands/traces.md

# Protected pipelines still byte-identical
bash orq-agent/scripts/check-protected-pipelines.sh

# Full-suite lint still green
bash orq-agent/scripts/lint-skills.sh
```
All checks exit 0.
  </acceptance_criteria>
  <verify>
    <automated>bash orq-agent/scripts/lint-skills.sh && bash orq-agent/scripts/check-protected-pipelines.sh && grep -q "/orq-agent:trace-failure-analysis" orq-agent/SKILL.md orq-agent/commands/help.md orq-agent/commands/traces.md</automated>
  </verify>
  <done>All 3 files contain the new cross-references, pipeline-order confirmed via awk check, protected-pipeline 3/3 SHA-256 matches, full-suite lint green.</done>
</task>

</tasks>

<verification>
Run full acceptance_criteria grep block including the pipeline-order awk check + protected-pipeline hash check + full-suite lint.
</verification>

<success_criteria>
- SKILL.md contains new Phase 38 H3, command row, coverage bullet block, directory-structure entries, and migration-status update.
- help.md Commands block contains `/orq-agent:trace-failure-analysis` inserted in pipeline-order between `/orq-agent:observability` and `/orq-agent:automations`.
- traces.md Companion Skills forward-handoff updated (no more `TODO(TFAIL)`).
- Protected-pipeline hash check passes (3/3).
- Full-suite lint passes.
</success_criteria>

<output>
After completion, create `.planning/phases/38-trace-failure-analysis-skill/38-03-SUMMARY.md` summarizing: files edited, pipeline-order position confirmed, protected-pipeline 3/3, lint green, and note whether the `TODO(TFAIL)` placeholder in other files (e.g. observability.md) was left for a later phase resolution.
</output>
