---
phase: 36-lifecycle-slash-commands
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/commands/automations.md
autonomous: true
requirements:
  - LCMD-06
must_haves:
  truths:
    - "Running /orq-agent:automations (no args) lists all Trace Automation rules in the workspace"
    - "Running /orq-agent:automations --create launches an interactive create flow via AskUserQuestion"
    - "The create flow collects: name, trace-filter criteria, target dataset, and experiment to auto-kick-off"
    - "All data fetches go through the Orq.ai MCP server; REST fallback via curl when MCP errors"
    - "Output ends with an Open in orq.ai trace-automations deep link + MCP tools used footer"
  artifacts:
    - path: "orq-agent/commands/automations.md"
      provides: "LCMD-06 — Trace Automations list + create slash command"
      contains: "## Constraints"
      contains_all:
        - "ORQ ► AUTOMATIONS"
        - "allowed-tools:"
        - "argument-hint:"
        - "--create"
        - "AskUserQuestion"
        - "trace-filter"
        - "dataset"
        - "experiment"
        - "MCP tools used"
        - "my.orq.ai/trace-automations"
        - "TODO(LCMD-06)"
  key_links:
    - from: "orq-agent/commands/automations.md"
      to: "Orq.ai Trace Automations API (via MCP tool if present, REST fallback)"
      via: "Step-by-step MCP tool invocations + curl fallback"
      pattern: "trace-automations|trace_automations"
    - from: "orq-agent/commands/automations.md"
      to: "Destructive Actions SKST-08 — --create is a mutation"
      via: "AskUserQuestion confirmation before POSTing the new rule"
      pattern: "AskUserQuestion"
---

<objective>
Create the `/orq-agent:automations` slash command at `orq-agent/commands/automations.md` — a two-mode command: list Orq.ai Trace Automation rules (read-only default) and create a new rule interactively with `--create` (mutation path gated by `AskUserQuestion`).

Purpose: LCMD-06. Let users manage the production-monitoring loop (new traces matching a filter → auto-kick-off an experiment) without leaving Claude Code.

Output: One new file passing Phase 34 SKST lint with `--create` branch using `AskUserQuestion` for every collected field.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-lifecycle-slash-commands/36-CONTEXT.md
@.planning/phases/36-lifecycle-slash-commands/36-VALIDATION.md

<interfaces>
<!-- Same copy-paste template shape as other Phase 36 commands, plus a mutation branch. -->

Argument conventions (locked in 36-CONTEXT.md):
- `/orq-agent:automations` — list mode (read-only).
- `/orq-agent:automations --create` — interactive create mode.

Create-mode fields (locked in 36-CONTEXT.md):
- name (string)
- trace-filter criteria (deployment key / status / tag combination)
- target dataset (dataset key or `new` to generate one)
- experiment to auto-kick-off (experiment key or `new` to generate one)

MCP tools (may not have a dedicated automations tool yet — that's why 36-CONTEXT.md flags it with `TODO(LCMD-06)` on the Open-in link; try REST first for the POST):
- List: try `mcp__orqai-mcp__search_entities type=trace_automation` first; fall back to `GET /v2/trace-automations`
- Create: REST `POST /v2/trace-automations` via curl (most reliable path given MCP catalog uncertainty)

Destructive action: `--create` writes a new rule to Orq.ai — must confirm via `AskUserQuestion` per SKST-08. The `systems.md` command is the closest in-repo template for a list+mutate pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author orq-agent/commands/automations.md</name>
  <files>orq-agent/commands/automations.md</files>
  <read_first>
    - orq-agent/commands/systems.md (nearest in-repo template for list + interactive create/remove with AskUserQuestion — copy the Step 4a add-flow shape)
    - orq-agent/commands/help.md (for the 9 SKST sections + banner pattern)
    - .planning/phases/36-lifecycle-slash-commands/36-CONTEXT.md (locked: two modes, --create collects 4 fields, Open-in link = https://my.orq.ai/&lt;workspace&gt;/trace-automations with TODO(LCMD-06) marker since this URL is inferred)
    - .planning/phases/36-lifecycle-slash-commands/36-VALIDATION.md (Task 36-06-01 verification map — `grep -q "\-\-create"` is BLOCKING)
    - orq-agent/scripts/lint-skills.sh (rules required-sections + allowed-tools)
  </read_first>
  <behavior>
    - `/orq-agent:automations` (no args): prints ORQ ► AUTOMATIONS banner + table of existing Trace Automation rules (Name, Filter, Target Dataset, Target Experiment, Enabled).
    - `/orq-agent:automations --create`: interactive flow using AskUserQuestion:
      1. Prompt for name
      2. Prompt for trace-filter criteria (deployment / status / tag)
      3. Prompt for target dataset (key or `new`)
      4. Prompt for target experiment (key or `new`)
      5. Render a summary
      6. AskUserQuestion: "Create this automation rule?" (options: yes / no)
      7. On yes → POST via MCP or REST; print success with rule ID
      8. On no → STOP without writing
    - List mode is read-only; create mode mutates Orq.ai state.
  </behavior>
  <action>
    Create `orq-agent/commands/automations.md`. Every H2 heading must appear verbatim.

    **Frontmatter (exact):**
    ```yaml
    ---
    description: List or create Orq.ai Trace Automation rules — auto-kick-off an experiment when new traces match a filter
    allowed-tools: Bash, Read, AskUserQuestion, mcp__orqai-mcp__search_entities, mcp__orqai-mcp__list_registry_keys
    argument-hint: "[--create]"
    ---
    ```

    **Body outline (each H2 present verbatim):**

    1. `# Trace Automations` + one-paragraph purpose line: "You are running the `/orq-agent:automations` command. This command lists Orq.ai Trace Automation rules or creates a new one that auto-kicks-off an experiment when new traces match a filter."

    2. `## Constraints`:
       - NEVER create an automation rule without AskUserQuestion confirmation — this writes to Orq.ai.
       - NEVER fabricate a rule list when MCP errors — surface the error and the REST fallback inline.
       - NEVER leave the `enabled` flag ambiguous in the rendered list — always show a boolean column.
       - ALWAYS render the rule list with Name / Filter / Target Dataset / Target Experiment / Enabled columns.
       - ALWAYS attempt MCP first; fall back to `curl -X {GET,POST} -H "Authorization: Bearer $ORQ_API_KEY" https://api.orq.ai/v2/trace-automations`.
       + "Why these constraints:" paragraph linking SKST-08 (destructive-action confirm) + MCP-first discipline.

    3. `## When to use` (3+ bullets: stand up a production-monitoring loop, re-verify an existing automation fires, add a new rule after deploying a new agent).

    4. `## When NOT to use` (2+ bullets: need to inspect which traces matched a rule → /orq-agent:traces filtered by the automation's experiment key; need to edit an existing rule → open Studio trace-automations page).

    5. `## Companion Skills` (→ /orq-agent:test to see experiment shape before wiring; → /orq-agent:traces to validate filter matches; ← user invocation).

    6. `## Done When`:
       - [ ] List mode: rules table rendered or "No automations configured" printed
       - [ ] Create mode: AskUserQuestion confirmation collected before POST
       - [ ] Create mode success: new rule ID echoed in the output
       - [ ] Create mode cancel: rule NOT written; banner prints "Cancelled — no automation created"
       - [ ] "MCP tools used:" footer present

    7. `## Destructive Actions`:
        The following actions MUST confirm via `AskUserQuestion` before proceeding:
        - **Create a new Trace Automation rule** — writes a POST /v2/trace-automations request to Orq.ai; the rule will begin matching new traces immediately once enabled.

    8. `## Step 1: Parse Arguments` — read `$ARGUMENTS`:
       - Empty → Step 2 (list mode)
       - `--create` → Step 3 (create mode)
       - Anything else → STOP with usage hint.

    9. `## Step 2: List Mode` — call `mcp__orqai-mcp__search_entities` with type=trace_automation. On MCP error, fall back: `curl -H "Authorization: Bearer $ORQ_API_KEY" https://api.orq.ai/v2/trace-automations`.
       Render banner + pipe-separated Markdown table `| Name | Filter | Target Dataset | Target Experiment | Enabled |`. If empty, print `No automations configured.\n\nCreate one with /orq-agent:automations --create`. Then print the Open-in + MCP-footer block. STOP.

    10. `## Step 3: Create Mode — Collect Fields` — use `AskUserQuestion` four times, one per field:
        1. `name`: short descriptive name (3-80 chars)
        2. `trace-filter`: a trace-filter expression — prompt with inline examples like `deployment_key=customer-support-agent AND status=error`
        3. `target dataset`: existing dataset key OR `new` to generate one via /orq-agent:datasets (if `new`, STOP and instruct user to create the dataset first)
        4. `experiment`: existing experiment key OR `new` to generate one via /orq-agent:test (if `new`, STOP and instruct user to create the experiment first)

    11. `## Step 4: Confirm` — render a summary block:
        ```
        ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
         ORQ ► AUTOMATIONS ► CREATE
        ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

         Name:       {name}
         Filter:     {trace-filter}
         Dataset:    {dataset}
         Experiment: {experiment}
        ```
        Then call `AskUserQuestion` with the question: `Create this Trace Automation rule?` and options `yes` / `no`. On `no` → print `Cancelled — no automation created.` and STOP.

    12. `## Step 5: Write Rule` — on `yes`: POST the rule via MCP (if an `mcp__orqai-mcp__create_trace_automation` tool exists at invocation time — try it first) OR REST fallback:
        ```
        curl -X POST -H "Authorization: Bearer $ORQ_API_KEY" -H "Content-Type: application/json" \
          -d '{"name":"{name}","filter":"{trace-filter}","dataset_key":"{dataset}","experiment_key":"{experiment}","enabled":true}' \
          https://api.orq.ai/v2/trace-automations
        ```
        On success, print:
        ```
        Created automation rule: {rule_id}
          Name: {name}
          Enabled: yes

        MCP tools used: search_entities (or REST fallback POST /v2/trace-automations)
        Open in orq.ai: https://my.orq.ai/trace-automations  # TODO(LCMD-06) verify canonical path via live MCP
        ```

    13. `## Anti-Patterns` (min 4 rows):
        - POSTing the rule without AskUserQuestion confirmation | Violates SKST-08 and can create rules the user did not intend
        - Rendering the rule list without the Enabled column | Disabled rules look identical to enabled ones; users act on stale info
        - Accepting `--create` with zero MCP connection | Fail fast with a REST fallback error message
        - Hiding the REST fallback when MCP is missing the tool | Users need to know which API surface responded

    14. `## Open in orq.ai`:
        - **Trace Automations:** https://my.orq.ai/trace-automations  (TODO(LCMD-06) — confirm canonical path via live MCP; Phase 37 may refine)
        - **Traces:** https://my.orq.ai/traces
        - **Experiments:** https://my.orq.ai/experiments

    15. `## Documentation & Resolution` — standard 4-line trust order.

    **Phase 35 snapshot-pinning:** No YAML `model:` lines in this file.

    The exact strings `--create`, `AskUserQuestion`, `trace-filter`, `dataset`, `experiment`, and `TODO(LCMD-06)` MUST appear literally — grep-verified acceptance anchors.
  </action>
  <verify>
    <automated>test -f orq-agent/commands/automations.md &amp;&amp; grep -q "^allowed-tools:" orq-agent/commands/automations.md &amp;&amp; grep -q "ORQ ► AUTOMATIONS" orq-agent/commands/automations.md &amp;&amp; grep -q "\-\-create" orq-agent/commands/automations.md &amp;&amp; grep -q "AskUserQuestion" orq-agent/commands/automations.md &amp;&amp; grep -q "trace-filter" orq-agent/commands/automations.md &amp;&amp; grep -q "dataset" orq-agent/commands/automations.md &amp;&amp; grep -q "experiment" orq-agent/commands/automations.md &amp;&amp; grep -q "MCP tools used" orq-agent/commands/automations.md &amp;&amp; grep -q "my.orq.ai/trace-automations" orq-agent/commands/automations.md &amp;&amp; grep -q "TODO(LCMD-06)" orq-agent/commands/automations.md &amp;&amp; bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/automations.md</automated>
  </verify>
  <acceptance_criteria>
    - File orq-agent/commands/automations.md exists.
    - Frontmatter contains `description:` and `allowed-tools:` non-empty; `allowed-tools` includes `AskUserQuestion`.
    - All 9 required H2 sections present.
    - Banner `ORQ ► AUTOMATIONS` appears.
    - `--create` flag literal appears.
    - `AskUserQuestion` literal appears (Destructive Actions SKST-08 gate).
    - All 3 field labels present (`trace-filter`, `dataset`, `experiment`).
    - `MCP tools used` footer present.
    - `my.orq.ai/trace-automations` Open-in link present with `TODO(LCMD-06)` marker.
    - `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/automations.md` exits 0.
    - `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0.
  </acceptance_criteria>
  <done>File orq-agent/commands/automations.md exists, passes SKST lint, declares list + `--create` modes with AskUserQuestion confirmation gate. LCMD-06 file-level requirement satisfied; manual MCP round-trip smoke (does POST actually write to a live workspace?) deferred to /gsd:verify-work 36.</done>
</task>

</tasks>

<verification>
- `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/automations.md` exits 0.
- `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0.
- All flag + AskUserQuestion + field + TODO greps hit.
</verification>

<success_criteria>
LCMD-06 file-level coverage complete: `/orq-agent:automations` lists rules and `--create` launches an AskUserQuestion-gated interactive create flow per ROADMAP Phase 36 Success Criterion #5 (second half). Manual MCP POST smoke deferred to /gsd:verify-work 36.
</success_criteria>

<output>
After completion, create `.planning/phases/36-lifecycle-slash-commands/36-06-SUMMARY.md` using the standard summary template.
</output>
