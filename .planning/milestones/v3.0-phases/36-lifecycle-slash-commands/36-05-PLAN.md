---
phase: 36-lifecycle-slash-commands
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/commands/quickstart.md
autonomous: true
requirements:
  - LCMD-05
  - LCMD-07
must_haves:
  truths:
    - "Running /orq-agent:quickstart prints an ORQ ► QUICKSTART banner plus a 12-numbered-step onboarding tour"
    - "Each of the 12 steps is an H2 with the exact heading prefix '## Step N:' (N = 1..12)"
    - "Each step includes an immediately-copy-pasteable prompt / command the user runs next"
    - "The sequence covers: MCP connect → enable models → create project → build agent → invoke → analyze traces → build evaluator → build dataset → run experiment → human review → annotation analysis → promote evaluator"
    - "Command is stateless — no sidecar progress file written"
  artifacts:
    - path: "orq-agent/commands/quickstart.md"
      provides: "LCMD-05 + LCMD-07 — 12-step interactive onboarding tour"
      contains: "## Constraints"
      contains_all:
        - "ORQ ► QUICKSTART"
        - "allowed-tools:"
        - "## Step 1:"
        - "## Step 2:"
        - "## Step 3:"
        - "## Step 4:"
        - "## Step 5:"
        - "## Step 6:"
        - "## Step 7:"
        - "## Step 8:"
        - "## Step 9:"
        - "## Step 10:"
        - "## Step 11:"
        - "## Step 12:"
        - "/orq-agent:workspace"
        - "/orq-agent:models"
        - "/orq-agent:traces"
        - "/orq-agent:datasets"
        - "/orq-agent:test"
        - "/orq-agent:harden"
        - "docs.orq.ai/docs/get-started"
  key_links:
    - from: "orq-agent/commands/quickstart.md"
      to: "All other /orq-agent:* commands (workspace, models, traces, datasets, test, harden)"
      via: "Copy-paste prompts inside each of the 12 Step sections"
      pattern: "/orq-agent:(workspace|models|traces|datasets|test|harden)"
---

<objective>
Create the `/orq-agent:quickstart` slash command at `orq-agent/commands/quickstart.md` — a single-file 12-step onboarding tour covering the full Build → Evaluate → Optimize lifecycle (MCP connect → enable models → create project → build agent → invoke → analyze traces → build evaluator → build dataset → run experiment → human review → annotation analysis → promote evaluator).

Purpose: LCMD-05 + LCMD-07 consolidated. Deliver the orq.ai-webinar-style 12-step tour with copy-paste prompts so a first-time user can walk the full pipeline without leaving Claude Code.

Output: One new file passing Phase 34 SKST lint with exactly 12 `## Step N:` sections (N=1..12) each containing a copy-pasteable prompt.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-lifecycle-slash-commands/36-CONTEXT.md
@.planning/phases/36-lifecycle-slash-commands/36-VALIDATION.md

<interfaces>
<!-- Same copy-paste template shape as other Phase 36 commands. Key differences for quickstart: -->

- Stateless — no sidecar progress file per 36-CONTEXT.md Claude's Discretion note.
- Consolidates LCMD-05 (API-key check + MCP registration + first-skill routing) AND LCMD-07 (12-step webinar tour). The 12 steps cover LCMD-05's onboarding intent.
- Step heading format MUST be `## Step N: {Name}` so the VALIDATION.md grep `grep -cE "^##? Step" orq-agent/commands/quickstart.md` returns ≥ 12.

Seed 12-step sequence (from 36-CONTEXT.md §quickstart 12-step sequence):
1. Connect MCP — `/orq-agent:workspace` to verify connection
2. Enable models — `/orq-agent:models <search>` + Studio activation
3. Create project — Studio
4. Build agent — `/orq-agent "<use-case>"`
5. Invoke agent — Studio Chat
6. Analyze traces — `/orq-agent:traces`
7. Build evaluator — `/orq-agent:harden --evaluators`
8. Build dataset — `/orq-agent:datasets`
9. Run experiment — `/orq-agent:test`
10. Human review — Orq.ai Annotation Queues
11. Annotation analysis — Studio
12. Promote evaluator — `/orq-agent:harden --promote`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author orq-agent/commands/quickstart.md</name>
  <files>orq-agent/commands/quickstart.md</files>
  <read_first>
    - orq-agent/commands/help.md (template — banner + 9 SKST sections; quickstart is essentially a longer help variant)
    - .planning/phases/36-lifecycle-slash-commands/36-CONTEXT.md (locked: 12 steps, copy-paste prompts, stateless, Open-in link = https://docs.orq.ai/docs/get-started; the 12-step seed sequence verbatim)
    - .planning/phases/36-lifecycle-slash-commands/36-VALIDATION.md (Task 36-05-01 verification map — `grep -cE "^##? Step" orq-agent/commands/quickstart.md` ≥ 12 is BLOCKING)
    - orq-agent/SKILL.md Commands table (to confirm exact command names referenced in each step — /orq-agent:workspace / :models / :traces / :datasets / :test / :harden — these must appear literally so the cross-link greps pass)
    - orq-agent/scripts/lint-skills.sh (rules required-sections + allowed-tools)
  </read_first>
  <behavior>
    - `/orq-agent:quickstart`: prints ORQ ► QUICKSTART banner + intro paragraph + 12 numbered Step sections.
    - Each Step section has: Name, one-paragraph purpose, a copy-pasteable prompt in a fenced code block, and an expected-outcome note.
    - No arguments. No state. User advances manually by running each step's prompt.
    - Read-only (no file writes, no API mutations from this command itself — the steps route to other commands that may mutate).
  </behavior>
  <action>
    Create `orq-agent/commands/quickstart.md`. Every H2 heading must appear verbatim.

    **Frontmatter (exact):**
    ```yaml
    ---
    description: 12-step interactive onboarding tour covering the Orq.ai Build → Evaluate → Optimize lifecycle with copy-paste prompts at each step
    allowed-tools: Bash, Read
    ---
    ```

    **Body outline (each H2 present verbatim). Note: the 9 SKST sections live AROUND the 12 Step sections. Total H2 count ≈ 21 (9 SKST + 12 Step).**

    1. `# Orq.ai Quickstart Tour` + one-paragraph purpose line.

    2. `## Constraints`:
       - NEVER skip the MCP-connect verification in Step 1 — every downstream step assumes a connected workspace.
       - NEVER re-order the 12 steps — the sequence encodes learning dependencies (agent before invoke; invoke before traces; traces before evaluator).
       - NEVER write a progress sidecar — command is stateless per 36-CONTEXT.md.
       - ALWAYS render each step's copy-paste prompt inside a fenced code block so users can triple-click to select.
       - ALWAYS include an Expected-outcome line per step so the user knows when to advance.
       + "Why these constraints:" paragraph about onboarding trust.

    3. `## When to use` (3+ bullets: first-time Orq.ai user, demo walkthrough, refresher after long break).

    4. `## When NOT to use` (2+ bullets: user already past Step 6 → jump to the specific command; user needs reference docs only → /orq-agent:help).

    5. `## Companion Skills` (→ /orq-agent:workspace Step 1; → /orq-agent:models Step 2; → /orq-agent Step 4; → /orq-agent:traces Step 6; → /orq-agent:harden Steps 7 and 12; → /orq-agent:datasets Step 8; → /orq-agent:test Step 9; ← user invocation).

    6. `## Done When`:
       - [ ] ORQ ► QUICKSTART banner printed
       - [ ] All 12 Step sections rendered
       - [ ] Each step has a fenced-block copy-paste prompt
       - [ ] Each step has an Expected-outcome line
       - [ ] Footer points to https://docs.orq.ai/docs/get-started

    7. `## Destructive Actions`: `- **None** — this command is read-only; it emits guidance. Steps 4, 7, 8, 9, 12 route to commands that mutate Orq.ai state — those commands own their own Destructive Actions confirmations.`

    8. `## Tour Intro` — banner block:
        ```
        ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
         ORQ ► QUICKSTART                 12 steps, ~30 min
        ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        ```
        + 1-paragraph intro explaining the flow (connect → enable → build → invoke → analyze → validate → promote).

    9. The 12 Step sections (exact H2 headings — these are grep-verified):

        `## Step 1: Connect MCP`
        - Purpose: verify the Orq.ai MCP server is registered and responding.
        - Copy-paste prompt (fenced):
          ```
          /orq-agent:workspace
          ```
        - Expected outcome: banner + non-empty agents/deployments/analytics table. If empty — install MCP: `curl -sfL https://raw.githubusercontent.com/NCrutzen/orqai-agent-pipeline/main/install.sh | bash`.

        `## Step 2: Enable Models`
        - Purpose: confirm at least one chat model is activated in the Model Garden.
        - Copy-paste prompt (fenced):
          ```
          /orq-agent:models claude
          ```
        - Expected outcome: at least one row with `Activated: yes`. Activate additional models in Studio: https://my.orq.ai/model-garden.

        `## Step 3: Create Project`
        - Purpose: create a project workspace to hold agents / deployments / datasets.
        - Copy-paste prompt (fenced):
          ```
          # Open Studio and create a project:
          # https://my.orq.ai/projects
          ```
        - Expected outcome: new project visible in `/orq-agent:workspace projects`.

        `## Step 4: Build Agent`
        - Purpose: generate a first agent spec.
        - Copy-paste prompt (fenced):
          ```
          /orq-agent "Build a customer support triage agent"
          ```
        - Expected outcome: generated spec under `./Agents/customer-support/`.

        `## Step 5: Invoke Agent`
        - Purpose: deploy the generated spec and test-invoke it in Studio Chat.
        - Copy-paste prompt (fenced):
          ```
          /orq-agent:deploy
          # then open Studio Chat: https://my.orq.ai/agents
          ```
        - Expected outcome: first trace appears in Studio.

        `## Step 6: Analyze Traces`
        - Purpose: inspect the invocation trace to confirm instrumentation.
        - Copy-paste prompt (fenced):
          ```
          /orq-agent:traces --last 1h
          ```
        - Expected outcome: at least one row with your test invocation's deployment key.

        `## Step 7: Build Evaluator`
        - Purpose: draft a first LLM-as-judge evaluator.
        - Copy-paste prompt (fenced):
          ```
          /orq-agent:harden --evaluators
          ```
        - Expected outcome: evaluator scaffold file; Phase 42 (EVLD) adds TPR/TNR validation.

        `## Step 8: Build Dataset`
        - Purpose: generate a small clean+edge dataset for the agent.
        - Copy-paste prompt (fenced):
          ```
          /orq-agent:datasets
          ```
        - Expected outcome: dataset file under `./Agents/{swarm}/datasets/`; Phase 39 (DSET) expands dataset shapes.

        `## Step 9: Run Experiment`
        - Purpose: run the dataset against the deployed agent with the evaluator attached.
        - Copy-paste prompt (fenced):
          ```
          /orq-agent:test
          ```
        - Expected outcome: experiment ID returned + results viewable at https://my.orq.ai/experiments.

        `## Step 10: Human Review`
        - Purpose: route a sample of test outputs to human annotators for Pass/Fail labels.
        - Copy-paste prompt (fenced):
          ```
          # Open Annotation Queues in Studio:
          # https://my.orq.ai/annotation-queues
          ```
        - Expected outcome: queue created; ≥30 Pass / ≥30 Fail labels collected (EVLD-04 / EVLD-06).

        `## Step 11: Annotation Analysis`
        - Purpose: compute TPR / TNR against the human-labeled held-out set (full tier gates this — Phase 42).
        - Copy-paste prompt (fenced):
          ```
          # Open Evaluator Validation in Studio:
          # https://my.orq.ai/experiments
          ```
        - Expected outcome: TPR and TNR numbers stored with the evaluator (Phase 42 EVLD-06 wiring).

        `## Step 12: Promote Evaluator`
        - Purpose: promote the validated evaluator to a runtime guardrail.
        - Copy-paste prompt (fenced):
          ```
          /orq-agent:harden --promote
          ```
        - Expected outcome: guardrail attached to the deployment; only promoted when TPR ≥ 90% AND TNR ≥ 90% (EVLD-08; enforced in Phase 42).

    10. `## Anti-Patterns` (min 4 rows):
        - Skipping Step 1 MCP verification | Every later step assumes a live connection; failures propagate silently
        - Re-ordering steps to "save time" | The sequence encodes dependencies (agent → invoke → traces → evaluator)
        - Writing a progress sidecar to track steps | Command is intentionally stateless — users own their progress
        - Promoting an evaluator (Step 12) before Step 11 validation | TPR/TNR gates exist to prevent bad guardrails; skipping them re-introduces the problem Phase 42 solves

    11. `## Open in orq.ai`:
        - **Get started docs:** https://docs.orq.ai/docs/get-started
        - **Model Garden:** https://my.orq.ai/model-garden
        - **Agents:** https://my.orq.ai/agents
        - **Experiments:** https://my.orq.ai/experiments
        - **Annotation Queues:** https://my.orq.ai/annotation-queues

    12. `## Documentation & Resolution` — standard 4-line trust order.

    **Phase 35 snapshot-pinning:** No YAML `model:` lines in this file. Any inline model references use dated snapshots.

    **Lint expectation:** `grep -cE "^##? Step" orq-agent/commands/quickstart.md` must return ≥ 12 (there are exactly 12 `## Step N:` headings).
  </action>
  <verify>
    <automated>test -f orq-agent/commands/quickstart.md &amp;&amp; grep -q "^allowed-tools:" orq-agent/commands/quickstart.md &amp;&amp; grep -q "ORQ ► QUICKSTART" orq-agent/commands/quickstart.md &amp;&amp; test "$(grep -cE '^##? Step [0-9]+:' orq-agent/commands/quickstart.md)" -ge 12 &amp;&amp; grep -q "## Step 1:" orq-agent/commands/quickstart.md &amp;&amp; grep -q "## Step 12:" orq-agent/commands/quickstart.md &amp;&amp; grep -q "/orq-agent:workspace" orq-agent/commands/quickstart.md &amp;&amp; grep -q "/orq-agent:models" orq-agent/commands/quickstart.md &amp;&amp; grep -q "/orq-agent:traces" orq-agent/commands/quickstart.md &amp;&amp; grep -q "/orq-agent:datasets" orq-agent/commands/quickstart.md &amp;&amp; grep -q "/orq-agent:test" orq-agent/commands/quickstart.md &amp;&amp; grep -q "/orq-agent:harden" orq-agent/commands/quickstart.md &amp;&amp; grep -q "docs.orq.ai/docs/get-started" orq-agent/commands/quickstart.md &amp;&amp; bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/quickstart.md</automated>
  </verify>
  <acceptance_criteria>
    - File orq-agent/commands/quickstart.md exists.
    - Frontmatter contains `description:` and `allowed-tools:` non-empty.
    - All 9 required SKST H2 sections present.
    - Banner `ORQ ► QUICKSTART` appears.
    - Exactly 12 `## Step N:` H2 headings present (Step 1 .. Step 12) — grep count ≥ 12.
    - All 6 sibling-command references present (`/orq-agent:workspace`, `/orq-agent:models`, `/orq-agent:traces`, `/orq-agent:datasets`, `/orq-agent:test`, `/orq-agent:harden`).
    - `docs.orq.ai/docs/get-started` Open-in link present.
    - `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/quickstart.md` exits 0.
    - `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0.
  </acceptance_criteria>
  <done>File orq-agent/commands/quickstart.md exists, passes SKST lint, declares 12 Step sections with copy-paste prompts covering the full lifecycle, stateless. LCMD-05 + LCMD-07 file-level requirements satisfied; manual UX smoke deferred to /gsd:verify-work 36.</done>
</task>

</tasks>

<verification>
- `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/quickstart.md` exits 0.
- `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0.
- Step-count grep returns ≥ 12.
- All 6 sibling-command references hit.
</verification>

<success_criteria>
LCMD-05 + LCMD-07 file-level coverage complete: 12-step onboarding tour rendered per ROADMAP Phase 36 Success Criterion #5 (first half). Manual UX smoke (does the flow feel natural on a fresh workspace?) deferred to /gsd:verify-work 36.
</success_criteria>

<output>
After completion, create `.planning/phases/36-lifecycle-slash-commands/36-05-SUMMARY.md` using the standard summary template.
</output>
