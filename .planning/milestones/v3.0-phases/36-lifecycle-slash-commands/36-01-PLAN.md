---
phase: 36-lifecycle-slash-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/commands/workspace.md
autonomous: true
requirements:
  - LCMD-01
must_haves:
  truths:
    - "Running /orq-agent:workspace prints an ORQ ► WORKSPACE banner with a single-screen overview"
    - "Output includes subsections for agents, deployments, prompts, datasets, experiments, projects, KBs, and evaluators"
    - "Output includes an analytics summary line (requests / cost / tokens / error rate snapshot)"
    - "Output ends with an Open in orq.ai deep-link + MCP tools used footer"
    - "Optional positional argument [section] filters output to that section only"
  artifacts:
    - path: "orq-agent/commands/workspace.md"
      provides: "LCMD-01 — workspace overview slash command"
      contains: "## Constraints"
      contains_all:
        - "ORQ ► WORKSPACE"
        - "allowed-tools:"
        - "argument-hint:"
        - "agents"
        - "deployments"
        - "prompts"
        - "datasets"
        - "experiments"
        - "projects"
        - "knowledge bases"
        - "evaluators"
        - "MCP tools used"
        - "my.orq.ai"
  key_links:
    - from: "orq-agent/commands/workspace.md"
      to: "Orq.ai MCP server (search_entities, search_directories, get_analytics_overview)"
      via: "Step-by-step MCP tool invocations in the command body"
      pattern: "mcp__orqai-mcp__"
    - from: "orq-agent/commands/workspace.md"
      to: "Phase 34 SKST lint"
      via: "9 required H2 sections + allowed-tools: frontmatter"
      pattern: "## (Constraints|When to use|When NOT to use|Companion Skills|Done When|Destructive Actions|Anti-Patterns|Open in orq.ai|Documentation & Resolution)"
---

<objective>
Create the `/orq-agent:workspace` slash command at `orq-agent/commands/workspace.md` — a thin MCP-backed, read-only command that prints a single-screen overview of every Orq.ai entity type (agents, deployments, prompts, datasets, experiments, projects, KBs, evaluators) plus an analytics summary line.

Purpose: LCMD-01. Give users Orq.ai workspace visibility from Claude Code without opening the Studio dashboard.

Output: One new file `orq-agent/commands/workspace.md` that passes Phase 34 SKST lint (9 sections + `allowed-tools:` frontmatter) and matches the house-style banner/format from `orq-agent/commands/help.md`.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-lifecycle-slash-commands/36-CONTEXT.md
@.planning/phases/36-lifecycle-slash-commands/36-VALIDATION.md

<interfaces>
<!-- Copy-paste template shape. Every new Phase 36 command MUST match this shape. -->

From orq-agent/commands/help.md (template for read-only command):

```yaml
---
description: <one-line purpose>
allowed-tools: Read, Bash  # plus MCP tools as needed
---
```

Required H2 sections (lint enforced by orq-agent/scripts/lint-skills.sh):
1. ## Constraints  (NEVER/ALWAYS + "Why these constraints:" paragraph)
2. ## When to use
3. ## When NOT to use
4. ## Companion Skills  (directional handoffs)
5. ## Done When  (falsifiable checklist)
6. ## Destructive Actions  (or "- **None** — this command is read-only")
7. ## Anti-Patterns  (table: Pattern | Do Instead)
8. ## Open in orq.ai
9. ## Documentation & Resolution

Banner format: `ORQ ► {VERB} {OBJECT}` wrapped in ━ box characters.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author orq-agent/commands/workspace.md</name>
  <files>orq-agent/commands/workspace.md</files>
  <read_first>
    - orq-agent/commands/help.md (read-only template — copy frontmatter shape + banner pattern + 9 SKST sections verbatim)
    - orq-agent/commands/systems.md (template for argument parsing + multi-branch output)
    - .planning/phases/36-lifecycle-slash-commands/36-CONTEXT.md (locked decisions: banner style, MCP-first, argument conventions, Open in orq.ai link = https://my.orq.ai/<workspace>/agents)
    - .planning/phases/36-lifecycle-slash-commands/36-VALIDATION.md (Task 36-01-01 verification map)
    - orq-agent/SKILL.md (existing MCP tool catalog — confirm search_entities / search_directories / get_analytics_overview are available)
    - orq-agent/scripts/lint-skills.sh (acceptance test the new file must pass)
  </read_first>
  <behavior>
    - `/orq-agent:workspace` (no args): prints ORQ ► WORKSPACE banner + one subsection per entity type (agents / deployments / prompts / datasets / experiments / projects / knowledge bases / evaluators) + an analytics summary line + "Open in orq.ai" link + "MCP tools used:" footer.
    - `/orq-agent:workspace agents` (positional filter): prints only the agents subsection.
    - Accepted section values: agents, deployments, prompts, datasets, experiments, projects, kb, evaluators, analytics.
    - Unknown section value → STOP with usage hint.
    - Each subsection uses a pipe-separated Markdown table (columns chosen at Claude's discretion per 36-CONTEXT.md — reasonable default: Name | Key | Status | Updated).
    - Read-only — no file writes, no Orq.ai mutations.
  </behavior>
  <action>
    Create `orq-agent/commands/workspace.md` with the EXACT structure below. Every H2 heading must appear verbatim (SKST lint is grep-based).

    **Frontmatter (exact):**
    ```yaml
    ---
    description: Single-screen Orq.ai workspace overview — agents, deployments, prompts, datasets, experiments, projects, KBs, evaluators + analytics summary
    allowed-tools: Bash, Read, mcp__orqai-mcp__search_entities, mcp__orqai-mcp__search_directories, mcp__orqai-mcp__get_analytics_overview, mcp__orqai-mcp__list_registry_keys
    argument-hint: "[agents|deployments|prompts|datasets|experiments|projects|kb|evaluators|analytics]"
    ---
    ```

    **Body outline (each H2 present verbatim):**

    1. `# Workspace Overview` + one-paragraph purpose line: "You are running the `/orq-agent:workspace` command. This command prints a single-screen overview of the Orq.ai workspace — all entity types plus an analytics summary line."

    2. `## Constraints` with:
       - NEVER mutate Orq.ai entities — this is a read-only overview.
       - NEVER fabricate entity counts when an MCP tool returns an error — show the error inline.
       - ALWAYS call MCP tools first; fall back to REST via `curl` with `$ORQ_API_KEY` only when MCP is unavailable.
       - ALWAYS print the "MCP tools used:" footer listing every tool invoked.
       + "Why these constraints:" paragraph tying the rules to SKST-06 + MCP-first PROJECT.md decision.

    3. `## When to use` (3+ bullets: new user onboarding check, quick inventory, pre-deploy sanity check).

    4. `## When NOT to use` (2+ bullets: need per-trace detail → use /orq-agent:traces; need model inventory → use /orq-agent:models).

    5. `## Companion Skills` (directional handoffs to /orq-agent:traces, /orq-agent:analytics, /orq-agent:models; ← user invocation).

    6. `## Done When` falsifiable checklist:
       - [ ] Terminal shows ORQ ► WORKSPACE banner
       - [ ] All 8 entity subsections (or the single requested section) printed
       - [ ] Analytics summary line present (requests / cost / tokens / error-rate)
       - [ ] "Open in orq.ai" deep-link rendered
       - [ ] "MCP tools used:" footer lists at least one tool

    7. `## Destructive Actions` with exact text: `- **None** — this command is read-only (reads Orq.ai workspace data via MCP).`

    8. `## Step 1: Parse Argument` — read `$ARGUMENTS`; if empty → full overview; if one of {agents|deployments|prompts|datasets|experiments|projects|kb|evaluators|analytics} → filtered mode; else → STOP with usage hint.

    9. `## Step 2: Fetch Workspace Data` — for each requested section, call the matching MCP tool (documented per subsection): agents → `search_entities` with type=agent; deployments → `search_entities` with type=deployment; prompts → `search_entities` with type=prompt; datasets → `search_entities` with type=dataset; experiments → `search_entities` with type=experiment; projects → `search_directories`; kb → `search_entities` with type=knowledge_base; evaluators → `search_entities` with type=evaluator; analytics → `get_analytics_overview`. If a tool errors, fall back to `curl -H "Authorization: Bearer $ORQ_API_KEY" https://api.orq.ai/v2/...` and note the fallback inline.

    10. `## Step 3: Render Banner + Sections` — print the box-char banner `━━━…` + `ORQ ► WORKSPACE` (or `ORQ ► WORKSPACE ► {SECTION}` in filtered mode) + `━━━…`. For each section print an H3 like `Agents (N)` followed by a pipe table: `| Name | Key | Status | Updated |` (exact ordering at Claude's discretion per 36-CONTEXT.md).

    11. `## Step 4: Render Analytics Summary Line` — single line: `Requests: {N} | Cost: {USD} | Tokens: {N} | Error rate: {N}% (last 24h)`. Use `get_analytics_overview` window = 24h unless `analytics` section filter is combined with a future `--last` flag (not in scope for this command — use /orq-agent:analytics for drill-down).

    12. `## Step 5: Print Open-in-orq.ai + MCP Footer` — emit exactly:
        ```
        Open in orq.ai:
          https://my.orq.ai/agents

        MCP tools used: search_entities, search_directories, get_analytics_overview
        ```
        (Tool list reflects what was actually invoked; use `, REST fallback` suffix if any section fell back to curl.)

    13. `## Anti-Patterns` table (min 3 rows): mutating entities instead of reading | fabricating counts on MCP errors | collapsing all sections into a single table without H3 subheadings.

    14. `## Open in orq.ai` block:
        - **Agents:** https://my.orq.ai/agents
        - **Deployments:** https://my.orq.ai/deployments
        - **Experiments:** https://my.orq.ai/experiments
        - **Traces:** https://my.orq.ai/traces

    15. `## Documentation & Resolution` — same 4-line trust order used in help.md / systems.md (MCP tools > docs MCP > docs.orq.ai > this skill file).

    **Phase 35 snapshot-pinning check:** The file will NOT contain any `model:` lines (this command lists models in a table but does not emit YAML spec fragments). If any example model ID is shown in prose, use a dated snapshot like `claude-sonnet-4-5-20250929`.

    Do NOT create any auxiliary files. Do NOT touch orq-agent.md / prompt.md / architect.md.
  </action>
  <verify>
    <automated>test -f orq-agent/commands/workspace.md &amp;&amp; grep -q "^allowed-tools:" orq-agent/commands/workspace.md &amp;&amp; grep -q "^description:" orq-agent/commands/workspace.md &amp;&amp; grep -q "ORQ ► WORKSPACE" orq-agent/commands/workspace.md &amp;&amp; grep -q "agents" orq-agent/commands/workspace.md &amp;&amp; grep -q "deployments" orq-agent/commands/workspace.md &amp;&amp; grep -q "prompts" orq-agent/commands/workspace.md &amp;&amp; grep -q "datasets" orq-agent/commands/workspace.md &amp;&amp; grep -q "experiments" orq-agent/commands/workspace.md &amp;&amp; grep -q "projects" orq-agent/commands/workspace.md &amp;&amp; grep -qi "knowledge bases\|knowledge base" orq-agent/commands/workspace.md &amp;&amp; grep -q "evaluators" orq-agent/commands/workspace.md &amp;&amp; grep -q "MCP tools used" orq-agent/commands/workspace.md &amp;&amp; grep -q "my.orq.ai" orq-agent/commands/workspace.md &amp;&amp; bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/workspace.md</automated>
  </verify>
  <acceptance_criteria>
    - File orq-agent/commands/workspace.md exists.
    - Frontmatter contains `description:` and `allowed-tools:` keys with non-empty values (SKST-01 via lint rule `allowed-tools`).
    - All 9 required H2 sections present and outside any XML block (SKST via lint rule `required-sections`).
    - Banner string `ORQ ► WORKSPACE` appears at least once.
    - All 8 entity-type tokens appear (agents, deployments, prompts, datasets, experiments, projects, knowledge base(s), evaluators).
    - `MCP tools used` footer phrase appears.
    - `my.orq.ai` Open-in link appears.
    - `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/workspace.md` exits 0.
    - Protected pipelines unchanged: `bash orq-agent/scripts/check-protected-pipelines.sh` still exits 0 (sanity — this plan touches zero protected files).
  </acceptance_criteria>
  <done>File orq-agent/commands/workspace.md exists, passes SKST lint, contains all 8 entity-type anchors + analytics line + MCP footer + Open-in-orq.ai link. LCMD-01 file-level requirement satisfied; manual MCP smoke deferred to /gsd:verify-work 36.</done>
</task>

</tasks>

<verification>
- `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/workspace.md` exits 0.
- `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0.
- Grep anchors above all hit.
</verification>

<success_criteria>
LCMD-01 file-level coverage complete: `/orq-agent:workspace [section]` command exists, passes SKST lint, renders the 8-subsection + analytics single-screen overview per ROADMAP Phase 36 Success Criterion #1. Manual MCP round-trip smoke deferred to /gsd:verify-work 36 per 36-VALIDATION.md "Manual-Only Verifications".
</success_criteria>

<output>
After completion, create `.planning/phases/36-lifecycle-slash-commands/36-01-SUMMARY.md` using the standard summary template.
</output>
