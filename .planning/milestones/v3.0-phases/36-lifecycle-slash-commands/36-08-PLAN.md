---
phase: 36-lifecycle-slash-commands
plan: 08
type: execute
wave: 3
depends_on:
  - "36-01"
  - "36-02"
  - "36-03"
  - "36-04"
  - "36-05"
  - "36-06"
  - "36-07"
files_modified:
  - .planning/phases/36-lifecycle-slash-commands/36-08-VERIFICATION.md
autonomous: true
requirements:
  - LCMD-01
  - LCMD-02
  - LCMD-03
  - LCMD-04
  - LCMD-05
  - LCMD-06
  - LCMD-07
must_haves:
  truths:
    - "Full SKST lint sweep across all 21 command files exits 0 (15 pre-existing + 6 new)"
    - "Protected-pipeline SHA-256 hash check exits 0 (3 entry points still byte-identical)"
    - "Every per-task VALIDATION.md grep anchor verifies (workspace banner, 4 traces flags, --group-by, chat/embedding/rerank, 12 quickstart steps, --create)"
    - "A 36-08-VERIFICATION.md file is written with a 7-row LCMD-{01..07} traceability table + 5-row ROADMAP phase-36 success-criteria checklist"
    - "All command cross-wires in SKILL.md + help.md resolve (grep anchors from Plan 07 all hit)"
  artifacts:
    - path: ".planning/phases/36-lifecycle-slash-commands/36-08-VERIFICATION.md"
      provides: "Phase 36 verification evidence trail (companion to SUMMARY.md)"
      contains_all:
        - "LCMD-01"
        - "LCMD-02"
        - "LCMD-03"
        - "LCMD-04"
        - "LCMD-05"
        - "LCMD-06"
        - "LCMD-07"
        - "lint-skills.sh"
        - "check-protected-pipelines.sh"
        - "ROADMAP success-criteria checklist"
  key_links:
    - from: "36-08-VERIFICATION.md"
      to: "ROADMAP.md Phase 36 Success Criteria (5 items)"
      via: "Per-criterion checklist row"
      pattern: "Success Criterion"
    - from: "36-08-VERIFICATION.md"
      to: "REQUIREMENTS.md LCMD-01..07"
      via: "7-row traceability table"
      pattern: "LCMD-0[1-7]"
---

<objective>
Run the full Phase 36 verification sweep and capture the evidence trail in `.planning/phases/36-lifecycle-slash-commands/36-08-VERIFICATION.md`.

Purpose: Final gate — prove that every LCMD requirement is file-level satisfied AND that the protected pipeline invariant survived Phase 36. This mirrors the Phase 34/35 phase-close VERIFICATION.md pattern (structured evidence document sits next to SUMMARY.md and feeds /gsd:verify-work).

Output: One new file — the verification evidence trail — containing captured command output, a 7-row LCMD traceability table, a 5-row ROADMAP success-criteria checklist, an inventory of the 6 new command files, and a list of deferred manual-only verifications.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36-lifecycle-slash-commands/36-CONTEXT.md
@.planning/phases/36-lifecycle-slash-commands/36-VALIDATION.md
@.planning/phases/36-lifecycle-slash-commands/36-01-SUMMARY.md
@.planning/phases/36-lifecycle-slash-commands/36-02-SUMMARY.md
@.planning/phases/36-lifecycle-slash-commands/36-03-SUMMARY.md
@.planning/phases/36-lifecycle-slash-commands/36-04-SUMMARY.md
@.planning/phases/36-lifecycle-slash-commands/36-05-SUMMARY.md
@.planning/phases/36-lifecycle-slash-commands/36-06-SUMMARY.md
@.planning/phases/36-lifecycle-slash-commands/36-07-SUMMARY.md

<!-- Reference: Phase 34-05 and Phase 35-05 VERIFICATION.md files are the canonical shape for this document. -->
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run full verification sweep and write 36-08-VERIFICATION.md</name>
  <files>.planning/phases/36-lifecycle-slash-commands/36-08-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/34-skill-structure-format-foundation/34-05-VERIFICATION.md (canonical shape — copy the structural skeleton)
    - .planning/phases/35-model-selection-discipline/35-05-VERIFICATION.md (Phase 35's phase-close evidence file — mirror its table layout)
    - .planning/phases/36-lifecycle-slash-commands/36-VALIDATION.md (the per-task verification map — every row becomes an evidence entry)
    - .planning/ROADMAP.md §Phase 36 Success Criteria (5 items — copy verbatim into the checklist)
    - .planning/REQUIREMENTS.md §Lifecycle Commands (LCMD-01..07 verbatim bullets)
    - orq-agent/scripts/lint-skills.sh (to understand silent-on-success vs loud-on-failure output)
    - orq-agent/scripts/check-protected-pipelines.sh (to understand its output shape)
  </read_first>
  <behavior>
    - Runs the full SKST lint + protected-pipeline check + all LCMD phrase-presence greps.
    - Captures each command's stdout/stderr + exit code.
    - Writes a single 36-08-VERIFICATION.md file that stands alone as an audit trail.
    - Does NOT create or edit any production command file.
    - Does NOT modify .planning/STATE.md (that's the orchestrator's job on plan completion).
  </behavior>
  <action>
    Create `.planning/phases/36-lifecycle-slash-commands/36-08-VERIFICATION.md`. Structure:

    **1. Frontmatter:**
    ```yaml
    ---
    phase: 36
    slug: lifecycle-slash-commands
    verified_at: 2026-04-20  # substitute with actual invocation date
    verifier: Claude Opus 4.7 (plan 36-08 executor)
    verification_type: phase-close evidence trail
    ---
    ```

    **2. Header:**
    ```markdown
    # Phase 36 — Verification Evidence Trail

    **Status:** All 7 LCMD file-level requirements verified. Manual MCP round-trip smokes deferred to `/gsd:verify-work 36`.
    **Command inventory:** 6 new command files created (workspace, traces, analytics, models, quickstart, automations).
    **Cross-cutting wiring:** orq-agent/SKILL.md + orq-agent/commands/help.md updated (Plan 07).
    **Protected pipelines:** 3/3 still byte-identical.
    ```

    **3. Captured commands and output:**

    Execute each of the following and capture both stdout and the exit code. Paste verbatim in fenced `bash` blocks.

    - **a.** Full SKST lint sweep:
      `bash orq-agent/scripts/lint-skills.sh`
      Expected: silent-on-success + `exit code 0`.

    - **b.** Protected-pipeline hash check:
      `bash orq-agent/scripts/check-protected-pipelines.sh`
      Expected: 3/3 matches + `exit code 0`.

    - **c.** Per-file lint on each new command (6 invocations):
      ```
      bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/workspace.md
      bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/traces.md
      bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/analytics.md
      bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/models.md
      bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/quickstart.md
      bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/automations.md
      ```
      Expected: each silent + `exit code 0`.

    - **d.** LCMD phrase-presence greps (one line per anchor, capture exit code):
      ```
      grep -q "ORQ ► WORKSPACE" orq-agent/commands/workspace.md && echo "PASS: workspace banner" || echo "FAIL: workspace banner"
      grep -q "ORQ ► TRACES" orq-agent/commands/traces.md && echo "PASS: traces banner" || echo "FAIL: traces banner"
      grep -q "\-\-deployment" orq-agent/commands/traces.md && echo "PASS: traces --deployment" || echo "FAIL: traces --deployment"
      grep -q "\-\-status" orq-agent/commands/traces.md && echo "PASS: traces --status" || echo "FAIL: traces --status"
      grep -q "\-\-last" orq-agent/commands/traces.md && echo "PASS: traces --last" || echo "FAIL: traces --last"
      grep -q "\-\-limit" orq-agent/commands/traces.md && echo "PASS: traces --limit" || echo "FAIL: traces --limit"
      grep -q "\-\-identity" orq-agent/commands/traces.md && echo "PASS: traces --identity stub" || echo "FAIL: traces --identity stub"
      grep -q "TODO(OBSV-07)" orq-agent/commands/traces.md && echo "PASS: OBSV-07 forward link" || echo "FAIL: OBSV-07 forward link"
      grep -q "ORQ ► ANALYTICS" orq-agent/commands/analytics.md && echo "PASS: analytics banner" || echo "FAIL"
      grep -q "\-\-group-by" orq-agent/commands/analytics.md && echo "PASS: analytics --group-by" || echo "FAIL"
      grep -q "ORQ ► MODELS" orq-agent/commands/models.md && echo "PASS: models banner" || echo "FAIL"
      grep -qi "chat" orq-agent/commands/models.md && grep -qi "embedding" orq-agent/commands/models.md && grep -qi "rerank" orq-agent/commands/models.md && echo "PASS: models type breakdown" || echo "FAIL"
      test "$(grep -cE '^##? Step [0-9]+:' orq-agent/commands/quickstart.md)" -ge 12 && echo "PASS: quickstart 12-step count" || echo "FAIL"
      grep -q "\-\-create" orq-agent/commands/automations.md && grep -q "AskUserQuestion" orq-agent/commands/automations.md && echo "PASS: automations --create + AskUserQuestion" || echo "FAIL"
      grep -q "workspace.md" orq-agent/SKILL.md && grep -q "traces.md" orq-agent/SKILL.md && grep -q "analytics.md" orq-agent/SKILL.md && grep -q "models.md" orq-agent/SKILL.md && grep -q "quickstart.md" orq-agent/SKILL.md && grep -q "automations.md" orq-agent/SKILL.md && echo "PASS: SKILL.md directory listing" || echo "FAIL"
      grep -q "/orq-agent:workspace" orq-agent/commands/help.md && grep -q "/orq-agent:traces" orq-agent/commands/help.md && grep -q "/orq-agent:analytics" orq-agent/commands/help.md && grep -q "/orq-agent:models" orq-agent/commands/help.md && grep -q "/orq-agent:quickstart" orq-agent/commands/help.md && grep -q "/orq-agent:automations" orq-agent/commands/help.md && echo "PASS: help.md pipeline-order index" || echo "FAIL"
      ```
      All 16 anchors must print PASS.

    **4. LCMD Traceability Table (7 rows):**

    ```markdown
    | Requirement | Artifact | Phrase / Flag Anchor | Verified |
    |-------------|----------|----------------------|----------|
    | LCMD-01 | orq-agent/commands/workspace.md | "ORQ ► WORKSPACE" + 8 entity subsections + analytics line | ✓ |
    | LCMD-02 | orq-agent/commands/traces.md | --deployment, --status, --last, --limit, --identity, TODO(OBSV-07), "errors first" | ✓ |
    | LCMD-03 | orq-agent/commands/analytics.md | --last, --group-by, model/deployment/agent/status, requests/cost/tokens/error rate | ✓ |
    | LCMD-04 | orq-agent/commands/models.md | chat + embedding + image + rerank + provider grouping (MSEL-02 lint clean) | ✓ |
    | LCMD-05 | orq-agent/commands/quickstart.md | API-key-check + MCP-registration guidance covered within the 12-step tour | ✓ |
    | LCMD-06 | orq-agent/commands/automations.md | list mode + --create + AskUserQuestion + trace-filter / dataset / experiment fields | ✓ |
    | LCMD-07 | orq-agent/commands/quickstart.md | Exactly 12 "## Step N:" headings covering the lifecycle | ✓ |
    ```

    **5. ROADMAP Phase 36 Success-Criteria Checklist (5 rows):**

    Read ROADMAP.md §Phase 36 Success Criteria verbatim. For each:
    ```markdown
    | # | Success Criterion (ROADMAP) | Evidence | Status |
    |---|-----------------------------|----------|--------|
    | 1 | /orq-agent:workspace [section] prints single-screen overview | workspace.md exists, SKST lint green, 8 entity-type anchors + analytics line present | file-level ✓; manual MCP smoke deferred to /gsd:verify-work |
    | 2 | /orq-agent:traces supports --deployment/--status/--last/--limit + errors first | traces.md exists, SKST lint green, 5 flag anchors + "errors first" present | file-level ✓; manual MCP smoke deferred |
    | 3 | /orq-agent:analytics reports requests/cost/tokens/error-rate with --last + --group-by | analytics.md exists, SKST lint green, all 4 group-by dims + 4 metrics present | file-level ✓; manual MCP smoke deferred |
    | 4 | /orq-agent:models lists Model Garden by provider × type | models.md exists, SKST + MSEL-02 lint green, 4 type tokens + provider grouping present | file-level ✓; manual MCP smoke deferred |
    | 5 | /orq-agent:quickstart delivers 12-step tour AND /orq-agent:automations lists/creates rules | quickstart.md has 12 Step sections; automations.md has list + --create branches with AskUserQuestion | file-level ✓; manual UX + POST smoke deferred |
    ```

    **6. Inventory (file listing):**

    ```markdown
    ## New Files (6)

    - orq-agent/commands/workspace.md (Plan 36-01)
    - orq-agent/commands/traces.md (Plan 36-02)
    - orq-agent/commands/analytics.md (Plan 36-03)
    - orq-agent/commands/models.md (Plan 36-04)
    - orq-agent/commands/quickstart.md (Plan 36-05)
    - orq-agent/commands/automations.md (Plan 36-06)

    ## Modified Files (2)

    - orq-agent/SKILL.md (Plan 36-07: +6 directory-listing entries, +1 Commands subsection)
    - orq-agent/commands/help.md (Plan 36-07: +6 lines in pipeline-order Commands block)

    ## Unchanged Protected Files (3)

    - orq-agent/commands/orq-agent.md (byte-identical `<pipeline>` block)
    - orq-agent/commands/prompt.md (byte-identical `<pipeline>` block)
    - orq-agent/commands/architect.md (byte-identical `<pipeline>` block)
    ```

    **7. Deferred Manual-Only Verifications** (copy from 36-VALIDATION.md §Manual-Only Verifications):

    ```markdown
    ## Deferred to /gsd:verify-work 36

    | Behavior | Requirement | Why Manual |
    |----------|-------------|------------|
    | Each command invokes the expected MCP tools and returns data | LCMD-01..06 | Requires live Orq.ai workspace with API key; MCP round-trip not file-level testable |
    | /orq-agent:quickstart sequential flow feels natural | LCMD-05, LCMD-07 | UX judgement on a fresh workspace |
    | /orq-agent:automations --create actually writes a rule | LCMD-06 | Requires a live POST to Orq.ai — not exercised by lint |
    ```

    **8. Sign-off block:**

    ```markdown
    ## Sign-off

    Phase 36 mechanically COMPLETE:
    - 8/8 plans closed
    - 7/7 LCMD requirements file-level verified
    - SKST lint green on all 21 command files (15 pre-existing + 6 new)
    - MSEL-02 snapshot-pinning lint green on models.md
    - Protected-pipeline 3/3 SHA-256 matches preserved
    - Cross-cutting wiring verified in SKILL.md + help.md

    Ready for `/gsd:verify-work 36` (3 manual smokes above).
    ```

    Do NOT modify any command file in this plan. Do NOT modify .planning/STATE.md. The file is the only artifact.
  </action>
  <verify>
    <automated>test -f .planning/phases/36-lifecycle-slash-commands/36-08-VERIFICATION.md &amp;&amp; grep -q "LCMD-01" .planning/phases/36-lifecycle-slash-commands/36-08-VERIFICATION.md &amp;&amp; grep -q "LCMD-07" .planning/phases/36-lifecycle-slash-commands/36-08-VERIFICATION.md &amp;&amp; grep -q "lint-skills.sh" .planning/phases/36-lifecycle-slash-commands/36-08-VERIFICATION.md &amp;&amp; grep -q "check-protected-pipelines.sh" .planning/phases/36-lifecycle-slash-commands/36-08-VERIFICATION.md &amp;&amp; grep -qi "roadmap" .planning/phases/36-lifecycle-slash-commands/36-08-VERIFICATION.md &amp;&amp; bash orq-agent/scripts/lint-skills.sh &amp;&amp; bash orq-agent/scripts/check-protected-pipelines.sh</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/36-lifecycle-slash-commands/36-08-VERIFICATION.md exists.
    - All 7 LCMD IDs (`LCMD-01` through `LCMD-07`) appear in the file.
    - Both lint scripts referenced by filename: `lint-skills.sh` AND `check-protected-pipelines.sh`.
    - A ROADMAP success-criteria checklist section is present (`grep -qi "roadmap"`).
    - Full-suite `bash orq-agent/scripts/lint-skills.sh` exits 0 at plan-run time (documented in the file's captured output).
    - `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0 at plan-run time.
    - File also contains: traceability table, inventory block, deferred-manual-verifications table, sign-off block.
  </acceptance_criteria>
  <done>36-08-VERIFICATION.md written with full captured output + 7-row LCMD traceability + 5-row ROADMAP checklist + inventory + deferred items + sign-off. SKST lint + protected-pipeline check both green. Phase 36 mechanically COMPLETE; ready for /gsd:verify-work 36.</done>
</task>

</tasks>

<verification>
- `bash orq-agent/scripts/lint-skills.sh` exits 0 (21 files: SKILL.md + 15 commands + 5 agents [subset lint scope]).
- `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0 (3/3 protected pipelines byte-identical).
- `test -f .planning/phases/36-lifecycle-slash-commands/36-08-VERIFICATION.md`.
- All 7 LCMD IDs appear in the verification file.
</verification>

<success_criteria>
Phase 36 verification sweep complete. Evidence trail committed. All 7 LCMD requirements file-level satisfied. Protected-pipeline invariant preserved. Manual LLM/UX/POST smokes enumerated for /gsd:verify-work 36 handoff.
</success_criteria>

<output>
After completion, create `.planning/phases/36-lifecycle-slash-commands/36-08-SUMMARY.md` using the standard summary template. The SUMMARY.md should reference 36-08-VERIFICATION.md as its primary evidence artifact.
</output>
