---
phase: 40-kb-memory-lifecycle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/agents/kb-generator.md
autonomous: true
requirements: [KBM-01, KBM-02, KBM-03]
must_haves:
  truths:
    - "kb-generator.md embeds chunking-strategy policy (sentence for prose, recursive for structured docs)"
    - "kb-generator.md emits chunking_strategy metadata in its output contract"
    - "kb-generator.md references retrieval quality test as a downstream invariant"
  artifacts:
    - path: orq-agent/agents/kb-generator.md
      provides: "Enhanced KB content generator with policy + metadata emission"
      contains: "chunking_strategy, sentence, recursive, retrieval quality"
  key_links:
    - from: orq-agent/agents/kb-generator.md
      to: orq-agent/commands/kb.md
      via: "Emitted metadata consumed by Step 7.1.5 / 7.6"
      pattern: "chunking_strategy"
---

<objective>
Enhance orq-agent/agents/kb-generator.md to embed the content-type-driven chunking policy (KBM-03) and emit `chunking_strategy` metadata in its output manifest. Reinforce KBM-01 (downstream retrieval test) and KBM-02 (upstream activation gate) as invariants in Constraints.

Purpose: The subagent already has Phase 40 Constraints bullets (lines 151-156) but lacks an explicit "Chunking Strategy Policy" section that spells out the detection rule + tells the subagent to emit `chunking_strategy` in its output contract so that kb.md Step 7.1.5 / 7.6 can consume it. Without this, kb-generator emits content but no metadata, forcing kb.md to re-detect content type.
Output: kb-generator.md with new "Chunking Strategy Policy" section + updated Output Format to emit metadata manifest.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-kb-memory-lifecycle/40-CONTEXT.md

@orq-agent/agents/kb-generator.md
@orq-agent/commands/kb.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Embed chunking policy + metadata emission in kb-generator.md</name>
  <files>orq-agent/agents/kb-generator.md</files>
  <action>
Read orq-agent/agents/kb-generator.md first. Preserve all 9 SKST sections, Approach A/B flows, and Output Format structure. Make these ADDITIVE edits:

1. **Insert new section "Chunking Strategy Policy (KBM-03)"** immediately after the existing "Output Format" section (after the "Output display:" block), before "## Constraints". Content:

   ```
   ## Chunking Strategy Policy (KBM-03)

   For every KB document you generate, pick a chunking strategy based on content type and emit it in the file manifest:

   - **Prose content type** (flowing text, narrative FAQ answers, policy paragraphs) → chunking strategy **sentence**, chunk_size 512, overlap 50.
   - **Structured content type** (dense heading hierarchy, tabular policy matrices, code snippets, JSON/YAML examples) → chunking strategy **recursive**, chunk_size 1024, overlap 100.

   **Heuristic:** Count H2/H3 headings per 1000 lines in your generated file. If density ≥ 5, it is structured; otherwise prose.

   **Structure your output to match the chosen strategy:**
   - Sentence strategy → flowing paragraphs with clear sentence boundaries; avoid bullet-heavy blocks that would fragment awkwardly at sentence-splits.
   - Recursive strategy → clear H2/H3 headings every 50-100 lines so the recursive splitter has natural boundaries.

   Record the decision in the file manifest (see Output Format below).
   ```

2. **Update the "Output display:" block** under the existing Output Format section to include a `chunking_strategy` column:

   ```
   KB Content Generated:

   | KB | Files | Chunking | Location |
   |----|-------|----------|----------|
   | {kb-name} | {N} files | {sentence\|recursive} | {swarm-dir}/kb-content/{kb-name}/ |

   Per-file manifest (written to `{swarm-dir}/kb-content/{kb-name}/manifest.json`):

       {
         "kb_name": "<kb-name>",
         "files": [
           {
             "path": "returns-and-refunds.md",
             "lines": 142,
             "content_type": "prose",
             "chunking_strategy": "sentence",
             "chunk_size": 512,
             "overlap": 50,
             "reason": "flowing policy paragraphs, H2 density < 5 per 1000 lines"
           }
         ]
       }

   This manifest is consumed by `/orq-agent:kb` Step 7.1.5 (chunking picker) and Step 7.6 (retrieval quality test).
   ```

3. **Append to the Done When checklist** (keep existing 6 items; add):
   - [ ] Per-file manifest.json written with `chunking_strategy` for every generated file
   - [ ] Retrieval quality downstream test (KBM-01) documented as invariant — this subagent does NOT run the test, but notes that `/orq-agent:kb` Step 7.6 will block wire-up on failure

4. **Update Constraints block** — the existing Phase 40 bullets (lines 151-156) are correct. Leave them. ADD one new bullet right after the existing ALWAYS bullets:

   - **ALWAYS** emit a `manifest.json` recording `chunking_strategy` per file so downstream retrieval quality testing (Phase 40 KBM-01) can reason about chunk boundaries.

5. **Append to Anti-Patterns** (keep existing rows; add):

   - **Do NOT emit KB content without a `manifest.json`.** The manifest carries the chunking decision; without it, the KB command cannot run KBM-03 picker validation and the retrieval quality test (KBM-01) has no baseline to compare against.

**Constraints (DO NOT violate):**
- Do NOT change frontmatter `tools: Read, Write, Bash, Glob, Grep` — no Agent dispatch from this subagent.
- Do NOT add `model:` YAML keys (this is a subagent, `model: inherit` already set).
- Preserve all existing Approach A / Approach B content verbatim.
- MSEL-02 snapshot-pinning does not apply here (no `model:` keys being added).

**Verify immediately after write:**

    bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/kb-generator.md
    grep -c "chunking_strategy" orq-agent/agents/kb-generator.md  # expect >= 3
    grep -c "sentence" orq-agent/agents/kb-generator.md           # expect >= 2
    grep -c "recursive" orq-agent/agents/kb-generator.md          # expect >= 2
    grep -c "retrieval quality" orq-agent/agents/kb-generator.md  # expect >= 2
    grep -c "manifest.json" orq-agent/agents/kb-generator.md      # expect >= 2
    bash orq-agent/scripts/check-protected-pipelines.sh            # 3/3 match
  </action>
  <verify>
    <automated>bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/kb-generator.md &amp;&amp; bash orq-agent/scripts/check-protected-pipelines.sh &amp;&amp; grep -q "chunking_strategy" orq-agent/agents/kb-generator.md &amp;&amp; grep -q "sentence" orq-agent/agents/kb-generator.md &amp;&amp; grep -q "recursive" orq-agent/agents/kb-generator.md &amp;&amp; grep -q "retrieval quality" orq-agent/agents/kb-generator.md &amp;&amp; grep -q "manifest.json" orq-agent/agents/kb-generator.md</automated>
  </verify>
  <done>kb-generator.md contains Chunking Strategy Policy (KBM-03) section, updated Output Format with manifest.json emission and chunking_strategy column, one new Constraints bullet, one new Anti-Pattern row, and 2 new Done When items. Lint passes; protected pipelines 3/3.</done>
</task>

</tasks>

<verification>
- Lint exit 0 on kb-generator.md
- All 5 grep anchors pass
- Protected pipelines SHA-256 3/3
</verification>

<success_criteria>
- Chunking policy embedded with content-type detection heuristic
- manifest.json emission documented in Output Format
- Backward compatibility preserved (existing Approach A/B flows untouched)
</success_criteria>

<output>
After completion, create `.planning/phases/40-kb-memory-lifecycle/40-02-SUMMARY.md`.
</output>
