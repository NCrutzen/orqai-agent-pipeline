---
phase: 40-kb-memory-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/commands/kb.md
autonomous: true
requirements: [KBM-01, KBM-02, KBM-03, KBM-04]
must_haves:
  truths:
    - "kb.md tests retrieval quality with sample queries after chunking and refuses deployment wire-up below threshold"
    - "kb.md verifies embedding model activation in AI Router before KB creation"
    - "kb.md picks chunking strategy from content type (sentence for prose, recursive for structured docs)"
    - "kb.md blocks misuse via KB-vs-Memory decision rule with exact lint-anchored phrasing"
    - "kb.md exposes --mode kb|memory to dispatch to memory-store-generator"
  artifacts:
    - path: orq-agent/commands/kb.md
      provides: "Enhanced KB command with KBM-01..04 steps + --mode flag"
      contains: "retrieval quality, embedding model, chunking strategy, KB-vs-Memory, --mode memory"
  key_links:
    - from: orq-agent/commands/kb.md
      to: orq-agent/agents/memory-store-generator.md
      via: "--mode memory dispatch step spawns the new subagent"
      pattern: "memory-store-generator"
    - from: orq-agent/commands/kb.md
      to: orq-agent/commands/kb/resources/
      via: "See resources/* for decision rules and test templates"
      pattern: "kb/resources/"
---

<objective>
Enhance orq-agent/commands/kb.md with the KBM-01..04 policy steps plus a --mode kb|memory dispatch.

Purpose: Users running /orq-agent:kb must (a) hit an embedding-activation gate, (b) receive a content-type-driven chunking choice, (c) pass a retrieval-quality test before wire-up, (d) be blocked when KB-vs-Memory misuse is detected, and (e) reach the new memory-store generator via a --mode flag without a separate command.
Output: Updated kb.md containing 5 new step insertions + KB-vs-Memory rule block + --mode dispatch + anti-pattern rows.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-kb-memory-lifecycle/40-CONTEXT.md
@.planning/phases/40-kb-memory-lifecycle/40-VALIDATION.md

@orq-agent/commands/kb.md
@orq-agent/agents/kb-generator.md
@orq-agent/scripts/lint-skills.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance kb.md with KBM-01..04 steps + --mode dispatch</name>
  <files>orq-agent/commands/kb.md</files>
  <action>
Read orq-agent/commands/kb.md first (Phase 34-conformant: 9 SKST sections, 10 Steps, banner "ORQ ► KB").

Make the following additive edits — PRESERVE all existing frontmatter, Constraints, When to use/NOT use, Companion Skills, Done When, Destructive Actions sections intact (already reference Phase 40 KBM-01..04). Keep banner verbatim.

1. **Frontmatter:** Add `Agent` dispatch coverage is already present. Confirm `allowed-tools` line includes `Agent` (already present). No frontmatter changes beyond adding a `--mode kb|memory` description line in the top prose after the "You are running..." paragraph:

   > This command supports `--mode kb` (default, full KB lifecycle) and `--mode memory` (dispatch to memory-store-generator subagent for memory store creation + round-trip test). A `--retrieval-threshold <N>` flag (default 70) configures the retrieval-quality pass bar for KBM-01.

2. **Insert new Step 0.5 (Mode Dispatch)** between Step 1 (Capability Gate) and Step 2 (Load API Key), OR as a sub-step of Step 1 right after the tier-gate passes. Use this exact heading and content:

   ```
   ## Step 1b: Mode Dispatch

   Parse the invocation flags:
   - `--mode kb` (default) → continue to Step 2 (full KB lifecycle: generate / provision / upload with KBM-01..04 gates).
   - `--mode memory` → dispatch to the `memory-store-generator` subagent (orq-agent/agents/memory-store-generator.md) with the captured context (swarm dir, agent slug). Skip Steps 2-10 of this command. The subagent handles memory store creation, wiring, and the read/write/recall round-trip test. Return to the user with the subagent's summary.
   - `--retrieval-threshold <N>` — optional integer 0-100, defaults to 70; passed to Step 7.6 retrieval quality test.

   **Invariant:** `--mode memory` ALWAYS routes to memory-store-generator; never inline memory logic in this command.
   ```

3. **Insert new Step 7.0 (Embedding Model Activation Check)** immediately BEFORE existing Step 7.1 (Embedding Model Picker). Heading:

   ```
   ### 7.0: Embedding Model Activation Check (KBM-02)

   Before presenting the embedding model picker, verify the user's activated embedding models in AI Router. Use MCP `list_models --type embedding` when available; fall back to REST `curl -s -H "Authorization: Bearer $ORQ_API_KEY" "$ORQ_BASE_URL/v2/models?type=embedding"`.

   Filter the picker options (Step 7.1) to show only activated embedding models. If the user's preferred model is NOT in the activated set, STOP with:

   ```
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ORQ ► KB — Embedding Model Not Activated
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   Requested embedding model `<model_id>` is not activated in AI Router.

   Remediation: Activate `<model_id>` in Orq.ai Studio → AI Router → Models → Embeddings before re-running this command.

   Alternatively, choose from activated models: <list>.
   ```

   **Invariant:** Embedding model is immutable after KB creation (existing Anti-Pattern row). Activation check MUST run before any `POST /v2/knowledge` call.
   ```

4. **Insert new Step 7.1.5 (Chunking Strategy Picker)** after Step 7.1 (Embedding Model Picker), before 7.2 (Host Selection). Heading:

   ```
   ### 7.1.5: Chunking Strategy Picker (KBM-03)

   For each KB, detect content type to pick the chunking strategy:

   - **Prose content type** — source files are `.md` with few H2s (< 5 per 1000 lines), `.txt`, `.pdf` → **sentence** chunker, `chunk_size: 512`, `overlap: 50`.
   - **Structured content type** — source files are `.md` with many H2s/H3s (≥ 5 per 1000 lines), `.html`, `.json`, or code (`.py`, `.ts`, `.js`) → **recursive** chunker, `chunk_size: 1024`, `overlap: 100`.

   Detection rule (bash):

       H2_COUNT=$(grep -cE '^##? ' "$source_file" 2>/dev/null || echo 0)
       LINE_COUNT=$(wc -l < "$source_file")
       H2_DENSITY=$(echo "scale=4; $H2_COUNT * 1000 / $LINE_COUNT" | bc 2>/dev/null)
       if [[ "$extension" =~ ^(html|json|py|ts|js)$ ]] || (( $(echo "$H2_DENSITY >= 5" | bc -l) )); then
         strategy="recursive"; chunk_size=1024; overlap=100; reason="structured"
       else
         strategy="sentence"; chunk_size=512; overlap=50; reason="prose"
       fi

   Record the choice in the KB plan (Step 7.4) and in KB metadata on creation:

       "metadata": {
         "chunking_strategy": "sentence",
         "chunk_size": 512,
         "overlap": 50,
         "reason": "prose"
       }
   ```

5. **Insert new Step 7.6 (Retrieval Quality Test)** immediately after existing Step 7.5 (Execute Provisioning), before existing Step 8. Heading:

   ```
   ### 7.6: Retrieval Quality Test (KBM-01)

   After chunking completes, run a retrieval quality test BEFORE wiring the KB to any deployment.

   1. **Generate 5-10 sample queries** from document headings in the uploaded content:
      - From heading `### Refund Policy` → query `How do I get a refund?`
      - From heading `## Billing FAQ` → query `Where do I update my credit card?`
      - Use an LLM (spec-generator model) to synthesize natural questions per heading.

   2. **Run each query** against the newly-created KB via MCP `search_entities` with `type=knowledge_chunks, knowledge_id=$KB_ID, query=$QUERY`. Fall back to REST `POST /v2/knowledge/$KB_ID/search` on MCP failure.

   3. **Pass criterion:** ≥ `--retrieval-threshold` (default 70) of queries return a chunk that semantically matches the intended source. Use an LLM-judge (binary Pass/Fail per query) or explicit user confirmation.

   4. **Refuse wire-up** on failure:

      ```
      ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
       ORQ ► KB — Retrieval Quality Test Failed
      ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

      Retrieval quality: <pct>% passed (threshold: <N>%).

      KB NOT wired to deployment.

      Remediation options:
        1. Reduce chunk_size (try 256 for sentence / 512 for recursive)
        2. Switch chunking strategy (sentence ↔ recursive)
        3. Re-ingest with cleaner source files (remove boilerplate)
        4. Lower threshold explicitly via --retrieval-threshold <N>
      ```

      Store test results under `{swarm-dir}/kb-content/{kb-name}/retrieval-test.json`.
   ```

6. **Insert new section "KB-vs-Memory Decision Rule"** immediately before "## Anti-Patterns". Heading and exact (lint-anchored) phrasing:

   ```
   ## KB-vs-Memory Decision Rule (KBM-04)

   - **KB (static reference data):** Docs, FAQs, product catalogs, policies, structured knowledge. Chunked + embedded + queried by similarity.
   - **Memory Store (dynamic user context):** Session history, preferences, per-user facts. Keyed + written at runtime by agent decisions.
   - **Block:** Memory for docs/FAQs → use KB. KB for conversation context → use Memory Store.

   When the user requests an action matching a blocked pattern (e.g., "Store product catalog in memory", "Use KB for last-N user turns", "Use memory for FAQ"), STOP and guide to the correct tool. Never silently coerce.
   ```

7. **Append new anti-pattern rows** to the existing Anti-Patterns table:

   | Pattern | Do Instead |
   |---------|-----------|
   | Storing product catalog / FAQ / policy docs in a memory store | Use a KB — static reference data is chunked + embedded + queried by similarity |
   | Using a KB for last-N conversation turns or per-user preferences | Use a memory store — dynamic user context is keyed + written at runtime |
   | Wiring a KB to a deployment without running the retrieval quality test | Run Step 7.6 first; refuse wire-up if the retrieval pass rate is below threshold |
   | Picking an embedding model before checking AI Router activation | Run Step 7.0 activation check first; the embedding model is immutable after KB creation |

8. **Update Companion Skills block** to add:
   - `→ memory-store-generator` subagent — invoked when `--mode memory` flag is passed

9. **Renumber downstream steps if needed** so the existing 10-step flow stays contiguous (7.0 → 7.1 → 7.1.5 → 7.2..7.5 → 7.6 → Step 8..10). Keep the existing Step numbers outside Step 7 unchanged.

**Constraints (DO NOT violate):**
- Do NOT modify the three protected pipelines (orq-agent.md / prompt.md / architect.md).
- Preserve banner `ORQ ► KB` verbatim.
- Keep 9 SKST sections (When to use, When NOT to use, Companion Skills, Done When, Constraints, Destructive Actions, Anti-Patterns, Open in orq.ai, Documentation & Resolution) — additions go under/within them.
- Use MCP-first-with-REST-fallback pattern (Phase 36 invariant) for every MCP call.
- Snapshot pinning (MSEL-02) applies only to `model:` YAML keys — prose examples are fine.

**Verify immediately after write:**

    bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/kb.md
    grep -c "retrieval quality" orq-agent/commands/kb.md  # expect >= 2
    grep -c "embedding model" orq-agent/commands/kb.md     # expect >= 2
    grep -c "chunking strategy" orq-agent/commands/kb.md   # expect >= 2
    grep -c "KB-vs-Memory" orq-agent/commands/kb.md        # expect >= 2
    grep -c "sentence" orq-agent/commands/kb.md            # expect >= 2
    grep -c "recursive" orq-agent/commands/kb.md           # expect >= 2
    grep -c -- "--mode memory" orq-agent/commands/kb.md    # expect >= 2
    bash orq-agent/scripts/check-protected-pipelines.sh    # 3/3 SHA-256 match
  </action>
  <verify>
    <automated>bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/kb.md &amp;&amp; bash orq-agent/scripts/check-protected-pipelines.sh &amp;&amp; grep -q "retrieval quality" orq-agent/commands/kb.md &amp;&amp; grep -q "embedding model" orq-agent/commands/kb.md &amp;&amp; grep -q "chunking strategy" orq-agent/commands/kb.md &amp;&amp; grep -q "KB-vs-Memory" orq-agent/commands/kb.md &amp;&amp; grep -q "sentence" orq-agent/commands/kb.md &amp;&amp; grep -q "recursive" orq-agent/commands/kb.md &amp;&amp; grep -q -- "--mode memory" orq-agent/commands/kb.md</automated>
  </verify>
  <done>kb.md contains Step 1b mode dispatch, Step 7.0 activation check, Step 7.1.5 chunking picker, Step 7.6 retrieval quality test, KB-vs-Memory decision rule block (with exact lint-anchored phrasing), 4 new anti-pattern rows, and --mode kb|memory + --retrieval-threshold flag description. Lint passes. 3/3 protected pipelines SHA-256 intact.</done>
</task>

</tasks>

<verification>
- `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/kb.md` → exit 0
- `bash orq-agent/scripts/check-protected-pipelines.sh` → 3/3 SHA-256 match
- All 7 grep anchors pass (retrieval quality, embedding model, chunking strategy, KB-vs-Memory, sentence, recursive, --mode memory)
</verification>

<success_criteria>
- kb.md enhanced with KBM-01..04 steps and --mode dispatch
- Exact KB-vs-Memory rule text present verbatim
- Protected pipelines invariant preserved (3/3 SHA-256)
- Lint exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/40-kb-memory-lifecycle/40-01-SUMMARY.md`.
</output>
