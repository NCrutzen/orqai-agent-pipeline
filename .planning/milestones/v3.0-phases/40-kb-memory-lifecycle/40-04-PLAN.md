---
phase: 40-kb-memory-lifecycle
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/commands/kb/resources/chunking-strategies.md
  - orq-agent/commands/kb/resources/kb-vs-memory.md
  - orq-agent/commands/kb/resources/retrieval-test-template.md
autonomous: true
requirements: [KBM-01, KBM-03, KBM-04]
must_haves:
  truths:
    - "Single-consumer resources directory exists at orq-agent/commands/kb/resources/"
    - "chunking-strategies.md documents sentence vs recursive decision rules"
    - "kb-vs-memory.md contains the exact KB-vs-Memory decision rule + anti-patterns"
    - "retrieval-test-template.md provides sample queries + pass criteria for KBM-01"
  artifacts:
    - path: orq-agent/commands/kb/resources/chunking-strategies.md
      provides: "Chunking strategy decision rules (KBM-03)"
      contains: "sentence, recursive, chunk_size, overlap"
    - path: orq-agent/commands/kb/resources/kb-vs-memory.md
      provides: "KB-vs-Memory decision rule + anti-patterns (KBM-04)"
      contains: "KB-vs-Memory, static reference data, dynamic user context"
    - path: orq-agent/commands/kb/resources/retrieval-test-template.md
      provides: "Retrieval test query templates + pass criteria (KBM-01)"
      contains: "retrieval quality, sample queries, 70%, threshold"
  key_links:
    - from: orq-agent/commands/kb.md
      to: orq-agent/commands/kb/resources/
      via: "Single-consumer resources per Phase 34 Resources Policy"
      pattern: "kb/resources/"
---

<objective>
Create 3 single-consumer resource files under `orq-agent/commands/kb/resources/` to park the long-form policy docs that would otherwise bloat kb.md.

Purpose: Phase 34 Resources Policy — single-consumer long-form docs live under `<skill>/resources/` not in flat `references/`. These 3 files carry the chunking decision rules, the KB-vs-Memory anti-patterns, and the retrieval test template that kb.md references by path.
Output: 3 resource markdown files. Freeform prose (single-level `commands/*.md` glob in lint-skills.sh auto-excludes `kb/resources/` per Phase 37 precedent).
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-kb-memory-lifecycle/40-CONTEXT.md

@orq-agent/commands/kb.md
@orq-agent/commands/observability/resources/openai-sdk.md
@orq-agent/commands/trace-failure-analysis/resources/grounded-theory-methodology.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 3 resource files under kb/resources/</name>
  <files>orq-agent/commands/kb/resources/chunking-strategies.md, orq-agent/commands/kb/resources/kb-vs-memory.md, orq-agent/commands/kb/resources/retrieval-test-template.md</files>
  <action>
Create the directory `orq-agent/commands/kb/resources/` and write 3 files. Reference the precedent in `orq-agent/commands/observability/resources/` and `orq-agent/commands/trace-failure-analysis/resources/` — freeform markdown prose, no SKST required (single-consumer resource, not a skill file).

### File 1: `orq-agent/commands/kb/resources/chunking-strategies.md`

Content outline:

    # Chunking Strategies (KBM-03)

    Single-consumer resource for `/orq-agent:kb` and `kb-generator` subagent. Documents the content-type-driven chunking decision rule.

    ## Decision Rule

    | Content Type | Indicators | Strategy | Chunk Size | Overlap | Reason |
    |--------------|-----------|----------|------------|---------|--------|
    | Prose | `.md` with few H2s (< 5 per 1000 lines), `.txt`, `.pdf` narrative | **sentence** | 512 | 50 | Preserves sentence boundaries; retrieval returns complete thoughts |
    | Structured | `.md` with many H2s/H3s (≥ 5 per 1000 lines), `.html`, `.json`, code (`.py`, `.ts`, `.js`) | **recursive** | 1024 | 100 | Respects heading/block boundaries; retrieval returns coherent sections |

    ## Detection Heuristic (bash)

        H2_COUNT=$(grep -cE '^##? ' "$source_file" 2>/dev/null || echo 0)
        LINE_COUNT=$(wc -l < "$source_file")
        DENSITY=$(echo "scale=4; $H2_COUNT * 1000 / $LINE_COUNT" | bc)
        if [[ "$ext" =~ ^(html|json|py|ts|js)$ ]] || (( $(echo "$DENSITY >= 5" | bc -l) )); then
          strategy="recursive"
        else
          strategy="sentence"
        fi

    ## Why These Defaults

    - **Sentence 512/50:** Cohere + OpenAI embedding models have 512-token sweet spot for prose recall; 50 overlap preserves cross-sentence context.
    - **Recursive 1024/100:** Structured docs benefit from larger chunks that capture full heading blocks; higher overlap prevents boundary cuts at H3 transitions.

    ## Override Guidance

    Users can override via `--chunking <strategy>:<size>:<overlap>` flag on `/orq-agent:kb`. Override is logged in KB metadata with `reason: "user-override"`.

    ## Examples

    - **Policies handbook** (`.md`, 15 H2s in 3000 lines) → density 5.0 → **recursive**, 1024/100
    - **FAQ page** (`.md`, 3 H2s in 1500 lines) → density 2.0 → **sentence**, 512/50
    - **API reference** (`.json`) → structured by extension → **recursive**, 1024/100
    - **Product brochure** (`.pdf`) → prose by extension → **sentence**, 512/50

    ## Related

    - `/orq-agent:kb` Step 7.1.5 (chunking picker)
    - `orq-agent/agents/kb-generator.md` "Chunking Strategy Policy" section
    - `retrieval-test-template.md` (next file — chunking directly impacts retrieval quality)

### File 2: `orq-agent/commands/kb/resources/kb-vs-memory.md`

Content outline:

    # KB vs Memory Decision Rule (KBM-04)

    Single-consumer resource for `/orq-agent:kb` and `memory-store-generator` subagent. Documents the decision rule that prevents docs-in-memory and conversation-in-KB anti-patterns.

    ## The Rule (Lint-Anchored — Exact Phrasing)

    - **KB (static reference data):** Docs, FAQs, product catalogs, policies, structured knowledge. Chunked + embedded + queried by similarity.
    - **Memory Store (dynamic user context):** Session history, preferences, per-user facts. Keyed + written at runtime by agent decisions.
    - **Block:** Memory for docs/FAQs → use KB. KB for conversation context → use Memory Store.

    ## Decision Matrix

    | User Intent | Correct Tool | Rationale |
    |-------------|--------------|-----------|
    | "Store our return policy so the agent can cite it" | **KB** | Static reference, chunkable, retrieval-by-similarity |
    | "Remember this user's shipping address across sessions" | **Memory Store** | Dynamic per-user, keyed write, runtime update |
    | "Give the agent access to our product catalog" | **KB** | Static reference, chunkable by SKU/category |
    | "Track what this user asked about last session" | **Memory Store** | Dynamic per-user, temporal context |
    | "Store FAQ content" | **KB** | Static reference; common mistake to put in memory |
    | "Keep last-N conversation turns for the LLM" | **Memory Store** | Dynamic per-session; common mistake to put in KB |

    ## Blocked Patterns

    These requests MUST trigger a STOP + redirect in the slash command / subagent:

    1. "Store product catalog in memory" → Block. Redirect to `/orq-agent:kb --mode kb`.
    2. "Use KB for last-N user turns" → Block. Redirect to `/orq-agent:kb --mode memory`.
    3. "Use memory for FAQ" → Block. Redirect to `/orq-agent:kb --mode kb`.
    4. "Store user preferences as a KB" → Block. Redirect to `/orq-agent:kb --mode memory`.

    ## Why This Matters

    KBs are chunked + embedded at ingestion time. Writing user-specific data into a KB means every new datapoint triggers a re-embedding cycle — prohibitively expensive.

    Memory stores have no similarity search. Putting docs into memory forces exact-key lookups, which means the LLM has to guess the right key instead of querying by natural language.

    The rule is not stylistic — violating it breaks either cost or retrieval quality.

    ## Related

    - `/orq-agent:kb` "KB-vs-Memory Decision Rule" section
    - `orq-agent/agents/memory-store-generator.md` embedded rule
    - `orq-agent/agents/kb-generator.md` Constraints (NEVER use memory-style stores for static reference data)

### File 3: `orq-agent/commands/kb/resources/retrieval-test-template.md`

Content outline:

    # Retrieval Quality Test Template (KBM-01)

    Single-consumer resource for `/orq-agent:kb` Step 7.6. Provides sample queries and pass criteria for the post-chunking retrieval quality test that gates wire-up.

    ## Pass Criterion

    **Default threshold:** 70% of sample queries return a chunk that semantically matches the intended source document.

    **Configurable:** `--retrieval-threshold <N>` flag on `/orq-agent:kb` (integer 0-100).

    **Judgment method:** LLM-judge with binary Pass/Fail per query, OR explicit user confirmation in interactive mode.

    ## Sample Query Generation

    Generate 5-10 natural questions per KB from document headings. Examples:

    | Source Heading | Generated Query |
    |----------------|-----------------|
    | `### Refund Policy` | "How do I get a refund?" |
    | `## Billing FAQ` | "Where do I update my credit card?" |
    | `### Shipping Times` | "When will my order arrive?" |
    | `## Account Setup` | "How do I create an account?" |
    | `### Password Reset` | "I forgot my password, what now?" |
    | `## API Rate Limits` | "How many requests can I make per minute?" |

    Use a capable LLM (the project's current spec-generator model) to synthesize the queries. Prompt shape:

        Given the heading "{heading}" from a {kb_name} document, generate ONE natural question a user would ask whose answer would be found in that section. Keep it to 10 words or fewer. Do not include the heading text literally.

    ## Test Execution

    For each query:

    1. Call MCP `search_entities` with `type=knowledge_chunks, knowledge_id=$KB_ID, query=$QUERY`. Fall back to REST `POST /v2/knowledge/$KB_ID/search`.
    2. Take the top-1 chunk returned.
    3. LLM-judge: "Does this chunk answer the query `{query}`? Respond PASS or FAIL only."
    4. Record result.

    ## Failure Remediation (Surface on Threshold Miss)

    ```
    ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
     ORQ ► KB — Retrieval Quality Test Failed
    ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    Retrieval quality: <X>% passed (threshold: <N>%).

    KB NOT wired to deployment.

    Remediation options:
      1. Reduce chunk_size (try 256 for sentence / 512 for recursive)
      2. Switch chunking strategy (sentence ↔ recursive)
      3. Re-ingest with cleaner source files (remove boilerplate)
      4. Lower threshold explicitly via --retrieval-threshold <N>
    ```

    ## Output Format

    Store results at `{swarm-dir}/kb-content/{kb-name}/retrieval-test.json`:

        {
          "kb_name": "<kb-name>",
          "threshold": 70,
          "passed": 8,
          "failed": 2,
          "pass_rate": 0.80,
          "queries": [
            {"query": "How do I get a refund?", "top_chunk": "...", "judge": "PASS"},
            ...
          ],
          "wire_up": "allowed" | "refused"
        }

    ## Related

    - `/orq-agent:kb` Step 7.6 (retrieval quality test)
    - `chunking-strategies.md` (chunk choice directly impacts retrieval quality)
    - `kb-vs-memory.md` (retrieval test only applies to KBs, not memory stores)

---

**Constraints (DO NOT violate):**
- Single-consumer invariant — these 3 files are ONLY consumed by kb.md + kb-generator.md + memory-store-generator.md (all under the `kb` skill umbrella).
- Do NOT add these to `orq-agent/references/` — they would fail the references-multi-consumer lint rule.
- No model: YAML keys (MSEL-02 clean by construction — prose only).
- Do NOT touch the 3 protected pipelines.

**Verify immediately after write:**

    test -d orq-agent/commands/kb/resources
    test -f orq-agent/commands/kb/resources/chunking-strategies.md
    test -f orq-agent/commands/kb/resources/kb-vs-memory.md
    test -f orq-agent/commands/kb/resources/retrieval-test-template.md
    grep -q "sentence" orq-agent/commands/kb/resources/chunking-strategies.md
    grep -q "recursive" orq-agent/commands/kb/resources/chunking-strategies.md
    grep -q "KB-vs-Memory\|KB vs Memory" orq-agent/commands/kb/resources/kb-vs-memory.md
    grep -q "static reference data" orq-agent/commands/kb/resources/kb-vs-memory.md
    grep -q "dynamic user context" orq-agent/commands/kb/resources/kb-vs-memory.md
    grep -q "retrieval quality" orq-agent/commands/kb/resources/retrieval-test-template.md
    grep -q "threshold" orq-agent/commands/kb/resources/retrieval-test-template.md
    bash orq-agent/scripts/lint-skills.sh       # full suite (kb/resources/ auto-excluded by commands/*.md single-level glob)
    bash orq-agent/scripts/check-protected-pipelines.sh  # 3/3
  </action>
  <verify>
    <automated>test -f orq-agent/commands/kb/resources/chunking-strategies.md &amp;&amp; test -f orq-agent/commands/kb/resources/kb-vs-memory.md &amp;&amp; test -f orq-agent/commands/kb/resources/retrieval-test-template.md &amp;&amp; grep -q "sentence" orq-agent/commands/kb/resources/chunking-strategies.md &amp;&amp; grep -q "recursive" orq-agent/commands/kb/resources/chunking-strategies.md &amp;&amp; grep -q "static reference data" orq-agent/commands/kb/resources/kb-vs-memory.md &amp;&amp; grep -q "dynamic user context" orq-agent/commands/kb/resources/kb-vs-memory.md &amp;&amp; grep -q "retrieval quality" orq-agent/commands/kb/resources/retrieval-test-template.md &amp;&amp; bash orq-agent/scripts/lint-skills.sh &amp;&amp; bash orq-agent/scripts/check-protected-pipelines.sh</automated>
  </verify>
  <done>Directory orq-agent/commands/kb/resources/ exists with 3 files. All grep anchors pass. Full-suite lint still green (resources/ auto-excluded). Protected pipelines 3/3.</done>
</task>

</tasks>

<verification>
- 3 resource files exist with correct anchors
- Full-suite lint exit 0
- Protected pipelines SHA-256 3/3
</verification>

<success_criteria>
- Single-consumer resources directory created per Phase 34 policy
- All 3 files carry exact lint-anchored phrasing needed for downstream plans 05/06
</success_criteria>

<output>
After completion, create `.planning/phases/40-kb-memory-lifecycle/40-04-SUMMARY.md`.
</output>
