---
phase: 37-observability-setup-skill
plan: 04
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - orq-agent/SKILL.md
  - orq-agent/commands/help.md
autonomous: true
requirements: [OBSV-01, OBSV-02, OBSV-03, OBSV-04, OBSV-05, OBSV-06, OBSV-07]
must_haves:
  truths:
    - "orq-agent/SKILL.md lists observability.md in the directory tree and in a new Phase 37 commands table"
    - "orq-agent/SKILL.md Resources Policy section acknowledges orq-agent/commands/observability/resources/ as the first live per-skill resources directory"
    - "orq-agent/commands/help.md lists /orq-agent:observability in the pipeline-order Commands block, positioned near onboarding"
    - "Neither SKILL.md nor help.md touches the 3 protected entry points (orq-agent.md / prompt.md / architect.md)"
    - "Both files still pass per-file SKST lint"
  artifacts:
    - path: "orq-agent/SKILL.md"
      provides: "Index wiring for /orq-agent:observability"
      contains: "observability.md"
    - path: "orq-agent/commands/help.md"
      provides: "Pipeline-order help listing for /orq-agent:observability"
      contains: "orq-agent:observability"
  key_links:
    - from: "orq-agent/SKILL.md Directory Structure"
      to: "orq-agent/commands/observability.md + orq-agent/commands/observability/resources/"
      via: "Directory tree + Phase 37 commands table rows"
      pattern: "observability"
    - from: "orq-agent/commands/help.md Step 2 Commands block"
      to: "orq-agent:observability slash command"
      via: "Literal line in the help banner output"
      pattern: "orq-agent:observability"
---

<objective>
Wire the new `/orq-agent:observability` command into the two index surfaces that drive discoverability: `orq-agent/SKILL.md` (the suite index) and `orq-agent/commands/help.md` (the user-facing help banner). This is a pure index-wiring plan — no behavior changes to any command.

Purpose: Every V3.0 phase that adds commands must touch exactly two files (SKILL.md + help.md), never the 3 protected entry points (locked in Phase 36 decision log). Phase 37 follows that recipe.
Output: Two edited files; both still lint-green.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-observability-setup-skill/37-CONTEXT.md
@orq-agent/SKILL.md
@orq-agent/commands/help.md
@orq-agent/scripts/lint-skills.sh

<interfaces>
From orq-agent/SKILL.md:
- Directory Structure block (lines ~53-106) — `commands/` subtree lists every slash-command file. Add `observability.md` under the Phase 36 commands group (or a new Phase 37 group).
- Phase 36 commands table sits under `### Phase 36 (Lifecycle Slash Commands)` — Phase 37 gets its own H3 `### Phase 37 (Observability)` AFTER the Phase 36 block (preserves historical phase grouping per Phase 36 decision log 2026-04-20).
- Resources Policy section (lines ~302-311) — UPDATE to note `orq-agent/commands/observability/resources/` as the first live per-skill resources directory (migration status line).

From orq-agent/commands/help.md:
- Step 2 Display Help block contains a literal "Commands:" listing (lines ~68-89). Insert `/orq-agent:observability` near the onboarding cluster — right AFTER `/orq-agent:quickstart` and BEFORE `/orq-agent:automations` keeps the pipeline-order mental model (discovery → onboarding → observability → governance).

Protected files (MUST NOT modify): orq-agent/commands/orq-agent.md, orq-agent/commands/prompt.md, orq-agent/commands/architect.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire observability.md into SKILL.md and help.md index surfaces</name>
  <files>orq-agent/SKILL.md, orq-agent/commands/help.md</files>
  <read_first>
    - orq-agent/SKILL.md (Directory Structure + Phase 36 commands table + Resources Policy)
    - orq-agent/commands/help.md (Step 2 Commands listing)
    - orq-agent/scripts/check-protected-pipelines.sh (confirm which files are protected)
    - .planning/phases/37-observability-setup-skill/37-CONTEXT.md (integration points)
  </read_first>
  <action>
**Edit A — orq-agent/SKILL.md Directory Structure block:**

In the `commands/` subtree, AFTER the line:
```
    automations.md               # Phase 36: Trace Automations list + create (LCMD-06)
```

INSERT:
```
    observability.md             # Phase 37: LLM instrumentation setup (OBSV-01..07)
    observability/
      resources/
        openai-sdk.md            # Phase 37: OpenAI SDK integration snippet
        langchain.md             # Phase 37: LangChain integration snippet
        crewai.md                # Phase 37: CrewAI integration snippet
        vercel-ai.md             # Phase 37: Vercel AI SDK integration snippet
        generic-otel.md          # Phase 37: Generic OpenTelemetry snippet
```

**Edit B — orq-agent/SKILL.md — add Phase 37 commands table AFTER the Phase 36 block:**

After the last row of the `### Phase 36 (Lifecycle Slash Commands)` table (the `/orq-agent:automations` row), INSERT a blank line then:

```markdown
### Phase 37 (Observability)

| Command | File | Tier Required | Purpose |
|---------|------|---------------|---------|
| `/orq-agent:observability` | `commands/observability.md` | any | Instrument LLM application for Orq.ai trace capture -- framework detection (OBSV-01), mode recommendation AI Router / OTEL / both (OBSV-02), integration codegen with instrumentors-before-SDK ordering (OBSV-03), baseline verification script with PII scan (OBSV-04), trace enrichment session_id / user_id / customer_id (OBSV-05), @traced decorators for 6 span types agent/llm/tool/retrieval/embedding/function (OBSV-06), per-tenant identity attribution + filter via `/orq-agent:traces --identity` (OBSV-07) |
```

**Edit C — orq-agent/SKILL.md — update Resources Policy migration status:**

Find the paragraph:
```
**Migration status:** No existing references qualify for migration (all 8 have ≥2 consumers). Phases 36-43 will create per-skill `resources/` directories on demand when single-consumer content appears.
```

REPLACE with:
```
**Migration status:** No existing references qualify for migration (all 8 have ≥2 consumers). Phase 37 established the first live per-skill resources directory at `orq-agent/commands/observability/resources/` (5 framework snippets consumed only by `observability.md`). Phases 38-43 will create additional per-skill `resources/` directories on demand when single-consumer content appears.
```

**Edit D — orq-agent/commands/help.md — insert `/orq-agent:observability` in the Commands block:**

In the Step 2 Display Help block, find the line:
```
  /orq-agent:quickstart        12-step onboarding tour
```

INSERT on the line AFTER it (so the ordering becomes quickstart → observability → automations):
```
  /orq-agent:observability     Instrument LLM app for trace capture (framework detect + codegen)
```

(Keep the 2-space left-pad / column alignment — the description column starts at the same column as sibling rows.)

**Edit E — orq-agent/commands/help.md — Companion Skills & Anti-Patterns update (optional but recommended):**

No edit required IF help.md does not enumerate commands outside Step 2 (it does not — verified in the file read). Leave the rest of help.md byte-identical.

**Post-condition checks:**
- `grep -c 'observability' orq-agent/SKILL.md` returns ≥ 8 (directory tree lines + Phase 37 table row mentions + resources-policy update).
- `grep -q 'orq-agent:observability' orq-agent/commands/help.md` succeeds.
- `bash orq-agent/scripts/lint-skills.sh --file orq-agent/SKILL.md` exits 0.
- `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/help.md` exits 0.
- `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0 (SHA-256 3/3 — orq-agent.md / prompt.md / architect.md untouched).
  </action>
  <verify>
    <automated>grep -q 'observability.md' orq-agent/SKILL.md && grep -q 'Phase 37 (Observability)' orq-agent/SKILL.md && grep -q 'OBSV-01..07\|OBSV-07' orq-agent/SKILL.md && [ "$(grep -c 'observability' orq-agent/SKILL.md)" -ge 8 ] && grep -q 'orq-agent:observability' orq-agent/commands/help.md && bash orq-agent/scripts/lint-skills.sh --file orq-agent/SKILL.md && bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/help.md && bash orq-agent/scripts/check-protected-pipelines.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'observability.md' orq-agent/SKILL.md` succeeds (directory tree entry)
    - `grep -q 'Phase 37 (Observability)' orq-agent/SKILL.md` succeeds (new H3 table)
    - `grep -c 'observability' orq-agent/SKILL.md` ≥ 8 (covers tree + table + resources-policy update)
    - `grep -q 'orq-agent:observability' orq-agent/commands/help.md` succeeds
    - `grep -q 'Instrument LLM app' orq-agent/commands/help.md` succeeds (new description column)
    - Per-file lint on both SKILL.md and help.md exits 0
    - Protected pipelines SHA-256 check returns 3/3 matches (unchanged)
    - Neither `orq-agent/commands/orq-agent.md`, `orq-agent/commands/prompt.md`, nor `orq-agent/commands/architect.md` is in the diff
  </acceptance_criteria>
  <done>SKILL.md and help.md both list `/orq-agent:observability`; Phase 37 follows the locked index-wiring recipe (touch exactly 2 files, never the 3 protected entry points); both files pass SKST lint; protected-pipeline hash unchanged.</done>
</task>

</tasks>

<verification>
Per-file lint: `bash orq-agent/scripts/lint-skills.sh --file orq-agent/SKILL.md` exits 0.
Per-file lint: `bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/help.md` exits 0.
Protected pipelines: `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0.
</verification>

<success_criteria>
- `/orq-agent:observability` appears in both index surfaces with correct Phase 37 attribution.
- Resources Policy migration status updated to reflect first live per-skill resources directory.
- 3 protected entry points remain byte-identical (SHA-256 check 3/3).
</success_criteria>

<output>
Create `.planning/phases/37-observability-setup-skill/37-04-SUMMARY.md` recording: 5 edit locations applied (A-D, E skipped as no-op), grep counts, lint + protected-pipeline exit codes, confirmation that orq-agent.md / prompt.md / architect.md are untouched.
</output>
