---
phase: 41-prompt-optimization-cross-framework-comparison
plan: 04
type: execute
wave: 2
depends_on: [41-01, 41-02, 41-03]
files_modified:
  - orq-agent/SKILL.md
  - orq-agent/commands/help.md
autonomous: true
requirements: [POPT-01, POPT-02, POPT-03, POPT-04, XFRM-01, XFRM-02, XFRM-03]
must_haves:
  truths:
    - "SKILL.md registers the 2 new commands under a new ### Phase 41 H3 block, updates Directory Structure with both resources/ subdirs, and updates Migration status"
    - "help.md lists both new commands under a new Phase 41 section in the Commands help banner, with flag summary for compare-frameworks"
    - "Protected pipelines (orq-agent.md, prompt.md, architect.md) remain byte-identical"
  artifacts:
    - path: "orq-agent/SKILL.md"
      provides: "Index registration for Phase 41 commands and resources"
      contains: "Phase 41"
    - path: "orq-agent/commands/help.md"
      provides: "Help banner listing new commands with flags"
      contains: "prompt-optimization"
  key_links:
    - from: "orq-agent/SKILL.md Directory Structure"
      to: "orq-agent/commands/prompt-optimization/resources/ and orq-agent/commands/compare-frameworks/resources/"
      via: "tree-block entries"
      pattern: "prompt-optimization/|compare-frameworks/"
---

<objective>
Wire the 2 new Phase 41 commands and their resources into `orq-agent/SKILL.md` index and `orq-agent/commands/help.md` banner. Follows the locked V3.0 index-wiring recipe: edit exactly SKILL.md + help.md, never touch orq-agent.md / prompt.md / architect.md (protected pipeline SHA-256 guard).
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-prompt-optimization-cross-framework-comparison/41-CONTEXT.md

# Index targets
@orq-agent/SKILL.md
@orq-agent/commands/help.md

# Pattern precedents for Phase N H3 block placement
# (SKILL.md ### Phase 36/37/38/39/40 blocks — follow same shape)

# Protected pipelines (DO NOT MODIFY)
# @orq-agent/commands/orq-agent.md
# @orq-agent/commands/prompt.md
# @orq-agent/commands/architect.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Phase 41 into orq-agent/SKILL.md</name>
  <files>orq-agent/SKILL.md</files>
  <action>
Edit `orq-agent/SKILL.md`:

1. **Directory Structure block:** under `commands/` tree, add entries after the Phase 40 `kb/` block:
   ```
       prompt-optimization.md       # Phase 41: Prompt optimization skill (POPT-01..04)
       prompt-optimization/
         resources/
           11-guidelines.md         # Phase 41: 11-guideline rubric (POPT-02)
           rewrite-examples.md      # Phase 41: before/after rewrite exemplars (POPT-03)
       compare-frameworks.md        # Phase 41: Cross-framework comparison skill (XFRM-01..03)
       compare-frameworks/
         resources/
           evaluatorq-script-templates.md  # Phase 41: Python + TS evaluatorq scaffolds (XFRM-01)
           framework-adapters.md           # Phase 41: per-framework adapter sketches (XFRM-01)
   ```

2. **New H3 section:** insert AFTER the existing `### Phase 40 (KB & Memory Lifecycle)` H3 block and its requirement-coverage sub-block. The new block:

   ```markdown
   ### Phase 41 (Prompt Optimization & Cross-Framework Comparison)

   | Command | File | Tier Required | Purpose |
   |---------|------|---------------|---------|
   | `/orq-agent:prompt-optimization` | `commands/prompt-optimization.md` | deploy+ | Analyze a prompt against the 11-guideline framework (role, task, stress, guidelines, output-format, tool-calling, reasoning, examples, unnecessary-content, variable-usage, recap); preserve `{{variable}}` literally; produce ≤5 suggestions (POPT-02); diff + AskUserQuestion approval (POPT-03); publish rewrite as a new orq.ai version preserving the original (POPT-04); recommend `/orq-agent:test` for A/B validation |
   | `/orq-agent:compare-frameworks` | `commands/compare-frameworks.md` | deploy+ | Generate an `evaluatorq` comparison script (Python or TypeScript via `--lang python|ts`) benchmarking the same agent across orq.ai, LangGraph, CrewAI, OpenAI Agents SDK, and Vercel AI SDK (XFRM-01); enforce fairness (same dataset / evaluators / model unless `--isolate-model`) (XFRM-02); smoke-invoke each adapter before the full run; shared `experiment_id` surfaces side-by-side in Experiment UI (XFRM-03) |

   **`/orq-agent:prompt-optimization` requirement coverage (Phase 41):**

   - POPT-01 — Prompt fetch (inline text or orq.ai prompt key); `{{variable}}` scan preserves every placeholder literally before any rewrite.
   - POPT-02 — 11-guideline analysis (role, task, stress, guidelines, output-format, tool-calling, reasoning, examples, unnecessary-content, variable-usage, recap); ≤5 suggestions per run.
   - POPT-03 — Rewrite + side-by-side diff + AskUserQuestion approval gate; no mutation without explicit "yes".
   - POPT-04 — New version published via MCP `create_prompt_version` (REST fallback `POST /v2/prompts/{key}/versions`); original preserved; `/orq-agent:test` recommended.

   **`/orq-agent:compare-frameworks` requirement coverage (Phase 41):**

   - XFRM-01 — evaluatorq script emitted in Python or TypeScript (user picks via `--lang python|ts`); exactly one job per framework across orq.ai, LangGraph, CrewAI, OpenAI Agents SDK, Vercel AI SDK.
   - XFRM-02 — Fairness checks (same dataset, same evaluator set, same model unless `--isolate-model`); fails fast with "Fairness check failed:" block on violation.
   - XFRM-03 — Per-framework smoke-invocation precheck before full run; shared `experiment_id` across all 5 jobs surfaces results side-by-side in the orq.ai Experiment UI.

   Resource docs under `orq-agent/commands/prompt-optimization/resources/` (11-guidelines, rewrite-examples) are consumed only by `commands/prompt-optimization.md`; resource docs under `orq-agent/commands/compare-frameworks/resources/` (evaluatorq-script-templates, framework-adapters) are consumed only by `commands/compare-frameworks.md` (single-consumer per Resources Policy below).
   ```

3. **Resources Policy — Migration status paragraph:** append a sentence noting Phase 41 adds the 5th and 6th per-skill resources directories at `orq-agent/commands/prompt-optimization/resources/` and `orq-agent/commands/compare-frameworks/resources/`.

4. **DO NOT** modify any SKST sections of orq-agent.md, prompt.md, or architect.md. Only SKILL.md is touched here.
  </action>
  <verify>
    <automated>grep -q '### Phase 41' orq-agent/SKILL.md &amp;&amp; grep -q '/orq-agent:prompt-optimization' orq-agent/SKILL.md &amp;&amp; grep -q '/orq-agent:compare-frameworks' orq-agent/SKILL.md &amp;&amp; grep -q 'prompt-optimization/' orq-agent/SKILL.md &amp;&amp; grep -q 'compare-frameworks/' orq-agent/SKILL.md &amp;&amp; grep -q 'OpenAI Agents SDK' orq-agent/SKILL.md &amp;&amp; grep -q 'Vercel AI SDK' orq-agent/SKILL.md &amp;&amp; bash orq-agent/scripts/check-protected-pipelines.sh</automated>
  </verify>
  <done>
SKILL.md contains Phase 41 H3 block, both new commands, both resources subdirs in Directory Structure, 5 framework names referenced; protected-pipeline SHA-256 3/3 still green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Phase 41 into orq-agent/commands/help.md</name>
  <files>orq-agent/commands/help.md</files>
  <action>
Edit `orq-agent/commands/help.md`:

1. **Commands help banner (inside the code fence under Step 2):** insert two new lines after the `/orq-agent:automations` line and before `/orq-agent:help`. Maintain pipeline-order (optimize after observe/analyze):

   ```
     /orq-agent:prompt-optimization      Optimize a prompt against 11-guideline framework (deploy+ tier)
                                         (fetches prompt, preserves {{variable}}, max 5 suggestions, diff + approval, new version)
     /orq-agent:compare-frameworks       Cross-framework benchmark via evaluatorq (deploy+ tier)
                                         (--lang python|ts, --isolate-model, --dataset, --evaluators, --model)
   ```

2. **Done When checklist:** the existing "Every command registered under `/orq-agent` or `/orq-agent:*` appears in the Commands list" already covers this — no edit needed.

3. Preserve existing formatting, spacing, banner alignment.
  </action>
  <verify>
    <automated>grep -q 'prompt-optimization' orq-agent/commands/help.md &amp;&amp; grep -q 'compare-frameworks' orq-agent/commands/help.md &amp;&amp; grep -q -- '--lang python|ts' orq-agent/commands/help.md &amp;&amp; grep -q -- '--isolate-model' orq-agent/commands/help.md &amp;&amp; bash orq-agent/scripts/lint-skills.sh --file orq-agent/commands/help.md &amp;&amp; bash orq-agent/scripts/check-protected-pipelines.sh</automated>
  </verify>
  <done>
help.md lists both new commands with flag summaries; lint green on help.md; protected-pipeline SHA-256 3/3 still green.
  </done>
</task>

</tasks>

<verification>
- Phase 41 H3 block present in SKILL.md after Phase 40 block.
- Both resources subdirs present in Directory Structure.
- 2 new commands present in help.md banner with `--lang python|ts` + `--isolate-model` flags.
- Protected pipelines (orq-agent.md, prompt.md, architect.md) SHA-256 3/3 intact.
</verification>

<success_criteria>
Index wiring complete; suite-level Phase 41 surface discoverable; protected pipelines untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/41-prompt-optimization-cross-framework-comparison/41-04-SUMMARY.md`.
</output>
