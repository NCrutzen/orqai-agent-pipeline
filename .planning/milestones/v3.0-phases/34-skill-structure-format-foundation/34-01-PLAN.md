---
phase: 34-skill-structure-format-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - orq-agent/scripts/lint-skills.sh
  - orq-agent/scripts/check-protected-pipelines.sh
  - orq-agent/scripts/README.md
  - .planning/phases/34-skill-structure-format-foundation/golden/orq-agent.sha256
  - .planning/phases/34-skill-structure-format-foundation/golden/prompt.sha256
  - .planning/phases/34-skill-structure-format-foundation/golden/architect.sha256
autonomous: true
requirements:
  - SKST-01
  - SKST-02
  - SKST-03
  - SKST-04
  - SKST-05
  - SKST-06
  - SKST-07
  - SKST-08
  - SKST-09
  - SKST-10
must_haves:
  truths:
    - "Running `bash orq-agent/scripts/lint-skills.sh --help` prints usage and exits 0"
    - "Running `bash orq-agent/scripts/check-protected-pipelines.sh --baseline` writes 3 SHA-256 files into `.planning/phases/34-skill-structure-format-foundation/golden/`"
    - "Running `bash orq-agent/scripts/check-protected-pipelines.sh` (no args) exits 0 against the freshly-captured baseline"
    - "Running `bash orq-agent/scripts/lint-skills.sh` (no args) reports current repo state across all 33 skill files without crashing — failures on missing sections are expected pre-Plan 02/03/04"
  artifacts:
    - path: "orq-agent/scripts/lint-skills.sh"
      provides: "POSIX lint for 10 SKST rules with --rule, --file, --files flags"
      contains: "REQUIRED_SECTIONS"
    - path: "orq-agent/scripts/check-protected-pipelines.sh"
      provides: "Golden SHA-256 capture + verify of `<pipeline>` blocks for 3 protected commands"
      contains: "orq-agent.md"
    - path: "orq-agent/scripts/README.md"
      provides: "Usage + exit-code documentation for both scripts"
    - path: ".planning/phases/34-skill-structure-format-foundation/golden/orq-agent.sha256"
      provides: "Pre-change golden hash for /orq-agent <pipeline> block"
    - path: ".planning/phases/34-skill-structure-format-foundation/golden/prompt.sha256"
      provides: "Pre-change golden hash for /orq-agent:prompt <pipeline> block"
    - path: ".planning/phases/34-skill-structure-format-foundation/golden/architect.sha256"
      provides: "Pre-change golden hash for /orq-agent:architect <pipeline> block"
  key_links:
    - from: "orq-agent/scripts/lint-skills.sh"
      to: "orq-agent/commands/*.md + orq-agent/agents/*.md + orq-agent/SKILL.md"
      via: "grep -qF on 9 section headings + frontmatter key presence check"
      pattern: "## Constraints|## When to use|## When NOT to use|## Companion Skills|## Done When|## Destructive Actions|## Anti-Patterns|## Open in orq.ai|## Documentation & Resolution"
    - from: "orq-agent/scripts/check-protected-pipelines.sh"
      to: ".planning/phases/34-skill-structure-format-foundation/golden/*.sha256"
      via: "awk extract <pipeline> → shasum -a 256 → diff vs golden"
      pattern: "awk.*<pipeline>.*<\\/pipeline>"
---

<objective>
Build the Wave 0 validation infrastructure that every downstream plan in Phase 34 depends on: a POSIX lint script that enforces all 10 SKST rules, a protected-pipeline hash verifier that guards the 3 byte-identical entry points (`/orq-agent`, `/orq-agent:prompt`, `/orq-agent:architect`), and pre-format-change golden baselines.

Purpose: Without this plan, every plan in Waves 1-3 has no mechanical verification. The lint script is the SKST-01..10 acceptance test; the protected-pipeline script is the operational interpretation of ROADMAP success criterion #5 (byte-identical `<pipeline>` block, not byte-identical file — new sections sit OUTSIDE `<pipeline>`).

Output: Two executable POSIX scripts, a short README, and 3 golden SHA-256 files captured BEFORE any skill body is modified in Plan 02/03/04.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-skill-structure-format-foundation/34-CONTEXT.md
@.planning/phases/34-skill-structure-format-foundation/34-RESEARCH.md
@.planning/phases/34-skill-structure-format-foundation/34-VALIDATION.md

<interfaces>
<!-- Canonical set of 9 required H2 section headings (SKST-03..10 + SKST-06). Executor MUST use these exact strings in the lint script REQUIRED_SECTIONS array. -->

Required H2 headings every skill file must contain (exact strings, grep -qF match):
- "## Constraints"                  (SKST-06)
- "## When to use"                  (SKST-03)
- "## When NOT to use"              (SKST-03)
- "## Companion Skills"             (SKST-04)
- "## Done When"                    (SKST-05)
- "## Destructive Actions"          (SKST-08)
- "## Anti-Patterns"                (SKST-07)
- "## Open in orq.ai"               (SKST-10)
- "## Documentation & Resolution"   (SKST-09)

Frontmatter allowlist key (SKST-01):
- commands/*.md + SKILL.md → `allowed-tools:`
- agents/*.md              → `tools:`

Pipeline extraction regex (for protected-pipeline script):
- Start marker: ^<pipeline>$
- End marker:   ^</pipeline>$
- awk idiom:    awk '/^<pipeline>$/{flag=1;next} /^<\/pipeline>$/{flag=0} flag' FILE
- hasher:       shasum -a 256 | cut -d' ' -f1

Protected entry point source files:
- orq-agent/commands/orq-agent.md
- orq-agent/commands/prompt.md
- orq-agent/commands/architect.md

Golden baseline destination:
- .planning/phases/34-skill-structure-format-foundation/golden/orq-agent.sha256
- .planning/phases/34-skill-structure-format-foundation/golden/prompt.sha256
- .planning/phases/34-skill-structure-format-foundation/golden/architect.sha256

CLI contract for lint-skills.sh (per 34-VALIDATION.md Per-Task Verification Map):
  bash orq-agent/scripts/lint-skills.sh                        # full scan, exit 0 on pass
  bash orq-agent/scripts/lint-skills.sh --help                 # usage, exit 0
  bash orq-agent/scripts/lint-skills.sh --file <path>          # one file
  bash orq-agent/scripts/lint-skills.sh --files <dir>          # all *.md under dir
  bash orq-agent/scripts/lint-skills.sh --rule <rule-id>       # single rule, rule-ids below
  Supported --rule values:
    allowed-tools            (commands + SKILL.md have allowed-tools key in frontmatter)
    tools-declared           (agents/*.md have tools key in frontmatter)
    required-sections        (all 9 H2 headings present)
    references-multi-consumer (every file under orq-agent/references/ is grep-referenced by ≥2 skills)

CLI contract for check-protected-pipelines.sh:
  bash orq-agent/scripts/check-protected-pipelines.sh           # verify, exit 0 on match
  bash orq-agent/scripts/check-protected-pipelines.sh --baseline # write current hashes to golden/*.sha256

Exit codes (both scripts): 0 = pass, 1 = any failure. Every failure line starts with "FAIL: ".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lint-skills.sh with all 10 SKST rules</name>
  <files>orq-agent/scripts/lint-skills.sh</files>
  <read_first>
    - .planning/phases/34-skill-structure-format-foundation/34-RESEARCH.md (read lines 461-506 for the canonical lint script template and lines 285-303 for the references-multi-consumer invariant)
    - .planning/phases/34-skill-structure-format-foundation/34-VALIDATION.md (lines 47-62 for the per-task --rule/--file/--files CLI contract)
    - orq-agent/commands/orq-agent.md (see current frontmatter `allowed-tools: Read, Write, Edit, Glob, Grep, Bash, WebSearch, WebFetch, Task` — lint must accept this)
    - orq-agent/agents/architect.md (see current frontmatter `tools:` shape — lint must accept this)
  </read_first>
  <action>
    Create `orq-agent/scripts/lint-skills.sh` (POSIX bash, `#!/usr/bin/env bash`, `set -u`). It MUST support four CLI modes:

    1. `--help` → print usage block and `exit 0`.
    2. `--file <path>` → lint exactly one file.
    3. `--files <dir>` → lint every `*.md` directly under `<dir>` (non-recursive for `commands/` and `agents/`; accept both `orq-agent/commands` and `orq-agent/commands/`).
    4. `--rule <rule-id>` → run ONLY the named rule across the default file set. Rule IDs: `allowed-tools`, `tools-declared`, `required-sections`, `references-multi-consumer`.
    5. No args → run ALL rules across the default file set: `orq-agent/SKILL.md` + `orq-agent/commands/*.md` + `orq-agent/agents/*.md`.

    Implement these rules (use these EXACT strings — do not paraphrase):

    **Rule `required-sections` (SKST-03, SKST-04, SKST-05, SKST-06, SKST-07, SKST-08, SKST-09, SKST-10):** For each file in the target set, `grep -qF` for each of these 9 headings. Emit `FAIL: <file> — missing section '<heading>'` on miss.
    ```
    REQUIRED_SECTIONS=(
      "## Constraints"
      "## When to use"
      "## When NOT to use"
      "## Companion Skills"
      "## Done When"
      "## Destructive Actions"
      "## Anti-Patterns"
      "## Open in orq.ai"
      "## Documentation & Resolution"
    )
    ```

    **Rule `allowed-tools` (SKST-01, commands + SKILL.md):** For `orq-agent/SKILL.md` and every `orq-agent/commands/*.md`, verify frontmatter contains a non-empty `allowed-tools:` key. Extraction idiom (inside awk, between the first two `---` lines):
    ```
    awk '/^---$/{c++; if(c>=2)exit; next} c==1 && /^allowed-tools:[[:space:]]*[^[:space:]]/' "$f"
    ```
    Non-empty output → pass. Empty → `FAIL: <file> — missing or empty 'allowed-tools:' frontmatter key`.

    **Rule `tools-declared` (SKST-01, subagents):** For every `orq-agent/agents/*.md`, same extraction but looking for `^tools:[[:space:]]*[^[:space:]]`. Fail message: `FAIL: <file> — subagent missing or empty 'tools:' frontmatter key`.

    **Rule `references-multi-consumer` (SKST-02):** For every file `orq-agent/references/*.md`, count how many skill files grep-reference its basename:
    ```
    count=$(grep -lrE "references/$(basename "$ref_file")" orq-agent/SKILL.md orq-agent/commands orq-agent/agents 2>/dev/null | wc -l | tr -d ' ')
    ```
    If `count < 2`, emit `FAIL: orq-agent/references/<name>.md — only <count> consumer(s); single-consumer refs must live under <skill>/resources/ (SKST-02)`.

    **Additional XML-tag guard (Pitfall 1, RESEARCH.md line 424):** For each of the 9 required sections, verify the H2 heading is NOT wrapped inside an open `<role>`, `<pipeline>`, or `<files_to_read>` block. Cheap check: for each heading found via `grep -n`, track whether the nearest prior unclosed XML open tag (any of `<role>`, `<pipeline>`, `<files_to_read>`, `<objective>`, `<instructions>`) has a matching close AFTER the heading. If yes, emit `FAIL: <file>:<line> — section '<heading>' is inside an unclosed XML block (must be outside)`. Keep this implementation simple: grep for each opening tag and its matching closing tag line numbers, ensure heading line is not between them.

    Every failure MUST start with `FAIL:` on its own line. Script MUST exit 0 iff no `FAIL:` lines were emitted, else exit 1.

    The `--help` block MUST list: usage synopsis, each mode, each rule ID, exit codes. Keep under 30 lines.

    Chmod +x after writing: the executor must run `chmod +x orq-agent/scripts/lint-skills.sh` as part of this task.
  </action>
  <verify>
    <automated>bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh --help | grep -q "Usage:" && test -x /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh</automated>
  </verify>
  <acceptance_criteria>
    - `orq-agent/scripts/lint-skills.sh` exists and is executable (`test -x` returns 0)
    - `bash orq-agent/scripts/lint-skills.sh --help` exits 0 and stdout contains the literal string `Usage:`
    - `bash orq-agent/scripts/lint-skills.sh --help` stdout lists all 4 rule IDs: `allowed-tools`, `tools-declared`, `required-sections`, `references-multi-consumer`
    - `bash orq-agent/scripts/lint-skills.sh --rule allowed-tools --file orq-agent/commands/orq-agent.md` exits 0 (current file already has the key)
    - `bash orq-agent/scripts/lint-skills.sh --rule allowed-tools --file orq-agent/SKILL.md` exits 1 (SKILL.md still lacks allowed-tools pre-Plan 04) AND stdout contains `FAIL: orq-agent/SKILL.md — missing or empty 'allowed-tools:' frontmatter key`
    - `bash orq-agent/scripts/lint-skills.sh --rule tools-declared --file orq-agent/agents/architect.md` exits 0
    - `bash orq-agent/scripts/lint-skills.sh --rule required-sections --file orq-agent/commands/orq-agent.md` exits 1 (sections not yet added pre-Plan 02) AND emits at least one `FAIL:` line naming a missing section
    - `bash orq-agent/scripts/lint-skills.sh --rule references-multi-consumer` exits 0 (per RESEARCH.md all 8 refs are multi-consumer)
    - Script file size is under 250 lines (sanity check: `wc -l orq-agent/scripts/lint-skills.sh` reports ≤ 250)
  </acceptance_criteria>
  <done>
    Lint script exists, is executable, supports all 4 CLI modes and all 4 rule IDs, and correctly identifies current repo state: passes on keys that exist (allowed-tools on commands, tools on subagents, multi-consumer refs) and fails on keys that don't (allowed-tools on SKILL.md, required-sections on every file). Exit codes are 0 for pass / 1 for any failure. Every failure line begins with `FAIL:`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create check-protected-pipelines.sh and capture golden baselines BEFORE any skill edits</name>
  <files>
    orq-agent/scripts/check-protected-pipelines.sh,
    .planning/phases/34-skill-structure-format-foundation/golden/orq-agent.sha256,
    .planning/phases/34-skill-structure-format-foundation/golden/prompt.sha256,
    .planning/phases/34-skill-structure-format-foundation/golden/architect.sha256
  </files>
  <read_first>
    - .planning/phases/34-skill-structure-format-foundation/34-RESEARCH.md (lines 603-630 for the canonical extract+hash snippet)
    - .planning/phases/34-skill-structure-format-foundation/34-VALIDATION.md (lines 25-33 for the command contract and estimated runtime)
    - orq-agent/commands/orq-agent.md (find the `<pipeline>` and `</pipeline>` line markers to confirm extraction works — grep -n '<pipeline>\|</pipeline>' on the file)
    - orq-agent/commands/prompt.md (same: confirm pipeline block exists)
    - orq-agent/commands/architect.md (same: confirm pipeline block exists)
  </read_first>
  <action>
    Create `orq-agent/scripts/check-protected-pipelines.sh` (POSIX bash, `#!/usr/bin/env bash`, `set -eu`). Supports two modes:

    1. `--baseline` → For each of the 3 protected commands (orq-agent, prompt, architect), extract the `<pipeline>` block, sha256 it, write to `.planning/phases/34-skill-structure-format-foundation/golden/<cmd>.sha256`. Create the `golden/` directory via `mkdir -p` if missing. Print one line per file: `WROTE: golden/<cmd>.sha256 <hash>`. Exit 0.

    2. No args (verify mode) → For each protected command, recompute the hash, read the golden file, compare. On match: print `OK: <cmd>.sha256 matches`. On mismatch: print `FAIL: orq-agent/commands/<cmd>.md <pipeline> block changed (<current> vs <golden>). If intentional, re-baseline with: bash orq-agent/scripts/check-protected-pipelines.sh --baseline`. Exit 1 on any mismatch; exit 0 on all match.

    Extraction idiom (exactly as in RESEARCH.md line 613):
    ```
    awk '/^<pipeline>$/{flag=1;next} /^<\/pipeline>$/{flag=0} flag' "orq-agent/commands/${cmd}.md" \
      | shasum -a 256 | cut -d' ' -f1
    ```

    Use `shasum -a 256` (present on macOS and Linux via coreutils compat per RESEARCH.md line 706), NOT `sha256sum`.

    Protected command list hard-coded at top of script:
    ```
    PROTECTED_COMMANDS=(orq-agent prompt architect)
    GOLDEN_DIR=".planning/phases/34-skill-structure-format-foundation/golden"
    ```

    Defensive check: if extracted pipeline block is empty (awk returns 0 bytes), emit `FAIL: orq-agent/commands/<cmd>.md has no <pipeline>...</pipeline> block` and exit 1 — this guards against accidentally running the script after someone deletes the XML tags.

    Chmod +x after writing.

    **CRITICAL ORDERING:** After creating the script, IMMEDIATELY run `bash orq-agent/scripts/check-protected-pipelines.sh --baseline` to capture the golden hashes. This MUST happen in this Wave 0 plan, BEFORE Plans 02, 03, 04 modify any skill files. The 3 `.sha256` files are artifacts of this task and MUST be committed.
  </action>
  <verify>
    <automated>bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/check-protected-pipelines.sh --baseline && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/check-protected-pipelines.sh</automated>
  </verify>
  <acceptance_criteria>
    - `orq-agent/scripts/check-protected-pipelines.sh` exists and is executable
    - Running with `--baseline` creates 3 files: `.planning/phases/34-skill-structure-format-foundation/golden/orq-agent.sha256`, `prompt.sha256`, `architect.sha256` — each file contains exactly one 64-char hex string + newline
    - Each golden file is non-empty: `test -s .planning/phases/34-skill-structure-format-foundation/golden/orq-agent.sha256` exits 0
    - Running the script with no args AFTER `--baseline` exits 0 (current hash matches just-written golden)
    - Stdout in verify mode contains `OK: orq-agent.sha256 matches`, `OK: prompt.sha256 matches`, `OK: architect.sha256 matches`
    - Script uses `shasum -a 256` (grep-verifiable: `grep -q 'shasum -a 256' orq-agent/scripts/check-protected-pipelines.sh`)
    - Script hard-codes the 3-command list (grep-verifiable: `grep -q 'PROTECTED_COMMANDS=(orq-agent prompt architect)' orq-agent/scripts/check-protected-pipelines.sh`)
  </acceptance_criteria>
  <done>
    Protected-pipeline script exists, supports `--baseline` and verify modes, and 3 golden SHA-256 baselines have been captured BEFORE any Plan 02/03/04 edits. Running the script in verify mode against the freshly-captured baselines exits 0. The baselines represent the pre-format-change `<pipeline>` block contents and will catch any drift introduced by downstream plans.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write orq-agent/scripts/README.md documenting both scripts</name>
  <files>orq-agent/scripts/README.md</files>
  <read_first>
    - orq-agent/scripts/lint-skills.sh (just created — read its --help output to ensure README matches)
    - orq-agent/scripts/check-protected-pipelines.sh (just created)
    - .planning/phases/34-skill-structure-format-foundation/34-VALIDATION.md (copy the sampling-rate guidance into the README)
  </read_first>
  <action>
    Create `orq-agent/scripts/README.md` with EXACTLY this structure (fill in command outputs from the two scripts). Target length: 40-80 lines.

    Required sections (use these H2 headings verbatim):

    ```
    # orq-agent/scripts

    Local validation scripts for Phase 34 Skill Structure & Format Foundation.
    Invoke-on-demand only — CI wiring is deferred to Phase 43 (DIST).

    ## lint-skills.sh

    **Purpose:** Enforce the 10 Skill Structure (SKST) rules across every skill file.

    **Exit codes:** 0 = all checks pass; 1 = any check failed.

    **Modes:**

    - `bash orq-agent/scripts/lint-skills.sh` — full scan (SKILL.md + all commands + all subagents, all rules)
    - `bash orq-agent/scripts/lint-skills.sh --help` — usage
    - `bash orq-agent/scripts/lint-skills.sh --file <path>` — single file
    - `bash orq-agent/scripts/lint-skills.sh --files <dir>` — every *.md in a directory
    - `bash orq-agent/scripts/lint-skills.sh --rule <rule-id>` — single rule only

    **Rule IDs:** `allowed-tools`, `tools-declared`, `required-sections`, `references-multi-consumer`.

    **Required H2 sections (SKST-03..10 + SKST-06):** Constraints, When to use, When NOT to use,
    Companion Skills, Done When, Destructive Actions, Anti-Patterns, Open in orq.ai,
    Documentation & Resolution.

    ## check-protected-pipelines.sh

    **Purpose:** Enforce byte-identical behavior of the 3 protected entry points
    (`/orq-agent`, `/orq-agent:prompt`, `/orq-agent:architect`) per ROADMAP Phase 34
    success criterion #5. New SKST sections sit OUTSIDE `<pipeline>` blocks; this script
    verifies the `<pipeline>` block itself is SHA-256-identical to the Phase 34 baseline.

    **Exit codes:** 0 = all 3 pipeline blocks match golden; 1 = any mismatch.

    **Modes:**

    - `bash orq-agent/scripts/check-protected-pipelines.sh` — verify current vs golden
    - `bash orq-agent/scripts/check-protected-pipelines.sh --baseline` — re-capture golden (Wave 0 only; intentional pipeline changes post-Phase-34 require explicit re-baseline with a note in the SUMMARY)

    **Golden baselines live at:** `.planning/phases/34-skill-structure-format-foundation/golden/{orq-agent,prompt,architect}.sha256`

    ## Sampling rate (from 34-VALIDATION.md)

    - After every skill-file commit: run `lint-skills.sh --file <modified-file>` (≤1s)
    - After every plan wave: run full `lint-skills.sh` + `check-protected-pipelines.sh` (≤5s total)
    - Before `/gsd:verify-work`: full suite must exit 0
    ```

    This README is the executor-facing contract — every Plan 02/03/04 task action will reference it.
  </action>
  <verify>
    <automated>test -f /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/README.md && grep -qF "## lint-skills.sh" /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/README.md && grep -qF "## check-protected-pipelines.sh" /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/README.md && grep -qF "Sampling rate" /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `orq-agent/scripts/README.md` exists
    - Contains literal string `## lint-skills.sh`
    - Contains literal string `## check-protected-pipelines.sh`
    - Contains literal string `Sampling rate`
    - Contains all 4 rule IDs verbatim: `allowed-tools`, `tools-declared`, `required-sections`, `references-multi-consumer`
    - Contains all 9 required section names listed explicitly
    - Contains the golden-baseline path `.planning/phases/34-skill-structure-format-foundation/golden/`
    - File is between 30 and 120 lines (`wc -l` sanity)
  </acceptance_criteria>
  <done>
    README exists with required sections, documents both scripts' CLI contracts, lists all 4 rule IDs and all 9 required sections by name, cites the golden-baseline path, and includes the sampling-rate guidance from 34-VALIDATION.md. Future executors in Plans 02-05 can read this file alone to understand how to invoke the lint scripts.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `bash orq-agent/scripts/lint-skills.sh --help` → exits 0, prints usage.
2. `bash orq-agent/scripts/check-protected-pipelines.sh --baseline` → writes 3 `.sha256` files.
3. `bash orq-agent/scripts/check-protected-pipelines.sh` (verify mode) → exits 0 (matches just-captured baselines).
4. `bash orq-agent/scripts/lint-skills.sh --rule references-multi-consumer` → exits 0 (per RESEARCH.md Reference Consumer Graph, all 8 references have ≥2 consumers today).
5. `bash orq-agent/scripts/lint-skills.sh` (full) → exits 1 (Plans 02/03/04 haven't added required sections yet); output contains `FAIL:` lines naming missing sections — this is EXPECTED and proves the lint is wired. Do NOT gate Plan 01 completion on the full lint exiting 0.
6. `ls .planning/phases/34-skill-structure-format-foundation/golden/ | wc -l` → 3.
7. `orq-agent/scripts/README.md` exists with required headings.

No skill file under `orq-agent/commands/`, `orq-agent/agents/`, or `orq-agent/SKILL.md` is modified by this plan.
</verification>

<success_criteria>
- [ ] `orq-agent/scripts/lint-skills.sh` exists, is executable, supports 4 CLI modes, and enforces 4 rule IDs
- [ ] `orq-agent/scripts/check-protected-pipelines.sh` exists, is executable, supports `--baseline` and verify modes
- [ ] `orq-agent/scripts/README.md` exists and documents both scripts
- [ ] 3 golden SHA-256 files captured in `.planning/phases/34-skill-structure-format-foundation/golden/` BEFORE any skill body edits
- [ ] `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0 against the freshly-captured baselines
- [ ] `bash orq-agent/scripts/lint-skills.sh --rule references-multi-consumer` exits 0
- [ ] Zero modifications to files under `orq-agent/commands/`, `orq-agent/agents/`, or `orq-agent/SKILL.md`
</success_criteria>

<output>
After completion, create `.planning/phases/34-skill-structure-format-foundation/34-01-SUMMARY.md` documenting:
- The lint script's CLI (copy from --help)
- The protected-pipelines script's CLI
- The captured golden hashes (paste from baseline run stdout — 3 hex strings)
- Confirmation that no skill files were modified
- Pointer to Plans 02, 03, 04 which consume this infrastructure
</output>
