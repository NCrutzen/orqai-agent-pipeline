---
phase: 34-skill-structure-format-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - "34-01"
  - "34-02"
  - "34-03"
  - "34-04"
files_modified:
  - .planning/phases/34-skill-structure-format-foundation/34-05-VERIFICATION.md
autonomous: true
requirements:
  - SKST-01
  - SKST-02
  - SKST-03
  - SKST-04
  - SKST-05
  - SKST-06
  - SKST-07
  - SKST-08
  - SKST-09
  - SKST-10
must_haves:
  truths:
    - "`bash orq-agent/scripts/lint-skills.sh` (full suite, no args) exits 0 across all 33 skill files (1 SKILL.md + 15 commands + 17 subagents)"
    - "`bash orq-agent/scripts/check-protected-pipelines.sh` exits 0 (3 protected entry points still byte-identical in `<pipeline>` block)"
    - "Every SKST requirement (SKST-01..10) is traceable to a lint rule + a grep-verifiable section or frontmatter key"
    - "34-05-VERIFICATION.md exists with a requirement-to-lint-rule traceability table and captured green lint output"
    - "All 5 ROADMAP Phase 34 Success Criteria are green: (1) allowed-tools + Constraints+Why present everywhere, (2) 9 sections + destructive actions + docs footer + open-in-orq everywhere, (3) resources policy documented (no migration since all shared), (4) lint check confirms conformance, (5) protected entry points byte-identical"
  artifacts:
    - path: ".planning/phases/34-skill-structure-format-foundation/34-05-VERIFICATION.md"
      provides: "Full-suite green proof: captured lint output, hash check output, per-requirement traceability table, roadmap-criteria checklist"
      contains: "SKST-01|SKST-02|SKST-03|SKST-04|SKST-05|SKST-06|SKST-07|SKST-08|SKST-09|SKST-10"
  key_links:
    - from: "34-05-VERIFICATION.md traceability table"
      to: "orq-agent/scripts/lint-skills.sh rules"
      via: "Every SKST-0X row names the lint rule + expected grep pattern + command to reproduce"
      pattern: "SKST-0"
---

<objective>
Run the full Phase 34 validation suite and capture green output in a verification document. This is the final quality gate before `/gsd:verify-work`. Produce a requirement-to-lint-rule traceability table and a ROADMAP-success-criteria checklist proving all 10 SKST requirements and all 5 ROADMAP Phase 34 success criteria are mechanically satisfied.

Purpose: Plans 01-04 produced infrastructure and per-file edits. This plan proves the whole system holds together at phase-close. The verification document is the evidence trail for `/gsd:verify-work` and becomes the reference for downstream V3.0 phases (36-43) that must call this phase's lint before marking themselves complete.

Output: `.planning/phases/34-skill-structure-format-foundation/34-05-VERIFICATION.md` with captured green lint output, full hash-check output, a requirement traceability table, and a ROADMAP criteria checklist — all three green.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-skill-structure-format-foundation/34-CONTEXT.md
@.planning/phases/34-skill-structure-format-foundation/34-RESEARCH.md
@.planning/phases/34-skill-structure-format-foundation/34-VALIDATION.md
@.planning/phases/34-skill-structure-format-foundation/34-01-SUMMARY.md
@.planning/phases/34-skill-structure-format-foundation/34-02-SUMMARY.md
@.planning/phases/34-skill-structure-format-foundation/34-03-SUMMARY.md
@.planning/phases/34-skill-structure-format-foundation/34-04-SUMMARY.md

<interfaces>
<!-- The 5 ROADMAP Phase 34 Success Criteria (from ROADMAP.md lines 120-125): -->

1. Every skill file declares `allowed-tools` (or `tools:` for subagents per schema split) + Constraints block opening with NEVER/ALWAYS + "Why these constraints" paragraph
2. Every skill declares When to use, When NOT to use, Companion Skills, Done When, Anti-Patterns table, Destructive Actions, Documentation & Resolution footer, Open in orq.ai
3. Skill-specific long-form docs moved to `<skill>/resources/` without breaking consumer links — in this repo: NO MIGRATION needed (all 8 refs are multi-consumer); policy documented in SKILL.md
4. Lint/validation check confirms all skills pass
5. Three protected entry points (`/orq-agent`, `/orq-agent:prompt`, `/orq-agent:architect`) remain byte-identical in `<pipeline>` block behavior

**The 10 SKST requirements (from REQUIREMENTS.md lines 124-135):**

| Req | Description | Lint rule | Expected grep |
|-----|-------------|-----------|---------------|
| SKST-01 | `allowed-tools` frontmatter (or `tools:` for subagents) | `allowed-tools` + `tools-declared` | `^allowed-tools:` on SKILL.md + commands/*.md; `^tools:` on agents/*.md |
| SKST-02 | Single-consumer docs → `<skill>/resources/`; shared → `references/` | `references-multi-consumer` | Every `orq-agent/references/*.md` grep-referenced ≥2 times |
| SKST-03 | When to use + When NOT to use | `required-sections` | `## When to use` + `## When NOT to use` per file |
| SKST-04 | Companion Skills directional handoffs | `required-sections` | `## Companion Skills` + at least one `→ ` or `← ` arrow |
| SKST-05 | Done When checklist with falsifiable criteria | `required-sections` | `## Done When` + at least one `- [ ]` unchecked checkbox |
| SKST-06 | Constraints block NEVER/ALWAYS + Why | `required-sections` | `## Constraints` + `**NEVER**` + `**ALWAYS**` + `**Why these constraints:**` |
| SKST-07 | Anti-Patterns table | `required-sections` | `## Anti-Patterns` + 2-column markdown table |
| SKST-08 | Destructive Actions w/ AskUserQuestion | `required-sections` | `## Destructive Actions` + (`AskUserQuestion` OR `N/A` OR `None`) |
| SKST-09 | Documentation & Resolution footer | `required-sections` | `## Documentation & Resolution` + trust order 1..4 |
| SKST-10 | Open in orq.ai section | `required-sections` | `## Open in orq.ai` + (`https://my.orq.ai/` OR `N/A`) |

**Files in scope (33 total):**
- 1 × `orq-agent/SKILL.md`
- 15 × `orq-agent/commands/*.md`
- 17 × `orq-agent/agents/*.md`

**Verification document template (`34-05-VERIFICATION.md`):**

```markdown
# Phase 34 Verification

**Verified:** <DATE>
**Phase:** 34-skill-structure-format-foundation
**Status:** COMPLETE / BLOCKED

## Captured green output

### Full lint suite
$ bash orq-agent/scripts/lint-skills.sh
<stdout captured verbatim from actual run>
<exit code: 0>

### Protected-pipeline hash check
$ bash orq-agent/scripts/check-protected-pipelines.sh
<stdout captured verbatim>
<exit code: 0>

### Per-rule breakdown
$ bash orq-agent/scripts/lint-skills.sh --rule allowed-tools
<stdout verbatim, exit 0>

$ bash orq-agent/scripts/lint-skills.sh --rule tools-declared
<stdout verbatim, exit 0>

$ bash orq-agent/scripts/lint-skills.sh --rule required-sections
<stdout verbatim, exit 0>

$ bash orq-agent/scripts/lint-skills.sh --rule references-multi-consumer
<stdout verbatim, exit 0>

## SKST requirement traceability

<10-row table with columns: Req | Description | Lint rule | Expected grep pattern | Green?>

## ROADMAP Phase 34 Success Criteria

<5-row checklist with evidence>

## File inventory

Total skill files in scope: 33
- orq-agent/SKILL.md: 1
- orq-agent/commands/*.md: 15
- orq-agent/agents/*.md: 17

All 33 pass `bash orq-agent/scripts/lint-skills.sh`: ✓ (exit 0)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full validation suite and produce 34-05-VERIFICATION.md with captured output + traceability + criteria checklist</name>
  <files>.planning/phases/34-skill-structure-format-foundation/34-05-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/34-skill-structure-format-foundation/34-VALIDATION.md (per-task verification map — copy the test IDs into the traceability section)
    - .planning/phases/34-skill-structure-format-foundation/34-01-SUMMARY.md (confirm lint script + golden hashes were captured)
    - .planning/phases/34-skill-structure-format-foundation/34-02-SUMMARY.md (confirm commands were edited)
    - .planning/phases/34-skill-structure-format-foundation/34-03-SUMMARY.md (confirm subagents were edited)
    - .planning/phases/34-skill-structure-format-foundation/34-04-SUMMARY.md (confirm SKILL.md was edited)
    - orq-agent/scripts/lint-skills.sh (verify the rule IDs match this verification's expectations)
    - orq-agent/scripts/check-protected-pipelines.sh
  </read_first>
  <action>
    Execute the full verification suite in this EXACT order, capturing stdout/stderr of each command:

    **Step 1 — Run and capture full lint:**
    ```bash
    cd /Users/nickcrutzen/developer/orqai-agent-pipeline
    bash orq-agent/scripts/lint-skills.sh > /tmp/34-lint-full.log 2>&1
    echo "exit: $?" >> /tmp/34-lint-full.log
    ```
    Exit code MUST be 0. If non-zero, STOP and investigate — do NOT write VERIFICATION.md with red output; file an issue instead and return "BLOCKED" status.

    **Step 2 — Run and capture protected-pipelines check:**
    ```bash
    bash orq-agent/scripts/check-protected-pipelines.sh > /tmp/34-protected.log 2>&1
    echo "exit: $?" >> /tmp/34-protected.log
    ```
    Exit code MUST be 0. If non-zero, STOP — the `<pipeline>` block in one of orq-agent.md, prompt.md, architect.md was perturbed during Plans 02/03/04. Diagnose which file, revert the perturbation, re-run.

    **Step 3 — Run each rule in isolation and capture:**
    ```bash
    for rule in allowed-tools tools-declared required-sections references-multi-consumer; do
      echo "=== --rule $rule ===" >> /tmp/34-per-rule.log
      bash orq-agent/scripts/lint-skills.sh --rule "$rule" >> /tmp/34-per-rule.log 2>&1
      echo "exit: $?" >> /tmp/34-per-rule.log
    done
    ```
    Every rule MUST exit 0.

    **Step 4 — Count files in scope:**
    ```bash
    ls orq-agent/SKILL.md | wc -l                    # should be 1
    ls orq-agent/commands/*.md | wc -l               # should be 15
    ls orq-agent/agents/*.md | wc -l                 # should be 17
    ```
    Record counts.

    **Step 5 — Per-file section presence spot check** (sanity: run grep across all 33 files for each of the 9 headings; count files missing each):
    ```bash
    for section in "## Constraints" "## When to use" "## When NOT to use" "## Companion Skills" "## Done When" "## Destructive Actions" "## Anti-Patterns" "## Open in orq.ai" "## Documentation & Resolution"; do
      missing=$(grep -L -F "$section" orq-agent/SKILL.md orq-agent/commands/*.md orq-agent/agents/*.md 2>/dev/null | wc -l | tr -d ' ')
      echo "'$section': missing in $missing files"
    done
    ```
    Every section MUST be missing in 0 files.

    **Step 6 — Write `34-05-VERIFICATION.md`** with:

    - Header: Phase 34 Verification, dated (use `2026-04-20` or actual run date), status `COMPLETE`.
    - **Captured green output section** — paste `/tmp/34-lint-full.log`, `/tmp/34-protected.log`, `/tmp/34-per-rule.log` contents as fenced code blocks.
    - **SKST requirement traceability table** — 10 rows, one per SKST-01..10, with columns:
      - Req (SKST-0X)
      - Description (from REQUIREMENTS.md lines 124-135)
      - Lint rule (from <interfaces> table)
      - Expected grep pattern
      - Green? ✓ (cite the lint output line that proves it)
    - **ROADMAP Phase 34 Success Criteria checklist** — 5 rows, one per criterion from ROADMAP lines 120-125. Each row has: criterion text (verbatim from ROADMAP), evidence (e.g., "`bash orq-agent/scripts/check-protected-pipelines.sh` exits 0 — see /tmp/34-protected.log"), status ✓.
    - **File inventory** — the counts from Step 4 with pass/fail marker.
    - **Downstream consumer note** — one paragraph stating: "Phases 36-43 must call `bash orq-agent/scripts/lint-skills.sh` on any new skill files before marking themselves complete. This phase ships the infrastructure; enforcement is lateral (each phase gates itself). CI wiring is owned by Phase 43 (DIST)."
    - **Deferred/open items:**
      - "Inferred URLs (4/11 per RESEARCH.md): 4 skills contain `<!-- TODO(SKST-10): verified in Phase 37+ -->` markers. Lint does not check URL validity (RESEARCH.md line 395). Resolution deferred to Phase 37+."
      - "Semantic byte-identical check (manual-only, per 34-VALIDATION.md Manual-Only Verifications): the protected-pipeline SHA-256 check proves `<pipeline>` block hash matches. Full semantic identity (invoking the 3 commands on a canned fixture and diffing spec JSON output) is deferred to the next `/gsd:verify-work` or user-initiated smoke test — listed here for audit trail."

    **Step 7 — Verify 34-05-VERIFICATION.md itself is well-formed:**
    ```
    test -s .planning/phases/34-skill-structure-format-foundation/34-05-VERIFICATION.md
    grep -qF "SKST-10" .planning/phases/34-skill-structure-format-foundation/34-05-VERIFICATION.md
    grep -qF "COMPLETE" .planning/phases/34-skill-structure-format-foundation/34-05-VERIFICATION.md
    grep -qF "exit: 0" .planning/phases/34-skill-structure-format-foundation/34-05-VERIFICATION.md
    ```
    All must exit 0.

    **Do NOT modify any file under `orq-agent/`.** This plan is read-only on code; it writes only the VERIFICATION.md document.
  </action>
  <verify>
    <automated>bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/lint-skills.sh && bash /Users/nickcrutzen/developer/orqai-agent-pipeline/orq-agent/scripts/check-protected-pipelines.sh && test -s /Users/nickcrutzen/developer/orqai-agent-pipeline/.planning/phases/34-skill-structure-format-foundation/34-05-VERIFICATION.md && grep -qF "SKST-10" /Users/nickcrutzen/developer/orqai-agent-pipeline/.planning/phases/34-skill-structure-format-foundation/34-05-VERIFICATION.md && grep -qF "COMPLETE" /Users/nickcrutzen/developer/orqai-agent-pipeline/.planning/phases/34-skill-structure-format-foundation/34-05-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `bash orq-agent/scripts/lint-skills.sh` (no args, full suite) exits 0
    - `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0
    - `bash orq-agent/scripts/lint-skills.sh --rule allowed-tools` exits 0
    - `bash orq-agent/scripts/lint-skills.sh --rule tools-declared` exits 0
    - `bash orq-agent/scripts/lint-skills.sh --rule required-sections` exits 0
    - `bash orq-agent/scripts/lint-skills.sh --rule references-multi-consumer` exits 0
    - `.planning/phases/34-skill-structure-format-foundation/34-05-VERIFICATION.md` exists and is non-empty
    - VERIFICATION.md contains the literal strings `SKST-01`, `SKST-02`, `SKST-03`, `SKST-04`, `SKST-05`, `SKST-06`, `SKST-07`, `SKST-08`, `SKST-09`, `SKST-10` (all 10 requirement IDs)
    - VERIFICATION.md contains the literal string `COMPLETE` (phase status)
    - VERIFICATION.md contains at least 3 fenced code blocks with `exit: 0` (captured green output)
    - VERIFICATION.md contains a traceability table with 10 rows (one per SKST-0X)
    - VERIFICATION.md contains the 5-row ROADMAP Phase 34 Success Criteria checklist
    - VERIFICATION.md cites the `TODO(SKST-10)` marker count and the manual semantic-check deferral in a Deferred/open items section
    - File counts match: 1 × SKILL.md, 15 × commands/*.md, 17 × agents/*.md (33 total in scope)
  </acceptance_criteria>
  <done>
    Phase 34 is mechanically verified: lint passes on all 33 skill files, protected-pipeline hash check confirms byte-identical `<pipeline>` blocks for the 3 protected entry points, all 10 SKST requirements are traceable to lint rules with green output, and all 5 ROADMAP Phase 34 Success Criteria are checked off with evidence in `34-05-VERIFICATION.md`. Downstream phases (36-43) have a clear invariant to uphold (call the lint before marking themselves done). This plan is the phase's final quality gate before `/gsd:verify-work`.
  </done>
</task>

</tasks>

<verification>
This plan IS the verification step. Its own verify step runs the full suite:

1. `bash orq-agent/scripts/lint-skills.sh` → exits 0 (all 33 files, all 4 rules).
2. `bash orq-agent/scripts/check-protected-pipelines.sh` → exits 0 (protected pipelines byte-identical).
3. `34-05-VERIFICATION.md` exists with captured green output, traceability, and ROADMAP criteria checklist.

No code files are modified by this plan.
</verification>

<success_criteria>
- [ ] `bash orq-agent/scripts/lint-skills.sh` exits 0 (full suite)
- [ ] `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0
- [ ] Every per-rule lint invocation exits 0 (4 rules: allowed-tools, tools-declared, required-sections, references-multi-consumer)
- [ ] `.planning/phases/34-skill-structure-format-foundation/34-05-VERIFICATION.md` exists with all required sections
- [ ] 10-row SKST traceability table in VERIFICATION.md
- [ ] 5-row ROADMAP Phase 34 Success Criteria checklist in VERIFICATION.md, all ✓
- [ ] File counts: 1 SKILL.md + 15 commands + 17 subagents = 33 files, all pass lint
- [ ] Deferred items (inferred URLs, semantic manual check) explicitly documented
- [ ] Zero modifications to files under `orq-agent/`
</success_criteria>

<output>
After completion, create `.planning/phases/34-skill-structure-format-foundation/34-05-SUMMARY.md` documenting:
- Phase 34 status: COMPLETE / BLOCKED
- Quoted exit codes for all 6 verification commands (full lint, protected-pipelines, 4 per-rule lints)
- Count of files in scope (33) and count passing (33)
- Pointer to `34-05-VERIFICATION.md` as the full evidence file
- Downstream handoff: "Phases 36-43 must invoke `bash orq-agent/scripts/lint-skills.sh` on any new skill files before their own `/gsd:verify-work` — the invariant is lateral enforcement."
- Outstanding TODOs for Phase 37+ (inferred URLs: 4 `TODO(SKST-10)` markers present per RESEARCH.md line 395)
</output>
