---
phase: 39-dataset-generator-enhancements
plan: 04
type: execute
wave: 2
depends_on: [39-01, 39-02, 39-03]
files_modified:
  - orq-agent/SKILL.md
  - orq-agent/commands/help.md
autonomous: true
requirements: [DSET-01, DSET-02, DSET-03, DSET-04, DSET-05, DSET-06, DSET-07, DSET-08]
must_haves:
  truths:
    - "SKILL.md index references the new dataset-generator/resources/ subdirectory"
    - "SKILL.md documents Phase 39 DSET-01..08 coverage for /orq-agent:datasets"
    - "help.md datasets entry reflects the new --mode / --trace-id / --shape flags"
    - "Protected pipelines (orq-agent.md, prompt.md, architect.md) remain SHA-256 identical"
  artifacts:
    - path: "orq-agent/SKILL.md"
      provides: "Suite index + Phase 39 coverage block + resources subdir listing"
      contains: "dataset-generator/resources, DSET-01, DSET-08"
    - path: "orq-agent/commands/help.md"
      provides: "/orq-agent:datasets updated flag summary"
      contains: "--mode, --trace-id, --shape"
  key_links:
    - from: "SKILL.md Phase 39 block"
      to: "commands/datasets.md + agents/dataset-generator.md + resources/"
      via: "file path + requirement ID references"
      pattern: "DSET-0[1-8]"
---

<objective>
Wire the Phase 39 surface into the suite index (SKILL.md) and help banner (help.md) so users can discover the new datasets modes and resources subdirectory.

Purpose: Following the Phase 34/36/37/38 index-wiring recipe — SKILL.md + help.md only, never the 3 protected pipelines.
Output: Updated index entries for Phase 39 surfaces.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-dataset-generator-enhancements/39-CONTEXT.md
@orq-agent/SKILL.md
@orq-agent/commands/help.md
@.planning/phases/39-dataset-generator-enhancements/39-01-PLAN.md
@.planning/phases/39-dataset-generator-enhancements/39-02-PLAN.md
@.planning/phases/39-dataset-generator-enhancements/39-03-PLAN.md

<interfaces>
Precedent pattern (Phase 37 / Phase 38): add a `### Phase N (Subsystem)` H3 block AFTER the last phase block, with coverage bullets referencing requirement IDs. Phase 39's surface is an ENHANCEMENT to an existing command (/orq-agent:datasets), not a new command, so the recipe adapts:
- Add a Phase 39 coverage block documenting DSET-01..08 under the existing dataset-generator entry.
- Add the new resources/ subdirectory to the Directory Structure diagram.
- Update the help.md datasets line (currently `Test dataset generation`) to reflect new flags at a high level.

Protected files (DO NOT touch): orq-agent/commands/orq-agent.md, orq-agent/commands/prompt.md, orq-agent/commands/architect.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Phase 39 into SKILL.md + help.md</name>
  <files>orq-agent/SKILL.md, orq-agent/commands/help.md</files>
  <read_first>
    - orq-agent/SKILL.md (full current file, especially Directory Structure and Phase 37/38 blocks for pattern reuse)
    - orq-agent/commands/help.md (for the datasets line)
    - orq-agent/scripts/check-protected-pipelines.sh
  </read_first>
  <action>
    EDIT 1 — orq-agent/SKILL.md:

    (a) In the Directory Structure ASCII tree, under `agents/` add a nested entry right after `dataset-generator.md`:
    ```
    dataset-generator/
      resources/
        adversarial-vectors.md    # Phase 39: 8-vector adversarial catalog (DSET-02)
        coverage-rules.md          # Phase 39: coverage rule definitions + remediation (DSET-03)
        shapes.md                  # Phase 39: multi-turn + RAG shape templates (DSET-05..07)
    ```

    (b) After the existing `### Phase 38 (Trace Failure Analysis)` block, insert a new H3 block:

    ```
    ### Phase 39 (Dataset Generator Enhancements)

    | Command | File | Tier Required | Purpose |
    |---------|------|---------------|---------|
    | `/orq-agent:datasets` | `commands/datasets.md` | deploy+ | Enhanced dataset generator — two-step dimensions→tuples→NL mode (DSET-01), 8-vector adversarial catalog (DSET-02), coverage rules with "Coverage check failed:" block-on-violation (DSET-03), Mode 4 curation with AskUserQuestion-confirmed deletions (DSET-04), category+dimension tagging for slice analysis (DSET-05), multi-turn shape with perturbation scenarios (DSET-06), RAG shape with expected_source_chunk_ids (DSET-07), promote-from-trace preserving input/output/intermediate_steps/metadata (DSET-08) |

    **`/orq-agent:datasets` requirement coverage (Phase 39):**

    - DSET-01 — Two-step generation mode via `--mode two-step` emits inspectable dimensions.md + tuples.md intermediate artifacts before NL generation.
    - DSET-02 — 8-vector adversarial catalog (persona-breaking, instruction-override, language-switching, formality-mismatch, refusal, format-forcing, multi-turn-manipulation, contradiction); 15-20% coverage, ≥3 per relevant vector; catalog lives in `agents/dataset-generator/resources/adversarial-vectors.md`.
    - DSET-03 — Coverage rules (every dimension value ≥2; no value >30%); violations block upload with "Coverage check failed:" remediation; see `agents/dataset-generator/resources/coverage-rules.md`.
    - DSET-04 — `--mode curation` dedupes, rebalances, fills gaps, resolves contradictions; every deletion requires AskUserQuestion confirm.
    - DSET-05 — Every datapoint tagged with `category` AND `dimension_values` so results-analyzer can slice.
    - DSET-06 — `--shape multi-turn` produces `messages` array + `perturbation_scenario` for conversational agents.
    - DSET-07 — `--shape rag` produces `expected_source_chunk_ids` for KB agents; consumed by RAGAS evaluators.
    - DSET-08 — `--mode promote-trace --trace-id <id>` fetches via MCP get_span/list_spans and preserves input, output, intermediate_steps, metadata (session_id, user_id, customer_id, identity).

    Resource docs under `orq-agent/agents/dataset-generator/resources/` (adversarial-vectors, coverage-rules, shapes) are consumed only by `agents/dataset-generator.md` (single-consumer; see Resources Policy below).
    ```

    (c) In the Resources Policy section's "Migration status" paragraph, append a sentence: "Phase 39 adds a third per-skill resources directory at `orq-agent/agents/dataset-generator/resources/` (3 files: adversarial-vectors, coverage-rules, shapes — consumed only by `dataset-generator.md`)."

    EDIT 2 — orq-agent/commands/help.md:

    Replace the existing line:
    `  /orq-agent:datasets          Test dataset generation`
    with:
    `  /orq-agent:datasets          Test dataset generation (--mode two-step|flat|curation|promote-trace, --trace-id, --shape single|multi-turn|rag)`

    Keep the line length reasonable; if it overflows the banner visual width noticeably, split the flag summary onto a continuation line prefixed with spaces so the command column alignment is preserved. Do NOT reorder the commands list (pipeline-order is load-bearing).

    DO NOT TOUCH: orq-agent/commands/orq-agent.md, orq-agent/commands/prompt.md, orq-agent/commands/architect.md.
  </action>
  <verify>
    <automated>
grep -q "Phase 39 (Dataset Generator Enhancements)" orq-agent/SKILL.md && \
grep -q "dataset-generator/resources" orq-agent/SKILL.md && \
grep -q "adversarial-vectors.md" orq-agent/SKILL.md && \
grep -q "coverage-rules.md" orq-agent/SKILL.md && \
grep -q "shapes.md" orq-agent/SKILL.md && \
grep -q "DSET-01" orq-agent/SKILL.md && \
grep -q "DSET-08" orq-agent/SKILL.md && \
grep -q -- "--mode two-step" orq-agent/commands/help.md && \
grep -q -- "--trace-id" orq-agent/commands/help.md && \
grep -q -- "--shape" orq-agent/commands/help.md && \
bash orq-agent/scripts/lint-skills.sh && \
bash orq-agent/scripts/check-protected-pipelines.sh
    </automated>
  </verify>
  <acceptance_criteria>
    - All 10 grep anchors exit 0.
    - Full-suite lint exits 0.
    - check-protected-pipelines.sh exits 0 (3/3 SHA-256 intact).
  </acceptance_criteria>
  <done>
    SKILL.md + help.md surfaced Phase 39 DSET-01..08 coverage; protected pipelines untouched; lint + hash checks green.
  </done>
</task>

</tasks>

<verification>
Full-suite lint + protected-pipeline check + 10 grep anchors.
</verification>

<success_criteria>
- SKILL.md has new Phase 39 block with DSET-01..08 coverage bullets.
- SKILL.md directory tree lists the 3 new resource files.
- help.md datasets line lists new flags.
- Protected pipelines SHA-256 3/3 intact.
</success_criteria>

<output>
After completion, create `.planning/phases/39-dataset-generator-enhancements/39-04-SUMMARY.md`.
</output>
