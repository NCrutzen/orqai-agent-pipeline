---
phase: 35-model-selection-discipline
plan: 03
type: execute
wave: 2
depends_on:
  - "35-01"
files_modified:
  - orq-agent/agents/spec-generator.md
autonomous: true
requirements:
  - MSEL-02
must_haves:
  truths:
    - "Spec-generator documents a `snapshot-pinned` rule: every emitted `model:` field MUST use a dated snapshot"
    - "The rule embeds the exact regex used by `lint-skills.sh` snapshot-pinned-models so the subagent and the lint check enforce the same contract"
    - "An embedding/speech alias exception is documented with the required comment shape `# alias-only -- pinning unavailable <date>`"
    - "Spec-generator knows how to consume the `cascade-candidate` tag from researcher output and emit a cascade block with quality-equivalence experiment instructions"
    - "All 9 SKST sections remain intact (`bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/spec-generator.md` exits 0)"
    - "No floating-alias model IDs introduced (`bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file orq-agent/agents/spec-generator.md` exits 0)"
  artifacts:
    - path: "orq-agent/agents/spec-generator.md"
      provides: "Snapshot-pinning enforcement rule + cascade block emission instructions; aligns with `lint-skills.sh` rule and with researcher.md Model Selection Policy"
      contains: "snapshot-pinned"
  key_links:
    - from: "spec-generator.md new Model Pinning section"
      to: "orq-agent/scripts/lint-skills.sh snapshot-pinned-models rule"
      via: "Subagent embeds the same regex `(-latest|:latest|-beta)$` so LLM-time rejection matches lint-time rejection; spec-generator also runs a self-check regex before emitting output"
      pattern: "regex reject"
    - from: "spec-generator.md cascade block template"
      to: "researcher.md Cascade Pattern section"
      via: "Researcher emits `cascade-candidate: true` + `quality-equivalence experiment` block; spec-generator reads the tag and renders a Model subsection with the cascade model pair + experiment instructions inline in the spec"
      pattern: "cascade-candidate"
---

<objective>
Update `orq-agent/agents/spec-generator.md` to embed a Model Pinning rule (MSEL-02) and a Cascade Emission subsection that consumes the `cascade-candidate` tag from researcher output. The new Model Pinning rule MUST embed the exact regex used by the `snapshot-pinned-models` lint rule so the subagent enforces the same contract at generation time that the lint enforces at review time. The embedding/speech alias exception is documented with the required comment shape. All 9 SKST sections must remain valid.

Purpose: Phase 35's text policy lives in TWO places: researcher.md decides WHAT to recommend, spec-generator.md decides HOW to EMIT it. Without snapshot-pinning discipline at the emission layer, floating aliases will slip through even if researcher recommendations are correct. The lint rule (Plan 01) catches violations post-hoc; embedding the regex + self-check into spec-generator makes violations rare at emission time.

Output: A spec-generator subagent file that passes `lint-skills.sh --file`, `snapshot-pinned-models --file`, AND contains four specific grep-verifiable elements: `snapshot-pinned`, `regex reject`, `alias-only -- pinning unavailable`, and a `cascade-candidate` handling subsection.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-model-selection-discipline/35-CONTEXT.md
@.planning/phases/35-model-selection-discipline/35-VALIDATION.md
@.planning/phases/34-skill-structure-format-foundation/34-03-SUMMARY.md
@orq-agent/agents/spec-generator.md

<interfaces>
<!-- Key structural constraints from the existing spec-generator.md (must preserve) -->

Spec-generator subagent YAML frontmatter (lines 1-6, must remain byte-identical):
- `name: orq-spec-generator`
- `tools: Read, Glob, Grep`
- `model: inherit`

9 SKST sections (must remain intact — Phase 34 validated). Key anchors:
- `## Constraints` (line ~897) — already mentions `MSEL-02` pinning requirement; Plan 03 reinforces this with a dedicated Model Pinning subsection + explicit regex.
- `## When to use` / `## When NOT to use` / `## Companion Skills` / `## Done When` / `## Destructive Actions` / `## Anti-Patterns to Avoid` / `## Open in orq.ai` / `## Documentation & Resolution` — all must remain.

**Insertion points for new content:**

1. **Model Pinning subsection** (inside existing H2 `### Model`, around line 319): ADD a new subsection `#### Snapshot Pinning Rule (MSEL-02)` immediately after the existing paragraph ending with "Examples of valid format: `anthropic/claude-sonnet-4-5`, `openai/gpt-4o`, `google-ai/gemini-2.5-pro`". This placement keeps pinning next to format guidance so the LLM sees both together.

2. **Cascade Emission subsection** (same `### Model` H3): ADD a new subsection `#### Cascade Block Emission (MSEL-03)` immediately AFTER the Snapshot Pinning subsection. When the research brief carries `cascade-candidate: true`, spec-generator must render a cascade Model block with the quality-equivalence experiment instructions inline in the spec.

**Required verbatim phrases** (each MUST appear at least once; grep-verifiable):
1. `snapshot-pinned` — phrase used to describe the requirement
2. `regex reject` — indicates the self-check regex
3. `alias-only -- pinning unavailable` — exact shape of the embedding/speech exception comment (two dashes, the literal string; the date appended by the LLM at generation time)
4. `cascade-candidate` — tag consumed from researcher output

**Required structural elements:**
- The same regex used by lint: `model:[[:space:]]*[^[:space:]]+(-latest|:latest|-beta)[[:space:]]*$` (or a clearly equivalent description), quoted in a bash-style code fence so the LLM sees it as a literal contract.
- A "Self-check before emitting" instruction: spec-generator runs the regex against its own draft output before finalizing.
- An embedding/speech alias exception with the exact comment shape.
- A Cascade Block template that references the researcher's `quality-equivalence experiment` output verbatim.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Insert Snapshot Pinning + Cascade Emission subsections into spec-generator.md (MSEL-02 + MSEL-03 consumption)</name>
  <files>orq-agent/agents/spec-generator.md</files>
  <read_first>
    - .planning/phases/35-model-selection-discipline/35-CONTEXT.md (locked decisions, especially §decisions §Snapshot pinning and §Cascade-aware pattern)
    - .planning/phases/35-model-selection-discipline/35-VALIDATION.md (Per-Task 35-03-01, 35-03-02)
    - orq-agent/scripts/lint-skills.sh (confirm the exact regex to embed — must match lint's regex)
    - orq-agent/agents/spec-generator.md (the file being modified)
    - orq-agent/agents/researcher.md (Plan 02 may have landed before this task; consume `cascade-candidate` output shape)
  </read_first>
  <behavior>
    - After edit: `grep -q "snapshot-pinned" orq-agent/agents/spec-generator.md` exits 0
    - After edit: `grep -q "regex reject" orq-agent/agents/spec-generator.md` exits 0
    - After edit: `grep -q "alias-only -- pinning unavailable" orq-agent/agents/spec-generator.md` exits 0
    - After edit: `grep -q "cascade-candidate" orq-agent/agents/spec-generator.md` exits 0
    - After edit: `bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/spec-generator.md` exits 0 (9 SKST sections intact, `tools:` frontmatter intact)
    - After edit: `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file orq-agent/agents/spec-generator.md` exits 0 (no floating-alias IDs slipped into the new subsections; illustrative examples MUST use dated snapshots)
    - After edit: full-suite `bash orq-agent/scripts/lint-skills.sh` still exits 0
    - After edit: `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0 (this plan does NOT touch command files)
  </behavior>
  <action>
    Edit `orq-agent/agents/spec-generator.md`. Locate the existing `### Model` H3 subsection (around line 319) that currently ends with:

    > `Examples of valid format: \`anthropic/claude-sonnet-4-5\`, \`openai/gpt-4o\`, \`google-ai/gemini-2.5-pro\``

    **Change 1 — Replace those illustrative examples with dated-snapshot examples.** The existing line uses floating aliases as examples, which is inconsistent with the new rule. Change the line to:

    ```
    Examples of valid format: `anthropic/claude-sonnet-4-5-20250929`, `openai/gpt-4o-2024-11-20`, `google-ai/gemini-2.5-pro` (use `models-list` MCP tool to get current dated snapshots).
    ```

    (Note: `gemini-2.5-pro` stays as-is because Google's model naming uses a semver-like scheme rather than dated snapshots; the MCP `models-list` output is authoritative. The lint regex only rejects `-latest|:latest|-beta` suffixes, which `gemini-2.5-pro` does not carry, so it stays compliant.)

    **Change 2 — Insert `#### Snapshot Pinning Rule (MSEL-02)` subsection IMMEDIATELY AFTER the Change 1 line** (and before the existing `### Response Format` H3):

    ```markdown
    #### Snapshot Pinning Rule (MSEL-02)

    Every `model:` field you emit MUST pin to a dated snapshot (or an equivalently stable, non-floating identifier). This is the spec-generator's most critical single-rule responsibility for model hygiene — researcher.md picks WHICH model, spec-generator.md guarantees it is snapshot-pinned.

    **Rule:** A model ID is snapshot-pinned if its final suffix is NOT one of `-latest`, `:latest`, `-beta`. Use this regex to self-check every emitted `model:` line before finalizing the spec:

    ```bash
    # regex reject: any model: line ending with a floating alias
    model:[[:space:]]*[^[:space:]]+(-latest|:latest|-beta)[[:space:]]*$
    ```

    This regex is identical to the `snapshot-pinned-models` rule in `orq-agent/scripts/lint-skills.sh`. Enforcing it at emission time (inside this subagent's self-check) AND at review time (lint) gives two independent guards against floating-alias regressions.

    **Self-check before emitting:**

    1. Draft the spec as usual, filling in `model: <recommended-from-research-brief>`.
    2. Before writing the final output, run the regex mentally (or literally, if you are using tools) against every `model:` line in your draft.
    3. If any line matches the regex, REWRITE that line with a dated snapshot equivalent. Use the MCP `models-list` tool to find the current dated snapshot ID for the provider+model family (e.g., `anthropic/claude-sonnet-4-5` → `anthropic/claude-sonnet-4-5-20250929`).
    4. Only after zero matches does the spec pass self-check.

    **Embedding/speech alias exception:**

    Some Orq.ai embedding models and speech models are exposed ONLY as aliases — the platform does not publish dated snapshots for them. For these legitimate alias-only cases, emit the alias and append an inline YAML comment with the EXACT shape `# alias-only -- pinning unavailable <YYYY-MM-DD>`, where `<YYYY-MM-DD>` is the date you verified via `models-list` that no dated snapshot existed:

    ```yaml
    model: some-provider/some-embedding-model  # alias-only -- pinning unavailable 2026-04-20
    ```

    This exception applies to embedding + speech models ONLY. Chat, tool-calling, code, and vision models ALWAYS have dated snapshots available; there is no alias-only exception path for those categories.

    #### Cascade Block Emission (MSEL-03)

    When the research brief's Model Recommendation carries `cascade-candidate: true`, do NOT collapse it into a single `model:` line. Instead, emit a full Cascade block in the agent spec's `## Model` section. The block includes the cheap primary, the capable escalation target, the trigger condition, and the quality-equivalence experiment instructions verbatim from the research brief.

    **Cascade block template** (render into the generated spec when `cascade-candidate: true`):

    ```yaml
    # Model configuration -- cascade pattern per Phase 35 MSEL-03
    # cascade-candidate: true
    # approved: false  -- flips true after the quality-equivalence experiment (Phase 42 runtime)
    model:
      primary_cheap: provider/cheap-model-DATED-SNAPSHOT
      escalation_capable: provider/capable-model-DATED-SNAPSHOT
      trigger: "escalate when cheap-tier confidence < 0.7"
      quality_equivalence_experiment:
        dataset: "{{swarm_test_dataset_id}}"
        evaluator_suite: "{{swarm_evaluator_set}}"
        tolerance_percentage_points: 5
        rule: "cascade approved only if (capable_pass_rate - cheap_pass_rate) <= tolerance"
    ```

    The `DATED-SNAPSHOT` placeholders above MUST be replaced with real dated snapshots from the research brief at generation time — they are literal markers that trigger the Snapshot Pinning Rule self-check above.

    When `cascade-candidate: false` (the default for normal use cases), emit a single `model: provider/capable-model-DATED-SNAPSHOT` line and populate `fallback_models:` from the research brief's Alternatives list (skip alternatives tagged `after quality baseline run` — those are cost-tier downgrades the user has not opted into yet; they belong in the research brief's audit trail, not in the production spec).
    ```

    **Change 3 — Update the existing Constraints bullet at line ~899.** The current line:

    > `- **NEVER** use floating model aliases (\`claude-sonnet-4-5\`) — pin to snapshot (\`claude-sonnet-4-5-20250929\`) per Phase 35 MSEL-02.`

    Replace with:

    ```
    - **NEVER** use floating model aliases (`claude-sonnet-4-5`) — pin to snapshot (`claude-sonnet-4-5-20250929`) per Phase 35 MSEL-02. See `#### Snapshot Pinning Rule (MSEL-02)` subsection in `### Model` above for the self-check regex and the `alias-only -- pinning unavailable <date>` embedding/speech exception shape.
    ```

    **Change 4 — Add a Pre-Output Validation checklist item** (around the existing checklist near line ~600). Insert a new bullet AFTER the existing model-validation bullet:

    ```
    - [ ] Every `model:` line passes the `regex reject: (-latest|:latest|-beta)$` self-check (MSEL-02 snapshot-pinned). Embedding/speech alias-only exceptions carry the comment `# alias-only -- pinning unavailable <YYYY-MM-DD>`.
    ```

    **Do NOT modify** any other Constraints bullet, any frontmatter key, the `<files_to_read>` block, or the existing Few-Shot Example (it already uses `anthropic/claude-sonnet-4-5` in one place — that is inside a generated spec illustration, which downstream has its own dated-snapshot enforcement via the new Pre-Output Validation checklist; the example's illustrative aliases do NOT match the lint regex because they lack the `-latest`/`:latest`/`-beta` suffix).

    **Verification after edit:**

    1. Four phrase-presence greps (all must exit 0):
       ```bash
       grep -q "snapshot-pinned" orq-agent/agents/spec-generator.md
       grep -q "regex reject" orq-agent/agents/spec-generator.md
       grep -q "alias-only -- pinning unavailable" orq-agent/agents/spec-generator.md
       grep -q "cascade-candidate" orq-agent/agents/spec-generator.md
       ```

    2. SKST + snapshot-pin lint on just the modified file (must exit 0):
       ```bash
       bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/spec-generator.md
       bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file orq-agent/agents/spec-generator.md
       ```

    3. Full suite (must still exit 0):
       ```bash
       bash orq-agent/scripts/lint-skills.sh
       ```

    4. Protected pipelines (must still exit 0):
       ```bash
       bash orq-agent/scripts/check-protected-pipelines.sh
       ```
  </action>
  <verify>
    <automated>bash -c 'cd /Users/nickcrutzen/developer/orqai-agent-pipeline && grep -q "snapshot-pinned" orq-agent/agents/spec-generator.md && grep -q "regex reject" orq-agent/agents/spec-generator.md && grep -q "alias-only -- pinning unavailable" orq-agent/agents/spec-generator.md && grep -q "cascade-candidate" orq-agent/agents/spec-generator.md && grep -q "Snapshot Pinning Rule (MSEL-02)" orq-agent/agents/spec-generator.md && grep -q "Cascade Block Emission (MSEL-03)" orq-agent/agents/spec-generator.md && bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/spec-generator.md && bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file orq-agent/agents/spec-generator.md && bash orq-agent/scripts/lint-skills.sh && bash orq-agent/scripts/check-protected-pipelines.sh'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "#### Snapshot Pinning Rule (MSEL-02)" orq-agent/agents/spec-generator.md` exits 0 (new subsection present)
    - `grep -q "#### Cascade Block Emission (MSEL-03)" orq-agent/agents/spec-generator.md` exits 0 (new subsection present)
    - `grep -q "snapshot-pinned" orq-agent/agents/spec-generator.md` exits 0 (MSEL-02 requirement phrase)
    - `grep -q "regex reject" orq-agent/agents/spec-generator.md` exits 0 (self-check indicator)
    - `grep -q "alias-only -- pinning unavailable" orq-agent/agents/spec-generator.md` exits 0 (embedding/speech exception shape, exact string)
    - `grep -q "cascade-candidate" orq-agent/agents/spec-generator.md` exits 0 (MSEL-03 tag consumption)
    - `grep -q "MSEL-02" orq-agent/agents/spec-generator.md` exits 0 (requirement traceable inline)
    - `grep -q "MSEL-03" orq-agent/agents/spec-generator.md` exits 0
    - `grep -q "(-latest|:latest|-beta)" orq-agent/agents/spec-generator.md` exits 0 (embedded regex matches lint rule's regex)
    - `grep -q "claude-sonnet-4-5-20250929" orq-agent/agents/spec-generator.md` exits 0 (dated-snapshot example present)
    - `grep -q "quality_equivalence_experiment" orq-agent/agents/spec-generator.md` exits 0 (cascade block template has the experiment key)
    - `bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/spec-generator.md` exits 0 (9 SKST sections intact)
    - `bash orq-agent/scripts/lint-skills.sh --rule snapshot-pinned-models --file orq-agent/agents/spec-generator.md` exits 0 (no floating-alias IDs slipped into the file)
    - `bash orq-agent/scripts/lint-skills.sh` (full suite) exits 0 (no regressions)
    - `bash orq-agent/scripts/check-protected-pipelines.sh` exits 0 (command files untouched)
    - Frontmatter `tools: Read, Glob, Grep` unchanged: `grep -c "^tools: Read, Glob, Grep$" orq-agent/agents/spec-generator.md` returns `1`
    - `<files_to_read>` block unchanged: `grep -c "^<files_to_read>$" orq-agent/agents/spec-generator.md` returns `1`
  </acceptance_criteria>
  <done>
    Spec-generator subagent now enforces MSEL-02 (snapshot pinning via embedded regex + self-check + embedding/speech alias exception) and consumes MSEL-03 cascade tags (renders a cascade block with `quality_equivalence_experiment` instructions when `cascade-candidate: true`). All 9 SKST sections pass lint. No floating-alias IDs introduced. Frontmatter and `<files_to_read>` unchanged. Aligned with researcher.md Plan 02 output shape.
  </done>
</task>

</tasks>

<verification>
Wave 2 (Plan 03) verification — spec-generator.md edits pass all phrase-presence + SKST + snapshot-pin checks:

1. 4 required verbatim phrases present (`snapshot-pinned`, `regex reject`, `alias-only -- pinning unavailable`, `cascade-candidate`).
2. New subsection headings `#### Snapshot Pinning Rule (MSEL-02)` + `#### Cascade Block Emission (MSEL-03)` present.
3. Embedded regex matches lint rule's regex.
4. `lint-skills.sh --file` exits 0 (9 SKST sections intact).
5. `lint-skills.sh --rule snapshot-pinned-models --file` exits 0 (no floating aliases in the new subsections).
6. Full-suite lint + protected-pipeline check still exit 0.
7. Frontmatter and `<files_to_read>` unchanged.
</verification>

<success_criteria>
- [ ] `#### Snapshot Pinning Rule (MSEL-02)` subsection inserted inside `### Model`
- [ ] `#### Cascade Block Emission (MSEL-03)` subsection inserted inside `### Model`
- [ ] 4 mandatory phrases present and grep-verifiable
- [ ] Embedded regex `(-latest|:latest|-beta)` matches lint rule regex
- [ ] Embedding/speech alias exception documented with exact shape `# alias-only -- pinning unavailable <date>`
- [ ] Existing Constraints bullet reinforced with forward-ref to the new subsection
- [ ] Pre-Output Validation checklist gains a new bullet for snapshot-pinning self-check
- [ ] Illustrative model IDs in updated examples are dated snapshots
- [ ] `tools:` frontmatter + `<files_to_read>` unchanged
- [ ] `lint-skills.sh --file` exits 0
- [ ] `lint-skills.sh --rule snapshot-pinned-models --file` exits 0
- [ ] Full-suite lint + protected-pipeline check still exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/35-model-selection-discipline/35-03-SUMMARY.md` documenting:
- New `#### Snapshot Pinning Rule (MSEL-02)` + `#### Cascade Block Emission (MSEL-03)` subsections inserted into spec-generator.md with line numbers
- 4 verbatim phrases confirmed via grep with line numbers
- Captured exit codes for all verification commands
- Confirmation that `tools:` frontmatter and `<files_to_read>` are unchanged
- Embedded regex `(-latest|:latest|-beta)` confirmed identical to lint rule regex
- Downstream handoff: "Plan 04 (catalog Capable Tier table) finishes Wave 2; Plan 05 runs full verification + writes VERIFICATION.md."
</output>
