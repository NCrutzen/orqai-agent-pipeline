---
phase: 42-evaluator-validation-iterator-enrichments
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/agents/results-analyzer.md
autonomous: true
requirements: [ITRX-04]
must_haves:
  truths:
    - "results-analyzer flags regressions with ⚠️ emoji when any evaluator score drops vs previous run"
  artifacts:
    - path: "orq-agent/agents/results-analyzer.md"
      provides: "Regression flag logic + ⚠️ marker rendering in test-results.md and terminal"
      contains: "⚠️, regression, previous run"
  key_links:
    - from: "orq-agent/agents/results-analyzer.md"
      to: "test-results.md rendering + terminal summary"
      via: "new regression detection phase comparing current scores to previous run"
      pattern: "⚠️"
---

<objective>
Enrich results-analyzer.md to detect per-evaluator regressions against the previous run and render ⚠️ marker in test-results.md and terminal summary.

Purpose: close ITRX-04. Constraints block already has the anchor `ALWAYS flag regressions with ⚠️ markers (Phase 42 ITRX-04)`; this plan adds the Phase-level machinery.
Output: updated results-analyzer.md only.

⚠ NOTE: This plan touches existing Phase 7 (test-results.md) and Phase 8 (terminal summary). Regression marker is ADDITIVE — existing PASS/FAIL column unchanged. Care required to preserve test-results.json schema (hardener parses it).
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-evaluator-validation-iterator-enrichments/42-CONTEXT.md
@.planning/phases/42-evaluator-validation-iterator-enrichments/42-VALIDATION.md
@orq-agent/agents/results-analyzer.md

<interfaces>
Existing Constraints already have ITRX-04 anchor:
- `ALWAYS flag regressions with ⚠️ markers (Phase 42 ITRX-04)`
This phrase MUST remain byte-identical.

Previous run data source: hardener and iterator both pass historical `test-results.json` when invoking results-analyzer for a re-test. Parameter: `previous_test_results_path` (optional; if absent, regression detection skipped).

test-results.json schema: do NOT rename or remove any existing field. Add a new `regressions[]` top-level array.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regression detection phase + ⚠️ rendering in test-results.md and terminal summary</name>
  <files>orq-agent/agents/results-analyzer.md</files>
  <action>
Read results-analyzer.md in full. Preserve YAML, existing Phase 1-8, Constraints (already has ITRX-04 anchor), Anti-Patterns, Open in orq.ai, Documentation & Resolution.

Insertions:

1. **Parameters table (existing table in file)** — add one row:
   | `previous_test_results_path` | string | null | When provided, results-analyzer compares current medians against previous to detect regressions per ITRX-04 |

2. **New Phase 2.5: `## Phase 2.5: Detect Regressions Against Previous Run (ITRX-04)`** — insert between existing Phase 2 (Triple-Run Aggregation) and Phase 3 (Determine Pass/Fail). The verbatim phrases `regression` and `previous run` MUST appear; the `⚠️` emoji MUST appear literally (not `[WARN]` — decided in CONTEXT Claude's Discretion, lean emoji).
   - Rule: if `previous_test_results_path` is non-null AND file exists, load previous per-agent per-evaluator medians. For each `(agent_key, evaluator)` pair:
     - `delta = current_median - previous_median`
     - If `delta < 0` → regression detected. Record in `regressions[]` top-level array of test-results.json:
       ```json
       {
         "agent_key": "...",
         "evaluator": "...",
         "previous_median": 0.84,
         "current_median": 0.78,
         "delta": -0.06,
         "previous_run_id": "...",
         "current_run_id": "..."
       }
       ```
   - If `previous_test_results_path` is null, skip regression detection and set `regressions: []`.
   - Never synthesize a "regression" without citing previous_median + previous_run_id (anti-pattern: ungrounded regression claims).

3. **Phase 6 (Write test-results.json)** — Step 6.1 Build JSON Structure: add top-level key `"regressions": []` next to existing `results` and `summary`. Update the schema snippet in-place.

4. **Phase 7 (Write test-results.md) Step 7.2 (Per-Agent Sections)** — modify the Evaluator Scores table to add a new column `Δ vs Prev` showing delta and ⚠️ marker:
   - For regressed evaluators: prefix median with `⚠️ ` and show delta: `| ⚠️ exactness | 0.78 | 0.015 | [...] | PASS | 0.80 | -0.06 |`
   - For non-regressed: delta column shows `+X.XX` or `—` (em-dash) when no previous run.
   - Column header: `Δ vs Prev`.

5. **Phase 7 Step 7.5 (Summary Section)** — if `regressions` array is non-empty, insert a new H3 above the existing Summary: `### ⚠️ Regressions Detected` with a small table listing each regression.

6. **Phase 8 (Terminal Summary) Step 8.1 (Compact Mode)** — in the Status column, when an agent has any regression, prefix with `⚠️`: `| {agent-key} | struct | 0.85 | ⚠️ PASS |`.

7. **Done When checklist** — append:
   - [ ] Regressions detected against previous run (when provided) and stored in test-results.json `regressions[]` (ITRX-04)
   - [ ] ⚠️ marker rendered in test-results.md Δ column and terminal Status column on any score drop

Preserve all non-listed sections byte-identical. DO NOT rename any existing JSON field (hardener parses test-results.json).
  </action>
  <verify>
    <automated>bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/results-analyzer.md &amp;&amp; grep -q "⚠️" orq-agent/agents/results-analyzer.md &amp;&amp; grep -qE "regression" orq-agent/agents/results-analyzer.md &amp;&amp; grep -qE "previous run" orq-agent/agents/results-analyzer.md</automated>
  </verify>
  <done>3 grep anchors present (⚠️, regression, previous run); lint exits 0; Phase 2.5 added; test-results.json schema gains regressions[] array; test-results.md + terminal render ⚠️ on any drop.</done>
</task>

</tasks>

<verification>
Run: `bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/results-analyzer.md` (exit 0)
Run: `bash orq-agent/scripts/check-protected-pipelines.sh` (3/3 SHA-256 match)
Grep 3 anchors: ⚠️ | regression | previous run
</verification>

<success_criteria>
- results-analyzer.md lint-clean
- 3 anchors verbatim
- test-results.json schema gains regressions[] without removing any existing key
- Protected pipeline SHA-256 3/3 intact
</success_criteria>

<output>
`.planning/phases/42-evaluator-validation-iterator-enrichments/42-05-SUMMARY.md`
</output>
