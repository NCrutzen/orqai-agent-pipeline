---
phase: 42-evaluator-validation-iterator-enrichments
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/agents/hardener.md
autonomous: true
requirements: [EVLD-07, EVLD-08, ITRX-06, ITRX-08]
must_haves:
  truths:
    - "hardener refuses to promote any evaluator to runtime guardrail unless TPR ≥ 90% AND TNR ≥ 90% on test set"
    - "hardener sets evaluator sample_rate with volume-based defaults: 100% <1K/day, 30% 1K–100K/day, 10% ≥100K/day"
    - "hardener exposes human-review-queue hook requiring minimum human-reviewed spans before promotion (configurable, tier-gated)"
    - "hardener applies prevalence correction formula when reporting estimated true success rates"
  artifacts:
    - path: "orq-agent/agents/hardener.md"
      provides: "TPR/TNR gate + sample_rate defaults + human-review-queue hook + prevalence correction"
      contains: "TPR ≥ 90%, TNR ≥ 90%, sample_rate, 100%, 30%, 10%, human-review-queue, prevalence correction, theta_hat"
  key_links:
    - from: "orq-agent/agents/hardener.md"
      to: "Phase 5 guardrail PATCH payload"
      via: "sample_rate field with volume-based default selection logic"
      pattern: "sample_rate"
---

<objective>
Enrich hardener.md to enforce TPR/TNR ≥ 90% promotion gate, sample_rate volume defaults, human-review-queue hook, and prevalence correction formula.

Purpose: close EVLD-07/08 + ITRX-06/08. Constraints block already has partial anchors (EVLD-08 + ITRX-08); this plan adds the Phase-level machinery + prevalence correction.
Output: updated hardener.md only.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-evaluator-validation-iterator-enrichments/42-CONTEXT.md
@.planning/phases/42-evaluator-validation-iterator-enrichments/42-VALIDATION.md
@orq-agent/agents/hardener.md

<interfaces>
Existing Constraints already seed anchors:
- "NEVER promote an evaluator to a guardrail unless TPR ≥ 90% AND TNR ≥ 90% on the test set (Phase 42 EVLD-08)"
- "NEVER attach guardrails without setting a sample_rate (Phase 42 ITRX-08: 100% <1K/day, 30% 1K-100K/day, 10% ≥100K/day)"
These phrases MUST remain byte-identical.

Existing Phase 2 (Suggest Guardrails) — Step 2.3 Smart Defaults table currently has a `sample_rate` column but with fixed values (100/80); ITRX-08 replaces with volume-based logic.

Resources at `orq-agent/agents/hardener/resources/{sample-rate-volume-defaults.md, prevalence-correction.md}` are created by Plan 07.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TPR/TNR promotion gate phase, sample_rate volume logic, human-review-queue hook, prevalence correction</name>
  <files>orq-agent/agents/hardener.md</files>
  <action>
Read hardener.md in full. Preserve YAML, Phase 1-6 numbering, existing Constraints, Anti-Patterns, Open in orq.ai, Documentation & Resolution.

Insertions:

1. **New Phase 2.0: `## Phase 2.0: TPR/TNR Promotion Gate (EVLD-08)`** — insert BEFORE existing "## Phase 2: Suggest Guardrails". The phrases `TPR ≥ 90%` AND `TNR ≥ 90%` MUST appear verbatim (use Unicode ≥).
   - Rule: before any evaluator is suggested as a guardrail, hardener reads the evaluator's validation metadata from `{swarm_dir}/evaluator-validations/{evaluator_name}.json` (written by evaluator-validator subagent, Plan 06).
   - Required fields: `tpr`, `tnr`, `test_set_size`, `validated_at`.
   - Gate: if `tpr < 0.90 OR tnr < 0.90 OR test_set_size < 60 (≥30 Pass + ≥30 Fail)` → evaluator is NOT promoted; hardener emits: `"Promotion refused: {evaluator} TPR={tpr}, TNR={tnr}. Run evaluator-validator to collect labels before promoting."`
   - Pre-built guardrails (`orq_pii_detection`, `orq_harmful_moderation`, `orq_sexual_moderation`) are exempt — platform-maintained.
   - Unvalidated custom evaluators may still be attached as `settings.evaluators` (monitoring only) but NEVER as `settings.guardrails` (blocking).

2. **Modify Phase 2 Step 2.3 (Smart Defaults)** — REPLACE the existing fixed `sample_rate: 100/80` values with volume-based logic. New H4 subsection `#### Sample Rate (ITRX-08, Volume-Based Defaults)`. The verbatim phrase `sample_rate` + the three percentages `100%`, `30%`, `10%` MUST appear.
   - Lookup: hardener queries `/orq-agent:analytics --last 7d --group-by deployment` (or MCP `analytics-get`) for target agent's daily request volume.
   - Volume table:
     | Volume (requests/day) | sample_rate |
     |-----------------------|-------------|
     | < 1,000 | 100% |
     | 1,000 – 100,000 | 30% |
     | ≥ 100,000 | 10% |
   - Record reasoning in `## Guardrails` spec table: new column `Volume Rationale` (e.g., `"4.2k/day → 30%"`).
   - Safety evaluators (toxicity, harmfulness, orq_pii_detection, etc.) override volume table and use 100% regardless.
   - Reference resource: `orq-agent/agents/hardener/resources/sample-rate-volume-defaults.md`.

3. **New Phase 3.5: `## Phase 3.5: Human-Review-Queue Hook (ITRX-06)`** — insert between existing Phase 3 (Collect User Approval) and Phase 4 (Write Guardrail Config). The verbatim phrase `human-review-queue` (hyphenated, lowercase) MUST appear.
   - Rule: for agents at tier `full`, the user may opt-in via AskUserQuestion: `"Require N human-reviewed spans before promoting {evaluator}? [skip | require-N]"`. If require-N selected, the guardrail stays at `status: pending-human-review` in the spec; a background job polls `annotations-list --queue {name} --min-count N` and promotes only when reached.
   - Tier-gating: `core` and `deploy+` tiers skip this hook automatically. `full` tier surfaces it.
   - Configurable: users choose N (default 30 human-reviewed spans). Record in the `## Guardrails` table new column `Human Review Minimum`.

4. **New Phase 6.0: `## Phase 6.0: Prevalence Correction (EVLD-07)`** — insert BEFORE existing Phase 6 (Quality Gate Check). The verbatim phrases `prevalence correction` AND `theta_hat` MUST appear.
   - Rule: when hardener reports a "true success rate" in quality-report.md, the raw observed pass rate `p_observed` is corrected for judge imperfection using TPR/TNR:
     ```
     theta_hat = (p_observed + TNR − 1) / (TPR + TNR − 1)
     ```
   - Only applied when TPR + TNR > 1 (corrective range). If ≤ 1, judge is worse than random — report raw with warning.
   - quality-report.md gains a new column in the Evaluator Scores table: `Corrected (θ̂)` next to `Score`.
   - Reference resource: `orq-agent/agents/hardener/resources/prevalence-correction.md`.

5. **Done When checklist** — preserve existing rows; ensure these 4 are present (add if missing):
   - [ ] Every attached guardrail has TPR ≥ 90% and TNR ≥ 90% validation (EVLD-08)
   - [ ] Every guardrail has sample_rate set with volume-based rationale (ITRX-08)
   - [ ] Human-review-queue hook exposed on full tier (ITRX-06)
   - [ ] Prevalence correction applied to reported success rates (EVLD-07)

Preserve all non-listed sections byte-identical. Protected pipelines not touched.
  </action>
  <verify>
    <automated>bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/hardener.md &amp;&amp; for anchor in "TPR ≥ 90%" "TNR ≥ 90%" "sample_rate" "100%" "30%" "10%" "human-review-queue" "prevalence correction" "theta_hat"; do grep -q "$anchor" orq-agent/agents/hardener.md || { echo "MISSING: $anchor"; exit 1; }; done</automated>
  </verify>
  <done>All 9 grep anchors present verbatim; lint exits 0; Phase 2.0, Phase 3.5, Phase 6.0 added; Step 2.3 sample_rate logic replaced with volume-based table.</done>
</task>

</tasks>

<verification>
Run: `bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/hardener.md` (exit 0)
Run: `bash orq-agent/scripts/check-protected-pipelines.sh` (3/3 SHA-256 match)
Grep 9 anchors.
</verification>

<success_criteria>
- hardener.md lint-clean
- 9 grep anchors present verbatim
- Protected pipeline SHA-256 3/3 intact
</success_criteria>

<output>
`.planning/phases/42-evaluator-validation-iterator-enrichments/42-04-SUMMARY.md`
</output>
