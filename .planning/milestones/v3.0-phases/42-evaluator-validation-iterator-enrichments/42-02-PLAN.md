---
phase: 42-evaluator-validation-iterator-enrichments
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/agents/failure-diagnoser.md
autonomous: true
requirements: [ESCI-01, ESCI-02, ESCI-08]
must_haves:
  truths:
    - "failure-diagnoser classifies every failure as one of: specification / generalization / dataset / evaluator BEFORE proposing fixes"
    - "failure-diagnoser grades outcomes not paths — no evaluator encodes exact tool-call sequences"
    - "failure-diagnoser separates dataset-quality from evaluator-quality issues in iteration-proposals.json output"
  artifacts:
    - path: "orq-agent/agents/failure-diagnoser.md"
      provides: "4-category classification + outcome-based grading + dataset/evaluator separation"
      contains: "specification, generalization, dataset, evaluator, outcome-based, no path grading, dataset-quality, evaluator-quality"
  key_links:
    - from: "orq-agent/agents/failure-diagnoser.md"
      to: "iteration-proposals.json schema"
      via: "new classification field + separated action-plan sections"
      pattern: "classification.*specification|generalization|dataset|evaluator"
---

<objective>
Enrich failure-diagnoser.md to enforce 4-category classification, outcome-based grading, and dataset-vs-evaluator issue separation.

Purpose: close ESCI-01/02/08. Constraints block already has partial ESCI-01/08 anchors; this plan upgrades them from constraint-only prose to an explicit Phase (classification step) + output-schema change.
Output: updated failure-diagnoser.md only.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-evaluator-validation-iterator-enrichments/42-CONTEXT.md
@.planning/phases/42-evaluator-validation-iterator-enrichments/42-VALIDATION.md
@orq-agent/agents/failure-diagnoser.md

<interfaces>
Existing Constraints already reference Phase 42 anchors:
- "NEVER propose prompt edits without first naming the failure mode (specification / generalization / dataset / evaluator per Phase 42 ESCI-01)"
- "ALWAYS separate dataset-quality issues from evaluator-quality issues in the output (Phase 42 ESCI-08)"
These phrases MUST remain byte-identical; the plan adds corresponding Phase + schema machinery.

Output file iteration-proposals.json (Phase 5) currently has `per_agent[].changes[]`. New additions go alongside, not inside, existing fields.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4-category classification phase + outcome-based grading rule + dataset/evaluator separation schema</name>
  <files>orq-agent/agents/failure-diagnoser.md</files>
  <action>
Read failure-diagnoser.md in full. Preserve existing Phase 1-5 flow, YAML, Constraints, Anti-Patterns, Open in orq.ai, Documentation & Resolution.

Insertions:

1. **New Phase 2.0: Classify Failure Mode** — insert BEFORE existing "## Phase 2: Diagnose Failure Patterns". H2: `## Phase 2.0: Classify Failure Mode (ESCI-01)`. Body:
   - Opening paragraph: "Before diagnosing WHICH prompt section to change, classify WHY the failure occurred. Every failure MUST fall into one of 4 classes."
   - 4-row table with verbatim class names `specification`, `generalization`, `dataset`, `evaluator`:
     | Class | Definition | Typical Fix Owner |
     |-------|------------|-------------------|
     | `specification` | Prompt/instructions incomplete or ambiguous | prompt-editor |
     | `generalization` | Prompt correct but agent fails on unseen distribution | dataset-generator + prompt-editor |
     | `dataset` | Labels wrong, missing references, contradictions in expected outputs | dataset-generator curation mode |
     | `evaluator` | Judge prompt wrong, threshold misaligned, TPR/TNR below 90% | evaluator-validator subagent |
   - Tiebreaker rules: upstream-fix-first; if both specification and generalization apply, pick specification.
   - Record the class on every `per_agent[].changes[]` entry as `classification: "specification"|"generalization"|"dataset"|"evaluator"`.

2. **New H3 inside Phase 3 (Propose Section-Level Prompt Changes)**: `### Outcome-Based Grading Rule (ESCI-02)` — insert before existing Step 3.1. Body MUST contain verbatim:
   - The phrase `outcome-based` (hyphenated).
   - The phrase `no path grading` (lowercase, verbatim).
   - Rule: "Proposed evaluators MUST grade OUTCOMES (did the final output satisfy the user intent?) not PATHS (did the agent call tool X before tool Y?). Do not encode exact tool-call sequences in evaluator logic."
   - Anti-example row: ❌ "Evaluator checks `tool_calls[0] == 'search'`" → ✅ "Evaluator checks `output mentions source URL`".
   - `(Phase 42 ESCI-02)`.

3. **Upgrade Phase 5 schema (Write iteration-proposals.json)** — ESCI-08 separation. Change the JSON schema in Step 5 (top-level `per_agent[]` object) to have TWO action-plan arrays instead of the single `changes[]`. Keep `changes[]` for backwards-compat but ADD:
   - `dataset_quality_issues[]` — each entry: `{issue: "mislabeled|missing-reference|contradiction", eval_ids: [...], evidence: "...", recommended_action: "invoke dataset-generator --mode curation"}`
   - `evaluator_quality_issues[]` — each entry: `{issue: "judge-prompt-ambiguous|threshold-misaligned|TPR-below-90", evaluator_name: "...", evidence: "...", recommended_action: "invoke evaluator-validator"}`
   - Document the separation rule verbatim: "dataset-quality and evaluator-quality issues MUST be in separate arrays — never conflated into changes[]."
   - The literal phrases `dataset-quality` and `evaluator-quality` MUST appear in the surrounding prose so grep anchors fire.

4. **Done When checklist** — append:
   - [ ] Every failure classified as specification / generalization / dataset / evaluator (ESCI-01)
   - [ ] Outcome-based grading rule documented (ESCI-02, no path grading)
   - [ ] iteration-proposals.json separates dataset-quality from evaluator-quality (ESCI-08)

Preserve all non-listed sections byte-identical. Protected pipelines not touched.
  </action>
  <verify>
    <automated>bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/failure-diagnoser.md &amp;&amp; for anchor in "specification" "generalization" "dataset" "evaluator" "outcome-based" "no path grading" "dataset-quality" "evaluator-quality"; do grep -q "$anchor" orq-agent/agents/failure-diagnoser.md || { echo "MISSING: $anchor"; exit 1; }; done</automated>
  </verify>
  <done>All 8 grep anchors present; lint exits 0; Phase 2.0 classification table present with 4 class names; schema shows 2 separate arrays.</done>
</task>

</tasks>

<verification>
Run: `bash orq-agent/scripts/lint-skills.sh --file orq-agent/agents/failure-diagnoser.md` (exit 0)
Run: `bash orq-agent/scripts/check-protected-pipelines.sh` (3/3 SHA-256 match)
Grep 8 anchors: specification | generalization | dataset | evaluator | outcome-based | no path grading | dataset-quality | evaluator-quality
</verification>

<success_criteria>
- failure-diagnoser.md lint-clean
- 8 ESCI anchors present verbatim
- iteration-proposals.json schema documents 3 arrays (changes, dataset_quality_issues, evaluator_quality_issues)
- Protected pipeline SHA-256 3/3 intact
</success_criteria>

<output>
`.planning/phases/42-evaluator-validation-iterator-enrichments/42-02-SUMMARY.md`
</output>
