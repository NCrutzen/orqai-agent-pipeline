---
phase: 42-evaluator-validation-iterator-enrichments
plan: 08
type: execute
wave: 2
depends_on: ["42-06", "42-07"]
files_modified:
  - orq-agent/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "SKILL.md lists evaluator-validator.md as subagent #19"
    - "SKILL.md Directory Structure block includes 3 new per-subagent resources/ subdirs with 7 files"
    - "SKILL.md Resources Policy migration status mentions Phase 42 additions"
    - "SKILL.md has '### Phase 42 (Evaluator Validation & Iterator Enrichments)' H3 block with 28 requirement coverage"
  artifacts:
    - path: "orq-agent/SKILL.md"
      provides: "Index wiring for new subagent + 3 resources subdirs + Phase 42 H3 block"
      contains: "evaluator-validator, Phase 42, EVLD, ESCI, ITRX, action-plan-template, decision-trees, sample-rate-volume-defaults, prevalence-correction, tpr-tnr-methodology, annotation-queue-setup, 4-component-judge-template"
  key_links:
    - from: "orq-agent/SKILL.md Directory Structure + Subagents table + Resources Policy"
      to: "new evaluator-validator.md + 7 resource files"
      via: "index-wiring recipe (edit only SKILL.md, protect pipelines)"
      pattern: "Phase 42"
---

<objective>
Wire Phase 42 additions into the suite index (`orq-agent/SKILL.md`): register new evaluator-validator subagent, 3 resources subdirs, 7 resource files, and emit Phase 42 H3 block under the Phases section with full 28-requirement coverage.

Purpose: reuse V3.0 index-wiring recipe — edit ONLY SKILL.md, protected pipelines untouched. This is the Wave 2 gate: all Wave 1 file changes must land first.
Output: updated SKILL.md only.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-evaluator-validation-iterator-enrichments/42-CONTEXT.md
@orq-agent/SKILL.md

<interfaces>
Precedent (from STATE.md key decisions): "V3.0 index-wiring recipe — when adding N new commands/subagents, touch exactly SKILL.md + help.md and never the 3 protected entry points (orq-agent.md/prompt.md/architect.md) — reusable for every future V3.0 phase."

Phase 42 adds NO new user-facing command, so help.md is NOT touched (per 42-CONTEXT Integration Points: "iterator/hardener surfaces pick up via /orq-agent:iterate + /orq-agent:harden"). Only SKILL.md.

Existing SKILL.md anchors (grep from earlier inspection):
- Line 51: `## Directory Structure`
- Line 301: `## Subagents`
- Line 343: `### Phase 40 (KB & Memory Lifecycle)` (within Subagents table)
- Line 347: Memory Store Generator row
- Line 443: Resources Policy migration-status paragraph (longest line — append Phase 42 clause)
- Phase 41 H3 block near line 268

Insertion order matches existing V3.0 pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add evaluator-validator to Subagents table + Directory Structure + Resources Policy + Phase 42 H3 block</name>
  <files>orq-agent/SKILL.md</files>
  <action>
Read SKILL.md in full. Verify ALL Wave 1 files landed:
```bash
test -f orq-agent/agents/evaluator-validator.md || exit 1
test -f orq-agent/agents/iterator/resources/action-plan-template.md || exit 1
test -f orq-agent/agents/iterator/resources/decision-trees.md || exit 1
test -f orq-agent/agents/hardener/resources/sample-rate-volume-defaults.md || exit 1
test -f orq-agent/agents/hardener/resources/prevalence-correction.md || exit 1
test -f orq-agent/agents/evaluator-validator/resources/tpr-tnr-methodology.md || exit 1
test -f orq-agent/agents/evaluator-validator/resources/annotation-queue-setup.md || exit 1
test -f orq-agent/agents/evaluator-validator/resources/4-component-judge-template.md || exit 1
```

**Edits (6 distinct insertion points):**

1. **`## Directory Structure` block** — extend the `agents/` subtree. Add under the existing agents listing (alphabetical-ish insertion; use the same tree-ASCII style as kb/resources block):
   ```
   evaluator-validator.md         # Phase 42: Evaluator validation (EVLD-01..06, 09, 10)
   evaluator-validator/
     resources/
       tpr-tnr-methodology.md     # Phase 42: TPR/TNR confusion matrix methodology (EVLD-06)
       annotation-queue-setup.md  # Phase 42: Annotation Queue / Human Review creation (EVLD-09)
       4-component-judge-template.md  # Phase 42: 4-component judge prompt (EVLD-03)
   iterator/
     resources/
       action-plan-template.md    # Phase 42: Action Plan template (ITRX-02, ITRX-07)
       decision-trees.md          # Phase 42: 3 decision trees (ESCI-06)
   hardener/
     resources/
       sample-rate-volume-defaults.md  # Phase 42: Volume-based sample_rate (ITRX-08)
       prevalence-correction.md   # Phase 42: theta_hat formula + worked example (EVLD-07)
   ```

2. **`## Subagents` table — add Phase 42 H3 block** after existing `### Phase 40 (KB & Memory Lifecycle)` block (and any Phase 41 if present):
   ```markdown
   ### Phase 42 (Evaluator Validation & Iterator Enrichments)

   | Subagent | Path | Purpose |
   |----------|------|---------|
   | Evaluator Validator | `agents/evaluator-validator.md` | Validates LLM-as-judge evaluators via Annotation Queue label collection (EVLD-09), train/dev/test split (EVLD-05), TPR/TNR measurement on held-out test (EVLD-06), inter-annotator agreement (EVLD-10), one-evaluator-per-failure-mode splitting (EVLD-02), binary Pass/Fail default (EVLD-01), and 4-component judge template (EVLD-03) — emits `evaluator-validations/{name}.json` consumed by hardener promotion gate |
   ```

3. **Phase 42 H3 block under the top-level Phases section** (mirror Phase 40/41 style; append after Phase 41 block around line 268):

   ```markdown
   ### Phase 42 (Evaluator Validation & Iterator Enrichments)

   | Skill | Path | Tier | Purpose |
   |-------|------|------|---------|
   | Evaluator Validator subagent | `agents/evaluator-validator.md` | full | TPR/TNR validation pipeline (EVLD-01..06, 09, 10) |
   | Tester enrichments | `agents/tester.md` | full | Run-comparison table (ITRX-03), overfitting warning (ESCI-07), capability vs regression suites (ESCI-04), ≥95% pass-rate warn (ESCI-05), isolated graders (ESCI-03) |
   | Failure-diagnoser enrichments | `agents/failure-diagnoser.md` | full | 4-category classification (ESCI-01), outcome-based grading (ESCI-02), dataset-quality vs evaluator-quality separation (ESCI-08) |
   | Iterator enrichments | `agents/iterator.md` | full | P0/P1/P2 priority (ITRX-01), Action Plan + Evidence + Success Criteria (ITRX-02, ITRX-07), evaluator-version A/B (EVLD-11), annotation-comment absorption (ITRX-09), no-repeat rule (ITRX-05), decision trees (ESCI-06) |
   | Hardener enrichments | `agents/hardener.md` | full | TPR/TNR ≥ 90% promotion gate (EVLD-08), volume-based sample_rate (ITRX-08), human-review-queue hook (ITRX-06), prevalence correction (EVLD-07) |
   | Results-analyzer enrichments | `agents/results-analyzer.md` | full | ⚠️ regression flag on any score drop (ITRX-04) |

   **Phase 42 requirement coverage (28 IDs total):**
   - **EVLD-01..06, 09, 10** — evaluator-validator subagent (binary default, one-per-mode split, 4-component template, 100+ labels, train/dev/test, TPR/TNR, Annotation Queue, IAA)
   - **EVLD-07** — hardener prevalence correction (`theta_hat` formula in quality-report.md)
   - **EVLD-08** — hardener TPR/TNR ≥ 90% promotion gate
   - **EVLD-11** — iterator evaluator-version A/B (two evaluator columns on same experiment)
   - **ESCI-01..02, 08** — failure-diagnoser (classification, outcome-based grading, dataset/evaluator-quality separation)
   - **ESCI-03..05, 07** — tester (isolated graders, capability/regression suites, ≥95% warn, overfitting warning)
   - **ESCI-06** — iterator decision trees (prompt fix vs evaluator / upgrade model / eval good enough)
   - **ITRX-01..02, 05, 07, 09** — iterator (P0/P1/P2, Action Plan, no-repeat, Evidence + Success Criteria, annotation comments)
   - **ITRX-03** — tester run-comparison table
   - **ITRX-04** — results-analyzer ⚠️ regression flag
   - **ITRX-06, 08** — hardener (human-review-queue hook, volume-based sample_rate)
   ```

4. **Resources Policy migration-status paragraph** (around line 443) — append sentence at end:
   > Phase 42 adds the seventh, eighth, and ninth per-skill resources directories at `orq-agent/agents/iterator/resources/` (2 files: action-plan-template, decision-trees — consumed only by `agents/iterator.md`), `orq-agent/agents/hardener/resources/` (2 files: sample-rate-volume-defaults, prevalence-correction — consumed only by `agents/hardener.md`), and `orq-agent/agents/evaluator-validator/resources/` (3 files: tpr-tnr-methodology, annotation-queue-setup, 4-component-judge-template — consumed only by `agents/evaluator-validator.md`).

5. **Done When checklist for SKILL.md** — update subagent count: increment "All 18 subagents under `orq-agent/agents/`" → "All 19 subagents" (because evaluator-validator is the 19th).

6. DO NOT touch:
   - `orq-agent/commands/orq-agent.md`
   - `orq-agent/commands/prompt.md`
   - `orq-agent/commands/architect.md`
   - `orq-agent/commands/help.md` (Phase 42 adds NO new user-facing command)

Verify after edit that protected pipelines are byte-identical.
  </action>
  <verify>
    <automated>bash orq-agent/scripts/lint-skills.sh --file orq-agent/SKILL.md &amp;&amp; bash orq-agent/scripts/check-protected-pipelines.sh &amp;&amp; for anchor in "evaluator-validator" "Phase 42" "EVLD" "ESCI" "ITRX" "action-plan-template" "decision-trees" "sample-rate-volume-defaults" "prevalence-correction" "tpr-tnr-methodology" "annotation-queue-setup" "4-component-judge-template"; do grep -q "$anchor" orq-agent/SKILL.md || { echo "MISSING: $anchor"; exit 1; }; done &amp;&amp; grep -qE "All 19 subagents" orq-agent/SKILL.md</automated>
  </verify>
  <done>SKILL.md updated with 4 new blocks; 12 grep anchors present; subagent count updated 18→19; lint exits 0; protected pipelines 3/3 SHA-256 match.</done>
</task>

</tasks>

<verification>
Run: `bash orq-agent/scripts/lint-skills.sh --file orq-agent/SKILL.md` (exit 0)
Run: `bash orq-agent/scripts/check-protected-pipelines.sh` (3/3 SHA-256 match — this is the critical invariant gate)
Grep 12 anchors. Grep `All 19 subagents`.
</verification>

<success_criteria>
- SKILL.md has new Phase 42 H3 block with 6-row skill table + 28 requirement coverage bullets
- Directory Structure extended with 3 new resources/ subtrees + evaluator-validator.md
- Resources Policy paragraph mentions Phase 42 additions
- Subagents table has new evaluator-validator row
- Subagent count updated 18→19
- Protected pipeline SHA-256 3/3 intact
- lint-skills.sh exits 0
</success_criteria>

<output>
`.planning/phases/42-evaluator-validation-iterator-enrichments/42-08-SUMMARY.md`
</output>
