---
phase: 82.8-stage-4-handled-overview-and-stage-1-screenshots
plan: 07
type: execute
wave: 4
depends_on: ["82.8-03", "82.8-05", "82.8-06", "82.7.1-ship"]
files_modified:
  - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-4/page.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "Stage 1 detail-pane audit-expander renders StageScreenshotStrip with before/after paths from debtor.email_labels"
    - "Stage 4 Auto-archived section's detail-pane also renders StageScreenshotStrip (same component reuse)"
    - "Detail-pane loader joins email_pipeline.emails → debtor.email_labels on email_id and threads screenshot_*_path values"
    - "Empty-state ('No screenshots available') renders when both paths null"
    - "82.7.1 has merged before this plan runs (gated checkpoint)"
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx"
      provides: "Mount-point for StageScreenshotStrip in Stage 1 audit-expander"
      contains: "StageScreenshotStrip"
  key_links:
    - from: "stage-1/page.tsx + stage-4/page.tsx loaders"
      to: "_shell/detail-pane.tsx StageScreenshotStrip"
      via: "screenshotPathsByEmailId map prop"
      pattern: "screenshot_before_path"
---

<objective>
Mount the `StageScreenshotStrip` (Plan 06) inside the Stage 1 detail-pane audit-expander, and thread `screenshot_before_path` / `screenshot_after_path` from `debtor.email_labels` through the page loader → client-shell → detail-pane → strip.

Purpose: D-03 mount. Operator opens any Stage 1 row (or Stage 4 Auto-archived row) and sees the before/after iController screenshots.
Output: Mount edit + loader plumbing for two pages.

**HARD GATE:** This plan modifies `_shell/detail-pane.tsx` — owned by Phase 82.7.1 parallel session until merged. Plan MUST pause for human confirmation that 82.7.1 has shipped before any task runs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/82.8-stage-4-handled-overview-and-stage-1-screenshots/82.8-CONTEXT.md
@.planning/phases/82.8-stage-4-handled-overview-and-stage-1-screenshots/82.8-RESEARCH.md
@web/components/automations/bulk-review/audit/StageScreenshotStrip.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: GATE — confirm Phase 82.7.1 has merged</name>
  <what-built>Nothing yet. This is a pre-flight check.</what-built>
  <how-to-verify>
    1. Run: `git log --oneline -20 main | grep -iE "82\.7\.1|phase.*82\.7\.1"`
    2. Expected: one or more commits referencing Phase 82.7.1 close.
    3. Also verify: `git log --oneline --all -- web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx | head -5` shows the most recent commit author is NOT the parallel session.
    4. Operator confirms: "82.7.1 is merged and detail-pane.tsx is free to modify."
  </how-to-verify>
  <resume-signal>Type "82.7.1 merged" or "blocked — 82.7.1 not shipped yet"</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 1: Add screenshot-paths prop to detail-pane and mount StageScreenshotStrip</name>
  <files>
    web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx
  </files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx (entire file — find Stage 1 audit-expander render block; find where other audit panels mount; see what props the detail-pane already receives)
    - web/components/automations/bulk-review/audit/StageScreenshotStrip.tsx (its Props from Plan 06)
    - web/components/automations/bulk-review/audit/Stage2EvidencePanel.tsx lines 200-228 (existing screenshot strip render — same component should appear in Stage 1 audit slot)
  </read_first>
  <action>
    **Direct-prop pattern.** Add a new prop on the detail-pane and render `StageScreenshotStrip` next to the existing Stage 1 evidence panel. Do NOT route this through `build-stage-audit-map.ts` — keep audit-map plumbing untouched.

    1. Add prop to detail-pane component:
       ```ts
       screenshotPathsByEmailId?: Record<string, { before: string | null; after: string | null }>;
       ```

    2. Import StageScreenshotStrip:
       ```ts
       import { StageScreenshotStrip } from "@/components/automations/bulk-review/audit/StageScreenshotStrip";
       ```

    3. Inside the Stage 1 audit-expander render block (find the existing `Stage1EvidencePanel` mount), render the strip immediately AFTER the evidence panel (NOT replacing it):
       ```tsx
       {selected?.email_id && (
         <StageScreenshotStrip
           beforePath={screenshotPathsByEmailId?.[selected.email_id]?.before ?? null}
           afterPath={screenshotPathsByEmailId?.[selected.email_id]?.after ?? null}
         />
       )}
       ```
       (Use the actual local variable name for the selected row's email_id — the file already destructures something like `selected` or `selectedRow`. Adapt.)

    4. Do NOT touch the Stage 2/3/4 evidence panels.
    5. Do NOT touch `_shell/selection-context.tsx`.
    6. Do NOT touch `StageFeedbackPanel.tsx`.
    7. Do NOT modify `_shell/_lib/build-stage-audit-map.ts` — direct prop is the chosen channel.
  </action>
  <verify>
    <automated>grep -q "StageScreenshotStrip" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx && grep -q "screenshotPathsByEmailId" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx && npm --prefix web run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `StageScreenshotStrip` imported in detail-pane.
    - Rendered in Stage 1 audit-expander slot.
    - Reads paths from the new `screenshotPathsByEmailId` direct prop on detail-pane.
    - Typecheck passes.
  </acceptance_criteria>
  <done>Mount-point wired; detail-pane awaits per-page loader data.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Load screenshot paths in stage-1 and stage-4 pages, pass to detail-pane</name>
  <files>
    web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-4/page.tsx
  </files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx (entire file — find where it pre-fetches email body/timeline maps per email_id, mirror that loop)
    - web/app/(dashboard)/automations/[swarm]/stage-4/page.tsx (post-Plan 05 state — body+timeline pre-fetch + the three row sources)
  </read_first>
  <action>
    For each page (`stage-1/page.tsx` and `stage-4/page.tsx`), AFTER the existing email-id collection loop and BEFORE the client-shell render:

    1. Collect all unique email_ids from the page's rows (use the same `allEmailIdsForPrefetch` set from Plan 05, or build an equivalent for stage-1).

    2. Query `debtor.email_labels` once for those ids:
       ```ts
       const screenshotPathsByEmailId: Record<string, { before: string | null; after: string | null }> = {};
       if (allEmailIdsForPrefetch.length > 0) {
         const { data: labels } = await admin
           .schema("debtor")
           .from("email_labels")
           .select("email_id, screenshot_before_path, screenshot_after_path")
           .in("email_id", allEmailIdsForPrefetch);
         for (const row of labels ?? []) {
           screenshotPathsByEmailId[row.email_id as string] = {
             before: (row.screenshot_before_path as string | null) ?? null,
             after: (row.screenshot_after_path as string | null) ?? null,
           };
         }
       }
       ```

    3. Pass `screenshotPathsByEmailId` to the page's client-shell as a prop; the client-shell threads it to UnifiedDetailPane / detail-pane.

    4. For `stage-1/page.tsx`: adapt to its existing data flow. If it does not currently aggregate emailIds in a single set, build one from whatever row source it renders (likely `unifiedRows.map(r => r.email_id)`).

    5. Do NOT cross-schema join in one query; two-pass like Plan 05's loader is simpler and matches established patterns.

    6. If the swarm is NOT `debtor-email`, the labels query returns empty (no harm); the prop is still passed but every lookup is undefined → empty-state. Acceptable.
  </action>
  <verify>
    <automated>grep -q "screenshot_before_path" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/page.tsx && grep -q "screenshot_before_path" web/app/\(dashboard\)/automations/\[swarm\]/stage-4/page.tsx && grep -q "screenshotPathsByEmailId" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/page.tsx && grep -q "screenshotPathsByEmailId" web/app/\(dashboard\)/automations/\[swarm\]/stage-4/page.tsx && npm --prefix web run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Both pages query `debtor.email_labels` for the page's email_id set.
    - Both pages pass a path-by-email_id map to their client-shell.
    - The map keys reach detail-pane's `screenshotPathsByEmailId` prop.
    - Typecheck passes.
  </acceptance_criteria>
  <done>Stage 1 + Stage 4 detail-panes both render before/after screenshots from `debtor.email_labels.screenshot_*_path`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator verification — screenshots render</name>
  <what-built>StageScreenshotStrip mounted in Stage 1 + Stage 4 detail-panes; both pages load paths from debtor.email_labels.</what-built>
  <how-to-verify>
    1. `npm --prefix web run dev`
    2. Visit `/automations/debtor-email/stage-1`. Click any row in Bulk Review that has been processed in the last hour. Expand the audit section. Expected: two thumbnails labeled "Before" / "After".
    3. Click a thumbnail → inline full-width image expands (existing ScreenshotThumb behavior).
    4. Visit `/automations/debtor-email/stage-4`. Expand the "Auto-archived" collapsible. Click a row. Expand audit. Expected: same strip renders (paths from D-07 backfilled column-extract; some historical rows may show "No screenshots available" if the URL regex returned NULL — acceptable, document if widespread).
    5. Click a row archived >1h ago: confirm the strip still works (paths are fresh because we re-sign on-demand; the old `_url` columns being stale is no longer user-visible).
  </how-to-verify>
  <resume-signal>Type "screenshots render" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- 82.7.1 confirmed merged at Task 0
- Detail-pane edit lands cleanly (no merge conflict)
- Both pages thread the path map
- Operator confirms visual render
</verification>

<success_criteria>
Operator opens any recent Stage 1 / Stage 4 Auto-archived email and sees both iController before/after screenshots. Empty-state for older rows without paths.
</success_criteria>

<output>
After completion, create `.planning/phases/82.8-stage-4-handled-overview-and-stage-1-screenshots/82.8-07-SUMMARY.md`.
</output>
