---
phase: 82.8-stage-4-handled-overview-and-stage-1-screenshots
plan: 03
type: execute
wave: 2
depends_on: ["82.8-01"]
files_modified:
  - web/lib/automations/debtor-email/label-email-in-icontroller.ts
  - web/lib/inngest/functions/debtor-email-icontroller-tagger.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "label-email-in-icontroller.ts returns screenshot_before_path and screenshot_after_path alongside the URLs"
    - "debtor-email-icontroller-tagger.ts persists both _path and _url columns to debtor.email_labels"
    - "All existing success/conflict/failure return shapes from labelEmailInIcontroller continue to carry both *_url and *_path fields (paths null when no screenshot taken)"
  artifacts:
    - path: "web/lib/automations/debtor-email/label-email-in-icontroller.ts"
      provides: "Returns paths in addition to URLs from captureScreenshot"
      contains: "screenshot_before_path"
    - path: "web/lib/inngest/functions/debtor-email-icontroller-tagger.ts"
      provides: "Writes path columns to debtor.email_labels"
      contains: "screenshot_before_path"
  key_links:
    - from: "label-email-in-icontroller.ts captureScreenshot result"
      to: "tagger updatePayload"
      via: "before.path / afterShot.path return field"
      pattern: "screenshot_before_path"
    - from: "tagger updatePayload"
      to: "debtor.email_labels"
      via: "admin.schema('debtor').from('email_labels').update(...)"
      pattern: "screenshot_before_path"
---

<objective>
Plumb the storage `path` (already returned by `captureScreenshot()` but currently discarded) all the way through to the new `screenshot_before_path` / `screenshot_after_path` columns on `debtor.email_labels`. This is D-04's capture-side change — necessary so detail-pane can sign URLs on demand.

Purpose: Existing `_url` columns are 1-hour signed URLs and useless for the detail-pane operator-flow. Paths are stable.
Output: New tagged emails automatically populate the path columns; existing `_url` columns continue to be written for back-compat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/82.8-stage-4-handled-overview-and-stage-1-screenshots/82.8-CONTEXT.md
@.planning/phases/82.8-stage-4-handled-overview-and-stage-1-screenshots/82.8-RESEARCH.md
@web/lib/browser/screenshots.ts
@web/lib/automations/debtor-email/label-email-in-icontroller.ts
@web/lib/inngest/functions/debtor-email-icontroller-tagger.ts

<interfaces>
captureScreenshot return (web/lib/browser/screenshots.ts:25-52):
```ts
async function captureScreenshot(page, opts): Promise<{ path: string; url: string } | null>;
```

Current tagger persistence (debtor-email-icontroller-tagger.ts lines 165-170):
```ts
if (tagResult.screenshot_before_url) {
  updatePayload.screenshot_before_url = tagResult.screenshot_before_url;
}
if (tagResult.screenshot_after_url) {
  updatePayload.screenshot_after_url = tagResult.screenshot_after_url;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Plumb path through label-email-in-icontroller</name>
  <files>web/lib/automations/debtor-email/label-email-in-icontroller.ts</files>
  <read_first>
    - web/lib/automations/debtor-email/label-email-in-icontroller.ts (entire file — every `return { ... screenshot_*_url ... }` branch; there are multiple — labeled/skipped_conflict/failed/skipped_already/already_correct paths)
    - web/lib/browser/screenshots.ts (verify captureScreenshot returns `{ path, url } | null`)
  </read_first>
  <action>
    Edit `web/lib/automations/debtor-email/label-email-in-icontroller.ts`:

    1. **Extend the return-type / TS interface** (whatever local type the function returns — usually inline). Add two optional/nullable fields next to the existing URL fields:
       - `screenshot_before_path: string | null`
       - `screenshot_after_path: string | null`

    2. **Capture the path alongside the url.** Today (line ~88):
       ```ts
       const before = await captureScreenshot(page, { ... });
       beforeUrl = before?.url ?? null;
       ```
       Add a parallel local:
       ```ts
       beforePath = before?.path ?? null;
       ```
       Same treatment for `afterShot` (line ~158):
       ```ts
       afterPath = afterShot?.path ?? null;
       ```
       Declare `beforePath` / `afterPath` as `let beforePath: string | null = null;` / `let afterPath: string | null = null;` near the existing `beforeUrl` / `afterUrl` declarations at function-scope so they're visible in every return branch (including the catch-block failure return).

    3. **Update EVERY return branch** in this function (use grep `return {` to find them) to include both new fields. For branches with no screenshot taken (e.g. `skipped_already`, `skipped_conflict` before capture, early returns), set both paths to `null`. For the success `labeled` branch and the `SELECTION_DID_NOT_STICK` failure (both run after capture), wire `beforePath`/`afterPath`. The error-catch block also has a return — wire whatever path-locals are populated at that point (likely just `beforePath`).

    4. Do NOT rename existing fields. Do NOT drop `screenshot_before_url` / `screenshot_after_url`. Both pairs must coexist.
  </action>
  <verify>
    <automated>grep -q "screenshot_before_path" web/lib/automations/debtor-email/label-email-in-icontroller.ts && grep -q "screenshot_after_path" web/lib/automations/debtor-email/label-email-in-icontroller.ts && grep -q "before?.path" web/lib/automations/debtor-email/label-email-in-icontroller.ts && grep -q "afterShot?.path" web/lib/automations/debtor-email/label-email-in-icontroller.ts && npm --prefix web run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Every `return { ... }` branch in this file includes both `screenshot_before_path` and `screenshot_after_path` keys (verifiable: `grep -c "screenshot_before_path" file` equals `grep -c "screenshot_before_url" file`).
    - `before?.path` and `afterShot?.path` captured into locals.
    - `npm --prefix web run typecheck` exits 0.
    - Existing `_url` field plumbing untouched.
  </acceptance_criteria>
  <done>Function returns both URLs and paths from every code path.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Persist path columns in debtor-email-icontroller-tagger</name>
  <files>web/lib/inngest/functions/debtor-email-icontroller-tagger.ts</files>
  <read_first>
    - web/lib/inngest/functions/debtor-email-icontroller-tagger.ts lines 140-185 (the updatePayload assembly + the `admin.schema('debtor').from('email_labels').update(...)` call)
  </read_first>
  <action>
    Edit `web/lib/inngest/functions/debtor-email-icontroller-tagger.ts`:

    Immediately after the existing `screenshot_after_url` write block (around line 168-170), add:

    ```ts
    if (tagResult.screenshot_before_path) {
      updatePayload.screenshot_before_path = tagResult.screenshot_before_path;
    }
    if (tagResult.screenshot_after_path) {
      updatePayload.screenshot_after_path = tagResult.screenshot_after_path;
    }
    ```

    Mirror the exact existing pattern (truthy-check guard, same `updatePayload` object). If `updatePayload` has a TS type that's strictly typed (e.g. `Partial<EmailLabelUpdate>`), extend that type — or use the existing inline-typed object pattern.

    Do NOT change the existing URL writes. Do NOT change the update call itself. Do NOT change the schema/table name.
  </action>
  <verify>
    <automated>grep -c "screenshot_before_path" web/lib/inngest/functions/debtor-email-icontroller-tagger.ts | grep -q "^1$" && grep -c "screenshot_after_path" web/lib/inngest/functions/debtor-email-icontroller-tagger.ts | grep -q "^1$" && grep -q "tagResult.screenshot_before_path" web/lib/inngest/functions/debtor-email-icontroller-tagger.ts && npm --prefix web run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `tagResult.screenshot_before_path` and `tagResult.screenshot_after_path` both referenced exactly once.
    - Pattern mirrors existing URL persistence (truthy guard + `updatePayload.X = tagResult.X`).
    - `npm --prefix web run typecheck` exits 0.
    - No existing URL persistence line removed or modified.
  </acceptance_criteria>
  <done>Tagger writes both URL and path columns to `debtor.email_labels` after a successful labeling.</done>
</task>

</tasks>

<verification>
- Path columns appear in 1:1 places where URL columns appear in both files
- `npm --prefix web run typecheck` exits 0
</verification>

<success_criteria>
New tagged emails populate the path columns; existing URL writes preserved. Read-side plans can now JOIN on these columns and sign URLs on demand.
</success_criteria>

<output>
After completion, create `.planning/phases/82.8-stage-4-handled-overview-and-stage-1-screenshots/82.8-03-SUMMARY.md`.
</output>
