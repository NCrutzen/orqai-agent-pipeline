---
plan_id: 82.8-09
status: complete
---

# 82.8-09 — Restrict Stage 4 auto-archived backfill to Stage-1-noise rows

## Outcome

Pass A of `supabase/migrations/20260518b_backfill_stage4_auto_archived_noise.sql`
now requires `pe1.id IS NOT NULL` (the LEFT JOIN to `pipeline_events stage=1`
must match). This excludes Stage 3 intent labels — which also produce
`debtor.email_labels.icontroller_tag_status='tagged'` — from being
backfilled as `auto_archived_noise`.

## Change

Single line added to Pass A WHERE clause (line 42):

```sql
  AND pe1.id IS NOT NULL                  -- 82.8-09: restrict to rows that had a Stage 1 event (excludes Stage 3 intent labels)
```

In-place edit of the existing migration file. Safe because the migration
has not been applied in any environment (confirmed via `schema_migrations`
lookup on 2026-05-18 during UAT).

## Verification

- `grep -c "pe1.id IS NOT NULL" 20260518b_…sql` → `1` ✓
- `grep -c "ON CONFLICT ON CONSTRAINT pipeline_events_one_per_stage_email DO NOTHING"` → `1` ✓ (idempotency unchanged)
- Pass B (path extraction) untouched ✓
- No schema changes ✓

## Hard-separation contract

Per `docs/agentic-pipeline/README.md`: Stage 1 noise registry and Stage 3
intent registry never cross. Live writes (Plan 02 in
`classifier-verdict-worker.ts`) were already correctly scoped to the
`categorize_archive` (Stage 1) path. The backfill now matches that scope.
