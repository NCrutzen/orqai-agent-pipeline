---
phase: 70-telemetry-consolidation-pipeline-events
plan: 07
type: execute
wave: 4
depends_on: [70-06]
files_modified:
  - docs/agentic-pipeline/promotion-recommender.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [TELE-01, TELE-02, TELE-03]
must_haves:
  truths:
    - "docs/agentic-pipeline/promotion-recommender.md exists as a one-paragraph stub describing how Phase 72 will consume pipeline_events"
    - ".planning/REQUIREMENTS.md TELE-01..03 status checkboxes are flipped to complete with verification anchors"
  artifacts:
    - path: "docs/agentic-pipeline/promotion-recommender.md"
      provides: "Phase 72 stub per D-15"
      min_lines: 15
  key_links:
    - from: "docs/agentic-pipeline/promotion-recommender.md"
      to: "public.pipeline_events"
      via: "documents the SELECT shape Phase 72 will use"
      pattern: "pipeline_events"
---

<objective>
Close out Phase 70 with the documentation deliverables required by the locked decisions:
1. Per D-15: write `docs/agentic-pipeline/promotion-recommender.md` as a one-paragraph stub describing how Phase 72 will consume pipeline_events. NO code, NO Inngest function — just the contract.
2. Flip REQUIREMENTS.md TELE-01..03 from `pending` to `complete` with anchors.

Purpose: Completes the source-artifact coverage audit. Without this plan, REQUIREMENTS.md remains stale and the Phase 72 hand-off has no design memo.
Output: One new docs file; REQUIREMENTS.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/70-telemetry-consolidation-pipeline-events/70-CONTEXT.md
@docs/agentic-pipeline/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write promotion-recommender.md stub (D-15)</name>
  <files>docs/agentic-pipeline/promotion-recommender.md</files>
  <read_first>
    - .planning/phases/70-telemetry-consolidation-pipeline-events/70-CONTEXT.md (D-15 verbatim wording)
    - docs/agentic-pipeline/README.md (cross-link target)
    - .planning/REQUIREMENTS.md (LERN-01..05 — Phase 72 requirements the stub forecasts)
  </read_first>
  <action>
    Create `docs/agentic-pipeline/promotion-recommender.md` containing:
    1. H1 title: "Promotion Recommender (Phase 72 placeholder)".
    2. Status block: "Status: STUB — design memo only. Implementation lands in Phase 72."
    3. Purpose paragraph (3-5 sentences): the recommender is an Inngest cron that scans `public.pipeline_events` to surface graduated-automation candidates — recurring LLM-handled patterns that should be promoted to deterministic regex rules, sender mappings, or prompt-tunes (REQUIREMENTS LERN-01..05).
    4. Input contract: SELECT from public.pipeline_events filtered by `swarm_type`, `stage`, `created_at >= now() - interval '30 days'`, with `decision`, `confidence`, `decision_details`, `override` columns consumed. Reference the partial index `pipeline_events_override_partial_idx` (D-04) as the hot-path lookup for override-driven candidates.
    5. Output contract (forward-ref Phase 72): writes rows to `public.promotion_candidates` (table not yet created — Phase 72 ships it) with columns `kind` ('regex_rule'|'sender_mapping'|'prompt_tune'), `expected_volume`, `expected_savings`, `evidence_event_ids` uuid[].
    6. Non-goals for v1: never blocks the synchronous pipeline (LERN-02), never auto-applies a candidate without operator approval (LERN-04).
    7. Cross-link: `See docs/agentic-pipeline/README.md` for the funnel architecture this recommender observes.
    8. Footer: "Phase 70 only ships the read-side contract. Phase 72 owns the cron + UI + approval flow."
  </action>
  <verify>
    <automated>test -f docs/agentic-pipeline/promotion-recommender.md && wc -l docs/agentic-pipeline/promotion-recommender.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path
    - wc -l docs/agentic-pipeline/promotion-recommender.md returns >=15
    - grep -c "pipeline_events" docs/agentic-pipeline/promotion-recommender.md returns >=2
    - grep -c "Phase 72" docs/agentic-pipeline/promotion-recommender.md returns >=2
    - grep -c "STUB" docs/agentic-pipeline/promotion-recommender.md returns >=1
    - grep -c "promotion_candidates" docs/agentic-pipeline/promotion-recommender.md returns >=1
  </acceptance_criteria>
  <done>Stub committed; readable; cross-links to README.md.</done>
</task>

<task type="auto">
  <name>Task 2: Flip REQUIREMENTS.md TELE-01..03 status to complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 92-96 §Telemetry consolidation; lines 160-162 traceability table)
    - .planning/phases/70-telemetry-consolidation-pipeline-events/70-CONTEXT.md (verbatim wording for cross-check)
  </read_first>
  <action>
    1. In .planning/REQUIREMENTS.md §Telemetry consolidation (lines 92-96): flip the three checkboxes:
       - `- [ ] **TELE-01**: ...` -> `- [x] **TELE-01**: ...` (same description text)
       - `- [ ] **TELE-02**: ...` -> `- [x] **TELE-02**: ...`
       - `- [ ] **TELE-03**: ...` -> `- [x] **TELE-03**: ...`
    2. In the traceability table at the bottom (lines 160-162): change `pending` to `complete` for each TELE-* row, with a verification anchor: e.g. `| TELE-01 | Phase 70 | complete (migration 20260506a applied; 5 emit sites green; tests in 70-03/04/05) |`. Use Phase 69 row format as the model.
    3. Do NOT change any other rows. Do NOT change the Coverage line.
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*TELE-0" .planning/REQUIREMENTS.md && grep -c "TELE-0.*Phase 70.*complete" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "^- \[x\] \*\*TELE-0" .planning/REQUIREMENTS.md returns 3
    - grep -c "^- \[ \] \*\*TELE-0" .planning/REQUIREMENTS.md returns 0
    - grep -E "^\| TELE-0[123] \| Phase 70 \| complete" .planning/REQUIREMENTS.md | wc -l returns 3
    - No other rows altered: git diff --stat .planning/REQUIREMENTS.md shows only the 6 expected line changes (3 checkboxes + 3 traceability rows)
  </acceptance_criteria>
  <done>3 requirements flipped to complete; traceability table updated; no collateral edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none — docs-only plan) | No new code paths, no DB writes, no PII |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-07-01 | Repudiation | REQUIREMENTS.md flipped to complete before live verification | mitigate | Phase 70 verify-work step (run after this plan) re-checks every TELE-* must-have; if any fails, revert this plan's commit |
</threat_model>

<verification>
- promotion-recommender.md exists and contains the required sections.
- REQUIREMENTS.md TELE-01..03 are checked + traceability table updated.
- Git diff on REQUIREMENTS.md shows only the 6 intended line changes.
</verification>

<success_criteria>
- D-15 stub shipped.
- REQUIREMENTS.md reflects Phase 70 completion.
- All three TELE-* requirement IDs are now traceable to this phase via complete status + verification anchors.
</success_criteria>

<output>
After completion, create `.planning/phases/70-telemetry-consolidation-pipeline-events/70-07-SUMMARY.md`. Include the diff of REQUIREMENTS.md verbatim.
</output>
