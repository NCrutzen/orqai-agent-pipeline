---
phase: 70-telemetry-consolidation-pipeline-events
plan: 04
type: execute
wave: 2
depends_on: [70-02]
files_modified:
  - web/lib/inngest/functions/debtor-email-coordinator.ts
  - web/lib/inngest/functions/__tests__/debtor-email-coordinator.test.ts
  - web/lib/inngest/functions/classifier-invoice-copy-handler.ts
  - web/lib/inngest/functions/__tests__/classifier-invoice-copy-handler.test.ts
autonomous: true
requirements: [TELE-01, TELE-02]
must_haves:
  truths:
    - "Stage 3 coordinator writes one pipeline_events row inside persist-ranked step.run with stage=3, decision=top_intent"
    - "Stage 4 invoice-copy handler writes pipeline_events rows for completed AND failure paths (early-return + failRun)"
    - "Both emits live INSIDE existing step.run blocks per D-09; no new step.run added"
    - "agent_run_id and automation_run_id populated on Stage 3/4 rows where available"
  artifacts:
    - path: "web/lib/inngest/functions/debtor-email-coordinator.ts"
      provides: "Stage 3 dual-write inside persist-ranked step.run"
      contains: "emitPipelineEvent"
    - path: "web/lib/inngest/functions/classifier-invoice-copy-handler.ts"
      provides: "Stage 4 dual-write inside write-email-label step.run + failure-branch emits"
      contains: "emitPipelineEvent"
  key_links:
    - from: "web/lib/inngest/functions/debtor-email-coordinator.ts"
      to: "web/lib/pipeline-events/emit.ts"
      via: "import { emitPipelineEvent }"
      pattern: "emitPipelineEvent\\("
    - from: "web/lib/inngest/functions/classifier-invoice-copy-handler.ts"
      to: "web/lib/pipeline-events/emit.ts"
      via: "import { emitPipelineEvent }"
      pattern: "emitPipelineEvent\\("
---

<objective>
Wire Stage 3 (intent coordinator) and Stage 4 (invoice-copy handler) dual-writes inside their existing step.run blocks. Stage 4 has multiple emit paths: success (write-email-label step), early-return "no_invoice_reference", and failRun-on-unhandled.

Purpose: Two of the four "in-step.run" emit sites; this plan + Plan 03 together cover all 4 Inngest emits.
Output: Two production files modified, two test files extended, all tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-telemetry-consolidation-pipeline-events/70-CONTEXT.md
@.planning/phases/70-telemetry-consolidation-pipeline-events/70-RESEARCH.md
@web/lib/pipeline-events/emit.ts
@web/lib/pipeline-events/types.ts
@web/lib/inngest/functions/debtor-email-coordinator.ts
@web/lib/inngest/functions/classifier-invoice-copy-handler.ts
@web/lib/inngest/functions/__tests__/debtor-email-coordinator.test.ts
@web/lib/inngest/functions/__tests__/classifier-invoice-copy-handler.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stage 3 emit inside persist-ranked step.run (coordinator)</name>
  <files>web/lib/inngest/functions/debtor-email-coordinator.ts, web/lib/inngest/functions/__tests__/debtor-email-coordinator.test.ts</files>
  <read_first>
    - web/lib/inngest/functions/debtor-email-coordinator.ts (lines 88-93 replay-safe id pattern; lines 186-192 persist-ranked step.run)
    - .planning/phases/70-telemetry-consolidation-pipeline-events/70-RESEARCH.md §"Example: Stage 3 emit"
    - web/lib/inngest/functions/__tests__/debtor-email-coordinator.test.ts (existing test scaffolding)
  </read_first>
  <behavior>
    - When persist-ranked step.run runs after the coordinator agent returns ranked intents, emit ONE pipeline_events row: swarm_type='debtor-email', stage=3, email_id (canonical uuid from event), decision = output.ranked[0].intent, confidence = numericConfidence(output.ranked[0].confidence), decision_details = {ranked: output.ranked, language: output.language, urgency: output.urgency}, agent_run_id (in scope), automation_run_id (from event), triggered_by='pipeline'
    - Emit lives INSIDE the existing persist-ranked step.run, after the coordinator_runs UPDATE throws-on-error check
    - Test extends existing coordinator test to assert pipeline_events row matches expected payload
  </behavior>
  <action>
    1. In `web/lib/inngest/functions/debtor-email-coordinator.ts`:
       - Add imports: `import { emitPipelineEvent } from "@/lib/pipeline-events/emit";` and `import { numericConfidence } from "@/lib/pipeline-events/types";` (match existing import style).
       - Locate `await step.run("persist-ranked", async () => { ... })` around lines 186-192.
       - After the `if (error) throw new Error("persist-ranked: ...")` line and before the closing `}`, add the emit call per RESEARCH §"Example: Stage 3 emit" verbatim, using actual in-scope variable names. Use SWARM_TYPE constant if defined; otherwise hardcode `"debtor-email"` with a TODO comment for Phase 73 generalization.
       - For the email_id: per RESEARCH confirmation, event.data.email_id IS the canonical uuid (email_pipeline.emails.id) in scope. Assert this and require `email_id: event.data.email_id` directly. Do NOT add a null fallback or text-id fallback — if uuid is genuinely missing, the emit should fail loudly (no defensive null pollution of Stage 3 rows). The text-id fallback in the previous draft is removed to prevent decision_details.email_id_text ambiguity.
       - For agent_run_id and automation_run_id: pass through if in scope; otherwise null.

    2. In `web/lib/inngest/functions/__tests__/debtor-email-coordinator.test.ts`:
       - Add (or extend existing) supabaseInserts mock array.
       - Add assertion in the happy-path test: `expect(supabaseInserts.find(i => i.table === 'pipeline_events' && i.payload.stage === 3 && i.payload.decision === '<expected_intent>')).toBeTruthy();`
       - Assert decision_details.ranked array length >= 1.
       - Assert numericConfidence mapping for the top intent's confidence.
       - Assert pipeline_events.email_id matches uuid regex (no null, no fallback shape).
  </action>
  <verify>
    <automated>cd web && npx vitest run lib/inngest/functions/__tests__/debtor-email-coordinator.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "emitPipelineEvent" web/lib/inngest/functions/debtor-email-coordinator.ts` returns >=2
    - `grep -c "stage: 3" web/lib/inngest/functions/debtor-email-coordinator.ts` returns >=1
    - emit is inside persist-ranked step.run: `awk '/step.run\("persist-ranked"/,/^  \}\)/' web/lib/inngest/functions/debtor-email-coordinator.ts | grep -c emitPipelineEvent` returns >=1
    - No null-fallback pollution: `grep -c "email_id_text" web/lib/inngest/functions/debtor-email-coordinator.ts` returns 0
    - `cd web && npx vitest run lib/inngest/functions/__tests__/debtor-email-coordinator.test.ts` exits 0
    - Test asserts decision_details.ranked: `grep -c "decision_details.*ranked" web/lib/inngest/functions/__tests__/debtor-email-coordinator.test.ts` returns >=1
  </acceptance_criteria>
  <done>Stage 3 emit shipped inside persist-ranked; coordinator_runs UPDATE untouched; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stage 4 emits in invoice-copy handler (success + failure branches)</name>
  <files>web/lib/inngest/functions/classifier-invoice-copy-handler.ts, web/lib/inngest/functions/__tests__/classifier-invoice-copy-handler.test.ts</files>
  <read_first>
    - web/lib/inngest/functions/classifier-invoice-copy-handler.ts (line 142 failRun, line 222 no_invoice_reference early-return, lines 421-438 write-email-label step.run)
    - .planning/phases/70-telemetry-consolidation-pipeline-events/70-RESEARCH.md §"Example: Stage 4 emit"
    - web/lib/inngest/functions/__tests__/classifier-invoice-copy-handler.test.ts (existing supabaseInserts at line ~40)
  </read_first>
  <behavior>
    - Success path (write-email-label step.run): emit stage=4, decision='completed', confidence=0.9, decision_details={handler_key:'debtor-copy-document-body-agent', invoice_ref, body_version, detected_tone, draft_url, dry_run}, automation_run_id, triggered_by='pipeline'
    - Early-return path (no_invoice_reference, line ~222): emit stage=4, decision='failed', decision_details={failure_reason:'no_invoice_reference'}, automation_run_id, triggered_by='pipeline'. Wrap in a step.run if the early-return doesn't already use one — re-use the existing surrounding step.run if present. If the early-return path has NO step.run wrapper, add the emit as a plain awaited call with a comment citing the same exemption rationale as Stage 1 (synchronous request, no replay surface) — but verify with the file's actual structure first; an Inngest function's top-level body IS replayed, so we likely need step.run.
    - failRun-on-unhandled path (line ~142): emit stage=4, decision='failed', decision_details={failure_reason:<sanitized error message>}, automation_run_id, triggered_by='pipeline'. **Pre-decided design: this is the top-level Inngest catch boundary (NOT inside any step.run). The Inngest function top-level body IS replayed on failure, so a plain awaited INSERT is NOT replay-safe here. We MUST wrap the emit in a NEW step.run with the fixed stepId `step.run("emit-stage4-failrun", ...)` BEFORE invoking failRun. Rationale (executor MUST add this as a code comment verbatim above the new step.run): "We accept the extra step.run because the alternative (plain await at the catch boundary) would lose the failure event on Inngest replay; emitting inside a fresh step.run is replay-safe and the doubled step count for the failure branch is acceptable." This is a deliberate carve-out from D-09 (which prefers reusing existing step.run blocks); the catch boundary has none to reuse, so a new step.run is justified.**
    - Tests assert ALL three emit paths: existing happy-path test asserts completed; new test asserts no_invoice_reference emit; new test asserts failRun emit.
  </behavior>
  <action>
    1. In `web/lib/inngest/functions/classifier-invoice-copy-handler.ts`:
       - Add imports for emitPipelineEvent and Stage const if useful.
       - Success path (lines 421-438 area, write-email-label step.run): add the emit per RESEARCH §"Example: Stage 4 emit" verbatim.
       - Early-return path (line ~222 area, "no_invoice_reference"): inspect the file to determine whether this branch is inside a step.run already. If yes, add the emit there. If no, wrap the emit + the early-return in a `step.run("emit-stage4-no-invoice-reference", ...)` — but per D-09 prefer reusing an existing step.run. If none exists, document the carve-out in a comment.
       - failRun branch (line ~142): wrap the emit in a NEW `step.run("emit-stage4-failrun", async () => { await emitPipelineEvent(admin, { swarm_type: 'debtor-email', stage: 4, decision: 'failed', decision_details: { failure_reason: error.message }, automation_run_id, triggered_by: 'pipeline' }); })` placed BEFORE the failRun call. Add the verbatim rationale comment from <behavior> directly above the new step.run. Sanitize error.message (no full stack frames) per existing T-70-04-02 mitigation.
       - Use `swarm_type: swarm_type ?? "debtor-email"` to forward-prep for cross-cutting handler reuse (Phase 69 D-08 pattern).

    2. In `web/lib/inngest/functions/__tests__/classifier-invoice-copy-handler.test.ts`:
       - Existing supabaseInserts at line ~40 already exists per RESEARCH §Validation Architecture.
       - Add assertion in happy-path test for stage=4 decision='completed' row with decision_details.handler_key='debtor-copy-document-body-agent'.
       - Add new test "emits stage=4 failed when no_invoice_reference branch hit": craft a fixture with no invoice ref; assert the `decision: 'failed'` pipeline_events row.
       - Add new test "emits stage=4 failed when handler throws unhandled": craft a fixture that triggers the failRun path; assert pipeline_events row with decision='failed' AND that automation_runs status='failed' (TELE-02 zero consumer breakage).
  </action>
  <verify>
    <automated>cd web && npx vitest run lib/inngest/functions/__tests__/classifier-invoice-copy-handler.test.ts lib/inngest/functions/__tests__/classifier-invoice-copy-handler-isolation.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "emitPipelineEvent" web/lib/inngest/functions/classifier-invoice-copy-handler.ts` returns >=4 (1 import + 3 call sites)
    - `grep -c "stage: 4" web/lib/inngest/functions/classifier-invoice-copy-handler.ts` returns >=3
    - At least one emitPipelineEvent call inside write-email-label step.run: `awk '/step.run\("write-email-label"/,/^  \}\)/' web/lib/inngest/functions/classifier-invoice-copy-handler.ts | grep -c emitPipelineEvent` returns >=1
    - The failRun-branch step.run exists with the fixed stepId: `grep -c "emit-stage4-failrun" web/lib/inngest/functions/classifier-invoice-copy-handler.ts` returns >=1
    - The rationale comment is present: `grep -c "doubled step count for the failure branch is acceptable" web/lib/inngest/functions/classifier-invoice-copy-handler.ts` returns >=1
    - `cd web && npx vitest run lib/inngest/functions/__tests__/classifier-invoice-copy-handler.test.ts` exits 0
    - Test asserts both 'completed' and 'failed' decisions: `grep -E "decision.*['\"](completed|failed)['\"]" web/lib/inngest/functions/__tests__/classifier-invoice-copy-handler.test.ts | wc -l` returns >=2
  </acceptance_criteria>
  <done>3 emit paths shipped; legacy email_labels writes untouched; tests cover all three branches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Coordinator agent output -> persist-ranked step.run | output.ranked[].intent, output.ranked[].confidence are LLM-produced; trusted-after-validation by Phase 65 zod schema |
| Handler exception -> failRun emit | Error.message may contain stack frames or upstream PII; sanitize before persisting to decision_details |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-04-01 | Information Disclosure | decision_details on Stage 4 carries draft_url + invoice_ref + tone (PII-adjacent) | mitigate | Plan 02 RLS gates reads; draft_url is internal Browserless URL not user-facing |
| T-70-04-02 | Information Disclosure | failRun emit may capture raw Error.message containing customer data or stack frames | mitigate | Action: emit only `error.message` (not full stack); add a `// PII: sanitize before exposing in Bulk Review` comment so Plan 06 read-side scrubs |
| T-70-04-03 | Tampering | email_id text-vs-uuid drift on Stage 3 (coordinator_runs.email_id is text) | mitigate | Use event.data.email_id (uuid from email_pipeline.emails.id) per RESEARCH §Pitfall 3; assert uuid in scope and fail loudly if missing — no null/text-id fallback (W-70-04 fix) |
| T-70-04-04 | Repudiation | Stage 4 failRun emit may not land if step.run boundary missing | mitigate | Action requires emit inside a NEW `step.run("emit-stage4-failrun", ...)` BEFORE failRun call (replay-safe); plain await at top-level catch would lose the row on replay |
</threat_model>

<verification>
- Both files import and call emitPipelineEvent at the correct sites.
- Tests cover happy + failure paths.
- Full suite green: `cd web && npm test 2>&1 | tail -10`.
- TELE-02: grep shows existing legacy INSERTs (`coordinator_runs UPDATE`, `email_labels.insert`) unchanged in line count + payload shape.
</verification>

<success_criteria>
- Stage 3 + Stage 4 emits live (3 total Stage 4 paths).
- Tests assert all branches.
- No regressions in adjacent handler tests (isolation test still passes).
</success_criteria>

<output>
After completion, create `.planning/phases/70-telemetry-consolidation-pipeline-events/70-04-SUMMARY.md`
</output>
