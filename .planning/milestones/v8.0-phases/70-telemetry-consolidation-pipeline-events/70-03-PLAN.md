---
phase: 70-telemetry-consolidation-pipeline-events
plan: 03
type: execute
wave: 2
depends_on: [70-02]
files_modified:
  - web/lib/inngest/functions/stage-0-safety-worker.ts
  - web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts
  - web/lib/inngest/functions/classifier-label-resolver.ts
  - web/lib/inngest/functions/__tests__/classifier-label-resolver.test.ts
autonomous: true
requirements: [TELE-01, TELE-02]
must_haves:
  truths:
    - "Stage 0 safety worker writes one pipeline_events row per persist-verdict step.run with stage=0, decision in {safe, injection_suspected}"
    - "Stage 2 label-resolver writes one pipeline_events row per write-email-label step.run with stage=2, decision in {resolved, unresolved}"
    - "Both emits live INSIDE the existing step.run (no new step.run added) per D-09"
    - "Legacy table writes (automation_runs, email_labels) still happen unchanged — TELE-02 zero consumer breakage"
  artifacts:
    - path: "web/lib/inngest/functions/stage-0-safety-worker.ts"
      provides: "Stage 0 dual-write inside persist-verdict step.run"
      contains: "emitPipelineEvent"
    - path: "web/lib/inngest/functions/classifier-label-resolver.ts"
      provides: "Stage 2 dual-write inside write-email-label step.run"
      contains: "emitPipelineEvent"
  key_links:
    - from: "web/lib/inngest/functions/stage-0-safety-worker.ts"
      to: "web/lib/pipeline-events/emit.ts"
      via: "import { emitPipelineEvent }"
      pattern: "emitPipelineEvent\\("
    - from: "web/lib/inngest/functions/classifier-label-resolver.ts"
      to: "web/lib/pipeline-events/emit.ts"
      via: "import { emitPipelineEvent }"
      pattern: "emitPipelineEvent\\("
---

<objective>
Wire Stage 0 (safety) and Stage 2 (entity/customer-resolution) dual-writes inside their existing `step.run` blocks per D-06/D-09. Extend the corresponding handler tests to assert BOTH inserts (legacy + pipeline_events) happen in one step.

Purpose: Two of the four "in-step.run" emit sites identified by RESEARCH §System Architecture Diagram. Adds one INSERT next to an existing INSERT — no logic refactor.
Output: Two production files modified, two test files extended, all tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-telemetry-consolidation-pipeline-events/70-CONTEXT.md
@.planning/phases/70-telemetry-consolidation-pipeline-events/70-RESEARCH.md
@web/lib/pipeline-events/emit.ts
@web/lib/pipeline-events/types.ts
@web/lib/inngest/functions/stage-0-safety-worker.ts
@web/lib/inngest/functions/classifier-label-resolver.ts
@web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts
@web/lib/inngest/functions/__tests__/classifier-label-resolver.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stage 0 emit inside persist-verdict step.run</name>
  <files>web/lib/inngest/functions/stage-0-safety-worker.ts, web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts</files>
  <read_first>
    - web/lib/inngest/functions/stage-0-safety-worker.ts (lines 125-152, the persist-verdict step.run block)
    - web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts (existing assertions structure)
    - .planning/phases/70-telemetry-consolidation-pipeline-events/70-RESEARCH.md §"Example: Stage 0 emit"
    - .planning/phases/70-telemetry-consolidation-pipeline-events/70-RESEARCH.md §Open Questions Q3 (automation_run_id populated, agent_run_id NULL)
  </read_first>
  <behavior>
    - When stage-0-safety-worker runs and persists a verdict, it emits ONE pipeline_events row with: swarm_type='debtor-email', stage=0, email_id (canonical uuid), decision = llmResult.verdict ('safe'|'injection_suspected'), confidence=null (categorical), decision_details = {regex_matched, llm_reason, matched_span, safety_overridden:false}, cost_cents = llmResult.usage.cost_cents, automation_run_id (from event payload), agent_run_id=null, triggered_by='pipeline'
    - The emit happens INSIDE persist-verdict step.run, AFTER the automation_runs INSERT throws-on-error check, BEFORE the step.run callback returns
    - On Inngest replay of the same event, the step.run is not re-executed (existing behaviour); pipeline_events does not get duplicate rows
    - Test assertion: supabaseInserts mock array contains BOTH {table:'automation_runs', ...} AND {table:'pipeline_events', stage:0, decision:'safe'} for the safe-path test fixture
  </behavior>
  <action>
    1. In `web/lib/inngest/functions/stage-0-safety-worker.ts`:
       - Add import: `import { emitPipelineEvent } from "@/lib/pipeline-events/emit";` (or relative path matching existing import style — check file's other imports first).
       - Inside the existing `await step.run("persist-verdict", async () => { ... })` block (around lines 125-152), AFTER the `if (error) throw new Error(...)` line and BEFORE the closing `}`, insert a `// Phase 70 — TELE-01 dual-write` comment line followed by an `await emitPipelineEvent(admin, { swarm_type: "debtor-email", stage: 0, email_id, decision: llmResult.verdict, confidence: null, decision_details: { regex_matched: regexResult.matched, llm_reason: llmResult.reason, matched_span: llmResult.matched_span, safety_overridden: false }, cost_cents: llmResult.usage.cost_cents, automation_run_id: event.data.automation_run_id ?? null, triggered_by: "pipeline" });` call.
       - Reference the verbatim block in RESEARCH §"Example: Stage 0 emit" — match its field names against the actual variables in scope at that line in the file. If a variable name differs, use the actual in-scope name and add a one-line comment noting the rename.

    2. In `web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts`:
       - Locate or add a `supabaseInserts: Array<{table:string, payload:any}>` mock pattern (mirror the pattern from `classifier-invoice-copy-handler.test.ts:40`).
       - In the existing safe-path test, after the existing assertions, add: `expect(supabaseInserts.find(i => i.table === 'pipeline_events' && i.payload.stage === 0 && i.payload.decision === 'safe')).toBeTruthy();`
       - In the existing injection-suspected test, add the equivalent assertion with `decision === 'injection_suspected'`.
       - Add ONE new test "does not duplicate pipeline_events row on replay (handler invoked twice with same event)": call the handler twice; assert exactly ONE pipeline_events insert per stage=0 decision (relies on step.run memoization in the test harness if present; if not, document the limit and assert "at least one" with a TODO comment citing replay-safety is enforced by Inngest runtime, not the unit test).
  </action>
  <verify>
    <automated>cd web && npx vitest run lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "emitPipelineEvent" web/lib/inngest/functions/stage-0-safety-worker.ts` returns >=2 (import + call)
    - `grep -c "stage: 0" web/lib/inngest/functions/stage-0-safety-worker.ts` returns >=1
    - The `emitPipelineEvent(` call is on a line whose surrounding context is inside `step.run("persist-verdict"` block: `awk '/step.run\("persist-verdict"/,/^  \}\)/' web/lib/inngest/functions/stage-0-safety-worker.ts | grep -c emitPipelineEvent` returns >=1
    - `cd web && npx vitest run lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts` exits 0 with all tests passing
    - Test file references stage:0 in pipeline_events payload assertion: `grep -c "stage.*0" web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts` returns >=2
  </acceptance_criteria>
  <done>Production file imports + invokes emit inside existing step.run; tests green; legacy automation_runs INSERT untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stage 2 emit inside write-email-label step.run (label-resolver)</name>
  <files>web/lib/inngest/functions/classifier-label-resolver.ts, web/lib/inngest/functions/__tests__/classifier-label-resolver.test.ts</files>
  <read_first>
    - web/lib/inngest/functions/classifier-label-resolver.ts (lines 149-181, the write-email-label step.run block)
    - web/lib/inngest/functions/__tests__/classifier-label-resolver.test.ts (existing test scaffolding)
    - .planning/phases/70-telemetry-consolidation-pipeline-events/70-RESEARCH.md §Pattern 1 (BEFORE/AFTER block)
    - .planning/phases/70-telemetry-consolidation-pipeline-events/70-RESEARCH.md §Open Questions Q2 (resolver-failure branch is also an emit site)
  </read_first>
  <behavior>
    - On resolver success (matched customer), emit pipeline_events row: stage=2, decision='resolved', confidence=numericConfidence(result.confidence), decision_details={customer_account_id, customer_name, method, candidates_considered}, email_id=emailRow.id (canonical uuid), triggered_by='pipeline'
    - On resolver no-match, emit row: stage=2, decision='unresolved', same shape with customer_account_id=null
    - On resolver-error branch (status='failed' email_label row), emit row: stage=2, decision='unresolved', decision_details={failure_reason: error.message}, confidence=null
    - All three emits live INSIDE the existing write-email-label step.run, after the legacy email_labels INSERT throws-on-error check
    - Tests: extend existing test file with assertions for each branch (matched/unmatched/error)
  </behavior>
  <action>
    1. In `web/lib/inngest/functions/classifier-label-resolver.ts`:
       - Add import: `import { emitPipelineEvent } from "@/lib/pipeline-events/emit";` and `import { numericConfidence } from "@/lib/pipeline-events/types";` (match existing import path style).
       - Locate the existing `step.run("write-email-label", async () => { ... })` (around lines 149-181 per RESEARCH).
       - After the `if (error) throw new Error("email_labels insert failed: ...")` line, add a `// Phase 70 — TELE-01 dual-write` comment.
       - Inside the same step.run, emit one pipeline_events row whose decision/confidence/decision_details depends on which branch we are in (resolved / unresolved / error). If the existing step.run handles all three branches by writing different `status` values to email_labels, branch on the same condition. Pattern from RESEARCH §Pattern 1 AFTER block.
       - For the error branch (where the resolver threw before producing a result), the step.run already writes `status='failed'` to email_labels — emit with decision='unresolved', decision_details.failure_reason populated. Per RESEARCH §Open Q2 recommendation: emit it (TELE-01 = "every stage decision").

    2. In `web/lib/inngest/functions/__tests__/classifier-label-resolver.test.ts`:
       - If a supabaseInserts mock pattern is not already present, add one mirroring `classifier-invoice-copy-handler.test.ts:40`.
       - For each existing branch test (matched/unmatched/failed), add an `expect(supabaseInserts.find(i => i.table === 'pipeline_events' && i.payload.stage === 2 && i.payload.decision === '<expected>')).toBeTruthy();` assertion.
       - Add ONE new test that asserts numericConfidence mapping: when resolver returns `confidence:'high'`, the pipeline_events row has `confidence: 0.9`.
  </action>
  <verify>
    <automated>cd web && npx vitest run lib/inngest/functions/__tests__/classifier-label-resolver.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "emitPipelineEvent" web/lib/inngest/functions/classifier-label-resolver.ts` returns >=2
    - `grep -c "numericConfidence" web/lib/inngest/functions/classifier-label-resolver.ts` returns >=2
    - `grep -c "stage: 2" web/lib/inngest/functions/classifier-label-resolver.ts` returns >=1
    - emitPipelineEvent call is inside write-email-label step.run: `awk '/step.run\("write-email-label"/,/^  \}\)/' web/lib/inngest/functions/classifier-label-resolver.ts | grep -c emitPipelineEvent` returns >=1
    - `cd web && npx vitest run lib/inngest/functions/__tests__/classifier-label-resolver.test.ts` exits 0
    - Test file asserts pipeline_events row for both 'resolved' and 'unresolved' decisions: `grep -E "decision.*['\"](resolved|unresolved)['\"]" web/lib/inngest/functions/__tests__/classifier-label-resolver.test.ts | wc -l` returns >=2
  </acceptance_criteria>
  <done>Three branches emit; tests green for all three; numericConfidence verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Inngest event payload -> handler | event.data.email_id is uuid (canonical email_pipeline.emails.id); event.data.automation_run_id from ingest route is also uuid |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-03-01 | Information Disclosure | decision_details.customer_name (PII) written to pipeline_events | mitigate (covered by Plan 02 RLS) | RLS service_role-all gates writes; Bulk Review reads via authenticated server component (Plan 06) |
| T-70-03-02 | Tampering | email_id text-vs-uuid drift causes Postgres "invalid input syntax for type uuid" failures (RESEARCH §Pitfall 3) | mitigate | Action requires using `emailRow.id` (canonical uuid), not Outlook string id; acceptance criterion checks the field source |
| T-70-03-03 | Repudiation | Replay double-insert duplicates pipeline_events rows | mitigate | Emit lives inside existing step.run; Inngest replay memoization is the boundary (D-06, D-09); test harness asserts single insert when handler invoked twice |
</threat_model>

<verification>
- Both production files import and call emitPipelineEvent inside the correct step.run block.
- Both test files assert the dual-write payload shape per branch.
- `cd web && npm test 2>&1 | tail -20` shows the full suite green (no regressions in adjacent handlers).
</verification>

<success_criteria>
- Stage 0 + Stage 2 emits live in production code; legacy inserts unchanged.
- Tests for both handlers green with new pipeline_events assertions.
- TELE-02 verified: legacy automation_runs / email_labels INSERT calls are still present and unaltered.
</success_criteria>

<output>
After completion, create `.planning/phases/70-telemetry-consolidation-pipeline-events/70-03-SUMMARY.md`
</output>
