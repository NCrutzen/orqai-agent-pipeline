---
phase: 70-telemetry-consolidation-pipeline-events
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - web/app/api/automations/debtor-email/ingest/__tests__/route.test.ts
  - web/lib/pipeline-events/__tests__/emit.test.ts
autonomous: true
requirements: [TELE-01]
must_haves:
  truths:
    - "Vitest scaffolds exist for both Wave-0 gaps identified in RESEARCH.md (route.test.ts, emit.test.ts)"
    - "Both scaffold tests run via vitest and produce a deterministic FAIL (red) until later waves implement the code"
  artifacts:
    - path: "web/app/api/automations/debtor-email/ingest/__tests__/route.test.ts"
      provides: "Stage 1 ingest-route emit test scaffold"
      min_lines: 20
    - path: "web/lib/pipeline-events/__tests__/emit.test.ts"
      provides: "emitPipelineEvent helper test scaffold"
      min_lines: 20
  key_links:
    - from: "vitest run"
      to: "both new test files"
      via: "web/vitest.config.ts test discovery"
      pattern: "describe.*pipeline_events|describe.*ingest route Stage 1 emit"
---

<objective>
Create vitest scaffolds for the two Wave-0 test gaps identified in 70-RESEARCH.md §Wave 0 Gaps:
1. `web/lib/pipeline-events/__tests__/emit.test.ts` — for the `emitPipelineEvent` helper (introduced in Plan 02).
2. `web/app/api/automations/debtor-email/ingest/__tests__/route.test.ts` — for Stage 1 emit (introduced in Plan 04). The route currently has NO test file.

Purpose: Honor Nyquist rule — every later task that adds production code lands `<verify>` against an automated test that must already exist. Wave 0 creates the failing-red scaffolds.

Output: Two new vitest files. Both produce a deterministic failure (skipped or `expect.fail`) until Plans 02/04 wire them up. No production code in this wave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/70-telemetry-consolidation-pipeline-events/70-CONTEXT.md
@.planning/phases/70-telemetry-consolidation-pipeline-events/70-RESEARCH.md
@web/vitest.config.ts
@web/lib/inngest/functions/__tests__/classifier-invoice-copy-handler.test.ts
@web/app/api/automations/debtor-email/ingest/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold emit.test.ts (helper-level)</name>
  <files>web/lib/pipeline-events/__tests__/emit.test.ts</files>
  <read_first>
    - web/vitest.config.ts (test discovery, jsdom env)
    - web/lib/inngest/functions/__tests__/classifier-invoice-copy-handler.test.ts (supabaseInserts mock pattern at line ~40)
    - .planning/phases/70-telemetry-consolidation-pipeline-events/70-RESEARCH.md §Pattern 3 (helper signature)
  </read_first>
  <behavior>
    - Test 1 (skipped, fail-on-unskip): "emitPipelineEvent inserts a row into pipeline_events with the supplied payload"
    - Test 2 (skipped): "emitPipelineEvent throws when admin.from('pipeline_events').insert returns an error"
    - Test 3 (skipped): "emitPipelineEvent passes nullable optional fields (override, eval_type, case_id) through unchanged"
  </behavior>
  <action>
    Create `web/lib/pipeline-events/__tests__/emit.test.ts` containing a vitest `describe("emitPipelineEvent", ...)` block with three `it.skip(...)` test cases listed under <behavior>. Each `it.skip` body must contain a clear `// TODO Plan 02: implement` comment plus an `expect.fail("not implemented — Plan 02 wires this up")` line that would trigger if `.skip` is removed. Do NOT import `emitPipelineEvent` yet (the module does not exist in Wave 0). Use the `supabaseInserts` mock pattern referenced from classifier-invoice-copy-handler.test.ts as the model for later wiring (document this in a comment at the top of the file). The file must be valid TypeScript that vitest can discover and execute (the `it.skip` calls collect as "skipped" in the run output).
  </action>
  <verify>
    <automated>cd web && npx vitest run lib/pipeline-events/__tests__/emit.test.ts --reporter=verbose 2>&1 | grep -E "(skipped|3 passed|3 todo|emit.test.ts)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path `web/lib/pipeline-events/__tests__/emit.test.ts`
    - `grep -c "it.skip" web/lib/pipeline-events/__tests__/emit.test.ts` returns >=3
    - `grep -c "TODO Plan 02" web/lib/pipeline-events/__tests__/emit.test.ts` returns >=1
    - `cd web && npx vitest run lib/pipeline-events/__tests__/emit.test.ts` exits 0 (skipped tests do not fail the suite)
  </acceptance_criteria>
  <done>vitest discovers the file, reports 3 skipped tests, suite exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold ingest route.test.ts (Stage 1 emit, no existing test file)</name>
  <files>web/app/api/automations/debtor-email/ingest/__tests__/route.test.ts</files>
  <read_first>
    - web/app/api/automations/debtor-email/ingest/route.ts (lines 244-487, the 6 automation_runs.insert branches)
    - web/lib/inngest/functions/__tests__/classifier-invoice-copy-handler.test.ts (supabase mocking pattern)
    - .planning/phases/70-telemetry-consolidation-pipeline-events/70-RESEARCH.md §Pitfall 2 + §Open Questions Q1
  </read_first>
  <behavior>
    - Test 1 (skipped): "POST /ingest with a regex-classified email emits ONE pipeline_events row at Stage 1 with decision = matched topic"
    - Test 2 (skipped): "POST /ingest with an unknown email (regex no-match) emits ONE pipeline_events row at Stage 1 with decision = 'unknown'"
    - Test 3 (skipped): "Stage 1 emit carries the canonical email_pipeline.emails.id (uuid), NOT the Outlook string id"
  </behavior>
  <action>
    Create directory `web/app/api/automations/debtor-email/ingest/__tests__/` and inside it `route.test.ts` containing a vitest `describe("POST /api/automations/debtor-email/ingest — Stage 1 pipeline_events emit", ...)` block with three `it.skip(...)` test cases listed under <behavior>. Each `it.skip` must contain `expect.fail("not implemented — Plan 04 wires this up")` and `// TODO Plan 04: implement` comment. Do NOT import the route handler yet (Plan 04 may need to refactor for testability). Add a top-of-file comment block citing `70-RESEARCH.md §Open Questions Q1` (recommendation: ONE Stage 1 row per email, emitted at the `r = classify(...)` call site near line 283). The file must be valid TypeScript and discoverable by vitest.
  </action>
  <verify>
    <automated>cd web && npx vitest run "app/api/automations/debtor-email/ingest/__tests__/route.test.ts" --reporter=verbose 2>&1 | grep -E "(skipped|3 passed|3 todo|route.test.ts)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path `web/app/api/automations/debtor-email/ingest/__tests__/route.test.ts`
    - `grep -c "it.skip" web/app/api/automations/debtor-email/ingest/__tests__/route.test.ts` returns >=3
    - `grep -c "TODO Plan 04" web/app/api/automations/debtor-email/ingest/__tests__/route.test.ts` returns >=1
    - `grep -c "Open Questions Q1" web/app/api/automations/debtor-email/ingest/__tests__/route.test.ts` returns >=1
    - `cd web && npx vitest run "app/api/automations/debtor-email/ingest/__tests__/route.test.ts"` exits 0
  </acceptance_criteria>
  <done>vitest discovers the file, reports 3 skipped tests, suite exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none — wave 0 is test scaffolding only) | No new code paths, no PII, no DB writes |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-W0-01 | Tampering | scaffold left as `it.skip` indefinitely | accept | Plans 02/04 unskip in Wave 1/3; phase verification asserts no `.skip` remains in pipeline-events tests at phase close |
</threat_model>

<verification>
- Both scaffold files exist and are discovered by vitest.
- `cd web && npm test 2>&1 | grep -E "(emit.test.ts|route.test.ts)"` shows them in the run output.
- No production code modified (`git diff --stat web/lib web/app/api | grep -v __tests__` returns empty).
</verification>

<success_criteria>
- 2 new test files committed.
- vitest exit 0 on both files individually.
- Each file contains the skipped test names matching the <behavior> list.
</success_criteria>

<output>
After completion, create `.planning/phases/70-telemetry-consolidation-pipeline-events/70-01-SUMMARY.md`
</output>
