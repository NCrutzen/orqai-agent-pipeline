---
phase: 63
plan: 03
type: execute
wave: 3
depends_on: [63-01, 63-02]
files_modified:
  - docs/agentic-pipeline/README.md
  - CLAUDE.md
  - docs/debtor-email-pipeline-architecture.md
  - .planning/PROJECT.md
autonomous: true
requirements: [RFC-01]
must_haves:
  truths:
    - "docs/agentic-pipeline/README.md exists, names all 5 stages, has the headline funnel ASCII diagram, has a Tenancy section using today's 6 verified brands"
    - "CLAUDE.md 'Canonical Architecture Docs' points at docs/agentic-pipeline/README.md as primary; debtor-email doc retained as swarm-specific implementation map"
    - "docs/debtor-email-pipeline-architecture.md carries the SUPERSEDED banner per CONTEXT D-03"
    - ".planning/PROJECT.md verified free of speculative 'smeba-uk' / 'smeba-ie' strings (D-09)"
  artifacts:
    - path: "docs/agentic-pipeline/README.md"
      provides: "RFC entry point — overview, tenancy, index, headline funnel diagram"
      contains: "Stage 0, Stage 1, Stage 2, Stage 3, Stage 4"
    - path: "CLAUDE.md"
      provides: "Updated Canonical Architecture Docs section with both pointers"
      contains: "docs/agentic-pipeline/README.md"
    - path: "docs/debtor-email-pipeline-architecture.md"
      provides: "Existing doc carrying SUPERSEDED banner"
      contains: "SUPERSEDED for cross-swarm shape"
  key_links:
    - from: "docs/agentic-pipeline/README.md"
      to: "all 5 stage docs + 3 contract docs"
      via: "Index section with relative links"
    - from: "CLAUDE.md"
      to: "docs/agentic-pipeline/README.md"
      via: "Canonical Architecture Docs section, primary pointer"
    - from: "docs/debtor-email-pipeline-architecture.md"
      to: "docs/agentic-pipeline/README.md"
      via: "SUPERSEDED banner forward pointer"
---

<objective>
Tie the RFC together. Wave 1 produced 3 contract docs; Wave 2 produced 5 stage docs. This wave produces the README that indexes them, updates CLAUDE.md to point at the new canonical doc, adds the SUPERSEDED banner to the existing debtor-email doc, and runs the preventive D-09 brand-correction verification on PROJECT.md.

Purpose: closes RFC-01 (single canonical RFC + supersession banner + CLAUDE.md updated). After this plan, /gsd-verify-work should green-light all phase smoke-greps in VALIDATION.md.

Output: 1 NEW file (README.md), 2 MUTATIONS to existing files, 1 VERIFICATION (PROJECT.md possibly mutated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-architecture-rfc/63-CONTEXT.md
@.planning/phases/63-architecture-rfc/63-RESEARCH.md
@.planning/phases/63-architecture-rfc/63-VALIDATION.md
@CLAUDE.md
@docs/debtor-email-pipeline-architecture.md
@docs/agentic-pipeline/context-shape-contract.md
@docs/agentic-pipeline/override-model.md
@docs/agentic-pipeline/graduated-automation.md
@docs/agentic-pipeline/stage-0-safety.md
@docs/agentic-pipeline/stage-1-regex.md
@docs/agentic-pipeline/stage-2-entity.md
@docs/agentic-pipeline/stage-3-coordinator.md
@docs/agentic-pipeline/stage-4-handler.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/agentic-pipeline/README.md (RFC entry point)</name>
  <files>docs/agentic-pipeline/README.md</files>
  <read_first>
    - .planning/phases/63-architecture-rfc/63-CONTEXT.md (decisions D-01 layout, D-08 Tenancy section, D-09 brand correction, D-13 ASCII)
    - .planning/phases/63-architecture-rfc/63-RESEARCH.md (sections "Anthropic Agentic Guidance Synthesis", "Today-State Telemetry / Brand registry today", "File Manifest")
    - docs/debtor-email-pipeline-architecture.md (style template — banner pattern `> **Status:**`, ASCII glyphs)
    - All 8 sibling files in docs/agentic-pipeline/ (verify they exist and links resolve)
  </read_first>
  <action>
Create `docs/agentic-pipeline/README.md`.

Required H2 sections in this order:
1. Top blockquote banner (per existing docs/ style):
   ```
   > **Status:** RFC — locked architectural shape for v8.0 Agentic Platform (milestone 63-73)
   > **Audience:** engineers building or extending agentic pipelines (debtor-email today, sales-email Phase 73)
   > **Supersedes:** the cross-swarm shape sections of `../debtor-email-pipeline-architecture.md` (that doc retained as the debtor-email implementation map)
   ```
2. `## What This Is` — 2-3 sentences: this is the canonical 5-stage funnel architecture for cross-swarm agentic pipelines. Read this README first; per-stage and contract details live in self-contained sibling files. Cite Anthropic "Building Effective Agents" ONCE with URL `https://www.anthropic.com/engineering/building-effective-agents` — paraphrase the workflow-vs-agent distinction in our own voice.
3. `## The 5-Stage Funnel` — headline ASCII diagram showing: `inbound -> Stage 0 (safety) -> Stage 1 (regex) -> Stage 2 (entity) -> Stage 3 (coordinator) -> Stage 4 (handler) -> side-effects`. Use the canonical glyph set (`↓` `┌─` `└─` `│` `├─`). Each stage box names the stage number AND short purpose.
4. `## Tenancy (D-08)` — H3 sub-sections:
   - `### Brand multitenancy is day-1` — 2-3 sentences: every routing/prompt/category decision is `entity_brand`-scoped. No hardcoded brand enums anywhere in the codebase.
   - `### Today's brands (walkerfire.icontroller.eu tenant)` — bulleted list, EXACTLY these 6: `smeba`, `smeba-fire`, `firecontrol`, `sicli-noord`, `sicli-sud`, `berki`. State explicitly: `walkerfire.icontroller.eu` is the iController **tenant** (umbrella), NOT a brand.
   - `### Source of truth (today)` — split: `debtor.labeling_settings.brand_id` (2-letter NXT code) + `debtor.labeling_settings.nxt_database`. Phase 68 (SWRM-01) introduces the unified `swarms.entity_brand` registry that becomes single source of truth; today's per-mailbox columns become a denormalised read-model.
   - `### Future brand additions` — single sentence: "UK/IE expansion (Phase 999.1 backlog) — brand names TBD; new brands onboard via registry insert. Do NOT use placeholder names."
5. `## Index` — bulleted list of relative links:
   - Stages: `./stage-0-safety.md`, `./stage-1-regex.md`, `./stage-2-entity.md`, `./stage-3-coordinator.md`, `./stage-4-handler.md`
   - Contracts: `./context-shape-contract.md`, `./override-model.md`, `./graduated-automation.md`
   - Sibling (different concern): `../swarm-bridge-contract.md` — UI plumbing for `automation_runs`/kanban surface (NOT superseded by this RFC)
6. `## Reading Order` — recommended path: README -> contract docs (Wave 1) -> per-stage docs (Wave 2). Single-stage focus: read just that stage doc; everything needed is inline or linked OUT.
7. `## Implementation Patterns (link out)` — bulleted: `../orqai-patterns.md`, `../zapier-patterns.md`, `../inngest-patterns.md`, `../browserless-patterns.md`, `../supabase-patterns.md`. RFC docs do NOT duplicate these.
8. `## Forward References (RFC names, later phases ship)` — table: `Forward-ref | Where named | Ship phase`. Rows from RESEARCH "Forward-References" table: `pipeline_events` (Phase 70), `zapier_tools.allowed_for_intents` (Phase 64), `swarms.entity_brand` registry (Phase 68), `swarm_intents` table (Phase 68), Stage 3.5 full design (Phase 65), per-run cost ceilings (Phase 64), Stage 0 implementation (Phase 64), `web/lib/agentic-pipeline/types.ts` (Phase 64/70), Stage 4 canonicalisation (Phase 69), Bulk Review 4-axis UI (Phase 71), concrete promotion thresholds (Phase 71/72), sales-email/SugarCRM (Phase 73), UK/IE brand naming (Phase 999.1).

Style: English. ATX headers. No emojis. No frontmatter. Match `docs/debtor-email-pipeline-architecture.md` conventions. Implements D-01, D-08, D-09, D-13.
  </action>
  <verify>
    <automated>test -f docs/agentic-pipeline/README.md &amp;&amp; [ "$(grep -cE 'Stage [0-4]' docs/agentic-pipeline/README.md)" -ge 5 ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/agentic-pipeline/README.md`
    - `[ "$(grep -cE 'Stage [0-4]' docs/agentic-pipeline/README.md)" -ge 5 ]`
    - `grep -F "smeba" docs/agentic-pipeline/README.md && grep -F "smeba-fire" docs/agentic-pipeline/README.md && grep -F "firecontrol" docs/agentic-pipeline/README.md && grep -F "sicli-noord" docs/agentic-pipeline/README.md && grep -F "sicli-sud" docs/agentic-pipeline/README.md && grep -F "berki" docs/agentic-pipeline/README.md`
    - `! grep -E "smeba-uk|smeba-ie" docs/agentic-pipeline/README.md` (must NOT appear)
    - `grep -F "anthropic.com/engineering/building-effective-agents" docs/agentic-pipeline/README.md`
    - `grep -F "./stage-0-safety.md" docs/agentic-pipeline/README.md && grep -F "./stage-4-handler.md" docs/agentic-pipeline/README.md && grep -F "./context-shape-contract.md" docs/agentic-pipeline/README.md`
    - `! grep -rEi "netlify|railway|firebase|neon|puppeteer" docs/agentic-pipeline/README.md`
    - Doc-link integrity: every relative `./*.md` link in README resolves. Run: `for link in $(grep -oE '\./[a-z0-9-]+\.md' docs/agentic-pipeline/README.md | sort -u); do test -f "docs/agentic-pipeline/$link" || { echo "MISS $link"; exit 1; }; done`
  </acceptance_criteria>
  <done>README.md exists; all 5 stages named; today's 6 brands listed; speculative names absent; Anthropic URL present once; index links to all 8 sibling files; all relative links resolve.</done>
</task>

<task type="auto">
  <name>Task 2: Add SUPERSEDED banner to docs/debtor-email-pipeline-architecture.md</name>
  <files>docs/debtor-email-pipeline-architecture.md</files>
  <read_first>
    - .planning/phases/63-architecture-rfc/63-CONTEXT.md (decision D-03 — exact banner text)
    - .planning/phases/63-architecture-rfc/63-RESEARCH.md (section "Existing Doc Audit / Section-by-section disposition" — banner placement at top, after existing `> **Status:**` and `> **Supersedes:**` lines)
    - docs/debtor-email-pipeline-architecture.md (verify the existing top blockquote lines so the new banner is inserted in the right place, not duplicating existing header)
  </read_first>
  <action>
Edit `docs/debtor-email-pipeline-architecture.md`. Insert this exact banner line at the top of the file, immediately AFTER the existing `> **Status:** ...` and `> **Supersedes:** ...` blockquote lines, BEFORE any content section:

```
> **SUPERSEDED for cross-swarm shape** by [docs/agentic-pipeline/README.md](agentic-pipeline/README.md). Retained as the implementation map for the debtor-email swarm specifically.
```

Use the Edit tool. Match the existing blockquote style. Do NOT delete or modify any other content. Do NOT change the existing supersedes line referencing `email-agent-swarm-architecture.md`.

Implements CONTEXT D-03.
  </action>
  <verify>
    <automated>grep -i "SUPERSEDED for cross-swarm shape" docs/debtor-email-pipeline-architecture.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -i "SUPERSEDED for cross-swarm shape" docs/debtor-email-pipeline-architecture.md`
    - `grep -F "agentic-pipeline/README.md" docs/debtor-email-pipeline-architecture.md`
    - Original `> **Status:**` line still present: `grep -E "^> \*\*Status:\*\*" docs/debtor-email-pipeline-architecture.md`
    - Banner appears within first 20 lines: `head -20 docs/debtor-email-pipeline-architecture.md | grep -i "SUPERSEDED for cross-swarm shape"`
  </acceptance_criteria>
  <done>Banner present at top; relative link to RFC README; existing content unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md "Canonical Architecture Docs" section</name>
  <files>CLAUDE.md</files>
  <read_first>
    - .planning/phases/63-architecture-rfc/63-CONTEXT.md (decision D-03 — point at both: RFC for canonical shape, debtor-email doc for that swarm's implementation)
    - .planning/phases/63-architecture-rfc/63-RESEARCH.md (section "File Manifest / Doc-mutation tasks / CLAUDE.md")
    - CLAUDE.md (verify current "Canonical Architecture Docs" section content — currently only points at debtor-email-pipeline-architecture.md)
  </read_first>
  <action>
Edit `CLAUDE.md`. Update the "## Canonical Architecture Docs" section.

Replace the current single-bullet block (which only mentions the debtor-email doc) with TWO bullets, in this order:

```
## Canonical Architecture Docs

- **Agentic Pipeline (cross-swarm canonical)** → `docs/agentic-pipeline/README.md` — the v8.0 5-stage funnel architecture (Stage 0 safety → Stage 1 regex → Stage 2 entity → Stage 3 coordinator → Stage 4 handler), Stage 2→3 context-shape contract, 4-axis override model, graduated-automation hooks. Read this BEFORE designing any new agentic pipeline or extending an existing swarm.
- **Debtor Email Pipeline (swarm-specific implementation)** → `docs/debtor-email-pipeline-architecture.md` — implementation map for the debtor-email swarm specifically: Outlook ingest, classifier, swarm_categories registry, per-category handlers (label-resolver, invoice-copy), Bulk Review vs Kanban surfaces. Read this before editing any file in `web/lib/automations/debtor-email/` or `web/app/api/automations/debtor*/`.
```

Use the Edit tool. Preserve any other lines in CLAUDE.md (Auto-loaded Skills, Stack, etc.).

Implements CONTEXT D-03 + D-09 (no speculative brand names introduced).
  </action>
  <verify>
    <automated>grep -F "docs/agentic-pipeline/README.md" CLAUDE.md &amp;&amp; grep -F "docs/debtor-email-pipeline-architecture.md" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "docs/agentic-pipeline/README.md" CLAUDE.md`
    - `grep -F "docs/debtor-email-pipeline-architecture.md" CLAUDE.md`
    - `grep -E "Agentic Pipeline.*cross-swarm canonical" CLAUDE.md`
    - `grep -E "Debtor Email Pipeline.*swarm-specific implementation" CLAUDE.md`
    - `! grep -E "smeba-uk|smeba-ie" CLAUDE.md` (preventive)
  </acceptance_criteria>
  <done>CLAUDE.md "Canonical Architecture Docs" section has both pointers in correct order; debtor-email demoted to swarm-specific; no speculative brand names introduced.</done>
</task>

<task type="auto">
  <name>Task 4: D-09 preventive verification of .planning/PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/phases/63-architecture-rfc/63-CONTEXT.md (decision D-09 — full text)
    - .planning/phases/63-architecture-rfc/63-RESEARCH.md (section "File Manifest / Doc-mutation tasks / .planning/PROJECT.md" — RESEARCH confirms zero hits today; this task is preventive)
    - .planning/PROJECT.md (read in full to verify; if any speculative-name string surfaces, edit it; if not, no mutation needed)
  </read_first>
  <action>
Verify `.planning/PROJECT.md` is free of speculative brand strings `smeba-uk` and `smeba-ie`.

Step 1: Read the file. Run:
```
grep -nEi "smeba-uk|smeba-ie" .planning/PROJECT.md || echo "CLEAN"
```

Step 2:
- If grep prints "CLEAN" (no matches): no edit needed. Record in SUMMARY: "PROJECT.md verified clean — D-09 preventive check passed without mutation."
- If grep prints any matches: use the Edit tool to replace each speculative name with the data-driven phrasing from RFC tenancy section: "future brand additions, names data-driven via `swarms.entity_brand` registry insert (Phase 68; UK/IE Phase 999.1 backlog)". Preserve surrounding sentence structure.

Step 3 (regardless): also run the global preventive grep across docs/ + CLAUDE.md (covered by other tasks but re-verified here):
```
! grep -rEi "smeba-uk|smeba-ie" docs/ .planning/PROJECT.md CLAUDE.md
```
This MUST exit non-zero (zero matches anywhere).

Implements CONTEXT D-09.
  </action>
  <verify>
    <automated>! grep -rEi "smeba-uk|smeba-ie" docs/ .planning/PROJECT.md CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -rEi "smeba-uk|smeba-ie" .planning/PROJECT.md` (must exit non-zero)
    - `! grep -rEi "smeba-uk|smeba-ie" docs/` (must exit non-zero)
    - `! grep -rEi "smeba-uk|smeba-ie" CLAUDE.md` (must exit non-zero)
    - SUMMARY records whether any mutation was needed.
  </acceptance_criteria>
  <done>Global preventive grep across docs/, .planning/PROJECT.md, CLAUDE.md exits non-zero. SUMMARY notes mutation status.</done>
</task>

</tasks>

<threat_model>
Docs-only phase; no executable code; no migrations; no network calls. Threat surface = none. Acceptable per phase scope.
</threat_model>

<verification>
After all 4 tasks complete, run from repo root the full RFC-coverage smoke gate (matches VALIDATION.md per-task verification map):

- `test -f docs/agentic-pipeline/README.md && [ "$(grep -cE 'Stage [0-4]' docs/agentic-pipeline/README.md)" -ge 5 ]`
- `grep -i "SUPERSEDED for cross-swarm shape" docs/debtor-email-pipeline-architecture.md`
- `grep -F "docs/agentic-pipeline/README.md" CLAUDE.md`
- `! grep -rEi "smeba-uk|smeba-ie" docs/ .planning/PROJECT.md CLAUDE.md`
- `! grep -rEi "netlify|railway|firebase|neon|puppeteer" docs/agentic-pipeline/`
- `for s in 0-safety 1-regex 2-entity 3-coordinator 4-handler; do test -f "docs/agentic-pipeline/stage-${s}.md" || exit 1; done`
- `for f in context-shape-contract override-model graduated-automation README; do test -f "docs/agentic-pipeline/${f}.md" || exit 1; done`
- Doc-link integrity for README: `for link in $(grep -oE '\./[a-z0-9-]+\.md' docs/agentic-pipeline/README.md | sort -u); do test -f "docs/agentic-pipeline/$link" || { echo "MISS $link"; exit 1; }; done`

Manual review:
- Open README in markdown preview; ASCII funnel renders cleanly in monospace.
- Anthropic citation paraphrased, not quote-bombed.
- Tenancy section uses today's 6 verified brands ONLY.
- Forward-references table matches RESEARCH.md Forward-References section.
</verification>

<success_criteria>
- README.md exists, names all 5 stages, has Tenancy section with 6 today-brands, indexes all 8 sibling files
- debtor-email doc carries SUPERSEDED banner near top
- CLAUDE.md "Canonical Architecture Docs" has both pointers, RFC primary, debtor-email demoted
- Zero speculative brand names anywhere in docs/, .planning/PROJECT.md, CLAUDE.md
- Zero stack-violation strings in docs/agentic-pipeline/
- All relative links in README resolve
- All RFC-01..04 smoke-greps from VALIDATION.md green
</success_criteria>

<output>
After completion, create `.planning/phases/63-architecture-rfc/63-03-SUMMARY.md` recording:
- Files created (1) + mutated (2 or 3 depending on PROJECT.md)
- Whether D-09 PROJECT.md mutation was needed (likely no — RESEARCH confirmed clean)
- Final smoke-gate results
- Phase ready for /gsd-verify-work
</output>
