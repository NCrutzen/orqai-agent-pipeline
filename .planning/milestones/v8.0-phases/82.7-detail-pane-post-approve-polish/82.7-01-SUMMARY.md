---
phase: 82.7-detail-pane-post-approve-polish
plan: 01
subsystem: bulk-review-audit-ui
tags:
  - ui-polish
  - stage-feedback-panel
  - v7-tokens
requires: []
provides:
  - polished StageFeedbackPanel widget (D-04 closed)
affects:
  - web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx
tech-stack:
  added: []
  patterns:
    - "Inline V7 token styles (border + radius-sm + space-4) for card containers"
    - "Single-line inline-flex buttons with gap:6 for compact pill actions"
key-files:
  created: []
  modified:
    - web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx
decisions:
  - "Deleted the @media (max-width: 1365px) block entirely — single-line labels make narrow-width subtitle hiding obsolete; no orphan CSS retained."
  - "Reduced button vertical padding 8px→6px when collapsing from column→inline-flex so the visual button height matches the previous two-line variant."
  - "Kept data-row=\"primary\" on label spans (even though the @media hook is gone) to avoid breaking any latent selector-based test in Phase 82.5 verification specs."
metrics:
  duration_minutes: 4
  completed: 2026-05-16
  tasks_completed: 1
  files_modified: 1
---

# Phase 82.7 Plan 01: Detail-Pane Post-Approve Polish (D-04 StageFeedbackPanel) Summary

Pure visual polish: collapsed the StageFeedbackPanel's two-line Save/Confirm buttons to single-line and promoted its container from a top-border strip to a fully-bordered card, addressing operator UAT defect D-04 from Phase 82.6.

## What Was Built

- **Container card:** `border: 1px solid var(--v7-border)` + `borderRadius: var(--v7-radius-sm)` + `padding: var(--space-4)` (up from `var(--space-3)`). `marginTop` + `background` preserved.
- **Save button:** `display: inline-flex`, `gap: 6`, `padding: 6px 14px`. Label `Save note` (dropped `· come back later`). Outline brand-secondary + pill radius retained.
- **Confirm button:** mirror inline-flex collapse, padding/gap matched. Label `Confirm` (dropped `· looks right`). Lime fill, pill radius, mono font retained.
- **Removed dead CSS:** the entire `@media (max-width: 1365px)` block that hid `data-row="primary"` spans is gone — labels are unconditionally single-line now.

## What Was NOT Touched

- `onSave` / `onConfirm` callbacks
- `inFlight` state plumbing
- The textarea, ⌘+s/Esc handlers, R5 soft-confirm guard
- `OthersSaidBlock`, savedAt indicator, error banner
- Imports
- Controlled-component contract (Phase 82.5)

## Deviations from Plan

None — plan executed exactly as written.

## Verification

- `npx tsc --noEmit` → no errors attributable to this file (`tsc clean for StageFeedbackPanel`).
- grep verification (all required strings present / absent):
  - `Save note · come back later` → 0 matches
  - `Confirm · looks right` → 0 matches
  - `Save note` (own line) → 1 match (L284)
  - `Confirm` (own line) → 1 match (L314)
  - `borderTop: "1px` → 0 matches in container
  - `border: "1px solid var(--v7-border)"` → 1 match (L194)
  - `borderRadius: "var(--v7-radius-sm)"` → 2 matches (textarea L175 + container L195)
  - `data-testid="stage-feedback-save"` → preserved (L261)
  - `data-testid="stage-feedback-confirm"` → preserved (L290)
- Hard-separation invariant: no `swarm_intents` reference introduced (file does not touch dispatch path).

## Commits

- `49d069a` feat(82.7-01): polish StageFeedbackPanel — bordered card + single-line button labels

## Known Stubs

None.

## Self-Check: PASSED

- File exists: `/Users/nickcrutzen/Developer/agent-workforce/.claude/worktrees/agent-aea4c4551cc200404/web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx` — FOUND
- Commit exists: `49d069a` — FOUND
