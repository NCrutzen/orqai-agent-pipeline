---
phase: 82.7-detail-pane-post-approve-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx
autonomous: true
requirements:
  - D-04
tags:
  - ui-polish
  - stage-feedback-panel
  - v7-tokens
must_haves:
  truths:
    - "StageFeedbackPanel buttons render on a single line at the operator's default right-pane width (360-380px) — no label wrapping."
    - "Confirm button remains visually dominant (lime fill) and Save button remains clearly secondary (transparent fill, brand-secondary outline)."
    - "Container around textarea + buttons reads as one coherent card with full border + radius, not a top-border-only strip."
    - "Existing data-testids (`stage-feedback-panel-${stage}`, `stage-feedback-save`, `stage-feedback-confirm`) are preserved so Phase 82.5 verification specs still pass."
  artifacts:
    - path: "web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx"
      provides: "Polished override widget — single-line button labels, card container"
      contains: "Save note"
  key_links:
    - from: "StageFeedbackPanel container style"
      to: "var(--v7-border) + var(--v7-radius-sm) + var(--space-4) padding"
      via: "inline style object"
      pattern: "border: \"1px solid var\\(--v7-border\\)\""
    - from: "Save button label"
      to: "single-line `Save note` (no `· come back later`)"
      via: "label span contents"
      pattern: ">Save note<"
    - from: "Confirm button label"
      to: "single-line `Confirm` (no `· looks right`)"
      via: "label span contents"
      pattern: ">Confirm<"
---

<objective>
Polish the StageFeedbackPanel override widget so it reads as "one card, two clear actions" instead of "two buttons in a void". Per CONTEXT D-04: collapse two-line button labels to single-line; promote container from top-border strip to full bordered card with rhythm matching the surrounding stage cards; preserve hierarchy (Confirm primary lime, Save secondary outline) and all data-testids.

Purpose: Operator UAT (2026-05-16) reported the override widget "looks a bit off" — alignment/padding/contrast feel disconnected from the stage card above it. This is the first of 4 follow-up polish items from Phase 82.6 UAT.

Output: A single modified file (`StageFeedbackPanel.tsx`) with no functional behavior change — the Save/Confirm callbacks, the `inFlight` state, the textarea, and the responsive @media query (or its simplification) all keep their contracts. Pure visual delta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/82.7-detail-pane-post-approve-polish/82.7-CONTEXT.md
@.planning/phases/82.7-detail-pane-post-approve-polish/82.7-PATTERNS.md

<interfaces>
<!-- StageFeedbackPanel current contract — Phase 82.5 controlled-component shape. -->
<!-- Executor MUST preserve every callback signature; only inline styles + label strings change. -->

From web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx (current shape):
- Component is fully controlled by parent state (Phase 82.5 contract). Do NOT add local state.
- Container wrapper: L188-197 — currently `borderTop: 1px` + `var(--v7-panel-2)` background + `padding: var(--space-3)`.
- Save button: L268-296 — `flexDirection: column` (two-line), transparent bg, brand-secondary border, lime-pill radius.
- Confirm button: L298-327 — `flexDirection: column` (two-line), lime fill (`var(--v7-lime)` background, `var(--v7-bg)` text), pill radius.
- data-testids in use (MUST preserve): `stage-feedback-panel-${stage}`, `stage-feedback-save`, `stage-feedback-confirm`, `data-row="primary"` (on the label span).
- Responsive @media at L200-207 keys on `data-row="primary"` — if single-line labels make this query obsolete, remove the @media block entirely (don't leave dead CSS).

V7 token reference (from web/app/globals.css):
- `--v7-panel-2` — surface fill (keep)
- `--v7-border`, `--v7-line` — borders
- `--v7-radius-sm` — corner radius for cards
- `--v7-radius-pill` — pill radius for buttons (keep)
- `--v7-lime` — primary action fill (keep on Confirm)
- `--v7-brand-secondary`, `--v7-brand-secondary-soft` — outline / muted accent (keep on Save)
- `--space-2`, `--space-3`, `--space-4` — spacing scale
- `--font-mono` — button font (keep)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Polish container + collapse buttons to single-line in StageFeedbackPanel.tsx</name>
  <files>web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx</files>
  <read_first>
    - web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx (full file — currently ~330 lines; understand the controlled-component contract from Phase 82.5)
    - .planning/phases/82.7-detail-pane-post-approve-polish/82.7-PATTERNS.md §`StageFeedbackPanel.tsx — D-04 visual polish` (canonical excerpts and exact token list)
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx L93-108 (stage-card visual rhythm reference — node circle padding/border/radius)
  </read_first>
  <behavior>
    - Container at L188-197: full border (not borderTop-only) using `var(--v7-border)` 1px, `var(--v7-radius-sm)`, `var(--space-4)` padding. Background remains `var(--v7-panel-2)`. `marginTop: var(--space-3)` preserved.
    - Save button (L268-296): collapsed to single-line. Replace `flexDirection: "column"` with `display: "inline-flex"`, `alignItems: "center"`, `gap: 6`. Padding `6px 14px`. Label text becomes `Save note` (dropping `· come back later`). Icon span (💾 / …) kept as first child, `aria-hidden="true"`.
    - Confirm button (L298-327): same single-line collapse — `display: "inline-flex"`, `alignItems: "center"`, `gap: 6`, padding `6px 14px`. Label text becomes `Confirm` (dropping `· looks right`). Icon span (✓ / …) kept.
    - All `var(--v7-*)` tokens preserved; no new tokens introduced.
    - data-testid attributes preserved verbatim: `stage-feedback-panel-${stage}`, `stage-feedback-save`, `stage-feedback-confirm`. `data-variant="save"` and `data-variant="confirm"` preserved.
    - `data-row="primary"` on label spans: keep on both buttons to avoid breaking any selector-based test; the responsive @media block at L200-207 may be deleted since single-line layout obviates it (executor decides — if deleted, delete cleanly, leave no orphan CSS).
    - `inFlight` disabled-state styling preserved (opacity 0.6, cursor not-allowed).
  </behavior>
  <action>
    1. Read the current `StageFeedbackPanel.tsx` end-to-end.
    2. Update the container `<div>` (currently at ~L188-197): replace inline `style` with:
       ```
       style={{
         padding: "var(--space-4)",
         background: "var(--v7-panel-2)",
         border: "1px solid var(--v7-border)",
         borderRadius: "var(--v7-radius-sm)",
         marginTop: "var(--space-3)",
       }}
       ```
    3. Update the Save button inline style: change `flexDirection: "column"` → `flexDirection` removed; add `display: "inline-flex"`, `alignItems: "center"`, `gap: 6`; set `padding: "6px 14px"`. Keep `background: "transparent"`, `border: "1px solid var(--v7-brand-secondary)"`, `borderRadius: "var(--v7-radius-pill)"`, `color: "var(--v7-brand-secondary)"`, `fontFamily: "var(--font-mono)"`, `cursor` + `opacity` disabled handling.
    4. Update the Save label span: replace `Save note · come back later` → `Save note`. Keep `data-row="primary"` and `style={{ fontSize: 12 }}`.
    5. Update the Confirm button inline style: mirror Save's collapse — `display: "inline-flex"`, `alignItems: "center"`, `gap: 6`, `padding: "6px 14px"`. Keep `background: "var(--v7-lime)"`, `color: "var(--v7-bg)"`, `border: "1px solid var(--v7-lime)"` (or current value), `borderRadius: "var(--v7-radius-pill)"`, `fontFamily: "var(--font-mono)"`.
    6. Update the Confirm label span: replace `Confirm · looks right` → `Confirm`. Keep `data-row="primary"`.
    7. If the @media (max-width: ...) block at L200-207 only governed two-line→one-line collapse on narrow widths, delete it (it's now unconditionally single-line). If it carried any other rules, leave those rules and remove only the now-dead branch.
    8. Verify no other strings or props in the file are touched. NO change to `onSave`/`onConfirm` callbacks, the textarea, `inFlight` plumbing, or imports.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | grep -E "StageFeedbackPanel|audit/StageFeedbackPanel" || echo "tsc clean for StageFeedbackPanel"</automated>
    <grep_checks>
      - `grep -n "Save note · come back later" web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx` → MUST return 0 matches
      - `grep -n "Confirm · looks right" web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx` → MUST return 0 matches
      - `grep -n ">Save note<" web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx` → MUST return ≥1 match
      - `grep -n ">Confirm<" web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx` → MUST return ≥1 match
      - `grep -n "borderTop: \"1px" web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx` → MUST return 0 matches inside the panel container
      - `grep -n "border: \"1px solid var(--v7-border)\"" web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx` → MUST return ≥1 match (the new container border)
      - `grep -n "borderRadius: \"var(--v7-radius-sm)\"" web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx` → MUST return ≥1 match
      - `grep -n "data-testid=\"stage-feedback-save\"" web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx` → MUST return ≥1 match (preserved)
      - `grep -n "data-testid=\"stage-feedback-confirm\"" web/components/automations/bulk-review/audit/StageFeedbackPanel.tsx` → MUST return ≥1 match (preserved)
    </grep_checks>
  </verify>
  <acceptance_criteria>
    - Save button label is exactly `Save note` (no separator dot, no trailing descriptor).
    - Confirm button label is exactly `Confirm`.
    - Container has full 1px `var(--v7-border)` border + `var(--v7-radius-sm)` radius.
    - Container padding is `var(--space-4)` (up from `var(--space-3)`).
    - `tsc --noEmit` reports zero errors attributable to this file.
    - All four data-testids preserved.
  </acceptance_criteria>
  <done>Inline styles updated, labels collapsed, container promoted to bordered card. File compiles. No callback or import touched.</done>
</task>

</tasks>

<verification>
- `cd web && npx tsc --noEmit` → zero new errors
- Visual smoke (operator): open Stage 1, click a row, click `override stage` on any stage card → the StageFeedbackPanel now renders as a single bordered card with two single-line buttons (`Save note` + `Confirm`), no wrapping at the default right-pane width.
- Hard-separation invariant: no `swarm_intents` reference introduced (file doesn't touch dispatch path).
</verification>

<success_criteria>
- D-04 satisfied: container reads as one card with two clear actions.
- No regression on Phase 82.5 controlled-component contract (no local state added).
- All preserved data-testids match Phase 82.5 verification spec selectors.
</success_criteria>

<output>
After completion, create `.planning/phases/82.7-detail-pane-post-approve-polish/82.7-01-SUMMARY.md` per template.
</output>
