---
phase: 69-handler-agent-canonicalisation-cross-swarm-reuse
plan: 05
type: execute
wave: 5
depends_on: ["69-04"]
files_modified:
  - Agents/debtor-email-swarm/agents/debtor-copy-document-body-agent.md
  - .planning/phases/69-handler-agent-canonicalisation-cross-swarm-reuse/orq-baseline-prompt.txt
  - .planning/phases/69-handler-agent-canonicalisation-cross-swarm-reuse/69-05-SUMMARY.md
autonomous: false
requirements: [CANO-01]
tags: [phase-69, orq-mcp, prompt-patch, operator-gated]

must_haves:
  truths:
    - "Live `mcp__orqai-mcp__get_agent` capture of the body agent prompt exists in `orq-baseline-prompt.txt` under a `## Live Orq Capture (<ISO>)` header"
    - "List_models pre-flight verified primary + all fallback IDs are valid (no `mistral/mistral-large-latest`)"
    - "Live Orq agent prompt updated via `mcp__orqai-mcp__update_agent` to use `<brand_register>` (single brand) instead of `<entity_register>` (5-brand union)"
    - "Post-PATCH `mcp__orqai-mcp__get_agent` verifies the new prompt + `model.parameters.response_format` strict json_schema preserved"
    - "Spec file `Agents/debtor-email-swarm/agents/debtor-copy-document-body-agent.md` updated to match the live PATCH (frontmatter version + deployed_at + body content)"
    - "All 5+ BODY_VERSION sites bumped in lockstep"
  artifacts:
    - path: ".planning/phases/69-handler-agent-canonicalisation-cross-swarm-reuse/orq-baseline-prompt.txt"
      provides: "Pre-PATCH baseline + post-PATCH verification capture"
      contains: "Live Orq Capture"
    - path: "Agents/debtor-email-swarm/agents/debtor-copy-document-body-agent.md"
      provides: "In-tree authoritative paste-source aligned with live agent"
      contains: "<brand_register>"
  key_links:
    - from: "Plan 04 handler invocation"
      to: "Plan 05 Orq agent prompt"
      via: "input shape: brand_register + entity_brand"
      pattern: "brand_register"
---

<objective>
[BLOCKING] Apply the Orq.ai prompt PATCH to make the live `debtor-copy-document-body-agent` consume the new `<brand_register>` block (single brand per invocation) instead of the hardcoded `<entity_register>` 5-brand union. Operator-gated. Uses `mcp__orqai-mcp__*` tools per CLAUDE.md `cba7352b` (create→patch) and `f980a2a1` (list_models pre-flight) learnings.

Sequencing: Plan 04 must be deployed to Vercel BEFORE this plan applies the PATCH (D-19 — risk window of minutes; Phase 65 replay-safety surfaces failures as `automation_runs.status='failed'` rather than silent corruption).

Output: Live agent prompt updated, baseline + post-PATCH captures recorded, in-tree spec aligned, BODY_VERSION sites bumped to `2026-05-04.v2`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/69-handler-agent-canonicalisation-cross-swarm-reuse/69-CONTEXT.md
@.planning/phases/69-handler-agent-canonicalisation-cross-swarm-reuse/69-RESEARCH.md
@.planning/phases/69-handler-agent-canonicalisation-cross-swarm-reuse/orq-baseline-prompt.txt
@CLAUDE.md
@Agents/debtor-email-swarm/agents/debtor-copy-document-body-agent.md
@web/lib/automations/debtor-email/coordinator/types.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: `list_models` pre-flight + `get_agent` baseline capture (BEFORE any PATCH)</name>
  <files>.planning/phases/69-handler-agent-canonicalisation-cross-swarm-reuse/orq-baseline-prompt.txt</files>
  <read_first>
    - .planning/phases/69-handler-agent-canonicalisation-cross-swarm-reuse/orq-baseline-prompt.txt (entire — explains why baseline must be captured live, not from spec)
    - Agents/debtor-email-swarm/agents/debtor-copy-document-body-agent.md (current spec — paste-source claim)
    - CLAUDE.md (Orq.ai section — list_models pre-flight, create→patch, cba7352b, f980a2a1 learnings)
  </read_first>
  <what-built>Nothing yet — this is read-only inspection of live Orq state.</what-built>
  <action>
1. Run `mcp__orqai-mcp__list_models` and search the result for:
   - The agent's primary model ID (currently spec line ~28 says `anthropic/claude-sonnet-4-6` — but per CLAUDE.md that ID does NOT exist in Orq's catalog; the actual valid ID is `aws/eu.anthropic.claude-opus-4-6-v1` for Opus or `anthropic/claude-sonnet-4-5-20250929` for Sonnet 4.5).
   - Each fallback ID listed in the spec's model section (verify each against `list_models` output).
   - **Specifically check whether `mistral/mistral-large-latest` is in the catalog** — per CLAUDE.md this ID does NOT exist. If the spec uses it, the live agent likely does too (Pitfall §7). Replacement: `mistral/mistral-large-2411`.
   Capture findings: which IDs are valid, which need replacement.

2. Run `mcp__orqai-mcp__get_agent agent_key="debtor-copy-document-body-agent"` and capture verbatim:
   - The full `config.prompt` body
   - `model.id` and `model.parameters` (especially `response_format.json_schema.schema`)
   - `settings` (variables, tools)
   - `fallback_models` (root-level array per CLAUDE.md note)
   - Any `tools` references including json_schema tool resources

3. Append the captured data to `.planning/phases/69-handler-agent-canonicalisation-cross-swarm-reuse/orq-baseline-prompt.txt` under a new section:
   ```
   ## Live Orq Capture (2026-05-05T<HH:MM:SS>Z)

   ### list_models findings
   - Primary model in spec: <ID>  → catalog status: <valid|missing>; replacement: <ID|n/a>
   - Fallback 1: ...  → status: ...
   - Fallback 2: ...  → status: ...
   - mistral-large-latest in spec: <yes|no> → action: <replace with mistral-large-2411|n/a>

   ### get_agent.config.prompt (verbatim)
   <full prompt body>

   ### get_agent.model
   id: <model id>
   parameters.response_format: <full JSON shape>
   ...

   ### Drift findings (spec vs live)
   - <enumerate any differences between in-tree spec body and live prompt>
   ```

4. Compare the captured prompt body against the in-tree spec at `Agents/debtor-email-swarm/agents/debtor-copy-document-body-agent.md`. Report drift in the same section.
  </action>
  <how-to-verify>
    1. Operator confirms `list_models` output matches expectations (or flags drift).
    2. Operator confirms baseline capture is recorded in `orq-baseline-prompt.txt`.
    3. Operator confirms drift findings (or absence) are documented before proceeding.
    4. **STOP** here if material drift found — operator decides whether to reconcile spec to live OR to apply Plan 05's PATCH (which overwrites live).
  </how-to-verify>
  <acceptance_criteria>
    - `orq-baseline-prompt.txt` contains a new `## Live Orq Capture` section with timestamp.
    - All 4 sub-sections populated (list_models findings, prompt verbatim, model config, drift findings).
    - Operator has reviewed and explicitly authorizes Task 2 to proceed (or aborts).
    - If `mistral/mistral-large-latest` is found in fallback chain, the action item is recorded.
  </acceptance_criteria>
  <resume-signal>Type "baseline captured, proceed" or "drift found: <description>; reconcile first".</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Update spec file `debtor-copy-document-body-agent.md` (paste-source) BEFORE Orq PATCH</name>
  <files>Agents/debtor-email-swarm/agents/debtor-copy-document-body-agent.md</files>
  <read_first>
    - Agents/debtor-email-swarm/agents/debtor-copy-document-body-agent.md (entire file — frontmatter + Configuration table + prompt body + few-shot examples)
    - .planning/phases/69-handler-agent-canonicalisation-cross-swarm-reuse/69-CONTEXT.md (D-05 — new <brand_register> block shape)
    - .planning/phases/69-handler-agent-canonicalisation-cross-swarm-reuse/orq-baseline-prompt.txt (Live Orq Capture from Task 1)
  </read_first>
  <what-built>The in-tree paste-source spec is the source of truth that gets pasted to Orq Studio in Task 3. Updating it first allows operator to review the prompt diff before any live change.</what-built>
  <action>
Edit `Agents/debtor-email-swarm/agents/debtor-copy-document-body-agent.md`:

1. **Frontmatter updates:**
   - `version: 2026-04-23.v1` → `version: 2026-05-04.v2`
   - `deployed_at: "2026-04-30T00:00:00Z"` → `deployed_at: "2026-05-05T<actual ISO>Z"` (use today's date)
   - Decide on `swarm:` field — D-08 says the agent is `swarm_type='cross-cutting'`. Two options:
     - Keep `swarm: debtor-email-swarm` (filesystem location convention) and add a comment noting `swarm_type='cross-cutting'` in `public.orq_agents`.
     - Or change `swarm: cross-cutting`. Operator picks; default to KEEP `debtor-email-swarm` (filesystem location is unchanged) and ADD a frontmatter note `swarm_type_db: cross-cutting`.

2. **Replace the `<entity_register>` block in the Instructions/prompt body.** Find the existing block:
   ```xml
   <entity_register>
     <entity code="smeba">
       <display_name>Smeba</display_name>
       <register_language>nl</register_language>
       <signoff>Met vriendelijke groet</signoff>
       ... etc for 5 brands ...
     </entity>
     <entity code="berki">...</entity>
     <entity code="sicli-noord">...</entity>
     <entity code="sicli-sud">...</entity>
     <entity code="smeba-fire">...</entity>
   </entity_register>
   ```
   Replace with:
   ```xml
   <brand_register>
     <code>{{brand_register.code}}</code>
     <display_name>{{brand_register.display_name}}</display_name>
     <register_language>{{brand_register.register_language}}</register_language>
     <register_dialect>{{brand_register.register_dialect}}</register_dialect>
     <signoff>{{brand_register.signoff_phrase}}</signoff>
     <formal_address>{{brand_register.formal_address}}</formal_address>
   </brand_register>
   ```

3. **Update the instruction line** that references the entity. Find a line like:
   > "Look up `email_entity` in `<entity_register>` and use that brand's signoff."
   Replace with:
   > "Draft the reply body using `{{brand_register.register_language}}` (`{{brand_register.register_dialect}}` dialect). Address the customer using the form `{{brand_register.formal_address}}`. End with the signoff `{{brand_register.signoff}}`. The `<brand_register>` block contains exactly one brand — the one resolved upstream for this email. Do not assume any other brands exist."

4. **Update the variables block / json_schema** in the spec to reflect the new input shape:
   - Remove `email_entity`, `email_language`.
   - Add `entity_brand` (string), `language` (string), `customer_id` (string), `customer_name` (string|null), `recent_documents` (array), `context_version` (number), `brand_register` (object with 6 fields per D-05).

5. **Bump body_version literal in 5+ places:**
   - Frontmatter `version: 2026-05-04.v2` (done in step 1).
   - Any `<body_version>2026-04-23.v1</body_version>` literals in the prompt → `2026-05-04.v2`.
   - The spec's `response_format.json_schema.schema.properties.body_version.const` if present → `"2026-05-04.v2"`.
   - The spec's `variables.body_version.const` if present → `"2026-05-04.v2"`.
   - All 6 few-shot examples' footer text `body_version: 2026-04-23.v1` → `2026-05-04.v2`.

6. **Fix model fallback chain** if Task 1 found `mistral/mistral-large-latest`:
   - Replace with `mistral/mistral-large-2411`.

7. **Verify spec file is internally consistent:** every reference to `email_entity` is gone, every reference to `<entity_register>` is gone, `<brand_register>` appears exactly once, `body_version: 2026-05-04.v2` appears in all 5+ slots.

8. **Show the operator the diff** (e.g., `git diff Agents/debtor-email-swarm/agents/debtor-copy-document-body-agent.md`). Operator approves before proceeding.
  </action>
  <how-to-verify>
    1. `grep -c "<entity_register>" Agents/debtor-email-swarm/agents/debtor-copy-document-body-agent.md` returns 0.
    2. `grep -c "<brand_register>" Agents/debtor-email-swarm/agents/debtor-copy-document-body-agent.md` returns ≥1.
    3. `grep -c "2026-05-04.v2" Agents/debtor-email-swarm/agents/debtor-copy-document-body-agent.md` returns ≥5.
    4. `grep -c "email_entity\|email_language" Agents/debtor-email-swarm/agents/debtor-copy-document-body-agent.md` returns 0.
    5. Operator approves the diff.
  </how-to-verify>
  <acceptance_criteria>
    - All 8 sub-steps complete.
    - Spec file no longer contains `<entity_register>` or `email_entity`/`email_language` references.
    - `<brand_register>` block present with all 6 templated fields.
    - All 5+ BODY_VERSION sites bumped.
    - Operator approves diff.
  </acceptance_criteria>
  <resume-signal>Type "spec approved, proceed to PATCH" or "fix: <issue>".</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Apply Orq prompt PATCH via `mcp__orqai-mcp__update_agent` + verify with `get_agent`</name>
  <files>.planning/phases/69-handler-agent-canonicalisation-cross-swarm-reuse/orq-baseline-prompt.txt</files>
  <read_first>
    - Agents/debtor-email-swarm/agents/debtor-copy-document-body-agent.md (the now-updated spec from Task 2)
    - .planning/phases/69-handler-agent-canonicalisation-cross-swarm-reuse/orq-baseline-prompt.txt (Live Orq Capture from Task 1)
    - CLAUDE.md (Orq.ai section — `cba7352b` learning: PATCH preserves response_format; `f980a2a1` learning: validate model IDs)
  </read_first>
  <what-built>The Orq agent's live prompt + variables + fallback chain are about to change.</what-built>
  <action>
**Operator-confirmed sequence — pause and wait for "go" between each step:**

1. Construct the PATCH body. Read the updated spec file's:
   - Prompt body (the new prompt with `<brand_register>` block).
   - Variables block (the new shape per Task 2 step 4).
   - Model fallback chain (corrected per Task 2 step 6).
   - `response_format.json_schema.schema` if changed (note: `bodyAgentOutputSchema` is unchanged per D-07, so the response_format schema should be IDENTICAL to baseline — just verify, don't rewrite).

2. **Operator confirmation gate.** Show the operator:
   - The exact PATCH body to be sent.
   - The agent_key: `debtor-copy-document-body-agent`.
   - Confirmation that response_format.json_schema is preserved verbatim from baseline (no schema drift).
   Wait for "apply" or rejection.

3. Run `mcp__orqai-mcp__update_agent` with:
   ```
   agent_key: "debtor-copy-document-body-agent"
   body: {
     config: {
       prompt: <new prompt from spec>,
       model: {
         id: <validated primary model ID>,
         parameters: {
           response_format: <preserved verbatim from baseline>,
           ...other parameters preserved...
         }
       }
     },
     fallback_models: [<corrected list — root-level array per CLAUDE.md>],
     settings: {
       variables: <new variables shape from spec>
     }
   }
   ```
   Per CLAUDE.md `cba7352b`: `update_agent` PATCH persists `response_format`; do NOT use `create_agent` (drops it).

4. Run `mcp__orqai-mcp__get_agent agent_key="debtor-copy-document-body-agent"` IMMEDIATELY after PATCH. Verify:
   - `config.prompt` contains the new `<brand_register>` block.
   - `config.prompt` does NOT contain `<entity_register>`.
   - `model.parameters.response_format.json_schema.schema` is preserved (compare against baseline capture verbatim).
   - `settings.variables` contains the new keys (`entity_brand`, `brand_register`, `language`, `customer_id`, `customer_name`, `recent_documents`, `context_version`).
   - `fallback_models` contains the corrected list (no `mistral-large-latest`).

5. Append to `orq-baseline-prompt.txt` under a new section:
   ```
   ## Post-PATCH Verification (2026-05-05T<ISO>Z)

   ### Verified
   - prompt body contains <brand_register>: yes
   - prompt body contains <entity_register>: no
   - response_format.json_schema preserved byte-for-byte: yes
   - settings.variables updated: yes (keys: entity_brand, brand_register, language, customer_id, customer_name, recent_documents, context_version)
   - fallback_models updated: yes (no mistral-large-latest)
   - body_version literal in prompt: 2026-05-04.v2

   ### Failures
   - <none | enumerate>
   ```

6. **Smoke probe** (one minimal live invocation to confirm the agent doesn't 400):
   ```
   POST /v2/agents/debtor-copy-document-body-agent/responses
   body: {
     entity_brand: "smeba",
     brand_register: { code: "smeba", display_name: "Smeba", register_language: "nl", register_dialect: "nl-NL", signoff_phrase: "Met vriendelijke groet", formal_address: "u" },
     language: "nl",
     customer_id: "test-69-05",
     customer_name: "Test BV",
     recent_documents: [],
     context_version: 1,
     intent_result_intent: "copy_document_request",
     intent_result_sub_type: "invoice",
     intent_result_document_reference: "INV-TEST-69-05",
     intent_result_confidence: "high",
     fetched_document_invoice_id: "INV-TEST-69-05",
     fetched_document_filename: "test.pdf",
     fetched_document_document_type: "invoice",
     fetched_document_created_on: "2026-05-05",
     email_subject: "Test",
     email_body_text: "Kunt u factuur INV-TEST-69-05 opnieuw sturen?",
     email_sender_email: "test@example.com",
     email_sender_first_name: "Test",
     email_mailbox: "debiteuren@smeba.nl",
     email_id: "test-69-05",
     inngest_run_id: "smoke-69-05",
     stage: "generate_body",
     body_version: "2026-05-04.v2",
     emotion_trigger_match: false
   }
   ```
   Run via `curl` or `web/lib/automations/orq-agents/client.ts` `invokeOrqAgent`. Expect a 200 response with valid JSON matching `bodyAgentOutputSchema`. If you get `400 Invalid JSON detected`, the json_schema didn't survive the PATCH — re-issue PATCH with explicit response_format and re-verify.
  </action>
  <how-to-verify>
    1. PATCH returns 200 / success.
    2. `get_agent` shows new prompt and preserved response_format.
    3. Smoke probe returns 200 with valid JSON.
    4. Post-PATCH verification section in `orq-baseline-prompt.txt` is populated.
    5. Operator confirms the agent now uses single-brand prompt template.
  </how-to-verify>
  <acceptance_criteria>
    - `update_agent` PATCH succeeded.
    - Post-PATCH `get_agent` shows: `<brand_register>` present, `<entity_register>` absent, `response_format.json_schema` byte-equal to baseline, `settings.variables` updated, `fallback_models` corrected.
    - Smoke probe returns valid bodyAgentOutput JSON (no 400).
    - `orq-baseline-prompt.txt` has a `## Post-PATCH Verification` section.
  </acceptance_criteria>
  <resume-signal>Type "PATCH verified, agent live on new prompt" or "rollback: <issue>".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Orq Studio API | Operator authorizes each MCP call; CLAUDE.md learnings dictate workflow. |
| In-tree spec → Live agent | Spec is paste-source; PATCH writes spec content into Orq. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-15 | Tampering | PATCH drops `response_format` (CLAUDE.md `cba7352b`) | mitigate | Use `update_agent` (PATCH) NOT `create_agent`; explicit `model.parameters.response_format` in body; post-PATCH `get_agent` verify byte-for-byte. |
| T-69-16 | Tampering | Stale model ID in fallback chain causes Studio dropdown to render empty (CLAUDE.md `f980a2a1`) | mitigate | Mandatory `list_models` pre-flight in Task 1; Task 2 step 6 corrects fallback chain in spec; Task 3 PATCH applies correction live. |
| T-69-01 (revisited) | Prompt injection via `brand_register` field values | mitigate | Brand metadata is operator-managed via migrations only. Plan 07 documentation note will reinforce this trust assumption. |
| T-69-17 | Repudiation | Live agent diverges from spec without record | mitigate | `orq-baseline-prompt.txt` captures both pre-PATCH baseline AND post-PATCH verification with timestamps. |
| T-69-18 | DoS | Smoke probe 400s after PATCH (json_schema dropped) | mitigate | Task 3 step 6 smoke probe is the canary; on failure, re-PATCH with explicit response_format. CLAUDE.md `cba7352b` documents this exact failure mode. |
</threat_model>

<verification>
- All 3 checkpoint tasks completed with operator confirmation.
- Live Orq agent uses `<brand_register>` (single brand) prompt.
- `response_format.json_schema` preserved.
- Spec file in-tree matches live agent (no drift).
- Smoke probe returns valid JSON.
- All BODY_VERSION sites at `2026-05-04.v2`.
</verification>

<success_criteria>
- Live agent + in-tree spec aligned on new shape.
- `mistral/mistral-large-latest` purged from fallback chain (if previously present).
- Pre-PATCH baseline + post-PATCH verification recorded.
- Smoke probe green = Plan 06 regression suite can proceed.
</success_criteria>

<output>
After completion, create `.planning/phases/69-handler-agent-canonicalisation-cross-swarm-reuse/69-05-SUMMARY.md` covering:
- Pre-PATCH baseline summary (model IDs, drift findings)
- PATCH body diff against baseline
- Post-PATCH verification table (per Task 3 step 5)
- Smoke probe outcome
- Confirmation that BODY_VERSION lockstep update is complete (5+ sites)
- Hand-off note to Plan 06: "Live agent on new prompt; ready for regression fixture suite + LIVE_SMOKE=1 run."
</output>
