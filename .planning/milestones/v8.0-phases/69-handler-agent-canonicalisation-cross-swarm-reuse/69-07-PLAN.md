---
phase: 69-handler-agent-canonicalisation-cross-swarm-reuse
plan: 07
type: execute
wave: 7
depends_on: ["69-06"]
files_modified:
  - docs/agentic-pipeline/stage-4-handler.md
  - docs/debtor-email-pipeline-architecture.md
  - .planning/REQUIREMENTS.md
  - CLAUDE.md
autonomous: true
requirements: [CANO-01, CANO-02, CANO-03, CANO-04]
tags: [phase-69, canonicalisation, documentation, requirements-checkoff]

must_haves:
  truths:
    - "`docs/agentic-pipeline/stage-4-handler.md` marks CANO-01..04 as IMPLEMENTED (not RFC) with phase 69 references"
    - "`docs/debtor-email-pipeline-architecture.md` body-agent section reflects new `<brand_register>` template + canonical input shape"
    - "`.planning/REQUIREMENTS.md` traceability table shows CANO-01..04 status='complete'"
    - "`CLAUDE.md` has a brief learnings entry if any new patterns emerged (codegen-from-jsonb pattern)"
    - "Audit grep: `grep -rn 'email_entity\\|email_language\\|<entity_register>' web/lib docs/agentic-pipeline docs/debtor-email-pipeline-architecture.md` returns zero hits in production code paths (test fixtures may keep negative-assertion references)"
  artifacts:
    - path: "docs/agentic-pipeline/stage-4-handler.md"
      provides: "Canonical Stage 4 handler doc reflecting Phase 69 implementation"
      contains: "CANO-01"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CANO-* requirement check-off"
      contains: "CANO-04 | Phase 69 | complete"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 69 implementation"
      via: "traceability table"
      pattern: "CANO-04 \\| Phase 69 \\| complete"
---

<objective>
Wave 6 — close out Phase 69 by updating canonical documentation, marking CANO-01..04 as complete in REQUIREMENTS.md, and capturing any new patterns/learnings in CLAUDE.md. Run a final audit grep to confirm zero residual `email_entity`/`<entity_register>` references in production code paths.

Output: 4 files modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/69-handler-agent-canonicalisation-cross-swarm-reuse/69-CONTEXT.md
@.planning/phases/69-handler-agent-canonicalisation-cross-swarm-reuse/69-06-SUMMARY.md
@docs/agentic-pipeline/stage-4-handler.md
@docs/debtor-email-pipeline-architecture.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update `docs/agentic-pipeline/stage-4-handler.md` — mark CANO-01..04 as IMPLEMENTED</name>
  <files>docs/agentic-pipeline/stage-4-handler.md</files>
  <read_first>
    - docs/agentic-pipeline/stage-4-handler.md (entire — find CANO-01..04 RFC references)
    - .planning/phases/69-handler-agent-canonicalisation-cross-swarm-reuse/69-CONTEXT.md (D-04, D-05, D-08 — implementation details to cite)
  </read_first>
  <action>
1. Read the existing doc and locate CANO-01..04 references (they're listed as "pending" / "RFC" / future work).
2. For each:
   - **CANO-01:** Add an "IMPLEMENTED — Phase 69" badge/note. Cite: input shape changed from `email_entity` (5-value enum) to `entity_brand` (string code) + `brand_register` (full metadata object) per Phase 69 D-04. Reference the new `<brand_register>` prompt block.
   - **CANO-02:** Add IMPLEMENTED note. Cite: brand list sourced from `swarms.entity_brand` jsonb registry (Phase 68 column, expanded by Phase 69 migration `20260505a`). TS `Entity` literal-union is build-time codegen (`scripts/gen-entity-types.ts`).
   - **CANO-03:** Add IMPLEMENTED note. Cite: `debtor-copy-document-body-agent.swarm_type='cross-cutting'` per migration `20260505b`. `loadAgent(agent_key)` runtime path unchanged (organisational hint only).
   - **CANO-04:** Add IMPLEMENTED note. Cite: regression suite includes `smeba-uk` fixture proving zero-prompt-edit onboarding via inline `brand_register`. UK/IE backlog (999.1) operationally onboards via `INSERT swarms.entity_brand` row + `npm run codegen`.
3. Add a "## Phase 69 implementation summary" section near the bottom of the doc with:
   - Migration files: `20260505a`, `20260505b`.
   - Code: `web/lib/swarms/brand-register.ts`, `scripts/gen-entity-types.ts`, refactor of `web/lib/inngest/functions/classifier-invoice-copy-handler.ts`.
   - Live agent: prompt PATCHed via Orq MCP on `<deployment date>` with `<brand_register>` block; response_format json_schema preserved.
   - Verification: 9 mocked fixtures + 4 live smoke tests green.
4. **Trust boundary note** (per threat model T-69-01): add a paragraph to the doc:
   > "Operator note: `swarms.entity_brand` is operator-managed via SQL migrations only. Treat brand metadata writes (signoff_phrase, register_language, etc.) with the same care as Orq agent prompt edits — content flows directly into the `<brand_register>` prompt block at runtime. Stage 0 input safety (Phase 64 SAFE-*) defends inbound emails; brand_register is internal config and is not within Stage 0's scope."
  </action>
  <verify>
    <automated>grep -q "CANO-01.*IMPLEMENTED\|CANO-01 — IMPLEMENTED\|CANO-01.*Phase 69" docs/agentic-pipeline/stage-4-handler.md && grep -q "CANO-04.*IMPLEMENTED\|CANO-04.*Phase 69" docs/agentic-pipeline/stage-4-handler.md && grep -q "brand_register" docs/agentic-pipeline/stage-4-handler.md && grep -q "20260505a" docs/agentic-pipeline/stage-4-handler.md</automated>
  </verify>
  <acceptance_criteria>
    - All 4 CANO requirements have IMPLEMENTED markers with Phase 69 references.
    - Phase 69 implementation summary section exists.
    - Trust boundary note about brand_register operator-management is present.
    - Migration filenames cited.
  </acceptance_criteria>
  <done>Stage 4 handler doc reflects Phase 69 reality.</done>
</task>

<task type="auto">
  <name>Task 2: Update `docs/debtor-email-pipeline-architecture.md` body-agent section + REQUIREMENTS.md traceability + audit grep</name>
  <files>docs/debtor-email-pipeline-architecture.md, .planning/REQUIREMENTS.md, CLAUDE.md</files>
  <read_first>
    - docs/debtor-email-pipeline-architecture.md (entire — find body-agent / copy-document section)
    - .planning/REQUIREMENTS.md (traceability table at lines 129-178)
    - CLAUDE.md (Orq.ai patterns section + learnings list)
  </read_first>
  <action>
**1. Update `docs/debtor-email-pipeline-architecture.md`:**
   - Find the body-agent / copy-document section.
   - Update the input-shape diagram or text to reflect: `customer_id`, `customer_name`, `language`, `entity_brand` (string), `recent_documents`, `context_version`, `brand_register` (object with 6 fields).
   - Remove references to `email_entity`, `email_language`, `<entity_register>` (5-brand union).
   - Add: "Body agent prompt uses a single-brand `<brand_register>` block templated per invocation. Brand metadata sourced from `swarms.entity_brand` jsonb registry."
   - Add note: "BODY_VERSION = `2026-05-04.v2` (Phase 69)."

**2. Update `.planning/REQUIREMENTS.md` traceability table:**

   Find lines for CANO-01..04 (currently `pending`):
   ```
   | CANO-01 | Phase 69 | pending |
   | CANO-02 | Phase 69 | pending |
   | CANO-03 | Phase 69 | pending |
   | CANO-04 | Phase 69 | pending |
   ```

   Replace with:
   ```
   | CANO-01 | Phase 69 | complete (input shape + prompt template; live smoke 4/4 green 2026-05-05) |
   | CANO-02 | Phase 69 | complete (jsonb-of-objects registry + TS codegen; migration 20260505a) |
   | CANO-03 | Phase 69 | complete (orq_agents.swarm_type='cross-cutting' for body agent; migration 20260505b) |
   | CANO-04 | Phase 69 | complete (smeba-uk fixture verified zero-prompt-edit onboarding; live smoke green) |
   ```

   Also update the acceptance bullets (lines 78-83) to remove the `[ ]` checkboxes and replace with `[x]`:
   ```
   - [x] **CANO-01**: ...
   - [x] **CANO-02**: ...
   - [x] **CANO-03**: ...
   - [x] **CANO-04**: ...
   ```

**3. Update `CLAUDE.md` — add a single learning entry under the Supabase or Patterns section:**

   Add after the existing "Inngest" patterns subsection or near the Supabase patterns:
   ```markdown
   - **Build-time codegen for registry-driven literal-union TS types** (Phase 69 D-03). When a registry table column (e.g. `swarms.entity_brand` jsonb) is the source of truth for a closed enumeration AND the codebase wants strict TS typing, do NOT hardcode the literal-union in code. Instead: write a `tsx` script (`scripts/gen-entity-types.ts`) that reads the registry at build-time and emits a `*.generated.ts` file with `as const` array + literal-union type. Run via `npm run codegen`. CI gate: `npm run codegen && git diff --exit-code` to detect drift. Pattern: stable diffs require sorting codes alphabetically in the script. Idempotency: read existing file, skip write if identical. Never hand-edit `*.generated.ts`. Phase 69 commit: `<commit-sha-tbd>`.
   ```

**4. Final audit grep:**

   Run:
   ```bash
   grep -rn "email_entity\|email_language" web/lib/inngest web/lib/automations | grep -v __tests__
   grep -rn "<entity_register>" web/ docs/agentic-pipeline docs/debtor-email-pipeline-architecture.md Agents/
   grep -rn "2026-04-23.v1" web/lib docs/agentic-pipeline docs/debtor-email-pipeline-architecture.md Agents/
   ```

   Each command MUST return zero matches in production code (test fixtures with negative assertions like `not.objectContaining({ email_entity })` are exempt; the grep includes `__tests__` exclusion above for the first command).

   If any match is found in non-test code, that's a Phase 69 leak — fix before commit.
  </action>
  <verify>
    <automated>grep -q "CANO-01 | Phase 69 | complete" .planning/REQUIREMENTS.md && grep -q "CANO-04 | Phase 69 | complete" .planning/REQUIREMENTS.md && grep -q "\[x\] \*\*CANO-04" .planning/REQUIREMENTS.md && grep -q "Build-time codegen for registry-driven" CLAUDE.md && grep -q "brand_register" docs/debtor-email-pipeline-architecture.md && [ "$(grep -rn 'email_entity\|email_language' web/lib/inngest web/lib/automations 2>/dev/null | grep -v __tests__ | wc -l)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - debtor-email-pipeline-architecture.md body-agent section updated to new shape.
    - REQUIREMENTS.md CANO-01..04 marked `complete` with verification notes.
    - REQUIREMENTS.md acceptance bullets `[x]` checked.
    - CLAUDE.md has new learning entry on registry-driven codegen pattern.
    - Audit grep returns zero `email_entity`/`email_language` matches in non-test production code.
    - Audit grep returns zero `<entity_register>` matches in docs + Agents/.
    - Audit grep returns zero `2026-04-23.v1` matches in production code (CLAUDE.md / phase docs comments may cite for historical context — acceptable).
  </acceptance_criteria>
  <done>Documentation aligned, requirements checked off, learnings captured, no orphan references remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → operator mental model | Stale doc post-Phase 69 = operator misuses brand_register or thinks entity_register still applies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-23 | Repudiation | Operator edits `swarms.entity_brand` thinking it's purely descriptive (not realising content lands in agent prompt) | mitigate | Trust boundary note in stage-4-handler.md (Task 1 step 4) explicitly states brand_register fields render into the prompt; treat with same care as agent edits. |
| T-69-24 | Information Disclosure | REQUIREMENTS.md left in `pending` state misleads future planners | mitigate | Task 2 step 2 explicitly checks off CANO-01..04 with verification timestamps. |
| T-69-25 | Tampering | Stale `2026-04-23.v1` body_version reference causes confusion in audits | mitigate | Audit grep at Task 2 step 4; phase doc retains historical mention only as part of "State of the Art" / "Old Approach" tables. |
</threat_model>

<verification>
- All 4 docs/files updated.
- Audit grep returns zero leaked references.
- REQUIREMENTS.md traceability table reflects CANO-* completion.
- CLAUDE.md has new learning.
</verification>

<success_criteria>
- Phase 69 closed: code + live agent + tests + docs + requirements all aligned on canonical context shape + brand-register registry pattern.
- Future planners reading REQUIREMENTS.md see CANO-01..04 as complete with verification breadcrumbs.
- New brand onboarding (Phase 999.1 UK/IE, Phase 73 sales-email) is documented as `INSERT swarms.entity_brand + npm run codegen + commit`.
</success_criteria>

<output>
After completion, create `.planning/phases/69-handler-agent-canonicalisation-cross-swarm-reuse/69-07-SUMMARY.md` covering:
- Files modified and what changed
- Audit grep output (verbatim, showing zero hits in production code)
- REQUIREMENTS.md before/after for CANO-* lines
- CLAUDE.md learning entry text (verbatim)
- Phase 69 close-out: "All CANO-* requirements complete; ready for `/gsd-verify-work 69`."
</output>
