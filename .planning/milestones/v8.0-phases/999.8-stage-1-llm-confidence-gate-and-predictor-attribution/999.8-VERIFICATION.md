---
phase: 999.8-stage-1-llm-confidence-gate-and-predictor-attribution
verified: 2026-05-11T13:41:23Z
status: human_needed
score: 4/4 must-haves verified (code) + 1 residual browser smoke
overrides_applied: 0
human_verification:
  - test: "Browser smoke on /automations/debtor-email/stage-1 after cutover"
    expected: |
      Two filter chip rows render below the noise-category strip:
        - Predictor: All | regex | LLM
        - Confidence: All | high | medium | low
      Clicking 'LLM' sets ?predictor=llm_2nd_pass and narrows the row list.
      Each post-cutover predicted row shows a right-edge chip:
        - 'LLM · medium' (or high/low) for LLM 2nd-pass predictions
        - 'regex' for regex Pass-1 predictions
        - Nothing for pre-cutover historical rows
      Visiting ?sub=pending hides the predictor/confidence chip strip.
    why_human: |
      Visual layout, hover/selection styling, and URL-state round-trip
      cannot be verified programmatically without booting Next.js and
      navigating the dashboard. RTL unit tests cover the component in
      isolation (5/5 GREEN) but do not verify mount geometry inside
      the post-Phase-82 _shell/RowList rightEdgeSlot composition.
  - test: "End-to-end smoke: ingest a Therese-Hendriks-shaped email"
    expected: |
      A new email with 'FW: Invoice <n>' subject and an invoice-correction
      body (asks for corrected invoice + credit note) goes through Stage 1
      and lands at automation_runs.status='predicted' (NOT auto-archived
      to 'Payment Admittance'). pipeline_events shows the LLM 2nd-pass row
      with decision_details.predictor='llm_2nd_pass' and llm_confidence
      in {'medium','low'} (or 'unknown'/medium per the new prompt). The
      row appears in /stage-1 with a 'LLM · medium' chip. No Outlook
      categorize/archive side-effect runs.
    why_human: |
      Requires live Inngest run + Outlook account state; cannot be
      verified by static analysis. The 5/5 Orq agent smoke (Plan 04)
      already validated the prompt re-calibration in isolation
      (Therese paraphrase → unknown/medium), and the unit tests cover
      the gate branch, but a real ingest→worker→verdict cycle is the
      only way to confirm the wiring end-to-end against production.
---

# Phase 999.8: Stage 1 LLM confidence gate + predictor attribution — Verification Report

**Phase Goal:** Add a hard confidence gate to the Stage 1 LLM 2nd-pass `categorize_archive` decision so only `llm_confidence === "high"` may auto-archive, AND attribute every Stage 1 prediction to its predictor (`regex` vs `llm_2nd_pass`) on the verdict row so downstream feedback math (Wilson-CI) groups on `(mailbox, predictor)`. Stage 1 only — does not touch regex thresholds (Phase 71) or Stage 3 (RFC hard-separation).

**Verified:** 2026-05-11T13:41:23Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Stage 1 LLM medium/low predictions no longer auto-archive | VERIFIED | Gate predicate `gateClearsForAutoArchive = !llmInvoked \|\| llmConfidence === "high" \|\| finalCategoryKey === "unknown"` at `web/lib/inngest/functions/classifier-screen-worker.ts:347-352`. Medium/low branch emits `classifier/screen.requires_review` (`:370`) instead of `classifier/verdict.recorded`. The verdict-worker is untouched (D-10 separation). Plan 04 Orq agent re-calibrated 2026-05-11 12:15 UTC: 5/5 smoke inputs returned expected `(category_key, confidence)` including the Therese Hendriks `FW: Invoice 17338747` case at `unknown/medium`. orq_agents row version='2026-05-11.v2'. Plan 01 gate test suite 6/6 GREEN; full screen-worker suite 18/18 GREEN. |
| 2 | Every Stage 1 prediction carries a predictor tag (`regex` \| `llm_2nd_pass`) | VERIFIED | Two write paths: (a) `pipeline_events.decision_details.predictor` denormalized at `classifier-screen-worker.ts:315` via `predictor: llmInvoked ? "llm_2nd_pass" : "regex"` (runs on every emit, both gate branches); (b) `agent_runs.predictor` populated by `recordVerdict` at `stage-1/actions.ts:147-166` deriving from `pipeline_events.decision_details.llm_invoked` (NOT from `regex.matchedRule` — Pitfall 2 mitigated). Migration `20260512_phase9998_agent_runs_predictor.sql` applied to prod (column + CHECK `predictor IN ('regex','llm_2nd_pass')`). `email_id` plumbing fixed forward (Pitfall 9): `VerdictInput` now requires real email_id; detail-pane, race-cohort-banner, row-list all updated. Plan 05 tests 6/6 GREEN. |
| 3 | Feedback math splits by predictor with D-03 calibration drift alert | VERIFIED | `labeling-flip-cron.ts` `evaluateMailbox` iterates `PREDICTORS = ["regex", "llm_2nd_pass"] as const` (`:43`, `:259`) invoking `evaluatePredictorStream` (`:118`) twice per mailbox with `.eq("predictor", predictor)` + `.not("predictor", "is", null)` (D-09 forward-only) + `.eq("swarm_type", "debtor-email")` (Plan 02 reconciliation — pre-flight showed cron silently no-op against 0 'debtor-email-labeling' rows vs 404 'debtor-email' rows). Audit rows keyed `mailbox_flip:<mailbox>:<predictor>` — two streams per mailbox. D-03 calibration drift: high-conf LLM FP rate computed; strict `>` gates produce silent (≤2%), `calibration_warn` audit (>2%, ≤5%), `calibration_alarm` audit + `classifier/calibration_drift.detected` event (>5%). Event declared in `events.ts`. Plan 06 tests 6/6 GREEN. |
| 4 | UI surfaces predictor + confidence chips | VERIFIED | (a) Filter chip strip: `PredictorConfidenceChipStrip` at `web/app/(dashboard)/automations/[swarm]/stage-1/predictor-confidence-chip-strip.tsx` delegates to `_shell/ChipStrip` (post-Phase-82 primitive); mounts in `page.tsx:1050` guarded by `sp.sub !== "pending"`. URL contract `?predictor=(regex\|llm_2nd_pass)`, `?confidence=(high\|medium\|low)`. Server-side enum allowlist (`validatedPredictor` / `validatedConfidence` at `page.tsx:524-528`, T-9998-13 mitigation) silently drops garbage. Filter applies `.eq("decision_details->>predictor", ...)` + `.eq("decision_details->>llm_confidence", ...)` at `:583-588`. Plan 07 tests 5/5 GREEN. (b) Per-row chip: `PredictorChip` component renders `LLM · medium`/`LLM · high`/`LLM · low` or `regex` (D-12: no rule_key in chip) or null for pre-cutover. Mounts via `_shell/RowList.rightEdgeSlot` in `client-shell.tsx:190-201` keyed by `predictorById` useMemo. `PredictedRow` shape extended with `predictor` + `llmConfidence`, derived server-side from `timelineMap` filtered on `stage===1` (hard-separation enforced). Plan 08 tests 5/5 GREEN. |

**Score:** 4/4 truths verified

### Therese Hendriks Regression Specifically

The motivating email — `email_id=09823c92-f6c4-4bce-bb9c-e7935e508e40`, Therese.Hendriks@ago-groep.nl, `FW: Invoice 17338747`, smeba-fire.be mailbox, 2026-05-08 — was tagged `payment_admittance/medium` by the pre-Phase-999.8 agent and auto-archived because no gate existed. Post-Phase-999.8:

1. **Prompt side (Plan 04 / D-02):** Re-calibrated Orq agent smoke-tested 2026-05-11 12:14 UTC returned `unknown / medium` for the Therese paraphrase (input #3 of 5; expected `unknown / medium` — exact match, first call, no retries). The `<calibration_examples>` block in the prompt encodes this exact case as the load-bearing few-shot.
2. **Code side (Plan 03 / D-01):** Even if the prompt-side classification regressed back to `payment_admittance/medium`, the gate predicate would still route it to `classifier/screen.requires_review` (medium confidence → non-unknown final_key → gate-blocked). `automation_runs.status` stays at `predicted`. No Outlook side-effects fire.
3. **Coverage in tests:** Both the unit gate test suite (6/6 GREEN) and the Orq live smoke (1/1 on the Therese case) cover this regression.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `web/lib/inngest/functions/classifier-screen-worker.ts` | Gate branch + denormalized predictor | VERIFIED | Gate at :347-380, denormalization at :315, prior `low→unknown` coercion removed. |
| `web/lib/inngest/events.ts` | Two new declared-with-no-listener events | VERIFIED | `classifier/screen.requires_review` (D-10) + `classifier/calibration_drift.detected` (D-03) declared. |
| `web/app/(dashboard)/automations/[swarm]/stage-1/actions.ts` | `recordVerdict` writes predictor + email_id | VERIFIED | Pipeline_events lookup at :147-166; insert includes `email_id` and `predictor`. |
| `web/lib/inngest/functions/labeling-flip-cron.ts` | Per-predictor Wilson-CI + D-03 alarm | VERIFIED | PREDICTORS loop, evaluatePredictorStream helper, calibration_drift event emission, audit row split. swarm_type aligned 'debtor-email' on both read and audit-row write. |
| `supabase/migrations/20260512_phase9998_agent_runs_predictor.sql` | `agent_runs.predictor` column + CHECK | VERIFIED | Idempotent DDL; applied to production 2026-05-11. |
| `supabase/migrations/20260512_phase9998_stage1_classifier_recalibrate.sql` | Audit record of prompt diff | VERIFIED | Applied; orq_agents.version='2026-05-11.v2'. |
| `web/app/(dashboard)/automations/[swarm]/stage-1/predictor-confidence-chip-strip.tsx` | Filter chip strip | VERIFIED | Delegates to `_shell/ChipStrip`; URL state via search params. |
| `web/app/(dashboard)/automations/[swarm]/stage-1/components/PredictorChip.tsx` | Per-row chip component | VERIFIED | D-08/D-12 render rules; null for pre-cutover. |
| `web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx` | PageSearchParams + loadPageData filter + JSX mount | VERIFIED | validatedPredictor/validatedConfidence allowlist; PredictedRow extended; chip strip mounted with sub!='pending' guard. |
| `web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx` | PredictorChip mounted via rightEdgeSlot | VERIFIED | predictorById useMemo + rightEdgeSlot closure. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| classifier-screen-worker (LLM medium/low branch) | Stage 1 row list | emit-pipeline-event (status=predicted) + denormalized predictor | WIRED | emit-pipeline-event runs unconditionally (both gate branches); row surfaces via existing status=predicted feed. |
| classifier-screen-worker (gate-blocked) | future learning loop | classifier/screen.requires_review event (no subscriber by design) | WIRED (declared) | Intentionally no subscriber per D-10. |
| recordVerdict | pipeline_events.decision_details | email_id query on stage=1 row | WIRED | maybeSingle() on most recent Stage 1 event; predictor derived from llm_invoked. |
| labeling-flip-cron | agent_runs | per-predictor `.eq("predictor", ...)` + `.not("predictor", "is", null)` | WIRED | Cold-start handled with `cold_start: true` reason hint when n < FLIP_N_MIN. |
| labeling-flip-cron (alarm tier) | future on-call/learning-loop | classifier/calibration_drift.detected event | WIRED (declared) | No subscriber yet; audit row in classifier_rule_evaluations is source of truth. |
| Stage 1 URL `?predictor=` / `?confidence=` | filtered row list | loadPageData enum allowlist → `.eq("decision_details->>...", ...)` | WIRED | hasFilters extended to include validated values so JOIN-back fires. |
| Stage 1 row (PredictedRow) | per-row chip | predictorById Map + rightEdgeSlot closure | WIRED | timelineMap → decision_details.predictor + llm_confidence, filtered on stage===1. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| PredictorChip per-row | predictor + llmConfidence (PredictedRow fields) | timelineMap[r.id] filtered ev.stage===1, reading decision_details.predictor + llm_confidence | Yes — populated by classifier-screen-worker:315 on every Stage 1 emit | FLOWING |
| PredictorConfidenceChipStrip filter | URL ?predictor / ?confidence → loadPageData JOIN-back | pipeline_events.decision_details->>predictor (denormalized Plan 02) | Yes — same source as per-row chip | FLOWING |
| agent_runs.predictor | derived in recordVerdict from pipeline_events.decision_details.llm_invoked | pipeline_events row at email_id+stage=1+swarm_type | Yes for post-cutover; NULL for pre-cutover (D-09 forward-only) | FLOWING (forward-only) |
| labeling-flip-cron audit rows | n, agree, ci_lo, action per (mailbox, predictor) | agent_runs filtered by predictor + human_verdict | Yes — pre-flight confirmed 404 'debtor-email' rows with human_verdict in prod | FLOWING (cold-start window for new predictor column until N≥50 per stream) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Gate predicate present | `grep "gateClearsForAutoArchive" web/lib/inngest/functions/classifier-screen-worker.ts` | 2 hits (`:347`, `:352`) | PASS |
| Requires-review event declared | `grep "classifier/screen.requires_review" web/lib/inngest/events.ts` | declared | PASS |
| Calibration-drift event declared | `grep "classifier/calibration_drift.detected" web/lib/inngest/events.ts` | declared | PASS |
| Per-predictor cron loop | `grep "PREDICTORS\\|evaluatePredictorStream" labeling-flip-cron.ts` | PREDICTORS const + helper + for-loop present | PASS |
| Migration files present | `ls supabase/migrations/20260512_phase9998_*.sql` | 2 files | PASS |
| PredictorChip in client-shell | `grep "PredictorChip\\|predictorById" client-shell.tsx` | 4 hits incl. rightEdgeSlot mount | PASS |
| Filter chip strip mounted | `grep "PredictorConfidenceChipStrip" page.tsx` | 2 hits (import + JSX) | PASS |
| Live agent smoke (5/5 inputs) | Plan 04 Task 4 transcript | All 5 expected outputs returned on first call; Therese case = `unknown/medium` ✓ | PASS |
| Browser smoke on /stage-1 | (not yet performed) | — | SKIP (deferred to human verify; see human_verification) |
| End-to-end ingest smoke | (not yet performed) | — | SKIP (deferred to human verify) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| REQ-GATE-01 | Plan 03 | Confidence gate at code level (D-01) | SATISFIED | classifier-screen-worker.ts:347-380 + 6/6 GREEN tests |
| REQ-VERDICT-08 | Plan 05 | recordVerdict writes predictor (D-07/D-08) | SATISFIED | actions.ts:147-183 + 6/6 GREEN tests |
| REQ-CRON-09 | Plan 06 | Per-predictor Wilson-CI split (D-07/D-09) | SATISFIED | labeling-flip-cron.ts PREDICTORS loop + audit rows + 6/6 GREEN tests |
| REQ-FP-03 | Plan 06 | D-03 calibration drift alarm | SATISFIED | strict-> gates at 2% warn / 5% alarm + event emission |
| REQ-CHIP-05 | Plans 07+08 | Filter chips + per-row chip (D-05/D-08) | SATISFIED | PredictorConfidenceChipStrip + PredictorChip components + 10/10 GREEN tests |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | — | — | No blockers found. Plan-by-plan SUMMARYs document zero stubs, zero TODOs, no destructured `inngest.send`, no replay-unsafe id generation (Phase 65 rule honored), all `.eq()` parametrized via Supabase client. |

Notes on intentionally-deferred items (not anti-patterns):
- `agent_runs.predictor` is forward-only; historical rows remain NULL by design (D-09). `labeling-flip-cron` filters `predictor IS NOT NULL`. Cold-start window before N≥50 per `(mailbox, predictor)` is accepted.
- `classifier/screen.requires_review` and `classifier/calibration_drift.detected` are declared-with-no-listener by design (D-10). Future learning-loop work picks them up.
- Pre-existing `load-page-data.test.ts` Tests 4-6 failures are Phase 82-06 carry-forward (view fixture issues) — confirmed identical with `git stash` baseline. Not introduced by this phase.

### Human Verification Required

1. **Browser smoke on `/automations/debtor-email/stage-1`** — verify both chip rows render below the noise-category strip, URL state round-trips on click, `?sub=pending` hides the strip, per-row chips render correctly for post-cutover predictions, pre-cutover rows show no chip with no layout drift. *Why human:* visual layout + URL round-trip cannot be verified without booting Next.js.

2. **End-to-end ingest smoke** — send a fresh Therese-Hendriks-shaped email (invoice-correction request with `FW: Invoice` subject) through the live pipeline and confirm: (a) no Outlook categorize/archive side-effect, (b) `automation_runs.status = "predicted"`, (c) `pipeline_events.decision_details` shows `predictor='llm_2nd_pass'` + `llm_confidence in {'medium','low'}` or `unknown/medium`, (d) row appears in `/stage-1` with `LLM · medium` chip. *Why human:* requires live Inngest run + Outlook account state.

### Gaps Summary

No code-level gaps. All four observable truths verified; all required artifacts exist, are substantive, are wired into the runtime, and have real data flowing through them (denormalization + recordVerdict + cron + UI). The motivating Therese Hendriks regression is covered by both prompt re-calibration (Orq smoke 1/1 on the case) and the code gate (which would catch it even if the prompt regressed). 

Two browser/integration smoke items remain as residual human-verify work. These are explicitly called out by the orchestrator's known-state as "Browser smoke not yet performed" — the code/database/prompt layers are complete; the residual is observational verification of the running system.

---

_Verified: 2026-05-11T13:41:23Z_
_Verifier: Claude (gsd-verifier)_
