---
phase: 999.8-stage-1-llm-confidence-gate-and-predictor-attribution
plan: 08
name: Wave 6 — Per-row predictor chip on Stage 1 row card (D-08, D-12)
type: tdd
wave: 6
depends_on: [01, 02, 07]
files_modified:
  - web/app/(dashboard)/automations/[swarm]/stage-1/components/PredictorChip.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/row-strip.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx
autonomous: true
gap_closure: false
requirements: [REQ-ROWCHIP-08]
must_haves:
  truths:
    - "Each Stage 1 row card renders a small chip showing the predictor"
    - "For LLM-predicted rows the chip text is 'LLM · {confidence}' (D-08) — e.g. 'LLM · medium'"
    - "For regex-predicted rows the chip text is 'regex' only — no rule_key (D-12 compact lock)"
    - "Chip sits in the existing per-row badge composition slot alongside BudgetBreachBadge / TaggingFailureBadge"
    - "Phase 81 dependency: row-strip.tsx exists at the renamed /stage-1/ path — confirm before starting"
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/components/PredictorChip.tsx"
      provides: "New per-row badge component"
      contains: "PredictorChip"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/row-strip.tsx"
      provides: "Mounts <PredictorChip /> alongside existing per-row badges"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx"
      provides: "PredictedRow shape carries predictor + confidence (read from pipeline_events.decision_details)"
  key_links:
    - from: "pipeline_events.decision_details.predictor"
      to: "PredictedRow.predictor field"
      via: "loadPageData row mapper"
      pattern: "predictor: details.predictor"
    - from: "PredictedRow.predictor"
      to: "<PredictorChip>"
      via: "row prop"
      pattern: "predictor=\\{row\\.predictor\\}"
---

<objective>
Render a compact predictor chip on every Stage 1 row card so operators see at a glance whether the prediction was made by regex or by the LLM 2nd-pass (D-08, D-12).

Format (D-08, D-12 locked):
- LLM-predicted: `LLM · medium` (or high/low) — confidence variant gives instant calibration context
- Regex-predicted: `regex` — compact, no rule_key (D-12 explicit lock; detail pane already shows the rule)

This is purely additive. No layout shift to existing badges; the chip sits alongside `BudgetBreachBadge` / `TaggingFailureBadge` in the same composition slot per PATTERNS §"row-list.tsx".

**Phase 81 dependency:** Same as Plan 07 — `row-strip.tsx` is at the renamed `/stage-1/` path. Confirm Phase 81 is on main before starting.

Output: One new badge component + row-strip composition update + small extension to `PredictedRow` shape in page.tsx to carry predictor + confidence through.

Runs parallel to Plan 07 (Wave 5). Both depend on Phase 81 being merged. No file overlap with Plan 07 except `page.tsx` — Plan 07 extends `PageSearchParams` + `loadPageData` filters; Plan 08 extends `PredictedRow` shape + row mapper. The two `page.tsx` changes are at different points in the file but touch the same file — coordinate by having Plan 08's `page.tsx` change land after Plan 07 to avoid merge conflicts.

**Wave-shape note:** Because both plans touch `page.tsx`, they are sequential (Plan 08 runs after Plan 07 within Wave 5), not strictly parallel. Listed in Wave 5 with file-conflict-induced ordering.

Implements D-08, D-12. Honors any Plan 01 RED tests for row rendering (if added there; if not, this plan adds an inline RTL test for the chip component).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/999.8-stage-1-llm-confidence-gate-and-predictor-attribution/999.8-CONTEXT.md
@.planning/phases/999.8-stage-1-llm-confidence-gate-and-predictor-attribution/999.8-RESEARCH.md
@.planning/phases/999.8-stage-1-llm-confidence-gate-and-predictor-attribution/999.8-PATTERNS.md
@.planning/phases/999.8-stage-1-llm-confidence-gate-and-predictor-attribution/999.8-02-SUMMARY.md
@.planning/phases/999.8-stage-1-llm-confidence-gate-and-predictor-attribution/999.8-07-SUMMARY.md
@web/app/(dashboard)/automations/[swarm]/stage-1/row-strip.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/components/budget-breach-badge.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/components/TaggingFailureBadge.tsx

<interfaces>
PredictedRow shape (extend, source page.tsx around line 99-184 per RESEARCH §6):
```typescript
type PredictedRow = {
  id: string;
  // ... existing fields ...
  predictor?: "regex" | "llm_2nd_pass" | null;       // ← NEW
  llmConfidence?: "low" | "medium" | "high" | null;  // ← NEW
};
```

Source (Plan 02 Task 3 denormalization):
- `pipeline_events.decision_details.predictor` → carries 'regex' or 'llm_2nd_pass'
- `pipeline_events.decision_details.llm_confidence` → carries 'low'/'medium'/'high' enum (already there)

Render rules:
- `predictor === "llm_2nd_pass"` → chip text `LLM · {llmConfidence}` (capitalize confidence: `LLM · medium`)
- `predictor === "regex"` → chip text `regex` (lowercase, no rule_key — D-12 lock)
- `predictor === null` → render nothing (pre-cutover row; defensive)

Visual idiom: mirror `BudgetBreachBadge` / `TaggingFailureBadge` — small client component, compact rounded pill, single line.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PredictorChip component + inline RTL test</name>
  <files>
    web/app/(dashboard)/automations/[swarm]/stage-1/components/PredictorChip.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/components/__tests__/PredictorChip.test.tsx
  </files>
  <behavior>
    - Given `{predictor: "llm_2nd_pass", llmConfidence: "medium"}`, renders text "LLM · medium"
    - Given `{predictor: "regex", llmConfidence: null}`, renders text "regex"
    - Given `{predictor: null}`, renders nothing (returns null)
    - Visual styling matches BudgetBreachBadge: small rounded pill, compact, single-line
  </behavior>
  <action>
    1. Create `components/PredictorChip.tsx`:
       ```tsx
       type Props = {
         predictor: "regex" | "llm_2nd_pass" | null | undefined;
         llmConfidence?: "low" | "medium" | "high" | null;
       };
       export function PredictorChip({ predictor, llmConfidence }: Props) {
         if (!predictor) return null;
         const label =
           predictor === "llm_2nd_pass"
             ? `LLM · ${llmConfidence ?? "?"}`
             : "regex";
         return <span className="...">{label}</span>;
       }
       ```
       Style with the same Tailwind / token utilities BudgetBreachBadge uses. Read BudgetBreachBadge first to mirror visual idiom exactly.

    2. Create RTL test at `components/__tests__/PredictorChip.test.tsx` covering the three behaviour cases. Use the surrounding test conventions for `@testing-library/react` setup.
  </action>
  <verify>
    <automated>cd web && npm run test -- PredictorChip</automated>
  </verify>
  <done>Component renders correctly for all three cases. Tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PredictedRow shape extension + loadPageData mapper</name>
  <files>web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx</files>
  <behavior>
    - `PredictedRow` type carries `predictor` and `llmConfidence` fields
    - The `loadPageData` row mapper populates these from `pipeline_events.decision_details.predictor` and `pipeline_events.decision_details.llm_confidence` respectively
    - For historical rows with no denormalized predictor, fields are `null` (PredictorChip renders nothing — graceful)
  </behavior>
  <action>
    1. Extend the `PredictedRow` type in page.tsx (~line 99-184) per `<interfaces>` above.

    2. In `loadPageData`'s row mapper (the section that builds `predictedRows` from the joined pipeline_events_email_summary data), populate the two new fields:
       ```typescript
       predictor: (decisionDetails?.predictor ?? null) as PredictedRow["predictor"],
       llmConfidence: (decisionDetails?.llm_confidence ?? null) as PredictedRow["llmConfidence"],
       ```

    3. Coordinate with Plan 07's changes: this task may touch the same `loadPageData` region as Plan 07 (which adds the predictor/confidence URL filters). Land Plan 08 AFTER Plan 07 to avoid merge conflicts within Wave 5.

    Run the full stage-1 suite — no regression.
  </action>
  <verify>
    <automated>cd web && npm run test -- stage-1</automated>
  </verify>
  <done>PredictedRow carries predictor + llmConfidence on new rows. Historical rows have null. No regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mount PredictorChip in row-strip.tsx</name>
  <files>web/app/(dashboard)/automations/[swarm]/stage-1/row-strip.tsx</files>
  <behavior>
    - row-strip renders `<PredictorChip predictor={row.predictor} llmConfidence={row.llmConfidence} />` adjacent to the existing `<BudgetBreachBadge>` / `<TaggingFailureBadge>` composition
    - When predictor is null (historical rows), nothing renders — no empty space (PredictorChip returns null internally)
  </behavior>
  <action>
    1. Import PredictorChip at the top of row-strip.tsx, mirroring the existing imports for BudgetBreachBadge / TaggingFailureBadge at lines 13 / 20.

    2. Place `<PredictorChip predictor={row.predictor} llmConfidence={row.llmConfidence} />` in the same JSX block where BudgetBreachBadge / TaggingFailureBadge currently render. Recommendation: insert it as the FIRST chip so operators see the predictor first — it's the most-load-bearing per-row metadata for this phase. If existing badge order has reasons (e.g. severity), inline a brief comment explaining the choice.

    3. NO other layout changes. Don't refactor the row composition; just add one component.
  </action>
  <verify>
    <automated>cd web && npm run test -- row-strip 2>&1 || echo "(if row-strip has no dedicated test, RTL coverage falls through PredictorChip + visual check below)"</automated>
  </verify>
  <done>PredictorChip rendered in row-strip. No layout shift on historical (predictor=null) rows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual check — chip on row cards in dev</name>
  <what-built>
    Per-row predictor chip on the Stage 1 row list.
  </what-built>
  <how-to-verify>
    1. `cd web && npm run dev`, visit `/automations/debtor-email/stage-1`.
    2. Confirm rows with new predictions (predictor populated post-cutover) show either "LLM · {conf}" or "regex".
    3. Confirm rows with no predictor (historical) show no chip (no empty space).
    4. Filter by `?predictor=llm_2nd_pass` (chip strip from Plan 07) — all visible rows should have "LLM · X" chips.
    5. Filter by `?predictor=regex` — all visible rows should have "regex" chips.
    6. Confirm chip styling matches the surrounding BudgetBreachBadge / TaggingFailureBadge — no visual drift.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- PredictorChip tests GREEN
- Stage-1 visual check approved
- Chip + Plan 07 filter interact consistently (filtered list's chips agree with the active filter)
- No regression
</verification>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| pipeline_events.decision_details (read) | server-derived data; chip is purely presentational |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9998-15 | Information Disclosure | chip reveals classifier internals to operator | accept | operators are the intended audience; chip is operational metadata, not customer-data |
</threat_model>

<success_criteria>
- Chip renders correctly for both predictor values + null fallback
- Visual idiom consistent with surrounding badges
- Coordinated with Plan 07 page.tsx changes (no merge conflict)
- No regression
</success_criteria>

<output>
Create `.planning/phases/999.8-stage-1-llm-confidence-gate-and-predictor-attribution/999.8-08-SUMMARY.md` with:
- Component implementation summary
- Confirmation that Plan 07 + Plan 08 page.tsx changes coexist
- Visual check result (screenshot if available)
- Test results
- Final phase-completion note: Phase 999.8 ready for `/gsd-verify-work` against the must_haves across all 8 plans
</output>
