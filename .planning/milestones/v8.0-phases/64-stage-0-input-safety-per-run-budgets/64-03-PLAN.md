---
phase: 64
plan: 03
type: execute
wave: 2
depends_on: [64-01]
files_modified:
  - web/lib/automations/debtor-email/nxt-zap-client.ts
  - web/lib/automations/debtor-email/resolve-debtor.ts
  - web/lib/automations/debtor-email/invoice-copy-handler.ts
autonomous: true
requirements: [BUDG-02]
tags: [zapier-tools, allowlist, default-deny, debtor-email]

must_haves:
  truths:
    - "callNxtTool with NULL or empty allowed_for_intents throws ToolNotAllowedForIntentError"
    - "callNxtTool with intent NOT in allowlist throws ToolNotAllowedForIntentError"
    - "callNxtTool with intent IN allowlist proceeds past the allowlist guard"
    - "All existing callsites of callNxtTool / lookupSenderToAccount / lookupIdentifierToAccount / lookupCandidateDetails are updated to pass intent"
    - "ToolNotAllowedForIntentError is a named export"
  artifacts:
    - path: "web/lib/automations/debtor-email/nxt-zap-client.ts"
      provides: "callNxtTool(tool_id, input, intent), ToolNotAllowedForIntentError, ZapierToolRow.allowed_for_intents"
      contains: "ToolNotAllowedForIntentError"
    - path: "web/lib/automations/debtor-email/resolve-debtor.ts"
      provides: "callsite passes intent to lookup helpers"
    - path: "web/lib/automations/debtor-email/invoice-copy-handler.ts"
      provides: "callsite passes intent='invoice_copy_request' to nxt.invoice_fetch"
  key_links:
    - from: "web/lib/automations/debtor-email/nxt-zap-client.ts"
      to: "public.zapier_tools.allowed_for_intents (live column from Plan 01 migration)"
      via: "loadTool SELECT projection"
      pattern: "allowed_for_intents"
    - from: "web/lib/automations/debtor-email/resolve-debtor.ts"
      to: "swarm_categories.key (intent identifier per D-07)"
      via: "intent string passed to callNxtTool"
      pattern: "(unknown|invoice_copy_request|payment_status_question)"
---

<objective>
Implement BUDG-02: tool calls are gated by intent allowlist. Add the `ToolNotAllowedForIntentError` class, extend `ZapierToolRow` with `allowed_for_intents`, extend `loadTool` SELECT projection, add a default-deny guard inside `callNxtTool`, and update every callsite to thread an `intent: string` argument.

Purpose: A copy-document handler attempting to invoke a payment-update tool MUST be rejected before the HTTP POST. Default-deny semantics (D-06): NULL or empty `allowed_for_intents` ⇒ no intent can invoke. This Phase-64 plan flips Plan 01's RED `nxt-zap-client-allowlist.test.ts` to GREEN.

Output: 1 modified client file + 2 modified callsite files. All tests in `web/lib/automations/debtor-email/__tests__/` pass; existing callsites compile and existing behavior on legitimate intent paths is unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-stage-0-input-safety-per-run-budgets/64-CONTEXT.md
@.planning/phases/64-stage-0-input-safety-per-run-budgets/64-RESEARCH.md
@.planning/phases/64-stage-0-input-safety-per-run-budgets/64-PATTERNS.md
@CLAUDE.md
@docs/zapier-patterns.md
@web/lib/automations/debtor-email/nxt-zap-client.ts
@supabase/migrations/20260430e_stage_0_safety_and_allowlist.sql

<interfaces>
<!-- Plan 01 test contract (RED → GREEN target) -->

From web/lib/automations/debtor-email/__tests__/nxt-zap-client-allowlist.test.ts:
- Imports `{ callNxtTool, ToolNotAllowedForIntentError }` from `../nxt-zap-client`
- Asserts `ToolNotAllowedForIntentError` is a named export
- Asserts NULL allowed_for_intents → throws
- Asserts empty array → throws
- Asserts intent not in array → throws
- Asserts intent in array → does NOT throw allowlist error

From supabase/migrations/20260430e_stage_0_safety_and_allowlist.sql (live in Supabase per Plan 01 Task 6):
- `public.zapier_tools.allowed_for_intents text[]`
- Backfill values:
  - `nxt.contact_lookup`, `nxt.identifier_lookup`, `nxt.candidate_details` → `['unknown', 'invoice_copy_request']`
  - `nxt.invoice_fetch` → `['invoice_copy_request']`

Existing callNxtTool signature (TO BE MODIFIED — see web/lib/automations/debtor-email/nxt-zap-client.ts:168-183):
```typescript
export async function callNxtTool<T extends NxtToolId>(
  tool_id: T,
  input: NxtToolInputs[T],
): Promise<NxtToolOutputs[T]>;
// gains `intent: string` as the third required parameter
```

Existing convenience exports (must also accept intent):
- `lookupSenderToAccount(...)` → currently calls `nxt.contact_lookup`
- `lookupIdentifierToAccount(...)` → calls `nxt.identifier_lookup`
- `lookupCandidateDetails(...)` → calls `nxt.candidate_details`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ToolNotAllowedForIntentError + extend nxt-zap-client.ts</name>
  <files>web/lib/automations/debtor-email/nxt-zap-client.ts</files>
  <read_first>
    - web/lib/automations/debtor-email/nxt-zap-client.ts (FULL file — especially lines 35-45 ZapierToolRow, lines 50-72 loadTool, lines 168-283 callNxtTool, lines 350-376 convenience exports)
    - web/lib/automations/debtor-email/__tests__/nxt-zap-client-allowlist.test.ts (Plan 01 RED test — pinned expectations)
    - .planning/phases/64-stage-0-input-safety-per-run-budgets/64-PATTERNS.md (lines 177-251 — full modification recipe)
    - .planning/phases/64-stage-0-input-safety-per-run-budgets/64-CONTEXT.md (D-05, D-06, D-07, D-08 — all four allowlist decisions)
    - .planning/phases/64-stage-0-input-safety-per-run-budgets/64-RESEARCH.md (Pattern 5 lines 428-466, Pitfall 2 — NULL = denied)
    - supabase/migrations/20260430e_stage_0_safety_and_allowlist.sql (column shape + backfill values)
  </read_first>
  <behavior>
    - `ToolNotAllowedForIntentError` is a named export, extends Error, has `tool_id` + `intent` public fields, name === "ToolNotAllowedForIntentError"
    - `ZapierToolRow` gains `allowed_for_intents: string[] | null`
    - `loadTool` SELECT projection includes `allowed_for_intents`
    - `callNxtTool` signature gains `intent: string` as third required parameter
    - Default-deny guard fires immediately after `loadTool` and BEFORE the existing `tool.pattern !== "async_callback"` check
    - NULL → throws; empty array → throws; intent not in array → throws; intent in array → proceeds
    - Convenience exports (`lookupSenderToAccount`, `lookupIdentifierToAccount`, `lookupCandidateDetails`) gain `intent: string` parameter and forward it to `callNxtTool`
  </behavior>
  <action>
    Modify `web/lib/automations/debtor-email/nxt-zap-client.ts`:

    1. **Add error class** near the top (after the existing schema declarations, before `loadTool`):
    ```typescript
    /** Phase 64 BUDG-02 / D-06: default-deny intent allowlist breach. */
    export class ToolNotAllowedForIntentError extends Error {
      constructor(public tool_id: string, public intent: string) {
        super(`Tool "${tool_id}" not allowed for intent "${intent}"`);
        this.name = "ToolNotAllowedForIntentError";
      }
    }
    ```

    2. **Extend `ZapierToolRow`** type definition — add field:
    ```typescript
    allowed_for_intents: string[] | null;
    ```

    3. **Extend `loadTool` SELECT** projection to include `allowed_for_intents`:
    ```typescript
    .select("tool_id, backend, pattern, target_url, auth_method, auth_secret_env, auth_field_name, callback_route, enabled, allowed_for_intents")
    ```

    4. **Modify `callNxtTool` signature**: add `intent: string` as the third required parameter:
    ```typescript
    export async function callNxtTool<T extends NxtToolId>(
      tool_id: T,
      input: NxtToolInputs[T],
      intent: string,
    ): Promise<NxtToolOutputs[T]> {
      if (!APP_URL) throw new Error("NEXT_PUBLIC_APP_URL is not set ...");
      const tool = await loadTool(tool_id);

      // Phase 64 BUDG-02 / D-06: default-deny.
      const allowed = tool.allowed_for_intents ?? [];
      if (allowed.length === 0 || !allowed.includes(intent)) {
        throw new ToolNotAllowedForIntentError(tool_id, intent);
      }

      if (tool.pattern !== "async_callback") {
        throw new Error(`zapier_tools: tool "${tool_id}" pattern=${tool.pattern}, expected async_callback`);
      }
      // ... rest of existing flow ...
    }
    ```

    5. **Modify convenience exports** (`lookupSenderToAccount`, `lookupIdentifierToAccount`, `lookupCandidateDetails` near line 368-375) to accept + forward `intent`. Example:
    ```typescript
    export async function lookupSenderToAccount(input: ..., intent: string) {
      return callNxtTool("nxt.contact_lookup", input, intent);
    }
    ```

    Do NOT change the existing HTTP / callback-routing logic. Do NOT change `loadTool`'s caching behavior.

    Run `cd web && npx tsc --noEmit` after editing — it WILL fail at the existing callsites (they don't yet pass `intent`). That's the planned breakage; Task 2 fixes those callsites.
  </action>
  <verify>
    <automated>cd web && npx vitest run lib/automations/debtor-email/__tests__/nxt-zap-client-allowlist.test.ts --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `ToolNotAllowedForIntentError` is a named export of `nxt-zap-client.ts` (`grep "export class ToolNotAllowedForIntentError" web/lib/automations/debtor-email/nxt-zap-client.ts` returns a hit)
    - `ZapierToolRow` type contains `allowed_for_intents: string[] | null`
    - `loadTool` SELECT contains the literal substring `allowed_for_intents`
    - `callNxtTool` first/second/third params are `tool_id, input, intent: string` in that order
    - Default-deny guard appears AFTER `loadTool` and BEFORE the `tool.pattern !==` check
    - All 5 tests in `nxt-zap-client-allowlist.test.ts` pass
    - `lookupSenderToAccount`, `lookupIdentifierToAccount`, `lookupCandidateDetails` each accept an `intent: string` parameter and forward it
  </acceptance_criteria>
  <done>nxt-zap-client.ts exports the error class, default-deny guard fires, allowlist test file is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Update callsites in resolve-debtor.ts and invoice-copy-handler.ts</name>
  <files>
    web/lib/automations/debtor-email/resolve-debtor.ts,
    web/lib/automations/debtor-email/invoice-copy-handler.ts
  </files>
  <read_first>
    - web/lib/automations/debtor-email/nxt-zap-client.ts (modified by Task 1 — confirm new signature)
    - web/lib/automations/debtor-email/resolve-debtor.ts (full file — find every call to lookupSenderToAccount / lookupIdentifierToAccount / lookupCandidateDetails / callNxtTool)
    - web/lib/automations/debtor-email/invoice-copy-handler.ts (full file — find every call to callNxtTool, especially nxt.invoice_fetch)
    - .planning/phases/64-stage-0-input-safety-per-run-budgets/64-CONTEXT.md (D-07 — intent identifier = swarm_categories.key)
    - supabase/migrations/20260430e_stage_0_safety_and_allowlist.sql (backfill values — defines which intents the existing tools accept)
    - .planning/phases/64-stage-0-input-safety-per-run-budgets/64-PATTERNS.md (line 250: search callsites with `Grep("callNxtTool|lookupSenderToAccount|lookupIdentifierToAccount|lookupCandidateDetails", type: "ts")`)
  </read_first>
  <action>
    Run `grep -rn "callNxtTool\|lookupSenderToAccount\|lookupIdentifierToAccount\|lookupCandidateDetails" web/lib/ web/app/` to enumerate ALL callsites.

    For each callsite, decide the intent string based on the surrounding context:

    **`web/lib/automations/debtor-email/resolve-debtor.ts`** — this module resolves debtor identity for unknown-bucket emails (label-resolver Stage 2). Intent for ALL `lookup*` calls here = **`"unknown"`** (the label-resolver is invoked when the regex classifier returned "unknown" — D-07 says intent = `swarm_categories.key`, and `unknown` is the canonical key for the unknown bucket).

    Pass `"unknown"` as the new last argument to every `lookupSenderToAccount` / `lookupIdentifierToAccount` / `lookupCandidateDetails` call. Example transformation:
    ```typescript
    // BEFORE
    const result = await lookupSenderToAccount({ sender_email });
    // AFTER
    const result = await lookupSenderToAccount({ sender_email }, "unknown");
    ```

    **`web/lib/automations/debtor-email/invoice-copy-handler.ts`** — this handler is invoked specifically for the `invoice_copy_request` intent. Pass `"invoice_copy_request"` to every `callNxtTool` invocation in this file (especially `nxt.invoice_fetch`).

    If the grep finds callsites in OTHER files (e.g., a triage worker or an API route), update those too — pick the intent based on the swarm_category that triggered the workflow. If unclear, propagate the intent from the surrounding event/function parameters rather than hardcoding (e.g., `await callNxtTool(tool_id, input, event.data.category_key)`).

    Confirm `cd web && npx tsc --noEmit` exits clean — every callsite must now compile against the new 3-arg signature.

    Run the existing debtor-email test suite to confirm no regressions: `cd web && npx vitest run lib/automations/debtor-email/__tests__/ --reporter=basic`.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -5; cd web && npx vitest run lib/automations/debtor-email/__tests__/ --reporter=basic 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports zero errors
    - Every grep hit for `callNxtTool|lookupSenderToAccount|lookupIdentifierToAccount|lookupCandidateDetails` in `web/` now passes 3 arguments (tool_id/input/intent or input/intent for convenience exports)
    - `resolve-debtor.ts` callsites pass `"unknown"` (or propagate `event.data.category_key` if the resolver is invoked from a context with a known category)
    - `invoice-copy-handler.ts` callsites pass `"invoice_copy_request"`
    - All existing tests in `web/lib/automations/debtor-email/__tests__/` pass
  </acceptance_criteria>
  <done>tsc clean, all existing debtor-email tests still GREEN, allowlist enforcement is wired end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Handler agent → Zapier tool registry | Untrusted intent claim crosses here. Default-deny (D-06) ensures unknown/missing allowlist denies. |
| Database `allowed_for_intents` column → in-process cache | 60s TTL cache; if column drifts (e.g., admin removes an intent), cache lag is at most 60s. Acceptable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-06 | Elevation of Privilege | A copy-document handler attempts to invoke a payment-update tool by passing `intent="invoice_copy_request"` to a tool whose allowlist does not include that intent | mitigate | Default-deny check inside `callNxtTool`: `ToolNotAllowedForIntentError` thrown before HTTP POST. Plan 01 test asserts this. |
| T-64-07 | Tampering | New tool registered without setting `allowed_for_intents` is silently invokable (Pitfall 2) | mitigate | NULL → empty array → throws. Test 1 in nxt-zap-client-allowlist.test.ts pins this. |
| T-64-08 | Spoofing | Caller passes a fake intent string (e.g., a typo `"invoice_copy_requests"` plural) and slips past | accept | Intent is a free-form string in this phase; D-07 says identifier = `swarm_categories.key` but we don't FK-enforce. Phase 68 may add `swarm_intents` registry. Documented as known limitation. |
</threat_model>

<verification>
1. `cd web && npx vitest run lib/automations/debtor-email/__tests__/nxt-zap-client-allowlist.test.ts --reporter=basic` — GREEN.
2. `cd web && npx tsc --noEmit` — zero errors.
3. `cd web && npx vitest run lib/automations/debtor-email/__tests__/ --reporter=basic` — full debtor-email suite GREEN (no regressions on existing callsites).
4. `grep -rn "callNxtTool(" web/lib/ web/app/ | grep -v "__tests__"` — every hit passes 3 arguments.
</verification>

<success_criteria>
- ToolNotAllowedForIntentError is exported and tested
- nxt-zap-client.ts default-deny guard fires for NULL / empty / non-matching intent
- All callsites compile against the new signature
- No regression in existing debtor-email tests
- Live `zapier_tools` rows already backfilled (Plan 01 Task 6) work end-to-end
</success_criteria>

<output>
Create `.planning/phases/64-stage-0-input-safety-per-run-budgets/64-03-SUMMARY.md` per `templates/summary.md`. Record:
- Number of callsites updated (output of `grep -rn` from Task 2)
- The intent string chosen for each modified file
- Confirmation that the live Supabase backfill matches the intent strings used in callers
</output>
