---
phase: 66-pipeline-consolidation-retire-triage-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/lib/inngest/functions/debtor-email-triage.ts
  - web/lib/inngest/functions/debtor-email-coordinator.ts
  - web/app/api/inngest/route.ts
  - web/lib/inngest/functions/__tests__/debtor-email-triage.test.ts
  - web/lib/inngest/functions/__tests__/debtor-email-coordinator.test.ts
autonomous: true
requirements:
  - CONS-02
must_haves:
  truths:
    - "File `web/lib/inngest/functions/debtor-email-coordinator.ts` exists; `debtor-email-triage.ts` does not"
    - "Exported const is `debtorEmailCoordinator`; `debtorEmailTriage` no longer exists"
    - "Inngest function id is `automations/debtor-email-coordinator`"
    - "`route.ts` registers `debtorEmailCoordinator` and no longer references `debtorEmailTriage`"
    - "Renamed test file `debtor-email-coordinator.test.ts` exists and passes under new import paths"
  artifacts:
    - path: web/lib/inngest/functions/debtor-email-coordinator.ts
      provides: "Renamed Stage 3 coordinator Inngest function"
      contains: 'id: "automations/debtor-email-coordinator"'
    - path: web/app/api/inngest/route.ts
      provides: "Inngest function registration"
      contains: "debtorEmailCoordinator"
    - path: web/lib/inngest/functions/__tests__/debtor-email-coordinator.test.ts
      provides: "Renamed coordinator unit test file"
  key_links:
    - from: web/app/api/inngest/route.ts
      to: web/lib/inngest/functions/debtor-email-coordinator.ts
      via: "import + serve({ functions: [...] }) entry"
      pattern: 'import\s*\{\s*debtorEmailCoordinator\s*\}'
---

<objective>
Mechanical Inngest function rename per `66-RESEARCH.md` § Inngest Rename Mechanics
and CONTEXT D-01: rename file, function id, exported const, registration site,
and the colocated test file. No behavioural change in this plan — the trigger
retarget lives in Plan 03. Internal imports of `@/lib/automations/debtor-email/triage/*`
remain untouched here; Plan 02 rewrites them when the directory moves.

Purpose: satisfy CONS-02's literal-rename surface so the static-audit grep in
Plan 05 returns 0 hits for `debtor-email-triage` / `debtorEmailTriage`.

Output: renamed source file, renamed test file, updated route registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-pipeline-consolidation-retire-triage-path/66-CONTEXT.md
@.planning/phases/66-pipeline-consolidation-retire-triage-path/66-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Rename file + function id + exported const</name>
  <files>web/lib/inngest/functions/debtor-email-triage.ts, web/lib/inngest/functions/debtor-email-coordinator.ts</files>
  <read_first>
    - web/lib/inngest/functions/debtor-email-triage.ts (entire file — must preserve `step.run`-wrapped non-deterministic id pattern from Phase 65 commit dd2583a, and the `(inngest.send as unknown as SendFn)({...})` pattern from commit dae6276)
    - 66-RESEARCH.md § Inngest Rename Mechanics
    - 66-CONTEXT.md D-01
  </read_first>
  <action>
    Use `git mv web/lib/inngest/functions/debtor-email-triage.ts web/lib/inngest/functions/debtor-email-coordinator.ts` (preserves history). Then in the new file:

    1. Change Inngest function id literal from `"automations/debtor-email-triage"` to `"automations/debtor-email-coordinator"`.
    2. Rename exported const `debtorEmailTriage` → `debtorEmailCoordinator`.
    3. DO NOT touch line 57 (`{ event: "debtor/email.received" }`) — Plan 03 retargets it.
    4. DO NOT touch any `from "@/lib/automations/debtor-email/triage/..."` imports — Plan 02 rewrites them.
    5. Preserve the `step.run`-wrapped run_id generation and the `inngest.send`-binding-safe call pattern verbatim. Do NOT introduce `const send = inngest.send`.
    6. If a JSDoc block-comment contains `*/` from a glob/cron/etc., flag — N/A here per CLAUDE.md but worth checking.
  </action>
  <verify>
    <automated>test ! -f web/lib/inngest/functions/debtor-email-triage.ts && test -f web/lib/inngest/functions/debtor-email-coordinator.ts && grep -q 'id: "automations/debtor-email-coordinator"' web/lib/inngest/functions/debtor-email-coordinator.ts && grep -q "export const debtorEmailCoordinator" web/lib/inngest/functions/debtor-email-coordinator.ts && ! grep -q "debtorEmailTriage\|debtor-email-triage" web/lib/inngest/functions/debtor-email-coordinator.ts</automated>
  </verify>
  <acceptance_criteria>
    - `web/lib/inngest/functions/debtor-email-triage.ts` does not exist
    - `web/lib/inngest/functions/debtor-email-coordinator.ts` exists
    - File contains exact string `id: "automations/debtor-email-coordinator"` (1 occurrence)
    - File contains `export const debtorEmailCoordinator` (1 occurrence)
    - File contains zero occurrences of `debtorEmailTriage` or `debtor-email-triage`
    - File still contains `{ event: "debtor/email.received" }` on line 57 (untouched — Plan 03 changes it)
    - File still contains `from "@/lib/automations/debtor-email/triage/` imports (untouched — Plan 02 changes them)
  </acceptance_criteria>
  <done>git mv produced a rename diff; function id, const name updated; behavioural code preserved verbatim</done>
</task>

<task type="auto">
  <name>Task 1.2: Update route.ts registration</name>
  <files>web/app/api/inngest/route.ts</files>
  <read_first>
    - web/app/api/inngest/route.ts (entire file — confirm only line 26 import + line 57 array entry need updating)
    - 66-RESEARCH.md § Inngest Rename Mechanics point 5
    - 66-CONTEXT.md D-07
  </read_first>
  <action>
    1. Change line 26 import: `import { debtorEmailTriage } from "@/lib/inngest/functions/debtor-email-triage";` → `import { debtorEmailCoordinator } from "@/lib/inngest/functions/debtor-email-coordinator";`
    2. Change line 57 array entry: `debtorEmailTriage` → `debtorEmailCoordinator`.
    3. Confirm by re-reading: file now references `debtorEmailCoordinator` exactly twice (1 import + 1 array entry) and zero `debtorEmailTriage`.
  </action>
  <verify>
    <automated>! grep -q "debtorEmailTriage\|debtor-email-triage" web/app/api/inngest/route.ts && [ "$(grep -c 'debtorEmailCoordinator' web/app/api/inngest/route.ts)" -ge 2 ]</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `debtorEmailTriage` or `debtor-email-triage` in `route.ts`
    - At least 2 occurrences of `debtorEmailCoordinator` (import + array entry)
    - Import path is `@/lib/inngest/functions/debtor-email-coordinator`
  </acceptance_criteria>
  <done>route.ts registers the renamed function; old name fully gone from this file</done>
</task>

<task type="auto">
  <name>Task 1.3: Rename test file</name>
  <files>web/lib/inngest/functions/__tests__/debtor-email-triage.test.ts, web/lib/inngest/functions/__tests__/debtor-email-coordinator.test.ts</files>
  <read_first>
    - web/lib/inngest/functions/__tests__/debtor-email-triage.test.ts (entire file — see 66-RESEARCH.md § Triage Directory Inventory item 8: lines 9, 75, 83 reference triage paths/names)
    - 66-RESEARCH.md § Triage Directory Inventory (import-rewrite scope point 8)
  </read_first>
  <action>
    1. `git mv web/lib/inngest/functions/__tests__/debtor-email-triage.test.ts web/lib/inngest/functions/__tests__/debtor-email-coordinator.test.ts`.
    2. Inside the renamed test file, update:
       - The import of the function under test: `from "../debtor-email-triage"` → `from "../debtor-email-coordinator"`; `debtorEmailTriage` import → `debtorEmailCoordinator`.
       - The `describe(...)` / test labels referencing "triage" → "coordinator".
       - **Keep** the `inngest.send({ name: "debtor/email.received", ... })` synthetic emit at line ~104 untouched in this plan — Plan 03 will retarget it to `"debtor-email/coordinator.requested"`. (Test will be temporarily emitting an event nothing subscribes to after Plan 03's events.ts deletion; Plan 03's task list updates this test in lock-step.)
    3. DO NOT touch imports of `@/lib/automations/debtor-email/triage/*` here — Plan 02 rewrites the directory move's import surface across all test files.
  </action>
  <verify>
    <automated>test ! -f web/lib/inngest/functions/__tests__/debtor-email-triage.test.ts && test -f web/lib/inngest/functions/__tests__/debtor-email-coordinator.test.ts && grep -q "debtorEmailCoordinator" web/lib/inngest/functions/__tests__/debtor-email-coordinator.test.ts && ! grep -q "debtorEmailTriage" web/lib/inngest/functions/__tests__/debtor-email-coordinator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Old test file path does not exist
    - New test file path exists
    - Test file imports `debtorEmailCoordinator` from `../debtor-email-coordinator`
    - Zero `debtorEmailTriage` references in renamed test file
    - File still contains `debtor/email.received` (Plan 03 changes this)
    - File may still contain `triage/*` imports (Plan 02 rewrites them)
  </acceptance_criteria>
  <done>Test file renamed; symbol-level references inside use new const + import path</done>
</task>

</tasks>

<verification>
At end of plan: `cd web && pnpm exec tsc --noEmit` should NOT yet pass cleanly because Plan 02 hasn't moved triage/* yet — but the rename surface itself is mechanically consistent. Plan 02 + Plan 03 close the type loop. Confirm via:

```bash
grep -rn "debtor-email-triage\|debtorEmailTriage" web/lib/inngest/ --include="*.ts" | grep -v ".next/"
```
Expected output: only matches inside the renamed file's own `triage/` subpath imports (e.g. `from "@/lib/automations/debtor-email/triage/agent-runs"`) — those die in Plan 02. Zero matches for the function-name literals.
</verification>

<success_criteria>
- File rename complete (git mv preserves history)
- Function id, exported const, route.ts registration updated to `coordinator` form
- Test file renamed; symbol references inside updated
- Trigger event line 57 untouched (Plan 03)
- `triage/*` import paths untouched (Plan 02)
</success_criteria>

<output>
Write `.planning/phases/66-pipeline-consolidation-retire-triage-path/66-01-SUMMARY.md` with the renamed-file diff summary, the route.ts before/after, and a note that tsc is intentionally still red until Plan 02 lands.
</output>
