---
phase: 66-pipeline-consolidation-retire-triage-path
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - web/lib/automations/debtor-email/triage/agent-runs.ts
  - web/lib/automations/debtor-email/triage/invoke-intent.ts
  - web/lib/automations/debtor-email/triage/types.ts
  - web/lib/automations/debtor-email/triage/detect-emotion.ts
  - web/lib/automations/debtor-email/triage/circuit-breaker.ts
  - web/lib/automations/debtor-email/triage/invoke-body.ts
  - web/lib/automations/debtor-email/coordinator/agent-runs.ts
  - web/lib/automations/debtor-email/coordinator/invoke-intent.ts
  - web/lib/automations/debtor-email/coordinator/types.ts
  - web/lib/automations/debtor-email/coordinator/detect-emotion.ts
  - web/lib/inngest/functions/debtor-email-coordinator.ts
  - web/lib/inngest/functions/classifier-invoice-copy-handler.ts
  - web/lib/automations/debtor-email/coordinator/orchestrator-types.ts
  - web/lib/automations/debtor-email/handlers/output-adapter.ts
  - web/lib/automations/debtor-email/handlers/__tests__/output-adapter.test.ts
  - web/tests/labeling/classifier-invoice-copy-handler.test.ts
  - web/lib/inngest/functions/__tests__/classifier-invoice-copy-handler.test.ts
  - web/lib/inngest/functions/__tests__/debtor-email-coordinator.test.ts
  - web/lib/automations/debtor-email/triage/__tests__/
  - web/lib/automations/debtor-email/coordinator/__tests__/
autonomous: true
requirements:
  - CONS-02
must_haves:
  truths:
    - "`web/lib/automations/debtor-email/triage/` directory does not exist"
    - "Four moved files live under `web/lib/automations/debtor-email/coordinator/`"
    - "`circuit-breaker.ts` and `invoke-body.ts` are deleted (zero importers verified)"
    - "All 8 external import sites rewrite from `triage/*` to `coordinator/*`"
    - "Vitest suite passes (no broken imports)"
  artifacts:
    - path: web/lib/automations/debtor-email/coordinator/agent-runs.ts
      provides: "Moved from triage/agent-runs.ts"
    - path: web/lib/automations/debtor-email/coordinator/invoke-intent.ts
      provides: "Moved from triage/invoke-intent.ts"
    - path: web/lib/automations/debtor-email/coordinator/types.ts
      provides: "Moved from triage/types.ts (4 external importers updated)"
    - path: web/lib/automations/debtor-email/coordinator/detect-emotion.ts
      provides: "Moved from triage/detect-emotion.ts"
  key_links:
    - from: web/lib/inngest/functions/debtor-email-coordinator.ts
      to: web/lib/automations/debtor-email/coordinator/*
      via: "import paths"
      pattern: 'from "@/lib/automations/debtor-email/coordinator/'
---

<objective>
Move the 4 live `triage/*` helpers into the existing `coordinator/` directory,
delete the 2 dead files, and rewrite the 8 external import sites enumerated in
`66-RESEARCH.md` § Triage Directory Inventory. After this plan: `triage/`
directory is gone; tsc passes; vitest passes; the only behavioural change still
pending is Plan 03's trigger retarget.

Purpose: close the directory-rename half of CONS-02. Verifies via tsc + vitest
+ the static-audit grep in Plan 05.

Output: 4 moved files, 2 deleted files, 8 external import sites rewritten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-pipeline-consolidation-retire-triage-path/66-CONTEXT.md
@.planning/phases/66-pipeline-consolidation-retire-triage-path/66-RESEARCH.md
@.planning/phases/66-pipeline-consolidation-retire-triage-path/66-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Verify dead-file claims, then move 4 live helpers + delete 2 dead helpers</name>
  <files>web/lib/automations/debtor-email/triage/*.ts, web/lib/automations/debtor-email/coordinator/*.ts</files>
  <read_first>
    - 66-RESEARCH.md § Triage Directory Inventory (the per-file table — confirms importers and MOVE/DELETE action per file)
    - 66-CONTEXT.md D-02 + D-06
    - web/lib/automations/debtor-email/triage/circuit-breaker.ts (full file — must verify zero callers before deleting)
    - web/lib/automations/debtor-email/triage/invoke-body.ts (full file — must verify zero callers before deleting)
  </read_first>
  <action>
    Step 1 — verify the DELETE claims with two independent greps (must each return ONLY the file itself):
    ```bash
    grep -rn "circuit-breaker\|circuitBreaker" web/ --include="*.ts" --include="*.tsx" | grep -v ".next/" | grep -v "triage/circuit-breaker.ts"
    grep -rn "invokeBodyAgent\|invoke-body" web/ --include="*.ts" --include="*.tsx" | grep -v ".next/" | grep -v "triage/invoke-body.ts"
    ```
    BOTH MUST RETURN ZERO LINES. If either returns hits, STOP and surface to the user — research's claim is stale and scope expands.

    Step 2 — `git mv` the 4 live files (preserves history):
    ```
    git mv web/lib/automations/debtor-email/triage/agent-runs.ts      web/lib/automations/debtor-email/coordinator/agent-runs.ts
    git mv web/lib/automations/debtor-email/triage/invoke-intent.ts   web/lib/automations/debtor-email/coordinator/invoke-intent.ts
    git mv web/lib/automations/debtor-email/triage/types.ts           web/lib/automations/debtor-email/coordinator/types.ts
    git mv web/lib/automations/debtor-email/triage/detect-emotion.ts  web/lib/automations/debtor-email/coordinator/detect-emotion.ts
    ```

    Step 3 — delete the 2 dead files with `git rm`:
    ```
    git rm web/lib/automations/debtor-email/triage/circuit-breaker.ts
    git rm web/lib/automations/debtor-email/triage/invoke-body.ts
    ```

    Step 4 — handle `triage/__tests__/`. For each test file under that directory, decide based on its target:
    - Test exercises one of the moved files → `git mv` to `coordinator/__tests__/<same-filename>` and update its `from "../<x>"` imports to point at the moved source (relative path remains the same since both source and test moved).
    - Test exercises one of the deleted files (`circuit-breaker.ts`, `invoke-body.ts`) → `git rm` the test (no source to test).

    Step 5 — confirm the `triage/` directory is empty and remove it: `rmdir web/lib/automations/debtor-email/triage/__tests__ 2>/dev/null; rmdir web/lib/automations/debtor-email/triage 2>/dev/null` (may be a no-op if git already removed it).

    Step 6 — DO NOT yet update the 8 external import sites — Task 2.2 handles that.
  </action>
  <verify>
    <automated>test ! -d web/lib/automations/debtor-email/triage && test -f web/lib/automations/debtor-email/coordinator/agent-runs.ts && test -f web/lib/automations/debtor-email/coordinator/invoke-intent.ts && test -f web/lib/automations/debtor-email/coordinator/types.ts && test -f web/lib/automations/debtor-email/coordinator/detect-emotion.ts</automated>
  </verify>
  <acceptance_criteria>
    - Pre-move: `grep -rn "circuit-breaker\|circuitBreaker" web/ --include="*.ts" --include="*.tsx" | grep -v ".next/" | grep -v "triage/circuit-breaker.ts"` returns 0 lines
    - Pre-move: `grep -rn "invokeBodyAgent\|invoke-body" web/ --include="*.ts" --include="*.tsx" | grep -v ".next/" | grep -v "triage/invoke-body.ts"` returns 0 lines
    - Post-move: `web/lib/automations/debtor-email/triage/` directory does not exist
    - Post-move: 4 files exist under `coordinator/` (agent-runs.ts, invoke-intent.ts, types.ts, detect-emotion.ts)
    - Post-move: `circuit-breaker.ts` and `invoke-body.ts` do not exist anywhere under `web/lib/automations/debtor-email/`
    - No name collisions in `coordinator/` (existing files: coordinator-complete.ts, escalation-gate.ts, orchestrator-types.ts, synthesis-types.ts — none clash)
  </acceptance_criteria>
  <done>4 files moved, 2 files deleted, triage/ dir gone, tests rehomed accordingly. tsc still red until Task 2.2.</done>
</task>

<task type="auto">
  <name>Task 2.2: Rewrite the 8 external import sites</name>
  <files>web/lib/inngest/functions/debtor-email-coordinator.ts, web/lib/inngest/functions/classifier-invoice-copy-handler.ts, web/lib/automations/debtor-email/coordinator/orchestrator-types.ts, web/lib/automations/debtor-email/handlers/output-adapter.ts, web/lib/automations/debtor-email/handlers/__tests__/output-adapter.test.ts, web/tests/labeling/classifier-invoice-copy-handler.test.ts, web/lib/inngest/functions/__tests__/classifier-invoice-copy-handler.test.ts, web/lib/inngest/functions/__tests__/debtor-email-coordinator.test.ts</files>
  <read_first>
    - 66-RESEARCH.md § Triage Directory Inventory (import-rewrite scope outside the triage/ dir — the numbered 1-8 list with line numbers)
  </read_first>
  <action>
    Per `66-RESEARCH.md` § Triage Directory Inventory § Import-rewrite scope outside the triage/ dir, perform the following 8 mechanical sed-equivalents. In every case the change is `@/lib/automations/debtor-email/triage/` → `@/lib/automations/debtor-email/coordinator/` (for absolute imports) or `../../triage/` → `../../coordinator/` (for relative imports inside test files).

    1. `web/lib/inngest/functions/debtor-email-coordinator.ts` — research lists 3 import lines (lines 21, 22-27, 28-31 in the original file). After rename: change every `@/lib/automations/debtor-email/triage/` → `@/lib/automations/debtor-email/coordinator/`.
    2. `web/lib/inngest/functions/classifier-invoice-copy-handler.ts` — lines 31, 37: `triage/detect-emotion` → `coordinator/detect-emotion`; `triage/types` → `coordinator/types`.
    3. `web/lib/automations/debtor-email/coordinator/orchestrator-types.ts` — line 3: `from "../triage/types"` → `from "./types"` (since the file is now in the same directory as types.ts).
    4. `web/lib/automations/debtor-email/handlers/output-adapter.ts` — line 7: `from "../triage/types"` → `from "../coordinator/types"`.
    5. `web/lib/automations/debtor-email/handlers/__tests__/output-adapter.test.ts` — line 7: same as #4.
    6. `web/tests/labeling/classifier-invoice-copy-handler.test.ts` — line 98 (`vi.mock` path): `@/lib/automations/debtor-email/triage/detect-emotion` → `@/lib/automations/debtor-email/coordinator/detect-emotion`.
    7. `web/lib/inngest/functions/__tests__/classifier-invoice-copy-handler.test.ts` — line 31 (`vi.mock` path): same kind of rewrite to `coordinator/...`.
    8. `web/lib/inngest/functions/__tests__/debtor-email-coordinator.test.ts` (renamed in Plan 01 Task 1.3) — lines 9, 75, 83: every `triage/*` import path → `coordinator/*` equivalent.

    After all 8 sites are rewritten, run `cd web && pnpm exec tsc --noEmit` and `cd web && pnpm exec vitest run`. Both MUST pass. Failures here = a missed import site → fix before claiming done.
  </action>
  <verify>
    <automated>! grep -rn "@/lib/automations/debtor-email/triage" web/ --include="*.ts" --include="*.tsx" | grep -v ".next/" && cd web && pnpm exec tsc --noEmit && pnpm exec vitest run --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "@/lib/automations/debtor-email/triage" web/ --include="*.ts" --include="*.tsx" | grep -v ".next/"` returns 0 lines
    - `grep -rn '"\.\./triage/' web/ --include="*.ts"` returns 0 lines (relative-path leftovers)
    - `cd web && pnpm exec tsc --noEmit` exits 0
    - `cd web && pnpm exec vitest run` exits 0 (entire test suite green)
    - Each of the 8 enumerated files has been touched at the expected line(s) and now references `coordinator/`
  </acceptance_criteria>
  <done>All 8 import sites point at `coordinator/`; tsc green; vitest green</done>
</task>

</tasks>

<verification>
```bash
cd web && pnpm exec tsc --noEmit
cd web && pnpm exec vitest run
grep -rn "triage" web/lib/automations/debtor-email/ --include="*.ts" | grep -v ".next/"
# Expected from the last grep: 0 lines.
```
</verification>

<success_criteria>
- triage/ directory gone
- 4 helpers live under coordinator/
- 2 dead files deleted (with zero-importer proof captured in commit message or summary)
- 8 import sites rewritten
- tsc + vitest green
</success_criteria>

<output>
Write `.planning/phases/66-pipeline-consolidation-retire-triage-path/66-02-SUMMARY.md` with: pre-move grep evidence for the 2 deletions, the per-file move log, and the post-rewrite test-suite outcome.
</output>
