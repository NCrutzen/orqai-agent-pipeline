---
phase: 66-pipeline-consolidation-retire-triage-path
plan: 04
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - docs/debtor-email-pipeline-architecture.md
  - docs/agentic-pipeline/stage-3-coordinator.md
autonomous: true
requirements:
  - CONS-03
must_haves:
  truths:
    - "No Inngest function file imports another Inngest function file (cross-handler invocation ban)"
    - "Every `debtor-email/<intent>.requested` emit site is in the allowlist (`debtor-email-coordinator.ts`, `coordinator-orchestrator.ts`, `coordinator-complete.ts`)"
    - "Both canonical docs describe the Stage 3 coordinator with current terminology (no description-by-old-function-id)"
  artifacts:
    - path: docs/debtor-email-pipeline-architecture.md
      provides: "Reviewed end-to-end; references to legacy function description (if any) replaced with coordinator naming. Human-triage references at lines 411, 413 remain untouched."
    - path: docs/agentic-pipeline/stage-3-coordinator.md
      provides: "Reviewed end-to-end; uses canonical 'coordinator' naming throughout"
  key_links:
    - from: "audit allowlist"
      to: "every <intent>.requested emit site in web/lib/inngest/"
      via: "manual checklist in summary"
      pattern: 'name: "debtor-email/[a-z-]+\.requested"'
---

<objective>
CONS-03 lock + D-10 doc reconciliation. Per `66-RESEARCH.md` § Stage 4 Handler
Invocation Audit, CONS-03 is structurally satisfied today (zero cross-handler
imports). This plan locks the invariant via a documented manual audit and
sweeps the canonical docs for any remaining "triage" reference-by-description.

Runs in Wave 3 in parallel with Plan 03 — touches different files entirely
(docs only; no source code).

Purpose: prove CONS-03 is satisfied AND will stay satisfied (audit checklist
becomes part of the SUMMARY); reconcile docs so future readers don't see
"triage" used to mean the Inngest function.

Output: 2 doc files reviewed (and updated if any reference-by-description is
found); written allowlist of `<intent>.requested` emit sites in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-pipeline-consolidation-retire-triage-path/66-CONTEXT.md
@.planning/phases/66-pipeline-consolidation-retire-triage-path/66-RESEARCH.md
@.planning/phases/66-pipeline-consolidation-retire-triage-path/66-02-SUMMARY.md
@docs/agentic-pipeline/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: CONS-03 cross-handler import audit + emit-site allowlist</name>
  <files>(verification-only — no source modifications expected)</files>
  <read_first>
    - 66-RESEARCH.md § Stage 4 Handler Invocation Audit (the methodology + the allowlist of legitimate emitters)
    - 66-CONTEXT.md D-05
  </read_first>
  <action>
    Run two grep audits and document each result. ANY non-empty output for the cross-import grep is a CONS-03 regression and STOPS this plan.

    Audit 1 — cross-handler import ban:
    ```bash
    grep -rn 'from "@/lib/inngest/functions/' web/lib/inngest/functions/ --include="*.ts" | grep -v __tests__
    ```
    Expected: 0 lines. Capture the literal command + output into the SUMMARY.

    Audit 2 — emit-site allowlist for `debtor-email/<intent>.requested`:
    ```bash
    grep -rn 'debtor-email/[a-z-]*\.requested' web/lib/inngest/functions/ --include="*.ts" | grep -v __tests__ | grep -v "// " | grep -E 'name:|inngest\.send|step\.run'
    ```
    Capture each emit site. Every line in output MUST originate from one of:
    - `web/lib/inngest/functions/debtor-email-coordinator.ts` (single-shot dispatch via registry, ~lines 217-228 in the pre-rename file)
    - `web/lib/inngest/functions/coordinator-orchestrator.ts` (template-literal fan-out, lines 90-108)
    - `web/lib/inngest/functions/coordinator-complete.ts` (synthesis trigger, line 56)
    - `web/lib/inngest/functions/classifier-label-resolver.ts` (NEW — Plan 03 added the `debtor-email/coordinator.requested` emit; this is `<intent>.requested`-shaped but not a Stage 4 handler invocation; document explicitly in the SUMMARY)

    Any line outside this allowlist = a regression. STOP and surface to user.

    Document both audits in `66-04-SUMMARY.md` with:
    - The exact command run
    - The exact output (or empty-output marker)
    - For Audit 2: a per-line classification linking each match to the allowlist entry above
  </action>
  <verify>
    <automated>[ "$(grep -rn 'from "@/lib/inngest/functions/' web/lib/inngest/functions/ --include="*.ts" | grep -v __tests__ | wc -l)" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - Audit 1: zero lines of cross-handler imports between Inngest function files (excluding tests)
    - Audit 2: every emit site appears in the documented allowlist; SUMMARY contains a per-line classification
  </acceptance_criteria>
  <done>CONS-03 audit captured in SUMMARY with command output and allowlist mapping</done>
</task>

<task type="auto">
  <name>Task 4.2: Doc reconciliation — debtor-email-pipeline-architecture.md</name>
  <files>docs/debtor-email-pipeline-architecture.md</files>
  <read_first>
    - docs/debtor-email-pipeline-architecture.md (entire file — end-to-end read)
    - 66-RESEARCH.md § Doc Update Inventory (per-line grep results + the "leave human triage references alone" note for lines 411, 413)
    - 66-CONTEXT.md D-10
  </read_first>
  <action>
    Read the file end-to-end. Look for:
    1. Any reference to the Inngest function by description (e.g. "the triage function", "debtor-email-triage Inngest function") — replace with "the Stage 3 coordinator" / "`debtor-email-coordinator` Inngest function".
    2. The two existing matches for "triage" at lines 411 and 413 — these refer to HUMAN operator triage. **DO NOT modify them**; verify their context still reads as human-action verb ("operator must triage", "unresolved-needing-triage" is a workflow label).
    3. Any reference to `debtor/email.received` (the deleted event) — replace with `debtor-email/coordinator.requested` and reconcile the Stage 2 → Stage 3 narrative to match Plan 03's wiring.
    4. Any reference to the `triage/` directory path — replace with `coordinator/`.

    If the read confirms the doc is already current (research said grep found 0 mechanical-rename matches), document that explicitly in the SUMMARY: "End-to-end read of `docs/debtor-email-pipeline-architecture.md` 2026-05-XX — no edits needed; lines 411, 413 left as-is per D-10 (human-triage workflow language, not the Inngest function)."

    Edit only what's actually wrong. Surgical changes; do not rewrite paragraphs.
  </action>
  <verify>
    <automated>! grep -n "debtor-email-triage\|debtorEmailTriage\|debtor/email.received" docs/debtor-email-pipeline-architecture.md && grep -q "triage" docs/debtor-email-pipeline-architecture.md</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `debtor-email-triage`, `debtorEmailTriage`, or `debtor/email.received` in this doc
    - The two existing "triage" matches (lines 411, 413 — human-operator workflow) remain (the `grep -q "triage"` check confirms human-triage references survive untouched)
    - Any reference to `triage/` directory path replaced with `coordinator/` (or absent)
    - End-to-end read recorded in SUMMARY (date + reviewer = executor)
  </acceptance_criteria>
  <done>Doc reconciled or confirmed-clean; human-triage references preserved; Inngest-function references updated if found</done>
</task>

<task type="auto">
  <name>Task 4.3: Doc reconciliation — stage-3-coordinator.md</name>
  <files>docs/agentic-pipeline/stage-3-coordinator.md</files>
  <read_first>
    - docs/agentic-pipeline/stage-3-coordinator.md (entire file — end-to-end read)
    - 66-RESEARCH.md § Doc Update Inventory
  </read_first>
  <action>
    Read end-to-end. Verify:
    1. The file uses "coordinator" terminology consistently (research confirmed grep found 0 "triage" matches in this file).
    2. The Stage 2 → Stage 3 trigger event mentioned (if any) is `debtor-email/coordinator.requested`, not `debtor/email.received`. If old event is mentioned, update.
    3. The function id (if mentioned) is `automations/debtor-email-coordinator`.

    If everything is already correct, record this in the SUMMARY: "End-to-end read of `docs/agentic-pipeline/stage-3-coordinator.md` 2026-05-XX — no edits needed."
  </action>
  <verify>
    <automated>! grep -n "debtor-email-triage\|debtorEmailTriage\|debtor/email.received\|automations/debtor-email-triage" docs/agentic-pipeline/stage-3-coordinator.md</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `debtor-email-triage`, `debtorEmailTriage`, `debtor/email.received`, `automations/debtor-email-triage` in this doc
    - End-to-end read recorded in SUMMARY
  </acceptance_criteria>
  <done>Doc confirmed using current Phase 65/66 terminology</done>
</task>

</tasks>

<verification>
```bash
# Cross-handler import ban
! grep -rn 'from "@/lib/inngest/functions/' web/lib/inngest/functions/ --include="*.ts" | grep -v __tests__
# Doc cleanliness
! grep -rn "debtor-email-triage\|debtorEmailTriage" docs/ --include="*.md" | grep -v ".planning/"
# Old event purge
! grep -rn "debtor/email.received" docs/ --include="*.md" | grep -v ".planning/"
```
All three should return zero lines.
</verification>

<success_criteria>
- CONS-03 audit recorded with command output + allowlist mapping
- Both canonical docs reviewed; edits applied where needed; human-triage references preserved
- SUMMARY captures the per-line allowlist classification
</success_criteria>

<output>
Write `.planning/phases/66-pipeline-consolidation-retire-triage-path/66-04-SUMMARY.md` with:
- Audit 1 command + output
- Audit 2 command + output + per-line allowlist classification
- Doc-review notes for both files (date + diff or "no edits needed" attestation)
</output>
