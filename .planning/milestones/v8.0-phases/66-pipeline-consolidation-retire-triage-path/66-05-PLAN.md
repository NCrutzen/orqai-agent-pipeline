---
phase: 66-pipeline-consolidation-retire-triage-path
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - .planning/phases/66-pipeline-consolidation-retire-triage-path/66-regression-report.md
autonomous: false
requirements:
  - CONS-01
  - CONS-02
  - CONS-03
must_haves:
  truths:
    - "Static-audit grep block returns zero hits for old name + zero cross-imports + zero old-event references + new-event present"
    - "Vercel-preview synthetic emit covers 4 regression paths"
    - "SQL queries against Supabase confirm exactly one coordinator_runs row per unknown-bucket smoke email and zero duplicates"
    - "`66-regression-report.md` is filled in with deploy URL, email_ids, query results, and screenshots"
  artifacts:
    - path: .planning/phases/66-pipeline-consolidation-retire-triage-path/66-regression-report.md
      provides: "Filled-in regression report with all acceptance gates checked"
  key_links:
    - from: "Vercel preview deploy"
      to: "Inngest dashboard preview env"
      via: "synthetic emit of debtor-email/coordinator.requested"
---

<objective>
Phase 66 verification gate: combine the static-audit grep block (CONS-01/02/03)
with a Vercel-preview synthetic-emit live smoke covering the 4 regression
paths from `66-VALIDATION.md`. This is the substitute for full-chain live
acceptance, since Stage 1 worker is missing in production (deferred per
CONTEXT.md `<deferred>` addendum).

Purpose: prove the rename + retarget did not regress production behavior;
fill in `66-regression-report.md` with the evidence.

Output: filled regression report; static-audit pass log; Inngest-dashboard
screenshots showing the renamed function id present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-pipeline-consolidation-retire-triage-path/66-CONTEXT.md
@.planning/phases/66-pipeline-consolidation-retire-triage-path/66-RESEARCH.md
@.planning/phases/66-pipeline-consolidation-retire-triage-path/66-VALIDATION.md
@.planning/phases/66-pipeline-consolidation-retire-triage-path/66-regression-report.md
@.planning/phases/66-pipeline-consolidation-retire-triage-path/66-01-SUMMARY.md
@.planning/phases/66-pipeline-consolidation-retire-triage-path/66-02-SUMMARY.md
@.planning/phases/66-pipeline-consolidation-retire-triage-path/66-03-SUMMARY.md
@.planning/phases/66-pipeline-consolidation-retire-triage-path/66-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 5.1: Static-audit grep block</name>
  <files>(verification only)</files>
  <read_first>
    - 66-VALIDATION.md § Wave 0 Requirements (the 4 grep commands)
    - 66-RESEARCH.md § Verification Scaffolding § Static audit queries
  </read_first>
  <action>
    Run the 4 static-audit commands from `66-VALIDATION.md` and capture each result. ALL FOUR must return 0 lines (or, for command 4, ≥3 lines for the new event).

    ```bash
    # 1. Zero hits for old name in app code (CONS-02 lock).
    grep -rn "debtor-email-triage\|debtorEmailTriage\|inngest:debtor-email-triage" web/ docs/ --include="*.ts" --include="*.tsx" --include="*.md" \
      | grep -v ".next/" | grep -v ".planning/"
    # Expected: 0 lines.

    # 2. Zero cross-imports between Inngest function files (CONS-03 lock).
    grep -rn 'from "@/lib/inngest/functions/' web/lib/inngest/functions/ --include="*.ts" \
      | grep -v __tests__
    # Expected: 0 lines.

    # 3. Zero references to the deleted event (D-03 retarget lock).
    grep -rn "debtor/email.received" web/ --include="*.ts" --include="*.tsx" \
      | grep -v ".next/"
    # Expected: 0 lines.

    # 4. New event present and consumed.
    grep -rn "debtor-email/coordinator.requested" web/ --include="*.ts" \
      | grep -v ".next/"
    # Expected: ≥3 lines (events.ts + coordinator subscription + label-resolver emit + tests).
    ```

    Paste each command's output (or empty marker) into the regression report under § "Static Audit Results".

    Also run the suite once more end-to-end: `cd web && pnpm exec tsc --noEmit && pnpm exec vitest run`. Both must exit 0.
  </action>
  <verify>
    <automated>[ "$(grep -rn "debtor-email-triage\|debtorEmailTriage\|inngest:debtor-email-triage" web/ docs/ --include="*.ts" --include="*.tsx" --include="*.md" | grep -v ".next/" | grep -v ".planning/" | wc -l)" -eq 0 ] && [ "$(grep -rn 'from "@/lib/inngest/functions/' web/lib/inngest/functions/ --include="*.ts" | grep -v __tests__ | wc -l)" -eq 0 ] && [ "$(grep -rn 'debtor/email.received' web/ --include="*.ts" --include="*.tsx" | grep -v ".next/" | wc -l)" -eq 0 ] && [ "$(grep -rn 'debtor-email/coordinator.requested' web/ --include="*.ts" | grep -v ".next/" | wc -l)" -ge 3 ] && cd web && pnpm exec tsc --noEmit && pnpm exec vitest run --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Command 1 output: 0 lines
    - Command 2 output: 0 lines
    - Command 3 output: 0 lines
    - Command 4 output: ≥3 lines (events.ts + coordinator + label-resolver, possibly + tests)
    - tsc passes
    - vitest passes
    - All 4 outputs pasted into `66-regression-report.md` § Static Audit Results
  </acceptance_criteria>
  <done>Static audit captured in regression report; all gates pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5.2: Live smoke against Vercel preview</name>
  <read_first>
    - 66-regression-report.md (the template — fill in the deploy URL + email_ids + path-by-path results)
    - 66-RESEARCH.md § Verification Scaffolding § Phase 66 live-smoke spec
    - 66-VALIDATION.md § Manual-Only Verifications
    - 66-CONTEXT.md `<deferred>` (note that Stage 1 gap means we test Stage 2→3→4 only, via synthetic emit of `debtor-email/coordinator.requested`)
  </read_first>
  <what-built>
    Phase 66 has landed in a Vercel preview deploy:
    - File rename + dir move + dead-file deletion (Plans 01-02)
    - Trigger retarget: coordinator subscribes to `debtor-email/coordinator.requested`; label-resolver emits it after Stage 2 closes (Plan 03)
    - CONS-03 audit + doc reconciliation (Plan 04)
    - Static-audit grep block green (Task 5.1)
  </what-built>
  <how-to-verify>
    1. Confirm Vercel preview deploy URL for the Phase 66 PR is healthy. Record URL + deploy timestamp in `66-regression-report.md`.
    2. Open the Inngest dashboard for the preview environment. Verify:
       - Function `automations/debtor-email-coordinator` is present and registered.
       - Function `automations/debtor-email-triage` is ABSENT (or visibly inactive — old runs may still show in history).
    3. From the Inngest dashboard, manually emit `debtor-email/coordinator.requested` 4 times — once for each regression path in `66-regression-report.md` § "4 Synthetic-Emit Regression Paths". Use prepared fixture email_ids that match the path semantics.
       - Path 1 (auto-reply): expect intent agent classifies as auto-reply intent OR escalation gate routes to handler-less fast-archive.
       - Path 2 (OOO temporary): same as Path 1.
       - Path 3 (payment admittance): expect intent agent classifies as `payment_admittance`-equivalent intent.
       - Path 4 (unknown → coordinator): the canonical CONS-01 acceptance — full Stage 2→3→4 chain.
       - **Note on scope:** Paths 1-3 in production normally never reach Stage 3 (regex catches them at Stage 1). Synthetically emitting `debtor-email/coordinator.requested` for them is a sanity check that the coordinator does not misbehave on edge inputs. The real CONS-01 evidence is Path 4.
    4. Run the 4 SQL queries from `66-regression-report.md` § "SQL Verification Queries" against Supabase. Paste results.
    5. Capture screenshots of the Inngest dashboard showing both function ids and at least one successful coordinator run; embed paths in the regression report.
    6. Tick all boxes under § "Acceptance Gate" in the regression report.
  </how-to-verify>
  <resume-signal>Type "approved — regression report filled" or describe issues found.</resume-signal>
</task>

</tasks>

<verification>
At end of plan:
- `66-regression-report.md` has all checkboxes ticked under "Acceptance Gate"
- Static-audit grep block paste-in section is filled
- 4 synthetic-emit results documented
- 4 SQL query outputs pasted
- Inngest dashboard screenshot path(s) referenced
</verification>

<success_criteria>
- CONS-01 verified via Path 4 synthetic-emit (Stage 1 gap acknowledged)
- CONS-02 verified via static-audit command 1 (0 hits)
- CONS-03 verified via static-audit command 2 (0 cross-imports)
- D-03 retarget verified via commands 3 + 4 (old gone, new present)
- Inngest dashboard confirms function rename took effect on the preview env
</success_criteria>

<output>
Write `.planning/phases/66-pipeline-consolidation-retire-triage-path/66-05-SUMMARY.md` with:
- Static-audit command outputs
- Live-smoke results per path
- SQL query results
- Acceptance Gate status (all boxes ticked = phase passes; any unticked = gap-closure phase needed)
- Reference to the filled `66-regression-report.md`
- Phase-level VERIFICATION.md outline (CONS-01/02/03 status mapped to evidence)
</output>
