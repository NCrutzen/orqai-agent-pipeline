---
phase: 56.7
plan: 03
type: execute
wave: 3
depends_on: [56.7-01, 56.7-02]
files_modified:
  - web/app/(dashboard)/automations/[swarm]/review/page.tsx
  - web/app/(dashboard)/automations/[swarm]/review/actions.ts
  - web/app/(dashboard)/automations/[swarm]/review/queue-tree.tsx
  - web/app/(dashboard)/automations/[swarm]/review/row-list.tsx
  - web/app/(dashboard)/automations/[swarm]/review/detail-pane.tsx
  - web/app/(dashboard)/automations/[swarm]/review/selection-context.tsx
  - web/app/(dashboard)/automations/[swarm]/review/keyboard-shortcuts.tsx
  - web/app/(dashboard)/automations/[swarm]/review/race-cohort-banner.tsx
  - web/app/(dashboard)/automations/[swarm]/review/row-strip.tsx
  - web/next.config.ts
  - web/tests/queue/page.test.tsx
  - web/tests/queue/actions.test.ts
  - .planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-05-PLAN.md
autonomous: true
requirements: [D-07, D-08, D-09, D-13, D-14, D-15]
must_haves:
  truths:
    - "Route /automations/[swarm]/review SSRs the queue using loadSwarm + loadSwarmCategories — no hardcoded 'debtor-email' literals in components"
    - "When loadSwarm returns null or enabled=false, page calls notFound() (Next 15 standard)"
    - "QueueTree reads ui_config.tree_levels (string[]) and builds nesting dynamically; debtor-email gets 3 levels (topic → entity → mailbox_id), Sales would get 2"
    - "Override-dropdown options are derived from loadSwarmCategories (NOT a static OVERRIDE_CATEGORIES const)"
    - "recordVerdict server action accepts swarm_type as an argument; Zod schema validates override_category as z.string() then post-validates against loadSwarmCategories (Pitfall 5)"
    - "next.config.ts redirects() forwards /automations/debtor-email-review and /automations/debtor-email-review/:path* → /automations/debtor-email/review with permanent: false (308 after one deploy cycle per D-07)"
    - "Old web/app/(dashboard)/automations/debtor-email-review/ directory is deleted in the same commit (atomic swap, avoids Next route-collision warning per RESEARCH.md)"
    - "All internal <Link href='/automations/debtor-email-review'> instances search-replaced to '/automations/debtor-email/review' (Pitfall 2)"
    - "60-05-PLAN.md frontmatter depends_on includes 56.7; its body's queue-rewrite tasks reference loadSwarm('debtor-email') instead of OVERRIDE_CATEGORIES (D-15)"
    - "MAILBOX_LABELS rendering gated on swarm_type==='debtor-email' (Q2 defer recommendation — out of scope for 56.7)"
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/review/page.tsx"
      provides: "Dynamic-segment SSR queue page; loads swarm + categories from registry; threads swarmType through loadPageData"
      contains: "loadSwarm\\(admin"
    - path: "web/next.config.ts"
      provides: "async redirects() forwarding old debtor-email-review URLs to new dynamic-segment route"
      contains: "async redirects"
    - path: ".planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-05-PLAN.md"
      provides: "Amended frontmatter (depends_on adds 56.7) + body switched to registry consumption"
      contains: "56.7"
  key_links:
    - from: "web/app/(dashboard)/automations/[swarm]/review/page.tsx"
      to: "web/lib/swarms/registry.ts"
      via: "await loadSwarm(admin, params.swarm) + await loadSwarmCategories(admin, params.swarm)"
      pattern: "loadSwarm\\(admin"
    - from: "web/app/(dashboard)/automations/[swarm]/review/detail-pane.tsx"
      to: "loadSwarmCategories return value"
      via: "props or server-component data prop; rendered as <SelectItem value={c.category_key}>{c.display_label}</SelectItem>"
      pattern: "category_key"
    - from: "web/next.config.ts redirects()"
      to: "/automations/debtor-email/review"
      via: "source=/automations/debtor-email-review[/:path*] destination=/automations/debtor-email/review[/:path*]"
      pattern: "debtor-email-review.*debtor-email/review"
    - from: ".planning/phases/60-…/60-05-PLAN.md frontmatter"
      to: "Phase 56.7 (registry)"
      via: "depends_on: [56-01, 56-02, 56.7]"
      pattern: "depends_on.*56\\.7"
---

<objective>
Stand up the swarm-agnostic queue UI at `/automations/[swarm]/review`, redirect the old debtor-email-specific path, and amend the existing 60-05 PLAN so its queue UI rewrite consumes the registry from day one rather than as a follow-up refactor (D-15).

Purpose: The CONTEXT.md phase boundary states: "any new automation/agent-swarm plugs in via DB rows instead of new code/routes." Wave 1 + Wave 2 made storage and dispatch generic. This wave makes the user-facing surface generic — the last edge of the boundary. After this plan: a Sales swarm row INSERT auto-creates `/automations/sales-email/review` with zero code change.

Output:
- Move/generalize 10 files from `debtor-email-review/` → `[swarm]/review/`.
- `next.config.ts` redirects with `:path*` forwarding (Pattern 5 in RESEARCH.md).
- Internal `<Link>` search-replace.
- Amend `60-05-PLAN.md` per Q3 grep recommendation.
- Extend 2 existing queue tests with the swarm_type-threading assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56.7-swarm-registry/56.7-CONTEXT.md
@.planning/phases/56.7-swarm-registry/56.7-RESEARCH.md
@.planning/phases/56.7-swarm-registry/56.7-01-SUMMARY.md
@.planning/phases/56.7-swarm-registry/56.7-02-SUMMARY.md
@CLAUDE.md
@web/app/(dashboard)/automations/debtor-email-review/page.tsx
@web/app/(dashboard)/automations/debtor-email-review/actions.ts
@web/app/(dashboard)/automations/debtor-email-review/queue-tree.tsx
@web/app/(dashboard)/automations/debtor-email-review/row-list.tsx
@web/app/(dashboard)/automations/debtor-email-review/detail-pane.tsx
@web/app/(dashboard)/automations/debtor-email-review/categories.ts
@web/next.config.ts
@web/lib/swarms/registry.ts
@web/lib/swarms/types.ts
@web/tests/queue/page.test.tsx
@web/tests/queue/actions.test.ts
@.planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-05-PLAN.md

<interfaces>
<!-- Wave 1 contracts (already shipped per 56.7-01-SUMMARY.md) -->

From web/lib/swarms/registry.ts:
```typescript
export async function loadSwarm(admin: SupabaseClient, swarmType: string): Promise<SwarmRow | null>;
export async function loadSwarmCategories(admin: SupabaseClient, swarmType: string): Promise<SwarmCategoryRow[]>;
```

From web/lib/swarms/types.ts (SwarmRow.ui_config shape):
```typescript
interface SwarmUiConfig {
  tree_levels: string[];     // debtor-email: ['topic','entity','mailbox_id']
  row_columns: { key: string; label: string; width: number }[];
  drawer_fields: string[];
  default_sort: string;
}
```

Existing RPC (unchanged per D-09):
```sql
classifier_queue_counts(p_swarm_type text)  -- already takes swarm_type
```

File mapping (RESEARCH.md table lines 720-732 — copy-paste source for genericization rules per file).
</interfaces>

<phase_56_amendment_target>
Per Open Q3 recommendation, the planner ran:
```bash
grep -l "OVERRIDE_CATEGORIES\|debtor-email-review" .planning/phases/56-*/*-PLAN.md .planning/phases/60-*/*-PLAN.md .planning/phases/61-*/*-PLAN.md
```
Result: the queue UI rewrite lives in **Phase 60, plan 60-05** (`.planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-05-PLAN.md`). 60-05's `files_modified` includes `web/app/(dashboard)/automations/debtor-email-review/page.tsx` + `queue-tree.tsx` + `predicted-row-list.tsx` + `predicted-row-item.tsx` + `race-cohort-banner.tsx`. Plans 60-06, 61-01, 61-02, 61-03 also reference the directory but do not contain the OVERRIDE_CATEGORIES symbol; they only need their `files_modified` paths updated when the directory move ships.

This plan amends 60-05 directly. 60-06/61-* path updates are mechanical and can be folded into Task 4 below (single sed pass). 56-NN plans were checked and do NOT match — Phase 56 is iController-labeling, distinct concern.
</phase_56_amendment_target>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move + generalize the 10 debtor-email-review files into [swarm]/review and rewrite page.tsx + actions.ts to consume the registry</name>
  <files>web/app/(dashboard)/automations/[swarm]/review/page.tsx, web/app/(dashboard)/automations/[swarm]/review/actions.ts, web/app/(dashboard)/automations/[swarm]/review/queue-tree.tsx, web/app/(dashboard)/automations/[swarm]/review/row-list.tsx, web/app/(dashboard)/automations/[swarm]/review/detail-pane.tsx, web/app/(dashboard)/automations/[swarm]/review/selection-context.tsx, web/app/(dashboard)/automations/[swarm]/review/keyboard-shortcuts.tsx, web/app/(dashboard)/automations/[swarm]/review/race-cohort-banner.tsx, web/app/(dashboard)/automations/[swarm]/review/row-strip.tsx</files>
  <action>
**Step A — Create the new directory and copy files.**
`mkdir -p "web/app/(dashboard)/automations/[swarm]/review"`. Copy all 10 files from `web/app/(dashboard)/automations/debtor-email-review/` (excluding `categories.ts` — that one is deleted, types come from `@/lib/swarms/types`).

**Step B — page.tsx genericization** (the largest change; per RESEARCH.md Pitfall 6 there are ~5 hardcoded 'debtor-email' literals to remove).
- Function signature: `export default async function Page({ params, searchParams }: { params: Promise<{ swarm: string }>, searchParams: Promise<…> })`. Next 15 awaitable params.
- Inside: `const { swarm: swarmType } = await params;`
- `const admin = createAdminClient();`
- `const swarm = await loadSwarm(admin, swarmType);`
- `if (!swarm || !swarm.enabled) { notFound(); }` — `import { notFound } from "next/navigation";`
- `const categories = await loadSwarmCategories(admin, swarmType);`
- Refactor existing `loadPageData(params, admin)` → `loadPageData(params, admin, swarmType)`. Replace EVERY hardcoded `'debtor-email'` literal in this file with `swarmType`. The classifier_queue_counts RPC call passes `p_swarm_type: swarmType`. The `automation_runs` query's `.eq("swarm_type", "debtor-email")` becomes `.eq("swarm_type", swarmType)`.
- Pass `swarm`, `categories`, and `swarmType` as props down to `<QueueTree>` / `<RowList>` / `<DetailPane>`.

**Step C — actions.ts genericization** (Pitfall 5).
- `recordVerdict` accepts `swarm_type: string` as part of its input.
- Zod schema: `override_category: z.string().nullable()` (NOT `z.enum(OVERRIDE_CATEGORIES)`). Then post-validate:
  ```typescript
  if (input.override_category !== null) {
    const cats = await loadSwarmCategories(admin, input.swarm_type);
    if (!cats.find(c => c.category_key === input.override_category)) {
      throw new Error(`unknown override_category: ${input.override_category} for swarm ${input.swarm_type}`);
    }
  }
  ```
- The Inngest event payload passes `swarm_type: input.swarm_type` through. (Already the case for the worker — Wave 2.)
- `emitAutomationRunStale(admin, \`${input.swarm_type}-review\`)` — already pattern-matched.
- DELETE `import { OVERRIDE_CATEGORIES } from "./categories"`. Replace with `import type { SwarmCategoryRow } from "@/lib/swarms/types";` if needed.

**Step D — queue-tree.tsx genericization** (D-08).
- Component takes `treeLevels: string[]` prop (passed from page.tsx as `swarm.ui_config.tree_levels`).
- Recursive `buildTree(rows, levels)` function: groups by `levels[0]`, then recurses on `levels.slice(1)`. Base case: when levels is empty, render leaf rows.
- For debtor-email's 3 levels (topic → entity → mailbox_id), behavior is unchanged. For a hypothetical Sales swarm with 2 levels (topic → entity), the tree just has one fewer nesting layer — automatic from the array length.
- **Q2 defer:** keep `MAILBOX_LABELS` in this file (or its current location), but gate its rendering on `swarmType === 'debtor-email'`. Comment explicitly: `// Q2 (Phase 56.7+1): move to ui_config.label_maps.mailbox_id; gated on swarm_type for now.`
- pushUrl in click handlers uses `\`/automations/${swarmType}/review?…\`` — NOT a hardcoded debtor-email path (Pitfall 2 — was the only instance per RESEARCH.md verified facts).

**Step E — row-list.tsx + detail-pane.tsx genericization.**
- `row-list.tsx`: accept `columns: SwarmUiConfig["row_columns"]` prop (from `swarm.ui_config.row_columns`). Render headers and cells from this array. For columns whose `key` is not present on a row, render empty.
- `detail-pane.tsx`:
  - Accept `categories: SwarmCategoryRow[]` prop (from `loadSwarmCategories`).
  - Override-dropdown: `categories.filter(c => c.action !== 'reject').map(c => <SelectItem value={c.category_key}>{c.display_label}</SelectItem>)` — keeps `unknown` (action='reject') out of the override list since it's the "skip" option, not a category override. Verify against today's debtor-email behavior in detail-pane.tsx lines 103-109.
  - Drawer fields driven by `swarm.ui_config.drawer_fields`.

**Step F — selection-context, keyboard-shortcuts, race-cohort-banner, row-strip.**
- Per RESEARCH.md file mapping table: these are truly generic, no change beyond the move.

**Step G — Atomic delete the old directory.**
After confirming all 10 files moved + types extracted, `rm -rf "web/app/(dashboard)/automations/debtor-email-review"`. Atomic swap (per RESEARCH.md "Special note on the Next dev cache" — both directories existing simultaneously triggers a route-collision warning).

**Step H — Search-replace internal links + sweep test imports** (Pitfall 2 + verification finding 3).
`grep -rn "/automations/debtor-email-review" web/ --include='*.ts' --include='*.tsx'` → there should be zero occurrences after this task. Replace any remaining `<Link href="/automations/debtor-email-review">` with `<Link href="/automations/debtor-email/review">`.

Additionally, update import paths in ALL test files that reference the old directory. Run `grep -rln "debtor-email-review" web/tests/ --include='*.ts' --include='*.tsx'` and rewrite every `from "@/app/(dashboard)/automations/debtor-email-review/..."` import to `from "@/app/(dashboard)/automations/[swarm]/review/..."`. Known affected files (verified during plan-check): `web/tests/queue/page.test.tsx`, `web/tests/queue/actions.test.ts`, `web/tests/queue/keyboard-shortcuts.test.tsx`, `web/tests/queue/rule-filter.test.tsx`, `web/tests/queue/race-cohort.test.tsx` — confirm and add any new ones surfaced by grep. Without this sweep, tests fail to import after Step G's directory delete.

RESEARCH.md verified the only hardcoded internal pushUrl was inside queue-tree.tsx itself, which Step D already addresses; this step is a defensive sweep + test-import migration.
  </action>
  <verify>
    <automated>cd "/Users/nickcrutzen/Developer/Agent Workforce" && test -f "web/app/(dashboard)/automations/[swarm]/review/page.tsx" && test ! -d "web/app/(dashboard)/automations/debtor-email-review" && grep -rn "automations/debtor-email-review" web/ --include='*.ts' --include='*.tsx' | grep -v '^#' | grep -c "debtor-email-review" | awk '$1 == 0 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
- New directory exists at `web/app/(dashboard)/automations/[swarm]/review/` with all 9 components (categories.ts removed).
- Old directory `web/app/(dashboard)/automations/debtor-email-review/` is deleted.
- Zero `/automations/debtor-email-review` literals remain in `web/` source.
- `page.tsx` calls `loadSwarm` and `loadSwarmCategories`; `notFound()` on null/disabled.
- `actions.ts` Zod schema uses `z.string()` for override_category with post-validation against the loaded list.
- `cd web && npx tsc --noEmit` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure next.config.ts redirects for old debtor-email-review URLs</name>
  <files>web/next.config.ts</files>
  <action>
Replace `web/next.config.ts` per the Pattern 5 example in RESEARCH.md lines 297-329. Specifically, add an `async redirects()` method to the existing `NextConfig` object — DO NOT remove the existing `serverExternalPackages` array (the file already has one).

Add two redirect rules:
```typescript
async redirects() {
  return [
    {
      source: "/automations/debtor-email-review",
      destination: "/automations/debtor-email/review",
      permanent: false, // 307 — flip to true after one deploy cycle (D-07)
    },
    {
      source: "/automations/debtor-email-review/:path*",
      destination: "/automations/debtor-email/review/:path*",
      permanent: false,
    },
  ];
}
```

The `:path*` form forwards query-string and any trailing path. Verified for Next 15 (RESEARCH.md A1 + CITED Next.js docs).

`permanent: false` (307) is intentional for one deploy cycle; D-07 calls for flipping to `permanent: true` (308) after operator bookmarks have been re-cached. The flip is a one-line change in a follow-up commit, NOT in scope for 56.7 — comment-document the timing.
  </action>
  <verify>
    <automated>cd "/Users/nickcrutzen/Developer/Agent Workforce/web" && grep -c "async redirects\|debtor-email-review.*debtor-email/review\|/:path\\*" next.config.ts | awk '$1 >= 3 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
- `next.config.ts` exports a config with `async redirects()` returning the two rules above.
- Both rules use `permanent: false` with a comment marking the planned flip-to-true.
- `cd web && npx next build 2>&1 | grep -i "redirect" | head` shows the redirects registered (or no error).
- Manual smoke (after deploy): `curl -I https://<host>/automations/debtor-email-review` returns 307 with Location header `/automations/debtor-email/review`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend existing queue tests to verify swarm_type threading</name>
  <files>web/tests/queue/page.test.tsx, web/tests/queue/actions.test.ts</files>
  <behavior>
- `page.test.tsx`: extend with one new test asserting that `loadPageData(params, admin, 'debtor-email')` threads `'debtor-email'` into the classifier_queue_counts RPC call (`p_swarm_type` arg) AND into the `automation_runs` `.eq("swarm_type", …)` filter. Mock loadSwarm to return a SwarmRow; mock loadSwarmCategories to return the 7 seeded categories. Assert that calling page with a different `params.swarm` (e.g. `'sales-email'` where loadSwarm returns null) calls `notFound()`.
- `actions.test.ts`: extend with one new test asserting `recordVerdict({ swarm_type: 'debtor-email', override_category: 'payment', … })` succeeds AND `recordVerdict({ swarm_type: 'debtor-email', override_category: 'bogus', … })` throws "unknown override_category". Mock `loadSwarmCategories` to return the seeded list; verify the inngest event payload includes `swarm_type`.

DO NOT duplicate existing tests. ADD one new `describe('swarm-aware')` block per file.
  </behavior>
  <action>
Read both existing test files. Identify the existing mock harness for `createAdminClient` and Inngest. Add the `describe('swarm-aware', () => { … })` block at the bottom of each.

In each new test:
- `vi.mock("@/lib/swarms/registry", () => ({ loadSwarm: vi.fn(), loadSwarmCategories: vi.fn() }))`.
- Per-test, set return values via `vi.mocked(loadSwarm).mockResolvedValue(...)`.

Run vitest, ensure RED (existing code may not handle swarm_type yet — confirm) → Task 1's edits made it GREEN.

Note: if Task 1 ran first and tests pass on first vitest run, that's fine — TDD discipline relaxes when the implementation already lands per the task ordering. Document any unexpected GREEN-on-first-run in the SUMMARY.
  </action>
  <verify>
    <automated>cd "/Users/nickcrutzen/Developer/Agent Workforce/web" && npx vitest run tests/queue/page.test.tsx tests/queue/actions.test.ts</automated>
  </verify>
  <done>
- Both test files have a new `describe('swarm-aware')` block.
- All tests pass; the new tests assert swarm_type threading.
- No regression in the pre-existing tests in these files.
  </done>
</task>

<task type="auto">
  <name>Task 4: Amend Phase 60-05-PLAN.md to depend on 56.7 + update mechanical path references in 60-06, 61-01..03</name>
  <files>.planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-05-PLAN.md</files>
  <action>
**Step A — 60-05 frontmatter amendment (D-15).**
Read `.planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-05-PLAN.md`.

Update frontmatter:
- `depends_on: [60-01, 60-02, 60-06]` → `depends_on: [60-01, 60-02, 60-06, 56.7]`
- `files_modified:` paths — change every `web/app/(dashboard)/automations/debtor-email-review/...` to `web/app/(dashboard)/automations/[swarm]/review/...`.

**Step B — 60-05 body amendment.**
In the body of 60-05 (the task descriptions), find any reference to:
- `OVERRIDE_CATEGORIES` from `./categories` → replace with `loadSwarmCategories(admin, 'debtor-email')` from `@/lib/swarms/registry`.
- `import { OVERRIDE_CATEGORIES } from "./categories"` → `import { loadSwarmCategories } from "@/lib/swarms/registry"; import type { SwarmCategoryRow } from "@/lib/swarms/types";`
- Any `z.enum(OVERRIDE_CATEGORIES)` Zod construct → `z.string()` with post-validation comment referencing Pitfall 5 in 56.7-RESEARCH.md.
- Any hardcoded `'debtor-email'` literal in the page query logic → `swarmType` (from `params.swarm`).
- Any `/automations/debtor-email-review` URL → `/automations/debtor-email/review`.

Add a **prominent note at the top** of 60-05 (or in its `<objective>` block):
> **Phase 56.7 dependency note:** The swarm registry (Phase 56.7) ships before this plan executes. This plan's queue rewrite consumes `loadSwarm` / `loadSwarmCategories` from `@/lib/swarms/registry` instead of static `OVERRIDE_CATEGORIES`. The `[swarm]/review/` directory is created by 56.7-03; this plan modifies files inside it, not in `debtor-email-review/`.

**Step C — Mechanical path updates in 60-06, 61-01, 61-02, 61-03.**
For each plan that has `files_modified` paths under `web/app/(dashboard)/automations/debtor-email-review/`, update them to `web/app/(dashboard)/automations/[swarm]/review/`.

(Do NOT amend `must_haves` truths or task bodies in these other plans — they're concerned with file content, not the directory move. The path update is purely housekeeping so executors find the right files.)

If 60-06 / 61-* contains hardcoded `/automations/debtor-email-review` URLs in its task bodies, replace with `/automations/debtor-email/review` (Pitfall 2 propagation). Verify with grep before declaring done.

**Step D — Validate other 56-NN plans (per Q3).**
Run `grep -l "OVERRIDE_CATEGORIES\|debtor-email-review" .planning/phases/56-*/*-PLAN.md`. Per the Phase 56 inspection (it's iController-labeling, no overlap), this should return 0 matches. If it returns non-zero, surface in SUMMARY for human review — do NOT silently amend Phase 56 plans without confirmation, since CONTEXT's Q3 explicitly bounded the search.
  </action>
  <verify>
    <automated>cd "/Users/nickcrutzen/Developer/Agent Workforce" && grep -c "56\\.7" .planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-05-PLAN.md | awk '$1 >= 1 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
- 60-05-PLAN.md frontmatter `depends_on` includes `56.7`.
- 60-05-PLAN.md body references `loadSwarmCategories` / `loadSwarm` (not OVERRIDE_CATEGORIES).
- 60-06-PLAN.md, 61-01..03-PLAN.md `files_modified` paths point at `[swarm]/review/` (not `debtor-email-review/`).
- `grep -rn "OVERRIDE_CATEGORIES" .planning/phases/60-* .planning/phases/61-*` returns zero matches in PLAN.md files.
- Q3-validation grep against `.planning/phases/56-*/` recorded in SUMMARY (expected: 0 matches).
  </done>
</task>

</tasks>

<verification>
End-of-wave / phase-completion checks:
1. **Live route smoke:** After deploy, `https://<host>/automations/debtor-email/review` renders the queue (debtor-email seed) identically to today's `/automations/debtor-email-review`. Approve flow round-trips through the rewritten worker (Wave 2) end-to-end.
2. **Old route 307s correctly:** `curl -I https://<host>/automations/debtor-email-review` returns `HTTP/1.1 307` with `Location: /automations/debtor-email/review`. Same for `…?topic=foo` query strings (path/forwarding).
3. **Sales-swarm dry-run:** Manually `INSERT INTO public.swarms (swarm_type, display_name, ui_config) VALUES ('sales-email-test', 'Sales Test', jsonb_build_object('tree_levels', jsonb_build_array('topic'), 'row_columns', jsonb_build_array(), 'drawer_fields', jsonb_build_array(), 'default_sort', 'created_at desc'))` — visit `/automations/sales-email-test/review` — page renders an empty queue (no `automation_runs` rows of swarm_type='sales-email-test'). DELETE the test row after.
4. **404 for unknown swarm:** `/automations/bogus-swarm/review` returns 404 (`notFound()` triggered).
5. **No leaks of debtor-email literal:** `grep -rn "'debtor-email'" web/app/\(dashboard\)/automations/\[swarm\]/` returns zero matches except for explicit Q2 gates with comment marker `// Q2 (Phase 56.7+1)`.
6. **60-05 amendment integrity:** `grep -c "56.7" .planning/phases/60-…/60-05-PLAN.md` ≥ 1; `grep -c "OVERRIDE_CATEGORIES" .planning/phases/60-…/60-05-PLAN.md` = 0.
7. **Test suite green:** `cd web && npx vitest run` passes; new swarm-aware tests in page.test.tsx + actions.test.ts pass.
</verification>

<success_criteria>
- A second swarm can be added by INSERTing one swarms row + N swarm_categories rows + (optionally) one Inngest worker on the dispatch event. Zero changes to `[swarm]/review/` components, zero changes to `classifier-verdict-worker.ts`, zero new Vercel deploy for routing (D-13 contract validated).
- Operator bookmarks to the old `/automations/debtor-email-review` URL continue to work via 307 (one deploy cycle of grace), then flip to 308 in a follow-up commit per D-07.
- Phase 60-05 (the queue rewrite plan) consumes the registry from day one — no follow-up refactor pass needed (D-15 contract met).
- Phase boundary fully crossed: every locked decision D-00 through D-17 is implemented in code or a planning artifact.
</success_criteria>

<output>
After completion, create `.planning/phases/56.7-swarm-registry/56.7-03-SUMMARY.md`. Highlight:
- Live URLs working post-deploy (debtor-email/review + redirect from old URL).
- Result of the Sales-swarm dry-run experiment (Verification step 3) — confirms D-13 operational contract.
- Final grep count for `OVERRIDE_CATEGORIES` in `.planning/` (expected 0 — it's deleted).
- Result of the Q3-validation grep against `.planning/phases/56-*/` (expected 0 hits; if non-zero, list and ask for follow-up).
- Note any Q2 (MAILBOX_LABELS) gates left in code; flag as "follow-up phase 56.7+1 cleanup."
- Confirm the next-cycle 307→308 redirect flip is logged as a follow-up commit (NOT in scope here per D-07).
</output>
