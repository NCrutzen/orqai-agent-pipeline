---
phase: 82.6-footer-approve-recordverdict-wiring
verified: 2026-05-15T00:00:00Z
status: human_needed
score: 7/7 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Open Stage 1 detail pane on a predicted row, click footer '✓ Approve (Stages X+Y)'"
    expected: "Row vanishes from list within 1-2s; automation_runs.status flips predicted → feedback (verify via Supabase REST); ≥1 feedback POST + ≥1 recordVerdict invocation in server logs"
    why_human: "Requires live dev/prod environment with real predicted rows and Supabase round-trip — cannot be observed via static analysis"
  - test: "Open detail pane on Stage 0, Stage 2, Stage 3, and Stage 4 rows in turn"
    expected: "Footer shows only Reject + Skip buttons; no Approve button anywhere"
    why_human: "Visual confirmation of conditional render across stage values"
  - test: "Re-run web/scripts/probe-approve-mode.ts against deployed build"
    expected: "Probe reports row leaves Stage 1 list AND recordVerdict invocation observed"
    why_human: "Browserless probe requires live deploy and credentialed run"
---

# Phase 82.6: Footer Approve → recordVerdict Wiring — Verification Report

**Phase Goal:** Wire footer "✓ Approve" on Stage 1 to call `recordVerdict` via `approvePrediction` wrapper so rows actually leave the queue (status flip, telemetry, Inngest, broadcast). Hide footer Approve on Stages 0/2/3/4. Optimistic removal via `pendingRemovalIds`. Non-fatal recordVerdict failure.

**Verified:** 2026-05-15
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `approvePrediction(row_id, swarm_type, decision)` exists and is exported from `stage-1/actions.ts` | ✓ VERIFIED | `stage-1/actions.ts:798` `export async function approvePrediction` |
| 2 | Wrapper hydrates VerdictInput from `automation_runs` + `email_pipeline.emails` join | ✓ VERIFIED | `stage-1/actions.ts:811-848` — single automation_runs select + cross-schema fallback |
| 3 | Wrapper delegates to `recordVerdict` (no re-implementation) | ✓ VERIFIED | `stage-1/actions.ts:866` `return recordVerdict({...})` — no parallel status flip / agent_runs / Inngest send in wrapper |
| 4 | Wrapper does NOT read `swarm_intents` (hard-separation lock) | ✓ VERIFIED | `grep swarm_intents stage-1/actions.ts` → 2 matches, both in comments (lines 549, 787); zero code refs |
| 5 | Footer Approve click triggers both feedback POSTs AND approvePrediction (in try/catch) | ✓ VERIFIED | `detail-pane.tsx:492-503` `try { await approvePrediction(...) } catch {}` after Promise.all feedback POSTs |
| 6 | Optimistic removal via `markPendingRemoval(row.id)` after approve | ✓ VERIFIED | `detail-pane.tsx:510` `markPendingRemoval(row.id)` — runs regardless of approvePrediction outcome (D-04) |
| 7 | Footer Approve button hidden on Stages 0/2/3/4 (only renders on `activeStage === 1`) | ✓ VERIFIED | `detail-pane.tsx:647` `{activeStage === 1 ? (<Button data-testid="detail-pane-primary"...) : null}` wraps only the primary; Reject/Skip siblings unconditional |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `web/app/(dashboard)/automations/[swarm]/stage-1/actions.ts` | approvePrediction wrapper | ✓ VERIFIED | Exported at line 798; delegates to recordVerdict at 866; imported and used in detail-pane.tsx:56,493 |
| `web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx` | Wired handler + footer guard | ✓ VERIFIED | Imports at 56-57; useSelection destructure at 237; handler at 493; markPendingRemoval at 510; ternary at 647 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| approvePrediction | recordVerdict | direct in-module call | ✓ WIRED | actions.ts:866 `return recordVerdict({...})` |
| approvePrediction | automation_runs + email_pipeline.emails | createAdminClient() lookup | ✓ WIRED | actions.ts:811-815 (automation_runs.select), 834-839 (email_pipeline.emails defensive fallback) |
| detail-pane handlePrimary | approvePrediction | try/catch await | ✓ WIRED | detail-pane.tsx:492-503 |
| detail-pane handlePrimary | useSelection().markPendingRemoval | post-approve setState | ✓ WIRED | detail-pane.tsx:237 (destructure) + 510 (call) |
| detail-pane footer JSX | activeStage === 1 guard | conditional render | ✓ WIRED | detail-pane.tsx:647 ternary wraps only primary Button; Reject/Skip remain unconditional |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| approvePrediction wrapper | run (automation_runs row) | `admin.from("automation_runs").select(...).eq("id", row_id).single()` | Yes — real Supabase query, throws on null | ✓ FLOWING |
| approvePrediction wrapper | emailRow fallback | `admin.schema("email_pipeline").from("emails").select(...).eq("id", run.email_id).maybeSingle()` | Yes — real cross-schema query | ✓ FLOWING |
| detail-pane approve branch | row.id, swarmType | Component props (passed by parent page) | Yes — props originate from server-rendered queue page | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| TypeScript compiles cleanly | `cd web && npx tsc --noEmit` (filtered to target files) | No errors in stage-1/actions.ts or _shell/detail-pane.tsx | ✓ PASS |
| approvePrediction export count | `grep -n "export async function approvePrediction" stage-1/actions.ts` | 1 match (line 798) | ✓ PASS |
| Primary button definition count | `grep -c 'data-testid="detail-pane-primary"' detail-pane.tsx` | 1 (single guarded definition) | ✓ PASS |
| Live row progression on click | (requires Browserless probe / live env) | N/A | ? SKIP — see human verification |

### Requirements Coverage

No REQUIREMENTS.md IDs declared for this phase — phase tracks D-IDs from CONTEXT.md.

| D-ID | Description | Status | Evidence |
|------|-------------|--------|----------|
| D-01 | approvePrediction server-action wrapper around recordVerdict | ✓ SATISFIED | actions.ts:798-870 |
| D-02 / D-02c | Stage 1 only; no Stage 2/3 UX duplication | ✓ SATISFIED | No Stage 2/3 code paths touched; footer guard restricts to activeStage===1 |
| D-02b | Footer Approve hidden on Stages 0/2/3/4 | ✓ SATISFIED | detail-pane.tsx:647 ternary |
| D-03 | Non-fatal recordVerdict failure (try/catch, no toast) | ✓ SATISFIED | detail-pane.tsx:492-503 empty catch; markPendingRemoval still runs |
| D-04 | Optimistic removal via markPendingRemoval(row.id) | ✓ SATISFIED | detail-pane.tsx:510 |

### Anti-Patterns Found

None. Comments mentioning `swarm_intents` in `stage-1/actions.ts` (lines 549, 787) are explicit negative-assertion documentation reinforcing the hard-separation lock — not code references.

### Hard-Separation Lock Audit

- New `approvePrediction` wrapper reads only `public.automation_runs` (Stage 1 state) and `email_pipeline.emails` (orthogonal corpus, owned by neither stage).
- Delegates only to `recordVerdict`, which is Stage-1-pure per the locked RFC: writes `automation_runs.status='feedback'`, fires `classifier/verdict.recorded` → `classifier-verdict-worker` reads `swarm_noise_categories.action` registry.
- Zero reads/writes against `swarm_intents`. Stage 3 dispatch path (Phase 80 inline widget actions) is untouched.
- Footer Approve is now physically absent on Stage 3 detail panes, eliminating any UI surface that could blur the separation.

### Human Verification Required

Automated checks confirm the wiring is structurally correct. Live behavioral confirmation requires operator/dev environment:

1. **Live Stage 1 approve smoke test** — click footer Approve on a real predicted row; confirm row vanishes within 1-2s AND `automation_runs.status` flips `predicted → feedback` via Supabase REST.
2. **Visual footer audit across stages** — open detail pane on Stage 0, 2, 3, 4 rows; confirm only Reject + Skip render.
3. **Re-run probe-approve-mode.ts** — Browserless probe should now show ≥1 feedback POST + ≥1 recordVerdict invocation + row leaves list (was previously: 2 feedback POSTs, no progression on commit 7112c78).

### Gaps Summary

No gaps. All structural must-haves verified. Phase awaits human/operator behavioral confirmation in live environment.

---

*Verified: 2026-05-15*
*Verifier: Claude (gsd-verifier)*
