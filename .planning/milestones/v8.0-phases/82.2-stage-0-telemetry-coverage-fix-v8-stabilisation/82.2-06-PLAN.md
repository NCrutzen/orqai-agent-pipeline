---
phase: 82.2-stage-0-telemetry-coverage-fix-v8-stabilisation
plan: 06
type: execute
wave: 3
depends_on: [82.2-04]
files_modified:
  - web/lib/inngest/functions/classifier-screen-worker.ts
  - web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts
  - web/lib/inngest/functions/__tests__/classifier-screen-worker.gate.test.ts
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-03, CONS-01, TELE-COV-01]
must_haves:
  truths:
    - "Stage 1 worker owns regex classification, automation_runs row creation for Stage 0 (the pending placeholder), auto-action dispatch on whitelist match, predicted-category bulk-review queueing, and Stage 1 pipeline_events emit"
    - "Hard separation rule (Stage 1 = noise filter only per stage-1-regex.md RFC) is preserved — regex classification still uses the noise-filter classifier, not the intent classifier"
    - "Auto-action (Outlook categorize+archive + iController-cleanup queue) is idempotent on Inngest replay"
  artifacts:
    - path: "web/lib/inngest/functions/classifier-screen-worker.ts"
      provides: "Owner of post-Stage-0 dispatch logic that previously lived in debtor ingest route"
      contains: "auto-action"
  key_links:
    - from: "classifier-screen-worker.ts"
      to: "categorizeEmail, archiveEmail (Outlook)"
      via: "step.run wrapped Outlook API calls"
      pattern: "categorizeEmail"
    - from: "classifier-screen-worker.ts"
      to: "automation_runs (debtor-email-cleanup queue)"
      via: "admin.from('automation_runs').insert"
      pattern: "debtor-email-cleanup"
---

<objective>
Migrate the debtor-email category-dispatch logic OUT of `web/app/api/automations/debtor-email/ingest/route.ts` and INTO `web/lib/inngest/functions/classifier-screen-worker.ts` (Stage 1 worker chain). This is D-A part 1 — the receiving end of the safety-ordering re-wire. Plan 07 then deletes the moved logic from ingest.

Purpose: D-A, D-B. Per CONTEXT and the locked RFC (`docs/agentic-pipeline/README.md`), Stage 0 must run first for every inbound email, and Stage 1 (noise filter) becomes the owner of category-based dispatch. Today the debtor ingest route runs regex classification → emits Stage 1 → dispatches auto-actions → conditionally fires Stage 0 — exactly backwards. After this plan, the Stage 1 worker receives `classifier/screen.requested` from the Stage 0 worker (post-`safe` verdict) and runs the dispatch logic that used to live in the ingest route.

**RFC alignment note:** The Stage 1 worker remains the NOISE-FILTER classifier per `docs/agentic-pipeline/stage-1-regex.md`. The regex classification + auto-action + bulk-review queueing being moved here is the *noise-filter dispatch* (whitelist categories = noise keys like `auto-reply`, `out-of-office`, etc., per the swarm_noise_categories registry). Intent classification (Stage 3) is NOT being moved into Stage 1 — the hard separation rule is preserved.

Output: Stage 1 worker accepts the extra dispatch responsibility; tests cover the new branches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82.2-stage-0-telemetry-coverage-fix-v8-stabilisation/82.2-CONTEXT.md
@.planning/phases/82.2-stage-0-telemetry-coverage-fix-v8-stabilisation/82.2-RESEARCH.md
@CLAUDE.md
@docs/agentic-pipeline/README.md
@docs/agentic-pipeline/stage-0-safety.md
@docs/agentic-pipeline/stage-1-regex.md
@web/lib/inngest/functions/classifier-screen-worker.ts
@web/lib/inngest/functions/stage-0-safety-worker.ts
@web/app/api/automations/debtor-email/ingest/route.ts
@web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts
@web/lib/inngest/functions/__tests__/classifier-screen-worker.gate.test.ts

<interfaces>
**Current ingest-route logic to migrate (debtor-email/ingest/route.ts L283–592):**

After Plan 07 thinning, these blocks live in the Stage 1 worker:
1. **Regex classification** (L284): `classify({ subject, from, bodySnippet })` from `@/lib/automations/debtor-email/classifier`
2. **Stage 1 pipeline_events emit** (L347–362): writes `stage=1`, `decision=r.category`, `decision_details.regex_rule_id`
3. **Whitelist match check** (L364–365): `isWhitelistMatch = whitelist.has(r.matchedRule)`, `autoActionAllowed = isWhitelistMatch && settings.auto_label_enabled`
4. **Bulk-review branch (`!autoActionAllowed`)** (L368–464):
   - If LLM-bound (currently `r.category === "unknown"` AND triage_shadow_mode AND entity) — the placeholder automation_runs row WITH `stage_0_safety_pending`. **POST-D-A this is dead code** because Stage 0 already ran before Stage 1; the placeholder row creation has migrated to the Plan 07 thin ingest (which creates it BEFORE firing stage-0/email.received).
   - Else: write `automation_runs.status='predicted'` with predicted category (the bulk-review queue row).
5. **Auto-action branch (`autoActionAllowed`)** (L467–592):
   - `categorizeEmail` (Outlook categorize)
   - `archiveEmail` (Outlook archive)
   - Insert `automation_runs.status='completed'` for the auto-action audit
   - Insert `automation_runs.status='pending'` for the iController-delete cleanup queue

**Event contract (UNCHANGED):** Stage 0 worker (post-Plan 04 refactor) sends `classifier/screen.requested` with payload `{ automation_run_id, email_id, message_id, source_mailbox, subject, body_text, swarm_type, entity }`. The Stage 1 worker already receives this (today's L80–82). The worker MUST NOT change the event name or payload — Plan 04 worker still emits this event.

**Stage 1 worker today (verified):** lives at L80+. Handles `classifier/screen.requested`, already does its own noise-filter classification work. After this plan it ALSO does the dispatch decisions above.

**Replay safety:**
- All side effects in step.run (CLAUDE.md). Each of {Outlook categorize, Outlook archive, automation_runs insert, pipeline_events insert} = its own step.run.
- `categorizeEmail` + `archiveEmail` are NOT idempotent on the Outlook side. Pattern: check `msg.categories` for MR_LABELS BEFORE calling categorize (the existing L272–281 idempotency guard in ingest must move with this logic). Replay-safe via this pre-check + Inngest's step.run replay semantics (the step runs once per unique step name).
- Phase 65 rule: any new UUID/timestamp used as a DB key MUST be inside step.run.

**Mailbox-id resolution:** the ingest route queries `swarms_labeling_settings` to get `mailbox_id`. After Plan 07, this query moves to BEFORE firing stage-0 (thin ingest still needs to validate the mailbox is ingest_enabled). The mailbox_id passes through Stage 0 event payload → Stage 0 worker re-emits to Stage 1 event payload → Stage 1 worker uses it for automation_runs writes. **Plan 07 will extend the stage-0/email.received payload to carry mailbox_id + entity + source_mailbox** — Stage 1 worker reads them from the inherited payload (already partly threaded via the `entity` and `source_mailbox` fields in today's payload; verify `mailbox_id` is added).

**Whitelist resolution:** today the ingest route loads `whitelist` from somewhere (likely `swarms_classifier_whitelist` or similar). That query also moves to the Stage 1 worker. Verify the load path by reading lines 1–280 of debtor-email/ingest/route.ts during execution.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend classifier-screen-worker tests for new dispatch responsibilities (RED)</name>
  <files>web/lib/inngest/functions/__tests__/classifier-screen-worker.test.ts</files>
  <behavior>
    - Test A: Unknown-category email (no whitelist match) → automation_runs row written with status='predicted', topic=null OR predicted category; no Outlook side-effects called
    - Test B: Whitelist-match email with auto_label_enabled=true → categorizeEmail called once, archiveEmail called once, two automation_runs rows inserted (`debtor-email-review` completed + `debtor-email-cleanup` pending)
    - Test C: Whitelist-match email with auto_label_enabled=false → automation_runs row with action='skipped_disabled'; no Outlook side-effects
    - Test D: Predicted-category non-unknown email (e.g. `auto-reply`) without whitelist match → automation_runs row with status='predicted', topic='auto-reply', action='skipped_not_whitelisted'; no Outlook side-effects
    - Test E (idempotency): Email already carries an MR_LABEL category → no categorize, no archive, automation_runs row with action='skipped_idempotent'
    - Test F (replay-safety): Calling the handler twice with the same event produces the same DB state (categorizeEmail call count = 1 due to step.run dedupe semantics; pre-check guard prevents double-categorize)
  </behavior>
  <action>
    Add a new describe block `describe("Phase 82.2 D-A — category dispatch (moved from ingest)", ...)`. Mock `categorizeEmail`, `archiveEmail`, supabase admin (capture inserts), and the whitelist loader. Use the existing classifier-screen-worker.test.ts mock-step pattern.

    These tests will FAIL until Task 2 (RED).
  </action>
  <verify>
    <automated>cd web && npx vitest run lib/inngest/functions/__tests__/classifier-screen-worker.test.ts -t "Phase 82.2 D-A" 2>&1 | tail -3</automated>
  </verify>
  <done>New test block exists with Tests A–F; current worker fails them (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add dispatch logic to classifier-screen-worker (GREEN)</name>
  <files>web/lib/inngest/functions/classifier-screen-worker.ts</files>
  <action>
    **First:** Read lines 1–280 of `web/app/api/automations/debtor-email/ingest/route.ts` to fully understand the dispatch logic to migrate (the context above summarises but planner-executor must verify the actual conditions and field names).

    **Then:** Extend the classifier-screen-worker handler. Today's worker already handles the noise-filter regex pass and dispatches to handlers. This task adds the FOLLOWING dispatch responsibilities AFTER the existing noise-filter regex but BEFORE handler emit — specifically for `swarm_type='debtor-email'` events (sales-email already has its own downstream chain; verify by reading sales-email ingest):

    1. **Wrap with `swarm_type === 'debtor-email'` guard** so this doesn't disturb sales-email's existing flow.
    2. **Idempotency pre-check** (step.run): if the inbound email already carries an MR_LABEL category, skip dispatch, write automation_runs.action='skipped_idempotent', return early. Move MR_LABELS constant + the check from ingest L272–281.
    3. **Whitelist + auto-action gate** (step.run): load whitelist + settings using the same query the ingest route uses today (verify by reading ingest L1–280). Pass `isWhitelistMatch` + `autoActionAllowed` down.
    4. **Bulk-review branch** (step.run): for `!autoActionAllowed` writes the `status='predicted'` automation_runs row (today's L402–423 logic). DROP the `stage_0_safety_pending` placeholder write — that placeholder is now created by the Plan 07 thin ingest BEFORE Stage 0 runs.
    5. **Auto-action branch** (separate step.runs): for `autoActionAllowed`:
       - `step.run("categorize", () => categorizeEmail(source_mailbox, message_id, label))`
       - `step.run("archive", () => archiveEmail(source_mailbox, message_id))`
       - `step.run("write-auto-action-audit", () => admin.from('automation_runs').insert(... status='completed' ...))`
       - `step.run("queue-icontroller-cleanup", () => admin.from('automation_runs').insert(... automation='debtor-email-cleanup', status='pending' ...))`
       Preserve the `icontroller_company` derivation from L564–565 (settings.icontroller_company ?? LEGACY_DEFAULT_ICONTROLLER_COMPANY).
    6. **emitAutomationRunStale** (step.run): mirror the realtime channel notifications today's ingest does at the relevant points.
    7. **Stage 1 pipeline_events emit — RESOLVED (planner pre-read of classifier-screen-worker.ts).** The Stage 1 worker ALREADY emits exactly one canonical Stage 1 `pipeline_events` row today at Step 4 (file lines ~292–321, `step.run("emit-pipeline-event", ...)` calling `emitPipelineEvent(admin, { stage: 1, ... })` per the Phase 70 dual-write contract). This existing worker emit is the canonical Stage 1 source of truth post-D-A. **DO NOT add a duplicate emit in this plan.** The Stage 1 emit currently ALSO being written by the debtor-email ingest route at L347–362 is the duplicate that must be removed; that deletion belongs to Plan 07 Task 2 (and is now mandated explicitly there). Action for this plan: leave the existing `step.run("emit-pipeline-event", ...)` block at L293–321 of classifier-screen-worker.ts untouched. Verify post-refactor that exactly one `emitPipelineEvent` call with `stage: 1` exists in the worker — `grep -c "stage: 1" web/lib/inngest/functions/classifier-screen-worker.ts` should return 1 (or the count it has today, which is 1).

    **Hard separation check (per RFC):** the regex classification done here MUST still use `swarm_noise_categories` (noise filter), not `swarm_intents`. Verify the loaded whitelist / classifier rules table matches the noise-filter registry. Document this in a code comment referencing `docs/agentic-pipeline/stage-1-regex.md`.

    **Replay-safety check (CLAUDE.md Phase 65):** no `crypto.randomUUID()` or `new Date()` outside step.run; no destructured `inngest.send`.

    All Task 1 tests should pass after this.
  </action>
  <verify>
    <automated>cd web && npx vitest run lib/inngest/functions/__tests__/classifier-screen-worker.test.ts</automated>
  </verify>
  <done>All classifier-screen-worker tests pass; dispatch logic for debtor-email lives in the worker; hard separation rule documented.</done>
</task>

<task type="auto">
  <name>Task 3: Idempotency + double-execution audit</name>
  <files>(read-only audit)</files>
  <action>
    Threat scenario (per CONTEXT additional_context #3): Stage 1 worker retries could double-execute auto-actions.

    Audit:
    1. Confirm each Outlook side-effect step.run has an idempotency guard. `categorizeEmail` already pre-checks msg.categories for MR_LABELS (Test E). `archiveEmail`: Outlook archive on an already-archived message is a no-op (verify in `web/lib/automations/debtor-email/outlook-actions.ts` or wherever the helper lives — grep for it).
    2. Confirm the `debtor-email-cleanup` automation_runs queue insert is idempotent-on-replay: today there's NO unique constraint on automation_runs.result->>'message_id' for this row class. Plan-level decision: do we need to add one? Document the answer. If yes, defer to a follow-up plan or add to this plan's migration scope; if no (the cleanup-worker already dedupes on iController side), document why.
    3. Run `grep -n "retries:" web/lib/inngest/functions/classifier-screen-worker.ts` — verify `retries` value (default 4 or explicit). Document the worst-case retry behavior.
  </action>
  <verify>
    <automated>grep -n "retries\|step.run.*categorize\|step.run.*archive" web/lib/inngest/functions/classifier-screen-worker.ts</automated>
  </verify>
  <done>Idempotency audit complete; documented in SUMMARY. Any required follow-ups flagged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stage 0 worker → Stage 1 worker | classifier/screen.requested event (post-`safe` verdict) |
| Stage 1 worker → Outlook (Graph) | categorizeEmail + archiveEmail (write side-effects) |
| Stage 1 worker → Supabase | automation_runs + pipeline_events inserts |
| Stage 1 worker → Inngest | downstream debtor-email-cleanup queue events |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82.2-06-01 | Tampering | Replay double-executes Outlook categorize | mitigate | Pre-check msg.categories for MR_LABELS before categorizeEmail; step.run dedupes per (run_id, step_name) |
| T-82.2-06-02 | Tampering | Replay double-queues iController cleanup row | mitigate | Cleanup-worker dedupes by message_id (verify in Task 3 audit); accept if confirmed |
| T-82.2-06-03 | Elevation of Privilege | Stage 1 worker bypasses Stage 0 if event arrives directly | mitigate | classifier/screen.requested is emitted ONLY by Stage 0 worker after safe verdict; no other producers (grep-verify in Task 3) |
| T-82.2-06-04 | Tampering | Cross-swarm leak (sales-email payload triggers debtor dispatch) | mitigate | `swarm_type === 'debtor-email'` guard wraps the new dispatch logic |
| T-82.2-06-05 | Information Disclosure | Stage 1 worker logs full body to error message | accept | Existing pattern; out of scope this phase |
</threat_model>

<verification>
- All classifier-screen-worker tests pass
- Hard-separation rule (Stage 1 = noise filter) preserved
- Replay-safety audit clean (CLAUDE.md rules)
- swarm_type='debtor-email' guard prevents cross-swarm leak
</verification>

<success_criteria>
- Stage 1 worker fully owns the debtor-email dispatch logic
- Plan 07 can safely delete the same logic from the ingest route without functional regression
- Tests cover all five dispatch outcomes (skipped_unknown / skipped_not_whitelisted / labeled / skipped_disabled / skipped_idempotent)
</success_criteria>

<output>
After completion, create `.planning/phases/82.2-stage-0-telemetry-coverage-fix-v8-stabilisation/82.2-06-SUMMARY.md`
</output>
