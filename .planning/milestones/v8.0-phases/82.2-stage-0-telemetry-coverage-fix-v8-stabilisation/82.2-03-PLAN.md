---
phase: 82.2-stage-0-telemetry-coverage-fix-v8-stabilisation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260513b_pipeline_health.sql
autonomous: true
requirements: [TELE-COV-02]
must_haves:
  truths:
    - "Coverage probe (D-07) has a durable write target that survives Vercel serverless deploys"
    - "Per-run coverage history is queryable for trend surfaces and /gsd-progress integration"
  artifacts:
    - path: "supabase/migrations/20260513b_pipeline_health.sql"
      provides: "pipeline_health table + RPC helpers"
      contains: "CREATE TABLE public.pipeline_health"
  key_links:
    - from: "stage-0-coverage-probe.ts (Plan 10)"
      to: "public.pipeline_health"
      via: "admin.from('pipeline_health').insert"
      pattern: "pipeline_health"
---

<objective>
Create the `public.pipeline_health` table that the daily coverage probe (Plan 10, D-07/D-08) writes to. Also create the two RPC helpers (`stage0_backfill_candidates`, `stage0_coverage_24h`) that Plan 09 and Plan 10 call.

Purpose: D-08 leaves "STATE.md vs pipeline_health table" to Claude's Discretion. RESEARCH §Coverage probe explicitly recommends pipeline_health because `.planning/STATE.md` is NOT bundled into Vercel serverless deploys (Risk #3). Table also retains per-run coverage history for trend surfaces — D-08 breach session-note (when written by Plan 10) references the row id for traceability.

Output: One migration file with table DDL + two RPC functions + RLS policies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82.2-stage-0-telemetry-coverage-fix-v8-stabilisation/82.2-CONTEXT.md
@.planning/phases/82.2-stage-0-telemetry-coverage-fix-v8-stabilisation/82.2-RESEARCH.md
@supabase/migrations/20260506a_pipeline_events.sql

<interfaces>
RPC helper signatures (called by Plans 09 + 10):

```sql
-- For Plan 09 backfill: returns one row per (email_id, swarm_type) gap
-- in the window, with the best-effort automation_runs join attached.
stage0_backfill_candidates(window_days int)
  RETURNS TABLE(email_id uuid, swarm_type text, completed_at timestamptz, result jsonb)

-- For Plan 10 coverage probe: per-mailbox 24h coverage counters.
stage0_coverage_24h(mailbox_arg text, swarm_arg text)
  RETURNS TABLE(stage1_count bigint, stage0_count bigint)
```

`pipeline_health` row shape (one row per probe run × mailbox):
- id uuid PK
- probe_run_id uuid (groups all mailboxes from one cron tick)
- mailbox text
- swarm_type text
- stage1_count bigint
- stage0_count bigint
- coverage_pct numeric (computed; null when stage1_count=0)
- breached boolean (coverage_pct < 0.99 AND stage1_count > 0)
- recorded_at timestamptz DEFAULT now()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration file with table + RPCs + RLS</name>
  <files>supabase/migrations/20260513b_pipeline_health.sql</files>
  <action>
    Write the migration with these elements (full SQL — no placeholders):

    1. `CREATE TABLE public.pipeline_health` with the columns listed in interfaces above. PK = id uuid DEFAULT gen_random_uuid(). Index on (recorded_at DESC) and (breached, recorded_at DESC) for /gsd-progress queries.

    2. RLS: enable, then service role only for INSERT (mirror pipeline_events posture from 20260506a). No anon/authenticated access.

    3. `CREATE OR REPLACE FUNCTION public.stage0_coverage_24h(mailbox_arg text, swarm_arg text)` per RESEARCH §Coverage query (lines 569–594). LANGUAGE sql STABLE. Encapsulates the per-mailbox 24h CTE.

    4. `CREATE OR REPLACE FUNCTION public.stage0_backfill_candidates(window_days int)` returning the LATERAL-join query from RESEARCH §Backfill (lines 305–323). LANGUAGE sql STABLE. Clamp `window_days` to ≤30 server-side per RESEARCH §V5 input validation. Reject negative values with `RAISE EXCEPTION`.

    5. `GRANT EXECUTE ON FUNCTION ... TO service_role` for both RPCs.

    6. Header comment block referencing Phase 82.2 D-04, D-05, D-07, D-08 and pointing at RESEARCH §Backfill / §Coverage query.

    Filename MUST be `20260513b_pipeline_health.sql` (b suffix — Plan 01 uses `a`).
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260513b_pipeline_health.sql && grep -q "CREATE TABLE public.pipeline_health" supabase/migrations/20260513b_pipeline_health.sql && grep -q "stage0_coverage_24h" supabase/migrations/20260513b_pipeline_health.sql && grep -q "stage0_backfill_candidates" supabase/migrations/20260513b_pipeline_health.sql</automated>
  </verify>
  <done>Migration file contains table DDL, both RPCs, RLS, and grants.</done>
</task>

<task type="auto">
  <name>Task 2: Apply migration via supabase MCP + smoke-test RPCs</name>
  <files>(no file change; remote DDL)</files>
  <action>
    Use supabase mcp `apply_migration` with name `20260513b_pipeline_health`. After apply:

    1. Verify table exists: `SELECT count(*) FROM public.pipeline_health` → returns 0.
    2. Smoke-test the coverage RPC: `SELECT * FROM public.stage0_coverage_24h('verkoop@smeba.nl', 'sales-email')` → returns one row with non-null counters.
    3. Smoke-test the backfill candidates RPC with a small window: `SELECT count(*) FROM public.stage0_backfill_candidates(1)` → returns a count (could be zero if no recent gaps; that's fine).
    4. Smoke-test the input clamp: `SELECT * FROM public.stage0_backfill_candidates(99)` → either clamps silently to 30 or raises. Document behavior.
  </action>
  <verify>
    <automated>Inline — supabase MCP queries return without error.</automated>
  </verify>
  <done>Table + RPCs exist and smoke-tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inngest worker → Supabase | Service role writes (Plan 10 probe inserts pipeline_health rows) |
| Inngest worker → Supabase | Service role calls to stage0_backfill_candidates / stage0_coverage_24h RPCs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82.2-03-01 | Information Disclosure | pipeline_health | mitigate | RLS denies anon/authenticated; service role only |
| T-82.2-03-02 | Tampering | stage0_backfill_candidates window_days arg | mitigate | Server-side clamp to ≤30, reject negative |
| T-82.2-03-03 | Information Disclosure | pipeline_health rows | accept | Only mailbox names + counts; no email content; matches D-08 PII posture |
</threat_model>

<verification>
- Migration applied; table visible in pg_tables
- Both RPCs callable by service role
- Window-clamp behavior documented
</verification>

<success_criteria>
- Plan 09 backfill function calls `stage0_backfill_candidates` without further schema work
- Plan 10 coverage probe writes pipeline_health rows without further schema work
</success_criteria>

<output>
After completion, create `.planning/phases/82.2-stage-0-telemetry-coverage-fix-v8-stabilisation/82.2-03-SUMMARY.md`
</output>
