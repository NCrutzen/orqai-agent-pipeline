# Phase 82.2: Stage 0 telemetry coverage fix - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-12
**Phase:** 82.2-stage-0-telemetry-coverage-fix-v8-stabilisation
**Areas discussed:** Emit-point architecture, Backfill strategy + sources, Suspected-branch verification, Coverage guard / regression prevention

---

## Emit-point architecture

| Option | Description | Selected |
|--------|-------------|----------|
| Single emit at function exit | Refactor so every code path falls through to ONE `emitPipelineEvent` call. New branches cannot forget to emit. | ✓ |
| Add emit to each existing branch | Locate missing-emit branches, add inline. Minimal diff; same bug class returns on future branches. | |
| Wrapper / step.run boundary emit | Wrap the safety-decision step; emit automatically based on returned decision. Biggest refactor; replay-safety questions. | |

**User's choice:** Single emit at function exit (D-01)

| Option | Description | Selected |
|--------|-------------|----------|
| Preserve all three event sub-types | operator-override, budget-breach, main-path stay distinguishable. Safer for downstream consumers. | ✓ |
| Collapse to one canonical Stage-0 event | One shape with a `reason` field. More scope. | |
| You decide | Defer to planner | |

**User's choice:** Preserve all three (D-02)

---

## Backfill strategy + sources

| Option | Description | Selected |
|--------|-------------|----------|
| Inngest one-shot function | Event-triggered, replay-safe, idempotent UPSERT, re-runnable. | ✓ |
| web/scripts/ one-shot Node script | Simpler; no Inngest plumbing; no retry/checkpoint. | |
| SQL migration | Fastest; idempotency harder; unknown_legacy logic awkward in pure SQL. | |

**User's choice:** Inngest one-shot function (D-03)

| Option | Description | Selected |
|--------|-------------|----------|
| automation_runs first, then unknown_legacy | Reconstruct decision='safe' by inference from Stage 1 having run; mark gaps as unknown_legacy. | ✓ |
| Re-run safety logic | Most accurate; expensive; LLM cost; divergence risk if rules changed. | |
| Conservative unknown_legacy for all gaps | Simplest; loses signal that emails were safe by inference. | |

**User's choice:** automation_runs first, then unknown_legacy (D-04)

| Option | Description | Selected |
|--------|-------------|----------|
| 30 days back from worker-fix deploy | Locked in must-haves; aligns with V9 corpus window. | ✓ |
| All history | Complete trace; unbounded cost; no operational benefit. | |
| 7 days back only | Cheapest; leaves hole between 7d and 30d. | |

**User's choice:** 30 days back from worker-fix deploy (D-05)

---

## Suspected-branch verification

| Option | Description | Selected |
|--------|-------------|----------|
| Code-read + synthetic injection test | Researcher reads worker; planner writes integration test with known-injection payload; asserts pipeline_events row with decision='suspected'. | ✓ |
| Code-read only | Determine if conditions realistically fire; no synthetic test. | |
| Production telemetry probe | Counter on "considered" vs emitted suspected; empirical answer over a week. Defers verification. | |

**User's choice:** Code-read + synthetic injection test (D-06)

---

## Coverage guard / regression prevention

| Option | Description | Selected |
|--------|-------------|----------|
| Inngest cron coverage probe | Scheduled function querying last 24h; alerts if <99% on any mailbox. | ✓ |
| Inline integration test in worker test suite | Catches code-level regression at PR time; misses infra/config divergence. | |
| Both: test + cron probe | Defence in depth. | |
| Just measure once at 7d | Cheapest; least durable; bug class returns silently. | |

**User's choice:** Inngest cron coverage probe (D-07)
**Note:** The synthetic injection test from D-06 already provides code-level regression cover for one specific branch, so a pure cron probe was sufficient — the "both" option would have been redundant.

| Option | Description | Selected |
|--------|-------------|----------|
| Hourly business hours, write to pipeline_health table | Per CLAUDE.md cron default; new health table. | |
| Daily 09:00, write to STATE.md | One probe/day; appends session-note; surfaces via /gsd-progress. | ✓ |
| Hourly + Slack/email on breach | Active alerting. Out of scope for telemetry hygiene. | |

**User's choice:** Daily 09:00, write to STATE.md (D-08)

---

## Claude's Discretion

- Exact UPSERT key for the backfill (likely `(email_id, stage, swarm_type)`)
- Whether the daily probe additionally persists coverage history in a `pipeline_health` table (D-08 only requires STATE.md as breach surface)
- Test framework choice for D-06 — match existing `web/lib/inngest/functions/__tests__/` pattern

## Deferred Ideas

- Active alerting (Slack/email/Twilio) on coverage breach
- Collapsing the three Stage-0 event sub-types into one canonical shape
- Backfilling beyond 30 days
- Persistent `pipeline_health` table for long-term coverage trends
