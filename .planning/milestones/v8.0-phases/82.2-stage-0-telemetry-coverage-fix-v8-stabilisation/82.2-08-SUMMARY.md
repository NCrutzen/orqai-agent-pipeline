---
phase: 82.2-stage-0-telemetry-coverage-fix-v8-stabilisation
plan: 08
type: execute
status: complete
requirements: [TELE-COV-01, CONS-01]
key_links:
  pipeline_events: stage=0 + stage=1 rows present for all 5 smoke emails
  d_a_bug_closure: confirmed on debiteuren@smeba.nl (whitelist auto-reply email shows stage=0)
---

# Plan 82.2-08 — End-to-end smoke pass

## Outcome — APPROVED by operator (2026-05-12)

Production deploy `1b53e13` went live on Vercel at ~10:30 UTC. Operator sent
synthetic emails through 5 active debtor mailboxes; all 5 produced the
expected `pipeline_events` stage=0 + stage=1 rows with `emit_source='main-path'`,
including the deterministic whitelist-match D-A bug-closure gate on
`debiteuren@smeba.nl`.

## Smoke matrix

| Email | Outlook source_id | Mailbox | Stage 0 | Stage 1 | Notes |
|---|---|---|---|---|---|
| whitelist | `...Ch3jAKwAA` | smeba.nl | safe / main-path | `ooo_temporary` | Subject hit `SUBJECT_AUTO_REPLY`, body promoted to OoO. categorize+archive completed, icontroller_delete queued (pending). **D-A bug-closure proven.** |
| happy A | `...Ch3jAKAAA` | smeba.nl | safe / main-path | unknown | Bulk-review |
| happy B | `...Ch3jAJQAA` | smeba.nl | safe / main-path | unknown | Bulk-review |
| happy | `...K9hV0wAA` | fire-control.nl | safe / main-path | unknown | Bulk-review |
| happy | `...Js6_HQAA` | berki.nl | safe / main-path | unknown | Bulk-review |

`automation_runs` window 10:30–10:45 UTC: 9 completed, 4 predicted, 1 pending. **Zero failed.**

## Coverage caveats

- **3 mailboxes not directly tested**: `debiteuren@smeba-fire.be`, `debiteuren@sicli-noord.be`, `debiteuren@sicli-sud.be`. Operator noted suspicion that `smeba-fire.be` is forwarded to `smeba.nl` at the Outlook level — investigation pending. Sicli-Noord and Sicli-Sud are unverified and may be re-smoked later if desired.
- **Sales-email (`verkoop@smeba.nl`) not smoked** — its ingest path is SugarCRM, not Outlook, and Plan 07's safety-ordering rewire targets the debtor-email swarm only. The sales-email ingest route received a payload-alignment edit (Plan 07) but the smoke didn't exercise it.

## Deferred items

1. **Duplicate `automation_runs` Stage-0 rows** on happy smeba A/B and the whitelist email. `pipeline_events` has the canonical single row (UNIQUE index works), but `automation_runs` shows two `stage_0_safety` `completed` rows per email seconds apart. Suspected cause: ingest placeholder row + worker fresh INSERT both reaching the table when the placeholder isn't being matched by the worker's `UPDATE ... WHERE id=? AND status='pending'`. Non-blocking for this phase (Stage 0 telemetry is correct); flagged for follow-up.

## Operator approval

> "Approved" — 2026-05-12, with note that smeba-fire.be forwarding to smeba.nl is suspected and under investigation. Wave 4 (backfill + probe) cleared to proceed.
