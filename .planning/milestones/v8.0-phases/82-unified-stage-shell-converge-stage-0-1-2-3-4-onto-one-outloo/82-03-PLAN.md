---
phase: 82-unified-stage-shell
plan: 03
type: execute
wave: 3
depends_on: [82-01, 82-02]
files_modified:
  - web/app/(dashboard)/automations/[swarm]/stage-2/page.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-2/__tests__/page.test.tsx
autonomous: true
requirements: []
decisions_addressed: [D-02, D-14, D-15, D-17]
must_haves:
  truths:
    - "/automations/debtor-email/stage-2 renders unified shell (chip strip + mailbox filter + empty row list + empty detail pane)"
    - "Phase 81 D-12 tagging-failures count banner preserved ABOVE the row list per OQ-3 resolution (banner-above, not folded into empty-state copy)"
    - "Debtor-email gets the ↗ Open link to /open-tagging-failures; other swarms get em-dash fallback"
    - "loadStage2WeeklyCount(admin) loader call preserved (head-count pattern from Phase 81-02)"
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/stage-2/page.tsx"
      provides: "Stage 2 page rendering unified shell with empty row list + tagging-failures banner"
      contains: "loadStage2WeeklyCount"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-2/__tests__/page.test.tsx"
      provides: "RTL test asserting V3 verification check"
      contains: "tagging|customer-mapping"
  key_links:
    - from: "stage-2/page.tsx"
      to: "_shell/{row-list,mailbox-filter,selection-context,detail-pane}.tsx"
      via: "imports + unified shell composition"
      pattern: "from .*_shell/"
    - from: "stage-2/page.tsx"
      to: "stage-2/_lib/load-stage-2-weekly-count.ts"
      via: "loader call preserved from Phase 81-02"
      pattern: "loadStage2WeeklyCount"
---

<objective>
Migrate Stage 2 to consume the unified `_shell/` library. Same shape as Stage 0 (empty row list) but with the Phase 81 D-12 tagging-failures count banner preserved above.

Purpose: V3 verification check + D-15/D-17. Preserves Phase 81 D-12 ergonomics while folding Stage 2 into the unified shell. OQ-3 resolved as "banner-above" — future-proofs for Phase 77 when real Stage 2 data arrives.
Output: Stage 2 page renders unified shell + tagging-failures count banner + ↗ link (debtor-email) / em-dash (other swarms).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-unified-stage-shell-converge-stage-0-1-2-3-4-onto-one-outloo/82-CONTEXT.md
@.planning/phases/82-unified-stage-shell-converge-stage-0-1-2-3-4-onto-one-outloo/82-PATTERNS.md
@.planning/phases/81-fold-stage-1-bulk-review-into-the-stage-keyed-shell-close-th/81-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite stage-2/page.tsx to consume _shell/ + preserve tagging-failures banner</name>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-2/page.tsx (current 90 LOC — Phase 81-02 shape)
    - web/app/(dashboard)/automations/[swarm]/stage-2/_lib/load-stage-2-weekly-count.ts (head-count loader from Phase 81-02)
    - web/app/(dashboard)/automations/[swarm]/stage-2/__tests__/page.test.tsx (existing test from Phase 81-02 — update, don't replace)
    - web/app/(dashboard)/automations/[swarm]/stage-0/page.tsx (reference: the pattern Plan 82-02 established)
    - .planning/phases/82-.../82-PATTERNS.md (lines 546-555 — Stage 2 preserve banner pattern)
  </read_first>
  <behavior>
    - Test 1: rendered page shows tagging-failures count banner with the count from `loadStage2WeeklyCount` (e.g., "Customer-mapping issues this week: 7" or equivalent existing copy).
    - Test 2: for swarmType='debtor-email', the banner includes an ↗ link with href containing the existing destination (preserve verbatim from current page — likely something like the deferred-runs surface).
    - Test 3: for swarmType='sales-email' (other swarm), the link is replaced with em-dash "—" per Phase 81-02 decision.
    - Test 4: empty row list renders with copy "No rows yet — Stage 2 awaits backend wiring in a follow-up phase." (D-15) OR Phase 77-aligned copy — preserve existing wording if more specific.
    - Test 5: MailboxFilter trigger renders.
    - Test 6: existing Phase 81-02 banner head-count assertion (from current test) still passes.
  </behavior>
  <action>
    Read the CURRENT `stage-2/page.tsx` and extract:
    - The banner JSX (count + ↗ link + em-dash branching) — preserve verbatim into a `<Stage2CountBanner count={n} swarmType={s} />` inline component.
    - The `loadStage2WeeklyCount(admin)` call — keep at top of the new page.

    Read the CURRENT `stage-2/__tests__/page.test.tsx` and identify which assertions cover the count banner. Preserve those assertions; ADD new assertions for unified-shell mount points (RowList, MailboxFilter, UnifiedDetailPane).

    Rewrite `web/app/(dashboard)/automations/[swarm]/stage-2/page.tsx` to follow the Stage 0 pattern (Plan 82-02) but with these differences:
    - `currentStage={2}` on the StageTabStrip
    - `activeStage={2}` on the UnifiedDetailPane
    - Call `const stage2Count = await loadStage2WeeklyCount(admin);` before render
    - Render `<Stage2CountBanner count={stage2Count} swarmType={swarmType} />` ABOVE the row list (OQ-3 resolution: banner-above, not folded into empty-state copy)
    - Empty-state copy: prefer the existing Phase 81-02 copy if it's more specific than D-15's generic; otherwise use D-15's "No rows yet — Stage 2 awaits backend wiring in a follow-up phase."
    - Stage 2 chip strip: single "All" disabled chip (count=0), same as Stage 0
    - Mount `<SelectionProvider rowIds={[]} initialSelectedId={sp.selected ?? null}>`
    - Do NOT mount `<AutomationRealtimeProvider>` (Stage 2 has no channel today — RESEARCH §Realtime)

    Update `web/app/(dashboard)/automations/[swarm]/stage-2/__tests__/page.test.tsx`:
    - Preserve all existing assertions covering the count banner (Phase 81-02 head-count test).
    - Add assertions covering Tests 4-5 above (empty row list copy, MailboxFilter trigger present).
    - For Test 3 (sales-email em-dash): add a separate `it()` block that mocks `loadSwarm` to return `swarm_type: 'sales-email'`.
  </action>
  <verify>
    <automated>cd web && npx vitest run --no-coverage 'app/(dashboard)/automations/[swarm]/stage-2/__tests__/page.test.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - `loadStage2WeeklyCount` still called: `grep -q "loadStage2WeeklyCount" web/app/\(dashboard\)/automations/\[swarm\]/stage-2/page.tsx`
    - `_shell/` imports present: `grep -c "from \"../_shell/" web/app/\(dashboard\)/automations/\[swarm\]/stage-2/page.tsx` returns at least 5
    - Em-dash branching preserved for non-debtor-email: `grep -E "swarm_type.*===.*debtor-email|swarmType.*===.*debtor-email" web/app/\(dashboard\)/automations/\[swarm\]/stage-2/page.tsx`
    - No "Bulk Review" copy: `! grep -i "bulk review" web/app/\(dashboard\)/automations/\[swarm\]/stage-2/page.tsx`
    - No realtime provider: `! grep "AutomationRealtimeProvider" web/app/\(dashboard\)/automations/\[swarm\]/stage-2/page.tsx`
    - All vitest cases green.
    - `cd web && npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Stage 2 renders unified shell with tagging-failures count banner preserved. V3 verification check satisfied. Phase 81-02 ergonomics carried forward.</done>
</task>

</tasks>

<verification>
- Existing Phase 81-02 head-count loader (`load-stage-2-weekly-count.ts`) unchanged.
- Phase 81-02 RTL test assertions still pass.
- TypeScript clean.
</verification>

<success_criteria>
- V3 verification check satisfied.
- D-15 + D-17 honored.
- OQ-3 resolved as banner-above.
- Phase 81-02 em-dash fallback for non-debtor-email swarms preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/82-unified-stage-shell-converge-stage-0-1-2-3-4-onto-one-outloo/82-03-SUMMARY.md`
</output>
