---
phase: 82.7.3
plan: 03
type: execute
wave: 2
depends_on: ["82.7.3-01", "82.7.3-02"]
files_modified:
  - web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-0/page.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-2/page.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-3/client-shell.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-4/client-shell.tsx
autonomous: true
requirements: [G-03, G-04]
tags: [ui, layout, row-list, mailbox-chip, stage-0, stage-1, stage-2, stage-3, stage-4]

must_haves:
  truths:
    - "The split-pane grid template in all 5 stage shells is `gridTemplateColumns: 'minmax(640px, 1fr) 540px'` (was `'minmax(0, 1fr) 460px'`). The list column has a 640px floor protecting the 5-column row grid (140+200+1fr+150+auto); the detail pane gets a fixed 540px (≈ +80px, +17% headroom over the previous 460px) so the `Submit override (Stage N) · Cancel override · Reject` footer fits on one line."
    - "`RowList` accepts an optional `mailboxLabels?: Record<number, string>` prop. When `mailboxLabels` is provided AND `row.mailbox_id != null` AND a label is resolved, the recipient column (200px) renders a small mono chip with `var(--v7-radius-pill)` BEFORE the sender text. When the label cannot be resolved, NO chip renders (graceful degrade per CONTEXT H-04) — no placeholder dash, no error."
    - "Each of the 5 stage call-sites threads its already-loaded `mailboxLabels` variable into `<RowList mailboxLabels={mailboxLabels} ... />`. The `loadMailboxLabels` loader is reused (no new loader)."
    - "On Stage 1 noise tab with the current 459-row dataset, ≥ 95% of rendered rows show a non-empty mailbox chip. Same holds on Stage 0, Stage 3, Stage 4 row-list mounts (visual spot-check)."
    - "No console errors fire on rows where `mailbox_id === null`."
    - "The detail-pane footer (`_shell/detail-pane.tsx`) is UNCHANGED in this plan — only the parent grid widens. `var(--v7-lime)` on the Submit button stays."
    - "All 5 stage tabs (0, 1, 2, 3, 4) get the same grid-template change for visual parity (RESEARCH recommends including Stage 2 even though its detail-pane content is Phase 82.3's; the grid is shared infrastructure)."
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx"
      provides: "RowListProps extended with optional `mailboxLabels`; recipient column renders mono pill chip when label resolves"
      contains: "mailboxLabels"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx"
      provides: "Grid widened to (minmax(640px,1fr), 540px); mailboxLabels threaded to RowList"
      contains: "minmax(640px, 1fr) 540px"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-0/page.tsx"
      provides: "Grid widened (matching client-shell pattern); mailboxLabels threaded to RowList"
      contains: "minmax(640px, 1fr) 540px"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-2/page.tsx"
      provides: "Grid widened for visual parity across stage tabs"
      contains: "minmax(640px, 1fr) 540px"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-3/client-shell.tsx"
      provides: "Grid widened; mailboxLabels threaded to RowList"
      contains: "minmax(640px, 1fr) 540px"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-4/client-shell.tsx"
      provides: "Grid widened; mailboxLabels threaded to RowList"
      contains: "minmax(640px, 1fr) 540px"
  key_links:
    - from: "5 stage page/client-shell <RowList> call-sites"
      to: "RowList.mailboxLabels prop"
      via: "mailboxLabels={mailboxLabels} prop threading (variable already in scope from loadMailboxLabels)"
      pattern: "mailboxLabels=\\{mailboxLabels\\}"
    - from: "row-list.tsx recipient column render"
      to: "mailboxLabels[row.mailbox_id]"
      via: "Conditional chip render — only when mailbox_id != null AND label resolves"
      pattern: "mailboxLabels\\?\\.\\[row\\.mailbox_id\\]"
    - from: "stage-N grid template"
      to: "detail-pane footer fits 3 buttons"
      via: "540px right column (≈80px headroom over previous 460px) + 640px list floor"
      pattern: "minmax\\(640px, 1fr\\) 540px"
---

<objective>
Implement G-03 (footer action row fully visible — widen detail pane via
parent grid template) and G-04 (mailbox label chip on every row, all
stage tabs) per locked decisions H-03 + H-04 in CONTEXT.md.

Purpose: Operator UAT 2026-05-18 — `Reject` is truncated past the right
edge of the detail-pane footer at the current 460px width, and the
operator must open the detail-pane to discover which mailbox a list row
belongs to (data exists — `Row.mailbox_id` is populated and
`loadMailboxLabels` is already loaded at the page level — but the
renderer does not surface it).

Output: Six single-file edits — one `RowListProps` extension + chip
render (`row-list.tsx`), five identical grid-template + prop-thread
changes across the 5 stage shells.

This plan is **Wave 2** (depends on `82.7.3-01` + `82.7.3-02`) so that
operator UAT after Wave 1 confirms the canonical-footer state (G-01)
before the pane widens — easier to verify G-03 against a known-good
footer baseline. The plans are file-disjoint from Wave 1 (Wave 1 touches
detail-pane components; Wave 2 touches grid containers and row-list);
the dependency is purely a UAT sequencing aid, not a code dependency.

**Regression guard:** `_shell/detail-pane.tsx` is NOT in `files_modified`
— the footer is untouched, `var(--v7-lime)` survives. Existing
RowList `feedbackMap` / `rightEdgeSlot` / `emptyState` props are
preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/82.7.3-detail-pane-and-stage-1-list-uat-followups-round-2/82.7.3-SPEC.md
@.planning/phases/82.7.3-detail-pane-and-stage-1-list-uat-followups-round-2/82.7.3-CONTEXT.md
@.planning/phases/82.7.3-detail-pane-and-stage-1-list-uat-followups-round-2/82.7.3-RESEARCH.md
@.planning/phases/82.7.3-detail-pane-and-stage-1-list-uat-followups-round-2/82.7.3-PATTERNS.md
@web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx
@web/app/(dashboard)/automations/[swarm]/_shell/_lib/types.ts
@web/app/(dashboard)/automations/[swarm]/_shell/_lib/load-mailbox-labels.ts
@web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx
</context>

<interfaces>
<!-- row-list.tsx — current RowListProps (presentation primitive): -->

```tsx
export interface RowListProps {
  rows: Row[];
  emptyState: EmptyState;
  rightEdgeSlot?: (row: Row) => ReactNode;
  feedbackMap?: Record<string, "confirm" | "override" | "unclear" | null>;
}
```

<!-- TARGET RowListProps (H-04 — add OPTIONAL prop, no breaking change): -->

```tsx
export interface RowListProps {
  rows: Row[];
  emptyState: EmptyState;
  rightEdgeSlot?: (row: Row) => ReactNode;
  feedbackMap?: Record<string, "confirm" | "override" | "unclear" | null>;
  /** H-04: id → short alias resolved server-side via loadMailboxLabels.
   *  Optional so existing call-sites that don't thread it compile unchanged. */
  mailboxLabels?: Record<number, string>;
}
```

<!-- row-list.tsx — current sender column render (200px column, ~L141-153): -->

```tsx
<span
  data-testid="row-sender"
  style={{
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
    fontSize: 12,
    fontFamily: "var(--font-mono)",
  }}
>
  {r.from_name ?? r.from_email ?? "(unknown sender)"}
</span>
```

<!-- TARGET render (chip prepended; reuse StageBadge styling at L201-226): -->

```tsx
<span
  data-testid="row-sender"
  style={{
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
    fontSize: 12,
    fontFamily: "var(--font-mono)",
    display: "flex",
    alignItems: "center",
    gap: 6,
    minWidth: 0,
  }}
>
  {mailboxLabels && r.mailbox_id != null && mailboxLabels[r.mailbox_id] && (
    <span
      data-testid="row-mailbox-chip"
      style={{
        flexShrink: 0,
        display: "inline-flex",
        alignItems: "center",
        padding: "1px 6px",
        fontSize: 10,
        fontFamily: "var(--font-mono)",
        background: "var(--v7-panel-2)",
        color: "var(--v7-text-muted)",
        border: "1px solid var(--v7-line)",
        borderRadius: "var(--v7-radius-pill)",
        lineHeight: 1.4,
        whiteSpace: "nowrap",
      }}
    >
      {mailboxLabels[r.mailbox_id]}
    </span>
  )}
  <span style={{ overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap", minWidth: 0 }}>
    {r.from_name ?? r.from_email ?? "(unknown sender)"}
  </span>
</span>
```

The chip is `flexShrink: 0` so the sender text truncates first (preserves
existing ellipsis behaviour). The chip render is guarded by THREE
conditions: `mailboxLabels` provided, `mailbox_id != null`, AND the
label string resolved (truthy). If any fails → no chip (graceful
degrade per CONTEXT H-04 boundary: not a placeholder dash, just
absence).

<!-- Grid template change — identical across 5 files: -->

CURRENT (in all 5 stage shells — verified by RESEARCH grep):
- `stage-0/page.tsx:243`
- `stage-1/client-shell.tsx:220`
- `stage-2/page.tsx:253`
- `stage-3/client-shell.tsx:225`
- `stage-4/client-shell.tsx:215`

```tsx
gridTemplateColumns: "minmax(0, 1fr) 460px",
```

TARGET (identical edit, 5 occurrences):

```tsx
gridTemplateColumns: "minmax(640px, 1fr) 540px",
```

The `minmax(640px, 1fr)` floor protects the 5-column row grid
(140+200+1fr+150+auto ≈ 634px minimum). The `540px` right column gives
~80px headroom over the previous 460px to fit `Submit override (Stage 1)`
+ `Cancel override` + `Reject` on one line.

<!-- mailboxLabels threading — pattern at each of the 5 call-sites: -->

`loadMailboxLabels(admin, swarmType)` is ALREADY called at each stage
page (per RESEARCH §"H-04 plumbing" — confirmed via grep), and the
resulting `mailboxLabels` variable is ALREADY threaded into
`<UnifiedDetailPane mailboxLabels={mailboxLabels} ... />`. Surgery is
adding the SAME prop to the sibling `<RowList ... />` mount:

```tsx
<RowList
  rows={...}
  emptyState={...}
  rightEdgeSlot={...}
  feedbackMap={...}
  mailboxLabels={mailboxLabels}  /* ← ADD */
/>
```

The exact RowList call-site in each of the 5 files lives inside the
same parent `<div style={{ display: "grid", gridTemplateColumns: ... }}>`
as the grid-template change above.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend RowListProps with optional `mailboxLabels` and render chip in recipient column (H-04 core)</name>
  <files>web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx (full file — RowListProps interface at L22-32, sender column at L141-153, StageBadge analog at L201-226)
    - web/app/(dashboard)/automations/[swarm]/_shell/_lib/types.ts (Row type — confirm `mailbox_id: number | null`)
    - web/app/(dashboard)/automations/[swarm]/_shell/_lib/load-mailbox-labels.ts (loader signature → `Record<number, string>`)
    - .planning/phases/82.7.3-detail-pane-and-stage-1-list-uat-followups-round-2/82.7.3-PATTERNS.md (H-04 chip styling — reuse StageBadge tokens; flex-shrink:0 on chip)
  </read_first>
  <behavior>
    - `RowListProps` gains an OPTIONAL `mailboxLabels?: Record<number, string>` field. Existing call-sites that don't pass it compile unchanged.
    - When `mailboxLabels` is passed AND `row.mailbox_id != null` AND `mailboxLabels[row.mailbox_id]` is a truthy string → a small mono pill chip renders BEFORE the sender text in the 200px recipient column.
    - When any of the three conditions fails → NO chip renders (no placeholder dash, no error). The sender text renders alone as before.
    - The chip uses ONLY existing brand tokens: `var(--v7-panel-2)` background, `var(--v7-text-muted)` text, `var(--v7-line)` border, `var(--v7-radius-pill)` radius, `var(--font-mono)` font.
    - The chip is `flexShrink: 0` so on long sender strings the sender text truncates first (existing ellipsis behaviour preserved).
    - The chip carries `data-testid="row-mailbox-chip"` for downstream assertion.
    - No console errors fire when a row has `mailbox_id: null`.
  </behavior>
  <action>
    1. Open `web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx`.
    2. Locate the `RowListProps` interface (~L22-32). Add the optional `mailboxLabels?: Record<number, string>` field at the end, with the JSDoc comment from the `<interfaces>` block above.
    3. Destructure the new prop in the component signature alongside the existing `rows`, `emptyState`, `rightEdgeSlot`, `feedbackMap` props.
    4. Locate the sender column render at ~L141-153 (the `<span data-testid="row-sender">` block). Replace it with the TARGET render in `<interfaces>` above:
       - Convert the outer `<span>` to `display: flex` with `alignItems: center`, `gap: 6`, `minWidth: 0` (so the inner sender span can truncate).
       - Prepend a conditional chip render guarded by `mailboxLabels && r.mailbox_id != null && mailboxLabels[r.mailbox_id]`.
       - Wrap the existing sender text in an inner `<span>` with the existing `overflow: hidden; textOverflow: ellipsis; whiteSpace: nowrap; minWidth: 0` styling so it remains the truncate target.
    5. The chip's inline style MUST use only existing `var(--v7-*)` tokens. NO hex literals, NO new tokens.
    6. The chip MUST carry `data-testid="row-mailbox-chip"`.
    7. DO NOT touch the StageBadge primitive (L201-226), the other 4 columns (140 / 1fr / 150 / auto), the row grid template, or any other prop.
    8. DO NOT remove or rename `feedbackMap`, `rightEdgeSlot`, or `emptyState` — they stay as-is.
  </action>
  <verify>
    <automated>
    cd /Users/nickcrutzen/Developer/agent-workforce &amp;&amp; \
    test "$(grep -c 'mailboxLabels?:' 'web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx')" -ge "1" &amp;&amp; \
    test "$(grep -c 'data-testid=\"row-mailbox-chip\"' 'web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx')" = "1" &amp;&amp; \
    test "$(grep -cE 'mailboxLabels\s*&amp;&amp;\s*r\.mailbox_id\s*!=\s*null' 'web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx')" -ge "1" &amp;&amp; \
    test "$(grep -c 'var(--v7-panel-2)' 'web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx')" -ge "1" &amp;&amp; \
    test "$(grep -c 'var(--v7-radius-pill)' 'web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx')" -ge "1" &amp;&amp; \
    test "$(grep -c 'flexShrink:\s*0' 'web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx')" -ge "1" &amp;&amp; \
    echo "Task 1 grep gates green"
    </automated>
  </verify>
  <acceptance_criteria>
    - `mailboxLabels?:` declared in `RowListProps`.
    - Exactly one `data-testid="row-mailbox-chip"` in the file.
    - Guard `mailboxLabels && r.mailbox_id != null` (or equivalent) present.
    - Brand tokens `var(--v7-panel-2)`, `var(--v7-radius-pill)`, `var(--v7-line)`, `var(--v7-text-muted)` appear at least once in the chip render.
    - `flexShrink: 0` on the chip span (truncate-first behaviour).
    - Existing `feedbackMap`, `rightEdgeSlot`, `emptyState` props unchanged.
    - StageBadge primitive (L201-226) unchanged.
  </acceptance_criteria>
  <done>
    `row-list.tsx` accepts an optional `mailboxLabels` prop and renders a small mono pill chip before the sender text when a label resolves. Null `mailbox_id` degrades gracefully.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Widen split-pane grid template + thread mailboxLabels into RowList across all 5 stage shells (H-03 + H-04 plumbing)</name>
  <files>
    web/app/(dashboard)/automations/[swarm]/stage-0/page.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-2/page.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-3/client-shell.tsx,
    web/app/(dashboard)/automations/[swarm]/stage-4/client-shell.tsx
  </files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx (line 220 — grid template; line ~267 — UnifiedDetailPane already gets mailboxLabels; the sibling RowList mount nearby)
    - web/app/(dashboard)/automations/[swarm]/stage-0/page.tsx (line 243 — grid template)
    - web/app/(dashboard)/automations/[swarm]/stage-2/page.tsx (line 253 — grid template; RESEARCH §Open Question 2 recommends including Stage 2 for visual parity)
    - web/app/(dashboard)/automations/[swarm]/stage-3/client-shell.tsx (line 225 — grid template)
    - web/app/(dashboard)/automations/[swarm]/stage-4/client-shell.tsx (line 215 — grid template)
    - .planning/phases/82.7.3-detail-pane-and-stage-1-list-uat-followups-round-2/82.7.3-RESEARCH.md (H-03 + H-04 plumbing — confirms `mailboxLabels` already loaded + threaded to UnifiedDetailPane at each call-site)
  </read_first>
  <behavior>
    - Each of the 5 files has its `gridTemplateColumns: "minmax(0, 1fr) 460px"` line changed to `gridTemplateColumns: "minmax(640px, 1fr) 540px"` (identical edit, 5 occurrences total — zero remaining `460px` matches in `_shell/` or stage page files after the edit).
    - Each of the 5 files has its `<RowList ... />` mount extended with `mailboxLabels={mailboxLabels}` (the variable is already in scope from the existing `loadMailboxLabels` call + threading to `UnifiedDetailPane` — reuse the same variable).
    - The `<UnifiedDetailPane mailboxLabels={mailboxLabels} ... />` prop and all other props on RowList and UnifiedDetailPane are UNCHANGED.
    - `_shell/detail-pane.tsx` is NOT modified.
  </behavior>
  <action>
    For EACH of the 5 files listed:

    1. Open the file.
    2. Locate the `<div>` with `style={{ display: "grid", gridTemplateColumns: "minmax(0, 1fr) 460px", ... }}`. Change the `gridTemplateColumns` value from `"minmax(0, 1fr) 460px"` to `"minmax(640px, 1fr) 540px"`. Do NOT modify the `gap`, `minHeight`, `display`, or any other inline-style property on the same `<div>`.
    3. Locate the `<RowList ... />` mount inside that grid `<div>`. Add the prop `mailboxLabels={mailboxLabels}` to the JSX (the `mailboxLabels` variable is already in scope — it's used a few lines later in `<UnifiedDetailPane mailboxLabels={mailboxLabels} ... />`). Place the new prop alongside the existing props; ordering does not matter.
    4. DO NOT modify any other JSX in the file. DO NOT touch the `<UnifiedDetailPane>` mount, the `loadMailboxLabels(...)` server-side call, or any data-loading code.

    After all 5 edits:

    5. Run `git diff --stat` and confirm exactly 5 files changed in this task (plus `row-list.tsx` from Task 1).
    6. Run the verification greps in `<verify>` below.

    **Special note on Stage 2** (RESEARCH §Open Question 2): SPEC.md says Stage 2 detail-pane CONTENT is out of scope (per Phase 82.3), but the parent grid TEMPLATE is shared infrastructure and is included here for visual parity. If Phase 82.3 later wants a different Stage 2 pane width, it can override locally; for now Stage 2 gets the same 540px treatment as the other 4 tabs. **However:** if Stage 2's `<RowList>` mount does NOT already have `mailboxLabels` loaded in scope (verify via grep for `loadMailboxLabels` in `stage-2/page.tsx`), then SKIP adding `mailboxLabels={mailboxLabels}` to its RowList mount and document this in the SUMMARY. The grid-template change still applies.
  </action>
  <verify>
    <automated>
    cd /Users/nickcrutzen/Developer/agent-workforce &amp;&amp; \
    test "$(grep -lE 'minmax\(0,\s*1fr\)\s*460px' web/app/\(dashboard\)/automations/\[swarm\]/{stage-0/page.tsx,stage-1/client-shell.tsx,stage-2/page.tsx,stage-3/client-shell.tsx,stage-4/client-shell.tsx} 2&gt;/dev/null | wc -l | tr -d ' ')" = "0" &amp;&amp; \
    test "$(grep -lE 'minmax\(640px,\s*1fr\)\s*540px' web/app/\(dashboard\)/automations/\[swarm\]/{stage-0/page.tsx,stage-1/client-shell.tsx,stage-2/page.tsx,stage-3/client-shell.tsx,stage-4/client-shell.tsx} 2&gt;/dev/null | wc -l | tr -d ' ')" = "5" &amp;&amp; \
    test "$(grep -l 'mailboxLabels={mailboxLabels}' web/app/\(dashboard\)/automations/\[swarm\]/{stage-0/page.tsx,stage-1/client-shell.tsx,stage-3/client-shell.tsx,stage-4/client-shell.tsx} 2&gt;/dev/null | wc -l | tr -d ' ')" -ge "4" &amp;&amp; \
    echo "Task 2 grep gates green"
    </automated>
  </verify>
  <acceptance_criteria>
    - Zero remaining `minmax(0, 1fr) 460px` matches across the 5 stage shell files.
    - Exactly 5 `minmax(640px, 1fr) 540px` matches across the 5 stage shell files (one per file).
    - At least 4 of the 5 stage shells have `mailboxLabels={mailboxLabels}` on their `<RowList>` mount (Stage 2 may be excluded if its scope does not load mailboxLabels — document if so).
    - `_shell/detail-pane.tsx` is NOT in the diff.
    - No `loadMailboxLabels` call duplicated or moved — only prop-thread surgery.
    - Existing `<UnifiedDetailPane mailboxLabels={...} ... />` mounts unchanged.
  </acceptance_criteria>
  <done>
    All 5 stage tabs share the wider 540px detail pane (footer fits 3 buttons) and the 640px list-column floor. `mailboxLabels` is threaded into every (or all-but-Stage-2 if scoping requires) `<RowList>` mount so chips render on all stages.
  </done>
</task>

</tasks>

<verification>
- Both task `<automated>` greps pass.
- `git diff --stat` shows exactly 6 files changed: `row-list.tsx` + 5 stage shells.
- `_shell/detail-pane.tsx` is NOT in the diff (footer untouched, `var(--v7-lime)` survives on canonical Submit).
- Manual smoke at 1440×900 viewport:
  - Stage 1 detail-pane footer renders `Submit override (Stage 1) · Cancel override · Reject` on a single line with no horizontal scrollbar inside the pane.
  - Right edge of `Reject` button's bounding box is strictly inside the detail-pane container's content box.
  - Stage 1 noise tab rows show mailbox chips on ≥ 95% of rendered rows (visual count).
  - Stage 0, Stage 3, Stage 4 row-list mounts also show chips on rows with non-null `mailbox_id`.
  - Stage 2 row-list mount: chips present IF `mailboxLabels` was threadable; grid-template change visible regardless.
  - List columns at the 640px floor still render the 5-column row grid without horizontal scroll.
  - No console errors on rows with `mailbox_id: null` (chip simply absent).
</verification>

<success_criteria>
- G-03 satisfied: footer action row (`Submit override · Cancel override · Reject`) fully visible at the post-fix 540px pane width on Stage 0, 1, 3, 4 (and Stage 2 for visual parity).
- G-04 satisfied: ≥ 95% of Stage 1 rows show a mailbox chip; Stage 0/3/4 (and Stage 2 if scoped) also surface mailbox identity per row; null `mailbox_id` degrades to no-chip gracefully.
- No new dependencies; brand tokens preserved; `_shell/detail-pane.tsx` untouched; `var(--v7-lime)` regression guard intact.
- No change to `loadMailboxLabels` loader signature; no Row type extension; no DB migration.
</success_criteria>

<output>
After completion, create `.planning/phases/82.7.3-detail-pane-and-stage-1-list-uat-followups-round-2/82.7.3-03-SUMMARY.md` documenting:
- G-03 + G-04 grep evidence (5x `minmax(640px, 1fr) 540px` matches; 4 or 5 `mailboxLabels={mailboxLabels}` threading sites; one new `mailboxLabels?:` field on RowListProps).
- Stage 2 disposition: was `mailboxLabels` threadable in stage-2/page.tsx, or did the grid-template change land alone? Document explicitly.
- Visual UAT result at 1440×900: footer fits on Stage 0/1/3/4; chip coverage % on Stage 1 noise tab.
- Confirmation that `_shell/detail-pane.tsx` is NOT in the diff (regression guard).
- Confirmation that `loadMailboxLabels` was reused (no new loader / mapper).
</output>
