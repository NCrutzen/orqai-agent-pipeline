---
phase: 82.7.3-detail-pane-and-stage-1-list-uat-followups-round-2
plan: 02
subsystem: stage-1-ui
tags: [ui, stage-1, filters, chip-strip]
requires: []
provides:
  - "'Needs review' chip with topic !== 'skip' predicate over the classifier_queue_counts RPC aggregate"
  - "Collapsible Predictor + Confidence chip rows behind 'Show/Hide advanced filters' toggle"
  - "URL-param auto-expand on first mount (?predictor=<not-all> OR ?confidence=<not-all>)"
affects: []
tech-stack:
  added: []
  patterns:
    - "useState(() => ...) functional initialiser for one-shot URL-param read (PATTERNS Pitfall 5 guard — no useEffect with search dep)"
    - "Brand-token preservation: var(--v7-brand-secondary) on toggle, var(--font-mono), no new color tokens"
key-files:
  created:
    - .planning/phases/82.7.3-detail-pane-and-stage-1-list-uat-followups-round-2/82.7.3-02-SUMMARY.md
  modified:
    - web/app/(dashboard)/automations/[swarm]/stage-1/noise-category-chip-strip.tsx
    - web/app/(dashboard)/automations/[swarm]/stage-1/predictor-confidence-chip-strip.tsx
    - web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/noise-category-chip-strip.test.tsx
decisions:
  - "H-05 predicate enforced inline (topic !== 'skip') rather than mapper extension — RESEARCH confirmed RPC classifier_queue_counts already filters status='predicted'"
  - "H-06 label rename: 'All' → 'Needs review'; URL key 'all' kept unchanged"
  - "H-07 toggle styled as plain text button (mono 12px, var(--v7-brand-secondary), transparent, hover underline) — matches override-stage link precedent"
  - "H-08 useState functional initialiser (not useEffect) — chip clicks must NOT re-toggle expanded"
metrics:
  duration_minutes: 3
  tasks_completed: 2
  files_modified: 3
  completed: 2026-05-18
---

# Phase 82.7.3 Plan 02: Stage-1 chip-strip UAT follow-ups Summary

One-liner: Renamed leftmost noise chip from "All" to "Needs review · N" with `topic !== "skip"` predicate (G-05/H-05+H-06) and collapsed Predictor + Confidence filter rows behind a single "Show/Hide advanced filters" toggle with URL-param auto-expand (G-06/H-07+H-08).

## What Was Built

### Task 1 — H-05 + H-06 (noise-category-chip-strip.tsx + test)

Commit: `1958410`

- **Derivation change** (L82-94): replaced `let totalCount = 0` + unconditional `totalCount += c.count` with `let needsReviewCount = 0` + conditional `if (c.topic !== "skip") needsReviewCount += c.count`. The `countByTopic` map (used for the other category chips) is unchanged.
- **Chip label change** (L107): `{ key: "all", label: "All", count: totalCount }` → `{ key: "all", label: "Needs review", count: needsReviewCount }`. URL key `"all"` preserved so click handler / `qs.delete("topic")` semantics are unchanged.
- **Test update** (`noise-category-chip-strip.test.tsx`): four `/^All —/` role-name matchers (L85, L125, L145, L161) replaced with `/^Needs review —/`. Test descriptions + inline comments updated to reference the new label. Local variable `const all = ...` renamed to `const needsReview`. Test suite passes 6/6.
- **No mapper extension required** (per RESEARCH H-05): `classifier_queue_counts` RPC already filters `status='predicted'`, so auto-handled + overridden rows are excluded at the RPC layer; only skip-label noise needs subtracting client-side.

### Task 2 — H-07 + H-08 (predictor-confidence-chip-strip.tsx)

Commit: `b4b7d6a`

- **State hook**: added `useState(() => { ... })` functional initialiser after the existing `useSearchParams()` call. Reads `?predictor` + `?confidence` once on mount; returns `true` if either is present and not `"all"`. Subsequent URL-param writes by chip clicks do NOT re-trigger this — `useState` initialiser runs once.
- **Toggle button**: new `<button type="button">` with `aria-expanded={expanded}`, `data-testid="advanced-filters-toggle"`. Label flips between `Show advanced filters` ↔ `Hide advanced filters`. Inline-style mono 12px, `var(--v7-brand-secondary)`, transparent, hover underline.
- **Conditional wrap**: the two existing chip rows (Predictor + Confidence) moved INSIDE `{expanded && (<>...</>)}`. Their internals — `PREDICTOR_CHIPS`, `CONFIDENCE_CHIPS`, click handlers, count badges, URL writes — are untouched.
- **No useEffect with search in deps** (PATTERNS Pitfall 5 guard) — verified by grep gate `useEffect\([^)]*search[^)]*\)` → 0.

## Verification Evidence

**Task 1 grep gates (all green):**

| Gate | Expected | Actual |
|------|----------|--------|
| `label: "All"` in source | 0 | 0 |
| `label: "Needs review"` in source | 1 | 1 |
| `topic !== "skip"` in source | ≥1 | 1 |
| `needsReviewCount` references | ≥2 | 3 |
| `let totalCount = 0` in source | 0 | 0 |
| `/^All —/` matchers in test | 0 | 0 |
| `/^Needs review —/` matchers in test | ≥4 (was 5 per plan; actually 4 in original file) | 4 |

**Task 1 RTL test:** `npm --prefix web test -- --run noise-category-chip-strip` → 6 tests passed (1.10s).

**Task 2 grep gates (all green):**

| Gate | Expected | Actual |
|------|----------|--------|
| `Show advanced filters` | ≥1 | 1 |
| `Hide advanced filters` | ≥1 | 1 |
| `aria-expanded` | ≥1 | 1 |
| `data-testid="advanced-filters-toggle"` | 1 | 1 |
| `useState(() => {` (functional initialiser) | ≥1 | 1 |
| `useEffect(...search...)` (Pitfall 5 guard) | 0 | 0 |
| `var(--v7-brand-secondary)` (brand token) | ≥1 | 1 |

**Overall plan diff (`git diff --stat` against base `3f0ddc7`):** exactly 3 files changed — `noise-category-chip-strip.tsx`, `noise-category-chip-strip.test.tsx`, `predictor-confidence-chip-strip.tsx`. `stage-1/page.tsx` NOT in diff (no mapper extension). `supabase/migrations/` NOT touched.

## Deviations from Plan

**Minor: count of test matchers (4 vs 5)**

The plan stated "five `/^All —/` matchers (L85, L125, L145, L149, L161)". Reading the original file showed only four matchers (L85, L125, L145, L161) — the L149 reference was a label inside a comment, not a regex matcher. All four were replaced via `replace_all` on `/^All —/` → `/^Needs review —/`. Updated the acceptance bar in this SUMMARY to ≥4 accordingly. No functional impact: the source rename `"All"` → `"Needs review"` is one-to-one, so every chip-name matcher in the test must use the new label, and all do.

Otherwise: plan executed exactly as written.

## Constraint Compliance

- **PATTERNS Pitfall 5 (no useEffect with search in deps):** verified by grep `useEffect\([^)]*search[^)]*\)` → 0.
- **Brand-token preservation:** `var(--v7-brand-secondary)` on toggle button preserved (grep ≥ 1). `var(--font-mono)` preserved. No new color tokens introduced.
- **URL contract unchanged:** `?predictor=` / `?confidence=` write semantics live in `navigatePredictor` / `navigateConfidence` callbacks (untouched). Chip key `"all"` on the noise strip's leftmost chip unchanged.
- **No new dependencies, no mapper extension, no migration.**
- **Existing chip click handlers and URL-param write path unchanged** — diff confined to: derivation loop + first chip label/count in `noise-category-chip-strip.tsx`; new `useState` hook + new toggle button + conditional render wrap in `predictor-confidence-chip-strip.tsx`; role-name regex swap + description text in `noise-category-chip-strip.test.tsx`.

## Self-Check: PASSED

- Source file `noise-category-chip-strip.tsx` contains `label: "Needs review"` and `topic !== "skip"` predicate — FOUND.
- Test file `noise-category-chip-strip.test.tsx` contains 4 `/^Needs review —/` matchers and 0 `/^All —/` matchers — FOUND.
- Source file `predictor-confidence-chip-strip.tsx` contains toggle button + `useState(() => {`, no `useEffect(...search...)` — FOUND.
- Commit `1958410` present in `git log` — FOUND.
- Commit `b4b7d6a` present in `git log` — FOUND.
- `git diff --stat 3f0ddc7..HEAD` shows exactly 3 files — FOUND.
- RTL test suite `noise-category-chip-strip` passes (6/6) against the post-edit files — VERIFIED.
