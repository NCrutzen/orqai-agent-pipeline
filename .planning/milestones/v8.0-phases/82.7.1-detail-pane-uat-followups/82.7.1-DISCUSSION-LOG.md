---
phase: 82.7.1-detail-pane-uat-followups
log_type: discussion
created: 2026-05-18
purpose: Audit trail of /gsd-discuss-phase Q&A. NOT consumed by downstream agents — see CONTEXT.md for locked decisions.
---

# Phase 82.7.1: Discussion Log

CONTEXT.md was created on 2026-05-16 directly from operator UAT observations
(no Q&A — Claude captured the 5 defects + recommended resolutions). The
`/gsd-discuss-phase 82.7.1` invocation on 2026-05-18 confirmed/locked the
recommendations on the 3 areas where ambiguity remained. E-03 and E-04 were
pre-locked (clear recommendations, no operator pushback).

## Q1 — E-01 Stage 0 override submit path

**Options presented:**
1. **Per-stage Submit inside dirty card** (Recommended) — mirror per-stage cancel-override from 82.7-03. Footer stays Stage-1-exclusive.
2. Footer on every stage tab when anyDirty — breaks 82.6 D-02b.
3. Hybrid — per-stage Submit on 0/2/3/4 + footer on Stage 1.

**Operator selected:** Option 1 (per-stage Submit inside dirty card).

**Why this matters downstream:** Planner threads `onSubmitDirty(stageN)` from
detail-pane → PipelineFlow → StageStep, parallel to the existing
`onCancelDirty(stageN)` callback. New per-stage Submit button renders in the
dirty branch of `stage-step.tsx`, sibling to the existing `cancel override`
link.

## Q2 — E-02 silent Approve visual cue

**Options presented:**
1. **150ms fade-out on pendingRemoval row** (Recommended) — lightest fix, honors silent UX.
2. Approve button spinner / disabled while in-flight — most explicit but heavier.
3. Both — fade-out + spinner.

**Operator selected:** Option 1 (150ms row fade-out).

**Why this matters downstream:** Planner adds CSS transition on row opacity
keyed to `pendingRemovalIds.has(row.id)` in `row-list.tsx`. No state changes
to the Approve button. Fade applies to BOTH Approve and Submit override paths
(per D-05 in CONTEXT.md).

## Q3 — E-05 unlabelled row: which tab?

**Options presented:**
1. Stage 1 (Noise) only.
2. Stage 0 (Safety) only.
3. Both tabs.
4. **Not sure — investigate both.**

**Operator selected:** Option 4 (not sure, investigate both).

**Why this matters downstream:** Researcher must scan BOTH Stage 0 safety-row
and Stage 1 noise-row components for nullable chip rendering paths. Per D-15,
fix the renderer side here regardless of which surface(s) the bug lives on.
If root cause turns out to be data-side requiring historical backfill, split
that into a separate phase.

## Pre-locked (no Q&A needed)

### E-03 — Brand-color dot tooltip
Recommendation in CONTEXT v1 was clear (hover tooltip via existing Radix
pattern, minimum-cost fix). No operator pushback during UAT. Locked as D-06.

### E-04 — Override form button polish
Recommendation in CONTEXT v1 was prescriptive (lime fill on Submit, ghost
button on Discard, padding). Operator's only comment was "could use some
make-up" — recommendation already aligned. Locked as D-09/D-10/D-11.

## Scope creep handled

None. Operator stayed within the 5 captured defects.

## Deferred (per CONTEXT.md)

- 999.8 item 7 (E2E ingest smoke) — separate operator test session.
- E-05 historical backfill — separate phase if root cause is data-side.
- REG / CAP radio card redesign — out of scope.
- E-03 legend strip alternative — rejected in favor of tooltip.
- E-01 footer-on-every-tab alternative — rejected (breaks 82.6 D-02b).
