---
phase: 999.7
slug: stage-0-budget-breach-failures
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-08
---

# Phase 999.7 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (web/) |
| **Config file** | web/vitest.config.ts |
| **Quick run command** | `cd web && npx vitest run lib/stage-0 lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts lib/inngest/functions/__tests__/budget-breach-handler.test.ts` |
| **Full suite command** | `cd web && npx vitest run` |
| **Estimated runtime** | ~30s quick / ~3min full |

---

## Sampling Rate

- **After every task commit:** Run quick command
- **After every plan wave:** Run full suite
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

*Populated by planner — one row per task with automated verify command, fixture path, and expected behavior.*

---

## Wave 0 Requirements

- [ ] `web/lib/stage-0/__tests__/strip-quoted-history.test.ts` — RED tests for strip helper covering 6 fixture categories
- [ ] `web/lib/stage-0/__tests__/fixtures/` — 6 real-shape email fixtures (Outlook NL reply, Gmail NL reply, mobile-client reply, mixed-case `>` quoting, signature directly after quoted block, no quoted history baseline) + 1 12k-token regression sample
- [ ] Update `web/lib/stage-0/__tests__/budget-counter.test.ts` D-14 assertion 5000 → 16000 (RED first, then GREEN with constant bump)
- [ ] Add worker integration test: stripped body is what reaches the LLM call; original body is what the forward-to-classifier event carries

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| 12358 / 12362-token 2026-05-07 production samples no longer breach | D-02 + D-04 | Production data; needs Supabase replay | Run `web/scripts/` replay script against the two stranded `automation_runs` IDs in test environment, assert `status='completed'` and `total_tokens < 16000` |
| Cents-ceiling loop guard still fires on a synthetic runaway | D-08 | Synthetic test relies on Orq response shape | Stub LLM client to return `usage.total_cost_cents: 16`, assert `pipeline/budget_breached` event is emitted |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
