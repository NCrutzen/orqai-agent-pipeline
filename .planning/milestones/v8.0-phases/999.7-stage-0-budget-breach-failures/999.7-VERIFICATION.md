---
phase: 999.7-stage-0-budget-breach-failures
verified: 2026-05-08T07:10:00Z
status: passed
score: 8/8 must-haves verified
overrides_applied: 0
---

# Phase 999.7: Stage 0 budget-breach failures on long emails — Verification Report

**Phase Goal:** Stop Stage 0 safety classifier from rejecting (and stranding) emails that exceed its 5000-token budget. Long emails must advance past Stage 0 instead of being marked failed/budget_breach. Hybrid: raise BUDGET_CEILING_TOKENS to 16000 AND strip quoted reply history before the LLM call.

**Verified:** 2026-05-08T07:10:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `BUDGET_CEILING_TOKENS === 16000` in source AND test asserts new value (D-02) | ✓ VERIFIED | `budget-counter.ts:27` → `export const BUDGET_CEILING_TOKENS = 16000;`. Test `budget-counter.test.ts:60-61` → `expect(BUDGET_CEILING_TOKENS).toBe(16000)`. Zero occurrences of `5000` remain. |
| 2 | Comment block on both ceilings documents role split (cents = runaway-loop guard, tokens = long-email guard) per D-08 | ✓ VERIFIED | `budget-counter.ts:1-24` header contains both literal markers `RUNAWAY-LOOP GUARD` and `LONG-EMAIL GUARD`, plus explicit "Phase 999.7 D-03 / D-08" reference and 2026-05-07 12358/12362 token rationale. |
| 3 | `strip-quoted-history.ts` exists, exports deterministic strip function with empty-string fallback (Pitfall 1 / T-2) | ✓ VERIFIED | `strip-quoted-history.ts:32-64` exports `stripQuotedHistory(body): StripResult`. Empty-input short-circuit (line 33-35), `parser_returned_empty` fallback (line 42-49), `parser_threw` try/catch fallback (line 56-63). Pure function — no I/O, no async. |
| 4 | Worker calls strip helper inside `step.run()` BEFORE the LLM call | ✓ VERIFIED | `stage-0-safety-worker.ts:31` imports `stripQuotedHistory`. Lines 112-114: `await step.run("strip-quoted-history", () => stripQuotedHistory(body))`. Line 119 passes `classifierBody` (= `stripResult.stripped`) to `regexScreen`; line 136 passes it to `llmInjectionVerdict`. Order locked by INTEG-01 test (lines 488-529). |
| 5 | **Pitfall 4** — forward-to-classifier event carries ORIGINAL body, NOT stripped | ✓ VERIFIED | Source: `stage-0-safety-worker.ts:252-255` — explicit Phase 999.7 Pitfall 4 comment, then `body_text: body` (NOT `classifierBody`). Also line 96 (safety_overridden short-circuit) forwards original `body`. INTEG-02 test (lines 531-583) asserts `forwardCall.data.body_text === originalBody` AND `!== strippedBody` — would fail immediately on regression. |
| 6 | The 12k-token regression fixture does NOT breach the 16k ceiling (INTEG-03) | ✓ VERIFIED | `12k-token-real-sample.txt` (18,144 bytes) exists at `web/lib/stage-0/__tests__/fixtures/`. INTEG-03 test (lines 585-651) loads the fixture via real `vi.importActual` strip helper, asserts `result.verdict === 'safe'`, `sendNames` does NOT contain `pipeline/budget_breached`, `status === 'completed'`, `strip_delta_chars < -8000`. PASSING. |
| 7 | Cents-ceiling breach path is untouched and still emits `pipeline/budget_breached` | ✓ VERIFIED | `budget-counter.ts:38-43` — cents check fires first with `cost_cents > 15`. Worker BUDG-01 test (lines 292-333) covers `mockBudget.mockReturnValue({ breached: true, reason: "cost_cents 18 > 15" })` → asserts exactly one `pipeline/budget_breached` event. PASSING. |
| 8 | CLAUDE.md compliance: `inngest.send` not destructured; replay-safe `step.run()` used; no new IDs minted | ✓ VERIFIED | Worker grep: `inngest.send(` called inline at lines 88, 170, 244 — never assigned to a const/destructured. Strip step is wrapped in `step.run("strip-quoted-history", ...)` at line 112 (Pitfall 3 mitigated). Strip helper is pure (no `crypto.randomUUID()`, no `Date.now()`, no random) — no replay-id concern. |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `web/lib/stage-0/budget-counter.ts` | `BUDGET_CEILING_TOKENS=16000`, role-split comments | ✓ VERIFIED | Constant present line 27; role-split header lines 1-24. |
| `web/lib/stage-0/strip-quoted-history.ts` | Deterministic strip function with fallbacks | ✓ VERIFIED | 64 lines, exports `stripQuotedHistory` + `StripResult` interface. |
| `web/lib/inngest/functions/stage-0-safety-worker.ts` | step.run wiring + ORIGINAL body forwarded | ✓ VERIFIED | Strip step at line 112; original body forwarded at lines 96 + 255. |
| `web/lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts` | INTEG-01/02/03/05 tests | ✓ VERIFIED | All four describe blocks present (lines 488, 531, 585, 653). |
| `web/lib/stage-0/__tests__/strip-quoted-history.test.ts` | 10 unit-test cases | ✓ VERIFIED | File exists; 10 it() cases cover all 7 fixtures + parser_returned_empty + determinism + empty input. |
| `web/lib/stage-0/__tests__/budget-counter.test.ts` | Asserts `BUDGET_CEILING_TOKENS === 16000` | ✓ VERIFIED | Line 60-61. Zero `toBe(5000)` occurrences. |
| Fixtures dir (7 files) | 6 reply shapes + 12k regression | ✓ VERIFIED | All 7 files present: outlook-nl-reply, gmail-nl-reply, mobile-client-reply, cited-quoting, signature-after-quote, no-quote-history, 12k-token-real-sample. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Phase scoped test suite green | `cd web && npx vitest run lib/stage-0 lib/inngest/functions/__tests__/stage-0-safety-worker.test.ts lib/inngest/functions/__tests__/budget-breach-handler.test.ts` | 6 files / 41 tests passing, duration 1.83s | ✓ PASS |

### Pre-existing Out-of-Scope Failures

`web/app/(dashboard)/automations/[swarm]/review/__tests__/safety-review-loader.test.ts` and 8 sibling Bulk Review test files fail with `TypeError: admin.schema is not a function`. Verified PRE-EXISTING:

```
git log --oneline -- safety-review-loader.test.ts
5ad38e4 feat(71-03): rewire Bulk Review predicted-row feed to pipeline_events_email_summary
08d3546 feat(70-06): rewire Bulk Review loadPageData feed to pipeline_events
0e591fa feat(64-05): loader branch + outlier enrichment for ?tab=safety
```

No commits from Phase 999.7 (`8f1ba49`, `ae0997c`, `ec88eac`, `cd2776b`, `6d30e5b`, `34f0cf3`, `20fd814`) touch this file. The failure is pre-existing infrastructure (Supabase admin mock missing the `schema()` builder added by Phase 70 dual-write). Out of scope per executor scope-boundary rule.

### Anti-Patterns Found

None. Spot-checks for `TODO|FIXME|placeholder` against the modified Stage 0 files came up clean. Strip helper has explicit defensive fallbacks (not stubs).

### Project Guardrails Compliance

| Guardrail | Status | Evidence |
|-----------|--------|----------|
| D-05/D-06: no signature stripping, no hard tail-truncation | ✓ HONORED | `strip-quoted-history.ts` only invokes `EmailReplyParser.read().getVisibleText()`. No signature regex, no `slice()`/`substring()` on length. Library's signature-stripping mode is NOT engaged (default `read()`). |
| D-07: budget check post-hoc against `total_tokens` after the LLM call | ✓ HONORED | Worker line 158-165: `budgetCheck` runs AFTER `llmInjectionVerdict`, against `llmResult.usage.total_tokens`. No pre-flight tokenizer added. |
| D-09: no automated backfill / replay scripts for stranded rows | ✓ HONORED | No new files under `web/scripts/` related to budget-breach replay. Manual replay deferred to operator (documented in 03-SUMMARY.md "Manual-verification Deferred Items"). |

### Human Verification Required

None. The phase contract is fully covered by automated tests:
- INTEG-01 locks step ordering.
- INTEG-02 locks Pitfall 4 (original body forwarded).
- INTEG-03 locks 12k-token regression at the 16000 ceiling.
- INTEG-05 locks dual-write telemetry.
- BUDG-01 (lines 292-333) locks cents-ceiling untouched.

Post-deploy smoke items listed in 03-SUMMARY ("Manual-verification Deferred Items") are operational confirmations, not phase-gate verifications — they cross the Vercel/Supabase/Inngest production boundary that this phase explicitly does not touch (D-09).

### Gaps Summary

No gaps. All 8 must-haves verified by source inspection AND a passing scoped test suite (41/41 green). Pre-existing `admin.schema` failures in unrelated Bulk Review test files are confirmed PRE-EXISTING (commit history pre-dates Phase 999.7) and out-of-scope per SUMMARY 03.

---

_Verified: 2026-05-08T07:10:00Z_
_Verifier: Claude (gsd-verifier)_
