# Phase 999.7: Stage 0 budget-breach failures on long emails — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-08
**Phase:** 999.7-stage-0-budget-breach-failures
**Areas discussed:** Approach, Pre-flight vs post-hoc check, Runaway-loop guard preservation, Backfill of already-stranded rows

---

## Gray Area Selection

| Option | Description | Selected |
|--------|-------------|----------|
| Approach (raise vs truncate vs hybrid) | Core call: bump ceiling, truncate upstream, or both | ✓ |
| Pre-flight vs post-hoc check | Should budget check run before the LLM call? | ✓ |
| Runaway-loop guard preservation | Phase 64 D-14 intent at 16k ceiling | ✓ |
| Backfill of already-stranded rows | What to do with existing budget_breach rows | ✓ |

---

## Approach

| Option | Description | Selected |
|--------|-------------|----------|
| Hybrid: raise moderately + strip quoted history | Bump ceiling + ingest-time strip; cost discipline plus tail tolerance | ✓ |
| Raise budget only | Single-constant change, accepts marginal LLM cost on long emails | |
| Truncate upstream only | Keep ceiling tight at 5000, rely entirely on strip + window | |

**User's choice:** Hybrid: raise moderately + strip quoted history.
**Notes:** Captured as D-01.

### Ceiling value

| Option | Description | Selected |
|--------|-------------|----------|
| 16000 | ~3.2× current; covers 12k samples with ~30% headroom | ✓ |
| 32000 | ~6.4× current; very generous tail | |
| Keep 5000, rely entirely on truncation | Tightest discipline; fragile on un-truncatable outliers | |

**User's choice:** 16000. Captured as D-02.

### Strip target

| Option | Description | Selected |
|--------|-------------|----------|
| Quoted reply history | Standard `>` blocks, `On X wrote:` dividers | ✓ |
| Signatures / disclaimers | Trailing signature + legal blocks | |
| Hard tail-truncate as safety net | Head+tail window if still over N tokens | |

**User's choice:** Quoted reply history only. Captured as D-04 (positive) and D-05 / D-06 (negatives).

---

## Pre-flight vs post-hoc check

| Option | Description | Selected |
|--------|-------------|----------|
| Keep post-hoc | Continue checking total_tokens after LLM call | ✓ |
| Add pre-flight prompt-token estimate | Estimate prompt tokens before call, short-circuit on outliers | |
| Replace post-hoc with pre-flight | Pre-flight only — removes loop guard | |

**User's choice:** Keep post-hoc. Captured as D-07.

---

## Runaway-loop guard preservation

| Option | Description | Selected |
|--------|-------------|----------|
| Cost ceiling does the loop-guard work | BUDGET_CEILING_CENTS=15 stays the loop guard; document role split | ✓ |
| Split prompt vs completion budgets | Cap completion_tokens separately (~2k) | |
| Add per-run iteration counter | Track call count per run | |

**User's choice:** Cost ceiling does the loop-guard work. Captured as D-08.

---

## Backfill of already-stranded rows

| Option | Description | Selected |
|--------|-------------|----------|
| One-shot replay script | web/scripts/replay-budget-breach-stranded.ts | |
| Leave as-is, surface in Bulk Review | Operators handle via existing budget_breach Kanban rows | ✓ |
| No backfill | Accept stranded rows | |

**User's choice:** Leave as-is, surface in Bulk Review. Captured as D-09.

---

## Claude's Discretion

- Exact regexes / parser approach for quoted-history stripping (lib vs hand-rolled).
- Test fixture coverage (Outlook NL, Gmail NL, mobile clients, threaded vs non-threaded).
- Strip helper file location (`web/lib/stage-0/` vs `web/lib/automations/debtor-email/`).
- Phase 64 D-14 test assertion migration (5000 → 16000).

## Deferred Ideas

- Pre-flight prompt-token estimation.
- Split prompt vs completion budgets.
- Per-run iteration counter.
- Signature / disclaimer stripping.
- Hard tail-truncate safety net.
- Automated backfill of stranded budget_breach rows.
