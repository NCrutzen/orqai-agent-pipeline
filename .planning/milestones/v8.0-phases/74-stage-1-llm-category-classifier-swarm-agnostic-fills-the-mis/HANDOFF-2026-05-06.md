# Phase 74 — Session Handoff (2026-05-06)

**Status:** Code-complete + deployed to production. All 5 plans in 4 waves merged. Smoke-tested live for both swarms.

**Outstanding diagnostic** (for next session): one in-flight test message stuck in `automation_runs.status='pending'`; an older debtor-email row (~9 min before the test) is also stuck in `pending`, suggesting an Inngest-side backlog or a transient worker issue, NOT a Phase 74 code defect. Needs investigation in next session.

---

## What shipped

| Wave | Plan | Status | Production live? |
|---|---|---|---|
| 1 | 74-01 — DB foundation (entity nullable, sales-email seed, orq_agents row, ICONTROLLER_MAILBOXES) | complete | ✓ migrations applied |
| 1 | 74-02 — Stage-0 swarm_type threading | complete | ✓ |
| 2 | 74-03 — Orq agent ritual | complete | ✓ Orq agent ID `01KQY3ZXEX17RSXFV3CMKRS0T7` (`Debtor Team/stage-1`), `enabled=true`, primary + 2 fallbacks all in JSON-Schema mode |
| 3 | 74-04 — classifier-screen-worker (12-test TDD) | complete | ✓ + bug fix: `agent_runs_status_check` extended to allow `predicted`/`failed`; worker now surfaces silent insert failures |
| 4 | 74-05 — Sales-email ingest | complete | ✓ refactored TWICE: SugarCRM-shape → Outlook all-fields → Outlook + Graph fetch (final shape mirrors debtor-email/ingest) |
| Patch | Path A — sales-email all-5-categories → `manual_review` | applied | ✓ pending Phase 75 |

## Production state (verified)

- `swarm_categories` rows for `swarm_type='sales-email'`: 5 (all with `action='manual_review'`)
- `orq_agents` row `stage-1-category-classifier`: `enabled=true`, `orqai_id='01KQY3ZXEX17RSXFV3CMKRS0T7'`
- `agent_runs.entity` nullable, CHECK dropped, `error_message` column present, `agent_runs_status_check` includes `predicted`/`failed`
- `zapier_tools` row `sales-email-ingest`: `target_url=https://agent-workforce-eosin.vercel.app/api/automations/sales-email/ingest`, `auth_secret_env=ZAPIER_INGEST_SECRET`
- ICONTROLLER_MAILBOXES extended with `administratie@fire-control.nl=12` and `debiteuren@smeba-fire.be=5`

## Smoke tests fired this session

| Time (UTC) | Test | Result |
|---|---|---|
| 12:08:49 | First end-to-end via SugarCRM-shape route, `automation_run_id=ea9543f7-...` | ✓ Stage 0 + Stage 1 pipeline_events written, LLM returned `category_key=unknown, confidence=high` (correctly identified as not fitting any of the 4 noise categories), `automation_runs.status=completed`. **However, agent_runs row was MISSING — root cause: CHECK constraint rejected status='predicted'.** Fixed by `phase74_agent_runs_status_extended` migration. |
| ~12:49:00 | Second end-to-end via Outlook+Graph route, `automation_run_id=e4a01beb-d65d-4ad7-82cd-09d999dec125` | ⚠ Route returned `200 OK` with valid `email_id` and Graph-style `source_id` — proves Graph fetch works and email_pipeline.emails + automation_runs rows wrote correctly. **BUT no Stage 0 / Stage 1 pipeline_events appeared after 2+ minutes**, and a separate debtor-email row from ~9 minutes earlier (`dfca7012-b050-47df-82b0-7392ff5a105e`) was ALSO stuck in `pending`. Suggests Inngest-side backlog or transient worker outage, not Phase 74 code. |

## Operator action items still open

1. **Set Vercel env var if not yet present:** `ZAPIER_INGEST_SECRET` — current value confirmed working: `fc671404ced6b5aee690a2f48963cf31f2fb91ece939bdba0af644719d613c1c`. Same value the existing debtor-email/ingest already uses.
2. **Publish the sales-email Zap.** The Outlook+Graph contract uses ONLY:
   - `auth` (literal secret value)
   - `messageId` (Outlook trigger's `1. Id` — Graph-compatible message id)
   - `source_mailbox` (literal `verkoop@smeba.nl`, or omit for default)
3. **Outlook trigger config** in the Zap: New Email at `verkoop@smeba.nl`.
4. **Three-mailbox 24h verification.** Run the SQL probes from `74-05-SUMMARY.md` end of day Thursday 2026-05-07 (operator-stated cadence — they're not working Friday).

## Outstanding diagnostic for next session

**Why are recent automation_runs stuck in `pending`?**

Evidence:
- `dfca7012-...` (debtor-email) — pending, 9.75 min old
- `e4a01beb-...` (sales-email) — pending, 2+ min old
- Older debtor-email rows (`4aefb2d0` at 9 min — `predicted`, `dcd3dddc` at ~19 min — `completed`) processed normally

Hypotheses:
1. **Inngest signing key / event key rotated** — `INNGEST_SIGNING_KEY` or `INNGEST_EVENT_KEY` env var changed, so events emit but Inngest can't verify signatures and silently drops. Check Vercel env + Inngest dashboard for matching keys.
2. **Inngest function suspension or run-quota** — too many failed runs in a short window may have triggered Inngest's auto-suspend on `stage-0/safety-worker`. Inspect via Inngest dashboard.
3. **Latest Vercel deploy missing function registration** — the Outlook+Graph refactor went out at ~12 min ago. Even though `web/app/api/inngest/route.ts` shouldn't have changed, possible the deploy regressed function exports. Probe `https://agent-workforce-eosin.vercel.app/api/inngest` returns 200 (good) but doesn't tell us if `classifier/screen-worker` is in the served-functions list.
4. **Stage-0 worker erroring on swarm_type='sales-email' specifically** — but earlier 12:08:49 sales-email test DID complete the chain successfully under the SAME worker code, so this is unlikely.
5. **Inngest cloud regional incident** — check `status.inngest.com`.

Recommended first step next session: open the Inngest dashboard, look at the run history for `e4a01beb-d65d-4ad7-82cd-09d999dec125` and `dfca7012-b050-47df-82b0-7392ff5a105e` — see whether the events were RECEIVED at all (signing-key issue) or RECEIVED-BUT-NEVER-DISPATCHED (worker suspension) or DISPATCHED-AND-ERRORED (worker bug).

## Phase 75 scope (queued for next session)

Roadmap entry written. Reduced scope vs. originally planned (because Sugar is no longer in the ingest path):

> **Phase 75: Sugar CRM cleanup tool — agent-callable resolve action.** Build an agent-callable tool that takes a SugarCRM Email record id and marks it `archived` (or sends to a designated folder). Used by Bulk Review operators OR by an agent that decides a sales-email noise message should be cleaned up in Sugar after being classified by the Outlook pipeline. NO verdict-worker refactor (Path A — sales-email all-5-categories stay on `action='manual_review'`).

Run `/gsd-spec-phase 75` to begin scoping.

## Files/links of interest

- **Latest Vercel production URL:** `https://agent-workforce-eosin.vercel.app`
- **Orq agent dashboard:** `https://my.orq.ai/cura/agents/01KQY3ZXEX17RSXFV3CMKRS0T7`
- **SPEC:** `.planning/phases/74-.../74-SPEC.md`
- **CONTEXT:** `.planning/phases/74-.../74-CONTEXT.md`
- **All 5 plans + summaries:** `.planning/phases/74-.../74-{01..05}-{PLAN,SUMMARY}.md`
- **Validation:** `.planning/phases/74-.../74-VALIDATION.md`
- **Per-plan rolling commit log:** `git log --oneline | grep '(74-'`

## Memory snapshot for next session

Project memories updated:
- `project_phase74_target_mailboxes.md` — three target mailboxes
- `project_sales_email_source_sugarcrm.md` — **NOTE: now stale.** Sugar is no longer the ingest source; Outlook is. Update or supersede this memory in next session.
