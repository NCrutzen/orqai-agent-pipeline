---
phase: 60
plan: 07
type: execute
wave: 4
depends_on: [60-02, 60-03, 60-04, 60-05, 60-06]
files_modified:
  - web/lib/classifier/cache.ts
  - .planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-07-FLIP-CHECKLIST.md
autonomous: false
requirements: [D-19, D-28]
must_haves:
  truths:
    - "After a documented 1-day clean run with the table-backed whitelist, FALLBACK_WHITELIST is removed from cache.ts (D-28 step 4)"
    - "After ≥14-day shadow window with plausible cron evaluations, operator flips CLASSIFIER_CRON_MUTATE=true in Vercel (D-19)"
    - "Flip-readiness checklist documents: shadow eval count, would-have-promoted spot-check, demotion-event review, runbook for rollback"
    - "Every prerequisite plan has shipped and verified BEFORE this plan executes"
  artifacts:
    - path: ".planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-07-FLIP-CHECKLIST.md"
      provides: "Operator-completed checklist with timestamps for fallback drop + env flip + post-flip verification"
      min_lines: 40
  key_links:
    - from: "production runtime"
      to: "public.classifier_rules.status mutations"
      via: "process.env.CLASSIFIER_CRON_MUTATE=true gate in classifier-promotion-cron.ts"
      pattern: "CLASSIFIER_CRON_MUTATE === \"true\""
---

<objective>
Two operator-gated cleanups that close the rollout per D-28 step 4 and D-19:

1. **Drop the FALLBACK_WHITELIST** from `web/lib/classifier/cache.ts` once the table-backed whitelist has run cleanly for ≥1 day (D-28 step 4 — no fallback path exercised, no errors logged).
2. **Flip `CLASSIFIER_CRON_MUTATE` to `true`** in Vercel env vars after the 14-day shadow window (D-19) — operator confirms cron evaluations match expected behavior on the `/automations/classifier-rules` dashboard.

Both are operator-judgment calls. This plan is `autonomous: false` because the timing is not Claude-decidable — only the operator knows when "1 day clean" and "14 days plausible" have passed.

If the orchestrator runs this plan before the timers elapse, the operator pauses the executor and resumes when criteria are met. The checklist file is the audit artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-CONTEXT.md
@.planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-RESEARCH.md
@.planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-02-SUMMARY.md
@.planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-03-SUMMARY.md
@.planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-04-SUMMARY.md

@web/lib/classifier/cache.ts
@web/lib/inngest/functions/classifier-promotion-cron.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm 1-day clean run before dropping FALLBACK_WHITELIST</name>
  <what-built>
    The data-driven whitelist read (`readWhitelist` in `web/lib/classifier/cache.ts`) shipped in plan 60-02 with a defensive `FALLBACK_WHITELIST` Set that mirrors the original 6 hardcoded debtor-email rule_keys. Per D-28 step 4, the fallback is dropped only after a documented 1-day clean run.
  </what-built>
  <files>(no source files modified — operator action only; updates the LOG/CHECKLIST artifact named in files_modified)</files>
  <action>
    Operator confirms ALL of the following before approving the drop:

    1. The 60-02 ingest-route refactor has been deployed to production for ≥24h.
    2. Vercel logs for the ingest route show ZERO occurrences of `[readWhitelist] fallback used` (add a one-line `console.warn` to that branch in cache.ts if it isn't already there before this gate — small fix is acceptable inside this task).
    3. `select count(*) from public.classifier_rules where swarm_type='debtor-email' and status='promoted'` returns ≥6 (backfill confirmed live).
    4. Spot-check: trigger a representative ingest payload (real Zapier replay or curl with X-Zapier-Secret) and confirm the row hits `automation_runs.status='predicted'` only when expected — i.e., promoted rules still auto-action.
    5. No production-incident channel reports about classification regressions in the past 24h.

    If ALL pass: edit `web/lib/classifier/cache.ts`:
    - DELETE the `FALLBACK_WHITELIST` const definition.
    - In the `readWhitelist` error branch, REPLACE the fallback path with a strict no-op:
      ```typescript
      if (error) {
        // Post-D-28 step 4: no hardcoded fallback. Return last-known-good cache; on
        // cold-cache + DB error, return empty Set (router takes everything to review).
        if (hit) return hit.rules;
        console.warn("[readWhitelist] DB error and no cache; routing all to review", { swarmType, error });
        return new Set<string>();
      }
      ```
    - DELETE all `FALLBACK_WHITELIST` import references elsewhere (grep to confirm).

    Update the cache test in `web/lib/classifier/__tests__/cache.test.ts`:
    - Replace the "returns FALLBACK_WHITELIST on cold-cache error" assertion with "returns empty Set on cold-cache error".
    - Keep the "returns last-known-good on DB error after first success" assertion.
    - Run `cd web && pnpm vitest run lib/classifier --reporter=basic` — must be GREEN.

    Append to `.planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-07-FLIP-CHECKLIST.md`:
    ```
    ## D-28 Step 4 — FALLBACK_WHITELIST drop

    - Deployed at: <iso timestamp>
    - 24h-clean confirmation:
      - Vercel logs scanned for [readWhitelist] fallback used: 0 occurrences ✓
      - classifier_rules promoted count: <N>
      - Ingest spot-check: <pass/fail>
    - Drop committed at: <iso timestamp>
    - Test suite: pnpm vitest run lib/classifier — GREEN
    ```
  </action>
  <verify><automated>test -f .planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-01-SCHEMA-PUSH-LOG.md || test -f .planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-07-FLIP-CHECKLIST.md</automated></verify>
  <done>Operator log/checklist file committed with all required fields populated</done>
  <acceptance_criteria>
    - `grep -c "FALLBACK_WHITELIST" web/lib/classifier/cache.ts` == 0
    - `grep -rn "FALLBACK_WHITELIST" web/` returns NO matches (callers updated)
    - `grep -c "subject_paid_marker" web/lib/classifier/cache.ts` == 0 (the 6 rule_keys are no longer hardcoded anywhere in cache.ts)
    - `cd web && pnpm vitest run lib/classifier --reporter=basic` exits 0
    - `cd web && pnpm tsc --noEmit -p .` exits 0 (no broken imports)
    - `60-07-FLIP-CHECKLIST.md` contains the §D-28 Step 4 block with timestamp
  </acceptance_criteria>
  <resume-signal>Type "fallback dropped" with the commit SHA, or describe the blocking observation</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Flip CLASSIFIER_CRON_MUTATE=true after 14-day shadow window</name>
  <what-built>
    classifier-promotion-cron has been running daily in shadow mode (CLASSIFIER_CRON_MUTATE unset or "false"), writing `classifier_rule_evaluations` rows with `action='shadow_would_promote'`, `'shadow_would_demote'`, or `'no_change'`. Per D-19, the operator flips the env var to `true` after a ≥14-day plausibility review.
  </what-built>
  <read_first>
    - .planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-CONTEXT.md (D-19 — flip rationale)
    - web/lib/inngest/functions/classifier-promotion-cron.ts (the gate at `process.env.CLASSIFIER_CRON_MUTATE === "true"`)
  </read_first>
  <files>(no source files modified — operator action only; updates the LOG/CHECKLIST artifact named in files_modified)</files>
  <action>
    Operator runs THIS exact protocol before flipping. Cannot be automated by Claude — Vercel env-var edit is a console action, and the 14-day window is calendar-driven.

    **Pre-flip checklist (operator completes in 60-07-FLIP-CHECKLIST.md):**

    1. ≥14 calendar days have passed since classifier-promotion-cron was registered (per 60-03 deploy timestamp). Record the calendar dates: `Cron registered: <date>`, `Today: <date>`, `Days elapsed: <N> ≥ 14`.

    2. Run on Supabase SQL Editor (or via psql with service-role):
       ```sql
       select action, count(*)
       from public.classifier_rule_evaluations
       where evaluated_at >= now() - interval '14 days'
       group by action;
       ```
       Expected distribution:
       - `no_change` ≫ 0 (most rules stable per day)
       - `shadow_would_promote` ≥ 0 (any candidate that crossed gates during shadow)
       - `shadow_would_demote` ≥ 0 (any promoted rule that dropped under 92% during shadow)
       - `promoted` and `demoted` should both be 0 (no live mutations during shadow)

    3. Open `/automations/classifier-rules` in the app:
       - Confirm shadow-mode banner shows ("Shadow mode — cron records evaluations…")
       - Confirm 6 backfilled debtor-email rules visible under "Promoted" group
       - Spot-check 5 randomly-selected sparkline trends for stability (no wild oscillations between 0.6 and 1.0 — would indicate underlying telemetry noise)

    4. Spot-check shadow_would_demote rows specifically: if any rules have `shadow_would_demote` actions in the past 14 days, manually verify the underlying agent_runs telemetry to ensure these are genuine drift signals (not bug-induced false negatives). Document the verdict per row.

    **Flip:**

    5. In Vercel dashboard → Project: agent-workforce (`prj_APDosWEbpdca53P5UxXst8tCJMVV`) → Settings → Environment Variables:
       - Add or edit `CLASSIFIER_CRON_MUTATE` with value `true` for the Production environment.
       - Save. Trigger a redeploy (or wait for next Inngest invocation pulling the latest function build).

    6. Verify post-flip:
       - On the next cron run (06:00 Amsterdam, Mon-Fri), check `select * from public.classifier_rule_evaluations where evaluated_at::date = current_date` — confirm at least one row has `action != 'shadow_*'` (now writing `no_change` / `promoted` / `demoted`).
       - On `/automations/classifier-rules`, confirm the shadow-mode banner is GONE.

    **Rollback path (in case of regression):**

    7. If unexpected demotions or promotions appear after flip:
       - Revert: edit `CLASSIFIER_CRON_MUTATE` back to `false` (or delete) → next cron run is shadow again.
       - Manually `update public.classifier_rules set status='promoted' where rule_key in (<list>)` to restore desired state if the cron demoted rules incorrectly.
       - Document incident in 60-07-FLIP-CHECKLIST.md and open a follow-up phase.

    Append to `60-07-FLIP-CHECKLIST.md`:
    ```
    ## D-19 — CLASSIFIER_CRON_MUTATE flip

    - Cron first registered: <iso>
    - Today: <iso>
    - Days elapsed: <N>
    - Shadow evaluation distribution (14d):
      - no_change: <count>
      - shadow_would_promote: <count>
      - shadow_would_demote: <count>
      - promoted: 0 ✓
      - demoted: 0 ✓
    - Sparkline spot-check (5 rules): <PASS/notes>
    - Shadow_would_demote review: <PASS/notes per rule>
    - Flip executed at: <iso>
    - Post-flip first cron run: <iso>, action distribution: <...>
    - Post-flip dashboard banner removed: <YES/NO>
    ```
  </action>
  <verify><automated>test -f .planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-01-SCHEMA-PUSH-LOG.md || test -f .planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-07-FLIP-CHECKLIST.md</automated></verify>
  <done>Operator log/checklist file committed with all required fields populated</done>
  <acceptance_criteria>
    - `60-07-FLIP-CHECKLIST.md` contains the §D-19 block with all fields filled
    - Shadow eval distribution shows `promoted: 0` and `demoted: 0` PRE-flip
    - Post-flip cron run produces ≥1 row with `action != 'shadow_*'`
    - `/automations/classifier-rules` no longer shows the shadow-mode banner after flip
  </acceptance_criteria>
  <resume-signal>Type "flip complete" with the timestamp and post-flip eval distribution, or describe the rollback decision</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Vercel env vars | Vercel team admin only |
| Operator → cache.ts edit | Code review + commit + deploy gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-07-01 | Tampering | Premature flip mutates classifier_rules unexpectedly | mitigate | 14-day window + checklist gates required; rollback is one env-var edit |
| T-60-07-02 | Repudiation | No record of who flipped or when | mitigate | 60-07-FLIP-CHECKLIST.md committed to git as the audit artifact |
| T-60-07-03 | Tampering | FALLBACK_WHITELIST drop loses safety net during a future DB outage | accept | Empty-Set fallback routes everything to review (safe-by-default — no incorrect auto-actions); operator restores DB before reviewer queue overflows |
| T-60-07-04 | Elevation | Wrong env-var scope (Preview instead of Production) | mitigate | Checklist explicitly names "Production environment" in the Vercel dashboard step |
</threat_model>

<verification>
- 60-07-FLIP-CHECKLIST.md exists with both §D-28 and §D-19 sections completed
- `grep FALLBACK_WHITELIST web/lib/classifier/cache.ts` returns nothing
- Production cron writes non-shadow actions on the day after flip
- /automations/classifier-rules shadow banner gone post-flip
</verification>

<success_criteria>
1. FALLBACK_WHITELIST removed from cache.ts after 1-day clean run.
2. CLASSIFIER_CRON_MUTATE=true in Vercel Production after 14-day shadow review.
3. Operator log captures the timestamps + spot-check results.
4. Post-flip cron writes live promoted/demoted/no_change actions.
</success_criteria>

<output>
After completion, create `.planning/phases/60-debtor-email-close-the-whitelist-gate-loop-data-driven-auto-/60-07-SUMMARY.md` linking to the FLIP-CHECKLIST.
</output>
