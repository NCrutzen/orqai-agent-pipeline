---
phase: 82.4
plan: 01
subsystem: database
tags: [supabase, postgres, migration, rls, email_feedback, v9-substrate]
requires:
  - phase: 82.3
    provides: "per-stage audit popup (the surface that will mount the prose-notes textarea in plan 03)"
provides:
  - "public.email_feedback table — durable write target for operator prose/verdict feedback per (email_id, stage)"
  - "Two indexes: (email_id, stage) for stage-tab loader joins; (operator_id, created_at DESC) for per-operator scans"
  - "Service-role-only RLS policy email_feedback_service_all"
affects:
  - "82.4-02 (feedback POST route — insert target)"
  - "82.4-05 (stage-tab list loader — JOIN on (email_id, stage))"
  - "82.4-07 (nightly snapshot cron — read source)"
  - "V9.0 synthesis (clusterer + drafter + Learning Inbox)"
tech-stack:
  added: []
  patterns:
    - "Idempotent migration: CREATE TABLE/INDEX IF NOT EXISTS + DROP/CREATE POLICY, safe to re-run"
    - "No FK across schemas — email_id references email_pipeline.emails by convention only (matches pipeline_events / pipeline_health)"
    - "Natural key includes created_at — multiple revisions per (email_id, stage, operator_id) by design; V9.0 reads latest per (email_id, stage)"
    - "Service-role-only RLS — anon + authenticated denied entirely; reads via createAdminClient() server-side"
key-files:
  created:
    - supabase/migrations/20260513c_email_feedback.sql
  modified: []
key-decisions:
  - "No UNIQUE constraint on (email_id, stage, operator_id) — revisions are intentionally retained for the V9.0 synthesis layer"
  - "No FK to email_pipeline.emails — cross-schema FK avoidance matches existing pipeline_events / pipeline_health convention"
  - "operator_id stored as text (UUID-shaped) per existing repo convention; server-stamps from auth.getUser().id in plan 02"
patterns-established:
  - "email_feedback as the canonical operator-feedback write target — every downstream plan in phase 82.4 reads/writes this single table"
requirements-completed: [FB-01, FB-02]
duration: ~5min (Task 1 prior session + Task 2 checkpoint resolution this session)
completed: 2026-05-13
---

# Phase 82.4 Plan 01: email_feedback Migration — Summary

`public.email_feedback` table created and applied to remote Supabase project `mvqjhlxfvtqqubqgdvhz` — durable write target for operator verdict+prose feedback per stage, foundational substrate for V9.0 synthesis layer.

## Performance

- **Duration:** ~5 min total (file authored in prior executor session; remote apply + verification this session)
- **Completed:** 2026-05-13
- **Tasks:** 2 (1 auto + 1 checkpoint, both green)
- **Files modified:** 1

## Accomplishments

- Migration file `supabase/migrations/20260513c_email_feedback.sql` committed (Task 1, prior session).
- Migration applied to remote Supabase project via MCP `apply_migration` (name `email_feedback_phase_82_4`).
- Live verification confirms all 8 columns, RLS policy, and 2 indexes (plus PK index) match the locked schema spec from `82.4-CONTEXT.md`.
- Plans 82.4-02 (POST route) and 82.4-05 (stage-tab loader) can now verify against the real remote schema without further setup.

## Task Commits

1. **Task 1: Write migration file 20260513c_email_feedback.sql** — `8301899` (feat)
2. **Task 2 [BLOCKING checkpoint]: Apply migration to remote Supabase project** — resolved via Supabase MCP `apply_migration` (no file diff; remote side effect only)

**Plan metadata:** (this final docs commit)

## Files Created/Modified

- `supabase/migrations/20260513c_email_feedback.sql` — CREATE TABLE + 2 indexes + RLS policy + grant; idempotent.

## Verification (Task 2 — applied remote)

Remote project `mvqjhlxfvtqqubqgdvhz`, schema `public`, table `email_feedback`:

**Columns (information_schema.columns, ordered):**

```
id              uuid                       NOT NULL
email_id        uuid                       NOT NULL
stage           smallint                   NOT NULL
verdict         text                       NOT NULL
corrected_value text                       NULL
prose_notes     text                       NULL
operator_id     text                       NOT NULL
created_at      timestamp with time zone   NOT NULL
```

**Policy (pg_policies):**

```
policyname=email_feedback_service_all  roles={service_role}  cmd=ALL
```

**Indexes (pg_indexes):**

```
email_feedback_pkey
email_feedback_email_id_stage_idx
email_feedback_operator_created_at_idx
```

All Task 2 acceptance criteria met:
- [x] Remote `public.email_feedback` table exists with all 8 columns and correct types.
- [x] `email_feedback_service_all` policy present (and is the only policy).
- [x] Both expected non-PK indexes exist alongside the auto-generated PK index.

## Decisions Made

None beyond plan-locked decisions. The natural-key-with-revisions design and cross-schema FK avoidance are pre-approved in `82.4-CONTEXT.md`; this plan executed the schema verbatim.

## Deviations from Plan

None — plan executed exactly as written. Task 1 produced the migration file matching the locked SQL body byte-for-byte; Task 2 applied that same SQL via MCP and verified against the expected column/policy/index shape.

## Issues Encountered

None.

## Threat Flags

None — the surface introduced (`public.email_feedback`) is fully covered by the plan's `<threat_model>` block (T-82.4-01-01 Tampering, T-82.4-01-02 Information Disclosure, T-82.4-01-03 Repudiation). Service-role-only RLS denies anon+authenticated entirely; `operator_id` will be server-stamped in plan 02's POST route (T-82.4-01-01 mitigation handoff).

## Self-Check: PASSED

**Must-haves (from plan frontmatter):**

- **Truths:**
  - [x] `email_feedback` table exists in remote Supabase project — verified via live `information_schema.columns` query.
  - [x] Multiple revisions per (email_id, stage, operator_id) are allowed (created_at is part of natural key) — verified: no UNIQUE constraint added on (email_id, stage, operator_id); the only unique index is the PK on `id`.
  - [x] Reads/writes are service_role only (RLS) — verified: `email_feedback_service_all` is the sole policy, restricted to `service_role`.
- **Artifacts:**
  - [x] `supabase/migrations/20260513c_email_feedback.sql` exists, contains `CREATE TABLE IF NOT EXISTS public.email_feedback` — confirmed in `git log -- supabase/migrations/20260513c_email_feedback.sql` (commit 8301899).
- **Key links:**
  - [x] Plan 02's feedback POST route can target `public.email_feedback` via `createAdminClient().from('email_feedback').insert(...)` — table + service-role policy live.
  - [x] Plan 05's stage-tab list loader can JOIN on (email_id, stage) — supporting index `email_feedback_email_id_stage_idx` live.

**Artifact existence:**
- FOUND: `supabase/migrations/20260513c_email_feedback.sql`
- FOUND: commit `8301899` in `git log`
- FOUND: remote `public.email_feedback` table with 8 columns, 1 policy, 3 indexes (including PK).

## Next Phase Readiness

Plan 82.4-02 (POST `/api/automations/debtor-email/feedback` route + zod schema + tests) can now begin against the live remote schema. No blockers.

---
*Phase: 82.4-feedback-capture-infrastructure-v8-stabilisation*
*Completed: 2026-05-13*
