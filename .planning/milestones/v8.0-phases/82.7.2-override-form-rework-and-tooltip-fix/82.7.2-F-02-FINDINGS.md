# F-02 entity_brand coverage findings (Phase 82.7.2)

**Date:** 2026-05-18
**Swarm:** debtor-email
**Window:** last 7 days (>= 2026-05-11T08:08:27Z)
**Source:** Supabase REST (service-role, read-only), production project `mvqjhlxfvtqqubqgdvhz`

## Q1 — Stage 1 emit count

```
SELECT count(*) FROM pipeline_events
WHERE swarm_type='debtor-email' AND stage=1 AND created_at > now() - interval '7 days'
```

Result: **275 rows**.

(REST: `GET /rest/v1/pipeline_events?swarm_type=eq.debtor-email&stage=eq.1&created_at=gte.2026-05-11T08:08:27Z` → `content-range: 0-0/275`.)

## Q2 — entity populated vs null

| Metric | Count |
|--------|-------|
| with_entity (`decision_details->>entity` IS NOT NULL) | **274** |
| without_entity (`decision_details->>entity` IS NULL) | **1** |
| coverage | **99.6 %** |

(REST counts via `Prefer: count=exact` on `not.is.null` and `is.null` filters on `decision_details->>entity`.)

## Q3 — entity value distribution (top 20)

| entity | count | in 5-brand registry? |
|--------|------:|----------------------|
| fire-control | 109 | **NO** — not in `brand-color.ts` BRAND_COLOR_TOKEN map |
| smeba-fire   |  64 | yes (`--v7-pink`) |
| smeba        |  63 | yes (`--v7-lime`) |
| berki        |  38 | yes (`--v7-amber`) |
| (null)       |   1 | n/a |

**Registry coverage of distinct non-null entities:** 3 / 4 (`smeba`, `smeba-fire`, `berki` mapped; `fire-control` falls through to `--v7-muted`).

**Volume-weighted brand-color coverage** if Branch B mapper change ships: 165 of 274 entity-bearing rows (60 %) would resolve to a non-muted swatch. The remaining 109 rows (40 %, all `fire-control`) would still render muted because `fire-control` is not in `BRAND_COLOR_TOKEN`.

## Q4 — `pipeline_events_email_summary` columns

```
email_id, email_received_at, first_event_at, last_event_at,
recipient_mailbox, sender_email, sender_name,
stage_0_decision, stage_1_decision, stage_1_overridden,
stage_2_decision, stage_2_overridden,
stage_3_decision, stage_3_overridden,
stage_4_decision, stage_4_overridden,
subject, swarm_type, tool_call_count, total_cost_cents
```

| Question | Answer |
|----------|--------|
| Does it expose `entity_brand`? | **No** |
| Does it expose `entity`?       | **No** |

→ PATTERNS.md L168–179 hypothesis confirmed: the view does NOT project entity / entity_brand, so `mapSummaryToPredictedRow` has nothing to read. Per-row entity must be back-joined from raw `pipeline_events.decision_details->>entity`.

## Decision (G-03 branch)

- [ ] **Branch A** (view exposes `entity_brand`): NOT selected — view exposes neither column (Q4).
- [x] **Branch B** (view exposes entity only, or neither — JOIN-back required): SELECTED.
- [ ] **Branch C** (Q2 shows <50% coverage): NOT selected — coverage is 99.6 %.

**Selected branch: B**

**Reasoning:** entity is populated on 274/275 = 99.6 % of Stage 1 emit rows (Q2), so the noise-tab "no swatch" symptom is a pure UI passthrough gap — not an enrichment gap. The summary view does not project entity/entity_brand (Q4), so the mapper must JOIN-back to raw `pipeline_events.decision_details->>entity` for the current page of rows (analogous to the existing filter JOIN-back at `stage-1/page.tsx:566-604`). The entity strings already match brand-color.ts keys for 3 of 4 brands; passing the entity string through as `entity_brand` resolves the swatch correctly for those 165 rows.

## Out-of-scope follow-up flag (NOT in this plan)

**`fire-control` (109 / 275 rows = 40 %) is NOT in the 5-brand registry**
(`brand-color.ts` BRAND_COLOR_TOKEN map). After Branch B wiring ships,
those 109 rows will still render the muted fallback swatch and the
tooltip will read "Unknown brand". Per CONTEXT G-03 / G-04 and
`project_brand_scope.md` (5 Benelux brands locked, iccafe/iccafe-france
deferred), registry expansion is an explicit out-of-phase decision for
Nick. Two options to escalate as a follow-up:

1. Add `fire-control` to `BRAND_COLOR_TOKEN` + `BRAND_DISPLAY_NAME` in
   `web/lib/swarms/brand-color.ts` (one-line per map; pick a token —
   `--v7-orange` if it exists, otherwise propose a new token). This is
   a 4-line change but it expands the locked 5-brand scope, so it needs
   operator sign-off, not autonomous addition. The mailbox
   `administratie@fire-control.nl` is on the Phase 74 target list
   (memory `project_phase74_target_mailboxes.md`), so the entity code
   is well-established upstream.

2. Keep the muted fallback and document `fire-control` as a known
   "no-swatch" case until a Stage 1 entity-to-brand mapping layer is
   added (likely tied to the swarms registry, not the per-email
   `decision_details.entity`).

**This plan does NOT make either choice.** Branch B mapper change ships
to fix the 3 mapped brands; Nick decides registry expansion separately.

## Audit safety

Zero production writes. All four queries use `GET /rest/v1/...` (read-only). No `--apply` gate, no DDL, no mutation.
