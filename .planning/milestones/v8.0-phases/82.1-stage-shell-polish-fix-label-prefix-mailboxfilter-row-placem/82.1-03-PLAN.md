---
phase: 82.1-stage-shell-polish-fix-label-prefix-mailboxfilter-row-placem
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-3/client-shell.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-4/client-shell.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-0/client-shell.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-2/client-shell.tsx
autonomous: true
requirements: []
decisions: [D-04, D-05, D-06]
must_haves:
  truths:
    - "/stage-1 row strip shows subject with ≥200px of visible text — no 'R..' or 'A..' truncation in a wide viewport."
    - "Sender column in _shell/row-list.tsx is constrained to minWidth: 120px, maxWidth: 200px."
    - "Each stage's two-column grid (row-list + detail-pane) uses gridTemplateColumns: 'minmax(540px, 720px) 1fr' (Stage 1) or its already-wider equivalent for stages with `1fr 2fr` grids — those stay untouched per CONTEXT (only Stage 1's narrow 380/460 grid is the bug)."
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx"
      provides: "Tightened sender column constraints"
      contains: "minWidth: 120"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx"
      provides: "Widened row-list pane"
      contains: "minmax(540px, 720px)"
  key_links:
    - from: "_shell/row-list.tsx sender span"
      to: "rendered subject span"
      via: "flex sibling with flex: 1 + ellipsis"
      pattern: "flex: 1"
---

<objective>
Fix 3 — Widen the row-list pane and tighten the sender column so subject text has ≥200px of visible room.

Purpose: Today `_shell/row-list.tsx` clamps sender at 160-220px and Stage 1's grid caps the row-list pane at 460px → subject squeezed to ~30-50px ("R..", "A..").

Output: Sender column 120-200px (D-04), Stage 1 grid `minmax(540px, 720px) 1fr` (D-05). Subject keeps `flex: 1` ellipsis (D-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/82.1-stage-shell-polish-fix-label-prefix-mailboxfilter-row-placem/82.1-CONTEXT.md
@web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten sender column in _shell/row-list.tsx</name>
  <files>web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx (lines 89-102, the sender `<span data-testid="row-sender">`)
  </read_first>
  <action>
    In `_shell/row-list.tsx`, locate the sender `<span data-testid="row-sender">` (around lines 89-102). Change two style properties:
    - `minWidth: 160` → `minWidth: 120`
    - `maxWidth: 220` → `maxWidth: 200`

    All other style properties (fontSize 13, color, overflow hidden, textOverflow ellipsis, whiteSpace nowrap) stay unchanged. Subject span (lines 103-115) is NOT modified — keeps `flex: 1` + ellipsis per D-06.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "_shell/row-list" ; true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "minWidth: 120" 'web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx'`
    - `grep -q "maxWidth: 200" 'web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx'`
    - `! grep -q "minWidth: 160" 'web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx'`
    - `! grep -q "maxWidth: 220" 'web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx'`
    - `grep -q "flex: 1" 'web/app/(dashboard)/automations/[swarm]/_shell/row-list.tsx'` (subject column flex untouched)
  </acceptance_criteria>
  <done>Sender column constrained to 120-200px in the shared row-list primitive.</done>
</task>

<task type="auto">
  <name>Task 2: Widen Stage 1 row-list pane grid template</name>
  <files>web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx (the two-column grid `gridTemplateColumns: "minmax(380px,460px) 1fr"` at line 161)
  </read_first>
  <action>
    In `stage-1/client-shell.tsx`, replace the two-column grid template:
    - `gridTemplateColumns: "minmax(380px,460px) 1fr"` → `gridTemplateColumns: "minmax(540px, 720px) 1fr"`

    No other style changes. The grid `gap`, `minHeight`, and child `<RowList>` / `<UnifiedDetailPane>` props stay.

    **Do not** touch Stage 3 and Stage 4 client-shells — they already use `"1fr 2fr"` (verified in CONTEXT additional_context; the 380/460px clamp bug is Stage 1-specific). Stage 0 and Stage 2 client-shells use their own (different) grids and are out-of-scope for D-05 widening — only widen if a manual smoke after Task 1+2 reveals truncation. Default: skip 0/2.

    Sanity verification step: after editing, `grep -n 'minmax(380px,460px)' web/app/(dashboard)/automations/[swarm]/` should return ZERO matches in stage-1/. Any stage-0/stage-2 hits → leave alone unless CONTEXT escalates.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "stage-1/client-shell" ; true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "minmax(540px, 720px) 1fr" 'web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx'`
    - `! grep -q "minmax(380px,460px) 1fr" 'web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx'`
  </acceptance_criteria>
  <done>Stage 1 row-list pane uses 540-720px clamp. Manual smoke on /stage-1 shows subject text without "R.." truncation at ≥1280px viewport.</done>
</task>

</tasks>

<verification>
Manual smoke on /stage-1 at 1280px+ viewport: row strip shows subject with visibly long text (≥200px), no "R..", no "A..". Sender column tight but readable for typical names.

Stage 3/4 visual unchanged (their grids stay 1fr 2fr).
</verification>

<success_criteria>
- _shell/row-list.tsx sender column: minWidth 120, maxWidth 200 (D-04).
- stage-1 grid: minmax(540px, 720px) 1fr (D-05).
- Subject keeps flex: 1 + ellipsis (D-06 — implicit, no edit).
- tsc passes.
</success_criteria>

<output>
After completion, create `.planning/phases/82.1-stage-shell-polish-fix-label-prefix-mailboxfilter-row-placem/82.1-03-SUMMARY.md` per template.
</output>
