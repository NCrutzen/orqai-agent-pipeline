---
phase: 82.1-stage-shell-polish-fix-label-prefix-mailboxfilter-row-placem
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx
autonomous: true
requirements: []
decisions: [D-01]
must_haves:
  truths:
    - "/stage-1 detail-pane pipeline cell labels render as 'Stage 0 — Safety', 'Stage 1 — Category', 'Stage 2 — Customer', 'Stage 3 — Intent', 'Stage 4 — Handler' with a single 'Stage N — ' prefix."
    - "No double-prefix string 'Stage 0 — Stage 0 —' (or any 'Stage N — Stage N —') exists anywhere in detail-pane.tsx output."
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx"
      provides: "STAGE_TITLES with bare labels"
      contains: '0: "Safety"'
  key_links:
    - from: "_shell/detail-pane.tsx STAGE_TITLES"
      to: "stage-1/components/stage-step.tsx:62 render"
      via: "stage.title concatenation with literal 'Stage {n} — ' prefix in stage-step"
      pattern: "Stage \\{stage.n\\} —"
---

<objective>
Fix 1 — Drop the `"Stage N — "` double-prefix from STAGE_TITLES in the unified detail pane.

Purpose: `stage-step.tsx:62` already renders `Stage {stage.n} — {stage.title}`. Today STAGE_TITLES values include `"Stage 0 — Safety"` etc., producing `"Stage 0 — Stage 0 — Safety"` on /stage-1.

Output: STAGE_TITLES contains bare labels only. Single canonical "Stage N — Label" form rendered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/82.1-stage-shell-polish-fix-label-prefix-mailboxfilter-row-placem/82.1-CONTEXT.md
@web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace STAGE_TITLES values with bare labels</name>
  <files>web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx (lines 82-90 are the target)
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/stage-step.tsx (verify line ~62 still prefixes with `Stage {stage.n} —`)
  </read_first>
  <action>
    In `_shell/detail-pane.tsx`, locate the `STAGE_TITLES` constant (lines 84-90). Replace the entire object literal so the values are bare labels (no `"Stage N — "` prefix):

    ```ts
    const STAGE_TITLES: Record<ActiveStage, string> = {
      0: "Safety",
      1: "Category",
      2: "Customer",
      3: "Intent",
      4: "Handler",
    };
    ```

    Per D-01. Do NOT touch any other constant in this file. Do NOT modify `stage-step.tsx` — it already renders `Stage {stage.n} — {stage.title}` and depends on `stage.title` being a bare label.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "_shell/detail-pane" ; true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '0: "Safety"' 'web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx'`
    - `grep -q '1: "Category"' 'web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx'`
    - `grep -q '2: "Customer"' 'web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx'`
    - `grep -q '3: "Intent"' 'web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx'`
    - `grep -q '4: "Handler"' 'web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx'`
    - `! grep -qE 'Stage [0-4] — (Safety|Category|Customer|Intent|Handler)' 'web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx'`
  </acceptance_criteria>
  <done>STAGE_TITLES contains bare labels. tsc passes. /stage-1 manual smoke shows pipeline cells reading "Stage 0 — Safety" etc., single prefix.</done>
</task>

</tasks>

<verification>
Manual smoke on `/stage-1`: pipeline cell headings read `Stage 0 — Safety`, `Stage 1 — Category`, `Stage 2 — Customer`, `Stage 3 — Intent`, `Stage 4 — Handler`. No doubled `Stage N —`.
</verification>

<success_criteria>
- STAGE_TITLES values are bare labels per D-01.
- TypeScript compiles.
- No `"Stage [0-4] — (Safety|Category|Customer|Intent|Handler)"` string remains anywhere in detail-pane.tsx (grep gate).
</success_criteria>

<output>
After completion, create `.planning/phases/82.1-stage-shell-polish-fix-label-prefix-mailboxfilter-row-placem/82.1-01-SUMMARY.md` per template.
</output>
