---
phase: 82.1-stage-shell-polish-fix-label-prefix-mailboxfilter-row-placem
plan: 02
subsystem: ui/stage-1-shell
tags: [layout, mailbox-filter, chip-strip, stage-1]
requires:
  - "stage-1/page.tsx renders <NoiseCategoryChipStrip>"
  - "_shell/mailbox-filter.tsx exists and accepts {mailboxes, selected}"
  - "getSwarmMailboxes() + parseSelectedMailboxes() already wired at the page level"
provides:
  - "Page-level flex row that hosts <NoiseCategoryChipStrip> + <MailboxFilter> side-by-side (justifyContent: space-between)"
  - "Stage1ClientShell freed of MailboxFilter — file ends in a clean state for plans 82.1-03 and 82.1-04"
affects:
  - "Visual layout of /automations/[swarm]/stage-1 (one row instead of two)"
tech-stack:
  added: []
  patterns:
    - "Hoist UI primitive from client shell to RSC page so it shares a row with sibling primitives"
key-files:
  created: []
  modified:
    - "web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx"
    - "web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx"
decisions:
  - "D-02: MailboxFilter mounted at page level next to NoiseCategoryChipStrip in a flex row"
  - "D-03: MailboxFilter component itself unchanged — only its mount point moved"
  - "noise-category-chip-strip.tsx untouched — its outer <div> with flex+gap composes correctly inside the parent flex row; no min-width-0 overflow observed in code shape"
metrics:
  duration: ~10min
  completed: 2026-05-11
  tasks_completed: 2
  files_modified: 2
---

# Phase 82.1 Plan 02: MailboxFilter row placement — Summary

Hoisted `<MailboxFilter>` out of `Stage1ClientShell` onto the chip-strip row at the page level so it sits to the right of `<NoiseCategoryChipStrip>` in a flex row with `justifyContent: space-between`.

## Tasks

| Task | Name                                                | Commit  | Files                                       |
| ---- | --------------------------------------------------- | ------- | ------------------------------------------- |
| 1    | Mount MailboxFilter on chip-strip row at page level | 1056d9e | stage-1/page.tsx                            |
| 2    | Remove MailboxFilter mount from Stage1ClientShell   | 695fb40 | stage-1/client-shell.tsx, stage-1/page.tsx  |

## What changed

**`stage-1/page.tsx`:**
- Added `import { MailboxFilter } from "../_shell/mailbox-filter"`.
- Wrapped `<NoiseCategoryChipStrip>` and `<MailboxFilter>` in a single flex container (`display: flex`, `alignItems: center`, `justifyContent: space-between`, `gap: var(--space-3)`, `marginBottom: var(--space-3)`). `mailboxes` and `selectedMailboxes` (already computed earlier in the function) feed `<MailboxFilter>` directly.
- Removed `mailboxes={mailboxes}` from the `<Stage1ClientShell>` prop hand-off (the page-level MailboxFilter consumes the local variable now).

**`stage-1/client-shell.tsx`:**
- Dropped the `MailboxFilter` import and the entire `<div justifyContent: flex-end>` wrapper that rendered it.
- Removed `mailboxes: MailboxOption[]` from `Stage1ClientShellProps`, the destructure, and the `MailboxOption` type import (no other references).
- Kept `selectedMailboxes` — it still drives the client-side `useMemo` row filter (URL → page → shell → row-list).
- Outer flex column wrapper (padding + gap) preserved; it now contains only the two-column grid.

## Verification

| Acceptance criterion                                                            | Result |
| ------------------------------------------------------------------------------- | ------ |
| `import { MailboxFilter }` present in page.tsx                                  | PASS   |
| `<MailboxFilter mailboxes={mailboxes} selected={selectedMailboxes} />` present  | PASS   |
| `justifyContent: "space-between"` present in page.tsx                           | PASS   |
| `MailboxFilter` absent from client-shell.tsx                                    | PASS (grep -c = 0) |
| `mailboxes: MailboxOption[]` absent from client-shell.tsx                       | PASS (no match) |
| `selectedMailboxes` still present in client-shell.tsx (row-filter useMemo)      | PASS (5 matches) |
| `mailboxes={mailboxes}` absent on `<Stage1ClientShell>` JSX                     | PASS (only matches are on page-level `<MailboxFilter>`, intentional Task 1 wiring) |
| `tsc --noEmit` clean for stage-1/(page\|client-shell\|noise-category-chip-strip) | PASS (no errors in these files; two pre-existing errors elsewhere — `__tests__/actions.predictor.test.ts` spread arg, `lib/classifier/corpus-mapping.ts` missing `spam` key — unrelated to this plan) |

## Deviations from Plan

None — plan executed exactly as written.

## Hard-separation guard

This plan is layout-only. `<NoiseCategoryChipStrip>` continues to read `swarm_noise_categories` (Stage 1 noise filter registry). No `swarm_intents` reads were added or removed at the Stage 1 surface; the `<Stage1ClientShell intents={intentRows}>` hand-off (downstream Stage 3 widget inside `UnifiedDetailPane`) is untouched.

## Note for follow-up plans

`client-shell.tsx` is left in a clean state. Plans 82.1-03 (row column widths — D-04/D-05/D-06) and 82.1-04 (Stage 1 override picker port — D-07..D-12) will continue to modify this file; they should rebase cleanly on top of commit `695fb40`.

## Self-Check: PASSED

- `web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx`: FOUND
- `web/app/(dashboard)/automations/[swarm]/stage-1/client-shell.tsx`: FOUND
- Commit `1056d9e`: FOUND in `git log`
- Commit `695fb40`: FOUND in `git log`
