---
phase: 82.1
plan: 01
subsystem: agentic-pipeline-ui / unified detail-pane
tags: [ui, polish, stage-shell, bugfix]
dependency-graph:
  requires:
    - "_shell/detail-pane.tsx existing STAGE_TITLES constant (Phase 82)"
    - "stage-1/components/stage-step.tsx:62 prefix render (Phase 82)"
  provides:
    - "Single-prefix 'Stage N — Label' rendering in /stage-1 pipeline cells"
  affects:
    - "All swarms using _shell/ unified detail-pane (debtor-email, sales-email)"
tech-stack:
  added: []
  patterns:
    - "Single source of truth for 'Stage N — ' prefix (lives in stage-step.tsx only)"
key-files:
  created: []
  modified:
    - "web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx"
decisions:
  - "D-01: STAGE_TITLES values are bare labels; 'Stage N — ' prefix is owned exclusively by stage-step.tsx:62"
metrics:
  duration: ~2min
  completed: 2026-05-11
  tasks: 1
  files: 1
---

# Phase 82.1 Plan 01: Drop Stage N double-prefix from STAGE_TITLES — Summary

**One-liner:** Removed `"Stage N — "` prefix from `STAGE_TITLES` values in unified `_shell/detail-pane.tsx` so the canonical render-time prefix in `stage-step.tsx:62` is the sole producer of the `"Stage N — Label"` shape, eliminating the `"Stage 0 — Stage 0 — Safety"` doubling bug on `/stage-1`.

## What Changed

`web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx` (lines 84-90):

- Before: `0: "Stage 0 — Safety", 1: "Stage 1 — Category", ...`
- After: `0: "Safety", 1: "Category", 2: "Customer", 3: "Intent", 4: "Handler"`

`stage-step.tsx:62` continues to render `Stage {stage.n} — {stage.title}`. Result: single canonical prefix.

## Task Log

| Task | Name                                            | Status | Commit  |
| ---- | ----------------------------------------------- | ------ | ------- |
| 1    | Replace STAGE_TITLES values with bare labels    | done   | 40d1866 |

## Verification

Acceptance criteria (all PASS):

- `grep '0: "Safety"'` — found
- `grep '1: "Category"'` — found
- `grep '2: "Customer"'` — found
- `grep '3: "Intent"'` — found
- `grep '4: "Handler"'` — found
- `! grep -E 'Stage [0-4] — (Safety|Category|Customer|Intent|Handler)'` — confirmed absent
- `cd web && npx tsc --noEmit -p tsconfig.json` — no errors for `_shell/detail-pane`

Manual smoke on `/stage-1` deferred to phase verification (UI-rendering check is also covered by Plans 02-04 manual smoke).

## Deviations from Plan

None — plan executed exactly as written. No deviations, no auto-fixes triggered.

## Threat Flags

None — UI label change only. No new network endpoints, auth paths, file access, or schema changes.

## Self-Check: PASSED

- File modified: `web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx` — FOUND
- Commit `40d1866` — FOUND (`git log --oneline | grep 40d1866`)
- All acceptance grep gates — PASS
- TypeScript compile — PASS
