---
phase: 82.3
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app/(dashboard)/automations/[swarm]/stage-1/components/pipeline-flow.tsx
  - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx
  - web/app/(dashboard)/automations/[swarm]/_shell/_lib/audit-types.ts
autonomous: true
requirements: [82.3-DATA-PLUMBING]
must_haves:
  truths:
    - "Each StageData entry can carry an optional auditDetails ReactNode slot"
    - "UnifiedDetailPane accepts a stage→ReactNode map for audit panels and threads it into stagesData"
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/_shell/_lib/audit-types.ts"
      provides: "StageAuditPayload + StageAuditMap types consumed by per-stage renderers"
      contains: "export interface StageAuditPayload"
    - path: "web/app/(dashboard)/automations/[swarm]/stage-1/components/pipeline-flow.tsx"
      provides: "StageData.auditDetails optional ReactNode"
      contains: "auditDetails?: ReactNode"
    - path: "web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx"
      provides: "UnifiedDetailPaneProps.stageAudit optional map"
      contains: "stageAudit?:"
  key_links:
    - from: "downstream per-page wiring (Plan 11)"
      to: "UnifiedDetailPane"
      via: "stageAudit prop (Record&lt;0|1|2|3, ReactNode&gt;)"
      pattern: "stageAudit="
---

<objective>
Pure type-plumbing. Extend `StageData` (`pipeline-flow.tsx`) with an optional `auditDetails?: ReactNode` slot, extend `UnifiedDetailPaneProps` with an optional `stageAudit` map, and weave the map into the `stagesData` `useMemo`. No visual behavior change yet — Plan 07 wires the expander render; this plan ONLY makes the slot exist.

Purpose: Decouple data plumbing from UI work so Waves 2 + 3 can land in parallel against a stable contract.
Output: `audit-types.ts` source-of-truth types; two field-additions to existing files; zero runtime behavior change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-CONTEXT.md
@.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-UI-SPEC.md
@web/app/(dashboard)/automations/[swarm]/_shell/_lib/types.ts
@web/lib/pipeline-events/types.ts

<interfaces>
From web/app/(dashboard)/automations/[swarm]/stage-1/components/pipeline-flow.tsx:
```typescript
export interface StageData {
  n: number;
  title: string;
  axis: OverrideAxis | null;
  state: StageState;
  currentValue?: string;
  widget?: ReactNode;
}
```

From web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx (existing props — DO NOT REMOVE):
```typescript
export interface UnifiedDetailPaneProps {
  row: Row | null;
  swarmType: string;
  activeStage: ActiveStage;
  categories: SwarmNoiseCategoryRow[];
  intents: SwarmIntentRow[];
  timeline: PipelineTimelineEvent[];
  bodyText: string | null;
  bodyHtml?: string | null;
  extrasBelowPipeline?: ReactNode;
  iControllerBanner?: ReactNode;
  predictedRow?: PredictedRow | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add audit-types.ts source-of-truth</name>
  <files>web/app/(dashboard)/automations/[swarm]/_shell/_lib/audit-types.ts</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/_shell/_lib/types.ts
    - web/lib/pipeline-events/types.ts
    - .planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-CONTEXT.md §Per-stage payload table
  </read_first>
  <behavior>
    - `StageAuditPayload` discriminated union with members keyed by `stage: 0 | 1 | 2 | 3`
    - Stage 0 member fields: `regex_patterns_fired: string[]`, `llm_injection_verdict: "clean" | "flagged" | "unknown" | null`, `llm_reasoning: string | null`, `budget_headroom_cents: number | null`, `raw: Record&lt;string, unknown&gt;`
    - Stage 1 member fields: `rule_key: string | null` (from swarm_noise_categories taxonomy when regex Pass-1; `"unknown"` when LLM Pass-2 fired), `predictor_source: "regex" | "llm" | null`, `confidence: "high" | "medium" | "low" | null`, `reasoning: string | null`, `raw: Record&lt;string, unknown&gt;`
    - Stage 2 member fields: `identifier_source: "thread" | "sender" | "identifier" | "unresolved" | null`, `confidence: "high" | "medium" | "low" | null`, `top_candidates: Array&lt;{ account_id: string; name: string; score: number }&gt;`, `screenshot_paths: { before: string | null; after: string | null }`, `raw: Record&lt;string, unknown&gt;`
    - Stage 3 member fields: `ranked_intents: Array&lt;{ intent_key: string; confidence: number }&gt;` (intent_key from swarm_intents taxonomy — hard-separation lock), `coordinator_reasoning: string | null`, `selected_intent_key: string | null`, `raw: Record&lt;string, unknown&gt;`
    - `StageAuditMap = Partial&lt;Record&lt;0 | 1 | 2 | 3, ReactNode&gt;&gt;` — already-rendered nodes consumed by detail-pane
  </behavior>
  <action>
    Create the new file `web/app/(dashboard)/automations/[swarm]/_shell/_lib/audit-types.ts`.

    Top of file (verbatim header):
    ```
    // Phase 82.3 Plan 02 — audit payload types for the per-stage Show details
    // expander. Hard-separation lock (docs/agentic-pipeline/README.md):
    //   - Stage1AuditPayload.rule_key references swarm_noise_categories ONLY
    //   - Stage3AuditPayload.*_intent_key references swarm_intents ONLY
    //   - Neither type references the other registry. Renderers consume one type.
    ```

    Then export:
    - `export interface Stage0AuditPayload { stage: 0; regex_patterns_fired: string[]; llm_injection_verdict: "clean" | "flagged" | "unknown" | null; llm_reasoning: string | null; budget_headroom_cents: number | null; raw: Record&lt;string, unknown&gt;; }`
    - `export interface Stage1AuditPayload { stage: 1; rule_key: string | null; predictor_source: "regex" | "llm" | null; confidence: "high" | "medium" | "low" | null; reasoning: string | null; raw: Record&lt;string, unknown&gt;; }`
    - `export interface Stage2AuditPayload { stage: 2; identifier_source: "thread" | "sender" | "identifier" | "unresolved" | null; confidence: "high" | "medium" | "low" | null; top_candidates: Array&lt;{ account_id: string; name: string; score: number }&gt;; screenshot_paths: { before: string | null; after: string | null }; raw: Record&lt;string, unknown&gt;; }`
    - `export interface Stage3AuditPayload { stage: 3; ranked_intents: Array&lt;{ intent_key: string; confidence: number }&gt;; coordinator_reasoning: string | null; selected_intent_key: string | null; raw: Record&lt;string, unknown&gt;; }`
    - `export type StageAuditPayload = Stage0AuditPayload | Stage1AuditPayload | Stage2AuditPayload | Stage3AuditPayload;`
    - `import type { ReactNode } from "react"; export type StageAuditMap = Partial&lt;Record&lt;0 | 1 | 2 | 3, ReactNode&gt;&gt;;`

    No runtime code. Types only.
  </action>
  <verify>
    <automated>test -f web/app/(dashboard)/automations/[swarm]/_shell/_lib/audit-types.ts &amp;&amp; grep -c "^export " web/app/\(dashboard\)/automations/\[swarm\]/_shell/_lib/audit-types.ts &amp;&amp; cd web &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `web/app/(dashboard)/automations/[swarm]/_shell/_lib/audit-types.ts`
    - `grep -c "^export " audit-types.ts` returns at least `6`
    - `grep "swarm_noise_categories" audit-types.ts` matches the hard-separation comment
    - `grep "swarm_intents" audit-types.ts` matches the hard-separation comment
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>StageAuditPayload + StageAuditMap exported, tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend StageData + UnifiedDetailPaneProps with the slot</name>
  <files>web/app/(dashboard)/automations/[swarm]/stage-1/components/pipeline-flow.tsx, web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/pipeline-flow.tsx (full file)
    - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx lines 100–340 (props + stagesData useMemo)
    - web/app/(dashboard)/automations/[swarm]/_shell/_lib/audit-types.ts (just created in Task 1)
  </read_first>
  <behavior>
    - `StageData` gains optional `auditDetails?: ReactNode` field (purely additive — no existing call site needs to change)
    - `UnifiedDetailPaneProps` gains optional `stageAudit?: StageAuditMap` prop
    - Inside `DetailPaneInner`'s `stagesData` useMemo, when building each StageData entry, set `auditDetails: stageAudit?.[n] ?? undefined` (only for n ∈ {0,1,2,3}; Stage 4 always undefined per CONTEXT.md `<out_of_scope>`)
    - `useMemo` dep array gains `stageAudit`
    - No render-time use yet (Plan 07 wires `<StageStep>` to render it)
  </behavior>
  <action>
    Edit 1 — `pipeline-flow.tsx`: inside `export interface StageData` block, add a new optional field BELOW `widget?: ReactNode;`:
    ```
      /** Phase 82.3 Plan 02 — pre-rendered audit panel ReactNode injected by
       *  per-stage renderers (Stage0EvidencePanel … Stage3EvidencePanel). Plan 07
       *  consumes this inside &lt;StageStep&gt; via the Show-details expander.
       *  Stage 4 always undefined per 82.3 CONTEXT.md &lt;out_of_scope&gt;. */
      auditDetails?: ReactNode;
    ```

    Edit 2 — `detail-pane.tsx`:
    (a) Add import at top of file: `import type { StageAuditMap } from "./_lib/audit-types";`
    (b) Inside `export interface UnifiedDetailPaneProps`, add ABOVE the closing brace:
    ```
      /** Phase 82.3 Plan 02 — pre-rendered per-stage audit panels (Stages 0–3).
       *  Plan 11 wires per-page audit payloads through this map. Stage 4 omitted
       *  per 82.3 CONTEXT.md &lt;out_of_scope&gt;. */
      stageAudit?: StageAuditMap;
    ```
    (c) Destructure `stageAudit` in BOTH `UnifiedDetailPane` (line 128) and `DetailPaneInner` (line 177); thread `stageAudit={stageAudit}` from outer → inner.
    (d) Inside the `stagesData` useMemo (starts ~line 242), when pushing each entry into `out`, change the existing `out.push({ n, title, axis, state, currentValue, widget });` to ALSO include `auditDetails: n === 4 ? undefined : stageAudit?.[n as 0 | 1 | 2 | 3]`.
    (e) Add `stageAudit` to the useMemo dependency array.

    No other behavior change. Stage 4 row is EXPLICITLY excluded.
  </action>
  <verify>
    <automated>grep -n "auditDetails?:" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/components/pipeline-flow.tsx &amp;&amp; grep -n "stageAudit?:" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx &amp;&amp; grep -n "StageAuditMap" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx &amp;&amp; cd web &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -10 &amp;&amp; npx vitest run --reporter=dot app/\\(dashboard\\)/automations 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "auditDetails?: ReactNode" web/app/\(dashboard\)/automations/\[swarm\]/stage-1/components/pipeline-flow.tsx` = 1
    - `grep -c "stageAudit?: StageAuditMap" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` = 1
    - `grep -c "import type { StageAuditMap }" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` = 1
    - `grep "auditDetails:" web/app/\(dashboard\)/automations/\[swarm\]/_shell/detail-pane.tsx` shows the assignment inside stagesData useMemo
    - `cd web &amp;&amp; npx tsc --noEmit` exits 0
    - existing detail-pane RTL tests still pass (no regression)
  </acceptance_criteria>
  <done>Slot threaded end-to-end; no runtime render change yet; all existing tests green.</done>
</task>

</tasks>

<verification>
- tsc clean
- Existing vitest suite for `_shell/` passes unchanged
</verification>

<success_criteria>
`StageData.auditDetails` + `UnifiedDetailPaneProps.stageAudit` exist and are wired through `stagesData`. Plans 03–06 can build renderers against `StageAuditPayload`. Plan 07 can read `stage.auditDetails` inside `<StageStep>`. Plan 11 can pass `stageAudit={...}` from each page.
</success_criteria>

<output>
After completion, create `.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-02-SUMMARY.md`.
</output>
