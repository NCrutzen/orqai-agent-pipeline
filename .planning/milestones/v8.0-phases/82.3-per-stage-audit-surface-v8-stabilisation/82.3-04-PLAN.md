---
phase: 82.3
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - web/components/automations/bulk-review/audit/Stage1EvidencePanel.tsx
  - web/components/automations/bulk-review/audit/__tests__/Stage1EvidencePanel.test.tsx
autonomous: true
requirements: [82.3-STAGE1-RENDERER]
must_haves:
  truths:
    - "Stage 1 expander shows which rule fired (Pass-1 regex OR Pass-2 LLM), confidence chip, and predictor-source chip"
    - "Degraded mode: when Pass-1 regex short-circuited, the panel renders from decision_details alone (no agent_runs row)"
  artifacts:
    - path: "web/components/automations/bulk-review/audit/Stage1EvidencePanel.tsx"
      provides: "Pure Stage 1 evidence renderer (noise registry only — hard-separation lock)"
      contains: "export function Stage1EvidencePanel"
  key_links:
    - from: "Stage 1 page audit-payload mapper (Plan 11)"
      to: "Stage1EvidencePanel"
      via: "props.payload: Stage1AuditPayload"
      pattern: "&lt;Stage1EvidencePanel payload="
---

<objective>
Stage 1 evidence renderer per UI-SPEC §Component Inventory. Renders the rule that fired (from `swarm_noise_categories` taxonomy or the `unknown` bucket per `docs/agentic-pipeline/stage-1-regex.md`), confidence chip, and predictor source chip. Hard-separation lock: NEVER reference `swarm_intents` or intent vocabulary in this component.

Purpose: Operators currently see `Current: noise` but not WHICH noise rule matched or whether the LLM Pass-2 overrode the regex. This panel surfaces both.
Output: Pure functional component + tests covering both Pass-1 regex (degraded mode, no LLM reasoning) and Pass-2 LLM (full payload).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-UI-SPEC.md
@.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-CONTEXT.md
@docs/agentic-pipeline/stage-1-regex.md
@web/app/(dashboard)/automations/[swarm]/_shell/_lib/audit-types.ts
@web/components/automations/bulk-review/audit/Stage0EvidencePanel.tsx

<interfaces>
From audit-types.ts:
```typescript
export interface Stage1AuditPayload {
  stage: 1;
  rule_key: string | null;
  predictor_source: "regex" | "llm" | null;
  confidence: "high" | "medium" | "low" | null;
  reasoning: string | null;
  raw: Record&lt;string, unknown&gt;;
}
```

Hard-separation lock (RFC):
- `rule_key` references `swarm_noise_categories` keys ONLY, OR the literal string `"unknown"` (closed-list per stage-1-regex.md)
- This panel MUST NOT import `swarm_intents` types or reference intent vocabulary

UI-SPEC tokens:
- chip-teal for `rule_key` chip
- chip-lime/chip-amber/text-muted for confidence high/medium/low
- chip-blue for predictor source
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stage1EvidencePanel pure component + tests</name>
  <files>web/components/automations/bulk-review/audit/Stage1EvidencePanel.tsx, web/components/automations/bulk-review/audit/__tests__/Stage1EvidencePanel.test.tsx</files>
  <read_first>
    - web/components/automations/bulk-review/audit/Stage0EvidencePanel.tsx (analog from Plan 03 — same shape)
    - docs/agentic-pipeline/stage-1-regex.md (closed-list = noise keys + unknown)
    - web/app/(dashboard)/automations/[swarm]/_shell/_lib/audit-types.ts
  </read_first>
  <behavior>
    - REASONING header + reasoning body when `reasoning` non-null; otherwise muted `No evidence captured for this stage.`
    - EVIDENCE header with three chips in this order:
      1. rule chip: text = `rule_key ?? "(no rule)"`, mono 12px, chip-teal background. `data-testid="stage1-rule-chip"`. Renders `(no rule)` muted when null.
      2. predictor source chip: text = `predictor_source ?? "—"`, chip-blue, `data-testid="stage1-predictor-chip"`. Renders muted dash when null.
      3. confidence chip: text = `confidence ?? "—"`, color by value (`high` → chip-lime, `medium` → chip-amber, `low` / null → text-muted), `data-testid="stage1-confidence-chip"`.
    - Degraded mode (Pass-1 regex short-circuit, no LLM reasoning): `reasoning === null && predictor_source === "regex"` → render REASONING block with muted italic `Regex Pass-1 matched — no LLM reasoning produced.` (locked copy from CONTEXT.md `<open_questions_for_discuss>` resolution)
    - Hard-separation guardrail (compile-time): file MUST NOT import from `swarm_intents` or reference `intent_key` in source
    - Includes `data-testid="stage1-raw-json-slot"` placeholder
  </behavior>
  <action>
    Create `web/components/automations/bulk-review/audit/Stage1EvidencePanel.tsx`. Mirror Stage0EvidencePanel's shape (same import style, same inline `var(--v7-*)` tokens, same 11px/13px/12px-mono typography).

    Hard-separation enforcement: at the top of the file, add this comment block (verbatim — Plan 11 + downstream checkers grep for it):
    ```
    // Phase 82.3 Plan 04 — Stage 1 evidence panel.
    // HARD-SEPARATION LOCK (docs/agentic-pipeline/README.md):
    //   - rule_key comes from swarm_noise_categories OR the literal "unknown".
    //   - This file MUST NOT import or reference swarm_intents.
    //   - Renderer for Stage 3 intent evidence lives in Stage3EvidencePanel.tsx.
    ```

    Test file mirrors `Stage0EvidencePanel.test.tsx` patterns. Cases:
    - Pass-1 regex full payload (rule_key="newsletter", predictor_source="regex", confidence="high", reasoning=null) → degraded-mode REASONING copy renders; rule chip shows "newsletter"; predictor chip shows "regex"; confidence chip shows "high"
    - Pass-2 LLM payload (rule_key="unknown", predictor_source="llm", confidence="medium", reasoning="Email looks like marketing but mentions an invoice number") → reasoning body renders; rule chip shows "unknown"
    - All-null payload → only empty-state copy
    - Snapshot test: rendered DOM does NOT contain any swarm_intents-domain string (no `intent_key`, no `ranked_intents`, no intent vocabulary). Use `screen.queryByText(/intent/i)` returns null.
  </action>
  <verify>
    <automated>test -f web/components/automations/bulk-review/audit/Stage1EvidencePanel.tsx &amp;&amp; ! grep -E "swarm_intents|intent_key|ranked_intents" web/components/automations/bulk-review/audit/Stage1EvidencePanel.tsx &amp;&amp; cd web &amp;&amp; npx vitest run --reporter=dot components/automations/bulk-review/audit/__tests__/Stage1EvidencePanel.test.tsx 2>&amp;1 | tail -20 &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists
    - `grep -c "export function Stage1EvidencePanel" Stage1EvidencePanel.tsx` = 1
    - `grep "HARD-SEPARATION LOCK" Stage1EvidencePanel.tsx` matches (locked comment)
    - `grep -E "swarm_intents|intent_key|ranked_intents" Stage1EvidencePanel.tsx` returns ZERO matches (hard-separation guardrail; the `!` in verify command enforces this)
    - `grep "Regex Pass-1 matched — no LLM reasoning produced." Stage1EvidencePanel.tsx` matches (locked degraded copy)
    - `grep -E "stage1-(rule|predictor|confidence)-chip" Stage1EvidencePanel.tsx` returns 3 testids
    - vitest exit 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Stage1EvidencePanel respects hard-separation, handles both Pass-1 and Pass-2 modes, vitest green.</done>
</task>

</tasks>

<verification>
Vitest green; tsc clean; hard-separation grep clean.
</verification>

<success_criteria>
Plan 11 can `import { Stage1EvidencePanel }` and feed `Stage1AuditPayload` derived from `pipeline_events.decision_details` where `stage = 1`.
</success_criteria>

<output>
After completion, create `.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-04-SUMMARY.md`.
</output>
