---
phase: 82.3
plan: 04
subsystem: bulk-review-audit-ui
tags: [react, tdd, stage-1, evidence-panel, hard-separation]
requires:
  - .planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-02-SUMMARY.md
provides:
  - "Stage1EvidencePanel: pure renderer for Stage 1 (noise filter) audit data"
affects:
  - web/components/automations/bulk-review/audit/Stage1EvidencePanel.tsx
tech-stack:
  added: []
  patterns:
    - "Mirror-of-Stage0 component shape (V7 tokens, inline CSS, 11/12/13px typography)"
    - "Hard-separation lock comment block grep-checkable by downstream plans"
key-files:
  created:
    - web/components/automations/bulk-review/audit/Stage1EvidencePanel.tsx
    - web/components/automations/bulk-review/audit/__tests__/Stage1EvidencePanel.test.tsx
  modified: []
decisions:
  - "Render '(no rule)' muted when rule_key is null (matches Stage0 conventions)"
  - "Predictor and confidence chips fall back to muted em-dash, not omitted, so the chip-row layout stays stable across Pass-1/Pass-2/null payloads"
  - "Hard-separation comment phrased as 'the Stage 3 registry' rather than the literal token, so the file passes its own grep guard while staying semantically locked"
metrics:
  duration: ~5 min
  completed: 2026-05-13
  tasks: 1
  files: 2
---

# Phase 82.3 Plan 04: Stage1EvidencePanel Summary

Pure read-only React component renders Stage 1 noise-filter audit data — which rule fired (regex Pass-1 OR LLM Pass-2 `unknown`), confidence chip, predictor-source chip — with locked degraded-mode copy when Pass-1 short-circuited without LLM reasoning.

## Tasks Completed

| Task | Name                                                | Commit  | Files                                                                  |
| ---- | --------------------------------------------------- | ------- | ---------------------------------------------------------------------- |
| 1a   | RED: failing test for Stage1EvidencePanel           | a430316 | __tests__/Stage1EvidencePanel.test.tsx                                 |
| 1b   | GREEN: Stage1EvidencePanel with hard-separation     | 1959e0b | Stage1EvidencePanel.tsx                                                |

## TDD Gate Compliance

- RED commit (`test(82.3-04)`): a430316 — 9 failing tests
- GREEN commit (`feat(82.3-04)`): 1959e0b — 9/9 passing

## Verification

- `vitest run` → 9/9 passed (1.07s)
- `grep -E "swarm_intents|intent_key|ranked_intents" Stage1EvidencePanel.tsx` → 0 matches (hard-separation guard)
- `grep "HARD-SEPARATION LOCK"` → present
- `grep "Regex Pass-1 matched — no LLM reasoning produced."` → present
- `grep -cE "stage1-(rule|predictor|confidence)-chip"` → 3
- `npx tsc --noEmit` → 1 pre-existing error in `app/(dashboard)/automations/[swarm]/stage-1/__tests__/actions.predictor.test.ts` (unrelated to this plan; logged as deferred)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Comment wording adjusted to satisfy own grep guard**
- **Found during:** Task 1 verification
- **Issue:** Initial HARD-SEPARATION LOCK comment block contained the literal token `swarm_intents` ("MUST NOT import or reference swarm_intents"), which made the file fail its own acceptance grep `grep -E "swarm_intents|intent_key|ranked_intents"` (returned 1 match instead of 0).
- **Fix:** Rephrased to "MUST NOT import or reference the Stage 3 registry" — semantically equivalent, grep-clean.
- **Files modified:** Stage1EvidencePanel.tsx (in-flight, before initial commit)
- **Commit:** rolled into 1959e0b

## Deferred Issues

- Pre-existing tsc error in `app/(dashboard)/automations/[swarm]/stage-1/__tests__/actions.predictor.test.ts:60` — TS2556 spread arg, not introduced by this plan. Not my surface.

## Self-Check: PASSED

- File exists: web/components/automations/bulk-review/audit/Stage1EvidencePanel.tsx ✓
- File exists: web/components/automations/bulk-review/audit/__tests__/Stage1EvidencePanel.test.tsx ✓
- Commit a430316 in git log ✓
- Commit 1959e0b in git log ✓
