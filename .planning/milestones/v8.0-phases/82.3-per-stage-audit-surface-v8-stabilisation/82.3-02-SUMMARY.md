---
phase: 82.3
plan: 02
subsystem: bulk-review-detail-pane
tags: [types, plumbing, audit-surface, hard-separation]
requires: []
provides:
  - StageAuditPayload discriminated union (Stage 0-3)
  - StageAuditMap (Partial<Record<0|1|2|3, ReactNode>>)
  - StageData.auditDetails optional ReactNode slot
  - UnifiedDetailPaneProps.stageAudit optional map prop
affects:
  - web/app/(dashboard)/automations/[swarm]/stage-1/components/pipeline-flow.tsx
  - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx
tech-stack:
  added: []
  patterns:
    - Hard-separation lock encoded at the type level (Stage1 ↔ swarm_noise_categories; Stage3 ↔ swarm_intents)
    - Pre-rendered ReactNode slot (rather than payload + renderer) keeps detail-pane decoupled from per-stage renderers
key-files:
  created:
    - web/app/(dashboard)/automations/[swarm]/_shell/_lib/audit-types.ts
  modified:
    - web/app/(dashboard)/automations/[swarm]/stage-1/components/pipeline-flow.tsx
    - web/app/(dashboard)/automations/[swarm]/_shell/detail-pane.tsx
decisions:
  - "Map values are ReactNode (already-rendered) rather than StageAuditPayload — keeps the shell renderer-agnostic"
  - "Stage 4 explicitly excluded from the map type (Partial Record over 0|1|2|3 only) per CONTEXT out_of_scope"
metrics:
  duration: ~3 min
  completed: 2026-05-13
  tasks: 2
  files_changed: 3
---

# Phase 82.3 Plan 02: Per-stage audit surface — type plumbing Summary

Added `StageAuditPayload` discriminated union + `StageAuditMap` source-of-truth types and threaded an optional `auditDetails` ReactNode slot through `StageData` → `UnifiedDetailPaneProps` → `stagesData` useMemo. Pure plumbing — zero runtime behavior change. Plans 03–06 build per-stage renderers against the payload types; Plan 07 wires `<StageStep>` to render the slot; Plan 11 passes `stageAudit={...}` from each page.

## What changed

### Task 1 — `audit-types.ts` (commit `df8d728`)
- New file `web/app/(dashboard)/automations/[swarm]/_shell/_lib/audit-types.ts`
- Discriminated union `StageAuditPayload = Stage0 | Stage1 | Stage2 | Stage3` keyed by `stage: 0|1|2|3`
- Hard-separation lock encoded in field naming + comments:
  - `Stage1AuditPayload.rule_key` references `swarm_noise_categories` only (`"unknown"` when LLM Pass-2 fired)
  - `Stage3AuditPayload.ranked_intents[].intent_key` and `selected_intent_key` reference `swarm_intents` only
- `StageAuditMap = Partial<Record<0|1|2|3, ReactNode>>` for already-rendered nodes consumed by detail-pane

### Task 2 — Slot threaded end-to-end (commit `3cdab9a`)
- `StageData` (`pipeline-flow.tsx`) gains `auditDetails?: ReactNode`
- `UnifiedDetailPaneProps` (`detail-pane.tsx`) gains `stageAudit?: StageAuditMap` + `import type { StageAuditMap } from "./_lib/audit-types"`
- `stageAudit` destructured in both `UnifiedDetailPane` (outer) and `DetailPaneInner` (inner) and forwarded outer→inner
- `stagesData` useMemo writes `auditDetails: n === 4 ? undefined : stageAudit?.[n as 0|1|2|3]` and adds `stageAudit` to its dep array
- Stage 4 explicitly excluded per CONTEXT `<out_of_scope>`

## Verification

- `npx tsc --noEmit` clean (only pre-existing TS2556 in `actions.predictor.test.ts`, out of scope per success criteria)
- All grep acceptance criteria pass:
  - `auditDetails?: ReactNode` in `pipeline-flow.tsx` = 1
  - `stageAudit?: StageAuditMap` in `detail-pane.tsx` = 1
  - `import type { StageAuditMap }` in `detail-pane.tsx` = 1
  - `auditDetails:` assignment in `stagesData` useMemo (line 337)
- Hard-separation comments present (`swarm_noise_categories`, `swarm_intents`)
- 6+ exports in `audit-types.ts` (Stage0-3 interfaces + StageAuditPayload + StageAuditMap)

## Deviations from Plan

None — plan executed exactly as written.

## Commits

| Task | Hash      | Message                                                                |
| ---- | --------- | ---------------------------------------------------------------------- |
| 1    | `df8d728` | feat(82.3-02): add StageAuditPayload + StageAuditMap source-of-truth   |
| 2    | `3cdab9a` | feat(82.3-02): thread auditDetails slot through StageData + UnifiedDetailPane |

## Self-Check: PASSED

- FOUND: web/app/(dashboard)/automations/[swarm]/_shell/_lib/audit-types.ts
- FOUND: commit df8d728
- FOUND: commit 3cdab9a
