---
phase: 82.3
plan: 06
type: execute
wave: 2
depends_on: [02]
files_modified:
  - web/components/automations/bulk-review/audit/Stage3EvidencePanel.tsx
  - web/components/automations/bulk-review/audit/__tests__/Stage3EvidencePanel.test.tsx
autonomous: true
requirements: [82.3-STAGE3-RENDERER]
must_haves:
  truths:
    - "Stage 3 expander shows the ranked intent list (top-down) with confidence chips, the selected intent highlighted, and the coordinator's one-line reasoning"
    - "Empty ranked_intents renders the locked copy without throwing"
  artifacts:
    - path: "web/components/automations/bulk-review/audit/Stage3EvidencePanel.tsx"
      provides: "Stage 3 evidence renderer (intent registry only — hard-separation lock)"
      contains: "export function Stage3EvidencePanel"
  key_links:
    - from: "Stage 3 page audit-payload mapper (Plan 11)"
      to: "Stage3EvidencePanel"
      via: "props.payload: Stage3AuditPayload"
      pattern: "&lt;Stage3EvidencePanel payload="
---

<objective>
Stage 3 evidence renderer per UI-SPEC. Renders the ranked intent list from `swarm_intents` (via `agent_runs.tool_outputs` of the `debtor-intent-agent`, Phase 65) — hard-separation lock: NEVER reference `swarm_noise_categories` or noise vocabulary.

Purpose: Operators need to see the coordinator's ranked candidates + why-selected reasoning to judge whether a Stage 3 verdict is sane before approving or overriding.
Output: Pure functional component + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-UI-SPEC.md
@.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-CONTEXT.md
@docs/agentic-pipeline/stage-3-coordinator.md
@web/app/(dashboard)/automations/[swarm]/_shell/_lib/audit-types.ts
@web/components/automations/bulk-review/audit/Stage0EvidencePanel.tsx

<interfaces>
From audit-types.ts:
```typescript
export interface Stage3AuditPayload {
  stage: 3;
  ranked_intents: Array&lt;{ intent_key: string; confidence: number }&gt;;
  coordinator_reasoning: string | null;
  selected_intent_key: string | null;
  raw: Record&lt;string, unknown&gt;;
}
```

Hard-separation lock: `intent_key` references `swarm_intents` keys ONLY. NEVER swarm_noise_categories.

UI-SPEC locked copy:
- `No ranked intents returned by coordinator.`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stage3EvidencePanel pure component + tests</name>
  <files>web/components/automations/bulk-review/audit/Stage3EvidencePanel.tsx, web/components/automations/bulk-review/audit/__tests__/Stage3EvidencePanel.test.tsx</files>
  <read_first>
    - web/components/automations/bulk-review/audit/Stage0EvidencePanel.tsx
    - docs/agentic-pipeline/stage-3-coordinator.md
    - web/app/(dashboard)/automations/[swarm]/_shell/_lib/audit-types.ts
  </read_first>
  <behavior>
    - REASONING header + `coordinator_reasoning` body, OR muted italic empty-state when null
    - EVIDENCE header + ordered list of `ranked_intents`:
      - each row: intent_key (mono, 12px), confidence chip (numeric 0–1, formatted `{(c*100).toFixed(0)}%`)
      - chip color: ≥0.75 chip-lime; 0.5–0.74 chip-amber; &lt;0.5 text-muted
      - row with `intent_key === selected_intent_key` gets `data-testid="stage3-intent-row-selected"` + bold weight + leading `▶` indicator
      - non-selected rows: `data-testid="stage3-intent-row-{intent_key}"`, regular weight
    - `ranked_intents.length === 0` → muted `No ranked intents returned by coordinator.` AND skip the list (header still renders)
    - All-null payload (no reasoning, empty list, null selected) → only top-level empty-state line
    - Hard-separation guardrail: file MUST NOT reference `swarm_noise_categories` or `rule_key`
    - `data-testid="stage3-raw-json-slot"` placeholder
  </behavior>
  <action>
    Create `web/components/automations/bulk-review/audit/Stage3EvidencePanel.tsx`. Mirror Stage1EvidencePanel's hard-separation header comment:
    ```
    // Phase 82.3 Plan 06 — Stage 3 evidence panel.
    // HARD-SEPARATION LOCK (docs/agentic-pipeline/README.md):
    //   - intent_key comes from swarm_intents ONLY.
    //   - This file MUST NOT import or reference swarm_noise_categories.
    //   - Renderer for Stage 1 noise evidence lives in Stage1EvidencePanel.tsx.
    ```

    Locked copy: `No ranked intents returned by coordinator.`

    Test cases:
    - full payload (3 ranked intents, selected_intent_key matches the top one, coordinator_reasoning present) → reasoning body renders; selected row has the `selected` testid + bold; other 2 rows have per-key testids
    - selected_intent_key matches a non-top entry → that mid-rank row gets selected styling
    - empty ranked_intents → locked copy renders
    - all-null → only empty-state line
    - guardrail: rendered DOM contains no `rule_key` or `swarm_noise` substring (assert `screen.queryByText(/rule_key|noise/i)` returns null)
  </action>
  <verify>
    <automated>test -f web/components/automations/bulk-review/audit/Stage3EvidencePanel.tsx &amp;&amp; ! grep -E "swarm_noise_categories|rule_key" web/components/automations/bulk-review/audit/Stage3EvidencePanel.tsx &amp;&amp; cd web &amp;&amp; npx vitest run --reporter=dot components/automations/bulk-review/audit/__tests__/Stage3EvidencePanel.test.tsx 2>&amp;1 | tail -20 &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function Stage3EvidencePanel" Stage3EvidencePanel.tsx` = 1
    - `grep "HARD-SEPARATION LOCK" Stage3EvidencePanel.tsx` matches
    - `grep -E "swarm_noise_categories|rule_key" Stage3EvidencePanel.tsx` returns ZERO matches
    - `grep "No ranked intents returned by coordinator." Stage3EvidencePanel.tsx` matches
    - `grep "stage3-intent-row-selected" Stage3EvidencePanel.tsx` matches
    - vitest exit 0 with at least 4 cases
    - tsc clean
  </acceptance_criteria>
  <done>Stage3EvidencePanel honors hard-separation, renders ranked list with selected highlight, vitest green.</done>
</task>

</tasks>

<verification>
Vitest green; tsc clean; hard-separation grep clean.
</verification>

<success_criteria>
Plan 11 can `import { Stage3EvidencePanel }` and feed `Stage3AuditPayload` derived from `agent_runs.tool_outputs` where `stage = 3`.
</success_criteria>

<output>
After completion, create `.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-06-SUMMARY.md`.
</output>
