---
phase: 82.3
plan: 01
subsystem: web/components/ui
tags: [shadcn, radix, ui-primitive, design-system]
requires: [radix-ui ^1.4.3 (already in deps)]
provides:
  - "@/components/ui/collapsible (Collapsible / CollapsibleTrigger / CollapsibleContent)"
affects:
  - "Downstream Phase 82.3 plans 03-09: stage audit expander rows"
tech-stack:
  added: []
  patterns:
    - "shadcn re-export pattern: function component wrapping Radix primitive with data-slot attribute (matches existing badge.tsx)"
key-files:
  created:
    - web/components/ui/collapsible.tsx
  modified: []
decisions:
  - "Used shadcn CLI as-emitted (radix-ui umbrella import) instead of per-primitive @radix-ui/react-collapsible; matches the repo's existing badge.tsx convention and the radix-ui ^1.4.3 dep already in package.json"
metrics:
  duration: ~3min
  completed: 2026-05-13
  tasks: 1
  files: 1
  commits: 1
---

# Phase 82.3 Plan 01: Install shadcn Collapsible Primitive — Summary

shadcn `Collapsible` primitive added at `web/components/ui/collapsible.tsx`, unblocking every downstream Phase 82.3 stage-audit expander.

## What Was Built

- `web/components/ui/collapsible.tsx` — shadcn re-export of Radix Collapsible (`Collapsible`, `CollapsibleTrigger`, `CollapsibleContent`), each wrapping the Radix primitive with a `data-slot` attribute.
- No `package.json` change required: the repo already pins `radix-ui ^1.4.3` (umbrella package), which the new file imports from. This matches the convention set by `web/components/ui/badge.tsx` (`import { Slot } from "radix-ui"`).

## Verification

- `web/components/ui/collapsible.tsx` exists.
- File exports the three required names via the final `export { Collapsible, CollapsibleTrigger, CollapsibleContent }` statement.
- `cd web && npx tsc --noEmit` introduces zero new errors. The single pre-existing error (`app/(dashboard)/automations/[swarm]/stage-1/__tests__/actions.predictor.test.ts:60` — TS2556 spread argument) is reproducible on `main` before this commit and is unrelated.
- Downstream import path `@/components/ui/collapsible` resolves cleanly (verified via tsc).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Spec/repo mismatch] Plan acceptance criterion required literal `@radix-ui/react-collapsible` in file + package.json**
- **Found during:** Task 1 verification.
- **Issue:** The plan's `verify.automated` block + acceptance criteria assumed shadcn would emit a per-primitive Radix import (`@radix-ui/react-collapsible`) and would add that as a new dep. But the current `shadcn@latest` CLI (and this repo's existing convention — see `web/components/ui/badge.tsx` line 3: `import { Slot } from "radix-ui"`) uses the **umbrella `radix-ui` package** (already pinned at `^1.4.3` in `web/package.json`). Forcing the per-primitive form would have introduced a parallel dep tree, violated the existing convention, and contradicted CLAUDE.md's "follow existing patterns" guidance.
- **Fix:** Kept the shadcn-emitted file as-is (`import { Collapsible as CollapsiblePrimitive } from "radix-ui"`). The plan's MUST-HAVE truth ("Radix Collapsible primitive is importable from @/components/ui/collapsible") is fully satisfied — the runtime behaviour and the downstream import path are exactly what plans 03-09 expect.
- **Files modified:** `web/components/ui/collapsible.tsx` (CLI-generated, untouched after).
- **Commit:** 5bc2d1e

### Auth Gates

None.

## Known Stubs

None.

## Self-Check: PASSED

- FOUND: web/components/ui/collapsible.tsx
- FOUND: commit 5bc2d1e in `git log`
- FOUND: 3 named exports (`Collapsible`, `CollapsibleTrigger`, `CollapsibleContent`)
- FOUND: `radix-ui` import in collapsible.tsx (umbrella package, ^1.4.3 already in deps)

## Commits

- `5bc2d1e` feat(82.3-01): install shadcn Collapsible primitive
