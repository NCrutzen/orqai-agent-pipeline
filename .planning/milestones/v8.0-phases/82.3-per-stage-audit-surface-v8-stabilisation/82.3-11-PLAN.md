---
phase: 82.3
plan: 11
type: execute
wave: 4
depends_on: [02, 03, 04, 05, 06, 07, 08, 09, 10]
files_modified:
  - web/app/(dashboard)/automations/[swarm]/_shell/_lib/build-stage-audit-map.ts
  - web/app/(dashboard)/automations/[swarm]/_shell/_lib/__tests__/build-stage-audit-map.test.ts
  - web/app/(dashboard)/automations/[swarm]/stage-0/page.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-2/page.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-3/page.tsx
  - web/app/(dashboard)/automations/[swarm]/stage-4/page.tsx
autonomous: false
requirements: [82.3-PER-PAGE-WIRING, 82.3-CHECKPOINT-UAT]
must_haves:
  truths:
    - "Each stage page (0, 1, 2, 3, 4) loads per-row JSONB audit payloads from pipeline_events / agent_runs / automation_runs and passes a stageAudit prop to UnifiedDetailPane"
    - "For an email with complete Stage 0–3 coverage, each expander reveals verdict + reasoning + evidence + screenshots when present"
    - "Stage 4 row remains untouched per CONTEXT <out_of_scope>"
    - "Operator UAT confirms the surface on the locked acceptance scenarios"
  artifacts:
    - path: "web/app/(dashboard)/automations/[swarm]/_shell/_lib/build-stage-audit-map.ts"
      provides: "Pure mapper: PipelineTimelineEvent[] + agent_runs/automation_runs rows → StageAuditMap"
      contains: "export function buildStageAuditMap"
  key_links:
    - from: "stage page loaders"
      to: "UnifiedDetailPane stageAudit prop"
      via: "buildStageAuditMap(timeline, agentRuns, automationRuns)"
      pattern: "stageAudit={"
---

<objective>
Final integration: build the JSONB→StageAuditPayload mapper, render the per-stage panels via that mapper, and thread the resulting `StageAuditMap` into `UnifiedDetailPane` from each stage page that already mounts the shell. Then hold a checkpoint for operator UAT on the locked acceptance scenarios from CONTEXT.md `<must_haves>`.

Purpose: Connect Wave 2 renderers + Plan 02 plumbing + Plan 07 expander + Plan 08 signed-URL API into a working end-to-end surface that the operator actually sees in Bulk Review.
Output: One mapper + tests + 5 page wirings + an operator UAT checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-CONTEXT.md
@.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-UI-SPEC.md
@docs/agentic-pipeline/README.md
@web/app/(dashboard)/automations/[swarm]/_shell/_lib/audit-types.ts
@web/lib/pipeline-events/types.ts
@web/app/(dashboard)/automations/[swarm]/stage-0/page.tsx
@web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx
@web/app/(dashboard)/automations/[swarm]/stage-2/page.tsx
@web/app/(dashboard)/automations/[swarm]/stage-3/page.tsx
@web/app/(dashboard)/automations/[swarm]/stage-4/page.tsx
@web/components/automations/bulk-review/audit/Stage0EvidencePanel.tsx
@web/components/automations/bulk-review/audit/Stage1EvidencePanel.tsx
@web/components/automations/bulk-review/audit/Stage2EvidencePanel.tsx
@web/components/automations/bulk-review/audit/Stage3EvidencePanel.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildStageAuditMap mapper + tests</name>
  <files>web/app/(dashboard)/automations/[swarm]/_shell/_lib/build-stage-audit-map.ts, web/app/(dashboard)/automations/[swarm]/_shell/_lib/__tests__/build-stage-audit-map.test.ts</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/_shell/_lib/audit-types.ts
    - web/lib/pipeline-events/types.ts
    - docs/agentic-pipeline/README.md §Stage 0..3 decision_details shape
    - docs/agentic-pipeline/stage-1-regex.md
    - docs/agentic-pipeline/stage-3-coordinator.md
    - web/lib/pipeline-events/__tests__/fixtures/override-events.ts (decision_details samples)
  </read_first>
  <behavior>
    - Input: `{ timeline: PipelineTimelineEvent[], agentRuns?: Array<{ stage: number; context: Record<string,unknown> | null; tool_outputs: Record<string,unknown> | null }>, automationRun?: { result: Record<string,unknown> | null } | null }`
    - Output: `StageAuditMap` — `Partial<Record<0 | 1 | 2 | 3, ReactNode>>` where each present key holds a `<StageNEvidencePanel payload={...} />` ReactNode
    - Stage 0 mapper: from `timeline.find(e => e.stage === 0)?.decision_details` extract `regex_patterns_fired` (default `[]`), `llm_injection_verdict` (cast against allowed enum or null), `llm_reasoning`, `budget_headroom_cents`; pass entire decision_details as `raw`
    - Stage 1 mapper: from `timeline.find(e => e.stage === 1)?.decision_details` extract `rule_key` (closed-list: any swarm_noise_categories key OR literal `"unknown"`), `predictor_source` (`"regex"` if event has no agent_run, `"llm"` if `agent_runs[stage=1]` present), `confidence` (map numeric `confidence` field via `numericToLabel`: ≥0.75 → "high"; 0.5–0.74 → "medium"; <0.5 → "low"; null → null), `reasoning` (from agent_runs[stage=1]?.tool_outputs.reasoning); raw = decision_details merged with tool_outputs
    - Stage 2 mapper: from `agent_runs[stage=2]?.context` extract `identifier_source`, `confidence`, `top_candidates` (default `[]`); from `automationRun?.result` extract `screenshot_paths.before` and `.after` (look at keys ending in `_before.png` / `_after.png`, or `result.screenshots: { before, after }`); raw = context merged with result
    - Stage 3 mapper: from `agent_runs[stage=3]?.tool_outputs` extract `ranked_intents` (default `[]` — items shaped `{ intent_key, confidence }` from swarm_intents taxonomy ONLY), `coordinator_reasoning`, `selected_intent_key`; raw = tool_outputs
    - If a stage has no source data at all (no timeline event AND no agent_runs row), DO NOT add that key to the map (caller renders the empty-state via the renderer's all-null branch — but actually omitting the key means the expander won't render at all per Plan 07's `auditDetails != null` guard; that's the correct behavior for "stage didn't run")
    - Hard-separation lock: stage 1 mapper writes `rule_key` only; stage 3 mapper writes `intent_key` only; mappers MUST NOT cross-populate
    - Defensive: on malformed JSONB (wrong shape, undefined fields), default to null/[] — never throw
  </behavior>
  <action>
    Create `web/app/(dashboard)/automations/[swarm]/_shell/_lib/build-stage-audit-map.ts`. Pure function. Returns `StageAuditMap`. Imports the four `Stage{N}EvidencePanel` components and renders each ReactNode inline:
    ```typescript
    return {
      0: stage0Present ? <Stage0EvidencePanel payload={stage0Payload} /> : undefined,
      1: ...
      ...
    };
    ```
    Filter undefined keys before returning (so consumers don't trip the truthy-check accidentally).

    Top of file (verbatim header):
    ```
    // Phase 82.3 Plan 11 — JSONB → StageAuditPayload mapper.
    // HARD-SEPARATION LOCK:
    //   - Stage 1 payload.rule_key sourced from pipeline_events.decision_details
    //     (closed list: swarm_noise_categories keys ∪ {"unknown"}).
    //   - Stage 3 payload.ranked_intents sourced from agent_runs.tool_outputs
    //     for stage=3 ONLY (swarm_intents taxonomy).
    //   - Mappers MUST NOT cross-populate. If a future RFC ever conflates the
    //     two registries, fix the RFC first — do not bend this mapper.
    ```

    Test cases (vitest):
    - complete fixture (timeline events for all 4 stages + agent_runs for stages 1/2/3 + automationRun.result with screenshots) → map has all 4 keys; rendered each stage panel testid present
    - Stage 0 only (others null) → map has only key `0`
    - Stage 1 Pass-1 regex (no agent_run row for stage=1) → predictor_source="regex", reasoning=null in payload; degraded copy renders in panel
    - Stage 1 Pass-2 LLM (agent_run for stage=1 with reasoning) → predictor_source="llm"
    - Stage 2 unresolved (agent_run.context.identifier_source="unresolved", top_candidates=[]) → renderer shows locked unresolved copy
    - Stage 2 missing screenshots → screenshot_paths both null → locked "No iController capture" copy
    - Stage 3 with 5 ranked_intents → all 5 in payload (truncation is renderer's concern)
    - malformed JSONB (`decision_details = { random: "garbage" }`) → defaults applied, no throw, returns partial payload
    - hard-separation guardrail: snapshot test asserts Stage 1 panel DOM never contains `intent_key`; Stage 3 panel DOM never contains `rule_key`
  </action>
  <verify>
    <automated>test -f web/app/\(dashboard\)/automations/\[swarm\]/_shell/_lib/build-stage-audit-map.ts &amp;&amp; grep -q "HARD-SEPARATION LOCK" web/app/\(dashboard\)/automations/\[swarm\]/_shell/_lib/build-stage-audit-map.ts &amp;&amp; cd web &amp;&amp; npx vitest run --reporter=dot app/\\(dashboard\\)/automations/\\[swarm\\]/_shell/_lib/__tests__/build-stage-audit-map.test.ts 2>&amp;1 | tail -25 &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `grep "HARD-SEPARATION LOCK" build-stage-audit-map.ts` matches
    - `grep -c "Stage[0-3]EvidencePanel" build-stage-audit-map.ts` = 4 (one import + one use per stage)
    - vitest exit 0 with ≥8 cases
    - tsc clean
  </acceptance_criteria>
  <done>Pure mapper ready; covers happy path, degraded, missing, malformed, and hard-sep guardrail.</done>
</task>

<task type="auto">
  <name>Task 2: Wire stageAudit prop into stage-0, stage-1, stage-2, stage-3, stage-4 pages</name>
  <files>web/app/(dashboard)/automations/[swarm]/stage-0/page.tsx, web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx, web/app/(dashboard)/automations/[swarm]/stage-2/page.tsx, web/app/(dashboard)/automations/[swarm]/stage-3/page.tsx, web/app/(dashboard)/automations/[swarm]/stage-4/page.tsx</files>
  <read_first>
    - web/app/(dashboard)/automations/[swarm]/stage-0/page.tsx (find the existing UnifiedDetailPane mount + the loader that builds `timeline`)
    - web/app/(dashboard)/automations/[swarm]/stage-1/page.tsx (same)
    - web/app/(dashboard)/automations/[swarm]/stage-2/page.tsx (same)
    - web/app/(dashboard)/automations/[swarm]/stage-3/page.tsx (same)
    - web/app/(dashboard)/automations/[swarm]/stage-4/page.tsx (same)
    - web/app/(dashboard)/automations/[swarm]/_shell/_lib/build-stage-audit-map.ts (Task 1)
  </read_first>
  <action>
    **Coverage note (per checker warning 1, revision 2026-05-12):** ALL FIVE stage pages mount `UnifiedDetailPane` (verified via `grep -l UnifiedDetailPane web/app/(dashboard)/automations/[swarm]/stage-*/page.tsx`). An operator selecting a row from the Stage 0 list view or the Stage 2 list view sees the same detail pane sidebar that Stage 1/3/4 show — therefore all five pages must thread `stageAudit` or the expanders silently no-op for two of the five entry points.

    For each of the five page files, in the loader/data-prep section that already produces `timeline: PipelineTimelineEvent[]`:

    1. Additionally load (server-side, via the existing admin/service-role Supabase client used by the page) the relevant rows for the SELECTED email/row only — do NOT bulk-load for the whole list:
       - `agent_runs` rows where `email_id = selectedEmailId` (columns: `stage, context, tool_outputs`). Stage filter: `in (0, 1, 2, 3)`.
       - `automation_runs.result` for the row's `automation_run_id` if available (single row).
    2. Compute `const stageAudit = buildStageAuditMap({ timeline, agentRuns, automationRun });`
    3. Pass `stageAudit={stageAudit}` into `<UnifiedDetailPane … />`.

    If no email is selected (list view, empty selection), pass `stageAudit={undefined}` — UnifiedDetailPane's empty-row branch already handles this.

    Stage 0 / Stage 2 / Stage 4 pages: same wiring. The Stage 4 ROW inside the pane has no expander per Plan 07, but Stages 0–3 still need to show evidence when viewing emails through any stage's surface. Identical `buildStageAuditMap(...)` call shape across all five pages — the mapper is page-agnostic.

    Do NOT modify any other loader behavior, query, or render path. Keep the data-fetch scoped to "selected row only" to avoid N+1 cost on the list view.
  </action>
  <verify>
    <automated>grep -c "stageAudit=" web/app/\(dashboard\)/automations/\[swarm\]/stage-0/page.tsx web/app/\(dashboard\)/automations/\[swarm\]/stage-1/page.tsx web/app/\(dashboard\)/automations/\[swarm\]/stage-2/page.tsx web/app/\(dashboard\)/automations/\[swarm\]/stage-3/page.tsx web/app/\(dashboard\)/automations/\[swarm\]/stage-4/page.tsx &amp;&amp; grep -c "buildStageAuditMap" web/app/\(dashboard\)/automations/\[swarm\]/stage-0/page.tsx web/app/\(dashboard\)/automations/\[swarm\]/stage-1/page.tsx web/app/\(dashboard\)/automations/\[swarm\]/stage-2/page.tsx web/app/\(dashboard\)/automations/\[swarm\]/stage-3/page.tsx web/app/\(dashboard\)/automations/\[swarm\]/stage-4/page.tsx &amp;&amp; cd web &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -10 &amp;&amp; npx vitest run --reporter=dot app/\\(dashboard\\)/automations 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Each of stage-0/page.tsx, stage-1/page.tsx, stage-2/page.tsx, stage-3/page.tsx, stage-4/page.tsx contains exactly one `stageAudit={buildStageAuditMap(...)}` JSX assignment
    - Each contains exactly one `buildStageAuditMap` import + one call
    - `grep -l "stageAudit={buildStageAuditMap" web/app/(dashboard)/automations/[swarm]/stage-*/page.tsx` returns all 5 files
    - tsc clean
    - Existing page tests still pass (any regression = stop and fix before checkpoint)
    - Bulk Stage 0 telemetry (Phase 82.2) is already shipped — `pipeline_events.decision_details` for stage=0 is populated for new debtor-email rows
  </acceptance_criteria>
  <done>End-to-end wiring complete across all 5 stage pages; pages compile and existing tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator UAT — Bulk Review per-stage audit surface</name>
  <what-built>
    Per-stage audit expanders are now wired into the Bulk Review detail-pane sidebar for ALL FIVE stage pages: `/automations/debtor-email/stage-0`, `/stage-1`, `/stage-2`, `/stage-3`, and `/stage-4`. Each Stage 0–3 row in the sidebar now shows a `Show details` toggle that reveals verdict + reasoning + evidence + screenshots. Stage 4 row is unchanged per CONTEXT `<out_of_scope>`. The "Planning" / "(no subject)" display bug is fixed at the header.

    **Precondition:** Phase 82.2 has shipped (verified via `/gsd-progress`). Without 82.2's Stage 0 telemetry, the Stage 0 expander shows the empty-state copy for 55–74% of debtor emails — that is expected, not a 82.3 bug.
  </what-built>
  <how-to-verify>
    Run the local dev server (`cd web && npm run dev`) and sign in as an operator. Then, with the LIVE acceptance test environment (CLAUDE.md §Test-First Pattern), step through the locked CONTEXT.md `<must_haves>` checklist:

    1. **Navigate** to `/automations/debtor-email/stage-1`. Select any predicted row.
       - Expect: detail-pane shows 5 stages with the existing override controls intact. Stage 0, 1, 2, 3 each show a `Show details` toggle BELOW the `Current:` line. Stage 4 shows no toggle.

    2. **Open Stage 0 expander**: expect REASONING + EVIDENCE sections. If Phase 82.2 telemetry is populated for this row, regex patterns + LLM injection verdict + budget headroom render. If not, empty-state copy `No evidence captured for this stage.`.

    3. **Open Stage 1 expander**: expect rule chip, predictor-source chip (`regex` or `llm`), confidence chip. Reasoning body present for LLM Pass-2 rows; degraded copy `Regex Pass-1 matched — no LLM reasoning produced.` for Pass-1 short-circuit rows.

    4. **Open Stage 2 expander**: expect identifier-source chip, confidence chip, top-3 candidates list. For a row that hit iController, expect `Before` + `After` screenshot thumbnails — click each thumbnail to inline-expand full-width. For an unresolved row, expect locked copy `Customer could not be resolved. No candidates returned.`

    5. **Open Stage 3 expander**: expect ranked-intent list (top-down), selected intent visibly highlighted (bold + ▶ indicator), coordinator one-line reasoning.

    6. **Open multiple expanders** at once (e.g., Stage 1 + Stage 3). Expect both stay open — no accordion auto-close.

    7. **Click "Show raw JSON"** inside any expander. Expect Geist Mono 12px pretty-printed JSON, max-height 240px with internal scroll.

    8. **Existing override flow** still works: click `override stage` on Stage 1, override the category, hit Approve. Expect no regression vs. Phase 82.1.

    9. **Sender / subject sanity**: find a row where the underlying email had `sender_name = "Planning"` in the database (use the 2026-05-11 smoke set, or any system-mailbox forward). Expect the sidebar header to render `From (unknown sender)` — NOT `From Planning`. Find a row where subject is null/empty → expect `(no subject)`.

    10. **sales-email swarm fallback**: navigate to `/automations/sales-email/stage-1`. Open a row. Expect Stage 0 + Stage 1 expanders to populate normally, Stage 2 + Stage 3 to show `Not yet implemented for this swarm.` (or to silently not render — both are acceptable per CONTEXT acceptance — confirm which matches what 82.3 actually shipped).

    11. **Network sanity**: open browser devtools Network tab. Open + close Stage 2 expander 3 times in rapid succession. Expect at most 2 calls to `/api/automations/audit/signed-url` per screenshot (200ms client-side cache absorbs the third).

    12. **Security sanity**: in devtools Network, copy any signed URL response. Confirm it is a Supabase Storage signed URL (`/storage/v1/object/sign/...`) NOT a direct anon-key URL. Confirm no `SUPABASE_SERVICE_ROLE_KEY` substring leaks anywhere in JS bundles (`grep` Network → JS → bundle search).

    13. **Cross-page entry-point coverage** (per checker warning 1): repeat steps 1–5 starting from `/automations/debtor-email/stage-0` and `/automations/debtor-email/stage-2`. Expect the SAME expanders in the detail-pane sidebar — wiring must be present for all five stage pages, not just stages 1/3/4.

    If anything fails, capture a screenshot, describe what you see, and reject — the planner will revise.
  </how-to-verify>
  <resume-signal>Reply `approved` or describe specific failures.</resume-signal>
</task>

</tasks>

<verification>
- Full vitest suite green: `cd web && npx vitest run`
- tsc clean
- Operator UAT signed off
</verification>

<success_criteria>
Every locked must-have in CONTEXT.md `<must_haves>` is operator-confirmed. The audit surface is live in Bulk Review for debtor-email Stage 0/1/2/3/4 pages. Stage 4 row stays untouched. Phase 82.4 (prose-notes capture) can now mount inside this surface.
</success_criteria>

<output>
After completion, create `.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-11-SUMMARY.md` including the UAT outcome and any operator-reported follow-ups for /gsd-verify-phase.
</output>
