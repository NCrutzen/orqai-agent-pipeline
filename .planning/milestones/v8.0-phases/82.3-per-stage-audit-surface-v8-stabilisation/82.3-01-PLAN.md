---
phase: 82.3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/components/ui/collapsible.tsx
  - web/package.json
autonomous: true
requirements: [82.3-COLLAPSIBLE]
must_haves:
  truths:
    - "Radix Collapsible primitive is importable from @/components/ui/collapsible"
  artifacts:
    - path: "web/components/ui/collapsible.tsx"
      provides: "shadcn Collapsible (Root/Trigger/Content) re-export of Radix"
      contains: "@radix-ui/react-collapsible"
  key_links:
    - from: "downstream stage-N evidence renderers"
      to: "web/components/ui/collapsible.tsx"
      via: "named import { Collapsible, CollapsibleTrigger, CollapsibleContent }"
      pattern: "from \"@/components/ui/collapsible\""
---

<objective>
Install the shadcn Collapsible primitive that UI-SPEC §Component Inventory names as the foundation for every stage audit expander. Without this, no other plan in Phase 82.3 can build the inline `Show details` / `Hide details` toggle.

Purpose: Honor UI-SPEC's locked decision ("Component library — Radix (via shadcn) — Collapsible, Badge, Button") instead of substituting native `<details>/<summary>`. Keeps the new audit surface visually + a11y-consistent with the rest of the V7 design system.
Output: `web/components/ui/collapsible.tsx` exporting `Collapsible`, `CollapsibleTrigger`, `CollapsibleContent`; `@radix-ui/react-collapsible` listed in `web/package.json` dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-UI-SPEC.md
@CLAUDE.md
@web/components/ui/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Collapsible primitive</name>
  <files>web/components/ui/collapsible.tsx, web/package.json</files>
  <read_first>
    - web/components/ui/badge.tsx (analog — confirms shadcn re-export shape used in this repo)
    - web/package.json (confirm where @radix-ui/* deps already sit)
    - .planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-UI-SPEC.md §Component Inventory
  </read_first>
  <action>
    Run `cd web && npx --yes shadcn@latest add collapsible --yes --overwrite=false` from the repo root. This adds `@radix-ui/react-collapsible` to `web/package.json` and emits `web/components/ui/collapsible.tsx` containing the standard shadcn re-export (`Collapsible`, `CollapsibleTrigger`, `CollapsibleContent`).

    If the CLI prompts because shadcn isn't pre-initialised (it IS — `shadcn_initialized: true` in UI-SPEC frontmatter), do NOT re-init; abort and report. Do NOT use `--legacy-peer-deps` unless `npm install` fails.

    After install, run `cd web && npm install` to materialise the lockfile entry.

    Do NOT modify any existing component. Do NOT edit components.json. Do NOT add a barrel export. Per UI-SPEC §Registry Safety this is an EXISTING-shadcn-block install, no third-party registry, no safety vetting.
  </action>
  <verify>
    <automated>test -f web/components/ui/collapsible.tsx &amp;&amp; grep -q "@radix-ui/react-collapsible" web/components/ui/collapsible.tsx &amp;&amp; grep -q "@radix-ui/react-collapsible" web/package.json &amp;&amp; cd web &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -v "^$" | head -5; echo "tsc_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `web/components/ui/collapsible.tsx` exists
    - File contains the literal string `@radix-ui/react-collapsible`
    - File exports the three names `Collapsible`, `CollapsibleTrigger`, `CollapsibleContent` (grep `^export` matches all three)
    - `web/package.json` "dependencies" contains key `@radix-ui/react-collapsible`
    - `cd web &amp;&amp; npx tsc --noEmit` exit code is 0 (no new type errors introduced)
  </acceptance_criteria>
  <done>Collapsible primitive installed and type-checks clean.</done>
</task>

</tasks>

<verification>
- `grep -l "CollapsibleTrigger" web/components/ui/collapsible.tsx` returns the path
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
Downstream plans 03–06 + 07 + 09 can `import { Collapsible, CollapsibleTrigger, CollapsibleContent } from "@/components/ui/collapsible";` without further setup.
</success_criteria>

<output>
After completion, create `.planning/phases/82.3-per-stage-audit-surface-v8-stabilisation/82.3-01-SUMMARY.md`.
</output>
