# Phase 82.5 — Pick-up Note

**State as of 2026-05-13:** Code-complete. All 7 plans landed on `main` (commits `2262430` → `4284582`). Verifier verdict: **8/8 SPEC requirements PASS** (see `82.5-VERIFICATION.md`).

## What's left before closeout

Two human checkpoints — neither blocks code, both block "Phase 82.5 ✅":

### 1. Operator UAT (Plan 07 Task 3)
Script lives in `82.5-07-SUMMARY.md` under "Deferred". Walk the 8 SPEC requirements (R1–R8) plus W4 keyboard shortcut on a real preview. Approve in the SUMMARY or open a gap-closure plan if any requirement misses.

### 2. Playwright 1280×800 snapshot run (R8)
Spec authored at `web/playwright-snapshots/82.5/feedback-panel-1280.spec.ts`, gated on `PHASE_82_5_SMOKE_URL`. Run:
```bash
cd web && PHASE_82_5_SMOKE_URL=<preview-url> npm run test:e2e
```
Snapshot must show no horizontal scrollbar and both Save + Confirm buttons visible at 1280×800. If it fails, the R7 morph button or container width needs a tweak — not a phase blocker, but ship-blocker.

## Pre-existing issues NOT from this phase (don't conflate)

- **STATE.md plan counter is malformed** — header shows `Phase --phase — 82.5` (placeholder from a `gsd-sdk` arg-passing glitch earlier). Every executor agent reported `state.advance-plan` parse errors. **Fix:** manually edit STATE.md frontmatter `current_plan` + `plan_count` + body counters, or re-run `gsd-sdk query state.begin-phase --phase 82.5 ...` with corrected positional args (the `--phase`/`--name` flag form silently mis-binds).
- **ROADMAP.md has no row for 82.5** — `roadmap.update-plan-progress` reports no matching checkbox. Add the row if you want progress tracked there.
- **Pre-existing TS error:** `web/app/(dashboard)/automations/[swarm]/stage-1/__tests__/actions.predictor.test.ts:60` — TS2556 from Phase 999.8 RED scaffold (commit `e576374`). **Untouched** — out of scope for 82.5.
- **`requirements.mark-complete R1 R6 R7 R8`** ran but changed nothing — REQUIREMENTS.md row pattern doesn't match. SUMMARY frontmatter `requirements_addressed` is the source of truth.

## Untracked at session end
- `.planning/phases/82.5-feedback-ux-deepdive-v8-stabilisation/82.5-VERIFICATION.md` ← commit this when you're ready
- Unrelated pre-existing untracked files (debug notes, backfill JSON, exec briefings, ui-reviews) — leave alone

## Once UAT + Playwright pass
1. Commit VERIFICATION.md.
2. Run `/gsd-ship` to open a PR or fold into the active branch strategy.
3. Update STATE.md / ROADMAP.md formatting issues above so the next phase doesn't inherit them.
