---
phase: 82.5-feedback-ux-deepdive-v8-stabilisation
artifact: UAT report (Task 3 closeout)
target: agent-workforce-q93iu7i0c-moyne-roberts.vercel.app (prod, commit 81047fd)
operator: n.crutzen@curagroep.com (test account)
viewport: 1280x800
runner: web/scripts/uat-82.5-browserless.ts (Browserless cloud chromium)
date: 2026-05-15
---

# Phase 82.5 — Operator UAT Report

Production deploy `agent-workforce-q93iu7i0c-moyne-roberts.vercel.app` (commit `81047fd`, includes lockfile fix on top of phase code at `d7061db`). UAT driven via Browserless cloud chromium, viewport hard-pinned at 1280x800.

Screenshots: `/tmp/uat-82.5-screenshots/*.png` (artifact-only, not committed).

## Summary

| Outcome | Count |
|---|---|
| **PASS** | 9 |
| WARN | 1 |
| SKIP | 1 |
| FAIL | 0 |

No regressions surfaced. Both WARN/SKIP are "no prior feedback on this row" — expected when logging in as a fresh test account; not implementation defects.

## Per-requirement results

| # | Req | Status | Evidence |
|---|---|---|---|
| 0 | AUTH | PASS | Login as `n.crutzen@curagroep.com` redirected to `/automations/debtor-email/stage-1` |
| 1 | **R3** (row dots) | PASS | 25 `[data-testid="row-verdict-dot"]` elements rendered; all `data-verdict="none"` (dashed empty) since the test account has no feedback history yet |
| 2 | **R1** (read-back) | WARN | Textarea empty on mount — no prior `email_feedback` rows for the selected row under this operator. Static audit confirms the parent-seeding hook is wired (`stage-step.tsx:77-83`); needs re-test on a row that has historical feedback |
| 3 | R1 (others toggle) | SKIP | No cross-operator notes on the selected row — `OthersSaidBlock` returns `null` (intended behavior) |
| 6a | **R4** (under-textarea microcopy) | PASS | `[data-testid="override-coupling-helper"]` visible; computed color = `rgb(255, 181, 71)` = `--v7-amber` |
| 6b | **R4** (picker microcopy) | PASS | `[data-testid="override-coupling-helper-picker"]` visible in dirty state |
| 8 | **R7** (button morph) | PASS | Primary button label: `"✓ Submit override (Stage 1)"` — override-mode branch fires correctly (initial `dirty[activeStage]=true` state) |
| 10 | **R8** (1280x800 layout) | PASS | `document.scrollWidth - clientWidth = 0`, `detail-pane.scrollWidth - clientWidth = 0` — no horizontal overflow |
| 10b | **R5/R8** (Save+Confirm visible) | PASS | Both `[data-testid="stage-feedback-save"]` and `[data-testid="stage-feedback-confirm"]` visible at 1280x800 |
| 11d | **W4** (typing `n`) | PASS | Character `n` typed into focused textarea; document-level Skip handler did NOT fire (`e.stopPropagation()` confirmed via end-state value) |
| 11e | **W4** (Enter inserts newline) | PASS | `\n` present in textarea after Enter press; primary button did NOT trigger |

## Not exercised in this run

These are **code-side verified** by the static audit (see `82.5-VERIFICATION.md`) but not exercised by Browserless:

- **R1 positive read-back** — requires a row with prior `email_feedback.prose_notes` under the active operator
- **R2** — covered by vitest (`app/api/automations/debtor-email/feedback/__tests__/route.test.ts`, 6/6 pass)
- **R5 destructive POST** (Save / Confirm flow) — UAT_FULL branch not implemented; would write real rows to prod `email_feedback`
- **R5 empty-Confirm soft-dialog** — `window.confirm()` not auto-driven
- **R6 future-pill collapse** — requires a row with ≥2 consecutive trailing skipped stages and no prior interaction
- **R7 Approve mode label** — `"✓ Approve verdicts that ran (Stages 0+1)"` — requires all dirty axes cleared
- **W4 `Cmd+S` saves** — requires destructive POST tolerance
- **W4 Esc** — requires the audit expander to be present (timing-sensitive)
- **W6 override single-write** — requires a SQL `SELECT count(*)` against prod `email_feedback` after an override click

## Recommendation

**Phase 82.5 closeout green-lit.** All exercised paths pass. The remaining items are either (a) already vitest-covered (R2), (b) covered by static audit (R5/R6/R7-approve/W4-Cmd-S/W6), or (c) need an operator with historical feedback on a real customer row — best validated as part of the debtor-person onboarding cycle starting 2026-05-18 rather than synthetic UAT.

Proceed to `/gsd-ship` whenever you're ready.
