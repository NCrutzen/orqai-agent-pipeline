---
phase: 82.5-feedback-ux-deepdive-v8-stabilisation
plan: 07
subsystem: feedback-ux
tags: [feedback, ui, playwright, stage-0, stage-2, parity]
requires:
  - 82.5-02  # Playwright installed + config
  - 82.5-05  # UnifiedDetailPane feedbackMap consumer (R1/R6/R7)
  - 82.5-06  # feedbackMap threaded through pages + Option Z shell
provides:
  - "OptionZDetailPane forwards feedbackMap → Stage 0/2 panes have R1/R6/R7 parity with Stage 1/3"
  - "Playwright 1280×800 snapshot spec for R8 regression coverage"
affects:
  - "web/app/(dashboard)/automations/[swarm]/_shell/option-z-detail-pane.tsx"
  - "web/playwright-snapshots/82.5/feedback-panel-1280.spec.ts"
tech_stack:
  added: []
  patterns:
    - "Per-stage paneFeedbackMap reduction: { [activeStage]: feedbackMap[selectedId] }"
    - "Playwright test.skip guard on PHASE_82_5_SMOKE_URL for CI safety"
key_files:
  created:
    - "web/playwright-snapshots/82.5/feedback-panel-1280.spec.ts"
  modified:
    - "web/app/(dashboard)/automations/[swarm]/_shell/option-z-detail-pane.tsx"
decisions:
  - "Stage 0 + Stage 2 panes reuse UnifiedDetailPane via OptionZDetailPane wrapper — no duplicated R1/R6/R7 logic; pass-through pattern keeps hard-separation lock (categories=[], intents=[])"
  - "Playwright spec stays test.skip when PHASE_82_5_SMOKE_URL is unset — preserves green CI without requiring a live preview URL"
  - "No .md fallback for R8 acceptance — Playwright was installed in Plan 02; SPEC R8 requires a runnable screenshot"
metrics:
  duration_seconds: 84
  tasks_completed: 2
  tasks_deferred: 1
  completed_date: "2026-05-13"
---

# Phase 82.5 Plan 07: Feedback UX Closeout — Stage 0/2 parity + R8 Playwright snapshot Summary

One-liner: Wired feedbackMap through OptionZDetailPane (Stage 0/2 panes now get R1/R6/R7 parity via shared UnifiedDetailPane) and authored the SPEC R8 Playwright 1280×800 snapshot spec — no .md fallback, no scope downgrade.

## What Was Built

### Task 1 — OptionZDetailPane feedbackMap pass-through (commit 839e97c)

- Added `FeedbackMap` + `FeedbackReadBack` imports from `web/lib/automations/debtor-email/feedback/types`.
- Removed the placeholder `void feedbackMap` (Plan 06 forward-compat marker) and replaced the type-only import inside `Props` with a direct typed prop.
- Added a `paneFeedbackMap` useMemo deriving `Partial<Record<0|1|2|3, FeedbackReadBack>> | undefined` from `feedbackMap[selectedId]` keyed on `activeStage`. Guard: returns `undefined` when there is no selection, no map, or `activeStage > 3` (Stage 4 has no feedback read-back surface).
- Wired `feedbackMap={paneFeedbackMap}` into the existing `<UnifiedDetailPane>` JSX.
- Hard-separation preserved: `categories={[]}` and `intents={[]}` still passed (Stage 0/2 sit outside both registries; `email_feedback` is orthogonal — keyed on `(email_id, stage)`).

Result: SPEC R6 "Same behavior on all 4 stage tabs" satisfied. Stage 0 + Stage 2 panes inherit R1 read-back, R6 future-pill, R7 bottom-button morph behavior from the shared UnifiedDetailPane consumer (Plan 05) without code duplication.

### Task 2 — Playwright 1280×800 feedback-panel snapshot spec (commit 8b3d1f1)

- Created `web/playwright-snapshots/82.5/feedback-panel-1280.spec.ts`.
- Hard-pinned viewport `{ width: 1280, height: 800 }` via `test.use`.
- Asserts visibility of `[data-testid="stage-feedback-save"]` and `[data-testid="stage-feedback-confirm"]`.
- Asserts no horizontal overflow on detail-pane root (`scrollWidth - clientWidth <= 0`) — uses `[data-testid="detail-pane-root"]` if present, falls back to the grid right column (`minmax(0, 1fr) 460px`).
- `test.skip` guard on `PHASE_82_5_SMOKE_URL` for CI safety; spec only runs when an authenticated preview URL is supplied.
- Stores a PNG at `playwright-snapshots/82.5/feedback-panel-1280.png` (artifact-only; not committed).
- `npx playwright test --list` recognizes the spec — config + spec wiring verified.

### Task 3 — Operator UAT — **DEFERRED**

Operator UAT is a human-verify checkpoint and was **not** executed during autonomous run. Per the documented Copilot-style "skip checkpoint, surface in summary" pattern (--auto flow):

- **Status:** DEFERRED — operator UAT not run during autonomous execution.
- **Coverage:** UAT walks through all 8 requirements (R1 read-back, R2 GET endpoint, R3 row dot, R4 amber microcopy, R5 Save/Confirm dual-button, R6 future-pill on all 4 stage tabs, R7 bottom-button morph, R8 1280×800 viewport) plus W4 keyboard shortcuts on focused textarea and W6 single-write override branch.
- **Action required from user:** Manually walk through the UAT script in `82.5-07-PLAN.md` Task 3 `<how-to-verify>` at the operator UI (1280×800). Run the Playwright snapshot first as the automated R8 gate:
  ```bash
  cd web && PHASE_82_5_SMOKE_URL="<preview-url>/automations/debtor-email/stage-1?selected=<uuid>" \
    npm run test:e2e -- playwright-snapshots/82.5/feedback-panel-1280.spec.ts
  ```
- **gsd-verifier** will assess closeout readiness.

## Verification

### Task 1 (automated greps + tsc)

- `grep "feedbackMap"` → present
- `grep "paneFeedbackMap"` → present
- `grep "activeStage > 3"` → present
- `grep "feedbackMap={paneFeedbackMap"` → present
- `grep "categories={\[\]}"` → present (hard-separation preserved)
- `grep "intents={\[\]}"` → present (hard-separation preserved)
- `npx tsc --noEmit` → no errors on option-z-detail-pane

### Task 2 (Playwright list)

- File exists at `web/playwright-snapshots/82.5/feedback-panel-1280.spec.ts`
- `viewport: { width: 1280, height: 800 }` present
- `stage-feedback-save` + `stage-feedback-confirm` selectors present
- `PHASE_82_5_SMOKE_URL` env guard present
- `npx playwright test --list` → recognized:
  `[chromium] › 82.5/feedback-panel-1280.spec.ts:19:7 › 82.5 feedback panel — 1280×800 viewport (R8) › save + confirm visible, no horizontal overflow`

## Deviations from Plan

None — plan executed exactly as written. Task 3 (operator UAT) deferred per --auto contract (documented Copilot-style skip-checkpoint pattern); not a deviation.

## Commits

- `839e97c` — feat(82.5-07): forward feedbackMap through OptionZDetailPane (Stage 0/2 R1/R6/R7 parity)
- `8b3d1f1` — test(82.5-07): add Playwright 1280×800 feedback-panel snapshot spec (R8)

## Threat Surface Review

No new surface introduced.

- T-82.5.07-01 (info disclosure on paneFeedbackMap): mitigated — upstream Plan 01 strips raw `operator_id` from the FeedbackMap shape; this wrapper is a pure pass-through and never mutates.
- T-82.5.07-02 (hard-separation spoofing): mitigated — `categories={[]}` and `intents={[]}` still explicitly passed; verified by grep gates above.

## Phase 82.5 Rollup (Plans 01–07)

Plan 07 closes the phase. Cross-plan dependency satisfaction:

- **R1 read-back** — Plan 05 (UnifiedDetailPane consumer) + Plan 06 (page-level feedbackMap threading) + Plan 07 (Stage 0/2 parity via OptionZDetailPane pass-through). ✅
- **R2 GET endpoint** — Plan 01. ✅
- **R3 row dot** — earlier plan. ✅
- **R4 amber microcopy** — Plan 03. ✅
- **R5 Save+Confirm dual-button** — Plan 03 (StageFeedbackPanel). ✅
- **R6 future-pill on all 4 stage tabs** — Plan 05 (UnifiedDetailPane) + Plan 07 (Stage 0/2 parity via paneFeedbackMap). ✅
- **R7 bottom-button morph** — Plan 05 + Plan 07 (parity). ✅
- **R8 Playwright 1280×800 snapshot** — Plan 02 (Playwright install) + Plan 07 (spec). ✅ (runnable; awaits PHASE_82_5_SMOKE_URL for execution)

UAT (Task 3) is the final sign-off gate; once operator walks through the 12-step UAT script and Playwright snapshot passes against a preview URL, Phase 82.5 is complete.

## Self-Check: PASSED

- ✅ `web/app/(dashboard)/automations/[swarm]/_shell/option-z-detail-pane.tsx` modified (commit 839e97c)
- ✅ `web/playwright-snapshots/82.5/feedback-panel-1280.spec.ts` created (commit 8b3d1f1)
- ✅ Both commits in git log
- ✅ tsc clean; Playwright recognizes spec
