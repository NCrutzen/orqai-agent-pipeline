---
phase: 68-swarm-registry-generalisation-canonical-context-shape
plan: 07
type: execute
wave: 4
depends_on: [02, 03, 04, 05, 06]
files_modified:
  - web/lib/swarms/__tests__/sales-email-stub.test.ts
autonomous: true
requirements: [SWRM-03]
must_haves:
  truths:
    - "A brand-new swarm row + 3 intent rows can be inserted via SQL only"
    - "loadSwarm, loadSwarmIntents, loadHandlerEvent, evaluateSideEffects, loadCanonicalContextShape all return the stub data"
    - "No source file is modified to add the second swarm — proves SWRM-03 zero-code-edit onboarding"
    - "Test cleanup uses ON DELETE CASCADE to drop swarm_intents children"
  artifacts:
    - path: "web/lib/swarms/__tests__/sales-email-stub.test.ts"
      provides: "SWRM-03 integration proof"
      contains: "describe.*SWRM-03"
  key_links:
    - from: "stub test seed"
      to: "swarms + swarm_intents tables"
      via: "service-role admin client"
      pattern: "createAdminClient"
---

<objective>
Author the integration test that proves SWRM-03: onboarding a new swarm requires only SQL inserts; every Phase 68 helper transparently picks up the new swarm_type.

Purpose: SWRM-03 acceptance proof.
Output: One vitest integration test against the live Supabase schema (test data seeded + cleaned).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/68-swarm-registry-generalisation-canonical-context-shape/68-RESEARCH.md
@web/lib/swarms/registry.ts
@web/lib/swarms/side-effects.ts
@web/lib/swarms/dynamic.ts

<interfaces>
Plan 02 exports:
- `loadSwarm`, `loadSwarmIntents`, `loadHandlerEvent`, `loadCanonicalContextShape`, `__resetCacheForTests` from `web/lib/swarms/registry`
- `evaluateSideEffects` from `web/lib/swarms/side-effects`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author sales-email-stub integration test</name>
  <read_first>
    - 68-RESEARCH.md lines 437-525 (full test shape — copy verbatim, adapt path/import names if needed)
    - 68-VALIDATION.md line 47 (sales-email-stub task spec)
    - web/lib/swarms/__tests__/registry.test.ts (existing test patterns for vitest config)
    - CLAUDE.md (service-role key usage for automation writes)
  </read_first>
  <files>web/lib/swarms/__tests__/sales-email-stub.test.ts</files>
  <action>
Create file using the structure from 68-RESEARCH.md lines 444-521. Adjust:

1. Use `createAdminClient` (or whatever the project's service-role helper is — check `web/lib/supabase/admin.ts` or similar). Check `web/lib/swarms/__tests__/registry.test.ts` for existing pattern; if registry.test.ts uses a mocked client, this stub test instead needs the REAL admin client because it exercises the live schema.
2. `STUB_SWARM = "sales-email-stub"`.
3. `beforeAll`: insert swarm row with all 5 new columns populated (use `@/lib/debtor-email/classify` and `@/lib/automations/debtor-email/resolve-debtor` as the module paths so dynamic-import would succeed if invoked; this test does NOT need to invoke the dynamic loaders — only proves registry reads work). Insert 3 intent rows: `lead_qualify`, `demo_request`, `pricing_query`.
4. `afterAll`: `await admin.from("swarms").delete().eq("swarm_type", STUB_SWARM)` — CASCADE drops swarm_intents.
5. Tests:
   - `loadSwarm` returns row with all 5 new columns populated.
   - `loadSwarmIntents` returns 3 rows; `intent_key` set is `["demo_request", "lead_qualify", "pricing_query"]` after sort.
   - `loadHandlerEvent(admin, STUB_SWARM, "demo_request")` === `"sales-email/demo_request.requested"`.
   - `loadHandlerEvent(admin, STUB_SWARM, "missing_intent")` === `null`.
   - `evaluateSideEffects(admin, STUB_SWARM, "stage2_match_live", { foo: true })` returns 1 descriptor; `{ foo: false }` returns 0.
   - `loadCanonicalContextShape(admin, STUB_SWARM)` returns object with `version === "stub.v1"`.
6. Call `__resetCacheForTests()` after seeding (so the 60s TTL cache doesn't return stale).

If this test requires environment variables that aren't available in CI, gate it with `describe.skipIf(!process.env.SUPABASE_SERVICE_ROLE_KEY)` so it runs locally and in any environment that has service-role access.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx vitest run lib/swarms/__tests__/sales-email-stub.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `web/lib/swarms/__tests__/sales-email-stub.test.ts`
    - `grep "sales-email-stub" web/lib/swarms/__tests__/sales-email-stub.test.ts` matches
    - `grep "loadHandlerEvent.*demo_request" web/lib/swarms/__tests__/sales-email-stub.test.ts` matches
    - `grep "evaluateSideEffects" web/lib/swarms/__tests__/sales-email-stub.test.ts` matches
    - `grep "loadCanonicalContextShape" web/lib/swarms/__tests__/sales-email-stub.test.ts` matches
    - `grep "afterAll" web/lib/swarms/__tests__/sales-email-stub.test.ts` matches (cleanup)
    - `cd web && npx vitest run lib/swarms/__tests__/sales-email-stub.test.ts` exits 0 (when SUPABASE_SERVICE_ROLE_KEY is available; skipIf is acceptable otherwise)
  </acceptance_criteria>
  <done>SWRM-03 proven: zero-code-edit onboarding test green.</done>
</task>

</tasks>

<verification>
Test runs end-to-end against live Supabase; cleanup is idempotent so re-runs succeed.
</verification>

<success_criteria>
SWRM-03 acceptance bullet: a synthetic new swarm row exercises the entire registry helper API without a single source-file edit.
</success_criteria>

<output>
Create `.planning/phases/68-swarm-registry-generalisation-canonical-context-shape/68-07-SUMMARY.md`.
</output>
