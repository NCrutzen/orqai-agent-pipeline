---
phase: 68-swarm-registry-generalisation-canonical-context-shape
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - web/lib/swarms/types.ts
  - web/lib/swarms/registry.ts
  - web/lib/swarms/dynamic.ts
  - web/lib/swarms/side-effects.ts
  - web/lib/swarms/__tests__/registry.test.ts
  - web/lib/swarms/__tests__/dynamic.test.ts
  - web/lib/swarms/__tests__/side-effects.test.ts
  - web/lib/automations/debtor-email/resolve-debtor.ts
autonomous: true
requirements: [SWRM-01, SWRM-02, SWRM-04]
must_haves:
  truths:
    - "loadHandlerEvent(admin, 'debtor-email', 'copy_document_request') returns 'debtor-email/copy_document_request.requested'"
    - "evaluateSideEffects filters by trigger AND gate equality, returns SideEffectDescriptor[]"
    - "loadStage1Classifier and loadStage2Resolver dynamically import the module path stored in the registry, cached"
    - "resolveDebtor exported also as resolveEntity (Stage 2 contract alias)"
  artifacts:
    - path: "web/lib/swarms/registry.ts"
      provides: "loadSwarmIntents, loadHandlerEvent, loadCanonicalContextShape"
      exports: ["loadSwarmIntents", "loadHandlerEvent", "loadCanonicalContextShape"]
    - path: "web/lib/swarms/side-effects.ts"
      provides: "evaluateSideEffects + SideEffectDescriptor union with kind discriminator"
      exports: ["evaluateSideEffects", "SideEffectDescriptor", "SideEffectTrigger"]
    - path: "web/lib/swarms/dynamic.ts"
      provides: "loadStage1Classifier, loadStage2Resolver"
      exports: ["loadStage1Classifier", "loadStage2Resolver"]
  key_links:
    - from: "registry.ts loadHandlerEvent"
      to: "swarm_intents table via Supabase"
      via: "loadSwarmIntents"
      pattern: "from\\(\"swarm_intents\"\\)"
    - from: "side-effects.ts evaluateSideEffects"
      to: "swarms.side_effects jsonb"
      via: "loadSwarm"
      pattern: "swarm\\?\\.side_effects"
---

<objective>
Build all registry helper modules (with unit tests) so call-site swaps in Wave 3 are pure plug-and-play.

Purpose: Centralise registry-driven dispatch contracts in `web/lib/swarms/`.
Output: 3 new/extended source modules + 3 test files; alias-export `resolveEntity` from `resolve-debtor.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/68-swarm-registry-generalisation-canonical-context-shape/68-RESEARCH.md
@web/lib/swarms/registry.ts
@web/lib/swarms/types.ts
@web/lib/swarms/__tests__/registry.test.ts
@web/lib/automations/debtor-email/resolve-debtor.ts

<interfaces>
Existing registry.ts exports (web/lib/swarms/registry.ts):
- `loadSwarm(admin, swarmType): Promise<SwarmRow | null>` — 60s TTL Map cache, last-known-good on error
- `loadSwarmCategories(admin, swarmType): Promise<SwarmCategoryRow[]>`
- `__resetCacheForTests(): void`

Existing types.ts has `SwarmRow.side_effects` already declared.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend types.ts + registry.ts with intent + shape + side-effects loaders</name>
  <read_first>
    - 68-RESEARCH.md lines 320-340 (registry.ts extension shape)
    - 68-RESEARCH.md lines 191-202 (helper inventory)
    - web/lib/swarms/registry.ts (current 69 lines — match TTL pattern)
    - web/lib/swarms/types.ts (extend SwarmRow with new columns)
    - web/lib/swarms/__tests__/registry.test.ts (existing test patterns)
  </read_first>
  <behavior>
    - `loadSwarmIntents(admin, 'debtor-email')` returns 8 rows; second call hits cache (no second .from() call)
    - `loadHandlerEvent(admin, 'debtor-email', 'copy_document_request')` returns `'debtor-email/copy_document_request.requested'`
    - `loadHandlerEvent(admin, 'debtor-email', 'unknown_intent')` returns `null`
    - `loadCanonicalContextShape(admin, 'debtor-email')` returns object with `version` field
    - `__resetCacheForTests()` also clears the new `INTENTS_CACHE`
  </behavior>
  <files>web/lib/swarms/types.ts, web/lib/swarms/registry.ts, web/lib/swarms/__tests__/registry.test.ts</files>
  <action>
1. **types.ts**: Extend `SwarmRow` with optional fields:
   ```ts
   stage1_regex_module: string | null;
   stage2_entity_resolver: string | null;
   stage3_coordinator_agent_key: string | null;
   canonical_context_shape: CanonicalContextShape | null;
   entity_brand: string[] | null;
   ```
   Add `SwarmIntentRow` type: `{ swarm_type: string; intent_key: string; handler_agent_key: string | null; handler_event: string; requires_orchestration: boolean; created_at: string; updated_at: string }`.
   Add `CanonicalContextShape` interface: `{ version: string; fields: Record<string, { type: string; nullable?: boolean; enum?: string[]; default?: unknown; description?: string; items?: unknown }> }`.

2. **registry.ts**: Add at module top:
   ```ts
   const INTENTS_CACHE = new Map<string, { value: SwarmIntentRow[]; expires: number }>();
   ```
   Export `loadSwarmIntents(admin, swarmType)` — same TTL/last-known-good pattern as `loadSwarmCategories`, queries `from("swarm_intents").select("*").eq("swarm_type", swarmType)`.
   Export `loadHandlerEvent(admin, swarmType, intentKey)` — calls `loadSwarmIntents`, returns `intents.find(i => i.intent_key === intentKey)?.handler_event ?? null`.
   Export `loadCanonicalContextShape(admin, swarmType)` — calls `loadSwarm`, returns `swarm?.canonical_context_shape ?? null`.
   Update `__resetCacheForTests` to clear `INTENTS_CACHE`.

3. **registry.test.ts**: Extend with new tests using existing mock pattern. Cover the 5 behavior cases above. Use mocked Supabase client (no live DB in unit tests).
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx vitest run lib/swarms/__tests__/registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export async function loadSwarmIntents" web/lib/swarms/registry.ts` matches
    - `grep "export async function loadHandlerEvent" web/lib/swarms/registry.ts` matches
    - `grep "export async function loadCanonicalContextShape" web/lib/swarms/registry.ts` matches
    - `grep "INTENTS_CACHE.clear" web/lib/swarms/registry.ts` matches (test reset)
    - `grep "SwarmIntentRow" web/lib/swarms/types.ts` matches
    - `grep "CanonicalContextShape" web/lib/swarms/types.ts` matches
    - `cd web && npx vitest run lib/swarms/__tests__/registry.test.ts` exits 0
  </acceptance_criteria>
  <done>Registry helpers exported, typed, unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build side-effects.ts + dynamic.ts modules + tests, alias-export resolveEntity</name>
  <read_first>
    - 68-RESEARCH.md lines 343-384 (side-effects.ts shape)
    - 68-RESEARCH.md lines 386-429 (dynamic.ts shape)
    - 68-RESEARCH.md lines 431-435 (resolveEntity alias)
    - 68-CONTEXT.md D-06, D-07, D-15 (locked side-effects + dynamic-import contracts)
    - web/lib/automations/debtor-email/resolve-debtor.ts (current export: `resolveDebtor`)
  </read_first>
  <behavior>
    side-effects.ts:
    - `evaluateSideEffects(admin, 'debtor-email', 'stage2_match_live', {dry_run: false, customer_account_id_present: true, icontroller_company_present: true})` returns 1 descriptor (the icontroller-tag inngest_event)
    - Same call with `dry_run: true` returns 0 (gate fails)
    - `evaluateSideEffects(admin, 'debtor-email', 'stage1_categorize_archive', {category_action: 'categorize_archive'})` returns 1 descriptor with `kind === 'automation_run_insert'`
    - SideEffectDescriptor type union has both kinds

    dynamic.ts:
    - `loadStage1Classifier(admin, 'debtor-email')` resolves to a function (the real `classify` export from `@/lib/debtor-email/classify`)
    - Second call hits MODULE_CACHE — no second `import()` call
    - `loadStage2Resolver(admin, 'debtor-email')` resolves to a function (the alias `resolveEntity` from `@/lib/automations/debtor-email/resolve-debtor`)
    - Throws structured error if module path missing or doesn't export expected symbol

    resolve-debtor.ts: `resolveEntity` export equals `resolveDebtor` (alias).
  </behavior>
  <files>web/lib/swarms/side-effects.ts, web/lib/swarms/dynamic.ts, web/lib/swarms/__tests__/side-effects.test.ts, web/lib/swarms/__tests__/dynamic.test.ts, web/lib/automations/debtor-email/resolve-debtor.ts</files>
  <action>
1. **side-effects.ts** — implement EXACTLY as RESEARCH lines 343-384:
   - Type union `SideEffectDescriptor = InngestEventDescriptor | AutomationRunInsertDescriptor` discriminated by `kind`.
   - `SideEffectTrigger = 'stage1_categorize_archive' | 'stage2_match_live' | 'stage3_handler_complete' | 'stage4_synthesis_complete'`.
   - `evaluateSideEffects(admin, swarmType, trigger, ctx)` calls `loadSwarm`, reads `swarm?.side_effects`, filters by `trigger`, then by `Object.entries(d.gate).every(([k,v]) => ctx[k] === v)`. Returns the matching descriptors.

2. **dynamic.ts** — implement EXACTLY as RESEARCH lines 386-429:
   - `MODULE_CACHE: Map<string, unknown>` at module level.
   - `importByPath<T>(path)` with `/* @vite-ignore */` comment.
   - `loadStage1Classifier(admin, swarmType)` → loads `swarms.stage1_regex_module`, imports, validates `mod.classify` is a function, returns `mod.classify`. Throws if missing.
   - `loadStage2Resolver(admin, swarmType)` → same shape for `mod.resolveEntity`.
   - Export typed `ClassifyFn` + `ResolveEntityFn` aliases.

3. **resolve-debtor.ts** — append at the end:
   ```ts
   export { resolveDebtor as resolveEntity };
   ```
   Use a re-export (not function wrapper) so cache identity is preserved. Keep existing `resolveDebtor` export untouched.

4. **side-effects.test.ts** — mock `loadSwarm` to return a swarm row with `side_effects: [...two descriptors from migration backfill...]`. Test 3 cases: (a) trigger+gate match, (b) gate mismatch returns empty, (c) wrong trigger returns empty.

5. **dynamic.test.ts** — mock `loadSwarm` to return `stage1_regex_module: '@/lib/debtor-email/classify'`. Test cache hit/miss + the missing-export throw path. Use `vi.fn` and `vi.mock` if needed; if dynamic import is awkward to mock in vitest, use `loadStage1Classifier` against the real path (the module exists — it's the actual debtor-email classifier).
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx vitest run lib/swarms/__tests__/side-effects.test.ts lib/swarms/__tests__/dynamic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export async function evaluateSideEffects" web/lib/swarms/side-effects.ts` matches
    - `grep "kind: \"inngest_event\"" web/lib/swarms/side-effects.ts` matches (type union)
    - `grep "kind: \"automation_run_insert\"" web/lib/swarms/side-effects.ts` matches
    - `grep "export async function loadStage1Classifier" web/lib/swarms/dynamic.ts` matches
    - `grep "export async function loadStage2Resolver" web/lib/swarms/dynamic.ts` matches
    - `grep "MODULE_CACHE" web/lib/swarms/dynamic.ts` matches
    - `grep "export { resolveDebtor as resolveEntity" web/lib/automations/debtor-email/resolve-debtor.ts` matches
    - `cd web && npx vitest run lib/swarms/__tests__/side-effects.test.ts lib/swarms/__tests__/dynamic.test.ts` exits 0
  </acceptance_criteria>
  <done>side-effects.ts + dynamic.ts shipped with tests; resolveEntity alias in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → registry helper | callers (Inngest workers) pass swarmType strings; helpers query Supabase service-role |
| registry → dynamic import | module-path strings from DB are passed to `import()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Tampering | swarms.stage1_regex_module / stage2_entity_resolver | mitigate | Service-role-only writes; column populated by migration. dynamic.ts validates the module exports the expected symbol — throws structured error if not. |
| T-68-02 | Elevation | dynamic `import()` | accept | Module path strings come from a service-role-controlled DB column; no user input reaches `import()`. |
| T-68-03 | Information Disclosure | side_effects[] gate evaluation | mitigate | Gate is simple equality match against a runtime ctx object; no PII in ctx. |
</threat_model>

<verification>
All three test files green; type checking passes (`tsc --noEmit` if available — not required to gate the plan but good signal).
</verification>

<success_criteria>
Wave 3 plans can `import { loadHandlerEvent, evaluateSideEffects, loadStage1Classifier, loadStage2Resolver }` directly without further codebase exploration.
</success_criteria>

<output>
Create `.planning/phases/68-swarm-registry-generalisation-canonical-context-shape/68-02-SUMMARY.md`.
</output>
