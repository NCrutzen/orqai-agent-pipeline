---
phase: 82.9-stage-2-audit-panel-evidence-expansion
verified: 2026-05-20T04:38:46Z
status: human_needed
score: 7/7 must-haves verified (code-side); 2 deferred human-action checkpoints
overrides_applied: 0
human_verification:
  - test: "Operator edits the 'candidate_details' Zap to add contact_person + recent_invoices[]; smoke-tests against acceptance NXT; applies migration 20260519_phase82_9_candidate_details_schema_extension.sql to production Supabase project mvqjhlxfvtqqubqgdvhz"
    expected: "Zap response includes contact_person (string|null) and recent_invoices (string[] length <= 5, newest first); registry row's output_schema declares both new fields; live end-to-end callNxtTool returns and parses cleanly"
    why_human: "Zapier UI write access not exposed to Claude for this workflow; production Supabase migration requires operator credentials. Documented in 82.9-01-SUMMARY 'Pending Human Action'."
  - test: "Operator opens Bulk Review for debtor-email in dev/staging; finds one Stage 2 row per method (thread_inheritance / sender_match / identifier_match / llm_tiebreaker / unresolved) plus one legacy row (decision_details ? 'inputs' is false); expands each audit panel"
    expected: "INPUTS section renders per-method content at top; REASONING section visible only for llm_tiebreaker; EVIDENCE chips preserved; CANDIDATES list shows id + name + contact_person + recent_invoices for rich rows; legacy row shows muted 'Legacy run — limited evidence captured.' line + slim top_candidates fallback; no console errors"
    why_human: "Visual layout parity with Stage 3 panel + cross-method rendering must be eyeballed against real pipeline_events rows. Documented in 82.9-04-SUMMARY 'Operator visual checkpoint'."
---

# Phase 82.9: Stage 2 audit-panel evidence expansion — Verification Report

**Phase Goal (ROADMAP.md L760-762):** Expand the Stage 2 audit panel from `method`+customer chip to a full evidence surface — INPUTS per resolver path, REASONING when LLM tiebreaker fired, full CANDIDATES list (not just count). Achieved via widening `ResolveResult` (`resolve-debtor.ts`), `decision_details` payload (`classifier-label-resolver.ts`), `Stage2AuditPayload` + mapper + `Stage2EvidencePanel.tsx` in lockstep.

**Verified:** 2026-05-20T04:38:46Z
**Status:** human_needed — all 7 code-level must-haves VERIFIED in codebase + all 50 tests green; two intentionally-deferred operator checkpoints remain (Zap edit + prod migration apply; visual UAT in Bulk Review).
**Re-verification:** No — initial verification.

## Goal Achievement

### Observable Truths (derived from ROADMAP requirements INPUT-01..04, TYPE-01, UI-01, BRIDGE-01)

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1   | **INPUT-01** `thread_inheritance` persists `prior_email_label_id` + `conversation_id` in `decision_details.inputs` | VERIFIED | `resolve-debtor.ts:64-67` declares the arm; `:147` populates the literal; `classifier-label-resolver.ts:230` writes `result.inputs` into `decision_details` for the success path; legacy/runThreadInheritanceOnly literals at `:126, :380, :415, :427` populate the same field |
| 2   | **INPUT-02** `sender_match` persists `sender_email` + rich `candidates[]` (id, name, contact_person, recent_invoices) | VERIFIED | `resolve-debtor.ts:69-73` arm + `:209` populates it after Layer-2 single-hit candidate-details fetch; `nxt-zap-client.ts:147-148` Zod schema accepts `contact_person` + `recent_invoices` from upstream |
| 3   | **INPUT-03** `identifier_match` persists `matched_identifiers[]` + rich `candidates[]` | VERIFIED | `resolve-debtor.ts:74-78` arm + `:267` populates it |
| 4   | **INPUT-04** `llm_tiebreaker` persists `candidates` + `llm_reason` (+ sender + matched_identifiers) | VERIFIED | `resolve-debtor.ts:79-85` arm + populations at `:341, :370, :388` (incl. graceful slim-fallback paths on fetch/LLM failure with sentinel `llm_reason`); per D-02 only `llm_reason` persisted (no model/agent_key/temperature duplication) |
| 5   | **TYPE-01** `Stage2AuditPayload` widened with discriminated `inputs` + optional `candidates` + optional `reasoning` | VERIFIED | `audit-types.ts:33` exports `CandidateView`; `:40-65` exports `Stage2InputsView` 5-arm discriminated union; `:79-82` adds `inputs?` / `candidates?` / `reasoning?` fields to `Stage2AuditPayload`. Back-compat fields preserved per Pitfall 4 |
| 6   | **BRIDGE-01** Mapper exhaustively branches on `method` for new rows; legacy rows degrade to chip-only render | VERIFIED | `build-stage-audit-map.ts:149-162` `asCandidates` helper with Tier-3 `.slice(0, 5)`; `:461-464` detects legacy via `isRecord(d.inputs)` (D-04 + Pitfall 3); `:473-507` 5-arm switch (`thread_inheritance`, `sender_match`, `identifier_match`, `llm_tiebreaker`, `unresolved`) plus tolerant default arm. 16/16 mapper tests pass (9 baseline + 7 new) |
| 7   | **UI-01** Panel renders INPUTS block above EVIDENCE; REASONING below for tiebreaker; rich CANDIDATES list; legacy fallback line | VERIFIED | `Stage2EvidencePanel.tsx:380` `INPUTS` header; `:387` `REASONING` header (conditional); `:259` `CANDIDATES` header; `:179` legacy `Legacy run — limited evidence captured.` line; `:184-221` 5-arm switch in `renderInputs`. 12/12 panel tests pass (6 baseline + 6 new) |

**Score:** 7/7 truths verified (code-side).

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `web/lib/automations/debtor-email/nxt-zap-client.ts` | Extended Zod schema with `contact_person`, `recent_invoices.max(5)` | VERIFIED | L147-148 exact match; existing CandidateDetail type widens via `z.infer` |
| `supabase/migrations/20260519_phase82_9_candidate_details_schema_extension.sql` | One-row UPDATE on `zapier_tools.output_schema` for `nxt.candidate_details` | VERIFIED | File present (1761 bytes); contains UPDATE + jsonb_set + DO-block smoke check; not yet applied to prod (deferred — see human_verification) |
| `web/lib/automations/debtor-email/resolve-debtor.ts` | Widened `ResolveResult.inputs` with discriminated `Stage2Inputs` union (5 arms) | VERIFIED | `Candidate` type at L53; `Stage2Inputs` union at L62-91; `inputs: Stage2Inputs` in `ResolveResult` at L99; 8 populating literals across 5 method arms incl. graceful fallback paths |
| `web/lib/inngest/functions/classifier-label-resolver.ts` | Writer persists `decision_details.inputs` on success + threadInheritanceOnly + error paths populate discriminated inputs | VERIFIED | `inputs: result.inputs` at L230 (success path); discriminated inputs at L126, L380, L415, L427 (fallback paths) |
| `web/app/api/automations/debtor/label-email/route.ts` | Legacy webhook handler populates discriminated inputs on every ResolveResult literal | VERIFIED (transitively) | Modified per Plan 02 SUMMARY commit 4c388ba; required to satisfy widened type — TS compile would fail otherwise (it doesn't) |
| `web/app/(dashboard)/automations/[swarm]/_shell/_lib/audit-types.ts` | `Stage2InputsView` discriminated union + `CandidateView` + widened `Stage2AuditPayload` | VERIFIED | L33-82 |
| `web/app/(dashboard)/automations/[swarm]/_shell/_lib/build-stage-audit-map.ts` | Stage 2 mapper exhaustive 5-arm switch + legacy fallback + Tier-3 slice(0,5) | VERIFIED | L149-162 (asCandidates), L461-507 (switch) |
| `web/app/(dashboard)/automations/[swarm]/_shell/_lib/__tests__/build-stage-audit-map.test.tsx` | 7 new test cases covering all 5 method arms + Tier-3 + legacy | VERIFIED | 16 tests total, all green |
| `web/components/automations/bulk-review/audit/Stage2EvidencePanel.tsx` | INPUTS/REASONING/CANDIDATES sections + 5-arm switch + legacy fallback line | VERIFIED | Headers + switch + legacy line all present at expected lines |
| `web/components/automations/bulk-review/audit/__tests__/Stage2EvidencePanel.test.tsx` | 6 new fixtures per method + legacy | VERIFIED | 12 tests total, all green |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `nxt-zap-client.ts CandidateDetailMatchSchema` | Resolver Candidate persistence | `callNxtTool → CandidateDetailMatchSchema.parse(match)` | WIRED | Schema extended (L147-148); resolver `:209` and `:267` Layer-2 candidate-details fetch flows through `toCandidate()` helper into the discriminated `inputs.candidates` |
| `resolve-debtor.ts ResolveResult.inputs` | `pipeline_events.decision_details.inputs` | `classifier-label-resolver.ts:230 inputs: result.inputs` inside emitPipelineEvent | WIRED | Verified by grep + by the fact that `tsc --noEmit` passes with the now-mandatory `inputs` field on ResolveResult |
| `pipeline_events.decision_details` | `Stage2AuditPayload.inputs` | `build-stage-audit-map.ts:461-507` 5-arm switch reading `d.inputs` | WIRED | Mapper detects legacy via `isRecord(d.inputs)`; 7 new tests assert each arm's projection |
| `Stage2AuditPayload.inputs` | `Stage2EvidencePanel` rendered INPUTS/REASONING/CANDIDATES sections | `renderInputs(payload.inputs)` + `renderCandidates(payload)` + conditional REASONING block | WIRED | 6 new panel tests assert per-arm output incl. legacy fallback line |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `Stage2EvidencePanel.tsx` | `payload.inputs`, `payload.candidates`, `payload.reasoning` | Server-side mapper `build-stage-audit-map.ts` reading `pipeline_events.decision_details` | YES for new rows (Plan 02 writer populates discriminated inputs); LEGACY for pre-deploy rows (panel intentionally renders fallback line — D-04 forward-only) | FLOWING — note that production data flow requires operator to ship the Zap edit (Plan 01 Task 3) so the upstream `candidate_details` Zap actually returns the new fields. Until then, resolver receives `contact_person:null` + `recent_invoices:[]` (Zod `.optional().default([])`) — which is the documented A1 fallback. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| All four Phase 82.9 test suites green | `npx vitest run` on build-stage-audit-map + Stage2EvidencePanel + resolve-debtor + classifier-label-resolver | 4 test files, 50 passed, 4 todo (legacy placeholders preserved), 0 failed in 2.16s | PASS |
| Phase 82.9 commits exist in main branch | `git log` lookup of 9 referenced commit hashes (74fa737, df59c0e, 4c388ba, 0c2dcc5, effeb7b, d8ca890, 68fbf71, add9eee, 398c525) | All 9 resolve to expected feat/test commits | PASS |
| Three-tier recent_invoices bound declared | Inspect Tier 1 (migration `"maxItems": 5`), Tier 2 (Zod `.max(5)`), Tier 3 (mapper `.slice(0, 5)`) | All three present (migration L1761 file; nxt-zap-client.ts:148; build-stage-audit-map.ts:161) | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| INPUT-01 | 82.9-02 | thread_inheritance → prior_email_label_id, conversation_id | SATISFIED | resolve-debtor.ts:64-67, classifier-label-resolver.ts:126 |
| INPUT-02 | 82.9-01, 02 | sender_match → sender_email, candidates[id,name,…] | SATISFIED | nxt-zap-client.ts:147-148 + resolve-debtor.ts:69-73, :209 |
| INPUT-03 | 82.9-02 | identifier_match → matched_identifiers, candidates | SATISFIED | resolve-debtor.ts:74-78, :267 |
| INPUT-04 | 82.9-02 | llm_tiebreaker → candidates, llm_reason | SATISFIED | resolve-debtor.ts:79-85, :341/:370/:388 (D-02 — only llm_reason persisted, no model/temp/agent_key duplication, by design) |
| TYPE-01 | 82.9-03 | Stage2AuditPayload widened with inputs + candidates + reasoning | SATISFIED | audit-types.ts:33-82 |
| UI-01 | 82.9-04 | Panel renders INPUTS above EVIDENCE; REASONING below for tiebreaker | SATISFIED (code); HUMAN-VERIFIED VISUAL DEFERRED | Stage2EvidencePanel.tsx:259, 380, 387 |
| BRIDGE-01 | 82.9-03 | Mapper reads new keys; legacy rows degrade to chip-only render | SATISFIED | build-stage-audit-map.ts:461-507 + legacy-fallback test |

No orphaned requirements detected.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | — | No TODO/FIXME/placeholder/empty-return patterns introduced by Phase 82.9 commits | — | — |

The Plan 02 SUMMARY documents intentional graceful slim-fallback `Candidate` records (`contact_person: null`, `recent_invoices: []`) when Layer-2 candidate-details fetch fails (T-82.9-06 mitigation). This is intentional resilience behavior, not a stub. Plan 04 SUMMARY documents intentional grep-false-positive avoidance (`"use client"` and `Legacy run …` only appear in code, not comments).

### Human Verification Required

1. **Plan 01 Task 3 (CHECKPOINT — pending operator)** — Edit the existing Zapier Zap "candidate_details" to JOIN contact-persons + invoices relations and project `contact_person` (string|null) + `recent_invoices` (top-5 newest-first, SQL `LIMIT 5`). Smoke-test against acceptance NXT (read-only SELECT on `nxt_benelux_prod`). Apply migration `20260519_phase82_9_candidate_details_schema_extension.sql` to prod Supabase (`mvqjhlxfvtqqubqgdvhz`). Confirm registry row via SQL probe (see 82.9-01-SUMMARY L93-101). If `contact_person` is unreachable, A1 fallback (`null`) is accepted by Zod.

2. **Plan 04 Task 3 (CHECKPOINT — pending operator)** — Open Bulk Review for debtor-email in dev/staging; expand one Stage 2 audit panel per method (`thread_inheritance` / `sender_match` / `identifier_match` / `llm_tiebreaker` / `unresolved`) plus one legacy row. Confirm visual parity with Stage 3 sibling panel (font sizes, gaps, chip styles, header capitalisation). Confirm INPUTS at top, conditional REASONING for llm_tiebreaker only, EVIDENCE chips preserved, CANDIDATES with contact_person + recent_invoices, legacy fallback line. Confirm no console errors.

### Gaps Summary

No code-level gaps. All ROADMAP requirements have implementation evidence; all 50 tests across the 4 affected suites are green; all 9 referenced commits are present on `main`; the four mapper-bridge layers (Zap → Zod parse → resolver → writer → mapper → panel) are wired end-to-end with TS-checked discriminated unions enforcing per-method invariants.

The two remaining items are **structural human-action checkpoints** documented in the plans themselves (Plan 01 Task 3 + Plan 04 Task 3): the Zapier UI edit + prod Supabase migration apply, and the visual UAT. Both are intentionally deferred per the phase's "code-first, human-action-last" execution model and are surfaced above for operator follow-up. Until Plan 01 Task 3 ships, the panel renders the A1-fallback shape (`contact_person: null`, `recent_invoices: []`) for new rows — visually equivalent to the rich shape but with empty values, which is acceptable per the Zod schema's `.optional().default([])` and the panel's "No contact recorded" italic fallback.

**Verdict:** COMPLETE (code-side) — phase goal achieved end-to-end in the codebase. Operator follow-up required to land Zap + prod migration + visual UAT.

---

_Verified: 2026-05-20T04:38:46Z_
_Verifier: Claude (gsd-verifier)_
