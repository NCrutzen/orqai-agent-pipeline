# Phase 82.9 — Discussion Log

**Date:** 2026-05-19
**Mode:** discuss (default)
**Phase:** 82.9 — Stage 2 audit-panel evidence expansion

## Gray areas presented (multi-select)

1. Decision-details schema shape ✓ selected
2. LLM tiebreaker reproducibility depth ✓ selected
3. Candidate richness ✓ selected
4. Historical-row treatment ✓ selected

All 4 selected.

## Area 1 — Schema shape

**Options:**
- Per-path discriminated keys (recommended)
- One flat optional record

**Selected:** Per-path discriminated keys.
**Notes:** TS discriminated union on `method`. `customer_account_id`/`customer_name` stay at top level for back-compat; new richness under `inputs`. → D-01 in CONTEXT.

## Area 2 — LLM tiebreaker reproducibility depth

**Options:**
- Reason + model + agent_key (recommended)
- Reason only
- Full repro envelope

**Selected:** Reason only.
**Notes:** User chose minimal payload. Orq.ai trace IDs + `agent_runs` row cover replay/forensics elsewhere; no duplication in `decision_details`. → D-02 in CONTEXT.

## Area 3 — Candidate richness

**Options:**
- {id, name} only (recommended)
- Include contact_person + recent_invoices

**Selected:** Include contact_person + recent_invoices.
**Notes:** Operator wants forensic candidate detail without a NXT round-trip during review. Recent_invoices bounded to top 5. Derived requirement: `lookupCandidateDetails` (or a sibling endpoint) must be extended on the Zapier side — flagged for the planner as Plan 01. → D-03 in CONTEXT.

## Area 4 — Historical rows

**Options:**
- Forward-only (recommended)
- Backfill last 30 days
- Backfill last 7 days

**Selected:** Forward-only.
**Notes:** Legacy rows render "Legacy run — limited evidence captured" line in INPUTS. Backfill noted as deferred idea if operator friction emerges. → D-04 in CONTEXT.

## Deferred ideas

- Stage 2 backfill (7-day window) — revisit if operator friction.
- LLM tiebreaker full repro envelope (model/agent_key/prompt_version/temperature/trace_id).
- Clickable prior-label link in `thread_inheritance` inputs (cross-email nav).
- Stage 4 audit panel (no panel exists today).

## Claude's discretion

The following were NOT discussed individually — user explicitly delegated:
- `Stage2AuditPayload` type details (follows mechanically from D-01).
- Mapper bridge branching logic (exhaustive switch on `method`).
- Panel layout (mirror Stage 3 layout from commit `ff12322`).
- Vitest coverage scope (all 5 method paths + legacy fallback — planner decides exact test count).
