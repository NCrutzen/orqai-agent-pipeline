---
phase: 82.9
slug: stage-2-audit-panel-evidence-expansion
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-19
---

# Phase 82.9 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (web/ workspace) |
| **Config file** | `web/vitest.config.ts` |
| **Quick run command** | `cd web && npm test -- --run <changed-file>` |
| **Full suite command** | `cd web && npm test -- --run` |
| **Estimated runtime** | ~30–60 seconds full suite |

---

## Sampling Rate

- **After every task commit:** Run `cd web && npm test -- --run <changed-spec>`
- **After every plan wave:** Run `cd web && npm test -- --run`
- **Before `/gsd-verify-work`:** Full suite + `npm run typecheck` (or `tsc --noEmit`) must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

> Filled in by the planner from PLAN.md frontmatter. Each task in each PLAN.md must map to a row here before execution starts.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | INPUT-01..04 / TYPE-01 / UI-01 / BRIDGE-01 | — | — | unit / integration | TBD | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `web/app/(dashboard)/automations/[swarm]/_shell/_lib/__tests__/build-stage-audit-map.test.tsx` — **already exists** (301 lines). Plan 03 Task 3 EXTENDS this file to cover all 5 Stage 2 method arms (`thread_inheritance`, `sender_match`, `identifier_match`, `llm_tiebreaker`, `unresolved`) plus the legacy `inputs == null` fallback. No Wave 0 creation step needed.
- [x] No framework install required — vitest already configured in `web/`.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Panel renders INPUTS / REASONING / CANDIDATES correctly in Bulk Review | UI-01 | Visual layout matches Stage 3 reference panel | Open Bulk Review for debtor-email, expand a Stage 2 row per method path (thread_inheritance / sender_match / identifier_match / llm_tiebreaker / unresolved), confirm INPUTS at top, conditional REASONING for llm_tiebreaker, CANDIDATES list rendered with contact_person + recent_invoices |
| Legacy rows (pre-deploy) still render the chip-only fallback | D-04 | Cannot be re-created in a unit test without a recorded legacy row | Find a pre-deploy `pipeline_events` row (decision_details.inputs is null) and open it; INPUTS section shows muted "Legacy run — limited evidence captured" |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (verified by gsd-plan-checker 2026-05-19)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references — none missing; mapper test file already exists
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-19 (gsd-plan-checker passed with 0 blockers, 5 documentation-only warnings)
