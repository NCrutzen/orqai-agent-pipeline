---
phase: 05.1-fix-distribution-placeholders
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/commands/update.md
  - orq-agent/commands/deploy.md
  - orq-agent/commands/test.md
  - orq-agent/commands/iterate.md
  - orq-agent/commands/set-profile.md
  - orq-agent/SKILL.md
autonomous: true
gap_closure: true
requirements: [DIST-03, INST-05]

must_haves:
  truths:
    - "Zero occurrences of literal 'OWNER/REPO' remain anywhere in orq-agent/"
    - "/orq-agent:update curl URLs contain NCrutzen/orqai-agent-pipeline and point to valid raw.githubusercontent.com paths"
    - "Capability-gate fallback messages in deploy.md, test.md, iterate.md, set-profile.md contain correct install URL"
    - "SKILL.md distribution section install URL contains NCrutzen/orqai-agent-pipeline"
  artifacts:
    - path: "orq-agent/commands/update.md"
      provides: "Update command with working GitHub URLs"
      contains: "NCrutzen/orqai-agent-pipeline"
    - path: "orq-agent/commands/deploy.md"
      provides: "Deploy command with correct install fallback URL"
      contains: "NCrutzen/orqai-agent-pipeline"
    - path: "orq-agent/commands/test.md"
      provides: "Test command with correct install fallback URL"
      contains: "NCrutzen/orqai-agent-pipeline"
    - path: "orq-agent/commands/iterate.md"
      provides: "Iterate command with correct install fallback URL"
      contains: "NCrutzen/orqai-agent-pipeline"
    - path: "orq-agent/commands/set-profile.md"
      provides: "Set-profile command with correct install fallback URL"
      contains: "NCrutzen/orqai-agent-pipeline"
    - path: "orq-agent/SKILL.md"
      provides: "Skill documentation with correct install URL"
      contains: "NCrutzen/orqai-agent-pipeline"
  key_links:
    - from: "orq-agent/commands/update.md"
      to: "https://raw.githubusercontent.com/NCrutzen/orqai-agent-pipeline/main/VERSION"
      via: "curl command in Step 3"
      pattern: "raw\\.githubusercontent\\.com/NCrutzen/orqai-agent-pipeline/main/VERSION"
    - from: "orq-agent/commands/update.md"
      to: "https://raw.githubusercontent.com/NCrutzen/orqai-agent-pipeline/main/install.sh"
      via: "curl command in Step 5"
      pattern: "raw\\.githubusercontent\\.com/NCrutzen/orqai-agent-pipeline/main/install\\.sh"
---

<objective>
Replace all `OWNER/REPO` placeholder literals with `NCrutzen/orqai-agent-pipeline` across 6 files (15 occurrences) and remove 3 stale TODO comments from update.md.

Purpose: The v0.3 milestone audit identified these placeholders as a high-severity gap breaking Flow D-Update and all capability-gate fallback install URLs.
Output: All command files and SKILL.md with functional GitHub URLs.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05.1-fix-distribution-placeholders/05.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace OWNER/REPO in all 6 files and remove TODO comments</name>
  <files>orq-agent/commands/update.md, orq-agent/commands/deploy.md, orq-agent/commands/test.md, orq-agent/commands/iterate.md, orq-agent/commands/set-profile.md, orq-agent/SKILL.md</files>
  <action>
Perform exact string replacements across all 6 files. The canonical values come from install.sh (GITHUB_OWNER="NCrutzen", GITHUB_REPO="orqai-agent-pipeline").

**Pattern A — URL replacement (all 6 files, 12 occurrences):**
Replace every `OWNER/REPO` with `NCrutzen/orqai-agent-pipeline` in GitHub raw content URLs. Use `replace_all` on each file with:
- old: `OWNER/REPO`
- new: `NCrutzen/orqai-agent-pipeline`

File-by-file occurrence counts to verify completions:
- `orq-agent/commands/update.md`: 7 occurrences (lines 31, 46, 49, 81, 86, 110, 115, 147 — note: 3 are in TODO comments handled below)
- `orq-agent/commands/deploy.md`: 2 occurrences (lines 29, 56)
- `orq-agent/commands/test.md`: 2 occurrences (lines 29, 56)
- `orq-agent/commands/iterate.md`: 2 occurrences (lines 29, 56)
- `orq-agent/commands/set-profile.md`: 1 occurrence (line 30)
- `orq-agent/SKILL.md`: 1 occurrence (line 162)

**Pattern B — TODO comment removal (update.md only, 3 occurrences):**
After the URL replacements, remove these 3 lines entirely from `orq-agent/commands/update.md`:
- `<!-- TODO: Replace OWNER/REPO with actual GitHub repository path -->` (was at lines 46, 81, 110)

These TODO comments will now reference `NCrutzen/orqai-agent-pipeline` after Pattern A runs, making them stale. Remove the entire line (including any trailing newline) so no blank line artifacts remain.

**Order matters:** Run Pattern A (replace_all) first on update.md, THEN Pattern B (remove TODO lines). The TODO lines will contain `NCrutzen/orqai-agent-pipeline` after Pattern A, so use the post-replacement string for Pattern B removal:
- old: `<!-- TODO: Replace NCrutzen/orqai-agent-pipeline with actual GitHub repository path -->\n`
- new: (empty)

If the TODO text does not match exactly after replacement (the OWNER/REPO inside the TODO may or may not get replaced depending on tool behavior), grep for any remaining `<!-- TODO:` lines in update.md and remove them.
  </action>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/claude-code-prompt-agent && grep -r "OWNER/REPO" orq-agent/ ; test $? -eq 1 && echo "PASS: No OWNER/REPO found" || echo "FAIL: OWNER/REPO still present"</automated>
  </verify>
  <done>
    - Zero occurrences of literal "OWNER/REPO" in any file under orq-agent/
    - All GitHub URLs use "NCrutzen/orqai-agent-pipeline"
    - No stale TODO comments remain in update.md
    - No formatting or indentation broken in any file
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify URL correctness and file integrity</name>
  <files>orq-agent/commands/update.md, orq-agent/commands/deploy.md, orq-agent/commands/test.md, orq-agent/commands/iterate.md, orq-agent/commands/set-profile.md, orq-agent/SKILL.md</files>
  <action>
Run verification checks to confirm all replacements are correct and no files were corrupted:

1. **Positive match:** Grep for `NCrutzen/orqai-agent-pipeline` across all 6 files. Expected counts:
   - update.md: 4 URL occurrences (was 7 minus 3 removed TODOs)
   - deploy.md: 2
   - test.md: 2
   - iterate.md: 2
   - set-profile.md: 1
   - SKILL.md: 1
   - Total: 12

2. **Negative match:** Grep for `OWNER/REPO` across orq-agent/. Must return zero results.

3. **No stale TODOs:** Grep for `<!-- TODO:` in update.md. Must return zero results.

4. **URL format check:** Grep for `raw.githubusercontent.com/NCrutzen/orqai-agent-pipeline/main/` in all files. Every GitHub raw URL must follow this pattern. No malformed URLs (double slashes, missing path segments).

5. **Markdown structure check:** Confirm no empty fenced code blocks, no orphaned backticks, no missing newlines at end of file. A quick visual scan of each file's structure is sufficient.

If any check fails, fix the issue before marking done.
  </action>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/claude-code-prompt-agent && echo "=== Positive ===" && grep -rc "NCrutzen/orqai-agent-pipeline" orq-agent/commands/update.md orq-agent/commands/deploy.md orq-agent/commands/test.md orq-agent/commands/iterate.md orq-agent/commands/set-profile.md orq-agent/SKILL.md && echo "=== Negative ===" && grep -r "OWNER/REPO" orq-agent/ ; test $? -eq 1 && echo "PASS" || echo "FAIL" && echo "=== TODO check ===" && grep -c "<!-- TODO:" orq-agent/commands/update.md ; test $? -eq 1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - Positive grep returns 12 total matches across 6 files
    - Negative grep returns zero matches for OWNER/REPO
    - Zero stale TODO comments in update.md
    - All raw.githubusercontent.com URLs are well-formed
  </done>
</task>

</tasks>

<verification>
1. `grep -r "OWNER/REPO" orq-agent/` returns no results
2. `grep -rc "NCrutzen/orqai-agent-pipeline" orq-agent/commands/ orq-agent/SKILL.md` returns 12 total matches
3. `grep "<!-- TODO:" orq-agent/commands/update.md` returns no results
4. All URLs follow pattern `https://raw.githubusercontent.com/NCrutzen/orqai-agent-pipeline/main/{file}`
</verification>

<success_criteria>
- DIST-03: update.md curl URLs resolve to `raw.githubusercontent.com/NCrutzen/orqai-agent-pipeline/main/VERSION`, `CHANGELOG.md`, and `install.sh`
- INST-05: Fallback messages in deploy.md, test.md, iterate.md, set-profile.md contain working install URL for users at lower capability tiers
- Zero instances of `OWNER/REPO` anywhere in orq-agent/
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-fix-distribution-placeholders/05.1-01-SUMMARY.md`
</output>
