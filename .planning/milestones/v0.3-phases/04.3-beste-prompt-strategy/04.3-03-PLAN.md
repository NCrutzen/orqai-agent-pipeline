---
phase: 04.3-beste-prompt-strategy
plan: 03
type: execute
wave: 2
depends_on:
  - "04.3-01"
  - "04.3-02"
files_modified:
  - orq-agent/commands/orq-agent.md
  - orq-agent/agents/architect.md
  - orq-agent/agents/dataset-generator.md
  - orq-agent/agents/readme-generator.md
autonomous: true
requirements:
  - SPEC-02
  - SPEC-12
  - ORCH-01

must_haves:
  truths:
    - "Orchestrator prompt (orq-agent.md) uses XML tags for section boundaries instead of only markdown headers"
    - "Orchestrator prompt reduces rule-list anti-patterns in favor of heuristic guidelines"
    - "Architect prompt wraps few-shot examples in <example> tags with <input>/<output> pairs"
    - "Architect prompt converts anti-pattern bullet lists to heuristic guidelines with diverse examples"
    - "Dataset generator and readme generator prompts use XML-wrapped examples for consistency"
    - "All secondary target prompts maintain their existing functionality unchanged"
  artifacts:
    - path: "orq-agent/commands/orq-agent.md"
      provides: "Orchestrator prompt using XML tags and heuristic guidelines"
      contains: "<instructions>"
    - path: "orq-agent/agents/architect.md"
      provides: "Architect prompt with XML-wrapped examples and heuristic guidelines"
      contains: "<example>"
    - path: "orq-agent/agents/dataset-generator.md"
      provides: "Dataset generator with XML-wrapped examples"
      contains: "<example>"
    - path: "orq-agent/agents/readme-generator.md"
      provides: "README generator with XML-wrapped examples"
      contains: "<example>"
  key_links:
    - from: "orq-agent/commands/orq-agent.md"
      to: "orq-agent/agents/architect.md"
      via: "orchestrator invokes architect as first pipeline stage"
      pattern: "architect"
    - from: "orq-agent/commands/orq-agent.md"
      to: "orq-agent/agents/spec-generator.md"
      via: "orchestrator invokes spec generator in Wave 2"
      pattern: "spec-generator"
---

<objective>
Apply consistent XML tagging and heuristic-first patterns to the orchestrator prompt and all secondary subagent prompts (architect, dataset generator, readme generator), ensuring the orq-agent's own prompts model the same patterns it advocates for generated agents.

Purpose: Consistency -- the orq-agent's own prompts should demonstrate the same XML-tagged, heuristic-first patterns that its generated agent specs follow. This also improves the quality of these prompts themselves, as Anthropic's patterns apply equally to the orq-agent's subagents.

Output: Updated orq-agent.md, architect.md, dataset-generator.md, and readme-generator.md with XML tags and heuristic guidelines.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.3-beste-prompt-strategy/04.3-CONTEXT.md
@.planning/phases/04.3-beste-prompt-strategy/04.3-RESEARCH.md
@.planning/phases/04.3-beste-prompt-strategy/04.3-01-SUMMARY.md
@.planning/phases/04.3-beste-prompt-strategy/04.3-02-SUMMARY.md
@orq-agent/commands/orq-agent.md
@orq-agent/agents/architect.md
@orq-agent/agents/dataset-generator.md
@orq-agent/agents/readme-generator.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor orq-agent.md orchestrator prompt with XML tags and reduced rule-lists</name>
  <files>orq-agent/commands/orq-agent.md</files>
  <action>
Refactor the main orchestrator prompt (orq-agent.md) to use XML tags for section boundaries and convert rule-heavy anti-pattern lists to heuristic guidelines.

**Changes:**

1. **XML section boundaries:** Wrap the major sections of the orchestrator prompt in XML tags for clear parsing:
   - `<role>` -- the orchestrator's identity and purpose
   - `<pipeline>` -- the 7-step execution pipeline (Steps 0-7)
   - `<classification>` -- input classification logic
   - `<error_handling>` -- graceful degradation rules
   - `<output_rules>` -- output format and directory structure rules
   Keep step numbering and step headers within the `<pipeline>` tag as-is -- the XML wrapping adds structure without breaking the existing flow.

2. **Rule-list reduction:** Identify sections with extensive "CRITICAL:", "NEVER:", "IMPORTANT:" patterns and convert to heuristic guidelines where appropriate:
   - Keep CRITICAL/NEVER rules for: pipeline step ordering, file path safety, output directory structure
   - Convert to heuristics for: classification logic (currently rule-based, could be more heuristic), subagent invocation patterns, error handling decisions
   - The goal is not to remove all rules but to balance rules (for safety/structure) with heuristics (for judgment calls)

3. **Avoid breaking changes:** The orchestrator is the most critical file (26KB). Changes must be structural (wrapping in XML tags, consolidating redundant rules) not behavioral. Every pipeline step must produce the same outcome. Test by reading through each step and confirming no functional logic changes.

**What NOT to change:**
- Pipeline step ordering (Step 0-7)
- Flag parsing logic (--output, --gsd)
- Wave execution pattern
- Blueprint review checkpoint flow
- Output directory structure
- Any file path references
  </action>
  <verify>
    <automated>grep -c "&lt;role&gt;\|&lt;pipeline&gt;\|&lt;classification&gt;\|&lt;error_handling&gt;\|&lt;output_rules&gt;" orq-agent/commands/orq-agent.md | awk '{if ($1 >= 5) print "PASS: XML section tags present ("$1" occurrences)"; else print "FAIL: Expected 5+ XML section tags, found "$1}'</automated>
    <manual>Read orq-agent.md and verify all 8 pipeline steps still present and functional, XML tags wrap sections cleanly, and no behavioral logic has changed</manual>
  </verify>
  <done>Orchestrator prompt uses XML tags for section boundaries (role, pipeline, classification, error_handling, output_rules). Rule-heavy sections converted to heuristic guidelines where safe. All pipeline steps preserve existing behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade architect, dataset-generator, and readme-generator with XML examples and heuristic guidelines</name>
  <files>
    orq-agent/agents/architect.md
    orq-agent/agents/dataset-generator.md
    orq-agent/agents/readme-generator.md
  </files>
  <action>
Apply consistent XML tagging and heuristic-first patterns to the three secondary subagent prompts.

**architect.md changes:**

1. **XML-wrapped examples:** The architect has 3 few-shot examples (simple/moderate/complex). Wrap each in `<example>` tags with `<input>` (use case description) and `<output>` (blueprint) sub-tags. This matches the pattern the spec generator now produces.

2. **Anti-pattern to heuristic conversion:** The architect has an anti-patterns section with bullet lists. Convert to:
   - Keep explicit rules for: complexity gate (max 5 agents), agent key format, blueprint structure requirements
   - Convert behavioral guidelines to heuristics: "When deciding if a use case needs multiple agents, ask: would a single skilled person handle this sequentially, or would they naturally delegate to specialists? If sequential, single agent. If delegation, multi-agent."
   - Convert "Do NOT over-engineer" bullets into an example showing over-engineering vs. right-sizing

3. **XML section boundaries:** Wrap major sections in XML tags: `<role>`, `<decision_framework>`, `<complexity_gate>`, `<output_format>`, `<examples>`, `<constraints>`

**dataset-generator.md changes:**

1. **XML-wrapped examples:** Wrap the few-shot dataset examples in `<example>` tags
2. **XML section boundaries:** Wrap major sections: `<role>`, `<dataset_format>`, `<adversarial_taxonomy>`, `<examples>`, `<constraints>`
3. Minor scope -- dataset format is structured, less prose-heavy. Focus on example wrapping and section boundaries.

**readme-generator.md changes:**

1. **XML-wrapped examples:** Wrap the few-shot README examples in `<example>` tags
2. **XML section boundaries:** Wrap major sections: `<role>`, `<readme_format>`, `<examples>`, `<constraints>`
3. Minor scope -- README generation is straightforward. Focus on consistency with other subagents.

**For all three files:**
- Preserve all existing functionality -- these are structural improvements
- Do not change output formats, file references, or template relationships
- Apply Claude 4.6 guidance: avoid aggressive tool-triggering language ("CRITICAL: You MUST use..."), use normal prompting instead
  </action>
  <verify>
    <automated>for f in orq-agent/agents/architect.md orq-agent/agents/dataset-generator.md orq-agent/agents/readme-generator.md; do count=$(grep -c "&lt;example&gt;\|&lt;role&gt;\|&lt;constraints&gt;" "$f" 2>/dev/null); echo "$f: $count XML tags"; done | awk '{total += $NF} END {if (total >= 9) print "PASS: Total XML tags across 3 files: "total; else print "FAIL: Expected 9+ total XML tags, found "total}'</automated>
    <manual>Read each file and verify examples are wrapped in XML tags, sections have XML boundaries, and no functional changes to output formats</manual>
  </verify>
  <done>Architect has XML-wrapped examples and heuristic guidelines replacing anti-pattern bullet lists. Dataset generator and readme generator have XML-wrapped examples and section boundaries. All three files maintain existing functionality with improved structural consistency.</done>
</task>

</tasks>

<verification>
- [ ] orq-agent.md has XML section tags (role, pipeline, classification, error_handling, output_rules)
- [ ] orq-agent.md pipeline Steps 0-7 all present and unchanged in behavior
- [ ] architect.md 3 examples wrapped in `<example>` tags
- [ ] architect.md anti-patterns converted to heuristic guidelines (keeping safety rules)
- [ ] dataset-generator.md examples wrapped in XML tags
- [ ] readme-generator.md examples wrapped in XML tags
- [ ] No output format changes in any file
- [ ] No file path or template reference changes
- [ ] Consistent XML tag usage across all modified files
</verification>

<success_criteria>
All orq-agent prompts consistently use XML tags for section boundaries and example wrapping. The orchestrator prompt demonstrates heuristic-first patterns. Anti-pattern bullet lists are replaced with heuristic guidelines where appropriate, keeping explicit rules only for safety/structure concerns. All files maintain their existing functional behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-beste-prompt-strategy/04.3-03-SUMMARY.md`
</output>
