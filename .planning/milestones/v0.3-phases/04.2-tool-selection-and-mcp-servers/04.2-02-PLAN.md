---
phase: 04.2-tool-selection-and-mcp-servers
plan: 02
type: execute
wave: 2
depends_on:
  - 04.2-01
files_modified:
  - orq-agent/commands/orq-agent.md
  - orq-agent/agents/spec-generator.md
  - orq-agent/agents/researcher.md
  - orq-agent/SKILL.md
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03
  - TOOL-04

must_haves:
  truths:
    - "The orchestrator pipeline includes a tool resolver stage between architect and researcher"
    - "The spec generator defers to TOOLS.md for tool selection when available"
    - "The researcher notes TOOLS.md as authoritative for tool configuration"
    - "SKILL.md lists the tool resolver subagent, tool catalog reference, and tools template"
  artifacts:
    - path: "orq-agent/commands/orq-agent.md"
      provides: "Updated orchestrator with tool resolver stage"
      contains: "Tool Resolver"
    - path: "orq-agent/agents/spec-generator.md"
      provides: "Spec generator updated to defer to TOOLS.md"
      contains: "TOOLS.md"
    - path: "orq-agent/agents/researcher.md"
      provides: "Researcher updated to note TOOLS.md"
      contains: "TOOLS.md"
    - path: "orq-agent/SKILL.md"
      provides: "Updated skill index with tool resolver entries"
      contains: "tool-resolver"
  key_links:
    - from: "orq-agent/commands/orq-agent.md"
      to: "orq-agent/agents/tool-resolver.md"
      via: "Task tool subagent invocation"
      pattern: "tool-resolver\\.md"
    - from: "orq-agent/commands/orq-agent.md"
      to: "TOOLS.md output path"
      via: "File path passed to downstream stages"
      pattern: "TOOLS\\.md"
    - from: "orq-agent/agents/spec-generator.md"
      to: "TOOLS.md"
      via: "Input file reference for tool selection"
      pattern: "TOOLS\\.md"
---

<objective>
Wire the tool resolver into the existing orchestrator pipeline and update downstream subagents and SKILL.md to be aware of TOOLS.md.

Purpose: The foundation artifacts from Plan 01 are inert until wired into the pipeline. This plan inserts the tool resolver stage between architect and researcher, updates the spec generator and researcher to consume TOOLS.md, and updates the skill index.

Output: Updated orchestrator command, spec generator, researcher, and SKILL.md.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-tool-selection-and-mcp-servers/04.2-CONTEXT.md
@.planning/phases/04.2-tool-selection-and-mcp-servers/04.2-RESEARCH.md
@.planning/phases/04.2-tool-selection-and-mcp-servers/04.2-01-SUMMARY.md
@orq-agent/commands/orq-agent.md
@orq-agent/agents/spec-generator.md
@orq-agent/agents/researcher.md
@orq-agent/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert tool resolver stage into orchestrator pipeline</name>
  <files>
    orq-agent/commands/orq-agent.md
  </files>
  <action>
Modify the orchestrator command (`orq-agent/commands/orq-agent.md`) to insert a new tool resolver stage between Step 5 (Blueprint Review) and Step 6 (Execute Generation Pipeline). This changes the pipeline from:

```
Architect -> Researcher -> Spec Generator -> (Wave 3)
```

To:

```
Architect -> Tool Resolver -> Researcher -> Spec Generator -> (Wave 3)
```

**Specific changes:**

1. **Add a new step "Step 5.5: Run Tool Resolver"** between Step 5 (Blueprint Review) and Step 6 (Execute Generation Pipeline). Insert it AFTER the "Post-Blueprint Updates" section of Step 5 and BEFORE Step 6's Wave 1.

   Content of the new step:

   ```
   ## Step 5.5: Run Tool Resolver

   Display the tool resolver banner:

   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ORQ > TOOL RESOLVER
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   The tool resolver ALWAYS runs (never skipped) -- even when the researcher is skipped, tools still need resolution.

   Spawn the tool resolver subagent using the Task tool:
   - Agent file: @orq-agent/agents/tool-resolver.md
   - Input: Pass the blueprint file path ({OUTPUT_DIR}/[swarm-name]/blueprint.md) and the original user input
   - Files to read: The tool resolver loads its own references via files_to_read -- do NOT load them in the orchestrator

   The tool resolver writes: {OUTPUT_DIR}/[swarm-name]/TOOLS.md

   On completion, display:
   ✓ Tool resolution complete: [summary of tool types found]

   Error handling: If the tool resolver fails:
   - Write marker file: {OUTPUT_DIR}/[swarm-name]/TOOLS.md.incomplete with error details
   - Log failure in failures list
   - Continue to Wave 1 -- downstream stages will still function without TOOLS.md but tool recommendations will be less accurate

   Store the TOOLS.md path for use in Wave 1 (researcher) and Wave 2 (spec generator).
   ```

2. **Update Step 6 Wave 1 (Research)** -- Add TOOLS.md as additional input to the researcher invocation. In the researcher spawn section, add a 3rd input file path:
   - Add: `3. TOOLS.md: {OUTPUT_DIR}/[swarm-name]/TOOLS.md (or note "Tool resolution unavailable" if Step 5.5 failed)`

3. **Update Step 6 Wave 2 (Spec Generation)** -- Add TOOLS.md as additional input to the spec generator invocation. In the spec generator spawn section, update the input list:
   - Add between research brief and agent key: `3. TOOLS.md: {OUTPUT_DIR}/[swarm-name]/TOOLS.md (or note "Tool resolution unavailable" if Step 5.5 failed)`
   - Renumber the agent key input to 4.

4. **Update Step 6 pipeline tracker initialization** -- Add `tool_resolver` to the `stages_completed` structure.

5. **Update Step 7.1 directory tree** -- Add `TOOLS.md` to the output tree listing (after blueprint.md, before ORCHESTRATION.md).

6. **Update Step 7.5 pipeline metadata JSON** -- Add tool_resolver stage entry to the `stages` classification and `stages_completed` array:
   ```json
   "tool_resolver": { "decision": "run", "reason": "Always runs to resolve tool needs" }
   ```

7. **Update the stage decisions table in Step 2** -- Add a row:
   ```
   | Tool Resolver | RUN | Always runs -- resolves tool needs from blueprint regardless of input detail |
   ```

Do NOT renumber existing steps. Use "Step 5.5" to avoid disrupting the existing step numbering that downstream references may depend on.
  </action>
  <verify>
    <automated>grep -q "Tool Resolver" orq-agent/commands/orq-agent.md && grep -q "tool-resolver.md" orq-agent/commands/orq-agent.md && grep -q "TOOLS.md" orq-agent/commands/orq-agent.md && grep -c "Step 5.5" orq-agent/commands/orq-agent.md | awk '{if ($1 >= 1) print "PASS: Step 5.5 exists"; else print "FAIL: Step 5.5 missing"}'</automated>
    <manual>Verify Step 5.5 is positioned between Step 5 and Step 6. Verify TOOLS.md is passed as input to both researcher (Wave 1) and spec generator (Wave 2). Verify pipeline metadata includes tool_resolver stage.</manual>
  </verify>
  <done>Orchestrator pipeline includes tool resolver as Step 5.5 between blueprint review and generation waves. TOOLS.md path is passed to researcher and spec generator. Pipeline metadata tracks tool_resolver stage. Tool resolver always runs (never skipped).</done>
</task>

<task type="auto">
  <name>Task 2: Update downstream subagents and SKILL.md for TOOLS.md awareness</name>
  <files>
    orq-agent/agents/spec-generator.md
    orq-agent/agents/researcher.md
    orq-agent/SKILL.md
  </files>
  <action>
**File 1: `orq-agent/agents/spec-generator.md`** -- Minimal update per RESEARCH.md recommendation

Add a new section after the existing "Critical Rules" section (or as rule #6 if numbered):

```markdown
### TOOLS.md Integration

When TOOLS.md is available for this swarm (provided as an input file path by the orchestrator):
- **Read TOOLS.md first** before generating the Tools section of the agent spec
- **Use TOOLS.md recommendations as authoritative** for tool selection -- do not override MCP, built-in, or HTTP tool choices from TOOLS.md
- **Add function tool JSON schemas** as needed (TOOLS.md provides scaffolds; spec generator fills in detailed parameter descriptions based on the agent's specific role)
- **Reference TOOLS.md config JSON** for MCP tool setup -- copy the Orq.ai-native config JSON directly into the agent spec's tool configuration
- When TOOLS.md is NOT available (tool resolution failed or was unavailable): generate tool recommendations independently using the existing tool generation logic

This ensures TOOLS.md and agent spec tool sections are consistent, not contradictory.
```

Also add the TOOLS.md file path to the "You process ONE agent at a time" section as input #4 (optional):
```
4. Optionally, TOOLS.md for the swarm (authoritative tool landscape and per-agent assignments)
```

**File 2: `orq-agent/agents/researcher.md`** -- Minimal update per RESEARCH.md recommendation

Add a note in the "Research Strategy > Approach" section, after the existing paragraph about researching the entire swarm:

```markdown
### TOOLS.md Awareness

When TOOLS.md is available (provided as an input file path by the orchestrator), the tool resolver has already identified and verified the tool landscape for the swarm. In this case:
- Still produce tool recommendations in your research brief (since TOOLS.md may be incomplete for domain-specific tools)
- Note "See TOOLS.md for authoritative tool configuration" at the end of each agent's Tool Recommendations section
- Do NOT contradict TOOLS.md tool type choices (e.g., if TOOLS.md recommends an MCP server for Slack, do not recommend an HTTP tool for Slack instead)
- Focus your tool research on domain-specific tools that may not be in TOOLS.md rather than re-evaluating tools already resolved
```

**File 3: `orq-agent/SKILL.md`** -- Add tool resolver to skill index

1. **Update Directory Structure** -- Add three new entries:
   ```
   agents/
     tool-resolver.md             # Phase 4.2: Tool resolver subagent
   references/
     tool-catalog.md              # Unified tool catalog (built-in + MCP + HTTP/function patterns)
   templates/
     tools.md                     # Template: tool landscape and per-agent assignments
   ```

2. **Update Output Directory Convention** -- Add TOOLS.md to the output structure:
   ```
   Agents/[swarm-name]/
     TOOLS.md                     # Tool landscape and per-agent assignments (always generated)
   ```

3. **Add tool resolver to Subagents table** -- Under Phase 2 (Core Generation) section, add a new "Phase 4.2 (Tool Resolution)" section:
   ```markdown
   ### Phase 4.2 (Tool Resolution)

   | Agent | File | Purpose |
   |-------|------|---------|
   | Tool Resolver | `agents/tool-resolver.md` | Resolves tool needs per agent by consulting curated catalog and web search, produces TOOLS.md with verified Orq.ai-native configs |
   ```

4. **Update References table** -- Add:
   ```
   | `tool-catalog.md` | Unified catalog: built-in tools, 21 MCP servers, HTTP/function patterns with resolution priority chain |
   ```

5. **Update Templates table** -- Add:
   ```
   | `tools.md` | Template for TOOLS.md output with capability-first organization and per-agent config JSON |
   ```

6. **Update Key Design Decisions** -- Add:
   ```
   - **MCP-first tool resolution:** Tool resolver prefers MCP servers for external integrations, verified via web search before recommending
   - **Tool resolver always runs:** Even when researcher is skipped, tools still need resolution from the architect blueprint
   - **TOOLS.md is authoritative:** Spec generator and researcher defer to TOOLS.md for tool selection; both add domain-specific detail but do not override
   ```

7. **Update pipeline description** in Commands section if present -- note the new pipeline order: Architect -> Tool Resolver -> Researcher -> Spec Generator -> (Wave 3).
  </action>
  <verify>
    <automated>grep -q "TOOLS.md" orq-agent/agents/spec-generator.md && grep -q "TOOLS.md" orq-agent/agents/researcher.md && grep -q "tool-resolver" orq-agent/SKILL.md && grep -q "tool-catalog" orq-agent/SKILL.md && grep -q "tools.md" orq-agent/SKILL.md && echo "PASS: all files updated with TOOLS.md awareness" || echo "FAIL: some files missing TOOLS.md references"</automated>
    <manual>Verify spec generator defers to TOOLS.md as authoritative. Verify researcher notes TOOLS.md without contradicting it. Verify SKILL.md has all three new entries (tool-resolver, tool-catalog, tools template) and updated pipeline description.</manual>
  </verify>
  <done>Spec generator defers to TOOLS.md for tool selection when available. Researcher notes TOOLS.md as authoritative while still producing its own recommendations. SKILL.md lists all three new artifacts (tool-resolver subagent, tool-catalog reference, tools template) and documents the updated pipeline order.</done>
</task>

</tasks>

<verification>
1. Orchestrator pipeline order is: Architect -> Tool Resolver (Step 5.5) -> Researcher (Wave 1) -> Spec Generator (Wave 2) -> Post-Generation (Wave 3)
2. Tool resolver always runs (never skipped)
3. TOOLS.md is passed as input to both researcher and spec generator
4. Spec generator defers to TOOLS.md for tool selection
5. Researcher notes TOOLS.md without contradicting it
6. SKILL.md reflects all new artifacts and updated pipeline
7. Pipeline metadata tracks tool_resolver stage
</verification>

<success_criteria>
The tool resolver is fully wired into the orchestrator pipeline. Running `/orq-agent` with any use case description will execute the tool resolver after the architect and before the researcher, producing a TOOLS.md that downstream stages consume. SKILL.md accurately documents the complete skill structure.
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-tool-selection-and-mcp-servers/04.2-02-SUMMARY.md`
</output>
