---
phase: 05-references-install-and-capability-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/references/orqai-api-endpoints.md
  - orq-agent/references/orqai-evaluator-types.md
  - orq-agent/templates/deploy-log.json
  - orq-agent/templates/test-results.json
  - orq-agent/templates/iteration-log.json
autonomous: true
requirements:
  - REF-03
  - REF-04
  - REF-05

must_haves:
  truths:
    - "Orq.ai API endpoints reference covers agents, tools, datasets, evaluators, and experiments CRUD endpoints with method, path, and description"
    - "Orq.ai evaluator types reference documents all 41 evaluators across 3 categories (19 function, 10 LLM, 12 RAGAS) plus 4 custom evaluator types (LLM, Python, HTTP, JSON)"
    - "V2.0 output templates exist in JSON format for deploy-log, test-results, and iteration-log"
  artifacts:
    - path: "orq-agent/references/orqai-api-endpoints.md"
      provides: "REST API endpoint reference for deployer, tester, iterator subagents"
      contains: "/v2/agents"
    - path: "orq-agent/references/orqai-evaluator-types.md"
      provides: "Evaluator taxonomy for tester subagent"
      contains: "function evaluator"
    - path: "orq-agent/templates/deploy-log.json"
      provides: "Deployment audit trail template"
      contains: "deployment_id"
    - path: "orq-agent/templates/test-results.json"
      provides: "Test results template"
      contains: "evaluator"
    - path: "orq-agent/templates/iteration-log.json"
      provides: "Iteration audit trail template"
      contains: "iteration"
  key_links:
    - from: "orq-agent/references/orqai-api-endpoints.md"
      to: "Phase 6 deployer subagent"
      via: "Deployer reads API reference for endpoint paths and payloads"
      pattern: "orqai-api-endpoints"
    - from: "orq-agent/references/orqai-evaluator-types.md"
      to: "Phase 7 tester subagent"
      via: "Tester reads evaluator reference for evaluator selection"
      pattern: "orqai-evaluator-types"
---

<objective>
Create new Orq.ai-specific reference files and V2.0 output templates that Phase 6-8 subagents will consume.

Purpose: Phase 6 (deployer) needs API endpoint paths, Phase 7 (tester) needs evaluator taxonomy, and all V2.0 phases need structured output templates for audit trails. Creating these now means later phases can focus on logic, not reference research.

Output: 2 new reference files (API endpoints, evaluator types) + 3 new JSON templates (deploy-log, test-results, iteration-log).
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-references-install-and-capability-infrastructure/05-RESEARCH.md
@orq-agent/references/orqai-agent-fields.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Orq.ai API endpoints and evaluator types references</name>
  <files>orq-agent/references/orqai-api-endpoints.md, orq-agent/references/orqai-evaluator-types.md</files>
  <action>
Create two new reference files. Both must follow project conventions (header with purpose, tables, under 1000 words each). User decision: single Orq.ai API file with sections per domain, not split into separate files.

**File 1: `orq-agent/references/orqai-api-endpoints.md`**

Structure:
1. Header: "Orq.ai REST API endpoint reference for autonomous deployment, testing, and iteration"
2. Base URL: `https://api.orq.ai/v2/`
3. Authentication: `Authorization: Bearer $ORQ_API_KEY`
4. Sections by domain, each with a table of Method | Path | Description:
   - **Agents**: CRUD (create, list, get, update, delete) + agent execution endpoints
   - **Tools**: CRUD for 5 tool types (function, HTTP, code, MCP, JSON schema)
   - **Datasets**: CRUD + row management
   - **Evaluators**: CRUD + 4 custom types (LLM, Python, HTTP, JSON)
   - **Experiments/Deployments**: Create, run, get results
   - **Prompts**: Create, version, get
   - **Memory Stores**: CRUD for KB support
   - **Models**: List (used for API key validation)
5. Keep endpoint descriptions brief (method + path + 5-word description). Do NOT include request/response bodies -- subagents can fetch full docs when needed.

Fetch endpoint inventory from https://docs.orq.ai/llms.txt via WebFetch for accuracy.

**File 2: `orq-agent/references/orqai-evaluator-types.md`**

Structure:
1. Header: "Orq.ai evaluator types reference for automated testing and guardrail configuration"
2. **Built-in Function Evaluators** (19): Table with Name | What it measures | Score type (binary/continuous). Include: exactness, bleu, rouge, hallucination, faithfulness, answer_relevancy, context_precision, context_recall, toxicity, readability, etc.
3. **LLM Evaluators** (10): Pre-built LLM-as-judge evaluators with custom judge prompt capability
4. **RAGAS Evaluators** (12): RAG-specific quality metrics
5. **Custom Evaluator Types** (4 categories): LLM (custom judge prompts), Python (custom code), HTTP (external endpoints), JSON (custom schema) -- user defines their own logic
6. **Selection Guidance**: When to use which evaluator type based on what you're testing (semantic quality -> LLM, structural checks -> function, RAG quality -> RAGAS)

Fetch evaluator details from https://docs.orq.ai/docs/evaluators/library via WebFetch for accuracy.
  </action>
  <verify>
    <automated>test -f orq-agent/references/orqai-api-endpoints.md && test -f orq-agent/references/orqai-evaluator-types.md && grep -q "/v2/agents" orq-agent/references/orqai-api-endpoints.md && grep -q "function evaluator\|Function Evaluator" orq-agent/references/orqai-evaluator-types.md && echo "PASS"</automated>
    <manual>API reference has sections for agents, tools, datasets, evaluators, experiments. Evaluator reference covers all 3 categories plus 4 custom types.</manual>
  </verify>
  <done>Both reference files exist under 1000 words each, API reference covers all 5 CRUD domains, evaluator reference documents all 41 evaluators across 3 categories plus 4 custom types with selection guidance</done>
</task>

<task type="auto">
  <name>Task 2: Create V2.0 output templates in JSON format</name>
  <files>orq-agent/templates/deploy-log.json, orq-agent/templates/test-results.json, orq-agent/templates/iteration-log.json</files>
  <action>
Create three JSON template files for V2.0 pipeline output. User decision: structured data format (JSON), not markdown -- humans monitor activity in Orq.ai UI, these are for pipeline audit trail and subagent consumption.

**File 1: `orq-agent/templates/deploy-log.json`**
Schema for deployment audit trail. Fields:
```json
{
  "deployment_id": "{{DEPLOYMENT_ID}}",
  "swarm_name": "{{SWARM_NAME}}",
  "deployed_at": "{{TIMESTAMP}}",
  "tier": "{{TIER}}",
  "agents": [
    {
      "agent_key": "{{AGENT_KEY}}",
      "orqai_agent_id": "{{ORQAI_ID}}",
      "status": "created|updated|failed",
      "orqai_url": "{{STUDIO_URL}}",
      "version": "{{VERSION}}",
      "tools_deployed": ["{{TOOL_ID}}"],
      "team_of_agents": ["{{CHILD_AGENT_ID}}"]
    }
  ],
  "tools": [
    {
      "tool_name": "{{TOOL_NAME}}",
      "tool_type": "function|http|code|mcp|json_schema",
      "orqai_tool_id": "{{ORQAI_TOOL_ID}}",
      "status": "created|updated|failed",
      "assigned_to": ["{{AGENT_KEY}}"]
    }
  ],
  "verification": {
    "all_agents_verified": false,
    "discrepancies": []
  },
  "summary": "{{DEPLOYMENT_SUMMARY}}"
}
```

**File 2: `orq-agent/templates/test-results.json`**
Schema for test results. Fields:
```json
{
  "test_run_id": "{{TEST_RUN_ID}}",
  "swarm_name": "{{SWARM_NAME}}",
  "tested_at": "{{TIMESTAMP}}",
  "dataset": {
    "total_examples": 0,
    "train_count": 0,
    "test_count": 0,
    "holdout_count": 0
  },
  "evaluators": [
    {
      "name": "{{EVALUATOR_NAME}}",
      "type": "function|llm|ragas|python|http|json",
      "orqai_evaluator_id": "{{ORQAI_ID}}"
    }
  ],
  "results": {
    "overall_pass": false,
    "per_agent": [
      {
        "agent_key": "{{AGENT_KEY}}",
        "scores": {
          "{{EVALUATOR_NAME}}": {
            "median": 0.0,
            "variance": 0.0,
            "confidence_interval": [0.0, 0.0],
            "pass": false,
            "runs": [0.0, 0.0, 0.0]
          }
        },
        "worst_cases": []
      }
    ]
  },
  "summary": "{{RESULTS_SUMMARY}}"
}
```

**File 3: `orq-agent/templates/iteration-log.json`**
Schema for iteration audit trail. Fields:
```json
{
  "iteration_id": "{{ITERATION_ID}}",
  "swarm_name": "{{SWARM_NAME}}",
  "iterations": [
    {
      "iteration_number": 1,
      "started_at": "{{TIMESTAMP}}",
      "agent_key": "{{AGENT_KEY}}",
      "diagnosis": "{{PLAIN_LANGUAGE_DIAGNOSIS}}",
      "proposed_changes": [
        {
          "field": "instructions|tools|model|context",
          "change_type": "add|modify|remove",
          "reasoning": "{{WHY}}",
          "diff": "{{CHANGE_DIFF}}"
        }
      ],
      "approval_status": "pending|approved|rejected",
      "scores_before": {},
      "scores_after": {},
      "improvement_pct": 0.0
    }
  ],
  "stopping_reason": "max_iterations|max_api_calls|timeout|min_improvement|success",
  "total_api_calls": 0,
  "total_duration_seconds": 0
}
```

Each template should be a valid JSON file with placeholder values. Include a `_template_meta` field at the top:
```json
{
  "_template_meta": {
    "template": "deploy-log|test-results|iteration-log",
    "version": "2.0",
    "description": "..."
  },
  ...
}
```
  </action>
  <verify>
    <automated>node -e "['deploy-log','test-results','iteration-log'].forEach(f => { try { JSON.parse(require('fs').readFileSync('orq-agent/templates/' + f + '.json', 'utf8')); console.log('PASS: ' + f + '.json is valid JSON'); } catch(e) { console.log('FAIL: ' + f + '.json - ' + e.message); } })"</automated>
    <manual>All 3 templates contain appropriate fields for their domain (deploy has agents/tools/verification, test has evaluators/scores/confidence, iteration has diagnosis/changes/approval)</manual>
  </verify>
  <done>Three valid JSON template files exist in orq-agent/templates/ with appropriate fields for deployment audit, test results, and iteration tracking</done>
</task>

</tasks>

<verification>
- 2 new reference files exist under 1000 words each
- 3 new JSON templates are valid JSON
- API reference covers agents, tools, datasets, evaluators, experiments domains
- Evaluator reference covers all 3 categories (function, LLM, RAGAS) + 4 custom types
- No V1.0 subagent prompts were modified
</verification>

<success_criteria>
- `orq-agent/references/orqai-api-endpoints.md` has sections per domain per user decision
- `orq-agent/references/orqai-evaluator-types.md` documents all evaluator types with selection guidance
- All 3 JSON templates parse without errors and contain placeholder fields matching Orq.ai API concepts
</success_criteria>

<output>
After completion, create `.planning/phases/05-references-install-and-capability-infrastructure/05-02-SUMMARY.md`
</output>
