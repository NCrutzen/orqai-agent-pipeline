---
phase: 05-references-install-and-capability-infrastructure
plan: 04
type: execute
wave: 2
depends_on:
  - "05-03"
files_modified:
  - orq-agent/commands/deploy.md
  - orq-agent/commands/test.md
  - orq-agent/commands/iterate.md
  - orq-agent/commands/set-profile.md
  - orq-agent/SKILL.md
autonomous: true
requirements:
  - INST-04
  - INST-05

must_haves:
  truths:
    - "Running /orq-agent:deploy when tier is core produces a clear upgrade message with tier comparison table"
    - "Running /orq-agent:test when tier is below test produces a clear upgrade message"
    - "Running /orq-agent:iterate when tier is below full produces a clear upgrade message"
    - "Base /orq-agent generation works identically at all tiers"
    - "V2.0 commands check MCP availability and warn + fall back to V1.0 output when MCP unavailable"
    - "User can run /orq-agent:set-profile to switch between quality/balanced/budget model profiles"
    - "SKILL.md documents all new commands, references, and templates added in Phase 5"
  artifacts:
    - path: "orq-agent/commands/deploy.md"
      provides: "Deploy command stub with capability gate"
      contains: "gate"
    - path: "orq-agent/commands/test.md"
      provides: "Test command stub with capability gate"
      contains: "gate"
    - path: "orq-agent/commands/iterate.md"
      provides: "Iterate command stub with capability gate"
      contains: "gate"
    - path: "orq-agent/commands/set-profile.md"
      provides: "Model profile management command"
      contains: "quality"
    - path: "orq-agent/SKILL.md"
      provides: "Updated skill index with V2.0 additions"
      contains: "deploy"
  key_links:
    - from: "orq-agent/commands/deploy.md"
      to: ".orq-agent/config.json"
      via: "Gate function reads config to check tier"
      pattern: "config.json.*tier"
    - from: "orq-agent/commands/set-profile.md"
      to: ".orq-agent/config.json"
      via: "Profile command reads and writes config"
      pattern: "model_profile"
---

<objective>
Create capability-gated V2.0 command stubs, model profile management, and update SKILL.md so the command surface area is ready for Phase 6-8 implementation.

Purpose: Users need clear feedback when running commands above their tier, and model profiles need a management command. Command stubs with gates ensure Phase 6-8 can focus on implementation logic without building the gate infrastructure. SKILL.md must reflect all Phase 5 additions.

Output: 4 new command files (deploy, test, iterate, set-profile) + updated SKILL.md.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-references-install-and-capability-infrastructure/05-RESEARCH.md
@.planning/phases/05-references-install-and-capability-infrastructure/05-03-SUMMARY.md
@orq-agent/SKILL.md
@orq-agent/commands/orq-agent.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create capability-gated V2.0 command stubs and set-profile command</name>
  <files>orq-agent/commands/deploy.md, orq-agent/commands/test.md, orq-agent/commands/iterate.md, orq-agent/commands/set-profile.md</files>
  <action>
Create 4 new command files in `orq-agent/commands/`. Follow the existing command file pattern (see `orq-agent/commands/help.md` and `orq-agent/commands/update.md` for style).

**Common Gate Pattern** (used in deploy, test, iterate):

Each V2.0 command starts with a `<gate>` section:
1. Read `~/.claude/skills/orq-agent/.orq-agent/config.json`
2. If file missing: display "Install required. Run the orq-agent install script first." and STOP
3. Extract `tier` value
4. Check tier hierarchy: full > test > deploy > core
5. If current tier < required tier: display upgrade message with tier comparison table and STOP
6. After gate passes: check MCP availability (attempt a lightweight MCP operation)
7. If MCP unavailable: display warning "MCP server not available. Falling back to V1.0 copy-paste output." and produce V1.0-style output instead of autonomous operation (INST-05 fallback behavior)

**File 1: `orq-agent/commands/deploy.md`** (requires: deploy tier)
- Gate: requires "deploy" tier or higher
- Stub body: "Phase 6 will implement deployment logic here. For now, display: 'Deploy command ready. Implementation coming in Phase 6.'"
- MCP fallback: when MCP unavailable, output deployment instructions as copy-paste steps instead of autonomous deploy

**File 2: `orq-agent/commands/test.md`** (requires: test tier)
- Gate: requires "test" tier or higher
- Stub body: "Phase 7 will implement testing logic here. For now, display: 'Test command ready. Implementation coming in Phase 7.'"
- MCP fallback: when MCP unavailable, output test setup instructions as copy-paste steps

**File 3: `orq-agent/commands/iterate.md`** (requires: full tier)
- Gate: requires "full" tier
- Stub body: "Phase 8 will implement iteration logic here. For now, display: 'Iterate command ready. Implementation coming in Phase 8.'"
- MCP fallback: when MCP unavailable, output iteration guidance as manual steps

**File 4: `orq-agent/commands/set-profile.md`** (no tier gate -- works at all tiers)
- No gate (model profiles apply to all tiers including core)
- Accept argument: quality, balanced, budget, or show current
- Read current profile from `.orq-agent/config.json`
- If argument provided: update `model_profile` in config.json, display confirmation
- If no argument: display current profile and the profile comparison table:

```
Model Profiles:

  | Agent              | quality | balanced | budget |
  |--------------------|---------|----------|--------|
  | architect          | opus    | opus     | sonnet |
  | spec-generator     | opus    | opus     | sonnet |
  | researcher         | opus    | sonnet   | haiku  |
  | tool-resolver      | opus    | sonnet   | sonnet |
  | orch-generator     | opus    | sonnet   | sonnet |
  | dataset-generator  | opus    | sonnet   | haiku  |
  | readme-generator   | sonnet  | sonnet   | haiku  |
  | deployer (Phase 6) | opus    | sonnet   | sonnet |
  | tester (Phase 7)   | opus    | sonnet   | sonnet |
  | iterator (Phase 8) | opus    | opus     | sonnet |

  Current profile: quality
```

- Per user decision: default profile is "quality" (best output out-of-the-box)
- Profile is separate from install flow per user decision

**INST-05 Fallback Logic** (embedded in each V2.0 command gate):
- Base `/orq-agent` generation works identically at all tiers (no changes to orq-agent.md)
- V2.0 commands (deploy/test/iterate) check MCP availability after tier gate passes
- If MCP unavailable: warn user, then produce V1.0-style copy-paste output as fallback
- This ensures deploy+ tier users still get value even without MCP
  </action>
  <verify>
    <automated>for cmd in deploy test iterate set-profile; do test -f "orq-agent/commands/${cmd}.md" && echo "PASS: ${cmd}.md exists" || echo "FAIL: ${cmd}.md missing"; done && grep -q "gate\|Gate" orq-agent/commands/deploy.md && echo "PASS: deploy has gate" && grep -q "gate\|Gate" orq-agent/commands/test.md && echo "PASS: test has gate" && grep -q "gate\|Gate" orq-agent/commands/iterate.md && echo "PASS: iterate has gate"</automated>
    <manual>Each V2.0 command has a gate section checking tier. set-profile shows profile table. All commands reference config.json.</manual>
  </verify>
  <done>4 command files created: deploy/test/iterate with capability gates and MCP fallback, set-profile with profile table. Each gate shows tier comparison table on insufficient tier. Fallback produces V1.0 output when MCP unavailable.</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md with all Phase 5 additions</name>
  <files>orq-agent/SKILL.md</files>
  <action>
Update `orq-agent/SKILL.md` to document all new files and commands added in Phase 5. Read the current SKILL.md first, then add entries for:

**Directory Structure section** -- add these entries in the appropriate locations:

Under `references/`:
```
    agentic-patterns.md            # Phase 5: Anthropic composable patterns + context engineering
    orqai-api-endpoints.md         # Phase 5: REST API endpoint reference for V2.0 subagents
    orqai-evaluator-types.md       # Phase 5: Evaluator taxonomy for automated testing
```

Under `templates/`:
```
    deploy-log.json                # Phase 5: V2.0 deployment audit template (JSON)
    test-results.json              # Phase 5: V2.0 test results template (JSON)
    iteration-log.json             # Phase 5: V2.0 iteration audit template (JSON)
```

Under `commands/`:
```
    deploy.md                      # Phase 5: Deploy to Orq.ai (requires deploy+ tier)
    test.md                        # Phase 5: Automated testing (requires test+ tier)
    iterate.md                     # Phase 5: Prompt iteration (requires full tier)
    set-profile.md                 # Phase 5: Model profile management
```

Add a new section for configuration:
```
  .orq-agent/
    config.json                    # Capability tier + model profile settings
```

**Add a "Capability Tiers" section** after the directory structure explaining:
- core: spec generation only (/orq-agent)
- deploy: + deployment (/orq-agent:deploy)
- test: + automated testing (/orq-agent:test)
- full: + prompt iteration (/orq-agent:iterate)
- Default profile: quality
- Configure: /orq-agent:set-profile

**Add a "V2.0 Commands" section** listing deploy, test, iterate, set-profile with brief descriptions.

Keep SKILL.md as a lightweight index (target ~130-150 lines max).
  </action>
  <verify>
    <automated>grep -q "agentic-patterns" orq-agent/SKILL.md && grep -q "deploy-log" orq-agent/SKILL.md && grep -q "set-profile" orq-agent/SKILL.md && grep -q "config.json" orq-agent/SKILL.md && echo "PASS: all Phase 5 additions documented" && wc -l orq-agent/SKILL.md | awk '{if ($1 < 180) print "PASS: " $1 " lines"; else print "WARNING: " $1 " lines (target < 150)"}'</automated>
    <manual>SKILL.md lists all new references, templates, commands, and config. Capability tiers section explains tier hierarchy.</manual>
  </verify>
  <done>SKILL.md updated with all Phase 5 additions: 3 new references, 3 new templates, 4 new commands, config directory, capability tiers section, V2.0 commands section</done>
</task>

</tasks>

<verification>
- All 4 command files exist in orq-agent/commands/
- deploy, test, iterate commands have gate sections checking tier
- set-profile command shows/updates model profiles
- SKILL.md documents all Phase 5 additions
- No changes to orq-agent/commands/orq-agent.md (base command unchanged at all tiers)
</verification>

<success_criteria>
- Running a V2.0 command at insufficient tier shows upgrade message with tier comparison table per user decision
- MCP fallback produces V1.0 output per INST-05
- Model profiles follow GSD pattern (quality/balanced/budget) per user decision
- SKILL.md is a complete index of all skill files including Phase 5 additions
</success_criteria>

<output>
After completion, create `.planning/phases/05-references-install-and-capability-infrastructure/05-04-SUMMARY.md`
</output>
