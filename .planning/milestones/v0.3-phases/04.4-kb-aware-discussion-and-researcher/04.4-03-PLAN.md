---
phase: 04.4-kb-aware-discussion-and-researcher
plan: 03
type: execute
wave: 2
depends_on:
  - "04.4-01"
files_modified:
  - orq-agent/agents/spec-generator.md
  - orq-agent/agents/readme-generator.md
autonomous: true
requirements:
  - ADV-03

must_haves:
  truths:
    - "Agent specs reference named KBs in their Context section with descriptive names"
    - "Agent specs point to ORCHESTRATION.md for KB setup details"
    - "README includes KB setup steps when the swarm has knowledge base needs"
    - "README defers detailed KB design to ORCHESTRATION.md (no duplication)"
    - "Non-KB agents and non-KB swarms have no KB references in output"
  artifacts:
    - path: "orq-agent/agents/spec-generator.md"
      provides: "KB-aware context section in agent specs"
      contains: "knowledge_id"
    - path: "orq-agent/agents/readme-generator.md"
      provides: "KB setup steps in README output"
      contains: "knowledge base"
  key_links:
    - from: "orq-agent/agents/spec-generator.md"
      to: "agent spec context section"
      via: "knowledge_bases array with descriptive knowledge_id and ORCHESTRATION.md reference"
      pattern: "knowledge_id.*kb"
    - from: "orq-agent/agents/readme-generator.md"
      to: "ORCHESTRATION.md"
      via: "README KB steps reference ORCHESTRATION.md for detailed design"
      pattern: "ORCHESTRATION.md"
---

<objective>
Add KB-aware context references to spec generator output and KB setup steps to README output.

Purpose: Agent specs need to reference their knowledge bases by descriptive name so users know which KBs to connect in Orq.ai Studio. The README needs setup steps for KB creation so non-technical users have a complete guide.

Output: Modified spec-generator.md (KB context references) and readme-generator.md (KB setup steps)
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.4-kb-aware-discussion-and-researcher/04.4-CONTEXT.md
@.planning/phases/04.4-kb-aware-discussion-and-researcher/04.4-RESEARCH.md
@orq-agent/agents/spec-generator.md
@orq-agent/agents/readme-generator.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add KB-aware context references to spec generator</name>
  <files>orq-agent/agents/spec-generator.md</files>
  <action>
Modify the spec generator subagent prompt to produce KB-aware context sections in agent specs.

**What changes:** The spec generator already produces a Context section with `knowledge_bases`, `memory_stores`, and `variables`. For agents where the architect blueprint has `Knowledge base` set to something other than `none`, enhance the `knowledge_bases` array entries:

1. **Use descriptive KB names** (e.g., `product-docs-kb`, `hr-policy-kb`) -- NOT generic names like `kb-1` or `knowledge-base`. The KB name should match the name used by the researcher in the research brief.

2. **Include ORCHESTRATION.md reference** in the description:

```json
{
  "knowledge_bases": [
    {
      "knowledge_id": "product-docs-kb",
      "description": "Product documentation and user guides. See ORCHESTRATION.md KB Design section for setup details."
    }
  ]
}
```

3. **For agents with no KB need** (`Knowledge base: none` in blueprint): The `knowledge_bases` array should be empty or contain only entries already present (e.g., from previous phases). Do NOT add KB references for non-KB agents.

4. **Multiple KBs per agent:** If an agent references multiple knowledge bases (rare but possible), list all of them in the `knowledge_bases` array with descriptive names.

**Where to add in the prompt:** In the section that instructs the spec generator on how to fill the Context section of the agent-spec template. Add guidance that:
- Read the architect blueprint's `Knowledge base` and `KB description` fields for this agent
- Read the researcher's KB Design section (if available) for KB naming
- Use descriptive `knowledge_id` values matching the researcher's KB names
- Always include the ORCHESTRATION.md reference in the description
- For non-KB agents, leave knowledge_bases empty

**Self-validation checklist update:** Add a check: "If agent has Knowledge base != none in blueprint, knowledge_bases array is non-empty with descriptive names referencing ORCHESTRATION.md"
  </action>
  <verify>
    <automated>grep -c "ORCHESTRATION.md.*KB" orq-agent/agents/spec-generator.md | grep -v "^0$"</automated>
    <manual>Check that spec generator includes instructions for descriptive KB names, ORCHESTRATION.md references, and non-KB agent handling</manual>
  </verify>
  <done>Spec generator produces agent specs where KB-needing agents have descriptive knowledge_id values referencing ORCHESTRATION.md for setup details. Non-KB agents have empty knowledge_bases. Self-validation checklist updated with KB check.</done>
</task>

<task type="auto">
  <name>Task 2: Add KB setup steps to README generator</name>
  <files>orq-agent/agents/readme-generator.md</files>
  <action>
Modify the README generator subagent prompt to include KB setup steps when the swarm has knowledge base needs.

**What changes:** The README generator already produces numbered setup steps for non-technical users. When any agent in the swarm has KB needs, add a KB setup section.

**KB setup steps format (added to the numbered setup guide):**

```markdown
### Knowledge Base Setup

Before agents can retrieve information, create the following knowledge base(s) in Orq.ai Studio:

1. **Create knowledge base `[kb-name]`**
   - Navigate to Knowledge Bases in Orq.ai Studio
   - Create a new knowledge base named `[kb-name]`
   - See ORCHESTRATION.md "Knowledge Base Design" section for detailed configuration: source type, document preparation steps, and chunking recommendations

2. **Upload documents to `[kb-name]`**
   - Prepare documents following the preparation steps in ORCHESTRATION.md
   - Upload to the knowledge base in Orq.ai Studio

3. **Connect knowledge base to agent(s)**
   - In each agent's Context settings, add `[kb-name]` as a connected knowledge base
   - Agents using this KB: `[agent-key-1]`, `[agent-key-2]`

Repeat for each knowledge base listed in ORCHESTRATION.md.
```

**Important guidelines:**
- README includes HIGH-LEVEL setup steps only ("Create knowledge base X in Orq.ai Studio with the content described in ORCHESTRATION.md")
- ORCHESTRATION.md has the detailed design (chunking, metadata, document prep) -- do NOT duplicate those details in README
- Only include KB setup section when at least one agent has `Knowledge base != none` in the blueprint
- For non-KB swarms, do NOT include any KB setup section
- Steps should be written for non-technical users with clear, numbered instructions
- Reference ORCHESTRATION.md KB Design section for detailed configuration

**Where to add in the prompt:** In the section that defines the README output structure. Add conditional logic: "If any agent in the swarm has Knowledge base != none in the blueprint, include a Knowledge Base Setup section after the agent setup steps."

**Conditional section rendering:** This follows the existing pattern where single-agent swarms skip orchestration sections. KB setup section is conditional on KB needs.
  </action>
  <verify>
    <automated>grep -c "Knowledge Base Setup" orq-agent/agents/readme-generator.md | grep -v "^0$"</automated>
    <manual>Check that README generator includes conditional KB setup steps referencing ORCHESTRATION.md and follows non-technical user language</manual>
  </verify>
  <done>README generator includes conditional KB setup section with high-level steps for non-technical users. Detailed KB design deferred to ORCHESTRATION.md. Section omitted for non-KB swarms.</done>
</task>

</tasks>

<verification>
1. `grep "ORCHESTRATION.md.*KB" orq-agent/agents/spec-generator.md` shows KB reference pattern in spec generator
2. `grep "Knowledge Base Setup" orq-agent/agents/readme-generator.md` shows conditional KB section in README generator
3. Spec generator uses descriptive KB names (not generic)
4. README KB steps are high-level, referencing ORCHESTRATION.md for details
5. Both files handle the no-KB case (empty knowledge_bases / no KB section)
</verification>

<success_criteria>
- Agent specs for KB-needing agents include descriptive knowledge_id values pointing to ORCHESTRATION.md
- Agent specs for non-KB agents have no spurious KB references
- README includes actionable KB setup steps written for non-technical users
- README does not duplicate ORCHESTRATION.md KB design details
- KB setup section is conditional -- omitted when no KBs needed
</success_criteria>

<output>
After completion, create `.planning/phases/04.4-kb-aware-discussion-and-researcher/04.4-03-SUMMARY.md`
</output>
