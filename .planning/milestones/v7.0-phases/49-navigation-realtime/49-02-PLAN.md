---
phase: 49-navigation-realtime
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - web/app/(dashboard)/swarm/[swarmId]/layout.tsx
  - web/app/(dashboard)/swarm/[swarmId]/page.tsx
  - web/app/(dashboard)/swarm/[swarmId]/not-found.tsx
  - web/components/v7/swarm-layout-shell.tsx
autonomous: true
requirements: [NAV-02, RT-01]

must_haves:
  truths:
    - "Navigating to /swarm/[swarmId] for a swarm the user has access to renders the V7 layout shell with placeholder regions"
    - "Navigating to /swarm/[swarmId] for a swarm the user does NOT have project_members on returns a 404"
    - "SwarmRealtimeProvider wraps the swarm/[swarmId] page tree, so useRealtimeTable works anywhere inside"
    - "Navigating from /swarm/A to /swarm/B tears down the previous Realtime channel and creates a new one (supabase.getChannels() contains no stale swarm:* entries)"
    - "The layout shell shows placeholder regions for briefing, KPIs, fleet, kanban, and terminal, each captioned with the destination phase"
    - "The realtime status indicator renders in the header when the channel status is not SUBSCRIBED"
  artifacts:
    - path: "web/app/(dashboard)/swarm/[swarmId]/layout.tsx"
      provides: "Per-swarm access gate + Realtime provider boundary"
      contains: "SwarmRealtimeProvider"
      min_lines: 25
    - path: "web/app/(dashboard)/swarm/[swarmId]/page.tsx"
      provides: "V7 layout shell entry point"
      contains: "SwarmLayoutShell"
      min_lines: 15
    - path: "web/app/(dashboard)/swarm/[swarmId]/not-found.tsx"
      provides: "V7-styled 404 for inaccessible/non-existent swarms"
      min_lines: 15
    - path: "web/components/v7/swarm-layout-shell.tsx"
      provides: "V7 grid with placeholder regions and status indicator"
      exports: ["SwarmLayoutShell"]
      min_lines: 100
  key_links:
    - from: "web/app/(dashboard)/swarm/[swarmId]/layout.tsx"
      to: "project_members access check"
      via: "supabase count query"
      pattern: "project_members"
    - from: "web/app/(dashboard)/swarm/[swarmId]/layout.tsx"
      to: "SwarmRealtimeProvider"
      via: "children wrap"
      pattern: "<SwarmRealtimeProvider"
    - from: "web/components/v7/swarm-layout-shell.tsx"
      to: "useRealtimeTable + RealtimeStatusIndicator"
      via: "import + invoke"
      pattern: "useRealtimeTable|RealtimeStatusIndicator"
---

<objective>
Deliver the /swarm/[swarmId] route: layout (access gate + Realtime provider), page (V7 layout shell), and V7-styled 404.

Purpose: Completes NAV-02 (navigate by clicking a sidebar item) and locks in RT-01 (provider owns the single channel, tears down on navigation). Produces the visual proof that the V7 grid renders end-to-end.

Output: New route files under `web/app/(dashboard)/swarm/[swarmId]/` and the `SwarmLayoutShell` component that renders placeholder regions where later phases dock real components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-navigation-realtime/49-CONTEXT.md
@.planning/phases/49-navigation-realtime/49-UI-SPEC.md
@.planning/phases/49-navigation-realtime/49-RESEARCH.md
@.planning/phases/49-navigation-realtime/49-01-SUMMARY.md
@web/app/(dashboard)/layout.tsx
@web/components/ui/glass-card.tsx
@web/components/v7/swarm-realtime-provider.tsx
@web/components/v7/realtime-status-indicator.tsx
@web/lib/v7/use-realtime-table.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SwarmLayoutShell component (V7 grid + placeholders + status)</name>
  <files>web/components/v7/swarm-layout-shell.tsx</files>
  <read_first>web/components/ui/glass-card.tsx, web/components/v7/realtime-status-indicator.tsx, web/lib/v7/use-realtime-table.ts, .planning/phases/49-navigation-realtime/49-UI-SPEC.md</read_first>
  <action>Create `web/components/v7/swarm-layout-shell.tsx`:

1. 'use client' directive.
2. Imports: GlassCard from `@/components/ui/glass-card`; RealtimeStatusIndicator from `@/components/v7/realtime-status-indicator`; useRealtimeTable from `@/lib/v7/use-realtime-table`; useContext + SwarmRealtimeContext from `@/components/v7/swarm-realtime-provider` to read status directly.
3. Export `SwarmLayoutShell({ swarmName, swarmDescription })`:
   - Props: swarmName (string), swarmDescription (string | null).
   - Read channel status via `const { status } = useRealtimeTable('jobs');` — any table works since status is shared across the bundle.
   - Return a div flex-col gap-5 p-5 h-screen overflow-auto:
     - Header row: flex justify-between items-start. Left side flex-col:
       - Title: `<h1 className="font-[var(--font-cabinet)] text-[32px] leading-[1.1] font-bold tracking-[-0.03em] text-[var(--v7-text)]">{swarmName}</h1>`.
       - Description: if swarmDescription, `<p className="text-[16px] text-[var(--v7-muted)] mt-1">{swarmDescription}</p>`.
       - Status indicator row: mt-2 flex gap-2 items-center → `<RealtimeStatusIndicator status={status} />`.
     - Briefing row: grid grid-cols-1 lg:grid-cols-[1.4fr_0.8fr] gap-5:
       - `<PlaceholderRegion heading="Briefing" caption="AI narrative — Phase 51" className="min-h-[200px]" />`.
       - `<PlaceholderRegion heading="KPIs" caption="Snapshot grid — Phase 51" className="min-h-[200px]" />`.
     - Fleet row: grid grid-cols-1 md:grid-cols-2 xl:grid-cols-4 gap-4:
       - Four `<PlaceholderRegion heading="Subagent" caption="Agent card — Phase 51" className="min-h-[180px]" />` with headings labeled Fleet 1..4 is unnecessary — one heading "Subagent fleet" caption "Agent cards — Phase 51" across all four.
       - Simpler: wrap all four in a single outer PlaceholderRegion heading "Subagent fleet" caption "Agent cards — Phase 51" that internally shows a 4-cell grid of empty inner divs (ghost placeholders). min-h 180px.
     - Workbench row: grid grid-cols-1 lg:grid-cols-[1.2fr_0.8fr] gap-5:
       - `<PlaceholderRegion heading="Kanban" caption="Job board — Phase 52" className="min-h-[280px]" />`.
       - `<PlaceholderRegion heading="Terminal" caption="Event stream — Phase 52" className="min-h-[280px]" />`.
4. Define `PlaceholderRegion({ heading, caption, className, children })` internal function component:
   - Render `<GlassCard className={cn('p-5 flex flex-col items-center justify-center gap-1 text-center', className)}>`.
   - Heading: 20px Cabinet Grotesk 700 `text-[var(--v7-text)]`.
   - Caption: 12px Satoshi 400 `text-[var(--v7-faint)]`.
   - If children provided, render them beneath caption.
5. Import `cn` from `@/lib/utils`.

Do not import Next.js Link or navigation — the shell is purely presentational.</action>
  <verify>tsc --noEmit; component renders in Storybook-like test harness or dev server at /swarm/[id]</verify>
  <acceptance_criteria>
    - Exports SwarmLayoutShell as default-or-named (named export preferred)
    - Uses GlassCard for placeholder regions
    - Reads status via useRealtimeTable (validates the hook wiring works)
    - Renders at least 5 placeholder regions (briefing, KPIs, fleet, kanban, terminal)
    - Each placeholder labeled with destination phase ("Phase 51" or "Phase 52")
  </acceptance_criteria>
  <done>Shell renders when mounted inside a SwarmRealtimeProvider</done>
</task>

<task type="auto">
  <name>Task 2: /swarm/[swarmId]/layout.tsx — access gate + Realtime provider</name>
  <files>web/app/(dashboard)/swarm/[swarmId]/layout.tsx</files>
  <read_first>web/app/(dashboard)/layout.tsx, web/components/v7/swarm-realtime-provider.tsx, web/lib/supabase/server.ts, .planning/phases/49-navigation-realtime/49-RESEARCH.md</read_first>
  <action>Create `web/app/(dashboard)/swarm/[swarmId]/layout.tsx` as a server component:

1. Imports: notFound from 'next/navigation'; createClient from `@/lib/supabase/server`; SwarmRealtimeProvider from `@/components/v7/swarm-realtime-provider`.
2. Export default async function `SwarmLayout({ params, children }: { params: Promise<{ swarmId: string }>; children: React.ReactNode })`:
   - `const { swarmId } = await params;` (Next.js 16 params is a Promise).
   - `const supabase = await createClient();`
   - `const { data: { user } } = await supabase.auth.getUser();`
   - If !user, call `notFound()` (parent dashboard layout should have redirected to /login already; defensive).
   - `const { count } = await supabase.from('project_members').select('*', { count: 'exact', head: true }).eq('user_id', user.id).eq('project_id', swarmId);`
   - If !count, call `notFound()`.
   - Return `<SwarmRealtimeProvider swarmId={swarmId}>{children}</SwarmRealtimeProvider>`.
3. No 'use client' directive (server component).

Do not wrap with any additional layout chrome — the (dashboard) parent layout already provides the sidebar.</action>
  <verify>tsc --noEmit; visiting /swarm/&lt;accessible-id&gt; → shell renders; /swarm/&lt;inaccessible-id&gt; → Next.js 404</verify>
  <acceptance_criteria>
    - File is an async server component (no "use client")
    - Awaits params before destructuring swarmId
    - Calls notFound() when project_members count is 0
    - Wraps children in <SwarmRealtimeProvider swarmId={swarmId}>
  </acceptance_criteria>
  <done>Unauthorized access 404s; authorized access reaches the provider</done>
</task>

<task type="auto">
  <name>Task 3: /swarm/[swarmId]/page.tsx — fetch swarm metadata and render shell</name>
  <files>web/app/(dashboard)/swarm/[swarmId]/page.tsx</files>
  <read_first>web/lib/supabase/server.ts, web/components/v7/swarm-layout-shell.tsx</read_first>
  <action>Create `web/app/(dashboard)/swarm/[swarmId]/page.tsx`:

1. Server component (no 'use client').
2. Imports: notFound from 'next/navigation'; createClient from `@/lib/supabase/server`; SwarmLayoutShell from `@/components/v7/swarm-layout-shell`.
3. Export default async function `SwarmPage({ params }: { params: Promise<{ swarmId: string }> })`:
   - `const { swarmId } = await params;`
   - Fetch projects row: `const { data: swarm } = await supabase.from('projects').select('id, name, description').eq('id', swarmId).single();`
   - If !swarm, notFound() (should not normally happen since layout already gated, but defensive).
   - Return `<SwarmLayoutShell swarmName={swarm.name} swarmDescription={swarm.description} />`.</action>
  <verify>tsc --noEmit; page renders name + description in the header</verify>
  <acceptance_criteria>
    - Async server component
    - Fetches projects row by swarmId
    - Renders SwarmLayoutShell with swarmName and swarmDescription props
  </acceptance_criteria>
  <done>Swarm name and description visible on page</done>
</task>

<task type="auto">
  <name>Task 4: /swarm/[swarmId]/not-found.tsx — V7-styled 404</name>
  <files>web/app/(dashboard)/swarm/[swarmId]/not-found.tsx</files>
  <read_first>web/components/ui/glass-card.tsx, .planning/phases/49-navigation-realtime/49-UI-SPEC.md</read_first>
  <action>Create `web/app/(dashboard)/swarm/[swarmId]/not-found.tsx`:

1. Server component.
2. Imports: Link from 'next/link'; GlassCard from `@/components/ui/glass-card`.
3. Export default function `SwarmNotFound()`:
   - Return a div h-screen flex items-center justify-center p-5 with a GlassCard p-8 max-w-md text-center flex-col gap-4:
     - Heading: "Swarm not found" (24px Cabinet Grotesk 700 `text-[var(--v7-text)]`).
     - Body: "You may not have access to this swarm, or it no longer exists." (14/400 `text-[var(--v7-muted)]`).
     - Link to `/` with text "Return to dashboard" styled as a button: inline-flex px-4 py-2 rounded-[var(--v7-radius-pill)] bg-[var(--v7-teal-soft)] text-[var(--v7-teal)] text-[14px] font-medium hover:bg-[var(--v7-teal)] hover:text-[var(--v7-inverse)] transition-colors.</action>
  <verify>tsc --noEmit; visiting /swarm/non-existent-id shows the V7 404, not Next.js default</verify>
  <acceptance_criteria>
    - File exists and renders a GlassCard with the specified copy
    - Link to "/" uses next/link
    - Uses --v7-* tokens
  </acceptance_criteria>
  <done>404 is V7-styled</done>
</task>

<task type="auto">
  <name>Task 5: Verification checkpoint — TypeScript + visual smoke + channel lifecycle</name>
  <files>.planning/phases/49-navigation-realtime/49-VERIFICATION.md</files>
  <read_first>.planning/phases/49-navigation-realtime/49-01-SUMMARY.md, .planning/phases/49-navigation-realtime/49-CONTEXT.md</read_first>
  <action>Create `.planning/phases/49-navigation-realtime/49-VERIFICATION.md` with frontmatter `status: passed` (or `gaps_found` / `human_needed` if issues detected during execution).

Verification checks to run and document the outcome of:

1. **TypeScript:** `cd web && npx tsc --noEmit 2>&1 | grep -E "^(web/components/v7|web/lib/v7|web/app/\(dashboard\)/swarm)"` — expect no errors in Phase 49 scope. Pre-existing errors in `debtor-email-analyzer/`, `lib/automations/sales-email-analyzer/`, and `app/api/tools/outlook/archive/route.ts` are known and allowed.

2. **File existence grep:**
   - `ls web/lib/v7/types.ts web/lib/v7/swarm-data.ts web/lib/v7/use-realtime-table.ts`
   - `ls web/components/v7/swarm-realtime-provider.tsx web/components/v7/swarm-sidebar.tsx web/components/v7/swarm-list-item.tsx web/components/v7/sidebar-mini-stat.tsx web/components/v7/realtime-status-indicator.tsx web/components/v7/sidebar-chooser.tsx web/components/v7/swarm-layout-shell.tsx`
   - `ls 'web/app/(dashboard)/swarm/[swarmId]/layout.tsx' 'web/app/(dashboard)/swarm/[swarmId]/page.tsx' 'web/app/(dashboard)/swarm/[swarmId]/not-found.tsx'`

3. **Pattern grep (content verification):**
   - `grep -c "postgres_changes" web/components/v7/swarm-realtime-provider.tsx` → 4
   - `grep -c "removeChannel" web/components/v7/swarm-realtime-provider.tsx` → 1 (at minimum)
   - `grep -c "dashboard:swarms" web/components/v7/swarm-sidebar.tsx` → 1
   - `grep -c "SwarmRealtimeProvider" 'web/app/(dashboard)/swarm/[swarmId]/layout.tsx'` → ≥1
   - `grep -c "notFound" 'web/app/(dashboard)/swarm/[swarmId]/layout.tsx'` → ≥1
   - `grep -c "SidebarChooser" 'web/app/(dashboard)/layout.tsx'` → 1

4. **Must-haves summary:**
   - All 7 truths from 49-01-PLAN and 6 truths from 49-02-PLAN are implemented in code (verifiable via reading the files)
   - All 11 artifacts exist with the claimed exports/contains
   - All 7 key_links are present (grep patterns)

5. **Build check (informational, NOT blocking):**
   - `next build` is known to fail on the pre-existing Zapier SDK `@napi-rs/keyring` Turbopack issue in `web/app/api/tools/outlook/archive/route.ts` (tracked, unrelated to Phase 49). Do not attempt to fix this in Phase 49. Document the known failure in VERIFICATION.md and set status accordingly.

6. **Human verification (deferred to later session):**
   - Full browser-based test requires running `next dev` and navigating between swarms while watching `supabase.getChannels()`. This is optional in autonomous mode; mark as "deferred to human verification" in the VERIFICATION.md if not run.
   - If run: visit /swarm/A → observe single `swarm:A` + `dashboard:swarms` channels. Navigate to /swarm/B → observe `swarm:B` + `dashboard:swarms`, NO stale `swarm:A`.

VERIFICATION.md frontmatter fields:

```yaml
---
phase: 49-navigation-realtime
status: passed | gaps_found | human_needed
checked: 2026-04-16
must_haves_passed: N/total
gaps: []
---
```

Set status to `passed` when all code-level checks pass. Set to `human_needed` if only the browser navigation test remains. Set to `gaps_found` with a `gaps:` list if any must_have artifact is missing or a key_link grep returns 0 matches.</action>
  <verify>File exists with required frontmatter and sections</verify>
  <acceptance_criteria>
    - 49-VERIFICATION.md exists with YAML frontmatter
    - status field is one of: passed, gaps_found, human_needed
    - All grep checks in the "Pattern grep" section are documented with their actual results
  </acceptance_criteria>
  <done>VERIFICATION.md written and honest about what was verified</done>
</task>

</tasks>

<verification>
- [ ] All 4 source files created (shell, layout, page, not-found)
- [ ] VERIFICATION.md written with accurate status
- [ ] `cd web && npx tsc --noEmit` passes for Phase 49 files (pre-existing errors allowed)
</verification>

<success_criteria>
- Route /swarm/[swarmId] works for authorized swarms
- 404 for unauthorized or nonexistent swarms
- V7 layout shell visible with placeholder regions
- VERIFICATION.md status accurately reflects outcome
</success_criteria>

<output>
After completion, create `.planning/phases/49-navigation-realtime/49-02-SUMMARY.md`.
</output>
