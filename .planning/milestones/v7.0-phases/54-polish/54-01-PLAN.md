---
phase: 54
plan: 54-01
title: V7 migration across dashboard, executive, projects, settings
status: ready
created: 2026-04-16
---

# Plan 54-01 — V7 Polish Migration

Single atomic-commit plan that migrates all pre-V7 pages to V7 tokens + GlassCard per 54-UI-SPEC.

## Tasks (atomic commits)

### Task 1 — Upgrade `KpiCard` + `ProjectCard` to GlassCard
- Rewrite `web/components/dashboard/kpi-card.tsx`:
  - Replace `Card/CardHeader/CardContent` with `GlassCard` + simple divs
  - Use V7 token classes per UI-SPEC typography map
  - Preserve props API (`title`, `value`, `trend`, `estimated`, `tooltipText`, `icon`, `updatedAt`, `stale`)
  - `stale` amber border → `border-[var(--v7-amber)]` instead of `amber-300/700`
- Rewrite `web/components/project-card.tsx`:
  - Replace `Card/*` with `GlassCard` + divs
  - Add V7 hover: `hover:-translate-y-[3px] hover:shadow-[var(--v7-glass-shadow-heavy)]`
  - Use V7 muted / faint tokens for metadata

### Task 2 — Migrate home dashboard page
- `web/app/(dashboard)/page.tsx`:
  - Outer `p-5` (was `p-6`)
  - H1 Cabinet Grotesk 32px
  - Description Satoshi 14px muted
  - Replace 3 stat cards (`Card`+nested) with `GlassCard` blocks per UI-SPEC pattern
  - Empty state icon-bg → `bg-[var(--v7-panel-2)]`, H2 Cabinet Grotesk 20px, description muted
  - Remove debug `console.log` block (cleanup)

### Task 3 — Migrate executive dashboard page
- `web/app/(dashboard)/executive/page.tsx`:
  - Outer `p-5` + `space-y-5`
  - H1 Cabinet Grotesk
  - Header description muted
  - 3 error / empty states → GlassCard with V7 tokens
  - H3 section headings inside tabs → Cabinet Grotesk 20px `font-[var(--font-cabinet)]`
- No changes to `KpiGrid`, charts, `SourceStatusCard`, `ProjectHealthTable`, `AgentMetricsTable`, `RoiTable`, `StatusDistributionChart`, `TypeBreakdownChart`, `ActivityChart`, `SuccessRateChart`, `CostTrendChart`

### Task 4 — Migrate projects detail page
- `web/app/(dashboard)/projects/[id]/page.tsx`:
  - Outer `p-5`
  - Breadcrumb link hover `text-[var(--v7-text)]`, span active `text-[var(--v7-text)]`
  - H1 Cabinet Grotesk
  - Description + metadata muted + faint tokens
  - `Members` Card → GlassCard block
  - Empty states (Creations + Swarm Graph) → GlassCard wrappers

### Task 5 — Migrate settings page
- `web/app/(dashboard)/settings/page.tsx`:
  - Outer `p-5`
  - H1 Cabinet Grotesk
  - Description muted
  - Auth-profile grid: each `Card` → `GlassCard`
  - Keep HealthDashboard + CredentialList + SystemList internals untouched

## Verification (local)

- Each page imports successfully (`tsc --noEmit` clean on modified files)
- GlassCard renders with V7 panel bg/border/radius
- H1/H2/H3 use Cabinet Grotesk (visual check — `font-[var(--font-cabinet)]` class present)
- No breaking API changes to `KpiCard` / `ProjectCard` props
- No removed data-fetcher code / server actions / auth guards

## Non-goals

- No changes to chart rendering or data layer
- No new dependencies
- No new tokens — strictly use those defined in 48-UI-SPEC
- No Tabs redesign

## Risks + mitigations

| Risk | Mitigation |
|------|-----------|
| KpiCard API change breaks callers | Keep exported signature identical; only swap internals |
| ProjectCard hover interferes with Link click area | Keep `Link` outer wrapper; hover only lifts inner GlassCard |
| Tabs look wrong on V7 background | Accept in 54 — Tabs redesign deferred; tokens already inherit |
