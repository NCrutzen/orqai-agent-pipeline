# 2026-05-21 — Day Plan + Cross-Machine Handoff

**Owner:** Nick · **Started:** 08:51 CEST · **Mode:** PM-supervised, multi-session
**Purpose:** When this session resumes on another PC, Read this file first.

## Today's 4 Goals (priority order, locked)

| # | Goal | Workspace / branch | Scope |
|---|---|---|---|
| **G1** | Hardened GI workflow for multi-dev | `v8.1-tier-2-cleanup` / `ci/tier-2-pr-checks` | Finish Tier 2 CI: `pr-checks.yml` + GitHub branch protection on `main`. Verify failure modes (codegen drift, broken test, direct push to main). |
| **G4** | Launch info@smeba.nl swarm | NEW workspace needed | Stage 0 + Stage 1 noise + Outlook archive ONLY. Skips Stage 2/3 → sidesteps prereq blockers (Phase 78/85/86). Registry-only: 1 `swarms` row + ~7 `swarm_noise_categories` rows + Outlook ingest mailbox config. |
| **G3** | Demo video for Senior Leadership | (no code workspace) | Script + click-path + curated test emails firing Stage 1→2→3→4 (auto Outlook archive + iController cleanup). Nick records, narrates live. |
| **G2** | Bulk Review UI — info density | `bulk-review-flow-ux` | Visual/layout density refactor of rows + detail pane. Pending todos already done — this is NOT a todo sweep. Start-only acceptable if time short. |

## Day Shape

```
08:51 → ~11:00   G1 — finish Tier 2 CI + branch protection
11:00 → 13:00    G4 — info@smeba.nl Stage 0+1+archive swarm
~14:00 → 15:30   G3 — demo prep (script + test emails)
15:30 → EOD      G2 — Bulk Review density (start; ship-by-EOD soft)
```

Check-in cadence: **at slot boundaries (~every 2h)** — claude pings to confirm progress, scope holding, no drift.

## Critical Context (do not re-derive)

### info@smeba.nl pre-banked work
- **Seed:** `.planning/seeds/SEED-001-info-smeba-routing-swarm-onboarding.md`
- **Proposal:** `docs/designs/2026-05-19-smeba-info-routing-swarm-proposal.md` (5 deliberately-unresolved plan-time questions)
- **Blueprint:** `.claude/skills/spike-findings-agent-workforce/references/info-routing-swarm-phase-88.md`
- **Spike outputs:** `.planning/spikes/00{1,2,3,4}-*` (90d corpus 5505 rows in `email_pipeline.emails`, idempotent)
- **Stats:** 76.9% noise coverage, ~14 emails/day to Stage 3, ~7/day real business email, 56% regex transfer from debtor-email
- **NOT verkoop@smeba.nl** — that's V10.0 sales pipeline, different swarm

### Scope cut rationale (G4)
Stage 0+1+archive only sidesteps SEED-001's prereqs (78/84/85/86) because:
- Phase 78 codegen → only needed for `swarm_intents` (Stage 3). Not adding intents → not needed today.
- Phase 84 `swarms.tenant_domains` → ✓ done
- Phase 85 V3 schema → Stage 3 concern
- Phase 86 discovery surface → Stage 3 router vocabulary concern

### G1 status at session start
- `pr-checks.yml` already committed (e39ca67 on `ci/tier-2-pr-checks`) — has codegen, lint, typecheck, test, build
- `pr-checks.yml` has 1 unstaged modification — finish + commit + PR
- Branch protection on GitHub still TODO (codegen, lint, typecheck, test, build required checks; require PR; auto-delete head branches)
- TODO ref: `.planning/todos/pending/2026-05-20-tier-2-ci-pr-checks-workflow.md`

### Active workspaces (worktrees)
```
~/gsd-workspaces/bulk-review-flow-ux/Agent Workforce          → G2
~/gsd-workspaces/v8.1-tier-2-cleanup/agent-workforce          → G1 (current)
~/gsd-workspaces/milestone-v8.1/Agent Workforce               → v8.1 catch-all
~/gsd-workspaces/phase-82-10-learnings-consolidation/...      → unrelated
```

### Workspace gate reminder (CLAUDE.md)
Phase work MUST NOT run on `main`. Use `/gsd-new-workspace` for G4 — suggested name: `phase-999.9-info-smeba-stage-1-archive`.

## Per-Slot Checkpoint Criteria

**G1 done =** PR open, all 5 checks green on a sample PR, branch protection visible in GH settings, direct push to main rejected.

**G4 done =** swarm row inserted, noise categories seeded, info@smeba.nl in Outlook ingest list, smoke test on 1 live email shows Stage 0 ✓ → Stage 1 verdict ✓ → archive action ✓.

**G3 done =** demo script written, 4-6 test emails identified (one per stage outcome: pure noise auto-archive, debtor query → iController cleanup, multi-intent escalation, manual_review), click-path doc complete, screen recording captured.

**G2 done (today's slice) =** density audit of current Bulk Review screen + proposed layout sketch + 1-2 component edits landed (not full refactor).

## Drift Risks To Watch

1. **G4 scope creep into Stage 2/3** — if tempted, re-read SEED-001 "Why this is a seed, not 'just run it now'" section.
2. **Demo emails that don't exist** — pre-cherry-pick from `email_pipeline.emails` before recording; don't try to manufacture live during recording.
3. **G1 branch protection requires GH UI** — won't gh CLI everything; budget time for the settings page.
4. **Workspace gate on G4** — don't start phase work on `main` or on `ci/tier-2-pr-checks`. New workspace required.

## When Resuming on Another PC

1. `git fetch --all && git pull` on each active workspace branch.
2. Read this file.
3. Check `.planning/STATE.md` for any updates.
4. Ask: "where did we stop in the day shape?"
