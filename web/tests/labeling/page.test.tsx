// Phase 56-00 (D-20, D-22). Dashboard page server-component tests.
// Wave 0 scaffold — page.tsx lands in Wave 3 (56-08). RED until then.

import { describe, it } from "vitest";

describe("Debtor email labeling — page.tsx", () => {
  it.todo("renders RPC counts — calls label_dashboard_counts and shows totals (D-22)");
  it.todo("tree groups by mailbox — list grouped by source_mailbox under each nxt_database");
});
