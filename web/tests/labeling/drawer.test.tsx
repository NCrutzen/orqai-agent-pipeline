// Phase 56-00 (D-23). Drawer realtime broadcast subscription test.
// Wave 0 scaffold — drawer + use-counts-rpc hook land in Wave 3 (56-08).
// Channel name "automations:debtor-email-labeling:stale" is locked by D-23 +
// Phase 59 broadcast contract.

import { describe, it } from "vitest";

describe("Debtor email labeling — drawer + realtime", () => {
  it.todo("broadcast channel — subscribes to automations:debtor-email-labeling:stale on mount");
});
